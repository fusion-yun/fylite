# TODO — 当前任务列表（短期记忆）

★★**与 `PLAN.md` 的分工是项目设定（2026-09-12 用户裁定）**：`PLAN.md` 是**规划与长期记忆**
（每条任务的由来 · 依据 · 落地读数 · 状态；**行只改状态、不删除**）；**本文件只列还开着的那些**，
一条一行，说**下一步做什么**。一条做完就**改 `PLAN.md` 对应行的状态，并把它从本文件删掉** ——
落地记录留在 `PLAN.md`，不留在这里。

- 每条的**事实与依据在 `PLAN.md` 的同号行**，本文件不重复，两处不一致时以 `PLAN.md` 为准。
- **归属是硬约束**：标 `fyo` / `fydata` / `fydoc` 的条目按各该仓的治理落地，列在这里只为知道本仓在等什么。
- 三组的意思：**可做** = 不等人不等件，现在就能动手；**待裁** = 等用户一句口径；
  **阻塞** = 缺仓外材料，或被他仓条目卡住。

---

## 一、可做（19，不等人不等件）

| 号 | 归属 | 下一步做什么 |
| :--- | :--- | :--- |
| **F-30** | kernel | 全文已齐（2026-09-13 Karney–Fisch 1979 · Fisch 1987 · Ignat 1994 期刊版均入库）：**LH-②** 电子 Landau 吸收按内核 `lh-raytracing.md` §4.3 已定的设计动手，判据用摘录里的局地点表 |
| **F-31** | kernel | 余下只有**弱相对论极化**一项，Krivenski–Orefice 1983 与 Shkarofsky 1966 期刊版在库：按原文动手 |
| **F-1** | fylite · kernel | H-19 已关（2026-09-13，整圈 Wb · ABI 154）：`code/discharge` 把一维 p′/FF′（及 q 所需的 F）按整圈 Wb 写进声明的槽，再重跑两步算例看第二步是否过 `profiles_1d/q`；余下 10 条判据随之可评 |
| **I-3** | kernel | 物理接通已落（未提交）；**V3 漂移真因已定**：ONETWO 分析 χ 不带几何因子而推进带 gm3（边缘多传 2.7 倍），χ 须除以 gm3；★旧读数「3 s」实为 0.082 s、归因作废；**芯部链已在给定 LCFS 上跑通**（`pcs_core_chain_on_the_given_lcfs`，8 s 内 W_th 801 → 301 MJ 且加快）；**χ 定标律缩放已落**（`chi_scaling = ipb98` + τ_E 积分锚 τ_c 1 s + 比例项 k_p 3：16 s 处 W_th 793 MJ · P_fus 1472 MW 单调收敛；开环沉底 633 MJ、纯积分欠阻尼）；**PCS-V3 在芯部链上过**（W_th −1.7 % · P_fus −2.5 %，阈 ±5 %）；**βp/lᵢ 进步出口已落**（`globals = 1`，V4：lᵢ(3) 对 LCFS 内截同边缘的 ψ 图积分 0.661/0.661，βp 对驱动逐位；★`surfaces::li3` 偏滤器位形高估 +25 %〔私有通量区〕待裁）；**平衡按节拍重解已接进链（测量）**：0.69–0.97 s/次超预算须后台 · LCFS 未钉（99.5 % 面 rms 0.10 m、轴 +0.11 m、κ −0.09）· q0 16 s 降到 0.42（峰化）· vprime_old 双计缺陷已修；★**时间要求已放宽**（2026-09-13 用户：500 ms/步不作硬门，重解可同步、closure 2 与每节拍全解变为可选，新上限 `[TBD]`）；**持 LCFS 试验一（框边界真空场等通量修正，`hold_boundary`）持不住**：交付轮廓对交付图自身 ψ_b 面就差 6.0 cm（底）；稳定的只有偏移 + 垂直场（交付图 15 → 7.9 cm），加 Z / 径向 / 形状项一到两遍溢出框；行进态上停在 17 cm；余：**用户已选自由边界 + C4 按节拍重解**（2026-09-13；复用 V11 门：外场＝交付图减丝流、设定点＝电流形心，门上 LCFS rms 2.0 cm）——**已接进链**（`PCS_RESOLVE_FREE`）：表在 ψ_N 1 补零后 0.22 s settle，但 LCFS 对给定轮廓仍 9–10 cm、fb_r 3.4 MA；形状环第一版（含位置项的真空场拟合进外场）**更坏**：每解 3000 轮不 settle、轮廓差 11 → 24 cm、35–42 s；扫描否定「与 C4 抢位置」：只拟合形状项同样不 settle（13–21 cm），小步位置项 settle 但不降（11 → 12 cm）⇒ 直接加进外场的形状环无效，缺省关；C4 单独 9–10 cm 为现最好；余：**测响应矩阵再岭最小二乘**（用户 2026-09-13 已选；`code/discharge` 已有的做法）：第一版首遍 11.2 → 9.2 cm（首个有效修正）但其后发散、21–25 s/次；加单调接受并扫三组参数后最好只比 C4 单独好约 1 cm（9.1 / 8.5 cm）而墙钟 2–64 s ⇒ 响应环缺省关，持 LCFS 以 C4 单独为准（9–10 cm；底 ~6 cm）；加密到 129×129 更坏（65 上调的 C4 增益在 129 上交付源都不 settle、链上 11.9 cm、53 s）⇒ **持 LCFS 线收住：C4 单独 · 65×65 · 0.22 s · 9–10 cm** · 锯齿/q0 · `surfaces::li3` 私有通量区高估待裁 · V2 分步等价 · 换算后 V3 定阈 · **V2 分步等价（2026-09-14 已量）**：梯子档上分块续跑与整跑不逐位同，差舍入级不放大（40×1 对 1×40 最大 3.2e-15 相对），`code/evolve` 在 Miller 上逐开关逐位同 ⇒ ~~待裁：保持逐位或改阈 ≤ 1e-14 相对~~ ★**已裁 2026-09-14（用户）：改阈 ≤ 1e-14 相对，已落**——门 `a_resumed_march_is_the_same_march_under_the_pcs_opt_ins` 按节点相对差判（`V2_REL_TOL`）；芯部链加 `PCS_V2_DUMP` / `PCS_V2_AGAINST` 两分块对判：40×1 对 1×40 位不同 141/209、**最大相对 3.5e-15 ≤ 1e-14，PCS-V2 过** · 锯齿钳 q0 已量（`PCS_SAWTOOTH`：推进 q0 恒 1.000，P_fus / W_th 在 V3 带内）|
| **I-5** | fydoc · kernel · fylite | 回放文件已落；卡片逐线圈上限（`fylite:i_max_aturn`）与 `solver_dims` 65×65 已补（2026-09-14）；~~余：哪个门从卡片读线圈上限随 F-9 裁定~~ ★**F-9 已裁 2026-09-14（用户）：`code/discharge` 缺省读卡片，已落**（`fold_card_limits`：`pf_active/coil/fylite:i_max_aturn` 按元件匝比 / 通道权重折成通道上限；算例绑 `i_max_aturn` 仍优先；卡片与 `pf_active/supply` 同在时逐通道取较紧者；部分线圈无额定则拒；`card_limits = 0` 不读卡片；判据 `the_card_coil_ratings_fold_into_channel_limits`）· `code/discharge` 在 CFEDR 卡上起盒但塌陷（归 I-14）· 卡片落点随 facts / O-5 定 |
| **I-14** | kernel | **磁场模型第一步**：种子规则与塌陷选优已落（未提交），但门里第 1 遍线圈电流步在所有种子 / 反馈姿态下都塌（缺径向 / 垂直位置稳定化）；**路线 A + 位置控制 C4 已能给出贴近目标的 CFEDR 偏滤器平衡**（`code/discharge` `position_control = c4`：κ 1.88–1.92、分离面距 0.29 m、5–6 s）；下一步：设定点随遍修正以消 R0 偏内 0.33 m（**2026-09-14 代码已落**：`pc_track_r0` 增益，每遍按目标 R0 − 实现 R0 平移 C4 设定点，缺省 0 逐位不动；V13：增益 1.0 时 R0 7.871 到位但 κ 1.56、丢 X 点成限制器；增益 0.5 R0 7.73 · κ 1.73 · 距 0.265 m 亦限制器；跟踪时 6 路线圈贴上限；**无上限对照仍丢 X 点（增益 0.5：κ 1.65）⇒ 非线圈所限，外推形心与形状目标相争，此法判无效**，R0 偏内另寻）· **热启单拍已量**：仅交接安匝 0.613 s（176 轮，形状保住、下 X 点）⇒ **ψ 热启已落**（`psi_warm` 槽，摘要 612dd239）：0.531 s · 163 轮（省 20 %，形状同），余量在 C4 重新 settle · **固定下 X 点已试**：零点行（x_weight 1 / 10）是软约束，X 点离钉点 0.21 / 0.31 m，加 R0 跟踪后形状最近目标（δ 0.55/0.54 · 距 0.238 m）却成限制器 ⇒ 偏滤器 · R0 · 形状尚不可同得，需退火目标加硬约束 · **硬约束已落**（`x_hard` / `r0_hard`，`linalg::ridge_lstsq_eq`，缺省关）：**线性化修正里等式成立、结果里不成立**——X 点离钉点无上限 0.37 m、gamma 1 时 0.11 m，带上限第 2 遍塌、后遍无解；R0 仍 7.54 未动 ⇒ 须把约束放进非线性解或把等离子体响应并入修正矩阵 · **C4 控制器热启已落**（`c4_warm` + `c4_*_in` / `c4_*_out`）：安匝 + ψ + 控制器 0.495 s · 149 轮（从静止 0.609 s · 163 轮）· 热启单拍 ≤ 400 ms 未达 · **零点入解内已落**（`x_ctl`，`equilibrium::NullControl`，只在本轮 X 点离钉点 < 0.5 m 时接入，否则起步解塌）：X 点离钉点 0.06（无上限，偏滤器）/ 0.12 m（带上限，但成限制器），R0 7.745 · 加 R0 跟踪 shape_error 0.085 全场最小 ⇒ 固定下 X 点在上限内仍未成（缺 X 点界定）· **第 2 遍塌陷已查**：a 0.67 m 刚过 ¼ 判据被接受；`collapse_frac = 0.5` 消掉它但后遍仍跳、结果不更好 ⇒ 疑为退火无接受门 · **接受门已试**（`accept_gate`）：第 2 遍后每遍连 1/8 步都不优、停在 gap 0.50 m 限制器，比无门更差 ⇒ 外层修正（仅线圈真空响应）连小步都不是 gap 下降方向；**实测响应矩阵已试**（`response = fd`，每遍 15 路热启全解）：gap 单调降 0.707 → 0.381 m、零减半零塌陷 ⇒ 跳动根因确是矩阵缺等离子体响应；但终态限制器 · κ 1.59、81–89 s/次，形状不如 C4 基线；实测矩阵上加重（x_weight 10）/ 硬化（x_hard）零点行也已试：仍限制器、且丢单调 ⇒ **六种退火改法无一在线圈上限内同得偏滤器 · R0 · 形状**，最好仍是 C4 形心单独；上限内可达性待 I-19 参考线圈电流对照 · ★**已裁 2026-09-14（用户）：收在 C4 形心单独**（偏滤器 · κ 1.83 · 距 0.313 m · R0 偏内 0.28 m 照报；热启用安匝 + psi_warm + c4_warm），可达性随 I-19 重开；路线 B `stage = hold` 不收敛，留作实验件 |
| **H-22** | kernel · fylite | 外环已成环（#137985 第 3 轮收敛，dq0_rel 9.8e-5）；余判据 ①：给分步运行接 `--resume-from`，验「N 轮一次跑完 ≡ k 轮 + 续 N−k 轮」逐记录相同；另记未收敛时顶层 `run_state` 仍为 `succeeded` 的口径 |
| **H-23** | kernel | `code/psi_points` 已落（6 判据过）；余 ②：在有剖面诊断几何的炮上把 s1 的产出接给 s1b（随 H-36） |
| **H-25** | kernel | 合成孪生：`code/forward` 出平衡与磁测 + 合成 Thomson 点 → 跑链 → 逐项报 s0 / s3 离真值多远 |
| **H-26** | kernel | 双点模型开门，`separatrix_align` 的 `te_sep` 由绑定给出；与 C 接口路径逐位相同 |
| **H-27** | kernel | 压强拼合一步（nₑTₑ + ΣnᵢTᵢ + p_fast，分量出处进记录）；无快离子时与今天逐位相同 |
| **H-28** | kernel · fylite | 电流约束 j_bs + j_NBI + j_ECCD，s2 改绑拟合剖面；同批改 `code/rf_ray` 过期自述；先核 `current_source` 取的是哪一列 j_bs |
| **H-29** | kernel · fylite | s0 / s3 记录带 `psi_residual` 判据实例与逐诊断 `ComparisonFinding`，读数与 H-14a 判据逐位相同 |
| **H-30** | fylite · kernel | s0 ↔ s3 的 `ComparisonRecord`（β_N · lᵢ(3) · q₀ · q₉₅ · 磁轴），q = 1 面那条写 `unevaluated` |
| **H-33** | fylite | MCP `fylite_run` 收计划，三面解析出的计划逐字段相同 |
| **H-35** | fylite | 余 ③：记录出处写炮号与所解析的 provider（①② 已落；选项 2「按炮号 + 显式选择出卡」已落，计划钉 `variants/efit_w_pf` 变体卡片 `?shot=137985`）；`read_rules` 四种算子的真正施加另起 |
| **H-36** | kernel · fylite · fydoc | #70754 核心集已实取（38 探针 · 35 环 · Ip · 16 PF）；下一步：以 `pcs` provider 生成一张 EAST 卡片（探针 / 环几何与 PCS 38 / 35 同序）并定 PF 16 路 → 通道，才能跑 s0 对 `efit_east`（W 84.27 kJ · β_p 0.553 · q₉₅ 4.57 @ 4.06 s） |
| **H-37** | kernel · fydoc | EFIT 对拍卡在探针：`efit`（76 道，EXPMPI 序）卡片上一加 `FWTMP2` 就丢等离子体（连 EFIT 自拟合的 CMPR2 也是）——先核 fydoc `efit` provider 探针取向 / 符号对 EXPMPI 约定；纯环已收敛，轴 R 差 8 mm、q 低 16–28 %（跨度大 23 %） |
| **H-38** | kernel · fylite · fydoc | 彻底移除 est2：fydoc CASE-22 已归档（并行会话）· 内核 est2 工具 / 计划 / 示例已 `git mv` 进 `archive/`、B-06 已标 retired（公开册只手补该条）；余：`tests/data/east` + CASE-22 镜像归档（等 W4 公开跳过）· `mds-shot-to-fyo.py` 去留（等 W4 拆分裁定）· 归档同一步把 B-06 输入路径改指 `archive/`（不豁免） · efit_east #137985 算例 + 计划改绑（随 H-37） |

〔2026-09-12〕**本组曾空过**（当日 F-16 · F-34 · H-10 会话半 · F-1 门 · H-15 两项做完后），随后用户裁定「fylite 不作为本体前缀」，开出 O-5（2026-09-13 移入待裁：等 `FYO-ADR-16` 批准）。原记：F-16 · F-34 · H-10（会话文档那一半）· F-1（门）· H-15（1.4 与 1.2 的算法）当日做完并推送，各自的落地读数在 `PLAN.md` 同号行。余下的每一条都在**等一句裁定**或**缺仓外材料** —— 见下两组。〔2026-09-13〕H-19 已裁已关，F-1 因此回到本组。

## 二、待裁（6 行 · 7 个号，等用户一句口径）

| 号 | 归属 | 要裁的是什么 |
| :--- | :--- | :--- |
| **O-5** | fyo · kernel · fylite | **批不批 `FYO-ADR-16`**（fyo，Proposed）：09-12 裁定的 fyo 侧 2026-09-13 才真正落地（D-1 十槽已落 schema，两门俱绿）；D-2 把 B 表改判六处（斜角是几何换算 · `tf` 用 `b_field_phi_vacuum_r` · 离子 `name` · 限制器改端口 · LH 最大功率无承载 · 两处待核），D-3 取代 `FYO-ADR-14` D-2 两项。批准后按判定迁 B 类（K-2 第二批随之）。★同批实测的两处接缝不等批准：内核 `CORE_TRANSPORT` 的 `psin` 写作 `profiles_1d/grid/psi_norm`，而输运模型剖面只有 `grid_d` / `grid_v` / `grid_flux`（内核路径父段错）；K-2 新加的 `pf_active/coil/function/name` 在 fyo 的 `pf_active` 提升里解析不到（fyo 侧待核是否漏提） |
| **F-2** | fylite | 生成件里手写段活不过下一次 `--write`：三个候选落点（算例声明的 `caveat` · 本册子 RUN 页 · 渲染器认锚点）先定一个 |
| **F-9** | fylite · fydoc | `code/breakdown` 缺供电电流上限：EAST 已由 K-2 带 DD `pf_active/supply`；ITER 等其余装置补在装置描述侧（A-Box 没有），还是算例侧绑 `i_max_aturn` · ★**2026-09-14 用户裁定一半**：卡片线圈额定 `pf_active/coil/fylite:i_max_aturn` 由 **`code/discharge` 缺省读**（已落，`fold_card_limits`）；`code/breakdown` 仍只折 `pf_active/supply`，它读不读卡片**仍待裁** |
| **F-12** | fylite | `B-01` 的参考侧要不要改在 FUSE 1.1.5 上重跑（现有结论建立在已遗弃的 0.7.0 冻结答案上）|
| **G-3** · **G-4** | fylite | **记录由谁产**：把运行时的 `record()` 在页面上镜像一份，还是判定记录只由宿主产、页面只存宿主交来的那一份 —— 同时决定 `run_state` 的 `cancelled` 谁产、断点仓存什么 |
| **H-34** | kernel | T-C43 的 `FY_CONDIN` 收紧翻不翻缺省（q₀ 缺口 −47.1 % → +2.6 %，代价 dR −17.3 mm 出 R-1） |

## 三、阻塞（17 行 · 18 个号，缺件或等他仓）

| 号 | 归属 | 缺什么 |
| :--- | :--- | :--- |
| **K-2** | kernel · fydoc · fyo | 第一批已落（2026-09-13，接口修订 4 → 5；内核 71c7cef · 公开 415c4ac）。**第二批**（解算盒四边与网格数六条 → 计划设置）随 O-5 B 批，等 **O-5**（`FYO-ADR-16` 批准）；另 fyo 侧核 `pf_active/coil/function` 是否漏提 |
| **H-1** | kernel · fylite | 计划已落 `fylite_kernel/docs/cases/plans/kinetic-reconstruction.fyo.jsonld`，外环判据 2026-09-13 已裁（`dq0_rel` < 0.01 · 6 轮）；验收「页面与 `fy run` 读同一份字节」卡在运行时认 `has_occurrent_part` / `{id: "<步>/<ids>"}` 而不认 `has_step` / `x+run://`，页面不读计划（**H-2**）；设计书落点注待并行会话的 `FYL-SDD-07` 那批落地后补 |
| **H-15**（2.3） | fylite · fydoc | MSE 的 Er 修正：装置卷宗里**没有 MSE 几何**（`-12` G-4），也没有绑定表，所以既做不出行也量不了 —— 与 H-16 同一个缺口 |
| **H-10** | fylite | **会话文档那一半已完成**（闸 `validate-setting-is-the-document.mjs`，141 个档位）；计划文档落点 2026-09-13 已定（H-1），余下「写回**计划**文档」卡在运行时 / 页面还不读 `has_step` 形的计划（H-2） |
| 台基 **B** · **C** | kernel | EPED 自己的判据原文（B 的外环、C 的真 P-B 都要它）|
| **K-3** | kernel · fydata | 要 A-Box 的 `transport/*` 逐槽读数才判得动 |
| **E-2** | 需数据访问 | 本炮的加热 / 驱动 / 杂质 / 输运只在 MDSplus（`mds.invalid`，内网地址不入仓）|
| **E-6** | fydoc | 16 组 reviewer 与联系方式（**不可编造**，闸子红是正确结果）|
| **E-7** | 需数据访问 | 那份 g-file 不在任何仓里（两个平衡储能差 4.00 倍，无从判哪份对）|
| **F-8** | 可发信 | 向作者索取 GENE 那批次的 ν* 与逐半径 Miller 参数 —— 阻塞里唯一只差一封信的 |
| **F-14** | fylite | 该炮的线平均道比值（判 c4 落在 3–6.67× 的六片算不算条纹跳），依赖 **E-2** |
| **F-19** | fydoc | 七台自带装置的线圈电流上限（铭牌值）；EAST 现有的是 TokSys 的**供电**端子限值（K-2 进 `pf_active/supply`），不是线圈铭牌 |
| **F-20** | fylite · fydoc | 第二台带实测放电的真机（现在只有 EAST #137985）|
| **F-21** | kernel | 第二个构建环境（跨环境 wasm 字节不可复现，本会话无从复现）|
| **G-6** | kernel · fylite | **G-5 已解**（`../spo` 在，2026-09-13 两份 fyo ADR 已据以落 schema）、装置卡片在（内核 `machine_desc/` 七台）；余下 `coverage/` 的 `covers` 边要人逐条判断——`surface/` · `availability/` 可先算，但单出不成套（`PLAN.md` G-6） |
| **I-19** | 设计方 | 磁场模型材料：PF/CS 电源电压上限与回路参数 · 被动结构几何与电阻（fydoc G-7）· 磁测位置 · 平衡篇复核时刻的线圈电流 · **CASE-20 所用 EFIT 线圈几何与 `ccbrsp` 通道序** |
| **I-20** | fylite · kernel | **步长与调用预算**（PCS-W16）：会话每 10 ms 一次调用 295 ms、一次调用走 10 步也 297 ms ⇒ 耗时是调用开销；放开步长（交换上限 ~52 ms）10.27 s 只要 362 ms、末态差 ≤ 0.6 %。规划 A 分窗批量 · B 自适应步长 + 事件小步 · C 砍单次调用开销（缓存梯子 / 常驻推进）；判据 PCS-V15（对 I-10 10 ms 全程 ≤ 1 %，燃烧段墙钟 ≤ 60 s）· 分项 I-20a 基线与比对件 → b 分窗批量（应逐位同基线，燃烧段 ≈ 26 s）→ c 自适应步长 + 事件重走 + 步长减半对照（≈ 6 s）· d 内核逐步迹补 W_th / n_e / β_p / l_i · e 单次调用 ≤ 50 ms（缓存梯子 · 常驻推进）· f 推测执行可选，详见 PLAN.md「I-20 细化：自适应时间步」 |
| **I-21** | fylite · kernel | **多步计算快照 · 暂停 · 恢复**（PCS-W17，先以时间演化为例）：按时间片推进、片界留快照可暂停 / 恢复 / 分叉；**重要节点（状态改变）前保留快照**——回放节点 · 指令跳变 · 位形切换 · 平衡节拍 · 弹丸在节点前一步留，L-H 翻转 · 锯齿 · 塌陷 · 内核拒绝由滚动快照回溯提升；快照 = 自足续跑文档 + 驱动游标 + 出处。分项 a 续跑集合由内核声明 → b 快照格式与往返 → c 时间片驱动 → d `session.pause / resume / fork` → e 与 I-20 分窗合流 · f 推广到退火 / 节拍（后续）。判据 PCS-V16：恢复 ≡ 不中断（逐位）。依据：I-10 下降段 6172.99 s 崩溃只能从 6150 s 重跑；会话两次漏交状态。详见 PLAN.md「I-21 细化」 |
| **H-24** | fydoc · 需数据访问 | #137985 计划片上无可用剖面：唯一 Thomson 在 3.019 s 且判 `divergent`，4.0 s 要 MDSplus，而本地服务器没有这一炮（只有 70754 / 70745 / 38300）；EAST 实测线已转 H-36 |
