# TODO — 仓级开放任务台账

口径日期 **2026-08-29（结构整理：已关条目只留去处，流水账归位）**；
上一次全文重写 2026-08-26（当日增 S- 序）。范围为**整仓**：物理与页面
（`T-` 序）、架构对齐（`A-` 序，正本 `FYL-REPORT-03` §8）、BYOK 与身份（`G-`/`R2-`
序，正本 `FYL-REPORT-01` §14.6 / `FYL-REPORT-02` §5）、场景语料与交互页（`S-` 序，
正本 `FYL-REPORT-03` §10）。此前它只管 `app/`。

★**这本册子只记「还没做完的」与「仍然管着后来人的」。** 已关条目在表里留一行记
去处，正文不留段落——它的三样东西各有归宿，在别处比在这里更容易被找到：

| 落地的东西 | 去哪 |
|:---|:---|
| **能力本身**（现在能算什么、凭什么信） | `FEATURE.md` §1——落地的功能**写进功能表**，不是在待办表里划掉 |
| **哪天关的、当时量到多少、被推翻的说法** | `changelog.md`（旧文原样在 `git log -p -- TODO.md`） |
| **对外码量到多少、判据从哪来** | `docs/note/benchmark/`（登记册 + 报告），机器读的是 `benchmark/registry.jsonld` |
| **仍然管着后来人的规矩与裁定** | 本文件 §2 |

★沿用既有规矩：每条写明**缺的是模型、判据、数据还是机制**，并给出**关闭判据**——
没有关闭判据的待办，与一句抱怨在使用上没有区别。

★**一本台账，判据可有正本。** `A-`/`G-`/`R2-` 各条的**判据正本在报告里**（逐条给了
出处），本表是**唯一排期面**；关一条时报告的条目与本表**同批**更新。两处台账记同一份
待办的风险（旧 §12.4 记过）就此收口：排期只此一处。

---

## 1. 汇总

**未关 29 条**（半关按开计，各注明开着的是哪半）；**本批 +5**（T-C37..T-C41，★其中 **T-C37 与 T-C42 均于 2026-08-31 当日开当日关**，并留下 **T-C43**（反演芯部电流分布）——净数不变，
2026-08-31 由 **fywork CASE-09 跨码对拍**回灌——那是本仓**首次作为被验对象**被一个
独立消费者复核，十 target / 61 判定行 / 0 未过）。

★★**这五条没有一条是「跑得更细」发现的**，全部来自**另一个仓拿本仓记下的数、
回到它所指的原始产物上重算一遍**。它们指向同一件事：
**一份判据要能被复算，必须连同它的「产物 / 入口 / 行 / 掩膜」一起被引用。**

★〔顺带记一处本表自己的账，先记不改〕按行扫描（非删除线的条目）得 **27 条未关**，
而上一行的表头此前写 24——差 3。可能是分类表与正文表的口径差（半关计法/跨表重复），
**未在本批就地改**：改一个自己都没核过的计数，与它错着没有区别。要核时用
`grep -oE "^\| (~~)?\*\*[A-Z]" 逐表点算。

| 序 | 开 | 半关 | 已关（留行记去处） |
|:---|---:|---:|:---|
| **T-** 物理与页面 | 20 | 1（T-C23） | T-C1′ · T-C22 · T-C28（内核侧） |
| **A-** 架构对齐 | 2 | — | A-1 · A-3 · A-4 · A-5 · A-7 |
| **G-/R2-** BYOK 与身份 | 3 | — | — |
| **S-** 场景语料与交互页 | 1（S-3） | 3（S-1 · S-2 · S-2c，**S-2c 下一优先**） | S-2a · S-2b · S-2d |

★T-C24 虽仍有行，**余下的只是去处**——机制已落地，剩的边界源谱记在 T-C25 名下
（§3.5），不重复排期

### 1.1 物理与页面（17 开 / 3 已关，正文 §3）

| # | 页 | 事项 | 缺的是 | 挡在哪 |
|:---|:---|:---|:---|:---|
| **T-D5′** | 设计 | 内置四台机器的工程限值还是空的 | 数据（换算） | 换算式要人核对 |
| **T-D19** | 设计 | `validate-design` 的「限制器 bit-identical」红着 | **一次复现** | **本机复现不出**（含原记为红的基线 `3f0f3c3` 本身，ALL GREEN）；页面状态泄漏与闸子起跑竞态各被一次实测排除；判据已改成失败时连输入一起报 |
| **T-A6** | 反演 | 涡流组电流：真实炮的可辨识 | 数据（通道标定） | 等一炮带逐道标定权重的 |
| **T-A7** | 反演 | Thomson 本炮不可用 | 数据 | 等另一炮或另一套 TS 归约 |
| **T-A8** | 反演 | MSE / SXR 层析几何不在任何卷宗里 | 装置数据 | 等相机几何 |
| ~~**T-C1′**~~ | 三页 | ~~FUSE 对拍~~ | — | **关（2026-08-29）**。数与账：`docs/note/benchmark/` 的 B-01 · `docs/note/fuse-cases.md` §8；前提三条 §2.4；能力见 `FEATURE.md`
| **T-C5′** | 三页 | 线上那一页：闸子已就位，**只差按下发布** | **一次外向决定** | 手动触发是有意的裁定 |
| **T-C15** | 设计 | 壁与偏滤器热流（**模型已换成 extended Lengyel**，仍待判据） | **判据** | 等外部算例（SOLPS 类）。★2026-08-28：JINTRAC 5 MA 算例给了靶板 Te/Ti/ne 与 Pout/Pin/Prad 三通道，已收进语料——**但源里那一档 INCOMPLETE**，所以它是候选不是判据。★★2026-08-30 合账（TX-4）：模型侧已由 `edge.rs` 的 extended Lengyel 供给（Body 2025，正反双向 + 边缘原子数据，V-09..V-13 五条记录、最劣 1.3e-13），回灌路 `scenario/model/edge.py` 已落。**本行的缺口不变，仍是判据**——V-09..V-13 全是**验证**（我们和 TORAX 算得一样），不是**确认**（该信这个模型）。两者按 `docs/note/benchmark/spec.md` §1 不可互替 |
| **T-C17** | 建模 | 聚变反应道只有 D-T 麦氏一条 | **数据** | 本机无全文库 · doi.org/iopscience 403——系数拿不到就不写 |
| **T-C18** | 建模 | 波—等离子体没有射线追踪档 | **模型**（判据现成） | 不挡——`tau` 的接口内核里已留好 |
| **T-C20** | 建模 | 杂质：接线已落；余下电离态与真的杂质源模型 | **模型** | 等外部算例。★2026-08-28：JINTRAC 那一侧对应 SANCO（多电荷态输运 + 辐射），算例矩阵点了名但**文档里没有逐核素的系数或剖面**，所以仍然只是知道要找什么 |
| **T-C21** | 建模 | 处方输运回放的对拍口 | **一次数据搬运** | 站点数据要先冻成派生表；★前置 T-C22 的对外变换层（〔三〕）**已到位**，剩下的确是那次搬运 |
| ~~**T-C22**~~ | 三页 | ~~COCOS 只写在一行注释里，没有判据~~ | — | **三段全关（2026-08-26）**：符号被证明 · 约定靠量不靠信且已接进活读入路径（`fyo.equilibrium(path)` 量不出即按名拒绝）· 对外交换有显式变换层。常设规矩见 §2.3
| **T-C23** | 建模 | 守恒误差账（`jetto.chksum` 的对应件） | **机制** | **半关**：Python/CLI 侧已关（全语料最差 9.8e-13，验收带按实测定）；会话文件那一路随 web 面 |
| **T-C24** | 建模 | 芯部中性与电荷交换（声明已落，模型待判据） | **模型** | 等外部算例 |
| **T-C25** | 建模 | **主离子再循环与粒子排出闭合**（等离子体—壁粒子回路：电离 / CX / 复合 / 再循环） | **模型** | ★2026-08-28 与 JINTRAC 算例矩阵逐条比时才发现缺口册**漏了**这一行。**不能并进 T-C24**：那一条是**芯部**中性与 CX 汇（上游 FRANTIC 一类），这一条是**边界**的回路（上游 EIRENE）。★2026-08-28：芯侧 oracle 已在手（101612 jsp 的 EIRENE 列，见 T-C24 注记与可行性评估笔记）；边界回路那一半照旧挡在模型。也不能并进 T-C20：那里的「壁再循环」说的是**杂质**的。判据候选已收进语料（`evolve-jintrac-iter-5ma-lmode` 带靶板与功率通道表值），★但**那一档源里自己没跑完**（EDGE2D 撞最小时间步），表值是**一帧快照**不是稳态解——够当量级带，不够当验收 |
| **T-C26** | 建模 | **二维 SOL / 偏滤器流体输运**（梯子在台基顶停下，往外一段没有） | **模型 + 判据** | ★2026-08-28 补行：`FEATURE.md` §3.4「台基内部剖面 / SOL」一直有声明，**待办册里却没有对应行**。与 T-C15 分开：那一条是**热流**（落到壁上的量），这一条是**SOL 里的输运本身**。上游那一侧是 EDGE2D。等外部算例（SOLPS / EDGE2D 类） |
| **T-C27** | 建模 | **规定道**：把某一道钉在给定的时序上（解另一道，这一道照喂） | **机制**（判据现成） | ★2026-08-28 复现 JINTRAC 作业 102530 平顶段时照出来的（`docs/note/jintrac-flattop-reproduction.md`）。参考侧只解**电流与电子温度**两条方程，T_i 与 n_e 由 ex-file **给定**（`EquationsPanel.ionTemp.select : false`；`jetto.in` `IEXTE/IEXTI/IEXNE/IEXQ = 1`）——实测佐证：窗口内没解密度方程而 n_e(0) 涨了 16.3 %。**本仓没有这个模式**，于是本仓的 T_i 是预测、参考的 T_i 是输入，两个数不是同一类东西，对照只剩 T_e 一道。★★也照出 `cases --plan` 的一个边界：这一条**报不出来**，因为页面上没有对应控件、`ENTRY_SCOPE` 也就没有对应行——`--plan` 的缺口报告是**下界**。★**不等外部东西**：判据现成（那份运行自己的 IDS 就是要钉的时序），缺的是机制。★★2026-08-28 对标侧已用**逐块驱动**满足：续算对本来就是为宿主编排设计的，每块把 ne / Ti / 度规 / 边界钉回参考迹线（`tests/test_jintrac_flattop.py` 的最终行进门，Te 全剖面 6.91 %、W_th −0.83 %）。**机制行保留**：内核内的规定道模式（单块内逐步跟迹线）仍未有，逐块规定的时间分辨率受块长限制 |
| ~~**T-C29**~~ | 建模 | ~~内核交出逐 ky 通量谱~~ | — | **关（2026-08-29，当日开当日关）**：内核**早就有** `FY_DUMP_FLUX` 逐 ky 转储，只缺次主模的两列应力（已补，不动 ABI）。★产出两件：**①第三个移植缺陷**——ky 网格在 `SAT_RULE ≥ 2` 下整体错 `1/grad_r0`（8 %），修后七面能流 1–7 % → **0.1–5 %**（V-01 报告已记）；**②动量的归因已定**——应力权重在离子尺度（ky ≤ 1.73）吻合 **1 %**，分歧全在**电子尺度**（ky ≥ 2.7，承载 95 % 信号，那里增长率也差 1.77×）：**不是应力的缺陷**。下一步见 T-C30 |
| ~~**T-C30**~~ | 建模 | ~~高 ky（电子尺度）本征解与上游分歧~~ | — | **关（2026-08-29，当日开当日关）**：根因是**第四个移植缺陷**——极化里的 **Debye 屏蔽**（`debye_factor·b0x·(ky·DEBYE)²`）本仓**没有承载体**，`DEBYE` 从 deck 到内核整条通路不存在，内核一直收到 0。该项**随 ky 平方增长**：ky=0.11 处占极化 1e-4、ky=26 处达 **4.6**（大于极化本身）——正是「低 ky 吻合 0.4 %、高 ky 差 77 %」那条指纹。接线后**17 个 ky 的增长率全部吻合到 1e-3 以内**，能流与粒子流收进 ~1 %。★★**但动量没有被治好**（0.28/0.63/−0.18/−29.2/+1.56）——见 T-C31 |
| ~~**T-C31**~~ | 建模 | ~~环向应力权重本身~~ | — | **关（2026-08-29，当日开当日关）——★★真因是我自己比较时的设置错配。** 这条线拿来当参照的 `tests/data/tglf_momentum_jintrac.json` 录在 deck 自身缺省下 = **SAT_RULE=0 / GYRO**（它自己的 `eflux` 就写着 34.393，而规则 2 是 63.7；标准可执行文件在缺省下逐位复现该记录），而所有拿它做的测量都跑在 **SAT_RULE=2 / CGYRO**。**跨饱和规则比较**——这个坑同日已在能流上踩过一次并写进记录，然后在动量上又踩一遍。★曾有一轮结论说上游 `mflux` 从不被赋值，那是 **grep 假象**：`--include=*.f90` 匹配不到 `tglf_run.**F**90`，其 211/237 行注释写着 `Pi_i/Pi_GB` 正是在赋它。**设置对齐后本仓是对的**：对 `out.tglf.gbflux` 七面里五面逐位吻合，对上游自带旋转回归算例 tglf06（GA 标准算例）**规则 1/2 两个种全 1.0000**。★另两面（0.354/0.455）是**近临界取根**，x=0.455 上那两个 ky 扛着 101 % 的总应力（谱近抵消 1.49×）。★★**真找到的缺陷**：`solve_ky_modes` 把原始 `inp.species` 交给 `write_all_rows`，旋转驱动丢了 `sign_It` 与 `ave_c_tor_par(1,1)/Rmaj`（已录 deck 全 VPAR_SHEAR=0 故不可见，靠打开旋转问出来），修后开剪切 **2.3e-5** |
| ~~**T-C32**~~ | 建模 | ~~动量道自洽接线~~ | — | **关（2026-08-29，当日开当日关）**：整条链本来就齐，缺的只是闭包不带 ω。`surface_states(..., omega=)` 填 `w0`/`w0p`（dω/d**r**，对中平面小半径，与其它剖面同一条 `r_m` 与同一条端点规矩）→ `tglf_inputs(rotation=True)` 写 `VPAR_SHEAR`/`VPAR`/`VEXB_SHEAR`（内核里那段就是 `tgyro_tglf_map.f90` 的映射，`vpar_shear = -sign_it·(-rmaj·w0p)·a/c_s`，早已在）→ `kernel_coefficients(omega=)` 多出第三个钩子 `momentum(state) -> chi_phi`。★**不给 ω 就没有 `"momentum"` 键**（不是零不是 NaN）——平剖面没有可分辨的扩散率，返回一个就是编数。★另加 `fluxes_rust(stress=True)`：应力两行只在 searched 入口上，故以 `nwidth=0`、模数不变绕道过去——**同一次运行、通量逐位相同**（已断言）。★χ_φ 在**全剖面上一次算出**而不是逐面：`momentum_chi_phi` 拒绝的是「平剖面」，那是剖面的性质；逐面调用会在任何 ∂ω/∂ρ 恰好为零的内点上抛异常 |
| ~~**T-C33**~~ | 建模 | ~~E×B 谱移没有回灌进线性解~~ | — | **关（2026-08-29）**：双趟已落地（`tglf_TM.f90:65-80`——先置零扫一遍取参照增长率、再带剪切扫第二遍且宽度冻在第一趟，之后**把本征值重置回第一趟、只留第二趟权重**）。★两个各值一个因子的细节：`kx0_e→kx0` 的除数**按 UNITS 分支不按规则**（GYRO 无除数，`/2.1` 与 `0.7/grad_r0²` 是 CGYRO 的；按规则读把规则 1 压掉 2.1 倍）；GYRO 支的 `wE` 带几何因子 `1+0.40(|grad_r0²/B_geo0|−1)²`。**实测**：tglf05（`VPAR_SHEAR=0`，全部应力都是 E×B 破缺）从 −2.9e-08 到 **−7.2375（1.038）/ −4.2162（1.061）**；Waltz 带剪切两行从 12.4×/0.081× 到 **−3.12×/1.19×**（规则 1）。★逐 ky `kx0_e` 对上游 `out.tglf.spectral_shift_spectrum` **逐位相同**；剩下的 ~6% 是带剪切时的饱和强度余量，修改前就有 |
| ~~**T-C34**~~ | 建模 | ~~生产闭包跑 SAT2 却不带 revision 预设~~ | — | **关（2026-08-29，用户裁定开工）**：`kernel_coefficients`/`kernel_chi` 新增 `tgyro_revision`，缺省 **`"auto"`**（按 TGYRO 自己的配对：规则 1→rev 2、规则 2→rev 3）。`NMODES` 随预设进来并被认（8 模比 1 模只贵 **2%**；预设整体贵 3.2 倍，来自 `NBASIS_MAX=6`/`NKY=18`）。★**有意不照抄 `TGYRO_TGLF_MXH_FLAG=0`**——那是几何保真度的取舍不是模型设置，本闭包保留扩展谐波，故 deck 在 `SHAPE_*` 上不与 TGYRO 逐位相同（已写明）。★顺带修了一个真数值缺陷：`NBASIS_MAX=6` 在合成算例 ψ_N=0.7 上让特征解不收敛，上限 `60n+600` 只有 LAPACK 名义预算的两倍而本例需 100n–150n（实测二分），改为 `240n+2400` 并同步更新那条随上限缩放的回退误差界 |
| **T-C35** | 建模 | **FILTER 门限的累加值与上游不同** | **三个括号已量准，机理未定** | ★★★2026-08-30 用**上游自己的 FILTER 当探针**（不改 third_party，`USE_TRANSPORT_MODEL=.false.` 直接读 `(wr,wi)`）取三个括号：**tglf01** ky=0.1 \|freq\|=0.05621 → X_ours=**1.000**、X_up∈**[1.874, 2.008]**；**tglf02**（κ=2）ky=0.1 \|freq\|=0.05536 → 1.000、**[1.977, 2.129]**；**x072**（JINTRAC，NS=3）ky=0.035 \|freq\|=9.4995 → **1.413**、**[2.950, 3.084]**。★★**比值不是常数**：tglf01 上界 2.008 与 x072 下界 2.088 **不相交**——是一个缩放方式不同的项，不是少了个常数。★**已被这三点否掉的假设**（每个都拟过再失败）：恒 2 倍（x072 要 2.826 < 2.950）；把逐种项**求和**而非取 max（tglf01 的 1.980 合、x072 的 2.786 不合——那里三个种是 1.413/1.317/0.056）；各种 `1/ft`、`1/(1−ft)` 配对；只把种子翻倍（种子 0.62/0.75，够不到）。★还排除：基组归一（`hp3p0 = hp3·p0inv` 对缩放不变）、`pol`（两边逐字相同）、磁支的 `p0inv`（U0=0 时约化为 `inv(p0)`）、陈旧值（`get_matrix` 在前）。★★**别调常数**：JINTRAC 那道门 96 倍余量、不会反对。钩子 `FY_DUMP_FILTER` 已留 |
| **T-C36** | 建模 | **`Z_eff` 下限把纯氢等离子体挪走了** | **处置未定（量已量准）** | `neoclassical.rs:1067` 的 `let zm1 = (zeff - 1.0).max(1e-6);` 是为让 `sqrt(Z_eff − 1)` 在 `Z_eff = 1` 处导数有限，代价是**本仓永远不求值 Redl 拟合在 `Z_eff = 1` 的极限**——纯氢/纯氘被当成带痕量杂质算。★偏置**单向**（总是偏向更强的碰撞压低），量级小但此前无人量过（V-08 §4.1）：L31 在 ν\*=0 上 **1.1e-16**（那一项自带 ν\* 因子，恰好精确）、最劣 1.8e-4；**L32 与 α 的进入路径不带 ν\***，故在**无碰撞轴上就已偏**——L32 **2.7e-8**、α **2.7e-7**。★所以「无碰撞纯氢」这个最常被拿来当验证题的算例，本仓给的已经不是论文的 `Z_eff = 1`。★TORAX 的做法是取精确极限、另用**自定义 JVP** 处理自动微分的 NaN（`3639e479`）；本仓不做自动微分，所以那条路不直接照搬——**要定的是「导数有限」这个理由今天还成不成立** |
| ~~**T-C37**~~ | 反演 | ~~`recon_rs` 无条件依赖 `rfcoil.ddd`，那是整条 Python 反演路的唯一闸门~~ | — | **关（2026-08-31，当日开当日关）**。★★**先证伪了标题里的一半**：`rust/` 与 `app/` 对 `.ddd` / `rsilfc` / `rmp2fc` **零引用**——内核从来不要这份表，闸门只有装配层 `recon_rs.py:250` 这一行。**改法**：线圈→环那一行改为现算 `recon_rs.coil_loop_rows` = `device.channel_response(cond, *flux_loop_positions(), nu=8, nv=8)[0] / 2π`，与浏览器宿主（`worker.js: coilLoopRows`）和本函数探针那一半（`probe_element_response @ el`）同源；整个拟合只解析一次 `conductor_set()`，外场与线圈行不可能来自两台机器。★**顺带把 `table_dir` 从整个 scenario 面删掉**（analysis/design/control/model 共 11 个入口 + `moments` 两个）——它们**早已一个都不读**（`_conductors(table_dir)` 直接 `return conductor_set()`），留着只是宣告机器还能从别处来；结果字段 `tables` → `device`。**两条关闭判据都已满足**：①在**不含** `rfcoil.ddd` 的树上跑通了 Python 反演路（`tests/test_reconstruction.py` 先断言这棵树确实没有那份表，免得靠找到一份而通过）；②对 `fylite_port` 那份真表逐元最大相对差 **7.7e-5**（nu=nv=8），本机复算的收敛序列与外部实测**逐位一致** 4.884e-4→2.373e-4→1.511e-4→9.128e-5→7.724e-5，逐通道 7.0e-6…7.7e-5。★★**换表对最终答案的影响也量了**（把真表接回来逐一 A/B，同炮同设置、收敛的动理学组态）：I_p 1e-6 A · 轴 R 0.14 mm · 轴 Z 0.05 mm · q95 0.05 %；对离线 gfortran-EFIT oracle 则是 I_p −0.10 % · 轴 R 5.1 mm · 轴 Z 6.5 mm · q95 +7.3 %。★★**两件不是这次改出来的，A/B 已排除**：q0 低 64 %、以及 changelog 记过的 npp=3/nff=4 组态抛 `gs_inverse_solve` −112500——**接回真表照样发生**，是边缘为零基的形限与该组态自身的性质。★★**本行这几个数是同日 T-C42 修掉两处交付缺陷之前的读数**（写死的 R₀ 与号相反的通量跨度）：修后 q95 **+1.44 %**、q0 −58.9 %、错误码随迭代数变。★闸子两处：`python/tests/test_one_machine_source.py`（源级：包内不得再有 Green 表读入的调用者、不得再有入口收 `table_dir`——两种回归在运行期都是静默的）与 `tests/test_reconstruction.py`（**反演面的第一份测试**）。**留一半**：登记册的反演记录仍未立 → **T-C42** |
| **T-C38** | 建模 | ★**登记册的平衡求解器空位，现在补得上了**（判据已到手） | **机制**（立记录；判据不再缺） | `docs/note/benchmark/reports/X-01` 自陈这是**最大空位**——旧 EFIT oracle 库随许可消失、`spec.md` §2.4 裁为**不可再录**。★外部已把三层证据做出来（fywork CASE-09 P1，判据/切片/结论齐备，切片带 sha256 在 `data/crosscode/freegs/`）：**① Solov'ev 闭式解**——ψ = f/8 R⁴ + g/2 Z² + c₂R² 精确满足 Δ*ψ = fR²+g，本仓 Δ* 到 **4.5e-12**，FREEGS 二阶且残差**等于手推截断项 \|f\|h²/4**；★★**带对照组**：换一个 Solov'ev **类外**的场，本仓必须退回二阶（实测 **1.98**）——**没有它，4.5e-12 会被读成「算子准十个量级」，而那不成立**（那是守恒型写法碰上 R 的四次多项式）。**② 自由边界**——FREEGS（原样上游 `9fbec2056`）解自己的 TestTokamak 并以 g 文件交出，本仓 Δ* 在其解上闭合 GS **最劣 1.01 % / 中位 0.56 %**（LCFS 内 715 点）⇒ 两码同意同一个算子**且**同一套 COCOS 约定；★★**第二条对照组**：把「等离子体」从**连通区**换成 ψ_N∈[0,1] **阈值集**，最劣残差 **1.0 % → 30 %**（X 点下私有磁通区 ψ_N 读作 0.49）——这正是本仓 `gs_fixed_box` 文档写过的那条，被外部**独立撞上**。**③ EAST #94048** 七个量逐条复现 `PROVENANCE.md`。★**闭式解参照不随许可消失**，正对 X-01 那句「不可再录」。**关闭判据**：登记册新增平衡面记录（含两条对照组，缺一不可）+ `docs/note/benchmark/reports/` 一份 |
| **T-C39** | 建模 | ★**四条已立记录可以增强**（证据现成，只差落记录） | **机制** | 外部（fywork CASE-09）以**另一个消费者、另一条装配路**复核出四件可回灌的：**① V-04 缺活码对照**——现有证据立在**冻结**上游答案上；对**此刻本机活跑**的 GACODE（`6357db306`，SAT_RULE 两边显式写明）Miller **六例**最劣 **Q 2.63 % / S 1.92 % / Γ 2.52 %**（多数 < 0.1 %）。★两者分开的那一天（上游改了/平台变了/编译器换了）正是容差要重审的那天，**而冻结对照看不见那一天**。**② s-alpha 的第二条证据**——tglf03（唯一 `GEOMETRY_FLAG=0`）在活码上同样离群（Q 15.3 % / S 26.5 % / Γ 33.9 %）；★并点出一条本仓记录里没有的差别：**上游自己在 SAT_RULE 2/3 上直接拒收这张 deck，而本仓放行**——「拒绝」与「静默重解释」不是同一种失败。**③ B-01 层二判定域可扩张**——从**一个时刻**扩到**37 个**（含时 ITER，T_i0 11.2→16.5 keV、P_fus 80→184 MW），比值 **0.9951–1.0032**，α 份额最劣 1.00 %。**④ V-14 层一的第三见证**——由**另一个仓另写一遍**的组合算术，17 680 点上与本仓**逐位相同**、两者对 TORAX 2.24e-15，权重 sha256 同一。★★这一条值得单独看：同日本仓刚发现**自己测试里的手写判据复现了作者对上游源码的同一处误读**（主通量该截零而实现与判据两处都没截，门全绿）——**手写判据复现的是它作者的读法**，第三个实现正是补那一层的。**关闭判据**：四条各在其记录里加一行（或注明不采纳的理由） |
| **T-C40** | 建模 | ★★**两个 0-D 入口在同一状态上差 8 %，而登记册只覆盖其中一个** | **判据**（哪个是本仓的口径，未定） | B-01 层二比的是**反应率**（`kernel.dt_reactivity`）在 FUSE 自己的体积剖面上手工积分 ⇒ 1.0009；换成 `kernel.zerod_fusion_power`（本仓**自己的 0-D 积分器**，带它自己的体积权与剖面模型）在**同一份记录**上 ⇒ **0.92**。两者都不算错，但它们是**两个不同的对象**，而登记册的一行只覆盖前者。★**同源的量已被两个互不相干的参照钉住**：`zerod_stored_energy` 对 **METIS** 认证 84 行最劣 **10.81 %** / 中位 **2.55 %**（★与本仓自己门里记的数**逐位相同**），对 **FUSE** 含时 37 个时刻中位 **−4.06 %**——**同号同量级** ⇒ 这是本仓的**体积权近似**，不是某一个参照的口径（★一个参照上的偏差有两种读法，两个不相干的只有一种）。★另一处**可读性**问题同批记下：登记册 B-04 的一行摘要写「权重 3.5 %」指的是**密度体平均**那一档，而储能那一档本仓门里写的是「2.55 % median / 10.81 % worst」——照摘要取带会得到假红。**关闭判据**：定下 0-D 体积权的口径（或声明两个入口各自的适用面），并在 B-01/B-04 补一行覆盖 0-D 积分器那一路；摘要行标明它引的是哪一档 |
| **T-C41** | 建模 | **活入口 `fluxes_rust` 不收 Fortran 布尔，而本仓自己的冻结 oracle 路收** | **机制**（一处归一化） | `input.tglf.gen` 里布尔写作 `.true.` / `.false.`，`scenario.model.gyrofluid.fluxes_rust` 只读 0/1（`int(".false.")` 直接抛 `ValueError`），而 `tests/oracles/tglf.py` 自陈「Fortran `.true.`/`.false.` accepted」——**同一个仓里两条读同一种 deck 的路，一条收、一条不收**。外部消费者只能在自己门口补一层转换（fywork CASE-09 G-10 即如此）。**关闭判据**：`fluxes_rust` 直接吃未经处理的 `input.tglf.gen`，且既有门不变 |
| **T-C43** | 反演 | ★★★**两半都已归因，都是锚点**：共模＝径向锚点（已改成行并为缺省），散布＝**竖直锚点的虚拟丝对**（相关 +0.953，投影掉剩 1.14 σ）。**余下的问题只剩一个，而且提法变了**：不是「散布是什么」，是「**为什么这个平衡需要 70 kA 的外部竖直力**」——环残差 mean **−10.0 → +0.57 σ**、rms 10.25 → **3.82 σ**（判据要 ≤3）；dR −6.5 mm、dZ +2.6 mm 都更好 | **一次裁定**（机制已做出来并量过） | ★★**归因两次改写后定案**：锚点是用**虚拟电流**按住的（网格盒外 (2.00, ±2.80) m 一对丝，幅值 `fb_gain·|I_p|·质心偏差`，**其磁通留在返回的场里**）。径向那对**同号**⇒ 在环上共模主导（|mean|/sd 2.15）；竖直那对**反对称**⇒ 相消到千分之一（0.00）。★**物理上没有别的选择**：把等离子体往外推需要竖直场，任何造竖直场的轴对称电流都链过环——**径向锚点不能用力按，只能是一条关于解的行**。★★★**已实现并量过**：`inverse.rs` 新增齐次行 `Σ(R−R_a)·j·dA = 0`（除以 I_p 后残差读作米，权重＝`1/σ_R`，★量纲写在代码里——`current_fsa` 没写那件事花了一天），与 FSA 行同一条 per-cell 扫描、每轮重建；开关 **`FY_ANCHOR_W`**（沿用 `FY_EDGE_WP` 那套实验开关成例，**缺省关、缺省路径逐位不变**，全套 2827 绿）。**实测**（w ≥ 10 饱和值）：mean −10.00 → **+0.57 σ**、rms 10.25 → 3.82、li3 4.098 → **3.508**（参考的场 2.95）、通量跨度 +42.9 → **+26.0 %**、q₀ −58.9 → **−47.1 %**、dR +5.1 → **−6.5 mm**、dZ +6.5 → **+2.6 mm**；权重 1e2…3e4 逐位相同（实为硬约束），1e5 退出。★★**只改径向，是量出来的**：竖直也做成行 ⇒ 等离子体丢了（磁轴 +96.7 mm）——竖直方向动力学不稳定，反馈在那儿是**稳定器**，一行 damp 不住发散；它也不需要改（反对称，本就相消）。★★★**代价现在看得见**：q95 +1.44 → **+5.56 %**，掉出 R-4 的 3 % 带——**原先那个漂亮的 q95 有一部分是那股假磁通凑的**。**剩下的两半**：①`rms ≤ 3 σ` 未达（散布 2.24 → 3.77）——★**又排除两条**（开关打开后重测）：**不是通道基准**（改用参考交付的 `loopMeas` 重拟 sd 3.75 对 3.77，而两套基准本身差 6.5 σ ⇒ 那个系统差被拟合吸收了）·**不是系数不够**（(2,3)/(3,3)/(3,4)/(4,4) 照样掩膜清空，码 −105000/−105200/−105300/−105500，**开关开着也一样** ⇒ 那个失效模式与锚点无关）。★★**真空室涡流也已接上并排除（第四条）**：`reconstruct(vessel_sigma=)` 把 40 段当标称零电流 + 先验 σ 的自由通道，走内核里**此前零调用者**的 `gs_inverse_solve_coils`，两套响应照 T-C37 由被动几何现算。★★**只有幅值能定案**：σ_v = 10 kA 时 rms **2.67 σ 确实达标**，但要每段 ±12.9 kA、净和仅 0.4 % I_p 的交替振荡——**吸收残差的指纹**，不是平顶 4 s 的涡流（L/R 只有 0.39–13.1 ms）；物理先验（≈1 kA）根本不改善（sd 3.89 对 3.76），且 dZ +2.6 → +13.5 mm 掉出 R-1。拟合出的涡流已报进结果（`vessel_current`）。★★★**四条排除下来是同一个交易的四次出现**（径向锚点 · FSA 电流形状 · 涡流）：每一个能压低环残差的旋钮都在拿**位置**去换——这不像「还差一个模型项」，更像**这套数据与这个基之间的结构性张力**。下一个该动的是那个**掩膜清空**（它挡住了唯一没试过的方向：更大的基）。★另量到：饱和后**探针行与通道基准都不再影响解**（3 个系数被 I_p 与锚点行占掉两个，只剩一个自由度）；权重的**议价带是 w ≈ 1…10**（≲0.1 不起作用、≳10 饱和、1e5 退出）；②~~一次裁定~~ **已裁定并执行（2026-08-31）：缺省翻过来了**——径向锚点缺省即为行，权重 `1/σ_R` 缺省 10（σ_R = 0.1 m，故意放松且在饱和区），`FY_ANCHOR_W=0` 降级为**逃生口**。★★同批**两条判据的带放宽**（R-2 6→20 mm、R-4 3→8 %）——**理由必须连带读**：那两个数原先被假磁通**粉饰**（边界被机器上并不存在的电流的磁通撑着），**一个在假电流被拿掉后变差的数，本来就不在量它自称在量的东西**；同批 R-0 由 10.25 σ → **0.57 σ**、ψ 图 1.18 → **0.97 %**、q₀ 缺口 −58.9 → **−47.1 %**、li3 4.098 → **3.508**（参考的场 2.95）——朝数据与朝参考同时靠近。★★★**并且它把一个更尖的问题顶了上来**：拟合的好坏现在**直接依赖锚点的值**——假电流原先吸收了「探针给的质心」与「磁通环想要的质心」之间约 **46 mm** 的分歧，行按住之后这笔分歧写进了边界（R-2 的 17.8 mm 就是它）。**下一个该问的是：锚点该不该取探针单丝拟合给的那个数。**★★**掩膜清空已开工并修掉一半（2026-08-31）**：先加逐轮转储 `FY_DUMP_INVERSE` 看清——拟合与几何交替**只在一个方向稳定**，能跑的阶交接后跨度**增长** ~1 %/轮并稳住，(2,3) **收缩**而收缩是正反馈，**一步**跑掉（掩膜 510 → 10、跨度 −0.525 → −0.004），**系数几乎没动**（+6 %）⇒ 跳的是 `psi_b`（限制器/X 点搜索的不连续）。两处修改：**边界通量速率限制**（30 %/轮，★对能跑的阶惰性，缺省阶逐位不变）+ **掩膜下限 20 格**（★理由是可读性：(3,4) 曾在剩 1 格后又磨十五轮、|c| 冲到 4e12）。**结果**：(2,3)/(3,3) 由塌缩变**收敛**，**q₀ 缺口 −47.1 % → −20.0 %**（本记录最好）；(3,4)/(4,4) 仍失败但当场说清楚（it=116 剩 9 格 / it=104 剩 13 格）。★★**又是同一个交易（第五次）**：dR −6.4 → −12.4 mm 掉出 R-1，所以**缺省阶不动**。★仍未修：(3,4)/(4,4) 的病在更上游——第一次拟合的 |c| 就大五倍（2.2e6 对 4.3e5），是**条件数/截断集（CONDIN）**那条线；`gs_inverse_solve_coils`（零调用者，实测替代不了那股虚拟电流：放开 12 路只 −11 %）与 `fb_amp_r` 不上报。★★★**2026-08-31 续：散布那一半归因了，锚点的值也量了**。**①散布是竖直锚点。** 剩下的 3.8 σ 不是噪声：按绕磁轴的极向角排序**单调**地从 −6.7 σ 走到 +7.7 σ，只变一次号（m=1 单独占 **72.9 %** 方差、m≤2 占 89.5 %、m≤3 占 96.5 %）——**上下偶极**。与竖直丝对自己的图样（几何**从网格读**：(2.000, ±2.800) m）相关 **+0.953**，投影掉后 **3.77 → 1.14 σ**；对称的径向图样作对照只有 +0.506 / 3.25 σ。最小二乘幅值 47 kA，求解器自报 `fb_amp` **69.9 kA**（`FY_DUMP_INVERSE` 显示它收敛后**不衰减**；除以 `fb_gain·|I_p|` 是 **22 mm** 的质心力 ⇒ **这个平衡是被按住的**）。★★★**并且要更正一句写过的话**：内核注释与记录都写过「竖直那对反对称，所以在诊断上抵消（|mean|/sd = 0.00），因此不必做成行」——**抵消的是均值**；反对称源在其余每一阶矩上都是纯上下偶极，**让它对均值隐形的那条对称性，正是使它成为散布第一嫌疑的那条**。**一项在某一阶矩上抵消，不构成它在别的阶矩上也抵消的证据。****②涡流已第二次排除（第五条）。** 四十段自由拟合能同时把散布压到 2.02 σ、把 `fb_amp` 压到 25.9 kA（★两者**同步**下降，本身就是上面归因的旁证），但把涡流分布**截断**到被动结构能承载的极向谐波（新增 `vessel_modes`：净 + cos mθ/sin mθ）后，m≤0/1/2/3/5 **全部**只剩 3.6…4.0 σ、`fb_amp` 仍 51…56 kA ⇒ 那 2.02 是**不相干的过拟合部分**；四十段里**相干的上下偶极是真的**（中平面上 +15.9 kA / 下 −18.1 kA），单独取出只买到 3.82 → 3.72 σ。**③锚点的值量清楚了（第六条排除）。** 参考自身的电流质心由**两种不共享输入的拼法**读出——它自己的 p′/FF′ + 边界掩膜给 R 1.84648（I_p 复原 −1.0 %），GS 算子直接作用在它的 ψ 图上（不用剖面、几乎不依赖掩膜）给 1.84360（+0.4 %），相差 **2.9 mm** ⇒ **探针单丝锚点内偏 17.2 mm**。扫 `rc_anchor`：**R_out 的斜率是 1.04**，把锚点放到参考的质心上 R-2 最劣的 −17.8 mm **变成 +0.1 mm** ⇒ **R-2 整条缺口就是锚点估计量的偏置**（§4.6 放宽那条带的理由由此变具体）。★★★**但四件事要四个值**：环均值归零 1.870 · 环散布最小 1.8375 · 磁轴归零 1.8371 · R_out 归零 1.8445——**跨度 33 mm**，这个不一致本身就是结果。★★而**整族散布的下界是 3.72 σ**（连不加锚点的 3.90 也在内）⇒ **散布不是径向锚点的值**。**④余下的问题。** 扫 `zc_anchor`：`fb_amp` 随锚点上移**单调下降、没有极小**（−0.11 m → 150 kA，−0.042 m → 36.6 kA），外推到零要把磁轴抬高约 60 mm ⇒ 在实测 PF 电流与本仓拟合出的电流分布下，**自由的竖直平衡位置比环和参考给的高三四厘米**。两个候选：**`psi_ext` 的线圈→网格响应从未与任何 deck 表对拍过**（T-C37 对拍的是线圈→**环**那一路，7.7e-5；受力平衡吃的是前者），以及**电流过度峰化本身**（li3 3.5 对**参考自己的场 2.95**）。★闸子 +2（模块 16 条），`fb_amp_r` 仍不上报。★★★**同日续二：那 70 kA 不是缺了一个外场，而问题的提法又变了一次**。**先说度规**：读参考隐含外场要先减掉它自己的等离子体磁通，第一版逐格当**丝**折叠，给出「两侧 B_r = 0 线相差 **45 mm**」——足以当作装置线圈场缺陷发出去。**它是假的**，拦下它的是**让度规先满足自己的定义**（把 GS 算子作用回折出来的场）：丝折叠只复原 **73 %** 的 I_p、逐格误差 60 %、在等离子体**外**留 **330 A/格**（必须为零）；换成逐格**有限元**（线圈已经在用的那套面积积分）后复原 **+1.25 %**、逐格 2.5 %、外部 12.7 A/格，**同一对零线差 1.6 mm**。★★★两条教训：**流量比较解决不了受力问题**（力住在导数里）·**用一条度规下结论之前先让它满足自己的定义**。**①参考自己几乎没有被动电流**（边界外留三格护城河：净 −439 A、|Σ| 760 A、**上下偶极 +7.5 A**，对照四十段拟合想要的 17 kA）⇒ 涡流排除有了**参考侧见证**。**②`psi_ext` 对，包括导数**（磁通差 0.12 % of span、**B_r 零线差 1.6 mm**）。**③电流分布这一侧**：基阶几乎不影响（(1,2)…(3,3) 69.9 → 66.3 kA，而同批 q₀ 缺口 −47.1 → −20.0 %——**芯部对了，力没少**）；放开边缘更糟（156 kA、磁轴 −124 mm，★并查出 `FY_EDGE_FREE` 下**报出来的剖面不是跑出来的**，装配层仍过 `edge_zero()`）；约束到参考 FSA 形状也更糟（132 kA）★但**不算排除**——三个系数对二十点形状是**不可行约束**。★★**更正一句旧话**：本仓的 ⟨j⟩ **也反号**，只是反得太早（参考 x_N = **0.805 / 0.806** 两种不共享算术的拼法，本仓 0.688…0.703）——差的不是能不能反号，是**在哪里**。**④提法**：实测本仓电流质心对锚点的**斜率 1.53 > 1** ⇒ 竖直方向在这套迭代里**不稳定**；★★★**对不稳定模，比例镇定器稳态本就输出非零**——**「`fb_amp` 应当趋于零」这个期待本身是错的**，不是缺一项物理。而让它归零的机制**代码里已经有**（`zc_ref` 漂移修正，注释写明是为了「把参考点走到真正的无反馈平衡上」），**但被 `!zc_anchor.is_finite()` 关掉**——给了竖直锚点就改用永久的力按住。⇒ **T-C43 现在是一次裁定**：竖直锚点该不该只当设定点。★闸子再 +1（模块 17 条），新的一条**先断言度规的往返**再用它下结论。★★★**同日续三：裁定用实测代替——那份力是承重的，缺省不动**。加实验开关 **`FY_ZC_TRIM`**（缺省关）打开代码里已有、但被 `!zc_anchor.is_finite()` 关掉的 `zc_ref` 漂移修正，即把竖直锚点当**设定点**而非硬按。**它做到了它说的事，答案更差**：`fb_amp` 69.9 kA → **−0.02 kA**（第 **1851** 步真收敛，残差 9.9e-9），代价 dZ +2.6 → **+68.1 mm**（R-1 只给 8）、q95 +5.55 → **+16.1 %**、环 rms 3.82 → 3.99。★不是没跑完（800/1600/3200 步 → +66.4/+68.1/+68.1 mm，且与「完全不给竖直锚点」的 +68.8 mm 互证）。★★**投影掉 m=1 之后残差反而变大**（1.14 → 2.01）——力一撤那对丝就不再是解释了，**这正是归因没弄反的证据**。**用力把账算完**（`F_z = Σ I_格 B_r,ext 2πR`，等离子体自场无净力故平衡解为零）：参考（EFIT 的答案）**+12.3 kN**（z_c −72.9 mm，等效 −44.9 mm）· 本仓缺省 **+18.1 kN**（−84.1 mm，−66.6 mm）· `FY_ZC_TRIM` +9 N（−19.7 mm，−0.0 mm）。★★★**参考自己的答案也不在竖直受力平衡上**——真实放电的竖直位置是**被主动镇定**的，等离子体从不待在无力平衡上，**坚持要受力平衡的反演会把等离子体放错地方**（`FY_ZC_TRIM` 那一行是唯一平衡的，也是唯一坐在外场 B_r = 0 线上的）。⇒ **裁定：开关关着，缺省不动**；那份力是本仓对真机竖直控制的代用品，参考隐式地做同一件事只是从不记账。★★**缺口终于有了自己的单位**：**差额 5.9 kN**（本仓比参考多要 **48 %** 竖直支撑）＝**21.7 mm** 等效偏移，而控制律从不共享算术的一条路给 `fb_amp/(fb_gain|I_p|)` = **22.2 mm**（差 2 %）⇒ **要瞄准的是那 5.9 kN，不是把 `fb_amp` 弄小**（后者已量过，代价 68 mm）。★闸子再 +1（模块 18 条）。★★★**同日续四：挡住高阶基的是 CONDIN，而高阶基把 q₀ 的缺口收到 +2.6 %**。此前碰不了这条线的原因是**截断没有任何可观测量**；本批先露出来（`FY_DUMP_INVERSE` 新带 `keep` 保留模数 与 `cond` 均衡化正规矩阵的本征值跨度）：能跑的阶稳在 **4.1e7**（截断 `CONDIN = 1e8`），(3,4)/(4,4) **开局 5.8e11**，随后截断集与掩膜一起打摆（keep 6/7→5/7→7/7→6/7，掩膜 546→117→643→34）。★★**迟滞稳的是保留模的个数不是哪些**——本征值一交叉，同个数即另一个子空间，这里没有任何东西看得见 ⇒ **高阶的掩膜清空在截断的下游**，不是边界规则也不是系数本身。**收紧一个量级**（新增 `FY_CONDIN`，**缺省不变**；阈值在 3e7 与 1e7 之间）后 (3,4)/(4,4) **收敛**，并给出本记录最好的一组：**q₀ 缺口 +2.6 %**（缺省 −47.1 %）· q(核心)中位 **23.0 %**（62.6）· q95 **+3.43 %** · ψ 图 **0.96 %** · 通量跨度 **+14.6 %** · 环残差 **3.56** · dZ **+0.7 mm** · **Z_min −18.7 mm** · κ **+1.57 %** · a **+0.4 mm**；★(3,4) 保留 **5/7**（收紧多丢一个方向，稳定的答案正是方向更少的那个）。★★**对已能跑的阶几乎不是改动**：`npp ≤ 2` **逐位相同**（ψ 哈希一致），(2,3)/(3,3) 只差 **5e-11 / 1.2e-9** 的通量跨度。★但代价照例是位置（dR −6.4 → **−17.3 mm**，掉出 R-1）⇒ **仍是开关不是缺省，翻不翻是一次裁定**。**两处顺带更正**：①**下支腿那条例外被高估**——Z_min 的 −38 mm 此前归给「两码私有磁通区追踪规则不同」并单列，而同一套规则下**仅靠基阶**就收到 −18.7 mm ⇒ 大部分是基阶；②**li3 不是芯部形状的好代理**——(3,4) 的 q₀ 最好而 li3 更高（3.695 对 3.508），记录里多处把 li3 读作峰化程度。**同批 `fb_amp_r` 终于上报**（与 `trunc_keep` 一起占 out12 的 10/11 槽，**签名不变**，与 `fsa_rows_used` 占 8 槽同例）：缺省 **0**（径向锚点是行），逃生口下 **−68.2 kA**——★**替换掉一处估计**（R-0 的注写「需要 61 kA，而求解器自报 54–60 kA」用的是**竖直**那一路当代理）。★闸子再 +2（模块 20 条）。★★★**同日续六（更正）**：那两处掩膜修补**改成缺省关的开关**（`FY_SPAN_STEP` / `FY_MASK_FLOOR`）。把新内核装进浏览器后，页面自己的闸子 `validate-analysis` **5 红**；二分（每次一个变量）：两处都在 5 红 · 关速率限制 2 红 · 再关掩膜下限 **0 红**。代价具体：本来收敛的原始基拟合 → **第 212 轮法方程奇异**、卷宗九片 **8/9 → 6/9**、孪生退化的判词被掩膜消息顶掉。★三种改写对两条路都不成立（放宽 0.80 · 只限收缩方向——(3,3) 第 126 轮死 · 按塌缩签名触发）。⇒ 缺省关，两边的数都记下；需要它的判据自己要。★★**方法学**：「对能跑的阶惰性」是**相对于一组调用者**说的，而当时只有一组——第二组一到，那句「实测」就变成了「在那一组上实测」。★★**同日续五：芯部形状与竖直受力不是同一件事**。(3,4) 把 q₀ 缺口走完全程，但受力差只走四分之一：F_z 参考 **12.29 kN** · (1,2) **18.14**（差 5.85）· (3,3) 17.21（4.92）· **(3,4) 16.57（差 4.28）**；同批环残差 rms 3.82 → 3.56（**只走 7 %**）。★这 1.57 kN 里几乎没有质心的份（z_c −84.12 → −82.30 mm，只挪 1.8 mm）⇒ 变的是分布的**高阶矩** ⇒ **余下的 4.3 kN 不是芯部**。★**数字更正**：此前几处写「li3 3.5 对参考约 2.2」——**2.24 是本仓在 FSA 约束下的拟合**，参考自己的场是 **2.95**；而 li3 又被量出不是这个量的好代理（q₀ 最好的 (3,4) 其 li3 更高）⇒ **这条线上不要再用 li3 说事**。★闸子内加两条断言（q₀ 走完而残差不走）。★★★**同日续六：那句「几乎没有质心的份」是错的，已更正**——**从位移推力而没量灵敏度**。实测（同一外场刚性平移）`dF_z/dz = **−0.275 kN/mm**` ⇒ 1.8 mm = **0.50 kN**，占 1.57 的**三分之一**。整笔拆分（每个数出两遍、一致到 0.1 kN）：(1,2) **3.14 位置 + 2.71 形状** · (3,3) 2.83 + 2.09 · **(3,4) 2.64 + 1.64** ⇒ 升阶时两份都掉（−0.50 / −1.07）。★另一条路（矩展开）**没通过自己的检查**作废：对本仓差 1.1 kN、**对参考差 3.8 kN**（铺得太开，二阶截断不收敛），且其二阶矩是**带号电流加权**、参考外层电流反号 ⇒ ⟨δR²⟩ 可为负（−12.1 / −38.5 cm²），**不是展布度量** |
| ~~**T-C42**~~ | 反演 | ~~反演面在登记册上仍是空的（测试已有，记录未立）~~ | — | **关（2026-08-31，当日开当日关）**：登记册第 25 条 **B-06**（`docs/note/benchmark/reports/B-06-east-reconstruction.md` + `benchmark/scenarios/east-137985-magnetics.fyo.jsonld` + 完整账 `docs/note/east-reconstruction-benchmark.md`），**判为 B 对拍不是 V 验证**——★立项时本行写的是「归类为验证」，那是错的：两侧的 p′/FF′ 基不同（参考边缘非零、本仓边缘为零），不是同一个函数的两个实现，正是 `spec.md` §1 点名的那种夸大。**七条闸子**（`tests/test_reconstruction.py`）。★★**本册第一条输入为「测量」而不是「状态」的记录**，也因此是第一条能区分「测量残差很小」与「内部剖面错了」的记录——而它量到的正是这种情形：磁轴 5.07/6.54 mm（**零假设 26.9 mm**，反演在 Z 上挣了四倍）· 边界 ≤5.6 mm（下支腿 −38 mm 单列）· ψ 图 rms 1.18 % · q95 **+1.44 %**；而 q₀ **−58.9 %**、通量跨度 +42.9 %、li3 4.10。★★**第一次认真比对就撞出两处一直在交付路上的缺陷**（都在装配层、都不会让任何测试变红）：**①`f_edge` 的 R₀ 写死 1.85 m**（测量字典根本没有 `rcentr` 键，缺省每次生效；EAST 是 1.75 m）⇒ F 边值/q/交付 `rcentr` 一律偏 5.7 %，修后 F(1)=3.1500 与参考**逐位相同**、q95 从 +7.28 % 收到 +1.44 %；**②装配层 `span_pr` 号相反**（内核用 `(ψ_a−ψ_b)/2π`）⇒ **交付的压强剖面号是反的**（写出去的 g-file 带负压强，而同一次拟合的动理学行匹配的是正的），且 F 沿 ψ 积错方向、q 全剖面受影响。★★**②为什么活到今天**：`test_recon_profiles.py` 检查得并不马虎（两条独立路径积同一组系数），但它**自己写死 `SPAN = 0.37`**（正数＝内核的号）——号错的不是 `fit_profiles` 而是 `reconstruct` 递给它的那个数，**那个数没有任何测试看过**；何况测试内部两条路径共享同一个 SPAN，取反也一起翻（同 V-14 记过的那类「第二个实现共享了输入就不是第二个见证」）。★同批更新 X-01（「平衡重构的活参考不可恢复」此前被读成「不可比对」）。**留一半**：芯部 q 的缺口 → **T-C43** |
| ~~**T-C28**~~ | 建模 | ~~锯齿没有周期~~ | — | **内核侧关（2026-08-28）**：`saw_period` + 跨块相位携带（`saw_elapsed_in/out`）已沉，两个宿主接线，门在 hollow 夹具上自立（计量 + 跨块相位等于长跑）。★★随手更正一处归因：此行原以「JINTRAC 参考按 DTSAW=0.1 s 约 163 次崩塌」为判据——查运行自己的配置，**参考的锯齿模型关着**（`SawteethPanel.select : false`、`ISAW = 0`，DTSAW 是没被读的缺省），对标里锯齿应当关着跑（笔记 §8d）。触发判据的修复（轴外 q = 1 面）与容差不受影响，独立成立。★留一半：**页面控件**（`sawperiod`）未加——worker 的行与跨块携带已接好（`sp.sawPeriod` 今天恒 undefined = 0），控件落地即生效 |

★**现在真能动手的**：**T-C43**（★★★诊断已落到**一处机制**：锚点用虚拟电流按住，径向那对是同号丝故在环上留共模——去掉它 −10.0 σ → +0.24 σ；要做的是把锚点改成**能与数据议价的行**而不是往场里注电流，同时别丢掉径向精度）· **T-C35**（SAT2 在规则 2 上的饱和余量——三个独立数据点已钉成对照闸子）· **T-C27**（规定道的内核模式）；★★**2026-08-31 新增的五条里有四条也不等任何外部东西**——★**T-C37 / T-C42 已当日关**（几何现算搬进 Python 路、`table_dir` 从 scenario 面删净；登记册第 25 条 **B-06** 立起来，★★**第一次认真比对就撞出两处一直在交付路上的缺陷**——写死的 R₀ 与号相反的通量跨度，后者让交付的压强剖面带负号；芯部 q 那一半转 **T-C43**）· **T-C38 / T-C39**（判据与切片都现成，只差把记录立起来/加一行）· **T-C41**（一处归一化）。**T-C40 是这五条里唯一真缺判据的**——它要的不是数据，是一次裁定：0-D 的体积权以哪个入口为本仓口径——★T-C33/T-C34 与 **V-04 的四处移植缺陷全部已关**——**T-C29 / T-C30 / T-C31 / T-C32 均当日开当日关**，★动量线的**无 E×B 剪切**那半已收（应力对上游逐位、旋转投影修复、闭包三个钩子），★T-C31 是被**证伪前提**关掉的（参照 `mflux` 是上游从不赋值的输出）——T-C28 已内核侧关，判据当时就现成（JINTRAC 作业 102530 那份运行自己的 IDS 就是要钉的时序），缺的只是机制；
★这也是这一档里**第一条不等外部东西的**，此前一条也没有。★2026-08-29：**T-C1′ 也不再等了**——它等的那台机器（Julia + FUSE 全套依赖）就是本机，已关。其余仍在等：T-C18 / T-C17 / T-C20 / T-C24 /
**T-C25 / T-C26**（后两条 2026-08-28 新增）的判据都写着「先要一份外部算例或公开表值」（本机 `doi.org`/`iopscience` 出口 403，
按仓规矩不能凭记忆写系数）；T-C21 挡在一次数据搬运。**T-C22 全关**（三段齐备、
已接进活读入路径），T-C23 半关。★T-D19 已从「诊断」
改为「一次复现」——本机三遍全绿、两个自然解释各被实测排除（§3.2），要的是**另一台
宿主**。
其余等数据或等一次人来拍板——**写清楚这一点本身就是这张表的用处**。
★2026-08-29：「等一台有 Julia 的机器」这一条**已从表上消失**——本机装上了
Julia 1.11.4 与 FUSE 0.7.0，T-C1′ 因此关掉；全表复查过，没有第二条挂在这个理由上。

### 1.2 架构对齐（A-1..A-7，判据正本 `FYL-REPORT-03` §8）

| # | 事项 | 收编 | 一句话判据 |
|:---|:---|:---|:---|
| ~~**A-1**~~ | ~~语义表化~~ | R2-02 | **已关（2026-08-26）**：最后三个两宿主各写一遍的语义门（`fyo:magnetics` / `fyo:pf_active` / `fyo:tf`）**入内核声明**（三张**无槽**表——标签是共有的事实，段里的载荷是装置卷宗自己的），两宿主改读生成物；判据第一条**按字面补齐**——原有闸子只判「写进同一位置」，新增一道判**读回同一个值**（真文档、多量级、`==` 精确比）；`fyo.js` 行数棘轮 ≤ 198 入闸 |
| **A-2** | 浏览器 15 条 worker 命令入声明面（按 `kernel_id`） | 新 | dispatch 与目录一致的闸子；BYOK 工具定义由目录反射 |
| ~~**A-3**~~ | ~~确定性入清单~~ | R2-03 | **已关（2026-08-26）**：`env_fingerprint` 记「会改一个数」的变量（**按声明取，不按手写名单**）与线程设定；`breakdown-iter` 跨 1/4 线程交付数逐位相同，闸七道 |
| ~~**A-4**~~ | ~~谱系反查~~ | R2-04 | **已关（2026-08-26）**：`fylite whence <file>` —— **按内容反查**（把文件哈希去比每份运行清单已记的 `sha256`），故**复制过、改过名的照样认得**，而**改过字节的认不出来——那正是诚实的答案**；一行谱系带 run / 工具 / 会话 / 时刻 / 代码版本 / **宿主**（A-3）/ 上游 run。退出码即判决 |
| ~~**A-5**~~ | ~~句柄可读别名~~ | R2-05 | **已关（2026-08-26）**：`engine/alias.py` + `fylite alias` —— 名字**在 id 之旁、不取代它**（句柄 / 账本边 / 清单一律仍持 id，`find_run` 把别名放在**最后**才试，id 永远赢）。两条判据各有闸：`name@vN` **一经指定不改指**（冲突报错，且该情形**可达**——带缺口的登记簿）；**匿名不入册**（`None`/空/纯空白皆拒，且**拒绝时一个字节都不写**）。版本号由登记簿分配，同名同 run 幂等 |
| **A-6** | BYOK 落地三件（见 §1.3） | G-21/22 | 各自条目原判据 |
| ~~**A-7**~~ | ~~跨宿主重放~~ | 新 | **已关（2026-08-26）**：`engine/crosshost.py` —— **五个声明条目**逐个在 native 与 wasm 上各跑一次；「必须逐位相同的是哪几行」由内核声明（`ENTRY_OUT_KIND`，生成进两宿主）而不是闸子自己列，计数/标志**取哈希**、实数比实测带、噪声两边都要小；`env_fingerprint` 补上 `host`，差异由记录解释。账本一半：`for_ledger` 按**声明的 `kernel_entry`** 分拣，其余按名拒绝 |

### 1.3 BYOK 与身份（判据正本 `FYL-REPORT-01` §14.6 / `FYL-REPORT-02` §5）

| # | 事项 | 类别 | 状态 |
|:---|:---|:---|:---|
| **G-21** | 共享 origin（`fusion-yun.github.io/fylite/`）：接受并在密钥输入处告知，还是迁独立 origin | **裁定（须人拍板）** | 开 |
| **G-22** | 密钥禁入导出物 / 运行记录 / stdout 的扫描闸子 | 能力 | 开（待 LLM 前端真有导出面——先落闸子会扫一个不存在的东西） |
| **R2-01** | 「文档 / 受管工件」分级的导出物**显式标记** | 裁定 | 半关：分级规矩已进 `FYL-SDD-01` DE-COMP-05.2；**导出物标记未落** |

### 1.4 场景语料与交互页（S- 序，判据正本 `FYL-REPORT-03` §10）

★**2026-08-26 两条裁定**：①**物理先行**——推后 app 接线，先在 CLI/Python 完成物理
场景校验，三张场景页的场景提取成 case fyo 落盘；②**内核下沉 + 双薄面**——evolve
的外层编排不抄成第二宿主，而是沉为内核场景条目，两宿主各剩薄门面，JS 手写编排
行数入棘轮只减不增（缘由见 §2.8）。

| # | 事项 | 状态 | 一句话判据 |
|:---|:---|:---|:---|
| **S-1** | 三页场景语料齐备落盘（21 档） | **半关**（★另有一道闸子红着，见下） | 开着的半边：分析页三档由页面「导出 → 会话文件」重产替换、往返一致（随 S-2b 走） |
| **S-2** | **CLI/Python 物理场景校验**：逐 bar config → Python 入口逐字段映射，经工具面真跑、留清单/验收/账本 | **半关** | 已关：**五 bar**（zerod · transport · breakdown · discharge · evolve）逐字段全账、`fylite cases --plan/--run` 可跑；开着的：evolve 未沉能力（→ S-2c）与三条 browser-only 栏的既定拒绝 |
| ~~**S-2a**~~ | evolve 时间循环下沉：`evolve_heat` 场景条目 | **已关**（2026-08-26） | 批一＋批二均关：热通道 + 常数闭包 + 沉积 + ADAS 辐射（bulk + 杂质）+ 处方档 alpha；双面逐位一致闸；ABI 不动；`ADAS_ZA` 入内核声明并生成进 Python。语料 **5/13 在范围内**且逐例可跑 |
| ~~**S-2b**~~ | JS 面切到该条目（evolveRun 瘦身） | **已关**（2026-08-27）：范围内的配置整段走条目，循环留给没沉的能力。语料 **11/13 两个宿主同一套编排**；范围判定由内核 `ENTRY_SCOPE` 单源，两宿主同读 | ★**实测**：现在切会让 JS **增行不是减行**——在范围内的那条路径不是可删的独立块，而是**一打条件分支的 false 分支**（`wave`/`closure===3`/`ipCtl`/`momentum`/`sawtooth`/`pedestal`…）共用一个循环体，条目驱动的路径只能**并排**摆在循环旁边。循环要能整体离开，得先由 S-2c 沉够。剩下两件同批做：①条目补 `nu_exch_init`/`nu_exch_last` 续跑对，使**分块驱动与单次长跑逐位一致**（页面按 `report` 节奏报进度，分块边界不得丢交换帽）——★**现在不做**：它唯一的读者就是这次切换，先落等于给不存在的读者立能力（与 G-22 同一姿态）；②逐步 readings/trace 的去处裁定（条目补 per-step 块，或明说 trace 按报告节奏抽样）。**③已关**（见 S-2d） |
| ~~**S-2d**~~ | ADAS 物种表下沉 + 双宿主生成 | **已关**（2026-08-26） | `ADAS_ZA` 入内核声明并生成进 `_deck_names.py` 与新资产 `app/assets/deck-names.js`；`fylite.js` 两处手写字面量删除、改读生成表，缺文件时**抛错**而不是空表；worker / 三页 / node 测试台四个宿主的加载序入闸。★净行数**持平**（fylite.js −4、worker.js +4）：这一批移的是**归属**（手写表变生成表），行数要等循环离开才降——闸里写明了这笔账 |

**〔S-2b 前提重测〕**（2026-08-27，S-2c 批二–批五收口之后重量一次）

原话是「在范围内的那条路径不是可删的独立块，而是**一打条件分支的 false 分支**
（`wave`/`closure===3`/`ipCtl`/`momentum`/`sawtooth`/`pedestal`…）共用一个循环体」。
**这句话现在不成立了**，逐条对账：

| 原文点名的分支 | 现在在哪 |
|:---|:---|
| `pedestal` | **已沉**（批一） |
| `sawtooth` | **已沉**（批三） |
| `wave` / `closure===3` | **根本不在循环体里**——在循环之前的 setup |
| `ipCtl` · `momentum` | 仍在循环体里，共 **68 行**物理（`evIpControl` 40 · `evMomentumStep` 19 · `evOmegaShear` 9） |

实测：逐步循环体 **176 行**，顶层条件分支 **5 个**（不是一打），其中两个已沉
（交换帽计数、锯齿）、一个是页面自己的进度上报、一个是 `momentum`。

**但结论仍然成立，换了个理由。** 循环要能整体离开，挡路的不在循环体里而在它依赖的
**setup**：

```
循环体内未沉        68 行   ipCtl · momentum
setup 内未沉      1506 行   beam 256 · 外环细化 331 · flux-match 266 ·
                            T-C14 稳态电流+平衡 401 · LH 143 · 其余 109
合计              1574 行
```

只要还有一条能力没沉，**那些配置的循环就得留着**，于是条目驱动的路径只能**并排**——
JS 行数照样先涨。这一条没变。

★★**但这里有一件比行数更要紧的事，记下来免得被行数闸带偏。** 现在的状态是：同一个
语料算例，Python 走 `evolve_heat` 条目，浏览器走自己那圈 JS 循环——**一个能力，两套
编排**，正是 D-4 要终结的那个安排。眼下它们靠 `validate-evolve.mjs` 一道闸钉着相等，
是**判据**保证的一致，不是**构造**保证的。而语料 13 例里 11 例在范围内、另 2 例只差
数据——**没有一例需要那 1574 行里的任何一条**。

所以 S-2b 的价值不是减行，是让**每一个有算例的配置在两个宿主上走同一套编排**。

★★**那道行数闸已经换了口径**（2026-08-27，动代码之前先动它）。旧闸钉的是
`worker.js` + `fylite.js` 的**原始行数**——一个代理量，而 S-2b 恰恰是会把这个代理
量弄反的那一步。新判据量的是**东西本身**：逐步循环体 + 它传递调用到的一切，剥掉
`fy.*` 内核调用之后，只数**宿主自己拿主意**的行。三条排除各有理由，且三条都由反证
钉住：

| 注入 40 行 | 度量变化 | 判 |
|:---|---:|:---|
| 手写辅助函数 | **+43** | 红 ✓ |
| 只调条目的路径 | +3 | 近乎免费 ✓ |
| **注释** | **0** | 绿 ✓ |

第三条尤其要紧：**一道会为「把决定写清楚」收费的闸，教的是它本意的反面**——本仓已经
吃过一次，批二那天花了半天压缩散文去凑行数，而代码一行没动。

基线 **538**（原始跨度 986，差额全是散文、括号与内核调用，这三样都不该收费）。

★★★**S-2b 已落（2026-08-27）**，并且上面那句预判**兑现了**：手写行进量
（538）**一动没动**——切路径不删手写物理，循环对没沉的配置还得留着。动的是另一个
量：语料里 **11/13** 从「两套编排」变成「两个宿主同一套」。这正是当初换口径要保护
的那一步：旧的原始行数闸会把它判红，新的行进量闸放行，而**新立的覆盖闸**才记下它
买到了什么。

★落地形状：`evolveRun` 里**一条前置分支**，`evScopeMiss(sp)` 说在范围内就走
`evEntryMarch`——**一步一调**（页面要在步与步之间画，读数是按每步的状态算的；续跑
对让 N 次单步调用与一次 N 步调用**逐位相同**，所以代价只是每步一次 ABI 穿越）。它
填的是循环填的那些变量（`st`/`trace`/`crashes`/`steps`/`tNow`/`ctx.last*`），然后
**落回同一个出口**——收尾那段读数与那一次 `post` 一个字没改，仍然只有一个出口、一
份 payload。

★这一批的调试里有个教训：我**猜了两次**参数名（`ctx.srcDiag`、`sp.dep`），两次都
不中。真正解决问题的是**把报错改对**——`fyo.js` 的 `pack` 在说出是哪一行之前就解引
用了那个 undefined，改成先判 `v == null` 再报行名之后，一次就指出 `params/dep_centre`。
**报错说不清是哪一行，就是在逼下一个人去猜。**

★★**但要认清一件事：走(乙)也不会让这个数变小。** 切路径不删手写物理——那些分支
对没沉的配置还得留着。(乙) 买到的是**另一个量**：有多少配置在两个宿主上走的是同一
套编排。所以 S-2b 落地那一批要**同时**立第二道闸（条目覆盖的配置数只增不减），并让
行进量的基线**只在覆盖数上涨时**才允许上调——「对的一步放行，错的一步不放行」直接
写进判据，而不是靠每次争论基线。

| **S-2c** | **evolve 逐能力下沉**（按解锁例数排序，每条一批、各带判据） | 开（**批一〔基座〕已关**：语料 5/13 → **11/13**；余两例见下表） | 逐条见下表；每关一条：`cases._EVOLVE_UNSUNK` 删该行、`test_the_evolve_scope_ledger_names_what_is_missing` 的在范围下限上调、双面逐位一致闸覆盖新特性 |
| **S-3** | app 交互推进（**推后至 S-2 之后**）：P0=A-2+逐参数声明 → P1 通用 runner 页 → P2 LLM 生成路 → P3 dag 执行器 | 开 | 判据逐条见 `FYL-REPORT-03` §10.3 |

★~~**S-1 连带的一道红闸，2026-08-26 实测记入**：`validate-cases.mjs` 有 **3 项未过**
——分析页三栏「有算例菜单」全红，成因是三档语料落盘时 `analysis.html` 没有同批加上
`<select id="analysis-<bar>-case">`：语料写下了，读者没有。~~
**2026-09-01 关闭，方式是拆掉读者那一侧**：裁定「页面不再取算例」，`S.cases`、六个
算例选择器与 `validate-cases.mjs` / `validate-initial-case.mjs` 一并撤除。这道红闸
问的「三栏为什么没有菜单」自此不成其为问题——**没有一栏有菜单**。★语料本身没有丢，
搬到 `docs/cases/`，读者是 `fylite cases` 与 `fylite.scenario.cases`；分析页那三份
算例仍在目录里，可经 `fylite cases <name>` 取用、经「导入会话文件」施用。

★**另记一条环境性的红（2026-08-26 实测，非缺陷）**：`validate-recon.mjs` 的
「页面无脚本错误」一项在**只起静态服务器**时红，实测原因是页面探测
`/api/health` 得到 **404**——那是 MDSplus 请求面（`fylite-app`，`rust/fylite/src/bin/app/api.rs`）的端点，
静态服务器答不了。★兄弟闸子 `validate-recon-slices.mjs` 用 `page.route` 把
`/api/health` 与 `/api/measurements` 罐装掉，所以不受影响。**关闭判据**（二选一）：
跑该闸时同时起网关；或让它像兄弟闸那样罐装那两个端点，并说明「无脚本错误」这条
判的是页面自己的错，不是缺一个可选后端。

**S-2c 的工作表**（2026-08-26 实测：13 份 evolve 语料，5 份在范围内）：

| 未沉能力 | 解锁例数 | 卡住的算例 |
|:---|---:|:---|
| ~~基座模型（EPED1-NN）~~ | ~~6~~ | **2026-08-26 已沉**（批一）：β_N → EPED1-NN → 下一步 Dirichlet 边界，一步滞后照页面；验收多一条 `ped_extrapolation`（外推**永不 fail、但也永不 pass**，因为上游答而不拒——`evolve-fuse-iter` 定 chi 燃烧跑飞到 β_N≈7，把代理问出训练箱 0.49 个输入量程） |
| ~~电流通道（连带欧姆与自举）~~ | ~~2~~ | **2026-08-27 已沉**（批二）：`evolve_heat` 增 `ch_current`/`ohmic`/`bootstrap`/`v_loop` 四参、六路输入块、`psi`/`j_bs`/`p_ohm`/`q` 四路输出；闭包两行（Spitzer σ∥ + Redl 自举，束流与波**不补零冒充**）、滞后欧姆项 `E∥ = 2π ρ (dψ/dt)/V′` 回灌 `q_e`。**这两例仍未解锁**——它们还缺锯齿与 device/gfile 几何 |
| ~~锯齿~~ | ~~2~~ | **2026-08-27 已沉**（批三）：触发 `q_crossing` 于 1、混合 `saw_mix × r₁`、按 `∫V′y dρ` 守恒地摊平 te/ti/ne，`psi`/`q` 由内核自己重建；三读数（没有 q=1 面 / 碰撞了 / 触发了但混合半径给不出）各有一行。**这两例仍未解锁**——还缺几何档（与 useref/icd） |
| ~~device / gfile 几何档~~ | ~~2~~ | **2026-08-27 已沉**（批四）：`fylite.fyo.Ladder` 追踪一次，度规逐列喂条目；判据是**非自洽**的两条（电流对文件所载、q 对梯子追踪）。east-hmode 还缺 icd，iter-15ma-benchmark 还缺 useref |
| ~~参考重构剖面起步（useref）~~ | ~~1~~ | **2026-08-27 已沉**（批五）：`fylite.io.reference` 转写页面自己的 CSV 解析（列名、单位、钳位插值），**逐通道**覆盖：表里没有 T_i 就把 T_i 留在控件放的地方 |
| ~~处方驱动电流（icd）~~ | ~~1~~ | **2026-08-27 已沉**（批五）：与内核已有的沉积闭包同一个高斯，按**面积元**归一；`j_cd` 与 `j_bs` 分开报 |
| 密度 / 动量通道 · 束流 · 波 · 波形驱动 · 杂质稀释 · 续跑 | 0 | **本语料无人点名**——沉它们要先有算例，否则是给没有读者的能力立闸 |

★★**批二〔电流通道〕的三件事，记在这里因为都不是「把代码搬过来」**（2026-08-27）：

1. **度规缺的那一列，是一句错话挡住的**。电流通道要 IMAS `gm2` = ⟨|∇r|²/R²⟩，
   而处方 Miller 层给不出：浏览器 `evMillerMetric` 一直写 `gm2: null`，注释的理由
   是「四个标量定不出这个量」。**这句话是错的**——Miller 面集把 R(θ) 与 |∇r|(θ)
   完全定死，`geometry::solve` 里配料一样不缺，只是**从来没有这一列**。所以修法是
   把列加上（`fsa_grad_r2_over_r2`，与 T-M8 加 `<R^2>` 同一个权重、同一个道理），
   不是继续拒绝。★宿主**不能自己除出来**：`gm2` ≠ ⟨|∇r|²⟩/⟨R²⟩，比值的平均不是
   平均的比值，圆截面 R0/a = 6 上差 **5 %**（闭式：⟨1/R²⟩ = 1/(R0√(R0²−r²))）。
   ABI v114 → **v115**，新符号 `geo_surface_gm2`，导出 219 → 220。
   ★★这是**第二次按列长符号**（`_r2` 是第一次），记成账：geo 端口欠**一个带缓冲
   区长度参数的入口**，三个定长符号在下一次能同时看到所有调用方的 ABI 跃迁里一起
   退休。**不打算长第四个。**

2. **ψ 的起步值用错了关系，闸子是靠收敛阶抓出来的**。第一版按
   `dψ/dρ = V′ gm2 F/(4π²q)` 播种——那是**电流扩散度规**（ψ 方程的系数，正是 gm2
   的用处），不是 q 的定义。入口自己写的是 `q = 2π B0 ρ/(dψ/dρ)`，反解它才对。
   实测：错关系内部相对差 **4.34（差 5.3 倍）**，对的 2.8e-3。★闸子判的**不是
   容差而是收敛**——内部二阶（41→321 点降 60 倍）、边缘一阶（一侧差分）、轴上
   收敛到**恰好 0.5**（内核自己的 `max(ρ, ρ₁/2)` 轴规矩，是个因子不是误差）。
   **错的关系在任何容差下都过不了收敛判据**，这是这道闸的设计。

3. **两个「条件在测替身」的坑，是把替身变真的那一刻才现形的**。两处都写
   `!geo.gm2`，而意思都不是它：
   - T-C14 外环拒绝，意思是「解析层没有可重追的磁面」。gm2 一变真它就**静默不再
     触发**，要了五轮却只跑一轮——正是它存在要防的那件事。改成判 tier。
   - `evEnclosedIp` 的拒绝，意思是「有一张解出来的 ψ 可读」。解析层 `evPsiOf`
     恒返回 0，gm2 一变真，这里会从 `null`（诚实的「没有」）变成 **I_p = 0**
     （一个错数）。改成判 ψ 是否恒零。
   ★两个都不是测试抓到的，是**问「这个条件到底在说什么」**抓到的。教训写进
   §2.8 的规矩：**一道拒绝要判它真正指的那件事**，替身条件在替身为假的那些年里
   看不出区别，而在替身变真的那一天一起失效。★浏览器的电流通道在解析层**仍然
   拒绝**，理由换成了「没有起步磁通」——那是现在真正缺的东西；给解析层播种 ψ
   是另一批的事，不在这次悄悄开。

★★**批三〔锯齿〕的三件事**（2026-08-27）：

1. **触发判据读的那条 q，必须和上报的是同一条**。`transport::solve_psi` 的原始轴
   节点恰好是 `q(ρ₁)/2`——上一批的收敛闸测出它**收敛到 0.5**，正说明那是节点的产物
   而非节点的误差。锯齿判 `q(0) < 1`：读原始节点，一个 q(0) = 1.4 的健康放电会显示
   0.7，于是**每一步都碰撞，永远如此**。条目现在按内核**自己**为同一个量写下的约定
   （`surfaces::q_profile`：最内两点线性外推）报轴值。★这改了一个**已发布**的输出，
   上一批的轴判据随之从「收敛到恰好 0.5」改成「二阶收敛到给定的 q(0)」——**判据变强
   不是变松**，理由写在闸子里。

2. **密度成了状态，只因为锯齿**。这一档仍然没有密度**通道**，两次碰撞之间 `ne` 就是
   调用方给的那条。但浏览器的 `evSawtooth` 混合的是**三条**（te/ti/ni，然后 `ne =
   ni`），只混两条的条目会在锯齿放电上给出与页面**不同的答案**——那正是下沉要终结的
   分歧。所以 `ne` 变成真状态，`ne_cgs`/`beta_n_of` 全部改读它（`beta_n_of` 从捕获
   改成传参：捕获输入切片的闭包会一直报一个 march 已经不在的等离子体的 β_N，而台基
   读这个数）。

3. **两宿主的差是「放大」不是「分歧」，所以给它自己的闸而不是放宽带子**。锯齿变体
   在第 7 步之后越过 1e-12：实测 `p_ohm` 6 步内 4e-16、8 步 1.7e-13、12 步 1.3e-12
   ——**从机器精度开始、随步数增长**，是非线性行进把两份构建的最后一位放大，碰撞把
   状态甩得更远所以放得更快。★把带子放宽到能吞下 1.3e-12，就等于对**任何**不超过
   这个数的真分歧也点头。可判的是**形状**（起于机器精度、随步数增长）与**离散输出
   永不分家**（步数、碰撞次数不一致，浮点噪声解释不了）。所以对拍闸问六步，另立一
   道闸把放大本身钉住。

★另修一个**形状随数据变**的毛病：`kernel.scenario` 从前按**算出来的长度**决定标量
还是数组（`n == 1`），`nt = 1` 跑一次逐步的迹就成了裸浮点数，`model.evolve` 死在
`out["t"][:steps]` 上——偏偏 `nt = 1` 是调试时第一个会试的尺寸。现在按**声明**判。
声明是合同，随数字变的不是合同。

★★**批四〔几何档〕已通，路上撞了三件事，其中一件是我自己判错**（2026-08-27）。

**数据不缺**（按嘱先查 fydata）：`g900003.00230_ITER_15MA_eqdsk16*` 五种分辨率全过
`require_convention`（`dpsi, per radian`，裕度 35–170×）；第二份参考件
`15MA Inductive at burn-ASTRA.csv` 与页面 `modelParseReference` 的列名逐个对得上。
顺带：`g063982.04800` 被拒（裕度 1.97），正是当初定下「裕度 ≥ 3」判据的那个文件。

1. **规范（2π）——我一度判成内核有 bug，是错的。** 拿 ITER 平衡量出来：ψ 取
   per-radian 时 `q = B0ρ/(dψ/dρ)` 对上梯子追踪的 q，而 `solve_psi` 报的是 2π 倍，
   我据此以为内核两半自相矛盾。**裁定「默认 COCOS 17」之后立刻清楚了**：COCOS 17 的
   ψ 是**整圈 Wb**——这一条本仓早已在 `model/assembly.py` 与 `model/stationary.py`
   里就**这条通道**写明了。换成整圈 ψ，内核那两半严丝合缝：

   | 关系（ψ 取整圈 Wb） | 结果 |
   |:---|:---|
   | `q = 2π B0 ρ/(dψ/dρ)` | 1.0246 / 1.0259 / 1.0246 |
   | 梯子自己追踪出来的 q | 1.0247 / 1.0267 / 1.0253 |
   | `I = V′ gm2 (dψ/dρ)/(4π²μ0)` | 文件所载电流的 **0.926**（梯子停在 ψ_N = 0.95 的截断） |

   ★错的是**我喂进去的东西**：`Ladder.psi` 是 Wb/rad，条目要整圈。一行 `* 2π`。
   批二的播种反解的是内核自己的关系，所以本来就是整圈的——**没有已发布的数是错的**。
   ★教训仍然成立且要记住：**反解一条关系再用同一条关系检查它，永远会通过**。批二那道
   q 往返闸测的是自洽性，测不出规范。补上的两道闸都是**非自洽**的：拿 fydata 这张
   平衡，判「从行进出的 ψ 读出的电流 = 文件自己写下的电流」与「条目报的 q = 梯子追踪
   的 q」——文件不参与我们的推导，这才是判据。

2. **轴节点上，ψ 是唯一不能「重复最内一圈」的那一列。** 其余每一列都重复（面平均在
   退化面上没有值），而**轴上的磁通是平衡自己写下的数**。重复会让 `psi[0] == psi[1]`，
   而 Redl 闭包是**对 ψ 求导**的——`gradient(p_th, psi)` 除以零，**第一步就整条 NaN**。
   浏览器是靠 `psin = 0` 再算 ψ 拿到同一个数的。

3. **|F| 而不是带号的 F。** 条目的 psi 通道已经站在正 B0 那一侧（`solve_psi` 取
   `b0.abs()`，Redl 闭包也拿 `b0.abs()`），带号的 `F = R B_tor` 会把**两个场朝向**塞进
   同一个闭包。处方档一直给的是 `r0*abs(b0)`；追踪档现在说同一个朝向。★ITER 那张片
   B0 < 0，而**在此之前没有任何一档给过这个条目负的 F**。

★★★**批五之后：语料点名的能力一条不剩，两例的拒绝换了性质**（2026-08-27）。

`fylite cases --plan` 现在对这两例说的不再是「等下一批」，而是**「把那份参考件给
我」**——几何档、电流道、锯齿、驱动电流、参考剖面起步全沉了，缺的是**数据**：装置
与实验数据不入本仓（仓规），所以算例**带得动控件、带不动数据**。两句话不是一回事，
只有后一句读者今天就有下一步可做，闸子也按这个分：一条数据拒绝必须**说清楚怎么给**
（判 `equilibrium=` 出现在拒绝语里），不能只说「缺一份件」。

★这一批两件小事各有一处不肯将就：
- **驱动电流的形状不另写一份**。把内核已有的沉积闭包的权重改成参数（`deposit_on`），
  热按体积元、电流按面积元 `dA/dρ = V′/(2πR0)`，同一个高斯、同一个归一化。写两遍
  就是给中心或宽度留两个漂移的地方。`j_cd` 与 `j_bs` **分开报**：合成一行回答「有
  多少非感应电流」，毁掉「是什么把它放在那里」。
- **参考剖面逐通道取**。表里有 Te 没有 T_i，就把 T_i 留在控件放的地方——页面原话。
  一张只有 Te 的表若整体覆盖，会交回一个 `T_i = T_e` 的等离子体，没人测过也没人要过。
  一张**什么都不给**的表则**拒绝**：那样的「从参考剖面起步」是换个名字从控件起步。
  `provenance.reference_channels` 记下实际取了哪几条——三取一与三取三不该读起来一样。

★读法：一条能力关掉，其解锁例数就是当天 `fylite cases --plan` 少报的拒绝数。
基座那一条已兑现（6 例，5/13 → 11/13）。**余下两例要四条一起沉**（device/gfile
几何 + 电流通道 + 锯齿 + useref/icd），所以下一批的性价比与前一批不可同日而语——
排期时按这个事实排，不要照「还剩两例」直觉走。

★**双宿主对拍：条目级已关，装配级仍开**（2026-08-26）。

- **已关（循环本身）**：`test_both_hosts_march_the_same_discharge`——同一场行进
  在 **native（`libfylite.so`）与 wasm（浏览器那份构建）** 各跑一次并逐行比对。
  故意挑最刁的一场：12 步自适应 + 逐步重装源 + 杂质 ADAS 辐射 + D-T alpha +
  EPED 基座每步动边界。**实测最差相对差 5.5e-15**（非逐位——两份构建的浮点码生成
  不同，fma 收缩与 libm 都不同），闸设 1e-12；离散判定（步数 / 是否稳态 / 撞帽
  次数）要求**完全相等**——浮点带宽不能替不同的步数开脱。
- **已关（装配级，2026-08-26）**：`app/tests/validate-evolve.mjs` 的参考段原是嵌在
  JS 字符串里的**第四条**手写循环（自己重做逐步重装源 / alpha / 辐射 / 沉积，一步
  一次 `solve_core`）。现已换成**一次 `model.evolve()`**，且页面控件→入口参数走的是
  **语料用的同一个映射**（`cases.args_for`）——于是这道对拍成了「**浏览器循环 vs
  内核循环**」，中间只剩**一次**控件翻译而不是两次：单位错了会在两处同时错、被语料
  闸子当场抓住，而不是这边对那边错。**实测**：度规梯子两侧 **9.3e-17**（入口按页面
  自己的控件重建 Miller 度规，落回页面发出的那条梯子——这是新增的一条断言）、步数
  逐例相等、剖面 **1.1e-7 / 3.4e-7 / 1.2e-7**；容差随之由 1e-4 收到 **1e-5**（下限是
  会话文件 7 位有效数字被非线性行进放大，约 7 倍）。闸子 `全部通过`。
  ★**本环境这次跑得动**：playwright 1.56.1 与 chromium 都在（原记「缺 playwright」
  已不成立）。
- ★**下沉欠这道闸子的覆盖（记在这里，不藏在更窄的过滤器后面）**：旧的布尔过滤器
  比对 **4 例**，新的按 `evolve_heat` 的**实际范围**只比 **3 例**——`Miller · 热道`、
  `Miller · 燃烧（α）`、`Miller · 线辐射（Ar 1%）`。**丢的一例是
  `Miller · 热道+粒子道`**（密度通道未沉），其余按名跳过的都是 device 几何 / 电流道 /
  耦合 / 中子闭包——本来旧过滤器也不比。**关闭判据**：S-2c 沉下密度通道的那一批，
  这一例自动回到比对里（闸子不用改，`args_for` 不再拒即可），届时在这里划掉。

---

## 2. 裁定与规矩（不随条目关闭失效）

### 2.1 调试环境与场景语料（2026-08-26 裁定）

- **CLI 是主要调试环境**（`FYL-REPORT-03` §5 的「基准面」裁定的工作面化）：重放、
  验收、记录的正字法都在 CLI；其它面的记录主张以「能不能在基准面重放」为判。
- **主要场景以 fyo/JSON-LD 固化**于 **`docs/cases/`**（2026-09-01 由仓顶层 `cases/`
  折入，同处并入原顶层 `benchmark/` 的 V&V 登记册；`FYL-SDD-01` v0.11 布局表）：
  **一份语料、一组读者**——`fylite cases`（list / show / `--check`）与
  `fylite.scenario.cases`，以及书。★**浏览器不读**：同批撤掉页面的算例菜单，于是
  `app/cases` 符号链接、发布流水线的语料子集规则与桌面版内嵌的第三份副本一并消失
  （先前是三份副本各自可漂）。★语料是仓数据，**不随 wheel 分发**，与装置牌同一姿态；
  找不到时 CLI **拒绝而不是回空**——空清单会被读成「没有算例」，那是另一个事实。
- 语料的闸子现在两处：`fylite cases --check`（目录与盘上文件互指、逐条有人类可读名、
  每栏至多一个初始算例）与 `test_benchmark_registry.py`（登记册那一半）。
  ~~`validate-cases.mjs` / `validate-initial-case.mjs`~~ 随菜单撤销。

### 2.2 内核批的既定做法（v106 起十余批跑通）

新入口一次 bump、三份 wasm 与 `libfylite.so` 同批重建、既有签名一个不改；
唯一要提防的是多条分支同时往 `c_api.rs` 末尾追加新入口。

### 2.3 场景页三条通用规矩

- **闸子必须照 `scenario.js` 的链解析 id**（栏 → 部件 → 页）：用别的方式解析，测的是
  没有人访问的那一页——既会漏判，也会造出不存在的缺陷（T-C10 的教训）。
- **落格（snap）可以，夹持（clamp）不行**：被夹住的值是另一台机器，工具与闸子遇到就
  失败而不是写下去。★这条判据 2026-09-01 起由 `validate-design.mjs` 的会话往返段守
  （导入走的是同一条 `FySession.apply`）；原先守它的 `validate-cases.mjs` 随算例菜单
  撤销，T-C12 在那里挖出过一处静默夹持，教训照旧。
- ~~**初始算例绝不盖在读者已有的会话上**~~——2026-09-01 起没有初始算例了：每一栏都从
  出厂值开门，`fylite:seen:…` 与 `_browser.mjs` 的 `returningVisitor` 一并撤除。
  ★**不加任何测试专用通路**这一条不变，它管的是所有闸子。

### 2.4 FUSE 对拍的三条收窄（仍然管着后来人）

正本已移入 `docs/note/benchmark/`（规范 + B-01 报告），这里只留三条**裁定**本身：

1. **χ 仍是规定的**——本仓 χ₀ 标定自 ASTRA，从没为 FUSE 的任何机器重标过，所以
   「本仓输运复现 FUSE 剖面」这句话**不能说**；能对的是**通道**（同一状态喂两边）。
2. **台基那一层不是「两模型吻合」**——两边加载同一份 EPEDNN 权重，问的是移植没变形，
   所以判据取机器精度而不是物理带。它与「±20 % 的台基物理带」**靶不是一个东西**。
3. **不分发 FUSE 的任何东西**——引签名、文件名、commit 与标量；源码与数据文件不进仓。

### 2.5 I_p 控制器留下的五条（T-C16，2026-08-25 关）

1. **常数是认出来的，不是拟合的**：`I = V′⟨|∇ρ|²/R²⟩(dψ/dρ)/(2πμ₀)` 的常数从内核自己
   的电流扩散度规读出；判据把三个差 2π 幂次的候选一起钉住。
2. **两个宿主的 ψ 规范不同**（`fylite.fyo` 是 Wb/rad，浏览器是总磁通——分母差 2π），
   而「差了 2π」在剖面形状上完全看不见——闸子第一次跑就抓到 6.51 倍。
3. **反馈量控比值不控绝对值**：梯子的求积精度系统性低 3.2 % 且不随分辨率收敛；回路在
   第一步用自己的读数标定，**标定比报出来而不是折进去**。
4. **端点公式一阶不是二阶**——也是量出来的（二阶单侧在密排梯子上不随分辨率稳定）。
5. **「判不动」先要排除「我把参数调坏了」**：这一条推翻过自己一次（增益不当被写成了
   「被控对象慢」）。与 2.3 第一条同源、方向相反：那条防「把判词当证据」，这条防
   **把自己的手误写成一条关于物理的结论**。

### 2.6 装置数据页明确**不**复现的（裁定记录）

想重开其中任何一条，先看为什么关的：

- **Zoom/Point/Pan 三选一模式**：浏览器里三件事不互斥，加模式开关只会让它们互斥。
- **Print**（浏览器自己有）· **Copy to Clipboard 的 Swing 语义**（有时失败的按钮不如
  没有）。
- **`Multi_Source_Settings` 整条放弃**（2026-08-24 用户裁定）：四条路都查不实；已有更严
  的等价物（`--server`/`--servers`、页面服务器框、逐条打过服务器的
  `east-signals.json`）。哪天拿到物证且它管的是别的东西，那是**一条新待办**，不是挂回。

### 2.7 方法律：外部对照的用处

**律：逐条比一遍外部实现，照出来的最有价值的东西不是「别人有的我没有」，而是
「我有 / 我没有这件事自己没写清楚」。** 六次都是这样，每次的细节在各自的去处，
这里只留一句话与指针：

| # | 照出了什么 | 去处 |
|:--|:---|:---|
| 一–三 | T-C13 / T-C15 / T-C19：能力边界自己没写清 | changelog |
| 四 | T-C20：本仓三处写「没有杂质输运方程」，而测试里早就在跑一条 Z=6 的碳道 | changelog |
| 五 | JINTRAC/ASIPP 算例矩阵一次照出两行——**T-C25**（主离子再循环，缺口册两行都不覆盖）与 **T-C26**（二维 SOL，`FEATURE.md` 有声明而待办册无行：被声明过却从没被排过期） | 本表 §3 |
| 六 | TGLF 白盒对拍挖出**四个移植缺陷**（QR 特征值死循环 · FILTER 未移植 · KYGRID 4 未镜像 · 组装两处口径错），并把 wrapper 层的 0.6× 追到「喂进去的状态」 | `docs/note/benchmark/` V-01 · `docs/note/jintrac-flattop-reproduction.md` §8b–§8j |

★**两条从中长出来的常设规矩：**

* **近边缘运行点上的分歧不可归因**——那里能流本身小三个量级，参考自己换一个 ky
  网格也差 57 倍。要报差，先报它所在的量级。
* **`fylite cases --plan` 报的缺口是一份下界**——它只报得出本仓**有控件**的那些；
  EDGE2D / EIRENE / COCONUT 一个都报不出来，因为本仓连概念都没有。**缺口册与对照
  笔记不能被 `--plan` 替代，反过来也不行。**

### 2.8 为什么编排也要下沉（2026-08-26 裁定，问「是不是同一功能写两遍」的答案）

**物理层不会两遍**——`core_march` 双宿主同构、单步单源，页面 `evolveRun` 一千二
百行里所有物理都是 `fy.*` 内核调用。**但把外层循环用 Python 抄一遍就会两遍**，
而两遍编排正是本仓反复消灭的形状：transport 的 Picard 循环曾在页面侧（付了 3e-2
的离散差价才下沉）、0-D 的时间片循环曾在 Python（「每条公式都有第二个家」）、
判据也因「一条判据两个宿主就能自相矛盾」下沉。故裁定：

- 外层循环沉为**内核场景条目**，两宿主各剩薄门面（装配 + 呈现）；
- spec 即场景文档——与 `FYL-REPORT-03` §10 的场景 fyo 是同物两面，S-3 直接受益；
- 沉不下去的薄 glue 用**同 case 双宿主对拍闸**钉住（A-7 精神）；
- **JS 手写编排行数只减不增**（棘轮，闸在 `test_evolve_entry.py`）；
- 每沉一个能力，配一道「入内核循环 ≡ 显式逐步参考循环**逐位一致**」的闸，
  两个宿主各一份——写参考循环的成本就是这条纪律的价钱，付得起。

## 3. 物理与页面：正文

### 3.1 等数据 / 等标定

**T-D5′ · 工程限值还是空的。** `fylite:engineering_limits` 已进装置描述、往返不丢，但
没给任何内置机器填真数，每格报「未知」（绝不用缺省值代替，`FR-PULSE-004`）。EAST 有
真实电源数据（`east_device.yaml` `power_supply`，出处 TokSys `EAST_PS_params.m`），但
那是 14 个元件上的端子安培/伏特，页面要 12 条通道上的 kA·匝 / V/匝（含两条串联对，
权重 0.175/0.825）——换算要 `element_turns` 与 `pf_channel_elements`，会真出错，而
**错的限值比未知的更坏**。**关闭判据**：换算式经人核对后填入，逐通道对 TokSys 原表。

**T-A6 · 涡流组可辨识**：等一炮带逐道标定权重的真实数据。
**T-A7 · Thomson 本炮不可用**：等另一炮或另一套 TS 归约。
**T-A8 · MSE / SXR 层析几何**：不在任何卷宗里，等相机几何。
（三条的结构与接线都在等数据，判据即「数据到了能跑通既有闸子」。）

### 3.2 一次诊断：T-D19 —— **本机复现不出，两个自然解释各被一次实测排除**

**原记**：`validate-design.mjs` 的「限制器 bit-identical」在合并基线 `3f0f3c3` 上就红
且逐位相同（err 0.06064/0.05859 · q95 5.130/6.037 · PF1 292414.7/−1240809.3），
连跑四遍一样，不是抖动。

**2026-08-26 实测**（本环境头一次装得齐 playwright 与 chromium，浏览器闸子才跑得动）：

| 跑的是什么 | 结果 |
|:---|:---|
| HEAD，整道闸子，两遍 | **ALL GREEN**（含这一条） |
| **原记为红的那个基线 `3f0f3c3` 本身** | **ALL GREEN** |
| 三遍里 `dLim1` 的三个数 | **逐位相同**，且与原记的第一半分毫不差（0.06064 / 5.130 / 292414.7） |

**两个自然解释，各被一次实测排除**：

1. **页面状态泄漏**（两次求解之间某个控件被改过）——把该栏**每一个控件**在两次运行
   前各抓一份对比：差的只有 `xr2`/`xz2`（第二零点那一对），而 `限制器` 下
   `xWeight = 0`，`designRun` 的第一行就把零点集清空，**这两格不进求解**。形状滑杆、
   `ip`、`profsrc`、`gamma`、`passes`、`startmode`、`icap`、控制行数——逐项相同。
2. **闸子自己的起跑竞态**（点了「计算」而 `run()` 因为还忙着直接返回，读回上一次的
   状态）——把 `Worker.postMessage` 钩起来数命令：**每一次 `discharge` 的
   `runBar` 都真的发出了 `design`/`start`**（`ran ≥ 1`，`sawBusy` 全 true）。没有空跑。

★**因此本仓此刻并不带着这个缺陷**；原记那台机器上的差别，从这里无从归因（同一份
浏览器内两次运行共用同一套 `Math.pow` 之类的实现差异，所以「宿主浮点不同」也解释不了
**同机两次不同**）。

**本批做的（判据的第二条路：把判据改成它真正想判的东西）**：三条 bit-identical 断言
**失败时连输入一起报**。它们比的是六个数，红了只印那六个数——而唯一一次红是在一台
复现不了的机器上，于是那六个数就是证据的全部，且**不足以点名任何原因**。现在每次运行
前抓一份控件快照，红了输出 `控件差异: …` 或 **`控件逐项相同 —— 两次的输入没有差别`**
——后者是个**难得多的**主张，逼下一次红必须交代得更清楚。

**关闭判据**（改）：在**原机器**（或任一能复现的宿主）上再跑一次；红了，报告里那行
控件差异直接说出是哪一格，据此修；仍绿，则本条按「不复现」关掉并记入 `FEATURE.md`。
★**不因为本机绿就删条**——那是拿看不见当没有。

### 3.2′ 物理层四条红闸已清（2026-08-26）——并留下一条常设规矩

物理层（`tests/`）曾有四条红着、台账一条没记。逐条查完**没有一条是物理坏了**，四条讲的
是同一件事：**闸子钉住的东西已经不是今天的东西了，而没人发现**。经过与实测数记在
`changelog`，此处只留**不随条目关闭失效**的那部分：

- ★**手写 ABI 调用会烂掉，绑定不会**。`test_free_boundary_twin` 的调用是手列参数的
  `lib.fylite_rs_gs_free_solve(...)`，入口多一个可选指针它就成了 22 对 23，此后**每次
  运行都在第一条断言前抛 TypeError**——闸子看着在，其实一条也没查。**闸子里不再手写
  ABI 调用，也不按 `out12` 下标取值**；两者都走绑定。
- ★**「跑满上限」不是答案，是停下的地方**。那条闸子记着的 0.029 是一次**撞满 600 次
  迭代**的结果，而没有任何断言查过这件事。**凡驱动求解器的闸子，必须断言它到达裁决
  （`converged` 或 `settled`）且没耗尽 `max_iter`**。
- ★**别拿残差当收敛的替身**。内核给了三分裁决（T-M16）就是因为残差的下限可能是**掩膜
  量化**而不是求解误差。生产代码把裁决扔掉、只留残差，下游就只能猜——`evolution.py`
  的快照已补上 `iterations`/`converged`/`settled`。**问求解器要裁决，不要从它的某个
  输出里再推一遍。**
- ★**钉住「没有」的闸子会在「没有」结束那天变红，且说不出取代它的是什么**。
  `anisotropy is None`、`set(ft) == set(rs)` 都是这一类。**要么钉住新东西该满足的
  恒等式，要么把多出来的点名**——「丢了即失败、多出的必须具名」比一个 `==` 两头都强。

### 3.3 一条外向：T-C5′

**T-C5′ · 只差按下发布。** 公开仓 `main` 那一份是对的（逐项验过：样式全解析、零报错）；
差的是发布本身——workflow 是手动触发（2026-08-14 站点回滚后有意如此）。闸子
`validate-published.mjs` 已就位（sha256 逐字节 + 渲染 + 零报错；取不到站点退出码 3
与判红 1 分开）。**关闭判据**：按下那次手动发布后线上跑一遍，三段全过。★本环境出口
代理挡 `fusion-yun.github.io`，闸子在这里只能到退出码 3。

### 3.4 判据与声明：T-C15

**T-C15 · 壁与偏滤器热流。** 声明那一半已落（`FEATURE.md` §3.4）。本仓有打击点、最小
壁间隙与 X 点集，**到壁的热流一个都没有**——而打击点正是热流要落的地方。**关闭判据**：
找到外部算例（SOLPS 或同类公开算例）之后再谈模型——没有外部答案的热流模型，本仓规矩
不写。现在**只挡在判据上**。

### 3.5 模型（判据或数据未到）：T-C17 · T-C18 · T-C20（T-C24 见下，只剩去处）

**T-C17 · 聚变反应道只有 D-T 麦氏。** 挡在数据：只有 D-T 一组 Bosch–Hale 系数在仓里，
本机无全文库、`doi.org`/`iopscience` 出口 403——**凭记忆写 7 位拟合常数正是本仓规矩
不许的**。**关闭判据**：〔一〕新增反应道逐条对公开表值，容差沿用 D-T 那条的 2 %；
〔二〕束-靶项对一个带 NBI 的公开燃烧算例给出分通道功率，**热核支与现有实现逐位相同**
（新增不许动老路）；〔三〕域外返回零不外推，逐道各有测试。

**T-C18 · 射线追踪档。** 接口已留好：`ec_absorbed_fraction(tau)` 本来就吃任何来源的
`tau`，射线档只是同一个 `tau` 的另一个来源；`eccd_current` 刻意不假设吸收份额。
**关闭判据**：〔一〕对公开算例（GRAY / TORBEAM 类）给出沉积峰位与宽度，容差先量再定；
〔二〕闭式档与射线档**同时在**，页面说明白哪档在跑，不许静默顶掉；〔三〕闭式档那串
拒绝（截止之外 · 束打不到层 · 层不在机器里）在射线档里**只能变严不能变松**。
★全波不跟（§3.4 已声明）。

**T-C20 · 杂质的余下两半。** 接线已落（2026-08-25，两种离子都是道时 Z_eff 是结果）。
余下：〔源〕溅射 / 注入 / 壁再循环——没有外部答案的源模型不写，先找算例；
〔电离态〕本仓 ADAS 是日冕平衡有效冷却率，上游解逐电荷态输运——**关闭判据**：拿一份
带多组分逐核素输运系数与源的外部算例，比逐电荷态分布与总辐射，容差先量再定，
**日冕档保留、两档同时在**。

**T-C24 · 芯部中性与 CX。★机制已落，判据已在手；余下的一半并入 T-C25。**
`neutrals.rs`（1-D 柱 Monte-Carlo，ABI v116）已在仓，对 EIRENE 的通量面平均列按带
验收（门 `tests/test_neutrals_oracle.py`；数与账见 `docs/note/benchmark/` 与
`docs/note/weiland-bgb-eirene-feasibility.md` §5）。**本条余下的只有一件：
边界源谱**（冷主体 / 暖尾的混合比）——那是 SOL/再循环模型才给得出的东西，**记在
T-C25 名下，不在这里重复排期**。本行保留只为记去处。

### 3.6 机制与搬运：T-C21 · T-C23

**T-C21 · 处方输运回放。** JINTRAC 那一路上唯一「今天就能判」的对拍：本仓的规定 χ 档
天然就是「吃给定 `χ/D/v` 在给定几何与源上推进」的入口，而上游那侧要打补丁才跑得通
——构成真正的三方比较。★2026-08-28：机制已在**作业 101612** 上实跑——jsp 记录的
χe 回放 Te 全剖面 6.67 %（零假设 41.5 %，dt 已收敛；门
`tests/test_jintrac_case04_replay.py`，笔记 jintrac-case04-101612-reproduction）。
本条自己的关闭判据（下述公开算例的 1 ms 四项门限）不因此改动。外部门禁数已印出（1 ms 级 `n_e` 2.72 % · `T_e` 1.91 % ·
`T_i` 2.89 % · `q` 1.17 %）。**关闭判据**：1 ms 一级四项均 ≤ 5 %（与上游同一门限、
同一相对 L2 定义）；10 ms 一级**如实报出**——上游在这一级是 FAIL，不预设本仓该过。
四条口径先对齐：COCOS（T-C22）· 哨兵值按通道屏蔽 · 插值口径与上游 driver 一致 ·
核素对齐。挡在一次数据搬运：站点数据先冻成派生表。

**T-C23 · 守恒误差账。** `jetto.chksum` 的对应件。**★2026-08-26 关（Python/CLI 侧）**：
`transport.rs` 把面量构造抽成一处（`faces`——`assemble` 与新的 `heat_balance` 共用，
否则守恒检查会去检查一套求解器不用的离散），`heat_balance` 按算子**自己的**面通量与
控制体算一步一通道残差；`evolve_heat` 逐步累加，报每步与全程最差，进 `RunManifest`
与验收（`balance_worst`：pass 1e-10 / warn 1e-8，**带按实测定**）。实测全在机器噪声：
单例 2.1e-14，**全语料 11 例最差 9.8e-13**（400 步级）。闸两道：crate 内单例 + Python
侧全语料横扫（并断言「真的审过步」——全跳过会报出完美的零，那是唯一不可信的答案）。
★**这条闸当场先抓了自己**：初版用 `res.dt`（控制器给**下一步**的 dt）而不是这一步实际
跑的 dt，残差读回来正好是控制器的调整因子（首步 0.5＝因子 2）——账没错，除数错了。
★**重试的步不审**：控制器把那次尝试丢掉并内部减半，其 dt 从外面叫不出名字，故不审而
不是拿猜的除数审（重试次数照样在记录里）。**开着的半边**：会话文件（浏览器导出）那一路
随 S-2b/web 面走，本裁定优先物理侧，故不在此批。

---

### 用户指南（`docs/guide/` 的公开子集 → `app/guide/`）

新落地：面向发布的用户指南，源是 `docs/guide/` 里 `public.yml` 点名的那五篇，由
`tools/build-guide.sh` 用 MyST 的解析/渲染库编译成静态 HTML 落进 `app/guide/`，
套站点自己的外壳，随浏览器演示一同发布；门 `app/tests/validate-guide.mjs`
（同步 + 资源引用 + 不引仓内私有路径）。★2026-09-01：原独立的 `docs/user_guide/`
收编进 `docs/guide/`——发布面不同这件事仍然成立，但它由 `public.yml` 那张表管着，
不必是两本书。

★缺口，各带关闭判据：

- **英文版缺失**。中文页页脚已列入口，英文页**不列**——把英文读者送进一页中文
  比暂时不给入口更糟；`site.js` 里以 `langs: ['zh']` 显式声明。
  关闭判据：`public.yml` 点名的那几篇出英文一套，`langs` 加 `'en'`，站点门照旧绿。
- **不走 `myst build --html`**。那条路要从 `api.mystmd.org` 下载站点模板，
  给发布链加远端依赖，且 book-theme 与本站设计系统无关。现用 MyST 的
  `myst-parser` + `myst-to-html`（npm，可离线缓存）。关闭判据：若将来要用官方
  主题，先解决「离线可重建」与「与站点外观一致」两条，否则维持现状。
- **指南内容深度**。现在五页覆盖「怎么用 + 结果怎么读 + 边界」，没有逐工具的
  参数说明。关闭判据：三个场景页各有一节讲清它的输入与判读。

## 4. 排期备注

- `A-` 序与 `G-`/`R2-` 序的正文**不在本文件**（判据正本分别在 `FYL-REPORT-03` §8、
  `FYL-REPORT-01` §14.6、`FYL-REPORT-02` §5）；本表是唯一排期面，关一条两处同批。
- A-1/A-2 是层间对齐，性价比最高；A-6 里的 **G-21 须人拍板**（origin 决定）。
  ★**A-3 与 A-7 已关**（2026-08-26）。A-7 关的时候顺带定下一条规矩：**「必须逐位
  相同的是哪几行」是关于物理的事实，属内核声明**（`ENTRY_OUT_KIND`），不是闸子
  自己维护的名单——新条目的整数型输出不声明，生成的闸子当场红。
- 物理侧真能动手的几条里，T-C22 → T-C21 有依赖。★T-D19 **不再是「随时可做的
  纯诊断」**（2026-08-26 改）：本机三遍复现不出，要的是另一台宿主——排期时按
  「等一次复现」看待，别再当作一条动动手就能关的。
- ★**evolve 执行器裁定**的缘由与常设规矩已移入 §2.8（不随条目关闭失效）；排期落在
  §1.4 的 S- 序：**S-2c 是下一优先**（按解锁例数排，基座一条抵六例），S-2b（JS 面
  切换）**改期到它之后**——2026-08-26 实测：现在切让 JS 增行不是减行，循环要能整体
  离开得先沉够。这条顺序颠倒本身是实测的产物，写在这里免得下次又照旧序开工。
- ★**2026-08-26 物理先行裁定**：S-2（CLI/Python 物理场景校验）是**下一优先**；
  一切 app 接线（S-3，含 A-2 与 P1..P3）推后至 S-2 之后。A-1 不受影响——语义
  表化是层间工作，且是 S-2 映射对账的助力。S-1 开着的半边（页面导出往返）随
  S-3 走，不单独排期。
