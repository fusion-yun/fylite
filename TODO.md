# TODO — 仓级开放任务台账

登记 2026-09-06，**E 组 2026-09-08 重写；同日晚二次清理**（收官批：`B-08` · `B-10` · `C-11` · `C-10` 落地，`G-14` 修掉）；
**同日 D 组续做**（EAST 标准算例：E-7 · E-8 立项，E-2 清单短一截，新 `F-14`）；
**2026-09-10 E 组增 `F-15`..`F-23`**（起始设计岭缺省一役量到的九条：两条未决的求解器行为、三处闸子长期红或根本没在跑、两处数据缺口、一处构建不可复现、一处路径残留）。本表收**本仓及其上下游**当前未完成的事项——之所以合在一处，是因为它们
多数是同一条链上的：内核声明面 → fyo 本体 → fydata A-Box → fydoc 事实层 → 本仓消费。
分散在五个仓的开放项各自成立，但**谁被谁卡住**只有并排看才看得见。

**归属列是硬约束**：标 `fyo` / `fydata` / `fydoc` 的条目**须按各该仓的治理落地**，不在本仓
改；登记在此只为让本仓知道自己在等什么。标 `kernel` 的是 `fylite_kernel`，标 `fylite` 的
才是本仓自己的活。

---

## 0. 合并后任务清单（2026-09-11 归纳）

★★**本节只归纳，不作真源。** 每格末尾指向下面各表里的那一条，两处不一致时**以那一条为准**；
按〈记法〉本表只登记事实与依据，归纳不新增事实。★立此节的由来：2026-09-11
`origin/develop` 并入射频线 `2e8684d`（EC 的 ③ 伴随 ECCD · 锥形射线族 · L3 束追踪 ·
BORAY 本地 oracle · `core_sources` 守恒落梯子）之后，开放条目跨了两条互不相干的线，
并排列表已读不出「谁在等谁」。按用户指示分三组。

### 0.1 射线追踪 L1–L3（EC + LH）

★**分层的用语**：内核 EC 页只命名 **L2**（单射线 / 束中心线的冷等离子体轨迹 + 沿射线吸收 +
伴随法 ECCD）与 **L3**（束宽演化 · 衍射 · 聚焦）。**L1 是本节给「射线追踪之前那一层」起的名**
（闭式光深 + 效率模型 + 规定沉积表），不是内核页里的既有术语 —— 记在这里免得两处对不上。

| 层 | 合并后已实现（判据已过） | 余下 | 条目 |
| :--- | :--- | :--- | :--- |
| **L1** 闭式 / 表格 | `heating::ec_optical_depth`（已按 Bornatici 1983 原文表 IV 更正重印之误）· `eccd_efficiency`（对 METIS 认证存档 30 行：ours/METIS 0.026–0.64，中位 0.35，log 相关 0.976）· `sources = table` 的规定沉积 | 弱相对论极化选项（疑为 X2 对 GENRAY 晚 12 mm 之因）| F-31〈EC 残项〉 |
| **L2** 射线 | EC ①冷射线核 · ①b 托卡马克介质与边缘反射 · ② 相对论吸收与 ψ̄ 壳沉积 · ③ 伴随 ECCD，经 `code/rf_ray` 的 `deposit` / `current_drive` 接出；`core_sources` 按 `code/beam` 守恒算子落到绑定梯子（瓦数对壳 1e-9，可直接喂 `sources = table`）；锥形射线族 `rf_rings` / `divergence` 作 L2 束宽。LH ①①b 已实现（对私有 GENRAY EAST 2.45 GHz 四射线过 B 类）| ★**LH-② 2026-09-11 做完**（`rfray::lh_landau_alpha`，闭式；公式由 Maxwell 纵向介电函数**从头推出**，不抄原文，推导在内核 `docs/note/lh-raytracing.md` §4.3）：四道判据全过 —— 温度依赖逐位等于 ζ³e^{−ζ²}（差 < 1e-12）· ζ 与 `heating::lh_resonant_te` 同义（< 1e-12）· 冷极限指数归零且三种「无可阻尼物」给 0 不给 NaN · **谱隙量出来了**：5 keV 上 N∥ 1.5→4.0 跨 2.1e6 倍，格栅峰值 N∥≈2 处 α 0.138 1/m（7.3 m 才 e 折），故**② 单独给的是吸收下界**。★**LH-③ 同日也做完**（`rfray::lh_fp_solve` + `LhFp`）：定态由总平行流为零给出，是一条累积积分不是矩阵；驱动效率的闭式 η → (u2²−u1²)/(4 ln(u2/u1)) → 窄带 u0²/2 **由同一模型推出**，四个带实测差 < 2 %。★**一次真失败改出来的拒绝**：[6,7] 不随网格收敛（21.79 → 16.5）、[7,9] 给 3.7e5 —— 诊断为驱动电流落到求和舍入之下（e^−49 对 1e−16），于是 `efficiency()` 在 |J| ≤ 1e3 × floor 时**拒绝报数**，余量由实测定（接受侧 ≥ 6.5e4，拒绝侧 7.1 与 0.44）。★一维模型没有 (5+Zeff) / 陷俘 / 径向输运，**比趋势不比数值**。★**②↔③ 当日闭合**（`lh_landau_alpha_on`）：吸收可读在任意平行分布上，推广式代入 Maxwell 即回到 ② 的闭式（该恒等式作判据，4001/8001 点差 4.44e−4 / 1.30e−4，比 0.292 对上 du² 的 0.25，量值与收敛比一起卡）。实测（5 keV，平台 u∈[4,6]）：共振落在平台内 α 降到 Maxwell 的 **6.5e−5**（**平台是透明的**），落在平台上沿外 α 升到 **5.0e8 倍** —— 自洽吸收取决于波此前把功率放在哪里，这是「② 是下界」的兑现。**余**：把回代接进 `trace` 的循环迭代到自洽 · **LH-④ 接门与参照**（设计已定、全文缺三篇）· 离子多于 4 种的归并规则（现按名拒绝）· 公开物理页 `09-heating` 只同步到 ② | **F-30** · F-31 |
| **L3** 束 | 笛卡尔 3×3 复曲率 Riccati 方程加约束（Pereverzev 1998 · Poli 2001 的本仓写法；3×3 自带 2×2 约化漏掉的射线弯曲项）· `trace_beam` · `ec_deposit_beam` · 门 `beam_waist` / `beam_focus` / `beam_nodes`；**七条解析判据**（真空像散束 1.1e-8 · P98 §VI C 的 O/X 6.3e-8 / 2.8e-8 · 抛物通道 · 线性层九元全比 1.3e-8 · 束对邻射线 Jacobi 恒等式 7.9e-6）| B 类实算对标（TORBEAM / GRAY）**无可复算输入** `[TBD]`；无像差阶不含窄束的 N∥ 谱展宽与截面不均吸收 → 要不要做 GRAY 式射线族**待裁定** | **F-28** · F-31 ④ |
| **参照件** | GENRAY EAST O/X（私有 B 类）· BORAY 本机 Octave oracle（BSD-3，钉提交）· METIS 30 行 ECCD 带 · ★★**TORAY 全套 oracle 到手**（`FYDOC-CASE-21`：输入 + 名单 + **TORAY 自己的输出 nc**；冻结件 `cfedr_toray_20ma.txt`，判据 `tests/test_cfedr_toray_oracle.py`；驱动电流两侧差 **9 %**、轨迹差 ≤ 4 cm、吸收 1.0000 对 0.9990）| ★它当场量出的符号缺陷**同日修好**（F-33 关闭）：$\sigma$ 改为从文档推，修后 $N_\parallel$ 差 **1.5e-4**、轨迹差**亚毫米**、驱动电流比 **1.046**。余：EAST/GENRAY 夹具作第二检验 | F-27（i）· ~~F-33~~ 均已解 |
| **待裁定** | ★★**2026-09-11 全部裁定**（用户）：① 提 fyo 治理（提案 v1 已起草 → 本表 **O-4**）· ② LH-② 取 (a) 电子 Landau · ③④⑤⑥ 维持现状（LH 的 L3 不进表 · GRAY 式 N∥ 谱射线族暂不 · ECCD 香蕉区外修正在 L2 之外 · 多于 4 种离子仍按名拒绝）| 余下只有**弱相对论极化选项**：两篇全文不在容器，本会话做不了（不编公式）| **F-31** · O-4 |

### 0.2 复现 CASE-20（`FYDOC-CASE-20`；真源是内核 `docs/note/cfedr-15ma-reproduction.md`）

★★★**2026-09-11 收口陈述：复现做完了，余下每一条都卡在「交付件缺席」上，没有一条卡在模型上。**

| 层 | 复现到什么程度 |
| :--- | :--- |
| **几何（S1）** | 度规六量中位全部 |中位| < 2.5 %，最差随梯子加密减半直至收敛；**余差追到底，底在交付包**（见下 S1 行） |
| **源项（S2）· 闭包（S3）** | 逐项对上；③ 逐位同上游 NEO |
| **通量匹配（S4）** | 匹配区逐点 ±3.5 %，全档首次 `converged 1`（最差残差 8.06e-3）；解在给定起点与预算下唯一 |
| **外环（S5）** | 同域对照 W_th **−1.65 %** · P_fus **−0.11 %**；全域差（W_th −8.6 % · P_fus −18.3 %）**已归因**到匹配区**以内**那段未求解的核心 |
| **敏感性（S6）** | 饱和规则方向 · 台基密度三点，均复现 |
| **EC 对拍** | 驱动电流 **+1.7 %** · 吸收 71.94 MW 单程全吸收 · 峰位 ρ 0.3588 对 0.3450；rms 宽窄已量明是束发散角（≈2.1°） |

★**余下四条 `[TBD]`，全部是缺交付件**：`inone`（ONETWO 不能重跑）· `input.tgyro`（求解半径与选项）·
2.3 % 聚变功率差 · **S1 那两列 q 各自的来历**。**没有一条需要改模型。**

★**S0–S6 的逐步状态在内核那页**，本表只登记**卡住的**与**跨仓的**。合并后：

| 件 | 状态 | 余下 | 条目 |
| :--- | :--- | :--- | :--- |
| S0 参照冻结 · S1 几何 · S2 源项 · S3 闭包 · S5 外环 · S6 敏感性 | 跑完 | — | 内核页 §4.1–§4.2 · §5 |
| S4 通量匹配 | 匹配区逐点 ±3.5 %；★**2026-09-11 首次 `converged 1`**（热支在全档续跑十六轮，最差残差 **8.06e-3**）| ★**S4 对照已跑完**：平坦起给够轮数**收敛回交付支**（Q 24.10 → 12.42，落在交付支 12.20/12.32/12.46 那一族 1.8 % 内）⇒ **就这两个起点与这个预算而言解是唯一的**；精简档一度看到的「两支」判为档位效应（Q_i 偏高 29.9 % × α 正反馈）。**余**：匹配区**以内**那一段仍由内边界给（本趟轴上 T_e 15.54 对另一趟 24.08 keV），与 S2 源项一起承担对参照的 W_th −4.2 % / P_fus −17.6 % | 内核页 §5.1–§5.3.1 |
| **EC 对拍**（本案例最后一条未做的逐点比较） | ★★**2026-09-11 做成**：对交付的 `EC_3`，驱动电流 **+1.240 对 +1.219 MA（+1.7 %）**、峰位 ρ 0.3588 对 0.3450、吸收 71.94 MW 单程全吸收 | ★**rms 宽那条差已量明**：扫锥的发散角，**≈2.1°** 上 rms 对上（2° 0.0379 · 3° 0.0469 · 交付 0.0388），而峰位 · 吸收 · 电流几乎不动（0–3° 电流 1.240 → 1.233）—— 差的形状是束宽，不碰主结论。★负结果：`echin` 第 12 槽（两例都是 1.2）若是以度计的发散角则预言 rms ≈ 0.032，对不上，故**不是**它，仍 `[TBD]` | 内核页 §1.9 · `tests/test_cfedr_ec_against_the_delivery.py` |
| ψ̄ 归一 | 判明：两侧分母差 **1.56 %**（TORAY 侧用 ψ 图真跨度 15.9199，本仓用 statefile 一维网格 15.6752）| EC 逐点对比前必须统一；单独可解释约 0.003 的峰位偏移 | **F-32**（b）|
| ~~S1 的两个待归因项~~ | ★★★**2026-09-11 收口。** 「体积亏 3.57 %」早判非偏差；压缩这个候选实测排除；「中位符号翻了」判为记录抄错（在旧提交上重编重跑，逐位相同）；两个中位先证明**不是离散化**（481/961/1921 层四位不动），再由 dρ_N/dψ_N 高 0.826 % 解释掉 `gcap` 的 82 % | ★**追到底了，底在交付包里**：$\rho_N=\sqrt{\int q\,d\psi/\Phi_{\rm edge}}$ 只依赖 q，两侧都自证用的是这个定义（1.3e-4 / 7e-4），于是 ρ 差**全部**是 q 差；而**梯子的 q 不是本仓算的** —— 收的是 statefile 的 `qpsinpsi`，它与 ONETWO 剖面表的 `q` **差 −1.47 %，两列都是交付的**。★预言落空并被钉住：改喂剖面表的 q，`q` 中位没塌（−0.699 → −0.846 %），`gcap` 减半而 `hcap` 差近一倍 —— 比较在匹配 ρ_N 上做，而 ρ_N 自身差 0.493 %，**比较坐标把 ρ 差带进了每一个量**。**余下的度规差不是本仓的一处缺陷**，库内无任何选择能同时归零；判它需设计方说明那两列的来历，**与 `inone` / `input.tgyro` 同类 —— 缺的是交付件不是模型**（`[TBD]`）。仍开：`hcap` 补偿后残下的 −0.583 %，与该不一致纠缠，不单独可追 | **F-32** |
| 交付包的第二份平衡 | 判明：交付 `gfile_efit` **不是这次运行用的平衡**（ψ 图逐点差到 0.320 Wb/rad ≈ 2 % 跨度、磁轴 7.91 mm、`psiaxis` 1.20 %），**而本仓链条一处都不读它** | 对**交付件使用者**是事实（已写进 fydoc 案卷页）；对本仓不是阻塞 | **F-32**（已降级）|
| 缺席件 | — | `inone`（ONETWO 不能重跑，自举模型开关无从切）· `input.tgyro`（TGYRO 实际求解半径与选项 `[TBD]`）· 2.3 % 聚变功率差 `[TBD]` | 内核页 §1.7 |
| 迭代提速（工具，非结论）| 内核 `1dffef4`：`FY_S4_*` 保真度旋钮（便宜档）· 热启动 `FY_S4_DUMP` / `FY_S4_START_FILE`（每轮落盘）· 半径调度改动态取号 | ★**2026-09-11 标定已跑**（`s4_control_the_cheap_fidelity_against_the_full_deck`）：全档 92.5 s / eval · 便宜档 29.8 s（3.1×，Q_e 偏置 −4.7…−22.9 %）· 精简档 4.5 s（20.5×，+0.5…+10.9 %）；精简档 120 次 eval / 297 s 的落点与全档差 **0.7 % · 1.2 % · 1.1 %**（W_th · P_fus · Q）。**「用精简档找解、用全档定值」已量过**；逐点偏置仍须随数引用 | 内核页 §5.1 · `cfedr_s4.rs` 抬头 |

### 0.3b 杂质与 Z_eff：向导出量收窄（2026-09-12 立，用户选定范围 A）

★**现状（评估 2026-09-12）**：给定 $Z_{eff}$ 剖面的多组分闭合与辐射（日冕 ⟨Z⟩ 覆盖 He/Li/Be/C/N/O/Ne/Ar，ADAS 冷却 22 种，未知种辐射为零不报错）满足复现需求；**预测 $Z_{eff}$** 缺源与输运。
★**范围 A 已落**（内核 `impurity_transport`，`docs/note/cfedr-15ma-reproduction.md` §5.6）：一种注入杂质的定态输运剖面（$D$、$v$、边缘、源为设定），电荷日冕，$Z_{eff}$ 导出；三条判据（极限/守恒 · 夹具回归 · CFEDR 轮：平氩钉在 ONETWO 边缘值给 $Z_{eff}$ 中位 2.85、含量 2.11 倍——ONETWO 的氩是空心的）。**无 oracle**（ONETWO 给定 $Z_{eff}$ 反解氩）。
★**$D_z$ 随 χ 已挂**（`imp_d_over_chi = r` → $D_z = r\,\chi_{turb}$，eval/finish 阶段重解；χ≡1 夹具下与常数 D 逐点差 0.000e0）。
**余**：范围 B（He 灰通道）· W 的电荷矩 · $v_z$ 与新经典 $D_z$ 由闭合给出 · 未知种改按名拒绝 · 台基 $Z_{eff}$ 取台基值（B′ 已做）。

### 0.4 台基：向 EPED 收窄（2026-09-11 立，用户指示 A/B 进计划、C 待评）

★★**2026-09-12 oracle 找到了**：`FYDOC-CASE-20` 交付包的 `EPED/eped_state.nc` 是 EPED 自己的运行（十输入 · 答案 · KB/PB 扫描表），冻结为内核 `testdata/reference/cfedr_eped_15ma.txt`，门 `pedestal::tests::eped1nn_against_epeds_own_run_on_cfedr_15ma`。实测：EPED1-NN p_ped 93.0 kPa 对 EPED 110.7（ptotped）/ 115.8（p_E1）kPa，即 **−16 % / −20 %**；宽度 0.0448 对 0.0231（ptotwid）/ 0.0508（wid_E1）；外推距离 0.313 落在 a = 2.5 m（**CFEDR 在代理训练盒之外**）。计划 A 的逐高度 `gamma_KB` / `istable_PB` 表随之可用（内核 `docs/note/cfedr-15ma-reproduction.md` §5.5）。

★**现状**：本仓有的是 **EPED1-NN 代理**（`rust/fylite/src/pedestal.rs` 511 行 + 生成的权重表，
九组解，九道判据），不是 EPED。EPED 是**两条约束在一族自洽重建的平衡上求交**：
非局域剥离-气球稳定性（ELITE）与 KBM 起始 $\Delta\psi_N = 0.076\sqrt{\beta_{p,\mathrm{ped}}}$。

★**已量的差**（内核 `docs/note/pedestal-literature.md` §4）：压强比 0.850 / 0.910 / 0.859，
宽度比 0.945 / 0.965 / 0.826，$\beta_{N,\mathrm{ped}}$ 0.833 —— **系统性偏低，但落在论文自陈的
±20 % 之内**。所以「现有实现不够用」**不能由这个偏差论证**。

| 档 | 内容 | 状态 | 靶 |
| :--- | :--- | :--- | :--- |
| **A** | KBM 宽度约束 + **无限 n 气球**（沿场线一维 ODE 本征问题） | **2026-09-12 已落**：`pedestal::salpha_boundaries`（s–α 边际方程，s=1 → 0.612/2.604）；EPED 自己的 G 由表读出 0.072–0.078（E1 行 0.0744）；E1 台基逐面定位：两肩穿理想带、陡区第二稳定 | **A 量出的边界**：理想判据不定高不定宽，高度须 P-B（C）或交付表 E1 行 |
| **B** | A + **EPED 外环**：扫（高度, 宽度）族 → 每点重建含 $J_{bs}$ 的 GS 平衡 → 两约束求交 | **计划中** | 端到端对已发表的 8 个数 |
| **C** | 真 P-B（中 $n$ 全局本征值 + $\omega_*$ 抗磁稳定化，ELITE 做的事） | **评估结论见下：先找参照件，不先写求解器** | ✗ 容器内无中间量可对 |
| **B′** | 把外环里**冻结的三样**放进去（$n_{e,\mathrm{ped}}$ · 形状 $\delta,\kappa,a,R$ · $Z_{\mathrm{eff}}$；现在只有 $\beta_N$ 在迭代，且是显式一步滞后） | **2026-09-12 已接**（opt-in `eped_inputs = state`：态的边缘密度 · 物种 $Z_{eff}$ 边缘值 · 梯子边缘 Miller 形状；$a,R$ 不动；β_N 仍一步滞后）。判据：边缘 $Z_{eff}$ 2→3 使 p_ped ×1.056、宽度 ×1.127 | CFEDR 链上的落点移动待 S6 重跑 |
| — | **T-C8 口径差**：本仓送 $\delta_{95}/\kappa_{95}$ 与体 $Z_{\mathrm{eff}}$，上游不一定同源 | 开（跨仓） | 口径未对齐前，第二层那 9–17 % 的归因不可判 |

★★★**C 的进一步评估（2026-09-11）—— 结论是「先找参照件，不先写求解器」。**

**(1) C 到底要算什么。** 边缘对**中 n**（约 5–30）理想 MHD 剥离-气球模的线性稳定性，
判据是增长率对抗磁频率：$\gamma > \omega_*/2$。ELITE 用 $1/n$ 渐近展开（保到 $O(1/n^2)$）
在场线对齐坐标上把它化成一族按极向谐波耦合的径向常微分方程，解广义本征值问题。
**关键**：EPED 的全部要点正是那条**耦合**边界与「纯气球」或「纯剥离」都不同 ——
所以把 A（无限 n 气球）当成 C 的便宜替身**在物理上不成立**，A 只能作自检不能作替代。

**(2) 缺的不止求解器。** 还要 (a) 台基处**远高于输运梯子**的边缘网格与精确度规
（$|\nabla\psi|$ · 局域剪切 · 曲率）；(b) 台基分辨的边缘自举电流（本仓有 Redl 点模型，
但台基内的精度是另一回事）；(c) $\omega_{*i}$（现成）。

**(3) 真正的瓶颈是判据，不是代码。** 容器内可用的靶只有 **8 个端到端数**
（$p_{\mathrm{ped}}$ · $\Delta$），而且论文自陈 ±20 %。一个 P-B 求解器的内部自由度
（n 谱的取法 · 分辨率 · $\omega_*$ 系数 · 边缘电流的处理）**远多于 8 个带 ±20 % 的数所能约束的**。
业界验它用的是**稳定性图**（$\gamma/\omega_*$ 对 $n$；$j_{\mathrm{ped}}$–$\alpha$ 图），
本仓没有可复算的数字版 —— 这是 `[TBD]`，也是本条的真阻塞。

**(4) 本仓自己的经验说得更直白。** 射频线的 F-33（$\sigma_{B_p}$ 符号缺陷）**是因为有
TORAY oracle 才被当场量出来的**；没有 oracle 之前，同一份代码带着那个缺陷过了所有自洽判据。
一个没有参照件的 P-B 求解器**是不可证伪的**，写出来也不能用来判任何事。

**(5) 因此排序**（我的评估，非裁定）：
1. 先做 **B′ → A → B**（都有靶，且 A 可作 C 的**自检**：若代理给的台基离本仓自算的
   无限 n 气球边界很远，那本身就是一条可检出的不一致）；
2. 与此并行，**调查有没有授权相容、可作 oracle 的公开 P-B 码**（照 BORAY / TORAY 的先例），
   结果记进本行；
3. **只有在 (2) 有结果之后**，C 才谈得上立项，且须先立 ADR。

★**本仓已有而 EPED 需要的**：平衡重建（`equilibrium.rs` 2902 行 + 固定/自由边界 GS 求解器）·
流面几何与度规（`surfaces.rs` 2927 行）· 边缘自举电流（`neoclassical::redl_bootstrap_point`）·
KBM 闭合（一行，且已有判据证明代理满足它）。**缺的只有稳定性求解器**。

### 0.3 其他（按被谁卡住分）

| 组 | 条目 | 一句话 |
| :--- | :--- | :--- |
| 声明面 / 本体治理 | K-1 · K-2 · K-3 · K-4 · O-1 · O-2 · O-3 · E-3 | 裸路径的叶与首段不在 DD、两侧节名与单位写法未约定、四张内核自有表无本体承载、测量不确定度无 spo 承载 —— **都不在本仓改** |
| 需数据访问 | E-2 · E-7 · F-8（可发信）| 本炮加热 / 杂质 / 输运只在 MDSplus；EAST 本炮两个平衡互不相容；`C-08` 的 ν* 与逐半径 Miller 参数要向作者索取 |
| 语料与发布闸 | E-4 · E-6 | 五面态是计算产物不是测量（`dev:result` 七取值待裁）；`check_cases.py` 28 处 / 19 组红，其中 14 组等 reviewer 与联系方式 —— **按本仓规矩红着等人给，不许编** |
| 本仓功能与判据 | F-1 · F-2 · F-9 · F-12 · F-14 · F-16 · F-18 · F-20 | `discharge-iter` 的门 · 生成件里手写段活不过下一次 `--write` · `code/breakdown` · `B-01` 是否改在 FUSE 1.1.5 重跑 · POINT 条纹清洗只设下界 · 自由边界不收敛且不随预算单调 · node 五道闸长期红（缺数据 / 夹具过期 / 真错三者未分）· 真机参照只有 EAST 一台 |
| 构建与制品 | F-21 · ~~F-22~~ | F-21 差的只是**另一台机器上那两个制品文件**（判别法与工具链戳已落）；F-22 2026-09-11 关闭 |
| 装置 / 文档 | F-10 · F-13 · F-19 | 生成的装置清单指向不存在的路径 · `todelete/` 余下未判 · 装置文档不声明线圈工程限值 |

---

## A. 内核声明面（`fylite_kernel`）

证据：`fyo` 仓 `FYO-REPORT-05` v0.5（2026-09-06，对内核检出 `08d0c0b` 实测）。三条都**不会
让任何脚本非零退出**——这正是它们至今还在的原因。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| ~~**K-1**~~ | kernel | ★★**2026-09-11 关闭。** 六条裸路径的叶在 DD 中不存在，已在内核 `fyo.rs` 逐条加 `fylite:`，接口修订 **1 → 2**（改 path 必须升号）、摘要随之更新，公开仓 `test_fyo_paths_have_a_dd_home.py` 的基线按它自己的规矩删掉这六行。★**顺带推翻了一条记录在案的选择**：真空室那四条此前特意留在基线里，理由是「查的是声明表，这个量在 DD 里确实没有家」—— 那把两件事混了：「没有 DD 归宿」是事实，「所以路径必须带 `fylite:`」是内核自己的规则，两者不冲突而后者才是修法；佐证是 `fylite_runtime` 归一化时**原矩形本来就挂在 `fylite:geometry` 下**，即那个裸 `geometry` 指的路径真文档里一处都没有。★**余一条未答**：`core_transport` 的 `rho` 与 `rho_d` 是不是同一条网格（是就该合并）—— 本轮只修好了名字，这个问题仍开。★另：「`vacuum_toroidal_field/b0` 声明 0d 而 DD 记 1 维」**判为非缺陷**——本表的 rank 指一个时间片内的秩，同形的还有九个槽 | 内核 `fyo.rs` 抬头 @fyo-revision |
| **K-2** | kernel | ★★**2026-09-11 判明「加 `fylite:` 前缀」不是它的修法，故未改，条目仍开。** 首段确实不是 84 个 IDS 中的任何一个，但**真装置文档里这十六条的键一个都不存在**：`machine_desc/east/fylite_device_east.json` 的顶层是 `tf` · `pf_active` · `wall` · `magnetics` · `lh_antennas` 与 `fylite:grid` · `fylite:channel_map` · `fylite:vessel_resistivity_uohm_m` 一类，**早已整体搬到 `fylite:` 之下而本表没跟上**。所以 K-2 不是「裸写了非 DD 名」，是 **DEVICE 段相对真文档过期**：`machine/default_grid/r_min` 真正对应的是 `fylite:grid` 底下的某个键。加前缀只会让它看上去对而仍然指不到任何文档。**真修法 = 逐条对着真装置文档重映射**，已在 `test_fyo_paths_have_a_dd_home.py` 里以带理由的开列表登记，红着等那次重映射 | 本轮实测 |
| **K-3** | kernel · fydata | 条目不变（`fyo.rs` 的 `transport_inputs` 对 A-Box 的 `transport/fylite:rho` · `transport/fylite:y`）。★**本轮未动**：判定要 A-Box 那一侧的实际文档，而本会话虽已取到 `fydata`，核对与改名是跨仓的一件独立活，不并进本轮 | FYO-REPORT-05 O-9 |
| **K-4** | kernel · fyo | 单位书写法两侧未约定：12 处不同（9 处斜杠 vs 点-幂语法，3 处符号/角度约定 `amu`↔`u`、`1`↔`e`、`rad/s`↔`s^-1`），**量纲不符 0 处**。量表路径起点有两种约定（IDS 根 / 数组元素）且只写在散文里，机器可读的表不携带 | FYO-REPORT-05 O-5 · O-7 |

★ K-1 / K-2 / K-4 的"应改成什么"含〔推测〕成分，**不应据报告直接改写**——报告只出证据。

## B. 本体侧（`fyo`）

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **O-1** | fyo | 四张内核自有表（`discharge` 48 · `transport_inputs` 10 · `uq` 5 · `pulse` 4，共 67 行、全部规矩带 `fylite:` 前缀）与五条**承重**缺名（23 条程序间交接路径中占 5 条，尤以跨四 IDS 复发的 `fylite:psi_norm`）是对 `fyo:` 命名空间的提案，待裁定接纳与否 | FYO-REPORT-05 O-3 · O-4 · 须 `FYO-ADR-*` |
| **O-2** | fyo | 测量不确定度无本体承载：spo 有 `UncertaintyStatement`（`standard_uncertainty` + `uncertainty_origin`，正为此而设），但 **fyo 无任何槽指向它**。直接后果见 D 组 E-3 | fydata G-10 · 须 `FYO-ADR-*` |
| **O-3** | fyo | `wall` 单套二维描述（**用户裁定 2026-08-31**，非缺陷）的代价已量化：670 个带嵌套 `dd_path` 的类中恰 6 个不能沿属性树走到自己的路径，全在此一 IDS；`maxOccurs="3"` 压成一套。已由 `check_omega` 规则 (e) 设门（`UNREACHABLE_BY_RULING`，第 7 个出现即失败）。**仅在认为该代价过高时重开裁定** | FYO-REPORT-05 O-6 |
| ~~**O-4**~~ | fyo | **射频天线表的声明路径与约定**（2026-09-11 用户裁定 ①「现在就提 fyo 治理」立项；提案 v1 在内核 `docs/note/ec-raytracing.md` §7.1）。**现状**：`EC_LAUNCHERS` 八槽里 `name` · `frequency`（`beam/frequency/data`）· `power_launched` · `r` · `z` 走 DD 原名，**两个指向角与模式走 `fylite:`**。★**不是因为 DD 没有这两个名字** —— DD 确有 `beam/steering_angle_pol` / `steering_angle_tor`（rad，内核 `rust/fylite_runtime/ids/ec_launchers.tsv` 核过）；**没验的是约定**。**提案请 fyo 定三件**：(1) DD 那两个角与本仓的一对是不是同义 —— 是则给换算规则、`fylite:` 槽随即退役，否则**治理一对带约定文字的槽**（关键在约定要写下来，按名字猜约定正是一束波瞄到别处而无一处报错的路子）；(2) 模式要一个**支**：DD 只有 `beam/polarization/o_mode_fraction` ∈ [0,1]，而**份额不是支**（0.5 既不命名 O 也不命名 X，把一个变成另一个是模式转换模型，本门没有），现门只收 ±1 并对其它值按名拒绝；(3) **LH 天线行**的声明路径（发射位置 · N∥ 谱与功率权 · 极向 N · 反射面参数）—— LH 的发射以强加折射率而非两个指向角参数化，现按名拒绝。★**本仓不等**：`fylite:` 槽继续可用，门的行为不因提案改；治理落地后换路径。★**提案能提得出来是今天才成立的**：TORAY 的两个角已由数据推明（`angrid1` 极角 · `angrid2` 方位角，实测复现声明值到 0.004°），与本仓一对的换算是算术（102°/214° ⇒ θ_p 14.386° · θ_t 33.164°）—— 有了可提的约定，治理路径才有东西可治 | 内核 `docs/note/ec-raytracing.md` §7.1 · §4.7 · `rust/fylite/src/fyo.rs` 的 `EC_LAUNCHERS` 表注 ★★★**2026-09-11 结案：`FYO-ADR-10`（Proposed）已在 fyo 落地并推送。**用户裁定「fyo 天线参照 imas dd」，逐条对真 schema 核过之后 **fyo 一个自有天线槽都没有新增**：**D-1** 两个指向角的约定由 fyo **声明**并写进 schema（形式与提案逐字相同，且明写 φ̂ 由机器声明的 COCOS 定），**与 DD 自身约定是否一致仍 `[TBD]`**（`FYO-MAP-00` S-8，待 DD 全文）；**D-2** O/X 支**库里本来就有** `ec_launchers/beam/mode`，不新增槽；**D-3** LH 发射谱**本来就在 DD 血统里**（`antenna/row/n_phi` · `n_pol` · `power_density_spectrum_1d/2d`），并更正提案的提法 —— **N∥ 不是天线的属性**，它依赖平衡；**D-4** 反射面与取哪个根判为**模型设定而非装置属性**，不入 device 层，归属场景层待 CONOPS。★**本仓由此产生两条后续**：(a) `fylite:mode` **应当退役改绑** `beam/mode`；(b) 两个角**暂不改绑 DD 名** —— D-1 的 `[TBD]` 未解之前改绑等于把一个未验的一致性假设焊进链路 | fyo `docs/adr/FYO-ADR-10.md` · 内核 `docs/note/ec-raytracing.md` §7.1 |

## C. A-Box 侧（`fydata`）

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| ~~**D-1**~~ | fydata | **已关闭 2026-09-08**：`check_abox_shape.py` 新增 **S8** 收 `abox/amns/`（记法同装置树，`_ids` 认根类）。开核当次报 **1 处**——`charge_state` 挂在 `amns_data` 顶层，而 DD 放在 `process[]/charge_state[]` 之下；**未改数**，转为 fydata G-12（重铸属转换器的活） | fydata README G-1 |
| ~~**D-3**~~ | fydata | **已关闭 2026-09-08**（`fydata 0e114a1`）：**S10 的 147 处「该是数组的槽写成单个结构」**——`coil.element` 110（七份 pf_active）· `flux_loop.position` 35 · `time_slice.profiles_2d` 1 · `coil.conductor.cross_section` 1。T-Box 记 multivalued、DD 记 `1...N`，而 A-Box 写的是一个结构，于是**十三台装置一台也跑不动 `code/breakdown`**（内核按 DD 读 `pf_active/coil/0/element/0`）。★逐行改不走 load/dump（件是手工维护、头注释里有「盲目重跑会把 deg→rad 打回去」的警告）：每处只改块的第一个子键那一行，其后逐字节不变；判据是改前改后 `yaml.safe_load` **语义相同**。S10 147 → 0 | fydata `check_abox_shape.py` S10 · 本仓 `C-10` |
| ~~**D-2**~~ | fydata | **已关闭 2026-09-08**：新增 **S9**。★按字面实现的第一版**是错的**——「值域是类⇒必须写结构」报 173 处，而标量简写是通行记法。改判两条：简写的**类型**要配得上值域类；**同一棵树内**不得两种写法混用（跨树不算）。各抓到一件真事：EAST `ec_launchers` 的 `frequency: 140E9` 被 PyYAML 读成**字符串**（YAML 1.1 浮点正则要小数点+带号指数），四束皆是，已改 `1.4e+11`；`Wall2dLimiter.type` 装置树内混用整数与字符串，未改数（口径裁定，fydata G-13） | fydata G-11 |

## D. EAST 标准算例（`fydoc` `facts/experiment/east/137985/`）

现状：电流平顶已核（2.5 s 起 400.36 kA ± 0.27 %，4000 ms 在其内，E-1 已关闭）；磁测面成立，
**动理学面有了实测的量而仍不可用**。★★2026-09-08 该条目自己往前走了一步：fydoc 仓里那份
「待删」的 mdsip 录音（F-13）**含本炮交付 EFIT 的标量**，据此量出**本炮有两个互不相容的
平衡**——交付 EFIT 的储能 187.1 kJ 对 g-file 转换件的 46.7 kJ，**差 4.00 倍**（详见该条目 §五）。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **E-2** | 需数据访问 | 加热 / 驱动 / 杂质 / 输运的本炮数据只在 MDSplus（`202.127.204.12`）。★**2026-09-08 清单短了一截**：本炮的 `\WMHD` · `\BETAP` · `\LI` · `\PCRL01` · `\VP1` · `\DFSDEV` 与 EFIT `MEASUREMENTS` 已在 fydoc 的录音里（`\PCRL01` / `\VP1` / `\DFSDEV` 只有抽稀后的 253–463 点）。**加热 / 驱动 / 杂质 / 输运一支未录**，故不关闭 | 条目 §五.1 · §七 |
| **E-3** | fydoc · 待 fyo | Thomson 的不确定度只能标 `fylite:` 前缀（60 处）——被 B 组 O-2 卡住 | 条目 §八 |
| **E-7** | 需数据访问 | **本炮两个平衡互不相容**：交付 EFIT（录音）与 deck 储能差 **4.00 倍**、$\beta_p$ 差 4.45 倍，而 deck 自己 GS 闭合到 0.59 %。挡路的是**那份 g-file 不在任何仓里**（只有 sha256），其 `RUN_TYPE` 与名字里的 `loop` 读不到；`FYDOC-CASE-19` 收的三份 EAST g-file 没有本炮的。★★**对本仓的意义要说准**：受影响的是 **`B-06`**（`plan/R/east-137985`）而**不是** `V-15`（后者是 g-file 读写往返的恒等式，与压强内容无关）。`B-06` 的参照侧是 `oracle_east137985_4000ms`，而该 oracle 自陈其约束是「磁测 + `kprfit=1` 直接压强 65 点」——那 65 点正是 deck 的压强列。**故 `B-06` 作为「本仓能否复现这次重建」仍然成立**（它比的是同一个答案），**不**支持「本仓重建出了这一炮真实的平衡」这句更强的话 | 条目 §五.5 |
| **E-4** | 须语料裁定 | 自洽五面态是**计算产物**不是测量；`dev:result` 现有七个取值没有一个是为它准备的。**这是本条目自己走不过去的那一步** | 条目 §五 |
| ~~**E-5**~~ | fydoc | **已关闭 2026-09-08**（那一步已走）：逐片 12 道 PF 通流实测（fydoc `facts/tools/pf_drift.py`，纯标准库、只读）——最静段 **4.0–5.0 s**（总 3.17 %/s · 方向 1.17 deg/s），**4000 ms 落在其起点**，故重建选片不必改；全平顶方向仅转 7.91°，而逐道看 c1 降 50.9 %——两种读法量的不是同一件事，条目已并记。**「任一片都不是严格稳态」不变** | 条目 §二 |
| **E-6** | fydoc | `check_cases.py` 报 **28 处 / 19 组**不满足（其中 **14 组** reviewer 联系方式待补）。**既存项**，2026-09-08 再经 `git stash -u` 复核确认改动前完全相同。★当日走过 24/12 → 26/13 → **28/14** 三档：先是 `FYDOC-CASE-04`（FUSE）跑出答案、按该组**自己写的**规矩 `review.status` 由 `not-required` 改判 `pending`；再是 `FYDOC-CASE-19-east-efit` 立组（F-11），其 `reviewer` / `contact` 按同一条规矩记 `[TBD]`。**两次都不是新缺陷，是同一条规矩对新落账的东西生效** | fydoc `tools/check_cases.py` |

## E. 本仓（`fylite`）与内核（`fylite_kernel`）

★2026-09-08 更新。**此前这一节写着「本轮未在本仓留下未完成项」**——那句话在 09-06 成立，
当日的验证定序册第三轮与第三方源码盘点之后不再成立。下表是本仓／内核自己的活，
**不依赖上下游任何一条**。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **F-1** | fylite | **`discharge-iter` 的门**。物理校验册 88 条现只评了 24，做了它可评条数才会动。要两件事一起做：`run_json` 走树门，**且**算例把装置文档绑成输入 | RUN-2026-09-08 §四 · §六.3 |
| **F-2** | fylite | **生成件里的手写段没有活路**。手写归因活不过下一次 `--write`，**而且不会有任何东西报错**——这是它比「写错了」更坏的地方。三个候选落点（判据册算例声明的 `caveat` · 本册子的 RUN 页 · 渲染器认一个锚点）**要先定**，才好写第二段 | RUN-2026-09-08 F-24 |
| ~~**F-3**~~ | kernel | **已关闭 2026-09-08**（用户裁定：**Rust 侧重写**）：26 条里 **11 条已重写**——层析 6 + 自标定方法学 3 落 `diagnostics.rs`，装配层编排 3 落 `transport.rs`；`cargo test --lib` 459 → **470 passed**，并做过变异检验（把基的 `clamp(0.0,1.0)` 改 `0.9`，边界那条当场判负）。★**剩下 15 条不是「没写」而是「本 crate 没有那个主体」**：UKAEA 网络 8（模块不在任何树里，许可那两条早已捞回）· 层析的重建质量 5（要反演求解器，本 crate 只导出基与行）· `source_set` 台账报表 1。逐条落位见 `tests/PHYSICS-MIGRATION.md` 该节 | `tests/PHYSICS-MIGRATION.md` 退役表 |
| ~~**F-4**~~ | fylite | **已关闭 2026-09-08**：专用 `Pkg.develop` 环境跑通 `FUSE.test_case(Val(:ITER_time), dd)`（FUSE 1.1.5 / IMAS 7.3.0，437.7 s，**61 个时刻**，`evolve_error: null`），答案收进 fydoc `FYDOC-CASE-04-fuse/corpus/1.1.5/`。`B-08` 随之判 pass。★捕获脚本加 `FUSE_TEST_CASE` 门——与 0.7.0 那份**出自同一个读取器**；★走不通的一条路已记在语料 README：把写盘的 `dd` 读回来归约，`json2imas` 在留空的二维字段上抛 `MethodError` | plan.jsonld `B-08` · 内核 `180dd93` |
| ~~**F-5**~~ | fylite | **已关闭 2026-09-08**：CHEASE 本机构建成功并自行重跑 `ntcase=2`（不读冻结产物），`B-10` 判 pass（GS 残差 5.257e-02，带 8e-2）。★该条的主要产出不是那个数，是一个读法——**加密盒子不是收敛检验**（101×65 → 401×257 残差不降反升；改内部网格才降 2.1 倍） | plan.jsonld `B-10` |
| ~~**F-6**~~ | fylite | **已关闭 2026-09-08**：映射逐项落实，`C-11` 判 pass——离子热通量系统偏低 **−42…−53 %**、电子热通量最差 **+131 % 恰在阈值**、八点全在训练箱内（门里第一条就判这个）。★留痕的那一项：算例是「红/蓝氢」示踪设置（两支同位素氢 + Be + C）而网络只模一个有效主离子，**Be 那一支在低梯度端比氢两支合计还大**——所以离子那一栏的差里含着**成分折叠**，不全是代理误差 | plan.jsonld `C-11` |
| ~~**F-7**~~ | — | ~~`B-09`：要不要自跑 DINA 当参考~~ **作废（登记当日）**：DINA 的参考答案就在 `~/workspace/data/ITER Scenario/`，不需要自跑，那次许可/清净室裁定也就不必做了。★留着这一行是因为它示范了一件事——**这条待办从提出到作废不到一小时，而它提出的依据是台账里一句写着「未取回」的旧备注**。备注记的是写它那天的状态 | SURVEY-2026-09-08 §一 · §四 |
| **F-8** | 可发信 | **`C-08` 向作者索取 GENE 那批次的 ν* 与逐半径 Miller 参数**（或那次 JINTRAC 模拟的对应量）。这是六条阻塞里**唯一一条只差一封信**的 | plan.jsonld `C-08` |
| **F-9** | fylite · fydoc | **`code/breakdown` 仍跑不出结果，但挡路的换了一件事**。G-14 修掉后 ITER 与 EAST 过了几何这一关，卡在下一句：「the device gives no supply current limit (`power_supply/current_limit_kA`) and the plan binds no `i_max_aturn`」。两条路：装置描述补上供电限值（是**数据缺口**，ITER 的 A-Box 里没有；EAST 的手工牌里有 `current_limit_kA: 14.5`），或算例侧绑一个 `i_max_aturn`。**先定这一条走哪边**，再谈把 breakdown 接进定序册 | 2026-09-08 实测（`fy run design breakdown --device iter --facts <fydoc>/facts`）|
| ~~**F-10**~~ | fydoc · fylite | ★★**2026-09-12 关闭，修在消费方而非生成器**：fydoc `facts/device/<slug>/abox/README.md` 明写清单路径保留 `<tree_root>/<相对路径>.yaml` 写法（它兼作 `dev:provenanceKey`），**消费方去掉 `tree_root`、换 `.jsonld` 后缀**——运行时 `assembly::from_manifest` 此前逐字拼接，从未执行这条规则。现 `book_path` 依次试：按原样 · 去 `tree_root` · 换后缀（两种写法），存在者胜，都不存在按原样交出让下游报名。实测（`fy run design breakdown --device …`，六台）：cfedr 15 · best 19 · cfetr 15 · west 17 · iter 12 · east 16 个线圈进装置文档，「no `pf_active/coil`」不再出现；六台随即都停在下一道门「无供电电流上限」（**F-19**）。判据 `a_book_manifest_resolves_tree_root_and_the_jsonld_suffix`（书的布局缩影 + ITER 手写写法按原样取胜）。**原文**：**生成的装置清单指向不存在的路径**。`abox2jsonld.py` 把每份件搬位（剥 `tree_root`）并改名（`.yaml`→`.jsonld`），却把清单里的 `providers[].path` **原样抄过来**——于是 `best` / `cfetr` / `cfedr` 的清单指着 `fyo/latest/providers/pf_active/base.yaml`，书里没有这个文件，解析不到线圈；`west` 更是**一个 provider 都没声明**。四台因此在 `code/breakdown` 上报「the document carries no `pf_active/coil`」。★ITER 之所以能过，是因为**它那份清单是为书手写的**（`path: providers/pf_active/base.jsonld`）——不是生成器做对了 | 2026-09-08 实测（四台逐台跑）|
| ~~**F-11**~~ | fydoc | **已关闭 2026-09-08**（fydoc `9eb7657`）：三份 EAST g-file 收进算例书 `FYDOC-CASE-19-east-efit`（`corpus/` 与 `case.yaml` 同址、`checksums` 逐件在册、`payload: in`），本仓的门与登记册指针随之改指。★★查证时发现比「指针脆」更要紧的一层：2026-09-04 的裁定删 `corpus/experiment/` 时写明这批件**不可重取**、「只剩 git 历史与未跟踪的 `todelete/`」，而同日一次**讲文档规则**的提交把 `todelete/` 顺带跟踪了进来、提交信息一字未提——于是一批不可重取的件被一个名叫「待删」的目录持有着。★迁入**不等于放行**：`release: internal` 不变，review 待具名 | 登记册 `V-15` 的 `has_input` |
| **F-13** | fydoc | **`todelete/` 余下的部分未判**：`east/mdsip-137985.json`（一次 mdsip 会话的逐帧录音，fylite 的浏览器门经 `FYLITE_MDS_FIXTURE` 读它）是**测量**不是重建产物，按分工归实验层 / `fydata` 的 A-Box；另有 `todelete/device/{east,iter}` 与 `todelete/facts/`。★F-11 只搬了它该搬的那一份，**没有顺手替其余的决定归属**。★★**2026-09-08 这一条的分量变了**：那份录音不是只给浏览器门当夹具的——它是本炮**交付 EFIT 标量在整个生态里的唯一一份**（E-7 全靠它），且**含五炮**（137984 · 137985 · 137986 · 165704 · 165705）而文件名只写了一炮。一份这样的件住在名叫「待删」的目录里 | 2026-09-08 迁址时并记 · 同日实测 |
| **F-14** | fylite | ★**2026-09-12 判据形状已改（半关）**：`fylite.io.est2.fringe_gate`（纯函数）把判据改成**对数对称带** `gate·median < |n_e,line| < median/gate`（`gate` 0.15 → 天花板 6.67×），`reduce_est2` 走它；七条钉（塌下去 · 跳上去 166× / 7× · 带的对称性 · 无读数永不 good · 中位只取非零弦 · gate≤0 关闭）。★**没关的部分**：c4 的「3× 以上」六片里落在 3–6.67× 的仍过带——3× 是否即条纹跳**未判**（整数条纹的幅度取决于线密度，不是固定倍数），要用第二口径（弦中位数对 `\DFSDEV` 的比值 45 倍漂移）定夺，那一步需要该炮数据（E-2）。**原文**：**POINT 的条纹清洗判据只设下界**。`python/fylite/io/est2.py` 的 `good = (a_ne is not None) and (abs(a_ne) > floor)`（`floor = gate * median`），注释自陈的理由是「丢了条纹的弦会塌到中位数的一小部分」——**这对塌下去成立，对跳上去不成立**，而条纹跳是整数倍相位跳，两个方向都会发生。EAST #137985 实测：**c4 在 9 片中的 6 片上超同片存活中位数 3 倍以上**（最高 **166 倍**），每一片都带着 `weight_nel = 1.0` **交给重建**；c11 另在 1.0–2.0 s 三片上超 7–20 倍。★★同一支工具的第二条口径：弦中位数与干涉仪自己的线平均道 `\DFSDEV` 的比值在九片上走了 **45 倍**（0.03 → 1.34）——弦长不变，故不是几何；1.0–2.0 s 那三片的 POINT 归约实际在读噪声。★**重建选的 4000 ms 落在比值正常的那一段**（0.87–1.34），故本仓现有结论不受影响——但判据的形状是错的 | fydoc `facts/tools/point_chords.py`（2026-09-08 实测）|
| **F-12** | fylite | **`B-01` 的参考侧要不要改在 FUSE 1.1.5 上重跑**（该条现有结论建立在已遗弃的 0.7.0 冻结答案上，其 `status_note` 自陈这一问「仍未裁定」）。★2026-09-08 起**成本变了**：跑 1.1.5 所需的 Julia 环境已经在本机建好（F-4 的副产物），此前挡它的正是这一件 | 登记册 `B-01` status_note · SURVEY-2026-09-08 §一 |

| ~~**F-15**~~ | kernel | **起始设计的岭调过 2e-1 之后，退火一趟都不接受**。`code/discharge` `stage=anneal` 的 `pass` 事实报 **0** —— 八趟里没有一趟比没动过的起始设计更好，即退火一步没走；交出来的 κ=1.146（要的 1.389）、磁轴离实测轴 **102 mm**（EAST 小半径 0.44 m）。换 8 / 16 / 24 趟、`anneal_hi` 0.10 / 0.03 / 0.01 六种排程，`shape_error` **逐位相同**（0.14660），故不是排程敏感。λ≤1.5e-1 时正常下降（最优趟 6–8，`shape_error` 0.035–0.053）。★两解未分辨：接受规则只收「比历史最优更好」而步长降不到排程下限以下〔推测〕，或该电流水平附近确无更好的点。~~**这条定着岭缺省的上界**~~★★★**已查清并关闭 2026-09-11 —— 本条的三处判断都不成立，逐条量过（EAST #137985，一次只动一个旋钮）**：① ~~「上界 λ ≤ 1.5e-1」~~ —— **λ = 2e-1 能到 gap 0.01405**（`anneal_hi = 0.01`）；真上界在 **2e-1 与 3e-1 之间**，且它由 `anneal_hi` 决定，**不由 λ 单独决定**。② ~~「六种排程 `shape_error` 逐位相同，故不是排程敏感」~~ —— 那六次**全是 pass 0**，即起始设计，**排程根本碰不到它**；退火一失败，报出来的就是个与排程无关的量，**这个测量在原理上就量不出敏感性**。实测敏感性很大：**0.147 → 0.028**。③ 两个未分辨的解**都不成立**：**根本没有接受闸** —— `chan`/`res`/`sm` 每趟无条件前进，只有 `best` 的**记录**是有条件的，所以 `pass = 0` 从来不是「一步没走」；而更好的点确实存在（**0.0776 → 0.0141，好 5.5 倍**）。★**真实机理是发散**：λ = 2e-1 配缺省 `anneal_hi = 0.10` 时，游走从 gap 0.078 m 走到 **0.208 m**，一种排程下直接走死（`gap_last = nan`）。★**旋钮是 `anneal_hi` 不是 `passes`**：λ = 2e-1 上 `hi = 0.01` 在 8 趟与 24 趟都成，`hi = 0.03` / `0.10` 两种趟数都不成。★★**顺带修掉让它被误读三天的那件事**（内核 `c37ad01`）：**走飞了、走死了、探过但没更好，三种结局此前逐字节相同**——都是 `pass = 0` 加起始设计的数。现在门在 notes 里分开说，并给出**量出来的**补法（调小 `anneal_hi`）。两个方向都验过：发散那一组报出「0.0776 → 0.2079」，成功那一组**不报**——一条两边都报的提示，等于没修 | `fylite_kernel` `tests/test_start_against_the_machine.py`（2026-09-08 实测） |
| **F-16** | fylite | **装置档自由边界解不收敛，且不随预算单调**。`validate-worker-interp-device`，EAST，缺省 λ=1e-1：`free.residual` 400 步 5.6e-3、1200 步 6.8e-3（`settled: true`，423 步停）、4000 步 **6.1e-2**。**不随预算单调下降**，故不是步数不够。λ=3e-1 上同一条是 5.8e-2。★最早 λ=1e-3 时的 4.9e-8 **不可比**——那份属于一个塌成 2 cm 的位形（a_minor 0.0195 m 对要的 0.357 m），几乎没有东西要收敛 | `app/tests/validate-worker-interp-device.mjs`（2026-09-08 实测） |
| ~~**F-17**~~ | kernel | **五个测试模块自 T-4 迁移起就收集不了**，因此一直没在跑：`test_assembly`（取 `fylite.scenario.model.sources`）· `test_nn_tglfnn_ukaea`（`…model.tglfnn_ukaea`）· `test_point`（`fylite.io.kfile`）· `test_selfcal`（`fylite.scenario.analysis.selfcal`）· `test_tomography`（`…analysis.tomography`）——都是那一串刀把公共仓模块收进 `tests/oracles/` 之后没有回指的残留。★**红着的闸子有人看得见，收集不了的没有**：此前每一次「全量通过」的读数都不含这五个模块。★★★**已关闭 2026-09-11（核对内核仓记录，非重做）——本条的建议「只差改 import」是错的**：那五个文件 **2026-09-08 已按用户裁定退役并删除**，逐条的退役账在内核 `tests/PHYSICS-MIGRATION.md`〈退役表〉（57 条判据分四堆，含「丢了什么」一栏）。**没有 import 可改，因为文件不在了。**★退役**不是无损的**，而那张表逐个点了名：`test_point` 干净（判据与被测物同时退役）；`test_assembly` 丢 4 条（装配层编排的 Python 侧收敛判据，门测判出入不判中间态）；`test_selfcal` 丢 3 条（含一条**方法学**判据：前提不成立时该方法会安静地报「无需校正」）；`test_tomography` 丢 11 条（反演质量：基在边界为零 · m=0 纯通量 · 伪 m=1 · 截断 · 死道 · 噪声稳健 · 一相机对两相机的分辨率），Rust 侧只有一条掩膜判据接住；`test_nn_tglfnn_ukaea` 丢 8 条（要的正是那个不在的模块，两条已捞出成 `test_tglfnn_ukaea_stays_out.py`）。★★**顺带一条工作树纪律，已记在那张表里**：删除落在一次讲 TGYRO 的提交（`f64d7ad`）里，提交说明一字未提 —— 共享工作树上 `git rm` 入的是**共用索引**，于是并行会话的提交把它顺手带走了。本仓写过「提交只显式 `add` 自己的文件」，但它防的是收进别人的改动；**这次是自己暂存的删除被别人的提交收走** | 2026-09-08 `--collect-only` 实测 · 2026-09-11 核对退役表 |
| **F-18** | fylite | ★★**2026-09-12 五道逐道定性（实跑）**：① `validate-guide` —— **夹具过期**（五页带接口 152 戳，源已 153）→ `tools/build-guide.sh` 重生成，**绿**。② `validate-worker-vertical` —— **真错 + 夹具漂移两层**：真错在内核 `code/vstab`：设计页的合成机器声明 `magnetics/flux_loop: []`，门的守卫只测键在不在，`device_magnetics` 对空表拒「declares no flux loops」，整条判据回 `null`（`verticalOf` 的 `catch` 把原因吞了，用补印原因的 worker 副本量到）——已修（空表 = 无环，答无行；钉 `an_empty_flux_loop_list_is_a_device_without_loops_not_a_refusal`）。修后闸子走到比对，对 09-05 录的 1e-7 夹具不合：γ 58.55 对 −0.033、a 0.855 对 0.572、q95 4.87 对 3.37、细丝 285 对 68，而 I_p 同。★★**2026-09-12 定性并重录（这一格因此绿了）**：**旧夹具录的是退化位形**，两个独立口径同向 —— ① 离所问更远：同一个 0.74 m 目标，旧 a 差 **−22.8 %**、今 a 差 **+15.5 %**；② 判据本身说不通：旧夹具 **k 为负**（−6.6e6，k 是去稳力系数）故 γ 亦为负（「稳」），而 κ = 1.6 的位形在无反馈下竖直稳定是这条判据不该给出的答案，今天 k/k_ideal = 0.347 < 1 · γ = +58.6 1/s 才是阻性不稳定区该有的形。★**两个候选解释都排除了**：`031115a` 只在消息带 `targetCurve` 时改行为，而本闸不传；`09d11a5` 把出厂岭 3e-1 改成 1e-1，而把 3e-1 显式传回去**并不还原夹具**（实测 a 0.8717 · q95 6.904 · γ 64.21）。★**重录之外还钉了两样**：本闸按候选序取第一台读得懂的装置（今天 `best`，a_target 0.74 m），此前装置清单一变就会**静默**换一台机器 —— 现在机器名与目标小半径进了夹具，变了按名报出来。③ `validate-q` · ⑤ `validate-zerod` —— **夹具过期（对 T-4）**：各调 `fylite_rs_trace_surface` / `fylite_rs_dt_reactivity`，两者自 T-4 起 `#[cfg(feature = "oracle")]`，不在发行件的 42 个导出里；这两道属 oracle 构建（内核仓）或须改走树门——**内核仓已各有同题**：`tests/_oracle.py` 的 Bosch–Hale `<σv>`（`test_fuse_benchmark.py` 比通道）与 `trace_surface`（`test_discharge_code.py` · `test_bootstrap_code.py` · `test_coupled_code.py`），2026-09-12 全量跑过均绿，所以公开这两道是**重复且已死**，去留待裁定。④ `validate-flux-match` —— **缺数据 + 夹具过期，2026-09-12 已修夹具、并把真读数量出来**：三处改好了 —— 参照表缺席改为**按名跳过**（此前抛 ENOENT 退 1，读者看到的是失败而事实是没有参照件；表另有两处：内核 `rust/fylite/testdata/reference/`、fydoc `cases/FYDOC-CASE-01-astra/corpus/`）· 撤掉的算例菜单改为**直接读菜单当年读的同一份真源**（`docs/examples/evolve/<档>.jsonld` 的 `parameters`，页面不驱动的名字报出来不算错）· 导出按 `#model-iofmt-evolve-json`（菜单项，`validate-beam.mjs` 一直这么按；此前点的是菜单本身，下载事件永远等不到）。★**接上参照表跑到底的读数**：〔甲〕FUSE·ITER 那一档**不收敛** —— 30 轮后最差相对通量差 **22260.58 %**（判据 2.00 %），最差点电子道 ρ̂ = 0.667，栏报「失败」，故无件可导出。★**两种解释未判**：(a) 算例文档没带够当年那个预设套用器供的东西 —— 页面现在**没有「套用算例」入口**（`FyScenario` 只暴露 part / boot / pages / whenDevices / redraw）；(b) 这一档在页面上真的不收敛。定它需要一条口径裁定（页面要不要重新有算例 / 预设入口）。**原记**：**缺数据 + 夹具过期**：参照表在私有 oracle 树（`tests/data` 软链本检出没有；表本身在内核 `testdata/reference/` 与 `FYDOC-CASE-01` 语料里），把表接上再跑（playwright 在 `/opt/node22/lib/node_modules`，本机 8767 起服务）则死在 `model-evolve-case`——该控件自合并 `2df8370` 起已不在页面上。**余**：③⑤改路或迁到 oracle 构建（内核仓已各有同题且绿，见下）；④ 见该格（不收敛的两种解释待裁）。②**已结**。**原文**：**node 闸子五道长期红**：`validate-flux-match` · `validate-guide` · `validate-q` · `validate-worker-vertical` · `validate-zerod`。逐道未定性（缺数据 / 夹具过期 / 真错，三者未分）。已知一处细节：`validate-worker-vertical` 的失败是 `design` 答案里 `vertical: null`——闸子要一个垂直位移判据，门没给 | 2026-09-08 全量扫描 |
| **F-19** | fydoc | **装置文档不声明线圈工程限值**。七台自带装置的 `pf_active.coil` 无一带电流上限。后果：起始设计的真机闸只能拿 EAST #137985 **那一炮自己用到的**最大通道电流当界——那是**演示过的值**不是铭牌值，机器可能能交更多，也可能那一炮本就贴着限值。★与 **F-9** 是同一条数据缺口的两处露头（F-9 缺的是 `power_supply/current_limit_kA`，本条缺的是逐线圈的匝安上限）。★没有出处应留 `[TBD]`，不填估值 | `fylite_kernel` `tests/test_start_against_the_machine.py` 的判据取法（2026-09-08） |
| **F-20** | fylite · fydoc | **真机参照只有 EAST 一台**。起始设计岭缺省的物理判据全落在 #137985 一炮上——它是两仓里唯一带实测放电的机器（实测 LCFS · Ip · BRSP · 交付平衡）。其余六台只能拿合成目标（限制器包围盒 ×0.6，κ=1.6）扫，证据强度低一档：同一组 λ 扫下来 **ITER 在 3e-1 上反而最好，WEST 全平，CFETR 每个 λ 都不稳**——即 **F-15 那道坎在别的机器上没出现** | 2026-09-08 逐台实测 |
| **F-21** | kernel | **wasm 构建跨环境不可复现**。同一份源码，开发容器构建得 core 1 810 792 字节 / `55d8876d…`，另一台得 1 765 754 / `dca90b0c…`；**`kernel_ext` 也变**（615 886 → 615 110）而其源码那一役一字未动——差异因此锁在**构建环境**不在源码。两侧 rustc 均 1.94.1；容器内部可复现（同源多次构建逐位相同）。后果：`docs/note/app-provenance.md` 二进制表在两台机器之间来回改，`test_bundled_artifacts` 谁重建谁红（**2026-09-10 合并 develop 时又冲突一次**）。★★★**2026-09-11 诊断（单机，未关闭 —— 关闭要第二个环境）：一条假设已排除，并得到一个不需要重建、不需要第二台机器在手的判别法。**★**已排除：仓库路径**。本仓自己的源码路径进制品的形式是**相对**的（`strings` 实测：`fylite/src/stability.rs`），所以检出放在哪儿改不了字节 —— `rust/build.sh` 的 `--remap-path-prefix` 只映 `CARGO_HOME` / `RUSTUP_HOME`，而**本仓路径本来就不需要映**。这条假设本来最像，量一次就没了。★**容器内可复现，重新量过**：同一容器里真跑一次 `--wasm-check`（★第一次用了 `--wasm`，那不是有效开关，脚本在动 wasm 之前就退了，所以那次「哈希相同」量的是**没人改过的文件**，不是确定性 —— 记下来因为它差点被当成证据），核心 wasm **逐位相同**（`5bce04d886279f6ac35cb36b`）。★★**判别法：制品自己记着是哪个 rustc 编的**。核心 wasm 里嵌着 rustc 的**提交哈希**（本机 `/rustc/e408947bfd200af42db322daf0fadfe7e26d3bd1`，`rustc -vV` 报 1.94.1 · commit-hash `e408947bf` · **LLVM 21.1.8**）。本条现有证据只写了「两侧 rustc 均 1.94.1」——**那是版本串，立不住「同一个编译器」**：同为 1.94.1 的两份安装可以有不同的提交哈希与不同的 LLVM，而两者都改代码生成。所以拿两份分歧制品各跑一次 `strings -n 8 <wasm> | grep -oE '/rustc/[0-9a-f]+' | sort -u`：**哈希不同 → 本条是工具链差异而非构建系统缺陷，修法是钉住工具链**；**哈希相同 → 编译器出局**，剩下 LLVM / 链接器 / `wasm-opt` 版本 / 依赖锁文件。★**还差的一步就是这一步**，它不需要重建，只需要有人手上同时有那两个文件。★★**2026-09-11：判别法已经落进每一次构建**。`rust/build.sh` 的 `toolchain_json()` 把 rustc 版本串 · **提交哈希** · **LLVM 版本** · 目标三元组四项写进 `kernel-static.json`，于是两份构建记录并排就够判「是不是同一个编译器」，不必再 `strings` 制品。★**`RUSTFLAGS` 记的是摘要不是原文**（`rustflags_sha256`，前 16 位）：原文里带 `--remap-path-prefix=/root/.cargo=...`，而这份 json 被 `rust/fylite_runtime/build.rs` **逐字节嵌进** `libfylite_runtime.so` —— 记原文就等于把路径重映射本要藏起来的那条路径发出去，发布前的硬化闸当场拒了链接。**摘要判同异，够用；原文泄漏，不用。**★所以本条现在缺的**只有对侧那两个文件**，本机这边没有再能做的一步 | 2026-09-08/10 两侧实测 · 2026-09-11 本机诊断与工具链戳（内核 `35e25d1` · 修正 `4b6d6bc`）|
| ~~**F-22**~~ | kernel | **EAST 手工卡片仍写着构建机的绝对路径**。`9091776` 把 `/home/salmon/workspace/fydata/abox/experiment/east/137985` 从 `machine_desc/east/east_device.yaml` 改成了 `fydata:abox/…`，**同一字符串仍在 `machine_desc/east/fylite_device_east.json` 里**；而 EAST 的 facts 卡是从后者派生的（`tools/abox-to-facts.py` 自陈 EAST「手工卡片保持原样」），故它照旧出现在 `dist/facts/device/east.jsonld`。★★**2026-09-11 关闭**：路径在**三层**上，逐层改完并各配闸子 —— (1) 内核 `machine_desc/east/fylite_device_east.json` 与 `.yaml` 两份卡改成 `fydata:abox/experiment/east/137985`；(2) `tools/est2-dump-to-device.py` 加 `_logical_corpus()`，provenance 从此写逻辑路径而不是磁盘路径，**下一次重生不会把它写回来**；(3) ★真正的盲区在**公开仓自己的那份**：`fylite/dist/facts/device/east/east_device.yaml` 是手工维护的（`tools/abox-to-facts.py` 的 `HANDWRITTEN = {"east"}` 拒绝覆盖它），而 `east.jsonld` → `facts.rs` → 嵌进 `libfylite_runtime.so` 与 wasm 的是**它**；改完跑 `abox-to-facts.py east` + `facts-publish.py --flavour internal`，实测重建后的运行时里开发机路径为 0。闸子 `tests/test_machine_cards_carry_no_local_path.py` 由 3 条加到 **5 条**：新增两条读 `$FYLITE_PUBLIC/dist/facts/device/east/east_device.yaml`（一条判与内核卡逐字节同、一条扫本地路径前缀），逐条做过变异验证。★**这一条的教训写在这里**：我一度按「卡片已改」关掉它，而派生链的**第三层**还没动 —— 闸子当时只扫内核那一侧，所以答绿 | 2026-09-08 `grep` 实测 · 2026-09-11 三层逐层实测与闸子加宽 |
| ~~**F-23**~~ | kernel | **内核仓 pytest 长期红 9 条**（排除 F-17 那五个收集不了的模块）：`test_nn_surrogate` 4 · `test_benchmark_registry` 1 · `test_circuits` 1 · `test_jintrac_flattop` 1 · `test_loop` 1 · `test_tglf_selfconsistent` 1。全量读数 **9 failed / 1199 passed / 46 skipped / 9 xfailed**。★用「撤掉当役改动、重编、重跑」核过**是既有的**。★逐条未定性；〔推测〕`test_nn_surrogate` 那四条与权重不随仓发行是同一条线。★★★**已关闭 2026-09-11：九条红全清，但账要分清 —— 其中五条是「声明式跳过」，不是「修好了」。**实测（六个文件一起跑）：**48 通过 / 10 跳过 / 0 失败**。★**四条通过**：`test_benchmark_registry` · `test_jintrac_flattop` · `test_loop` · `test_tglf_selfconsistent`。★**五条是跳过**，且每条都自报理由与补法：`test_nn_surrogate` 的九个用例要 `sat2_em_d3d_azf-1.npz` 与 `epednn.npz`，本发行不带大模型权重；`test_circuits` 一条要 `rv6565.ddd`，本发行不带 Green 响应表（该文件另有一次提交明写「drop rv6565.ddd」，是有意退役）。★★**那条〔推测〕核实了，并且顺带核了 fydata**：用户告知模型数据在 fydata —— 实测 fydata 只有 `sat1_em_iter.npz` 与 `EPED1NNmodel.json`，**不是这两个名字**；而且 `sat1_em_iter.npz` 是 **fytok 格式**（`nn.load` 报「declares none of n_in, n_hidden, n_blocks, n_out, n_members, activation, xnames, ynames」），所以把 `$FYLITE_NN_DIR` 指过去仍是跳过 —— **按名拒绝，不是崩在 numpy 的 KeyError 上**（本会话加固 `Surrogate._REQUIRED` 的那一条正是为此）。★**结论**：这五条是**环境缺件**而非缺陷，补法写在跳过理由里；把它们读成「全绿」是错的，这一行就是为了不让人那么读 | 2026-09-08 全量扫描 · 同日 stash 对照 · 2026-09-11 逐文件复测并核 fydata | 2026-09-08 全量扫描 · 同日 stash 对照 |
| ~~**F-24**~~ | fylite · kernel | **T-C36：`ohm` 是差商，却按行进量比较**——`test_crosshost_replay` 的两条判据 2026-09-10 起红，而**它们自己的报错说错了原因**（写「a disagreement about the step」）。实测：**行进量全在机器精度上**（`te` 6.1e-16 · `ti` 1.2e-16 · `psi` 3.2e-16 · `q` 3.7e-15），只有 `ohm` 6.75e-12 与 `p_ohm` 2.9e-13 差四个量级。`ohm` 不是行进出来的：`scenario.rs` 用 `E_par = ratio*(psi[k]-prev[k])/dt` 现算，而本变体上 |psi| 33.35 对 |psi−prev| 中位 1.45e-4，**相消因子 1.07e5（最差节点 1.51e5）**；3.2e-16 × 1.51e5 = **4.8e-11 上界住了那 6.75e-12**。★所以这是**比较口径的范畴错误**，与 `ENTRY_OUT_KIND` 里 `noise` 那一行点明的是同一类；但 `ohm` **不是** `noise`（它是物理加热密度，「两边都小」不是它的判据），所以缺的是一个**新的 kind**：由状态行**差商**出来的实数行，判据是「不超过该状态行自己的一致度乘以相消因子」。落点在内核 `fyo::ENTRY_OUT_KIND` 与本仓 `engine/crosshost.py::compare`，**不是那个数字**。★★**故意留红**：放宽带宽正是该文件自己的说明禁止的（「a band chosen to swallow 6.2e-13 would agree with any future disagreement up to that size, including a real one」），而「多大的放大是可接受的」是**裁定**不是测量。机制已定，待裁的是那个上限★★★**同日追加的第二件，比第一件更要紧**：这条判据**没有任何东西核对两侧制品是否同源**。`compare()` 的 `environment` 记了 native 侧每个库的 `sha256`，wasm 侧**只记路径**（`{"host": "wasm", "artifact": str(WASM)}`，`crosshost.py:226`）。于是**一份过期的 wasm 读起来与一条真实的物理分歧完全一样**——而这正是一个会自我印证的假阳性：判据红了，报错说两个宿主不一致，而它们确实不一致，只是因为它们是两次不同的构建。★这不是假想：本轮为修自举 NaN 重建内核之后，native `.so`（17:51）与 wasm（14:06）**当场不同源**；上面那份相消诊断之所以站得住，是因为量之前**我手工比过两者的 sha256**并排除了版本偏差——**而判据自己不做这一步**。落点：`compare()` 记 wasm 的 `sha256`，并在两侧 ABI/构建戳不一致时**先按不同源拒绝**，而不是把差异算进 `worst` 交给带宽。★这一条**独立于**第一件，且应当先做：不先钉住同源，那个放大上限无论定成多少都在量一件说不清的事。★★**已做（2026-09-10 同日）**：省源台账补进两份 `.so` 的 sha256（此前只记两份 `.wasm`，于是只重建 `.so` 而不补 `--wasm-check`，台账与盘上 wasm 仍一致、闸子照样绿）；`test_bundled_artifacts` 连 `.so` 一起核（改一字节当场红）；`crosshost.compare()` 的记录补上 wasm 的 `sha256` 与字节数；`test_crosshost_replay` 三条比浮点的判据**在比之前**先对台账核同源，不同源就**跳过并说明**（实测：改 wasm 一字节，八条全部跳过并写明「NOT one build」，而不是报 `ohm` 分歧）。**余下待裁的只剩放大上限这一件。** ★★★**已裁定并关闭（2026-09-10，用户：选项 C）**：上限不是一个选定的带宽，而是**这一次运算自己的误差传播上限** `bound = C · reldiff(src) · max_k |src_k|/|src_k−prev_k|`，`C = 4`（因子取在最坏节点上，不逐节点取；C 明写为 `DIFFERENCED_C`，不藏在容差里）。**放大因子在两个宿主上各算一次，要求相差 ≤ 10 %、取较大者** —— 若两侧对**步长本身**有分歧（那才是真缺陷），因子就会分开，只信一侧的因子恰好会把最该抓的那种情形洗掉。实测：`ohm` 落在上限的 **0.023**，两侧因子相同（2.306e5），判词 `same`；注入检验 **1e-10 通过 / 1e-9 被拒**，即上限在 **~3e-10** 咬合，比它所解释的放大量紧三个数量级。落地：`fyo::ENTRY_OUT_KIND` 增两行 `differenced:psi,psi_prev_out`（14 → 16 行，`INTERFACE_DIGEST` 随之更新而 **`INTERFACE_REVISION` 不动**——只增不改，旧读者落到 `real` 路径即今日行为）；`crosshost.compare()` 认这个 kind 并新增 `differenced` 记录。公开侧 `test_crosshost_replay`：**14 通过 / 11 跳过**，两条有意的红转绿。记录见内核 `docs/note/cfedr-15ma-reproduction.md` §6.2 | 2026-09-10 实测（`crosshost.py:226`；native 17:51 对 wasm 14:06）|

| ~~**F-25**~~ | kernel | **取向已能推导，但 `signb`/`signq` 仍是 C ABI 的入参**（`c_api.rs:974` · `5212` · `5246`，缺省 +1）。D5 结案后（内核 `docs/note/cfedr-15ma-reproduction.md` §6.1）取向由 `mapping::orientation` 从带号数据按 COCOS 17 推出，于是这两个入参**同时存在两条真源**：宿主给的字面量，与从平衡推出的值。★现状不红只是因为仓内每个宿主都不再给它们；**契约面上它们仍然可被覆盖，而覆盖一个可推导量没有判据能拦**。待裁的是「还要不要留作入参」——留（则需一条判据核对宿主给的与推出的一致，不一致即拒）或撤（则动 ABI 契约，要走版本）。★★★**已裁定并关闭（2026-09-10，用户：「动 API 彻底解决 COCOS 一致性问题」）—— 撤。ABI 152 → 153。**三条导出去掉 `signb`/`signq` 入参；`surf20` 的 **slot 19（原保留位）改载带号环向通量** [Wb]，slot 6 的 `q` 带号，`surface_from_block` 用 `mapping::orientation` + `to_gacode` 推出取向。**两处拒绝而非缺省**：零通量 `-7`（此处零与「调用方没动那个保留位」分不开，故 `field_sign` 的「零映射到 +1」这条规矩**有意不适用**）· `neo_scal5` 前两位非零 `-8`。★**顺带查清的一件**：`signb`/`signq` **只有 `tglf_local` 与 `neo_inputs` 读**。于是旧的字面量点分成两类 —— **活的**（六处，改为推导；其中两处此前在门口就 `.abs()` 掉 `b0`，这才是下游必须「发明」取向的根因）与**惰的**（六处只为把 te/ne/ions 送进碰撞率的哑面，改用新的 `mapping::Surface::for_rates`，该构造器**根本无法表达取向** —— 要说「速率没有方向」，办法是让调用点无从可说）。★`geometry::Surface::signb` 是**另一个量**（Miller 求解器里 `|B|` 的符号，不是 GACODE 旗标），已加注区分；正向 `B_t` 下两者数值相同，这正是混用不会表现为错数的原因。★新判据册 `tests/test_orientation_has_one_source.py`：`mapping.rs` 之外**不得有取向字面量** · 读者集**钉在那两个函数**（每一条「翻 signb 只动旗标」的论证都是按这个集合量的） · 两处拒绝 · 经**制品块**量到的可分离性。**实测**：`cargo test --release --lib` 232 通过 / 0 失败；取向判据 9 通过；内核 pytest 见下一行 | ★★顺带一层：本炮之所以查得清，是因为上游把符号也交给预设——`profiles_gen` 三处 `abs()` 丢弃原件符号，而承载取向的 `kccw_*` 在 statefile 里缺席（实测计数 0），落到 DIII-D 缺省。**「入参」这个形状本身就是上游的形状**，所以这一裁定不是本仓的小事 | 2026-09-10 D5 结案时并记（§6.1 末） |
| ~~**F-26**~~ | kernel | **`test_evolve_fluxmatch_code` 的夹具在问一个没定义的问题**。它把**一次运行的压强**与**另一次运行的通量**配在一起，同时把自举打开；于是判据在核一个不自洽的态。实测：`bootstrap = 0` 时该轮返回 `ip` = **8.0000e5**（即请求值，五位吻合），说明求解路径本身是通的，**红的是夹具的口径不是代码**。待裁的是**这条判据应该断言什么**——（a）自举关、断言 `ip` 回到请求值（现已实测成立，但那就不再检验自举）；（b）两侧取同一次运行，自举开、断言自洽 `ip`；（c）退役该夹具，另立一条。★不擅自选：改判据的断言等于改它保护的东西。★★★**已裁定并关闭 2026-09-11（用户：选 D —— 拆成两条）**。先把机理量清楚：**输入梯子完全正常**（q 由内向外 1.000 → 2.600、ψ 0 → 0.759 Wb、a 0.45 · R₀ 1.85 · B₀ 2.0，教科书剖面）；开自举时该轮答 `ip` = **8.161850e6** 对请求 8e5（**10.20×**），因为**非感应电流自己就围出 8.3915e6 A**（`j_ni` 峰值 **1.03e8 A/m²**，高于合理量级约两个数量级），`v_loop` 被推成**负的**（−0.0677 V，**未钳位**）也抵不掉；输出 q 回来是 23.182 · 46.365 · 7.601 · 3.431 … 0.131 · 0.147 · 0.159 —— 轴上振荡、整体反转。★**病根是配对不是几何**：自举由 `dp/dψ` 驱动，而这份 ψ 从来不属于这份剖面；关掉自举，同一套夹具答 **8.000000e5**，请求值，六位吻合。★**且改不自洽**：匹配那一跑按构造就没有通量，所以不存在「对的 ψ」——探针是来补洞的，补法任意。**这条判据问了一个没有定义的问题。**★**落地（D）**：拆成 `test_the_loop_voltage_secant_lands_on_the_requested_current`（**自举关**，钉住割线命中：实测六位吻合，判据带宽 1e-4，比残差松四个数量级）与 `test_the_steady_current_round_reports_coherently_on_a_paired_state`（**保留配对、不断言 ip**，只判在任意态上都成立的那些：有限性 · 边界通量守恒 · frozen 报告 · σ > 0 · relax 单调 · 锯齿记录）。实测该文件 **6 通过**。★**D 的代价当场登记为 F-29，不由这次改判据吞掉** | 2026-09-10 实测（`bootstrap = 0` 对照）· 2026-09-11 逐项量清并拆判据 |

| ~~**F-27**~~ | kernel · fydoc | **EC 已裁定走清净室射线追踪（D1，2026-09-10 用户裁定），实现在另一会话，但本仓这一侧两件都没开始**。★**实测**：`fylite_kernel` 与 `fylite` 的所有远端分支里搜不到射线追踪的文件或提交（逐分支扫 `git ls-tree` 与 `git log --all`，按 `raytr` · `ray_tr` · `eccd` · `gray` · `射线追踪`；唯一命中是 CASE-09 语料里的 `gray.data` 与装置书的一张图），所以它在本会话看不见的地方 —— 另一个仓、或尚未推送。**登记在此是为了不让「已实现」三个字盖住余下的两件**：**（一）接线** —— EC 走哪道 `code/*` 门、参数面、拒绝条件，均未定；**（二）对拍缺参照件而非缺模型** —— EC-vs-TORAY 原先被 D1 阻塞，裁定之后阻塞换成了没有对拍口径：参考侧的 TORAY 本机试跑峰位差 0.023、吸收份额 0.487 未解释 `[TBD]`，要当参照须先恢复该运行的 `toray.in`。★清净室约束不随裁定放松：**不读 GRAY / GENRAY / TORAY 源**是 (b) 的定义的一部分。★**2026-09-11 补记（实现所在会话）**：搜不到是因为实现当时未提交——即内核 `rust/fylite/src/rfray.rs`（EC 与 LH 共用的冷射线核），设计与判据在内核 `docs/note/ec-raytracing.md` · `docs/note/lh-raytracing.md`；本条的（一）（二）两件仍未开始。★★**2026-09-11 晚补记（/goal「完成 L2/3」中途被用户叫停时的状态；内核已推 `2e8684d`）**：**（一）接线已完成**——EC 与 LH 共用 `code/rf_ray`（用户裁定）；`deposit`（ψ̄ 壳守恒沉积，全相对论吸收）· `current_drive`（Lin-Liu 2003 伴随法，香蕉区；Z_eff 无缺省、按名拒绝）· 锥形射线族 `rf_rings` / `divergence`（L2 束宽）· **`core_sources` 按 `code/beam` 的守恒算子落到平衡文档上绑定的梯子**（梯子上瓦数对壳 1e-9，可直接喂 `sources = table`；未绑梯子则带 ψ̄ 网格落在壳上）· L3 的 `beam_waist` / `beam_focus`（见 F-28）。**（二）参照件**：GENRAY EAST O/X（私有 B 类）· **BORAY 在本机 Octave 下跑作运行 oracle**（BSD-3，钉提交；X 半吸收点三码 GENRAY < 本线 < BORAY）· **METIS 认证存档 30 行 ECCD 带**（同点对 `ieccd` 与 `heating::eccd_efficiency`：ours/METIS 0.026–0.64，中位 0.35，log 相关 0.976；METIS 不给波，故二次谐波 X、N∥ 0.3 为明说假设，2 行无 X2 根按名跳过）已接；**TORAY 仍缺该运行的 `toray.in` `[TBD]`**。★顺带更正 `heating::ec_optical_depth`：照重印写的 n≥2 光深深 n 倍，已按 Bornatici 1983 原文表 IV 改。★内核 `caa95a4` 之后（③ 起）的改动已作 `2e8684d` 推到内核 develop；公开物理页 `09-heating` 只同步到 ② 沉积（`0c1b0d5`）。LH 余项转 **F-30**，待裁定项与 EC 残项转 **F-31**。★★**2026-09-11 更正口径（用户指出）**：本条（二）与本会话「复现 CASE-20 还缺什么」的答复把缺口说成「TORAY 输入未交付、要向设计方索取」，**过宽**。交付包里有 `fydoc` `cases/FYDOC-CASE-20-cfedr-hmode-15ma/corpus/H model 15MA 20240522/toray_inputs/` 下的 `echin`（13 241 B）与 `psiin`（328 342 B），两份都登在 `case.yaml` 的 sha256 表里；`echin` 表头带 1.9e11 Hz 与 6.3 T 的高斯数，束与场的物理定义在交付件里。**缺的只有控制名单 `toray.in` 一份**——而它非零关紧：只改其中 `NHARM` 就令驱动电流变 2.4 倍（内核 `docs/note/cfedr-15ma-reproduction.md` §1.6 实测）。所以（二）的结论不变（**缺参照件而非模型**），缺的是哪一件现在说得准。★★**2026-09-11 逐记录数完交付件之后，（二）的阻塞收成两条独立的，都实测**：**(i) 数值控制项缺席**——`toray.in` 确实不在交付目录里（按名与按内容全扫，命中只有 `HCD_profiles_from_statefile.nml` 的 `EC_3_*_toray`，那是输出）；但频率 · 场 · 剖面在 `echin`，功率与两路配置在 `notes.txt`（OMFIT 原始标签 `72EC96t213p 10invEC`）与 HCD nml，**谐波数由机器定死**（190 GHz 基频共振 $R = 7.240$ m 在等离子体内，二次谐波要 $R = 14.48$ m，在真空室外），所以真缺的是射线数 / 步长 / 停止条件与一对注入角的 2° 歧义。**(ii) ψ̄ 归一不同源**——TORAY 侧（`psiin`）用 ψ 图的真跨度 15.9199，本仓用 statefile 一维网格的 15.6752，**分母差 1.56 %**；EC 沉积按 ψ̄ 定位，所以逐点对比前必须统一，单独可解释约 0.003 的峰位偏移。★**本句 2026-09-11 同日改过一次**：初稿写作「平衡不同源 …… S1 是拿 g-file 冻的几何」，**那是错的** —— 查代码后确认本仓 CFEDR 链条一处都不读交付 g-file（S1 的参照表从 statefile 生成），而 `psiin` 的 ψ 图与 statefile 的**逐元素相同**。交付包里确有第二份平衡，那是交付件的事实，不是本条的阻塞；详见 **F-32**。两条都记在内核 `docs/note/cfedr-15ma-reproduction.md` §1.6.1–§1.6.3。★★★**2026-09-11 晚：(i) 解了，而且解法是用户指的路** —— 去 `FYDOC-CASE-21` 的 oracle 里找。那一组带着 `auxFILES/toray.in`（且**自陈是 ONETWO 按缺省机器生成的**，所以 CASE-20 的控制项可按同一组缺省推断）、`toray_inputs/echin` · `psiin`，以及 **TORAY 自己的输出 nc**（30 条射线全轨迹 + 201 格沉积与驱动电流）。于是 **EC 的 B 类参照件不再缺**：冻结 `rust/tools/gen_cfedr_toray_reference.py` → `testdata/reference/cfedr_toray_20ma.txt`，判据 `tests/test_cfedr_toray_oracle.py`（6 过 1 xfail）。实测：驱动电流两侧差 **9 %**（1.7256e-2 对 1.9031e-2 A/W）、轨迹在五个匹配 ψ̄ 面上差 **≤ 4 cm**、单程吸收 1.0000 对 0.9990、沉积峰 ψ̄ 差 0.03；模式由 nc 的 |N| 反推为 **O**（本仓 O 档差 1.4 %、X 档差 22 %）。★同时量出一个符号缺陷 → **F-33**。★`echin` 表头七槽与 TORAY 两个发射角的约定也由此解码，CASE-20 的「2° 角歧义」判定为标签与 `echin` 之差（TORAY 读 `echin`，所以是 98 / 215）。 | 内核 `docs/note/cfedr-15ma-reproduction.md` §6.3 · 2026-09-10 逐分支实测 · 内核 `docs/note/ec-raytracing.md` §4.3–§4.5 |★★★**2026-09-11 关闭：（一）（二）两件都完成了。**（一）接线由并行会话落地（`code/rf_ray` 的 `deposit` / `current_drive` / 锥形射线族 / L3 / 梯子映射）。**（二）对拍不再缺参照件，而且是两份**：① `FYDOC-CASE-21` 的 TORAY oracle（输入 + 名单 + 它自己的输出 nc）——驱动电流比 1.046 · 轨迹亚毫米 · N∥ 差 1.5e-4（修 F-33 之后）；② **CASE-20 自己的交付件**：`HCD_profiles_from_statefile.nml` 里 TORAY 的 `EC_3` 沉积与电流（71.93548524268196 MW · 1.219 MA），冻结件 `cfedr_ec_15ma.txt`、判据 `tests/test_cfedr_ec_against_the_delivery.py`。**实测（一条笔形射线，发射由 `echin` 读出）**：驱动电流 **+1.240 对 +1.219 MA（+1.7 %）**、峰位 ρ 0.3588 对 0.3450、吸收 71.94 MW 单程全吸收；rms 宽窄 27 %，成因是一条射线对 TORAY 的 30 条 `.gauss.` 射线（可用锥扫发散角量，带一个自由参数）。★模式非挑选：`echin` 第 7 槽推断为 O，而 X 支在本平衡上根本不吸收（0 %，停在 ρ 0.93），两条独立证据同向。★比较跑在 ρ 上，使包里两个 ψ̄ 归一之差（1.56 %，F-32）相消 —— 这也是 F-32(b) 的实际落地。详见内核 `docs/note/cfedr-15ma-reproduction.md` §1.9 | 内核 `docs/note/cfedr-15ma-reproduction.md` §1.9 · §6.3 · `docs/note/ec-raytracing.md` §4.7 |
| **F-28** | kernel | **EC 高斯束 / 量子光束追踪（L3）**。2026-09-11 用户确认内核自写清净室射线追踪，L2 只做冷等离子体哈密顿射线 + 沿射线吸收 + 伴随法 ECCD（单射线 / 束中心线，锥形射线族近似束宽）；**束宽演化、衍射与聚焦（高斯束 / 量子光束追踪）不在 L2 里**，登记于此。与 F-27 同一条线：F-27 管 L2 余下的接线与参照件，本条管 L2 之外。★清净室约束随条目走：**不读 GRAY / GENRAY / TORAY 源**，只按公开文献实现；GRAY 移植的暂停裁定不因本条解冻。★开工前提：L2 过判据（解析极限 · 闭式光深 · 效率），且有一个可复算的束宽参照 `[TBD]`。★★**2026-09-11 已实现（内核 `2e8684d`）**：11 篇方法 / 程序全文入库读完后定走**束追踪**——笛卡尔 3×3 复曲率 Ψ = ∂N/∂x 的矩阵 Riccati 方程加约束 Ψv = dN/ds（Pereverzev 1998 式 97、Poli 2001 式 8–9 的本仓写法；Appleton–Hartree 在真空正则，免 TORBEAM 的真空–等离子体匹配；3×3 形式自带 Balakin 2007 所说 2×2 约化漏掉的射线弯曲项）。`rfray::trace_beam` · `ec_deposit_beam`（中心射线吸收按 Gauss–Hermite 横截面分到 ψ̄，Balakin 2008 式 15）· 停因 `ParaxialLimit`（窄宽跌破一个真空波长）；门 `beam_waist` + `beam_focus`（成对、无缺省）· `beam_nodes` · 字段 `beam_width`，与 `rf_rings` 互斥。**「可复算的束宽参照」改由解析判据担当**：真空像散束 1.1e-8 · Pereverzev 1998 §VI C 均匀磁化垂直传播 O / X 6.3e-8 / 2.8e-8 · 抛物通道匹配束与呼吸 · 线性层九元全比 1.3e-8 · 束对邻射线的 Jacobi 恒等式 7.9e-6（样条介质上两套独立实现）；Prater 2008 未发表平衡与束初值，只作宽度量级。**余**：B 类实算对标（TORBEAM / GRAY）无可复算输入 `[TBD]`；无像差阶不含窄束的 N∥ 谱展宽与截面上不均吸收（Balakin 2008：ITER 上窗口宽度为 TORBEAM 的 1.38–1.71 倍）——要不要做 GRAY 式带 N∥ 谱的射线族**待裁定（F-31 ④）** | 内核 `docs/note/ec-raytracing.md` §4.6 · `docs/note/rfray-extracts/l3-*.md` |

| **F-30** | kernel | **LH 的 L2 余三段：LH-② 线性吸收 · LH-③ 一维准线性 Fokker–Planck 与驱动电流 · LH-④ 接门与参照**。2026-09-11 立（/goal「完成 L2/3」被用户叫停时的剩余）。LH-① 冷射线（Stix 多组分、慢波反向波发射规则）与 ①b 托卡马克介质 / 声明的边缘反射已实现，对 GENRAY EAST 2.45 GHz 四射线过 B 类。**全文**：Bonoli 1984 · Bonoli & Englade 1986 · Brambilla 1976 · Peysson 2012 · Rönnmark 1983 · Xie 2024 · Yu 2026 **在库**；**缺** Karney & Fisch 1979 · Fisch 1987 · Ignat 1994（LSC 源码按裁定不读）。逐页渲染核对过的公式摘录在内核 `docs/note/rfray-extracts/lh-*.md`。**LH-②**（设计已定）：Bonoli 1984 式 (20a)/(21a) 的电子 Landau 阻尼作 P 元反厄米部分，`D_I = Im P · ∂D/∂P` 取在射线所走的同一个 Stix D 上，沿射线功率系数 2α_s、`α_s = (ω/c) D_I sgn(∂D/∂ω|_k) / |∇_N D|`，与 EC 共用 Simpson 沉积步进（EC 读数须逐位不变）。★坑：**不能用垂直 k_I**——LH 群速度几乎沿 B，垂直率约为沿射线率的 30 倍；v_e 取 (T/m)^{1/2}、√2 在 ζ 里（Bonoli (40a)(40b) 的 5.7 / 7.0 恰好复现）。判据：摘录里独立算的局地点表（n_e 1.91e20 m⁻³、9 T、4.6 GHz、D、N∥ 3；T_e 2 / 3 / 5 keV → α_s 1.652917e-2 / 1.020860 / 20.89656 m⁻¹，λ→0）· 均匀板上的精确指数 · 带边缘反射的多程功率守恒 · 私有 GENRAY 四射线的功率列（B 类，只判趋势）；离子 Landau 与碰撞阻尼的系数 `[TBD]`。**LH-③**：Bonoli–Englade 一维 v∥ FP，E∥ = 0 时零通量闭式 `f ∝ exp(−∫w dw/(1 + D w³))`，D_ql 由射线功率按能量守恒定（δ 函数分 bin 有因子 2 之歧），与射线吸收（∂f/∂v∥）迭代到电流不变。★单位坑：该文 v_e = (2T/m)^{1/2}，教科书与 Bonoli 1984 取 (T/m)^{1/2}，ν0 差 2^{3/2}；该文式 (35b) 疑缺 ½。判据：无射频回 Maxwell · 强 D_ql 平台 · Alcator C / PLT 表（η̃ 0.121 / 0.099 可复算，各表 P_in = P_d + P_coll + P_τ 逐行闭合）。无陷俘、无 E∥ 协同、无径向扩散（与原文同）。**LH-④**：`code/rf_ray` 现对只有 `lh_antennas` 的计划**按名拒绝**；要补天线行的发射位置、N∥ 谱与功率权（Bonoli–Englade 的分 bin 法；Brambilla 1976 没给阵列谱的归一）、极向 N 与反射面参数——路径待 F-31 ①。参照：Peysson 2012 的 LH 算例**未给 q 剖面**（摘录由其电流剖面推得 q0 ≈ 0.676），BORAY-3D 的 15° 算例与 Peysson 的任一算例都不对应，只能各自复现。★★**2026-09-12 LH-④ 接门完成**（内核 `case::rf_ray_lh`，接口摘要 f6d06d0d，修订号不动：只加槽）：`code/rf_ray` 对只有 `lh_antennas` 的文档按 DD 的 `antenna/row/n_phi` + `row/power_density_spectrum_1d`（FYO-ADR-10 D-3）每 bin 发一条慢波射线（`row/position` 为发射点），`deposit` 时逐射线走自洽准线性回路，`core_sources` 的 `electrons/energy` 落壳；判据：四 bin 1 MW 发射 = 吸收 + 圈外 + 剩余到 1e-6（吸收 0.904 MW，MW 级饱和如 §4.4 所量）。★同日续：`row/n_pol`（本仓声明的极向切向意义）与每壳驱动电流 $-e n_e v_{te}\int u f\,du$（`j_shell` · `core_sources/j_parallel` · `i_driven`；解析算例 0.083 A/W）也接了。2-D 谱 `power_density_spectrum_2d`（每格一条射线）同日也接。**余**：反射参数（D-4）· 一维模型之外的 (5+Z_eff)/陷俘修正 · 参照 2–5 的对拍（GENRAY EAST 2.45 GHz 四射线 2026-09-12 经门过 B 类：(R,Z) ≤ 1.1e-4 m、N∥ ≤ 1.1e-3） | 内核 `docs/note/lh-raytracing.md` §4.3–§4.5 · `docs/note/rfray-extracts/lh-*.md` |
| **F-31** | kernel · 待裁定 | **射频线（EC / LH，L2–L3）的五项待裁定与 EC 残项**。2026-09-11 立。★★★**2026-09-11 用户逐条裁定（建议与裁定一致）**：**① 现在就提 fyo 治理**（提案 v1 已起草：内核 `docs/note/ec-raytracing.md` §7.1 —— 写全本仓约定、给出与 TORAY 两角的算术换算、向 fyo 提三件：DD 的 `steering_angle_pol/tor` 是否同义、模式要一个「支」而不是 `o_mode_fraction` 那样的「份额」、LH 行的声明路径；登记在本表 B 组 **O-4**）· **② 取 (a) 只做电子 Landau**（F-30 的 LH-② 模型因此确定，设计见内核 `docs/note/lh-raytracing.md` §4.3）· **③④⑤⑥ 全部维持现状**（LH 的 L3 不进表 · GRAY 式 N∥ 谱射线族暂不做 · ECCD 香蕉区外修正读作 L2 之外 · 多于 4 种离子仍按名拒绝）。**本条余下只剩弱相对论极化一项**（全文不在容器，做不了）。原五项与各自理由（含裁定列）见内核 EC 页 §7.0。原文如下：①天线表路径——走 fyo 治理，还是先用 `fylite:` 前缀的临时槽（EC 现用 `beam/fylite:angle_pol` · `fylite:angle_tor` · `fylite:mode`，理由在内核 `fyo.rs` 表注）；②LH-② 模型——只做 (a) 电子 Landau（本线读法：LSC / Bonoli–Englade 级的标准做法），还是以 (b) Rönnmark 完整热张量为主、(a) 作极限判据；③LH 的 L3（二维 / 三维反弹平均 FP、全波与模式转换、SOL 耦合、涨落展宽）进不进本表；④要不要在 F-28 之外做 GRAY 式带 N∥ 谱的准光学射线族；⑤ECCD 香蕉区以外的碰撞修正与动量守恒修正——本线读作 L2 之外，请确认或推翻。**EC 残项（不待裁定，可直接做）**：①弱相对论极化选项（Krivenski–Orefice 1983 + Shkarofsky 1966；疑为 X2 对 GENRAY 晚 12 mm 之因）—— ★**本会话做不了**：两篇全文在用户的库里、不在本容器，公式摘录也没有这两篇；无原文即无从实现（不编公式）。②离子多于 4 种的归并规则（现为按名拒绝）—— 归并规则本身是一条口径裁定，见待裁定 ⑥。③~~TORAY 参照缺 `toray.in`~~ —— **2026-09-11 解**：`FYDOC-CASE-21` 带名单与 TORAY 自己的输出，已成 B 类 oracle（见 F-27）。④~~公开物理页 `docs/physics/09-heating.md` 同步~~ —— **2026-09-11 做完**：新增〈伴随法 ECCD〉〈束宽：锥形射线族与束追踪〉〈沉积映到输运梯子〉三节；介质段补 $\sigma_{Bp}$ 由文档推出（含 {eq}`eq-p09-sigmabp` 与那句「符号只影响电流方向」的**更正**）；门表补 `current_drive`/`zeff`/`trap_theta`/`rf_rings`/`rays_per_ring`/`divergence`/`beam_waist`/`beam_focus`/`beam_nodes` 与 `current_shell`/`cone`/`beam_width`/`current_driven`/`current_outside`；拒绝段改写（`current_drive` 不再按名拒绝，改为缺 $Z_{\rm eff}$ 或不与 `deposit` 同开时拒绝、锥与束互斥）；范围句与〈适用域〉第 9 条逐条改对。公开 pytest 2195 passed。★同日的迭代提速（RF 判据墙钟约 60 s → 5.4 s：吸收子本地 Bessel 级数、`lin_liu_f` 合指数、τ > 60 不算电流、独立判据并行）已在内核 changelog | 内核 `docs/note/ec-raytracing.md` §6–§7 · `docs/note/lh-raytracing.md` §7 · 内核 `changelog.md` |
| **F-32** | kernel · fydoc | ★**编号说明**：本条 2026-09-11 立时取号 F-30，同日 `origin/develop` 已把 F-30 / F-31 给了射频线（`2e8684d`），故改号 **F-32**；F-30 不复用。 **交付的 `gfile_efit` 不是 CASE-20 那次运行用的平衡 —— S1 的几何冻结口径因此要重判。**★**实测（2026-09-11，逐记录读交付件）**：`toray_inputs/psiin` 的 `rmaxis` · `zmaxis` = 8.27683 · 0.909335 m，与 ONETWO `summary` 的「R at mag. axis 827.7 cm / Z 90.9 cm」**逐位吻合**；交付 `gfile_efit` 给 8.28474 · 0.908654，**不吻合**。`simag` −22.9865 对 −22.7132（深 1.20 %），**ψ 跨度 15.9186 对 15.6478，+1.73 %**；计算盒（`rdim` · `zdim` · `rcentr` · `rleft` = 8.6 · 16.0 · 7.8 · 3.5）与 `bcentr`（6.3 T）两侧相同。`psiin` 的 F(ψ) 边缘 49.14 = 7.8 × 6.3 逐位吻合，q 边缘 11.79 与 `summary` 吻合 —— 即**参考侧自己的平衡在 `psiin` 里**。★**后果**：S1 拿 g-file 冻几何，S5 的逐量差把几何当共同基底；1.73 % 的 ψ 跨度差会改 ρ 映射，所以（a）EC-vs-TORAY 的峰位差 0.023 必须在同一平衡上重算，（b）S1 的「几何一致」结论要说明是对哪一份平衡成立。★★**2026-09-11 同日判定，前提已消**：statefile 站在 `psiin` 一侧，而且是逐元素判的 —— `psiin` 的 129×129 ψ 图与 statefile 的 `psi` **16 641 点逐元素差 ≤ 1.3e-6**（单精度舍入量级）， `psiaxis` · `rma` · `zma` · `btor` 逐位相同。交付 g-file 的 ψ 图与它们**最大差 0.320 Wb/rad** （≈2 % 跨度）且非常数偏移（逐点比值 0.935–1.048）。**所以本条的量不是「跨度差 1.73 %」** （那是表观差，混进了 `psibdry` 取面不同这第二件事：statefile −7.3113 对 `psiin` −7.0679， 而两者 ψ 图相同 —— 标量声明之差，不是场之差），**而是 ψ 图逐点 2 % · 磁轴 7.91 mm · `psiaxis` 1.20 %**。★★**2026-09-11 同日查代码后，(a) 撤销、本条降级**：本仓 CFEDR 链条**不读交付 g-file** —— S1 的参照表由 `rust/tools/gen_cfedr_reference.py` **从 statefile 生成**（`psi_rz` 最小值 −22.984310 即 statefile 图的最小值），`cfedr_gates::s1` 追的是这张图，归一用 statefile 的 `psiaxis` / `psibdry`；全树搜 `gfile` 只命中通用几何档与 `outer.rs` 的合成夹具。 **所以那份 g-file 是交付包里没被用到的第二份平衡**：它是关于交付件的事实（已写进 `fydoc` 案卷页），不是本仓复现链上的阻塞。本条余下**只有 (b)**：EC 逐点对比前统一 ψ̄ 归一 —— TORAY 侧用图的真跨度 15.9199，本仓用 statefile 一维网格的 15.6752，**差 1.56 %**， 按 ρ ~ sqrt(ψ̄) 单独可解释约 0.003 的峰位偏移。 ★★**而查这一条时判明了 S1 的另一件**：S1「体积亏 3.57 %」**不是 V′ 偏低** —— ONETWO 的 `volume` 是交付 LCFS 围线围出的（Pappus 对到 6.7e-7），那条围线在 ψ = −7.0666， 不在 `psibdry` = −7.3113；梯子的 1484.19 对它追的那个面是对的（独立收敛积分 1484.74， 差 0.037 %）。顺带量出 statefile 一维通量网格相对它自己二维图的压缩常数 **1.015610**。 闸子 `cfedr_gates::s1::s1_the_volume_target_is_the_boundary_contour_not_psibdry` 已落。 ★★**2026-09-11 测完：不是那个压缩。** 判据 `s1_metrics_under_the_two_flux_mappings` 两种落位各跑一次 ——把交付 `npsi` 表拉到「真实」psi_N 之后 `hcap` 中位 −1.253 → **−3.267 %**、`gcap` +1.810 → **+5.772 %**、`q` −0.699 → **+4.963 %**（最差 4.43 → 14.84 %），**全部变差**；只有 `r2cap` / `xhm2` 的最差好 2.7 倍。`q` 是定 ρ_tor 的那一列，所以标称落位是对的，度规中位与压缩无关 —— **一个候选被实测排除**，条目本身仍开。★由此交付件那条不一致收紧成：**statefile 的 `F`/`q`/度规与标称 ψ 网格自洽，而 `psivolpnpsi` 只有乘 1.015610 才对得上**，即不一致在体积那一列。★★**那条「不能归因」的变动同日归因完了：动的是记录，不是数。** 现象是 `gcap` 中位今日实测 +1.810 %，而 09-10 记 −1.75 %（同 61 层，符号翻了），`r2cap` / `xhm2` / `q` 三项也整列反号。归因法：把主表判据在 2026-09-10 的提交 `c8be9d6` 上**另开工作树冷编重跑**，梯子 61 / 121 / 241 三档 —— 六个中位与六个最差**与今日逐位相同**，故**没有任何提交动过这些数**；旧记录**只有中位列抄错**（最差列 19.2 / 10.3 / 5.5 · 31.5 / 20.0 / 12.5 · 1.31 · 1.24 · 4.43 · 0.012 逐位对得上）。旁证同向：`surfaces.rs`（`gm2` 的出处）与 `kernels.rs`（那个 `gradient`）自 09-10 起一次提交都没动。★**补的钉子** `s1_the_metric_medians_are_pinned`：把默认 61 层的六个中位逐个钉到 ±0.02 个百分点，并在 `S1_LEVELS` 被设时拒绝运行 —— 原来的阈（|中位| < 3 %）对「六个数一起换符号」是盲的，这正是缺陷能活一天的原因。钉住不等于认定正确：两个中位的来源仍 `[TBD]`。成因 `[TBD]`（工作假设：OMFIT 外环的另一轮迭代；`notes.txt` 的 `trunk_iterations/.../V1_0522` 与之相容，不构成证据） | 内核 `docs/note/cfedr-15ma-reproduction.md` §1.6.1 · §1.6.3 |
| ~~**F-33**~~ | kernel | ★★★**射线追踪的极向场符号写死成 +1，而本仓声明 COCOS 17（$\sigma_{Bp} = -1$）—— 实测差 25 % 的 $N_\parallel$。** 2026-09-11 由新到手的 TORAY oracle（`FYDOC-CASE-21`）量出。**证据三条，互相独立**：(1) 拿冻结件的 ψ 图在 **TORAY 自己那条射线**上的六个点重算 $N_\parallel$：$\sigma=+1$ 给 0.479…0.655、$\sigma=-1$ 给 0.659…0.786，TORAY 是 0.674…0.804 —— **落在 $\sigma=-1$ 上**（1–3 %，残差是我那份粗糙的双线性梯度）；(2) COCOS 17 的定义本身就是 $\sigma_{Bp} = -1$（3·4·7·8·13·14·17·18 一组），而 `mapping::COCOS = 17`；(3) 物理：两份 statefile 都 `Ipsign = 1` 且 `tot_cur` 为正，正向环向电流在自己外侧给 $B_Z < 0$，而本数据 ψ 向外增大（`psiaxis` −26.5 < `psibdry` −7.1），于是 $B_Z = \sigma\partial_R\psi/R < 0$ 要求 $\sigma = -1$。★**为何一直看不见**：EC 页原写「符号只影响电流方向」——**那句不准确**，翻的只是极向那一半，$B$ 的方向因此转了 13°，$\cos\theta$ 跟着变；而本算例单程几乎全吸收（1.0000 对 0.9990），沉积位置对 $N_\parallel$ 不敏感，峰位仍在 ψ̄ 0.03 内 —— 掩盖它的是这团等离子体，不是判据。**LH 不会被掩盖**（群速度几乎沿 $B$）。★**修的代价要一并付**：`rfray` 自带的 22 条判据与 L3 的七条解析判据里，凡夹具自己按 $\sigma=+1$ 造 $B$ 的（如 `rfray.rs:2609`）必须同批改，否则只是把不一致从一处搬到另一处；③ 的电流方向判据要重看（本算例两档都给 $N_\parallel>0$，方向侥幸未变）。★判据已钉：`tests/test_cfedr_toray_oracle.py::test_our_parallel_index_matches_torays`，**strict xfail** —— 修好即转绿、strict 标记转红，逼修的人回来撤标记 | 内核 `docs/note/ec-raytracing.md` §4.2 · §4.7 · `rfray.rs:1145` |★★★**2026-09-11 同日关闭，并验到四位。** 修法：$\sigma$ 不再是字面量，而是 `PsiMediumInput::sigma_bp`，由 `code/rf_ray` 的门**从文档推** —— $\sigma = -\mathrm{sign}(I_p)\,\mathrm{sign}(\psi_\mathrm{bnd}-\psi_\mathrm{axis})$（安培定律）；文档不声明 `ip` 时仍取 +1 **但 notes 里写明是假设**，不从没有取向声明的文档里读出取向（D5 的规矩）。★**修后同一 oracle 实测**：$N_\parallel$ 差 **1.5e-4**（修前低 25 %）· $|N|$ 差 0.02 % · 轨迹 R 差 **≤ 0.3 mm**、Z **≤ 0.6 mm**（3.5 m 路径）· 最深 ψ̄ 差 0.07 % · 驱动电流 **1.9910e-2 对 1.9031e-2 A/W，比 1.046**（修前 0.907）。★**判据**：`rfray` 48 条 + 门的 Rust 判据 + python 门 14 条 + oracle 8 条**全绿**；strict xfail 已撤（它转绿了，标记随之撤掉 —— 那条机制按设计生效了一次）。★★★**第二检验同日做完，并揭出一件更该记的**：EAST g-file 夹具此前**把 ψ 图整张取负**（`psi_rz.map(|v| -v)` 连 `simag`/`sibry` 一起）—— 取负不改 ψ̄、只翻 $B_\mathrm{pol}$，**那就是 σ=−1，写成了数据变换而不是约定**；于是该夹具里两个错误互相抵消，它的 $N_\parallel$ 判据一直以 1e-3 通过。现在取负去掉、σ 由该参照自己的 `current` 推出，**48 条判据仍全绿**且更准：介质对 GENRAY 自报的 $B$ 差 **4.05e-6**、ec_o 的 $N_\parallel$ 差 **8.99e-6**、(R,Z) 差 5.2e-5 m。★**再加一条不经介质的直算**：GENRAY 自报的 $(B_R,B_Z)$ 沿射线五点对两支公式，σ=+1 两个分量符号都反、σ=−1 同号且主分量差 0.07 %。**推导由两套独立数据各自证成**（CFEDR/TORAY 与 EAST/GENRAY）。★**这一条的教训**：22 条自带判据全绿而符号一直错，因为夹具不声明 `ip` 且自己按同一符号造 $B$ —— **一条判据能发现的东西以它发现的东西为界**，跨实现的参照件是不可替代的。 | 内核 `docs/note/ec-raytracing.md` §4.2 · §4.7 · `rfray.rs` · `tests/test_cfedr_toray_oracle.py` |

| ~~**F-29**~~ | kernel | **`code/steady_current` 的自举路径全仓没有判据在跑**。2026-09-11 关闭 F-26 时按用户裁定（选项 D）把唯一驱动它的那条判据改成 `bootstrap = 0` —— 因为开着自举的那套夹具**问的问题没有定义**（见 F-26）。★**这个洞是本来就有的，不是这次改出来的**：实测全仓只有 `tests/test_evolve_fluxmatch_code.py` 调 `code/steady_current`，而 `stationary.rs` 里只有 1 条 Rust 单测。所以在此之前，自举路径的唯一「覆盖」是一条**注定失败**的判据 —— 那不是覆盖。★要补的是一个**自洽**的带电流态：先匹配、再由它播种一次带电流通道的演化，剖面与 ψ 出自同一跑，然后才谈得上断言 ip。这正是 F-26 里没选的选项 B，单独立条是因为它是**新写一条判据**，不是改一条。★★★**同日关闭（内核 `a418d34`）—— 而且不需要两段式播种**：带电流通道的那一跑**本来就同时带着两半**（`_state` 给剖面、`_lad` 给梯子），**拿它跟自己配**就不用探针、也不用凭空补什么。没有通量的是匹配那一跑，拿它出剖面正是把另一套夹具搞成病态的原因。★实测（自洽态）：**自举开 ip 7.944514e5（0.9931×）· v_loop 0.30372 · |j_ni| 峰值 1.315e5**；自举关 ip 8.000000e5 · v_loop 0.34762 · |j_ni| 0。**这独立印证了 F-26 的判断：求解从来不是问题**——同一条路径在配对态上答 10.2×，在自洽态上落在 0.7% 以内。★★**判据钉的是 `|j_ni|` 的量级，不只是割线落点**：病态配对上它是 **1.03e8 A/m²**（高于任何物理值两个数量级），这里是 1.3e5，**差 800 倍**。因为一个错了几个数量级的自举，仍然可以被一个回路电压抵到正确的总电流上，**那时候只有量级能揭发它**。★**先判空转**：自举关时 `j_ni` 必须**恰为零**、开时必须非零 —— 一条「自举关掉也能过」的自举判据等于没有。变异验过：把判据自己那一跑改成 `bootstrap = 0`，当场红在这一条上，不是红在后面某条。★带宽 2% 对实测 0.69%，理由写在报错里：自举在割线**内部**重算，所以收不到自举关那条判据的六位 | 2026-09-11 关闭 F-26 时并记（全仓 grep 实测）· 同日实现并变异验证 | 2026-09-11 关闭 F-26 时并记（全仓 grep 实测）|

★**本仓能自己做完**的现在是 F-1 / F-2 / F-12 / **F-14** / **F-16** / **F-18**（F-4 · F-5 · F-6 已关）；内核侧只剩 **F-21**（F-15 · F-17 · F-22 · F-23 已关；F-23 的九条里五条是环境缺件的声明式跳过，不是修好；F-21 已排除仓库路径一解，并给出不需第二台机器的判别法）；F-9 要先定走哪边；F-8 要对外联系；F-10 / F-13 / **F-19** 归 fydoc（F-3 · F-11 已关），**F-20** 要第二台机器的实测放电；**F-25 已裁定并做完（ABI 153）· F-26 仍等一次裁定，不等测量；F-27 接线已完成、只余 TORAY 参照件；F-28 已实现、只余 B 类对标；F-30（LH 的 L2 余三段）设计已定待写；F-31 等五项裁定（内核射频线的改动已推 `2e8684d`）**，登记在此只为让本仓知道自己在等什么。

★★那四条 `planned`（`C-06` TEQ · `C-07` TOSCA · `B-09` DINA · `C-10` TRANSMAK）**当日全部执行完毕**，
均判 pass；它们本就不在上表里——**执行是定序册的活，不是本表的活**。本表只收「不做就没人做」的事；
判决与排期的唯一生成源仍是 `docs/benchmark/plan/plan.jsonld`（2026-09-08 晚：**通过 34 · 阻塞 1**，
登记册 35 条 · 17 场景，当日复测 34/34 全过）。

〔仍然成立〕本仓在 A–D 那条链上的位置是**消费方**：E-2 到位则 EAST 标准算例可用于本仓的
对拍与回归；E-4 裁定后计算态才有身份可入库。

★★**2026-09-08 加一句**：E-7 提醒的是**同一个消费方位置上一件更细的事**——本仓从上游取来的
「参照答案」可以既是**忠实转录**又**不是这一炮**。`B-06` 的参照侧就是这样：oracle 忠实地复现了
那份 g-file，而那份 g-file 与本炮交付 EFIT 差 4 倍。**「转录对了」与「转录的是对的东西」是两问**，
本仓的登记册此前只问了前一问。

---

## G. 通用功能评估的落地（`FYL-REPORT-07`，2026-09-12）

`FYL-REPORT-07`（本仓 `docs/report/`）按八项通用功能评估了 fy 体系，出建议 R-1..R-9；
用户「落地 R1～9」同日执行。**落成的不再登记在此**（判据与实测见该报告 §9），本节只留
**仍然开着、且不做就没人做**的那几条。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **G-1** | kernel · fylite | ★★**2026-09-12 大半落地，且把这条的归因改判了**。**做了什么**：马奇实际滞后**六个**量，此前只有三个有名字、一个都交不过去。现在五条带前缀住在 `core_profiles` 上（`fylite:psi_prev` · `sigma_prev` · `exch_prev` · `dn_prev` · `vn_prev`，第六个 `zeff` 本来就有槽），条目块加三行入口（`zeff_prev` · `dn_prev` · `vn_prev`）与两行出口，门按声明的路径读回并塞进入口块；接口摘要 `d10a8fc02a5e6e5c`（只加槽，修订号不动），五个词记进内核 `OURS`。**证据它真的交接了**：把那三个槽从绑定文档里删掉，续跑这一步的 dt 从 **0.0069174 s 变成 0.0076811 s** —— 读到了、也改了答案。★★**改判一**：报告 §9.1 ① 把 61 % 归因于「滞后量交不过去、只能开 `lag_reset`」。实测**不是**：强制 `lag_reset=1` 与不开，读数**逐位相同**（t_end 0.01075795809849784、dt_next 0.006917393144882313），所以那个旋钮从来没动过这些数。★★**改判二（61 % 的分解）**：两条马奇**停在不同时刻**（40 步 0.41806 s 对 20+20 的 0.40008 s），所以「同一位置的剖面差 61 %」大半是时间错位。把控制器钉住（`dt=0.002 dttarget=1e9`）之后两侧 t_end **逐位相同**，残差是 **Te 9.54e-02 · n_e 4.84e-02**，峰值在**紧邻钉死边缘的那个节点**（31 点里的第 29 点），边缘处恰好 0，轴上 3.8e-02。**余**：那 9.5 % 是**剖面往返本身**的缺口（与滞后量无关，dt 已逐位相同），按半径形状看指向边缘/台基那一段的写回；`fylite:state` 要不要从「平的交接单」改成声明过的子树仍归 `-16` G-8。★顺带量到一条：门读 `lag_reset`，而模板的词表不认它（`fy run` 按名拒绝，只能走 `--plan`）—— 正是 G-2 揭开的那三套命名之别的又一例。**原文**：**`fylite:state` 的形归 `-16` G-8**。本次给了它名字（记录里的交接单）与三个读者，但**形**是一份平的清单，不是一棵声明过的子树。直接后果量到了：`code/evolve` 的三条滞后量交不过去（内核从 `evolve/fylite:*` 读、写进自己的原始条目块，而扁平树只收声明过的表里的槽），常数闭合下逐位相同，新经典闭合下 Te 差 **61 %**——今天靠打开内核自己的 `lag_reset` 如实处置 | `FYL-REPORT-07` §9.1 ① |
| ~~**G-2**~~ | kernel · fylite | ★★**2026-09-12 落地**：`code` 层的参数面现在**有一处声明，且是完整的**。推导 `rust/tools/case_params.py` 走门体**与门可达的每一个局部函数**（拒绝跟进派发器 `run` / `raw_entry`），`rust/build.sh` 把它生成进四个宿主（`fyo-interface.json` · `_fyo_interface.py` 的 `CODE_PARAMS` · `fyo-interface.js` · `fyo_interface.rs` 的 `CODES`），`fy list kernel` 文本按 `键=缺省` / `键!`（必填）逐门印出，`--json` 另带类型与**读它的函数**。实测：**35 扇门、617 条声明、342 个不同的键**；最大的 `code/evolve` 130 条，`code/transport` 29 条。★**完整而非下界，这是要点**：宿主拿不完整的声明当筛子，丢值是静默的（R-1..R-9 落地时量到两次）——所以 build 与 `tests/test_case_params.py` 双向卡：两个源文件里每一处 `s.<verb>("键")` 都必须至少被一个码声明。★**同一份推导也交给了图生成器**，于是 `fylite-kernel.jsonld` 的参数节点从 588 升到 **726**（1849 → 1987 节点），今早记下的「图是下界」当日消掉（`FYL-FYO-01` §4）。★**三套命名的关系量出来了**：模板的 `fylite:vocabulary` 是**页面控件名**，与门收的名字不是一回事（`btol`→`b_tol` · `wflux`→`weight_flux`），故公开侧闸子**不求两者一致**，只钉「交集不许变小」（`breakdown` 2 · `discharge` 10 · `evolve` 37 · `reconstruction` 2 · `transport` 16 · `zerod` 20）并记下 `pfwave` · `profile` · `series` 三个模板**没有对应的门**。**原文**：**code 层的参数面没有任何声明**（`FR-KERNEL-002` 在这一层未兑现）。模板的 `fylite:vocabulary` 是**语料用过的名字**（装配层），内核的 `*_PARAMS` 是**原始入口**的参数：`code/transport` 两者交集为 **0**，六个 code 连 `*_PARAMS` 块都没有。★本次落地被它绊了两次，两次都**不报错**——按模板筛端口，交接文档一份没绑；按模板筛参数，续跑从 t = 0 起而退出 0 | `FYL-REPORT-07` §9.2（C-28）·`-16` K-2 |
| **G-3** | fylite | **`run_state` 的 `cancelled` 没有产者**。枚举已收成一份（七值 + 反解），而 `fy run` 一次门调用跑完整个 march，**宿主没有介入点**；要产它得先把 `-18` U-8 的「一串门调用」搬进 `run.rs` | `FYL-REPORT-07` §9.3（C-5） |
| **G-4** | fylite | ★**2026-09-12 半落，并把挡路处说清了**。**做了**：`page_model.html`（行军那条 bar 所在的页）现在**自带** `run.js` 与 `checkpoint.js`（源 `pages/model.html`，经 `make-page-v2.mjs` 生成）；闸子 `validate-checkpoint.mjs` **不再 `addScriptTag` 注入**它们，改为先断言页面自己带了 —— 此前正是那次注入让「没有任何页面加载它们」躲过了闸（闸过的是产品没发的模块）。实测闸子仍全绿（含「每 N 步落一次断点；仓拒绝时行军照走」saved=2）。★★**真正挡路的不是 `<script>`**（这是本次量到的、比原话更准的一句）：**页面根本不产生 `spo:ComputationRecord`** —— 全仓扫 `app/assets/*.js`，只有 `bundle.js` 会**读**记录、`checkpoint.js` 会**校验**记录，没有一处**造**记录；而「断点就是记录」（U-10，有意不设断点格式），所以仓里没有可存的东西。**余**：在浏览器那侧造出记录（要么把运行时的 `record()` 那一套在页面上镜像一份，要么判定记录只由宿主产、页面只存宿主交来的那一份——这是一条口径裁定），之后断点闸才能换掉确定性假件 | `-18` 分期 U0 · `FYL-REPORT-07` §9.3 |
| **G-5** | fyo | **schema 改不动，因为 `../spo` 不在**：`PROJECT.md` §5 要 `check_conformance.py` 退 0 才准提交 `schema/`。`FYO-ADR-14` / `-15` 已立（Proposed），铸类待检出到位 | fyo `FYO-ADR-15` OI-1 |
| **G-6** | kernel · fylite | **`-20` 的三张表未生成**。`surface/` 今天算得出，`coverage/` 的 `covers` 边要人逐条判断，`availability/` 的 286 格要装置卡片在场——单出一张会把「一处产、两处渲染」变成「渲染半张」 | `-20` M-1 · G-1 · G-5 |

★**已关的**（同日，各带判据，详见报告 §9.4）：C-1 · C-2 · C-3 · C-4 · C-6 · C-10 · C-11 ·
C-12 · C-15 · C-16 · C-21 · C-23 · C-26 · C-27，共十四条。

---

## 记法

- 条目**只登记事实与依据**，不写结论；带〔推测〕的判断保留标记。
- 归属为他仓者，本表**不得**作为在本仓改动的依据。
- 关闭一条时写明**关闭日期与依据**（如 E-1：2026-09-06，逐片 `magnetics/ip` 实测）。
