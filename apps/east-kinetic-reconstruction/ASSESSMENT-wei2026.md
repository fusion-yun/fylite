# 评估：用本应用复现 Wei et al. 2026（EAST 关键剖面快速处理），实现 EAST 数据快速分析

> 文献：D. Wei, F. Wen, G. Li *et al.*, “Rapid processing of experimental data for key plasma profiles on EAST
> tokamak”, *AIP Advances* **16**, 085007 (2026), doi:[10.1063/5.0324803](https://doi.org/10.1063/5.0324803)（CC BY 4.0）。
> 评估日期 2026-09-18；本页里凡写「实测」的，都是这一天在本应用（`kinetic_recon.py`，只依赖 `libfylite.so`）
> 与 EAST 数据服务器上跑出来的。

## 一句话结论

**文献那条链的六步里，fylite 已经有四步的「核」，本应用已经通了其中的取数与平衡两步；缺的是数据绑定
（反射计 · XCS · ECE · 加热功率）、剖面拟合的台基形（mtanh 分段）、加热沉积接进功率平衡，以及批处理。**
★〔2026-09-20〕这一句写于 2026-09-18，**「数据绑定」那一项已经不对了**：反射计 · XCS · ECE 与 LH / EC 功率今天都在
fydoc `develop` 的 `bind/mdsplus/` 里；今天真正还缺的是**逐炮 EC 镜角 · 3# 回旋管功率时序 · LH 的 `position.phi` ·
bolometer 绑定**这四格，逐条见〈二〉。其余三项（台基形、沉积接进功率平衡、批处理）都已落地，见〈六〉。
复现文献的**剖面与输运结果**是可行的，预计三个阶段；复现它的 **「35 片 ≈ 20 s」** 这一指标，需要先说清楚一件事：
文献的 20 s 里**没有平衡反演**（它直接读 P-EFIT 的答案），而本应用的平衡是自己反演出来的——按 fylite 2026-09-15 的裁定，
efit_east 树只能作对拍，不能作建模输入。所以同一指标要换一种说法比：见〈四〉。

> **2026-09-18 更新：三段都做了，实测数见〈六〉。**〔2026-09-20 订正数〕#63948 4–8 s 的 T_e / n_e NRMSE
> **0.036 / 0.0069**（文献 0.03–0.09 / ≈ 0.015）；#81481 的 χ_e · χ_i 形与量级对上、交叉点 **ρ 0.46** 对 0.58；
> LH 驱动电流取决于未标定的 η_cd；一炮 **约 1.6 分钟**（2026-09-18 是约 5 分钟）。
> 复现不了文献主算例的根因是装置事实：#97034 以前的磁测量给错了一组道（已在 fydoc 修）。

## 一 · 文献做了什么

| 步 | 文献的做法 | 数 |
| :--- | :--- | :--- |
| 1 取数 | 只给炮号：MDSplus 取 TS（T_e · n_e）、微波反射计（n_e 主源）、XCS（T_i）、ECE（TS 缺时顶 T_e）；以 TS 时刻为基准、其余诊断在 50 ms 窗内配 | — |
| 2 坐标 | P-EFIT（MDSplus 上的平衡答案）→ ρ_tor | — |
| 3 清洗 | 迭代加权样条作全局参照 + 邻域 MAD 的局部稳健尺度；上下两侧各一对阈值（偏离全局 **且** 偏离邻点线性趋势才算离群）；**一次只剔最坏的一个**，重算后再判；边界点不剔；阈值由 50 片人工标注集按 0.5 F1 + 0.3 recall + 0.2 precision 定 | — |
| 4 拟合 | 按 H98 分 L / H 模；L 模全段样条；H 模芯部样条 + 台基 mtanh（台基顶先加权平均再非线性最小二乘）+ ρ ∈ [0.8, ρ_e] 按斜率积分平滑过渡；轴上导数为零；T_e,sep 固定 50 eV；T_i 台基取 T_i = T_e | NRMSE：n_e ≈ 0.015，T_e 0.03–0.09（#63948，4–8 s） |
| 5 加热 | LHW：METIS 的快速模块；ECW：TORAY（接在 ONETWO 里） | #81481 @ 5.3 s：LH 驱动电流 METIS 181.3 kA 对 GENRAY+CQL3D ≈ 176 kA |
| 6 输运 | ONETWO 功率平衡反解 χ_e · χ_i（含辐射、电子–离子交换） | — |
| — | 批处理 | #63948 35 片 19.95 s（ECW+ONETWO 5.15 · LHW 7.07 · 清洗+拟合+I/O 7.72） |

## 二 · 逐步对照：本应用与 fylite 今天到哪

| 步 | fylite / 本应用今天 | 缺什么 | 活儿在哪 |
| :--- | :--- | :--- | :--- |
| **1 取数** | **通**（实测）：本应用 `pull` 经 `libfylite.so` 的 mdsip 客户端取 `east` 树磁测量 · PF · Ip · TF · POINT 11 弦与 `ts_east` Thomson、`analysis` 的 TXCS T_i0；#137985 @ 4.041 s 与 B-06 离线归约件逐道相差 ≤ 4×10⁻¹³ | ★〔2026-09-20 复核 fydoc `develop` 68b7d8b 订正〕**①「反射计 · XCS · ECE 没有绑定」已不成立**：`facts/device/east/abox/bind/mdsplus/` 下今天有 `thomson_scattering` · `reflectometer_profile` · `spectrometer_x_ray_crystal` · `ece` · `nbi` · `interferometer` · `polarimeter` · `tf` · `summary`（另有 `magnetics_*` · `pf_active_pcs` · `ic_antenna`）。**仍缺的是 `bolometer` 的绑定**（只有 `static/now/bolometer.jsonld` 的静态描述，`bind/mdsplus/` 下没有这一件）；② 只取**最近一个** TS 脉冲，没有「以 TS 时刻为基准、其余诊断 50 ms 窗内配」；③ **旧炮 TS 时刻列不是秒**：#63948 的 `\TE_CORETS` 第 0 列是 76.9…128.3（35 个脉冲），按「最近时刻」取会取错 | ① fydoc A-Box 补 `bolometer` 绑定（装置数据，内部）→ 重生装置文档 → 重建库；② ③ 本应用 |
| **2 坐标** | 本应用用**自己反演的平衡**（档 M，拟合自己剔坏道）给 ψ_N；档 P 的外环把测点逐遍重映 | ρ_tor 要环向磁通：`code/ladder` 描迹出每个面的量，本应用还没接；**不读 P-EFIT**（裁定：efit_east 树只作对拍） | 本应用接 `code/ladder` |
| **3 清洗** | 档 P：对 p(ψ_N) 做 GCV 多项式光滑拟合，**一次剔最坏的一个**、超过 4σ 才剔——与文献同一个骨架（★2026-09-20 更正：这个**全局**拟合在峰化剖面上把芯部整段当离群剔光——#63948 5.022 s 剔 9 / 24、其中 7 个真实芯部点，档 P 储能 13.8 kJ，是实测 W_dia 的五分之一；档 P 的缺省已换成右栏的局部判据（`wei_profiles.clean_profile`，T_e、n_e 分别做），旧法留在 `--thomson-clean smooth`，另加清洗自检，见 README §2.4 · §3.4） | 文献的判据更细：**局部** MAD 尺度 + 邻点线性趋势的双条件、上下侧分开的阈值、边界点不剔；而且逐诊断（T_e、n_e、T_i 各自）做，不只是压强 | 本应用（纯数值，标准库即可） |
| **4 拟合** | `code/profile_fit`：移位勒让德多项式 + GCV 定阶（无台基形）；`code/separatrix_align` 能按 T_e,sep 平移标签 | **mtanh 台基 + 芯部样条 + 平滑过渡**没有；L / H 判别要 H98（`code/zerod` 有 0-D 账，未接） | 新增拟合：本应用内实现（非线性最小二乘，标准库可写），或内核加一扇门 |
| **5 加热** | `code/wave`：LH 快速沉积与驱动（n∥ 上移、驱动效率参数化）；`code/rf_ray`：EC 射线追踪（ECCD 部分落地）与 LH 射线；装置文档带 LH1 / LH2 频率 · n∥ 带 · 额定功率，EC 四路 140 GHz · 模式 | ★〔2026-09-20 复核 fydoc `develop` 68b7d8b 订正〕**①「LH / EC 没有 MDSplus 节点」已不成立**：`bind/mdsplus/lh_antenna.jsonld`（2 件天线）与 `bind/mdsplus/ec_launchers.jsonld`（PECRH1I…PECRH4I 四件 `power_launched`，`*1000` kW → W）都在。**仍缺的三格**：（a）**逐炮 EC 镜角**——`providers/ec_launchers/` 下只有 `li2023_81481` · `li2023_81490` 两件覆盖层，别的炮没有，也没有 MDSplus 节点；（b）**3# 回旋管的功率时序**——绑定在，`PECRH3I`（及 2# · 4#）在 #81481 的树上没有数，Li 记的那约 0.55 MW 拿不到；（c）**LH 天线的绝对环向位置**——`static/now/lh_antenna.jsonld` 里两件的 `position.phi` 都还是空表（端口字母 → φ 零点无公开出处，只落了端口间角差）；② 没有与 GENRAY+CQL3D / METIS 的对拍记录 | A-Box 补逐炮镜角与 `position.phi`（要装置方来源）；本应用接 `code/wave` · `code/rf_ray`（已接，见〈七〉〈八〉）；对拍 #81481 @ 5.3 s（文献给了 181 kA / 176 kA 两个答案） |
| **6 输运** | `code/interpretive`：给剖面反解 χ_e · χ_i，含辐射（ADAS）· α · 由环电压给的欧姆项 · 能量账；**有记录**（校验册 `tr-coupling-interpretive-inversion`：往返按 h² 收敛到 3.8×10⁻⁵；返回的是 χ₀/gm7，约定照实记） | ① **源项只收参数化的高斯沉积**（`dep_centre` · `dep_width`），接不进 `wave` / `rf_ray` 算出来的沉积剖面——这是链上**唯一要动内核的一处**；② 电子–离子交换项、环电压的来源要核 | 内核：`code/interpretive` 加「给定源剖面」的输入；本应用接它 |
| — 批处理 | 本应用一次一片：档 M 每片约 90–100 s（16 个竖直设定点 × 3–4 轮剔道，每轮 13–30 s），三档合计 125–145 s，`pull` 每片约 120 s（每片把约 170 个节点的全时序重读一遍） | 见〈四〉 | 本应用 |

另两处实测到的、做批处理前必须先修的：

- **旧炮的磁测量排布不同，本应用的起步规则在那里失效。**#63948 与 #81481 解析到的是 35 环 · 38 探针
  （provider `base` / `pcs`），环全叫 `FL<n>A`——本应用「从 FL*B 组起步」的规则一个环都选不到，档 M 会在没有环的情况下起步。
  要加一条回退：没有 B 组时从全部环起步（或先只用探针、收敛后再按残差收回环）。
- **`east` 树不总开得了。**同一天上午服务器对 #137985 报 `cannot open east shot 137985`，下午恢复。批处理要把
  「这一炮取不到」当作一条正常的输出，而不是整批失败。

### 数据缺口复核（2026-09-20）：「服务器上有数、A-Box 未绑」的九项已绑；真缺的是下面这些

fydoc 分支 `data/east-equilibrium-binding`（节点名四炮 #63948 · 81481 · 81490 · 137985 逐个只读实测）与本仓
`tools/abox-to-facts.py`（编进装置事实的 `fylite:signal`）：

| 项 | 绑到哪 | 哪几炮有数 |
| :--- | :--- | :--- |
| 平衡（**只作对拍**） | `equilibrium`：`efit_*`（efit_east）· `pefit_*`（pefit_east，**文献所用平衡的来源**）· `efitrt_*`（efitrt_east） | efit · efitrt 四炮；**pefit 三炮，#137985 无树** |
| CXRS T_i · v_t | `charge_exchange`：`cxrs_east:\TI_CXRS_T` 等 | 只 #81481 · 81490；5 道 R≈1.87–2.04 m，3.98–7.11 s |
| 约束量 | `magnetics/fylite:signal`：`h98_mhd` · `h98_dia` · `taue_mhd` · `tau98` · `tau89` · `betap_dia`（energy_east）· `w_mhd_efit` · `betan_efit` · `betap_efit`（efit_east） | energy_east 只 #63948 · 137985（后者 H98_DIA 全零）；efit_east 四炮 |
| 硬 X 射线 | `hard_x_rays`：`\HX1…20` 原始电压 | 前三炮 20 道；#137985 只 HX10–20 |
| 中子 | `neutron_diagnostic`：`\ZZ1 · ZZ2 · ZZ7 · ZZ8 · NF1–3` | 四炮（#137985 上 ZZ7 · ZZ8 全零） |
| LH `\PLHS<n>` | `lh_antennas[].fylite:plhs`（V；**含义未知**） | 四炮 |
| 辐射 | `bolometer.p_rad` 覆盖更正 | #63948 · 81481 · 81490；#137985 无数 |
| **Z_eff**（线平均） | `bremsstrahlung_visible`：`brem_east:\ZEFF_AVE` · `\ZEFF_AVEERR`（**须直接开该树**——此前「无 Z_eff」是经 analysis 树误判） | #63948 · 81481 · 81490；#137985 无树 |
| NBI 加速电压 / 束流 | `nbi`：`v1l…i2r`（`east:\NBI<n><L,R>HV · HI`）——V×I 是**电功率** | 四炮 |

**仍然真缺（服务器上没有，或有槽无数）**：NBI **吸收**功率（只有源功率与电功率，中性化 · 穿透损失无处取）；逐炮 **EC 镜角**
（#81481 · 81490 靠文献覆盖层）；2019 年代各炮的 **回旋管 2–4 功率**；**LH 沉积**剖面（只能算）；**逐弦辐射功率**
（`\PRAD_AXUV` 等在树模型里而四炮无数，直接开 prad_east 亦然）；#81481 · 81490 的 **W_dia / H98**（无 energy_east 树，
只有 EFIT 的 W_MHD）；**MSE**（mse_east 在 #137985 上有树而 `MSE0` 空）；**Z_eff 剖面**（`\ZEFF` 槽空；#137985 连线平均也没有）；
CXRS 在 #63948 · 137985 上没有，有的两炮也只有 5 道。

**首次对拍**（`kinetic_recon.py compare`；#137985 4.041 s 对离线 / 实时 EFIT，#63948 5.022 s 对三者）：同尺自检复现 EFIT / P-EFIT
自报的 l_i · β_p · W 至约 1 %，故表里的差是解的差。读数是取回的实验数据派生量，**不入仓**（留在运行目录）；定性结论：
边界平均相差 1–2.5 cm、磁轴相差 3–7 cm（我们偏外）、ψ_N 图 rms 差 0.03–0.07；q95 在 #137985 上我们低约 0.6–1.2；
#63948 上档 M 的 q0 · l_i 与 EFIT / P-EFIT 差得大（纯磁测量定不住芯部），档 K 靠近；该炮档 P 无效——`pull` 取到的 Thomson
脉冲时刻离该片 45 s（`ts_east` 在该炮的时基不可用），压强约束是错的。★2026-09-20 补：时基修好（脉冲 5.022 s）之后重测，
档 P 仍无效，原因换成了清洗——旧的全局光滑拟合剔光了芯部（W 13.8 kJ）；换成局部清洗后 W 37.7 kJ、q0 1.021、
l_i(1) 1.702（与离线 EFIT 的差：q0 < 0.01、l_i 0.09），与本评估阶段 A 独立积出的热储能 32.4 kJ 相容，余下对 W_dia 的差额在非热部分（README〈七 · 已知限制〉）。

## 三 · 复现路线（三个阶段，每段有验收数）

| 阶段 | 做什么 | 验收（对文献） |
| :--- | :--- | :--- |
| **A · 剖面**（只动本应用 + 装置绑定） | 反射计 / XCS / ECE 的 MDSplus 绑定补进装置事实并重建库；本应用：TS 时刻列的单位判别、以 TS 为基准的 50 ms 配窗、文献的局部稳健清洗、芯部样条 + mtanh 台基 + 斜率积分过渡、T_e,sep = 50 eV、T_i 台基 = T_e；输出 NRMSE | #63948，4–8 s：n_e NRMSE 均值 ≈ 0.015、T_e 在 0.03–0.09 之内 |
| **B · 加热** | 注入功率节点补进装置事实；本应用接 `code/wave`（LH）与 `code/rf_ray`（EC），EC 发射几何由用户给 | #81481 @ 5.3 s：LH 驱动电流落在 176–181 kA 附近，沉积峰位与文献图 8 一致 |
| **C · 输运** | 内核：`code/interpretive` 收给定源剖面；本应用把 A 的剖面、B 的沉积、档 M 的平衡（经 `code/ladder` 给度规）交给它 | #81481 @ 5.3 s：χ_e · χ_i 的形与量级对文献图 9(c)；能量账的 W 对磁测量储能（文献自己列为下一步的那项核对） |

★三段都在 fylite 的包络里：单机、不要调度器、不引外部程序（ONETWO · TORAY · METIS 不进来，只用 fylite 自己的
`interpretive` · `rf_ray` · `wave`）。因此复现的是**文献的方法与结果**，不是它的代码；两者的差（快速 LH 模型不同、
EC 用射线追踪而非 TORAY）要在 B 段的对拍里量出来。

## 四 · 「快速」这件事

文献的 **35 片 ≈ 20 s** 是：取数 + 清洗 + 拟合 + LH + EC + ONETWO，平衡用 P-EFIT 的现成答案。本应用同一件事要多做一步
**自己的平衡反演**（这是它比文献多给的东西：平衡与剖面自洽，而且不依赖 P-EFIT）。按今天的实测，一片的账是：

| 环节 | 今天 | 能压到 | 怎么压 |
| :--- | ---: | ---: | :--- |
| 取数 | 约 120 s / **片** | 约 120 s / **炮** | 每个节点的全时序只读一次，在所有时刻上归约（归约本身是毫秒级） |
| 档 M | 90–100 s / 片 | 约 3–8 s / 片 | 第一片照今天做（扫设定点 + 剔道）；后面各片**继承**上一片的剔道结果与竖直设定点，只在它附近 2–3 个设定点上解 |
| 剖面清洗 + 拟合 | — | 亚秒 / 片 | 纯数值 |
| 加热 + 输运 | — | 待测 | `code/wave` 是快速模型；`code/rf_ray` 的射线数可调 |
| 并行 | 无 | × 核数 | 各片独立（标准库 `multiprocessing`），每个进程一份库 |

估算：35 片、8 核，**约 2–3 分钟一炮**（其中 2 分钟是取数）。这比文献的 20 s 慢一个量级，但换来的是一份**自己反演、
自洽、带逐道残差与外环证书**的平衡；若只要剖面与 χ（与文献同口径），可以让档 M 只在第一片全扫、其余片只做一次求解。
★这两个数都是估算，要在 A 段做完后在 #63948 上实测替换。

## 五 · 风险

- **数据绑定是装置数据**：反射计 · XCS · ECE · 加热功率的节点名属 EAST 装置描述（权利方 ASIPP、不公开），补在 fydoc 的
  A-Box 里、编进内部版的库，由本应用从库里解析——与磁测量 · PF · POINT 今天的做法相同。★本应用脚本里写死的节点名只有
  Thomson · TXCS · TF · PCS Ip 那几个（`\TE_CORETS` 一类），与 fylite 公开代码 `python/fylite/io/mds.py` · `raw.py`
  里的写法一致；新增的诊断不再往脚本里写。
- **旧炮的数据形态**：TS 时刻列单位、磁测量排布都随年代变；#63948 是文献的主算例，A 段要先把它跑通。
- **快速模型的偏差**：文献自己量到 METIS 与 GENRAY+CQL3D 的沉积形状有差；fylite 的 `code/wave` 是另一种快速模型，
  差多少只能在 B 段量。
- **本应用的依赖约束**：只依赖 `libfylite.so`、不要 numpy。mtanh 的非线性最小二乘与样条要用标准库写（量不大）；
  若性能不够，改为内核加一扇拟合门，仍不破坏这一约束。

## 六 · 做完了什么（2026-09-18 实测）

三段都落了地；数全是这一天在 EAST 数据服务器上、用新构建的 `libfylite.so` 跑出来的（结果 JSON 带实验数据，不入仓）。

**做了的改动**

| 仓 | 改动 |
| :--- | :--- |
| fylite_kernel | `code/interpretive` 收给定源剖面（`sources = table`）与电子–离子交换（`exchange = 1`）；Rust 门 4 道，全套 `cargo test` 797 + 233 过（`FR-TR-011` 记录 1.2） |
| fydoc（私有） | TS · 反射计 · XCS · ECE · NBI · 抗磁能 / 环电压的 MDSplus 绑定；★**#97034 以前的磁测量改由新提供者 `base_tb` 给**（见下） |
| fylite | `tools/abox-to-facts.py` 把绑定编成 `fylite:signal`，LH / EC 功率节点不再丢（本应用就是它们的读者）；本应用加 `wei2026.py` · `wei_profiles.py`；`kinetic_recon.py` 旧炮的 TF 电流回退与起步环组回退 |

**★★顺带查出的装置事实错误**（本应用复现不了文献主算例的根因）：#97034 以前的 `east` 测量链解析到提供者 `base`，
它给的是 **FL*A 环与 HBP*N（法向）探针**——不是 EFIT 拟合的那一组。#63948 · #81481 上实测：**FL*B / 2π 与
efit_east `SILOPT` 逐道差 < 1 %、HBP*T 与 `EXPMPI` 差约 1 %**；FL*A 的比值散在 −1…1.2，HBP*N 读数小一个量级。
用旧名字，档 M 在 #63948 5.02 s 解出 χ²/dof 4.4×10⁴、磁轴 R 2.33 m 的无意义平衡；换成 FL*B · HBP*T（角度 = 法向角 −90°）后
**χ²/dof 0.855、磁轴 (1.898, 0.022) m、q95 6.13**（efit_east 对拍：1.904, 0.014, 5.99），剔掉的 5 根探针正是与 EFIT 对不上的那 5 根。
fydoc 已加提供者 `base_tb` 作 [0, 97030] 的 `east` 链缺省，旧组留作链 `east_na`。★按 2026-09-15 裁定 efit_east 只作对拍，
fydoc 里它的比对记为待证，独立的证据是内核反演本身。

**阶段 A · 剖面（#63948，TS 全部 35 个脉冲）**

★下表换成 **2026-09-19 晚缺省（`--scan coarse --nk 10`、`--workers 12`、每段 5 路）那一遍**的读数；括号里是 2026-09-18 的旧读数。

| | 本应用 | 文献 |
| :--- | :--- | :--- |
| 4–8 s 的 18 片：T_e NRMSE | 均值 0.036，0.011–0.055（旧：0.036，0.016–0.060） | 0.03–0.09 |
| 同上：n_e NRMSE（18 片全用反射计） | 均值 **0.0069**，0.0048–0.0094（旧：0.006，0.004–0.008） | ≈ 0.015 |
| 同上：T_i（XCS 6 道弦）NRMSE | 均值 0.103，0.058–0.150（旧：0.104，0.061–0.149） | 未报 |
| 全炮 35 片 | 35/35 片成；T_e 均值 0.0429（最差 0.199 在 0.623 s 的爬升段）、n_e 0.0075、T_i 0.105 | — |

- 清洗阈值文献没公布：低侧取 k = 2.0、α = 0.5，**按文献图 1 的剔除样式定**（2026-09-19 晚那一遍里 #63948 6.022 s 剔
  ρ 0.04 · 0.10 · 0.12 · 0.24 · 0.46 · 0.76 · 0.93——2026-09-18 是 0.09 · 0.11 · 0.22 · 0.49 · 0.78 · 0.94 六个点，ρ 随平衡走、
  样式不变；文献 5.98 s 标 0.07 · 0.2 · 0.45 · 0.5 · 0.67 · 0.8 · ~0.97，同一组 TS 坏道），不按 NRMSE 调。(3.0, 0.7) 时轴附近孤立的
  低点留下；★2026-09-18 记的「4.422 s 的 T_e NRMSE 到 0.16」**已被超过**：同一片在 2026-09-19 晚的缺省下是 **0.0123**
  （设定点 +26 mm、q₀ 1.662；`--workers 16` 时这一片走另一条热启动链，0.0599）。
- 两处本应用补的、文献没说的：参照曲线的 IRLS 从 5 点滑动中位起步（两个相邻坏点时等权起步会把高点当离群）；拟合光滑度由
  GCV 定（λ ≥ 1×10⁻⁶，下限不让反射计那种已经光滑的剖面被插值穿过）。n_e 的 NRMSE 比文献低，可信的解释是反射计剖面本身
  就是反演光滑过的——没有为了贴近文献去调。
- **L / H 判别不一样**：H98 按 IPB98(y,2)，W 取抗磁能；**2026-09-20 起** P_loss = P_absorbed − dW/dt
  （`--h98-recipe loss`，缺省；dW/dt 由抗磁能在相邻 TS 片上有限差分，首片前向、末片后向）；旧账
  （LH + EC + NBI 源 + 欧姆，不减 dW/dt）留在 `--h98-recipe legacy` 上逐位可重算。
  辐射功率自 fydoc 新立的 `bind/mdsplus/bolometer`（`analysis:\PRADTOT_AXUV`，AXUV 总辐射）**逐片记进
  `h98_inputs.account`，但缺省不减**：IPB98(y,2) 的拟合口径本身就是 P_heat − dW/dt，且本装置只取得到**总**
  辐射、逐弦剖面节点三炮无数，分不出芯部与偏滤器/SOL；`--h98-prad total` 可减一份总辐射作敏感性。
  ★**数没有往 0.943 走**（#63948 全炮 35 片）：dW/dt 在平顶段只有 ±0.03 MW 对 P_loss ≈ 2.5 MW，
  H98 4–8 s 从 0.586–0.701 只动到 0.586–0.700（判别仍是 34 L / 1 H）；再减 0.42 MW 总辐射也只到
  0.623–0.739（33 L / 2 H，5.022 s 一片改走 H 模分支）。
  文献在 5.98 s 记 0.943（H）。**装置自己的 `energy_east` 在同一时刻记 `H98_dia` = 0.761、`H98_mhd` = 0.875**
  ——两者都不是 0.943，故文献的数也不是「读装置的 H98 节点」。本应用 6.022 s 的账是 W_dia 66.3 kJ、
  P_loss 2.51 MW（其中 LH 耦合功率 2.25 MW、NBI 源 0.23 MW、欧姆 0.024 MW）。H98 ∝ W·P^−0.31，
  要从 0.650 走到 0.943 得 W 大 1.45 倍、或 P_loss 小到约 0.7 MW（即只算不到三成的 LH 功率）。
  **剩下的口径差最可能在 P**：文献走 ONETWO/METIS，用的是模型给出的**沉积（吸收）功率**，不是天线耦合功率；
  本应用的 `profiles` 一路没有逐片的 LH 沉积（`code/wave` 只在 `transport` 里跑）。这是剩下最大的一处口径差。

**阶段 B · 加热（#81481，TS 只存 6 幅，离 5.3 s 最近的是 5.517 s）**

- LH2 净功率 2.80 MW（入射 2.91 − 反射 0.11），`code/wave` 取 n∥ 上移 1.5–2.5（内核自己的 LH 测试用的范围；不上移时一点也沉积不下）。
- **I_LH = 479 kA（η_cd = 1×10¹⁹）；给出文献的 181 kA 要 η_cd = 3.8×10¹⁸**。功率沉积峰在 ρ ≈ 0.37（0.56 MW/m³），文献 METIS 0.24、
  GENRAY 0.30；电流密度峰在 ρ ≈ 0.12。★快速模型的 η_cd 是**要标定的系数**，本应用不按文献去调它——这一对数说的是
  「标到 181 kA 要多少」，不是「算出了 181 kA」。
- EC 0.48 MW（减炮前基线后）：发射几何不在装置事实里，没算（`--ec-launch` 给了才算）。

**阶段 C · 输运（同一时刻）**

- n_e 由 POINT 8 道弦拟合顶替（n_e0 = 4.9×10¹⁹，文献图 8a ≈ 5.3×10¹⁹）：这一炮没有反射计，5.517 s 的 TS n_e 有 1–4×10²⁰ 的坏道。
- T_i 只有芯部 T_i0（0.96 keV，文献 ≈ 1.1 keV），形状是假设的（抛物型 + 台基 T_i = T_e）。★先试过「T_e 形状缩放」，中半径
  T_i ≈ 0.16 T_e，交换被放大到等于全部注入功率——换掉了，理由记在代码里。
- **χ_e 0.07 → 0.9（ρ 0.5）→ 12（ρ 0.82），χ_i 1.3–1.7，两者交叉于 ρ ≈ 0.55**；文献 χ_e 0.35 → 10（峰 0.82）、χ_i 1.9–7、交叉于
  ρ ≈ 0.58。芯部交换 0.19 MW/m³，文献 qdelt ≈ 0.2 MW/m³。形与量级相符；χ_i 的绝对值系于假设的 T_i 形状。

**「快速」的账（实测替换〈四〉的估算）**：#63948 一炮 35 片，取数 52 s（每炮一次）+ 各片 251 s（12 进程，分段顺序热启动：
每段第一片全扫 85–185 s，其后每片约 30 s）≈ **5 分钟**。比文献的 20 s 慢一个量级，差在平衡反演（文献直接读 P-EFIT）；
剖面清洗与拟合每片约 1 s。
★2026-09-19：档 M 的冷启动全扫改为缺省粗扫（`--scan coarse`：16 个设定点先在 33² 上排序，只把前 4 个在 65² 上复核，
见 README〈三 · 所用算法〉§3.3）。同一台机器、同一份库、同一负载下两种扫法各跑一遍全炮：35 片的平衡与剖面逐片相同，
各片墙钟 **235 s → 192 s**（12 段的第一片冷启动 85–179 s → 48–124 s；墙钟由最慢的一段定，热启动各片不变）。
★2026-09-19（晚）：档 M 的反演外迭代缺省开 Anderson 混合（`--anderson 10`，内核 ab5e3444 起的库；只混固定日程之后的
Picard 尾巴，冷热两种扫都带，见 README §3.2；逐片的复核记录在 git 历史里的旧版 README）。同一份新库、`--scan coarse`、12 进程各跑一遍全炮：
各片墙钟 **184 s → 128 s**（`anderson 0` → `10`；12 段冷启动的首片 44–116 s → 40–74 s；热启动各片本来就只有 20–30 s，省得少），
加取数 52 s 共**约 3 分钟**。35 片里 34 片的平衡与剖面相同；**0.822 s 一片不同**：热启动在 −38…−22 mm 上解，−22 mm
纯 Picard 跑满 4000 轮不收敛，Anderson 让它收敛且 χ² 更小，设定点 −34 → −22 mm、q₀ 1.83 → 1.00、T_e NRMSE 0.052 → 0.099，
全炮 T_e NRMSE 均值 0.0418 → 0.0432（n_e 0.0080 不变）。换库本身（χ² 后处理掩码修复）在 `anderson 0` 下已把 T_e 均值
从 0.0415 移到 0.0418、多数片的设定点移 4–8 mm。
★2026-09-19（更晚）：三处提速，缺省换成 `--nk 10`（内核 `feat/gs-newton-krylov` 的 Newton–Krylov 外迭代，带失败即回卷）、
每个切片进程里档 M 一轮的独立设定点**并行**（`--jobs`，缺省 CPU 数 // `--workers`：64 线程、12 段 → 每段 5 路），以及
POINT 归约改用二分取窗（测量字典与原做法逐字节相同；原来每片约 16 s 花在把 22 条弦各 425 万个采样全扫两遍上，现在 0.5 s）。
同一炮 35 片、12 进程：各片墙钟 **42.8 s**（每段 1 路 61.5 s；同样代码下 `--anderson 10 --nk 0` 每段 1 路 141 s），加取数 51 s
共**约 1.6 分钟**（上一版约 3 分钟）。设定点并行不改答案（每段 1 路与 5 路 35 片逐片相同）；换加速器改了两片：0.822 s
（与 Anderson 一样救活 −22 mm，但 q₀ 1.52 而不是 1.00，T_e NRMSE 0.074）与 0.623 s（冷启动首片剔 7 道而不是 8 道，q₀ 4.52 → 5.08），
全炮 T_e NRMSE 均值 0.0429（纯 Picard 0.0418，Anderson 0.0432），n_e 0.0075。复核的全文在 git 历史里的旧版 README（〈加速器的复核〉·〈并行〉两节）；现行的做法见 README §3.2–3.3。
墙钟现在由冷启动的段首片定（平衡 30–38 s）；热启动各片的平衡约 2 s（原约 21 s，其中约 16 s 是 POINT 归约）。
★**`--workers` 改分段就改热启动链、会改答案**：同一份库、同一炮各跑一遍，16 段对 12 段 **7 片不同**
（1.422 · 1.822 · 2.022 · 3.222 · 3.422 · 4.422 · 4.622 s，设定点都差 4 mm，q₀ 最大从 1.009 跳到 1.709），
**全炮 T_e NRMSE 均值 0.04285（12 段）对 0.04515（16 段）**、n_e 0.00746 对 0.00749、T_i 0.1050 对 0.1049，
墙钟 42.8 s 对 51.7 s（每段 5 路对 4 路）。`--jobs` 不会（逐位相同）。★所以**报 NRMSE 必须连 `--workers` 一起报**。

**还没做的**（2026-09-18 记，2026-09-20 订正）：~~EC 沉积（要发射几何）~~〔2026-09-19 做了，见〈七〉〈八〉：几何由 `--ec-launch` 或装置事实的逐炮覆盖层给〕；H98 的口径与文献对齐（2026-09-20 已补 dW/dt 与辐射功率的账，**数没往 0.943 走**；
剩下要么是文献用了沉积功率而非耦合功率，要么它的 W 另有来源——见上〈阶段 A〉）；NBI 的注入/吸收功率（内核与装置事实都没有，
只有离子源功率）；η_cd 的标定（要 GENRAY/CQL3D 或实验的独立数）；
ECE 回退（TS 在的炮用不到，未实测）；~~本页结果还没进网页~~〔2026-09-21 做了：`kinetic_recon.html` 按 `@type`
分派，`profiles` 与 `transport` 各有一页——逐片剖面（清洗剔掉的点画成叉、H 模的台基段与 ρ_e、NRMSE 与 H₉₈ 随时间）与
一个时刻的输运（χ_e · χ_i 的无效段涂出来、源与沉积、LH · EC 的账）；见 README §4.3〕。

## 七 · EC / LH 几何补文献之后（2026-09-19）

fydoc 分支 `data/east-hcd-geometry`（e2f21cb）按文献补了 A-Box：EC 发射器在 M 窗口、四模块两行两列，设计发射点
R = 3.0 m、|Z| = 0.30 m（PECRH1I 在下右，Z = −0.30 m），模式改为 **X2**（原记 O，xu2016ecrh140 · liu2017ecrheast ·
wang2023xeccd 一致），环向扫描 ±25°、极向约 30°；LH1 2015–2019 在 N 窗口、2020 起在 B，LH2 在 E。**逐炮的镜面角没有公开值，
也没有 MDSplus 节点**——这一格仍缺。

**EC**：`--ec-launch R,Z,极向角,环向角` 给的是镜面处的几何；发射点在反演网格（R ≤ 2.8 m）外时，本应用沿束在真空里直线走进
网格边内 2 cm 再交给 `code/rf_ray`（`advance_in_vacuum`，角度在新点的当地框架里重算）。#81481 5.517 s 用文献的「轴上」设定
（wang2023：2.5 T 时 θ = 75°、φ = 180°，换成内核约定为极向 −15°、环向 0°）：
- 本炮 B0 = 2.62 T（R 1.75 m），140 GHz X2 共振层在 R ≈ 1.83 m，磁轴在 1.95 m——**共振层在轴内侧约 11 cm**，
  所以任何极向角沉积都落在 ρ ≈ 0.25–0.3（极向扫描 −25°…+5°：峰在 ρ 0.28–0.67），到不了文献图 9 的 ρ ≈ 0.08；
  文献的轴上沉积要这一炮的实际镜面角与场。
- 环向 0° 是垂直注入，驱动电流 ≈ 0（−0.1 kA）；文献的 ECCD 88 A/cm² 说明它的环向角不为零。
- **吸收份额（已查清，2026-09-19）**：X2 下 0.48 MW 全部吸收（极向 −25°…+5° 都是 476–482 kW）。先前一次「29 kW」是
  拿了旧库存下的加热数据——那里 EC 还记成 O 模，140 GHz 的 O1 共振要约 5 T，本炮里没有，所以几乎不吸收。
- **环向角决定沉积离轴多远**（高斯束：束腰 31 mm、镜面到束腰 1.2 m，wang2019eclauncher）：环向 0° → 峰在 ρ 0.26、
  I_EC ≈ 0；±10° → ρ 0.15、±13 kA；±20° → ρ 0.06、±24 kA、峰值电流密度 137 A/cm²。文献图 9 的 ECW 峰 ρ ≈ 0.08、
  ECCD 88 A/cm² 对应的是 |环向角| ≈ 20°（在公开的 ±25° 扫描范围内）——★这是灵敏度，不是拟合：本炮的镜面角没有公开值。
- **#81481 全链（EC 极向 −15°、环向 +20°、高斯束；LH η_CD = 0.8×10¹⁹，li2012lhcdeff 偏滤器值）**：χ_e 0.31（ρ 0.15）
  → 0.50 → 1.29（ρ 0.5）→ 2.17 → 2.89 → 14.7（ρ 0.82），χ_i 1.27–1.71，两者交叉于 ρ ≈ 0.5；文献 χ_e 0.35 → 1.5 → 5 → 10、
  χ_i 1.9–7、交叉 ρ ≈ 0.58。加 EC 后芯部 χ_e 与文献对上（0.31 对 0.35），中外半径仍偏低。

**LH**：文献给的 η_CD 只有 LH1（2.45 GHz，li2012lhcdeff，2012 年前）：限制器 0.49–0.59、偏滤器 0.80–0.83（×10¹⁹ A W⁻¹ m⁻²）；
LH2 的 ≈ 1.15×10¹⁹ 出自 2025 年会议预印本、除过方向性，不能直接比。用 0.8×10¹⁹，`code/wave` 给 I_LH = 384 kA
（2026-09-19 的读数；2026-09-20 在今天这一份库上重跑是 **377.9 kA**，见〈八〉末），仍是文献
181 kA 的两倍——快速模型里的 n_e 口径与经验式 η = n̄_e R I / P 的线平均密度不是一回事（本炮 n̄_e = 4.0×10¹⁹：按经验式
0.49–0.83×10¹⁹ 对应 183–310 kA）。
**LH 射线追踪**（`code/rf_ray` 的 LH 行，用 fydoc 的 N∥ 谱：峰 2.04、ΔN∥ 0.13、方向性 76 %）：射线要从 LCFS 内 1 cm 起
（在 4.5 cm 真空间隙外的点上慢波落在截止区，一条都发不出）；发出去以后 **69/76 条停在 FrequencyDrift**（内核写死
|dω/ω| ≤ 1e-5，`code/rf_ray` 不暴露；分段线性的 101 点剖面梯度不连续是可能的原因），只吸收 0.37 MW / 2.8 MW。
2026-09-19 查了两条路：换成 401 点光滑剖面**没用**（73/76 仍停在 FrequencyDrift）；内核加了 `dw_tol` 设定
（fylite_kernel `feat/rf-ray-dw-tol`，缺省仍 1e-5）后放到 1e-4 / 1e-3，吸收升到 1.38 / 1.72 MW，功率峰在 ρ ≈ 0.06——
**但驱动电流只有 −20…−30 kA（符号反、准线性行走不收敛）**，离 GENRAY+CQL3D 的 176 kA 远。LH 射线 + 1-D 行走的电流驱动
还不能顶替快速模型；LH 的 I_LH 目前仍以 `code/wave` + 文献 η_CD 区间为准（0.49–0.83×10¹⁹ → 235–398 kA）。

## 八 · #81481 的 EC 实际角度（2026-09-19，文献定）

Li M.H. *et al.* 2023, *Nucl. Fusion* **63** 046019（doi:10.1088/1741-4326/acbec9，fydoc `li2023lhcdtemperature`）§4.1 给了
**这一炮**的发射角：1# 回旋管 [α, β] = [77°, 200°]（镜面 R 3.0 m、Z −0.3 m，ρ_EC ≈ 0.05），3# 回旋管 [103°, 200°]（Z +0.3 m，
ρ_EC ≈ 0.05）；α 自竖直正向量、β 自 +R 量，β = 180° 纯加热、β > 180° 为同向 ECCD。换成内核约定：1# 极向 **−13.8°**（朝上）、
环向 **+19.5°**——本应用的重建里 Ip 沿 +φ̂，θt > 0 使 k 逆 Ip，即同向 ECCD（Fisch–Boozer），与文献一致。fydoc 分支
`data/east-ec-angles` 记下了原文与换算。

用这组角（高斯束：束腰 31 mm、镜面到束腰 1.2 m；LH η_CD 0.8×10¹⁹）重跑 #81481 5.517 s：EC 0.48 MW 全吸收，功率峰
**ρ 0.06**（Li 的 TORAY ρ_EC ≈ 0.05、Wei 图 9 ≈ 0.08），电流密度峰 120 A/cm²（Wei 88 A/cm²），I_EC 22.5 kA
（≈ 47 kA/MW；Li 两台约 1.05 MW 共 35 kA，≈ 33 kA/MW）。χ_e 0.32（ρ 0.15）→ 1.30（ρ 0.5）→ 14.8（ρ 0.82），χ_i 1.27–1.71，
交叉于 ρ ≈ 0.5——与〈七〉里 ±20° 的灵敏度结果几乎相同。

★**还差的一处**：Li 记 3# 回旋管同时约 0.55 MW，但本炮 `PECRH3I`（以及 2# · 4#）在 MDSplus 上没有数，Wei 的模型也只用了一路
0.48 MW。本应用只算有实测功率的那一路；3# 若要补，得先有它的功率时序。

★**2026-09-20 重跑并留档**：上面那一段是 2026-09-19 的读数。在本应用目录旁今天这一份 `libfylite.so`
（sha256 `db21dc8399…633e`）上、同一组角与同一个 η_CD 重跑同一片，结果 JSON 留在仓外归档目录
`tr_81481_5.517s_ec-li2023_eta0.8e19.json`（sha256 `6c55e9de86…eb87`，同名 `.cmd` 记命令；★EAST 数据与结果 JSON 不入仓）。
命令：`wei2026.py transport --shot 81481 --time 5.3 --eta-cd 0.8e19 --ec-launch 3.0,-0.3,-13.8,19.5 --ec-waist 0.031 --ec-focus 1.2`。
★**镜角与束腰这一次是命令行给的**：今天这一份库的装置事实里**还没有** `providers.ec_launchers` 的逐炮覆盖层，也没有
`fylite:beam_waist`——不给开关时 `heating.ec.skipped` 记「no published launch angles for this shot」、EC 一路根本不算。
读数（括号里是 2026-09-19 的）：

| 量 | 2026-09-20（留档） | 2026-09-19 |
| :--- | ---: | ---: |
| EC 吸收 · I_EC | 0.482 MW 全吸收 · **21.4 kA**（≈ 44 kA/MW） | 0.48 MW · 22.5 kA（≈ 47 kA/MW） |
| EC 功率峰 · 电流密度峰 | **ρ 0.063**（2.02 MW/m³）· **105 A/cm²** | ρ 0.06 · 120 A/cm² |
| I_LH（η_CD 0.8×10¹⁹） | **377.9 kA** | 384 kA |
| LH 沉积峰 | ρ 0.339（0.609 MW/m³） | — |
| χ_e | 0.47（ρ 0.13）→ **1.48**（ρ 0.50）→ 峰 3.53（ρ 0.64）→ 5.1（ρ 0.80）；ρ ≥ 0.83 判无效 | 0.32（ρ 0.15）→ 1.30（ρ 0.5）→ 14.8（ρ 0.82） |
| χ_i | 1.09–2.07 | 1.27–1.71 |
| 交叉 | **ρ ≈ 0.46** | ρ ≈ 0.5 |
| 芯部交换 | 0.175 MW/m³（轴上）· 0.186（ρ 0.13）；`p_exchange` 0.246 MW | 0.19 MW/m³ |
| 档 M | zc +18 mm · χ²/dof 1.674 · q₀ 3.388 · q₉₅ 7.075 | 未记 |

★**换库就换答案，这一片尤其**：χ_e 外半径那一段（ρ ≥ 0.80）在今天这一份库上被判无效——T_e 梯度在那里塌下去，
ρ 0.813 上算出 240 m²/s 这样的虚高值、再往外 `valid_e` 直接是 0。所以「14.8（ρ 0.82）」这一个数**今天拿不出对应的留档值**；
对文献图 9(c) 的比较只在 ρ ≤ 0.8 上成立。
