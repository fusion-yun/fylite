# FEATURE — 物理功能与待办

口径日期 **2026-08-29**（上一次全面校期 2026-08-24；本次整理清了三处与实现不符的旧叙述，见 `changelog.md` 同日条）。

本文件回答三个问题：**现在能算什么物理**、**明确算不了什么**、**接下来补什么**。
它不回答实现问题——接口与调用方式见 `docs/reference/api.md`，逐条实测的定量适用边界见
`docs/reference/fidelity.md`，改动史见 `changelog.md`。

★**贯穿全篇的一条规矩：能被外部答案判的物理，才写进来。** 一份没有判据的实现，与一个
猜出来的数在使用上没有区别。所以 §1 每条功能都带**验证依据**，§3 每条缺口都写明**缺的是
模型还是判据**——今天多数缺口缺的是后者。

★★**宁可拒绝，不给假数。** 请求落在模型范围之外时，本项目返回错误，而不是一个看着像
答案的数（§3.1）。因此 §3 不是待办的残余，它**就是能力边界本身**。

---

## 1. 物理功能

「验证依据」一列说的是**这份实现凭什么被认为是对的**——这比「有没有测试」重要。

### 1.1 零维放电与电磁位形

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| 规定的零维放电：安全因子、能量约束定标、聚变功率与增益、自举份额、环电压与磁通消耗 | 一族规定剖面 `(1−ρ²)^peaking`，**无台基** | 公开表值（反应率四点）+ 跨层恒等式（§6 第 3、4 层）。★**对外码整段放电时序的对拍尚未落地**——这一档今天只有内部一致性 |
| 线圈与真空室的互感矩阵、网格响应、匝空间缩放与通道折算 | 平行四边形导体元的解析互感（EFIT 元件约定） | 两个宿主**逐位相同**（判据用相等，不用近似）；装置描述与它转换自的原始牌往返核对 |
| 电压驱动的线圈—真空室回路时间推进 | 隐式欧拉，对称正定分解 | 同上（§6 第 4 层） |
| **运行域判据**：格林沃尔德密度与分数、柱形安全因子、`β_t` / `β_p` / `β_N` 与 Troyon 参考比 | 各自原文的闭式（Greenwald 1988、Troyon 1984）；极向周长取本层统一的椭圆约定 | 公开表值（§6 第 3 层）：ITER 基线的 `n_GW ≈ 1.2e20 m⁻³` 与 `β_N ≈ 1.8` 同为一组公开数；`β_N` 与 `β_t` 的定义式作为恒等式钉住。★**判据不是极限**：格林沃尔德是经验破裂边界，Troyon 系数 2.8 是无壁参考值，真正的 n = 1 边界取决于剖面与壁 |
| **磁通预算**：电感磁通、Ejima 斜坡电阻磁通、`∫V_loop dt`、给定摆幅下的可维持平顶时长 | `L_p = μ₀R₀(ln(8R₀/a) + l_i/2 − 2)`；Ejima 系数 0.45（Ejima 1982） | 跨层恒等式：`L_p` 与环电压那条**是同一个**（测试钉住）；未声明摆幅时返回"未知"而不是一个数 |
| **前馈脉冲设计**：给定电流轨迹反推各通道电压与被动结构感应电流 | 与本仓电路积分器**同一套隐式欧拉**，解析求逆 | **往返**：设计出的电压交回积分器必须逐位重现原轨迹（§6 第 4 层） |

### 1.2 平衡与重建

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| Grad–Shafranov 正解：固定边界与自由边界、X 点搜索、限制器磁通；★自由解的电流剖面两档——解析族（β₀/e<sub>mp</sub>/e<sub>np</sub>）或**表格化 p′/FF′ 当形状用**（每轮按 I<sub>p</sub> 归一，规范在归一中约去；v108，交付剖面在 ψ_N≈0.82 变号、解析族表达不了）；★接触点候选带 FreeGS 式 0.75\|Z<sub>x</sub>\| 私通量区守卫（朴素最大磁通规则曾把 EAST 参考放电判成限制器：同一张收敛场鞍点 ψ_N 朴素 1.213 对 bottleneck 1.000）；★收敛自报三分——达标（残差进公差**且**掩膜连续两轮不变）/ 稳态（物理量定住而掩膜量化抖动钉死残差，自行停下，出口拓扑用后置 bottleneck 重判）/ 用尽预算 | 五点差分 + 可分离椭圆算子的快速直接法 | **净室实现**，只依公开表述；表格档对解析档：e<sub>mp</sub>=e<sub>np</sub>=1 时逐位同解（单测钉住）；EAST 参考电流 + 交付 p′/FF′ 解出偏滤器，a/R₀/X 对交付重构 0.453/1.814/(1.62,−0.66) vs 0.440/1.814/(1.61,−0.72)（`test_free_boundary_verdict.py`） |
| 平衡重建：`p′(ψ̄)` / `FF′(ψ̄)` 多项式系数的线性最小二乘 + 外层 Picard；★**求解器返回不等于解出来了**——判据是方法对自己的两条陈述（等式约束确实成立：`\|I_fit − I_约束\|/I_约束 ≤ 0.5`；解出的是等离子体不是一团：`a ≥ 0.1 × 真空室半宽`），不成立就按失败报出并说明是哪一条 | Lao 1985 的结构 | 净室实现；与交付的动理学重构逐项对照（约束量与整体电感落、`q95` 与垂直位置不落，差距归因写在 `fidelity.md`）。退化解实测：孪生 Ip 700 kA 曾报「重构完成」而 `a` = 0.036 m、`q95` = 0.012、拟合电流 215 MA、χ² = 4.0e10——现按失败报出。★★**2026-08-31 起有登记记录**（B-06，对**离线 gfortran EFIT** 在同一份测量集上的答案）：磁轴 5.07 / 6.54 mm（喂进去的电流质心锚点本身是 8.14 / **26.93 mm**）· 边界 ≤ 5.6 mm（下支腿 −38 mm 单列）· ψ 图 rms 1.18 % · **q95 +1.44 %**；而 **q₀ −58.9 %**、通量跨度 +42.9 %、li3 4.10。★★**离数据**则是 −10.0 σ 且**几乎纯共模**（参考自己的场 0.53 σ）。主项已归因：**锚点是用虚拟电流按住的**（网格盒外一对丝，其通量在这组环上均匀到 47 %），**去掉径向锚点即 +0.24 σ 且竖直位置仍在**（dZ 6.5 → 5.3 mm，代价是 dR 5.1 → 14.8 mm）；电流集中度是次项（li3 2.24 ⇒ +25.2 σ）。★★**修法已成为缺省**（2026-08-31）：径向锚点是一条齐次行，权重 `1/σ_R` 缺省 10；共模 −10.00 → **+0.57 σ**，ψ 图 1.18 → 0.97 %，li3 4.10 → 3.51，dZ 反而更好（+6.5 → +2.6 mm）。★代价是 **q95 +1.44 → +5.55 %**、**R_out −5.6 → −17.8 mm**——那两个数原先是被假磁通粉饰的，两条带同批放宽并写明理由。`FY_ANCHOR_W=0` 是回到旧行为的逃生口。★★**掩膜清空同批修掉一半**：拟合↔几何的交替只在跨度增长的方向稳定，收缩即正反馈并一步塌掉（跳的是 `psi_b`，不是系数）；加**边界通量速率限制**（30 %/轮，对能跑的阶惰性）与**掩膜下限**（20 格）之后，(2,3)/(3,3) 由塌缩变收敛，**q₀ 缺口 −47.1 % → −20.0 %**；(3,4)/(4,4) 仍失败但当场报出 it 与剩余格数。诊断靠新加的 `FY_DUMP_INVERSE` 逐轮转储。★★★**散布那一半也归因了（2026-08-31）**：它不是噪声——按极向角排序单调穿过零点（m=1 占 72.9 % 方差），**是竖直锚点那对虚拟丝**（相关 +0.953，投影掉剩 1.14 σ）。〔更正：先前写「反对称所以抵消、故竖直那份无害」——**抵消的是均值**，反对称源在其余每一阶矩上都是纯上下偶极。〕★锚点的值也量了：参考自身的电流质心由**两种不共享输入的拼法**读出（相差 2.9 mm），**探针单丝锚点内偏 17.2 mm**；`R_out` 对它的斜率是 1.04，所以 R-2 那 −17.8 mm 整条就是这笔偏置。留 T-C43。★★★而这个问题已经收窄：从参考自己的图上读，**它几乎没有被动电流**（上下偶极 +7.5 A）、**本仓的 `psi_ext` 连 B_r = 0 线都只差 1.6 mm** ⇒ 不是缺了一个外场；基阶也几乎不影响它（69.9 → 66.3 kA，而同批 q₀ 缺口 −47 → −20 %）。★★★**实测竖直回路增益 1.53 > 1** ⇒ 这个模不稳定，而**对不稳定模，比例镇定器稳态本就输出非零**——「`fb_amp` 应当趋于零」这个期待本身是错的。★★★**这次裁定已用实测代替**：实验开关 `FY_ZC_TRIM`（缺省关）把设定点漂移打开后，`fb_amp` 确实降到 −20 A 并真收敛（1851 步），但**磁轴走到 +68.1 mm、q95 +16.1 %、环残差反而更差** ⇒ **那份力是承重的，缺省不动**。用力算账更说明问题：**参考自己的答案也扛着 12.3 kN**（真机竖直位置是被主动镇定的，等离子体从不待在无力平衡上；唯一受力平衡的那一行正是坐在外场 B_r = 0 线上、位置错 68 mm 的那一行），本仓 18.1 kN⇒ **缺口 = 5.9 kN（48 %）= 21.7 mm 等效偏移**，与控制律给的 22.2 mm 差 2 %。**要瞄准的是那 5.9 kN。**★★★**另一条线同日打通**：挡住高阶基 (3,4)/(4,4) 的是 **CONDIN 截断**（新增 `keep`/`cond` 转储后一目了然：能跑的阶稳在 4.1e7，失败的阶开局 5.8e11 并与掩膜一起打摆；迟滞稳的是保留模的**个数**不是**哪些**）。收紧一个量级（`FY_CONDIN`，缺省不变）后它们收敛，给出本记录最好的一组：**q₀ 缺口 +2.6 %**（缺省 −47.1 %）· q95 +3.43 % · ψ 图 0.96 % · 下支腿 −18.7 mm · κ +1.57 %；代价 dR −17.3 mm（出 R-1）⇒ 开关而非缺省。★顺带更正两处：**下支腿的 −38 mm 大部分是基阶不是「两码追踪规则不同」**；**li3 不是芯部形状的好代理**（q₀ 最好那组的 li3 反而更高）。★同批 `fb_amp_r` 终于上报（逃生口下 −68.2 kA，替换掉一处用竖直幅值当代理的估计）。★★★**2026-08-31 晚更正**：那两处掩膜修补（边界通量速率限制、掩膜下限）**改成了缺省关的开关**（`FY_SPAN_STEP` / `FY_MASK_FLOOR`）——它们那句「对能跑的阶惰性」是在**带锚点、线圈精确、交付基**那条路上量的，换到浏览器那条反演路（**不给锚点、拟合线圈、原始基**）上就不成立：一个本来收敛的拟合变成第 212 轮法方程奇异、卷宗九片 8/9 → 6/9。三种改写对两条路都不成立，故按成例做成开关，两边的数都记下。★★方法学：**「惰性」是相对于一组调用者说的**，而当时只有一组。★★**但它没有把受力差一起带走**：q₀ 走完全程（−47.1 → +2.6 %）时，竖直受力差只从 5.85 走到 **4.28 kN**（27 %）、环残差只走 7 %，⇒ **芯部形状与竖直受力是不同的自由度，余下的 4.3 kN 不是芯部**。★整笔差额拆成**位置 + 形状**（同一外场刚性平移，`dF_z/dz = −0.275 kN/mm`）：(1,2) 3.14 + 2.71 kN → (3,4) 2.64 + 1.64。★★度规教训一并写进门里：读参考隐含外场时逐格当**丝**折叠只复原 73 % 的 I_p，并给出「零线差 45 mm」这个**差点当成装置缺陷发出去**的假结论；换成有限元后是 1.6 mm。★★**真空室涡流已接上并排除**（`reconstruct(vessel_sigma=)`，40 段自由通道走内核 `gs_inverse_solve_coils`，响应由被动几何现算）：达标的那一档要每段 ±12.9 kA、净和仅 0.4 % I_p 的交替振荡——**吸收残差的指纹**而非平顶涡流。★★**第二个见证**：新增 `vessel_modes` 把涡流分布截断到被动结构能承载的极向谐波之后，m≤0/1/2/3/5 全部拿不回那份帮助（3.6…4.0 σ）⇒ 四十段拟合买到的 2.02 σ 是不相干的过拟合部分。★同批查出并修掉两处交付缺陷：写死的 R₀ = 1.85 m（EAST 是 1.75 m）与号相反的通量跨度（**交付的压强剖面带负号**） |
| 磁面几何：等值线追踪、面平均、`q`、体积与面积、形状矩、限制器判定 | — | 与局域几何一路互为对照（一路被给定形状，一路要先找到形状） |
| **位形判据**：打击点（边界磁面与壁的交点）、最小壁间隙、X 点集 | 边界磁面与壁折线求交；到**线段**而非顶点的距离 | 闭式算例（圆与方盒的交点与间隙在解析上已知）。★**偏滤器是拓扑不是形状**：这几个量才是判它的观测量，κ 与 δ 不是 |
| **位形起始设计**：按目标边界线性等磁通反解一组线圈电流（可加箱约束），供形状退火起步 | 固定丝云的自由边界逆问题，Tikhonov 增广 + 投影梯度 | 与"什么都不做"比：设计后边界磁通散布必须低一个量级；箱约束逐通道报告是否触限。★**不是平衡**：里面没有力平衡，页面与文档都写明 |
| **通道基准**：一次拟合只吃一套通道值——交付重构的通道值，或原始 est2 读数（总磁通，由页面按自己拟合所依的响应扣掉线圈那份）；**I<sub>p</sub> 等式约束随基准一起换** | — | 同源才可同拟：EAST #137985 @4 s 两个 I<sub>p</sub> 差 **1.90 %**（交付 393.46 kA vs Rogowski `pcrl01` 400.94 kA），混着喂是让一个解同时满足关于同一炮的两种说法。闸子 `validate-analysis.mjs` 断言会话文件里的 `fylite:ip_source` 与 `fylite:channel_basis` 同档、且换档后 li(3) 确实变（2.677930 → 2.377973） |
| 剖面拟合：平滑度由**基截断阶**声明，阶数由 GCV 选 | — | 平滑判据是显式参数，没有静默缺省 |
| 诊断几何：视线、线积分、磁测量矩拟合、层析 | — | 三个问题一份实现（线走哪、看到哪个 `ψ_N`、`∫f(ψ)ds`） |

### 1.3 稳定性与控制

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| **n = 0 刚性垂直稳定性**：不稳模态、开环增长率、理想失稳的场指数 | 消去后的等效质量与耦合向量 | 解析极限 + 跨宿主一致（§6 第 4 层）。★**无外部独立代码的对照** |
| 垂直控制回路的被控对象与闭环仿真 | 秩一更新 | 浏览器与命令行共用同一份实现，跨宿主判据钉住 |
| 场零（击穿）设计 | 带电流硬约束的最小二乘 | 同上；缩放规则单点定义，无静默缺省 |
| **I<sub>p</sub> 反馈**：调环电压追请求电流（可跟波形）；以及它要的那个观测量——**由 ψ 读出的等离子体电流** `I = V′⟨\|∇ρ\|²/R²⟩(dψ/dρ)/(2πμ₀)`（本页 ψ 是总磁通，故除 4π²μ₀） | PI（**不是 PID**：上游 ITER 那一档自己就是 D = 0，而在这个量上加微分等于对梯子的求积噪声求导）；误差取**相对**值，回路在第一步上用自己的读数标定 | 常数**认得出**：三个差 2π 幂次的候选各差 >5 / <0.2 / >30 倍，判据把它们一起钉住。跨宿主：两个宿主各写同样六行，落在同一个数上（**8.6e-11**）。回路：EAST 400 kA 上关着 +209.5 %、开着 **0.09 %**，跟一条 397→794 kA 的动目标末步 **0.54 %**。★**读数系统性低 3.2 %**——梯子自己的求积（V′ 0.7 %、gm2 1.1 %），实测不随分辨率也不随梯子边界收敛，**所以能拿去当反馈的是比值不是绝对值**，标定比与两个数一起报出 |

### 1.4 局域几何、映射与新经典输运

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| Miller 面的局域几何 | GACODE `geo` 的白盒移植（Apache-2.0） | 对上游记录答案 |
| 物理剖面 + 几何 → 归一化的求解器输入 | GACODE 映射层的移植 | 对上游逐参数原样通过 |
| 解析新经典族：Sauter、Redl、Hinton–Hazeltine、Chang–Hinton、Koh、Hirshman–Sigmar、Taguchi、Hinton–Rosenbluth | 各自原文 | **对上游 NEO 逐位** |
| 漂移动理方程求解：Laguerre / Legendre 基、碰撞算子矩阵、平衡 θ 层、块稀疏装配与稀疏直接解、通量与流矩 | NEO 的求解路线 | 端到端**对上游 NEO 容差 1e-8** |
| **新经典平行电导率 σ_neo**（T-A9）：Sauter 的 Spitzer 式乘捕获修正 F₃₃，Sauter-1999 与 Redl-2021 两档 | Sauter et al., Phys. Plasmas 6, 2834 (1999) 式 (13)/(13a)/(13b)；Redl et al., Phys. Plasmas 28, 022502 (2021) | **两个外部答案**：①与 NEO 解析支路内部用的那一份 F₃₃ **同一个函数**（逐位）；②对**漂移动理支路**（每条梯度置零、`Epar0 = 1`，其 ⟨j·B⟩ 就是电导率）在 ν\*_e = 0.33 / 3.28、f_t = 0.566 上：Redl-2021 差 **0.31 % / 0.08 %**，Sauter-1999 低 **2.2 % / 6.7 %** |
| **⟨j·B⟩ ↔ ⟨j_φ/R⟩/⟨1/R⟩ 的逐面换算**（T-A9），双向 | Grad–Shafranov 恒等式（`μ0 j = F′B + μ0 p′R e_φ`）——严格，不是近似 | 对 NEO 解析支路**自己同时返回的** `jpar` 与 `jtor` 逐面 **< 1e-12**；页面上对同一次拟合的 p′/FF′ 两条路 **2.7e-12** |
| **逐面 ⟨1/R⟩ / ⟨1/R²⟩ 与 ⟨B²⟩ 的极向—环向拆分**（T-A9） | 与度规梯子同一条 `R dl/\|∇ψ\|` 权重 | 圆截面上对**闭式** ⟨1/R⟩ = 1/R₀、⟨1/R²⟩ = 1/(R₀√(R₀²−a²))：**< 3e-4**（差值来源：721 点折线 + 双线性采样的 \|∇ψ\|） |

自举电流既可走解析族，也可走漂移动理解；分辨率—代价是实测的（外循环缺省档比高精度档
快约 8 倍，结果差 0.22 %）。

★**自举—欧姆—拟合电流的闭环已经关上**（T-A9）。平衡重建给的是 ⟨j_φ⟩、解析自举族给的
是 ⟨j·B⟩，两者是**不同的量**；换算落地之后，反演页在**同一个度量**（⟨j·B⟩/B₀）上给出
三条**可加**的曲线 j_tot = j_bs + j_ohm，自洽外环把自举电流交给内核的**规定电流道**
（`gs_inverse_solve` 的 `j_pre`，ABI v17 起就在，此前无人调用），只把余下那份留给磁场
拟合。EAST #137985 @ 4 s 实测：自举份额 4.033 → 4.237 → 4.239 %，两轮自停，
**跨轮变动 0.036 %**。★换算里的 p′ 项是**抗磁电流**，属于总量而不属于任何一道——所以被
换算的是**拟合值**而不是分量；实测去掉它，环向电流动 24.3 %。

### 1.5 湍流输运

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| 神经代理的**求值**（`nn.rs` + `fylite.nn`，ABI v117）：dense 输入层 + 平铺深度 + 残差块、elu/gelu、可选**幂律头**、模型自带输入调理、log10 掩膜与输入输出标准化、ensemble 均值与离散度 | 上游模型文件的架构逐层**断言**后导出 | ★★**权重不编进 `libfylite.so` 或 wasm**——它们是 `nn_tables/` 里的数据，用时才读；`$FYLITE_NN_DIR` 可覆盖。**EPED-NN 与 TGLF-NN 走同一条码路**（前者带幂律头/平铺深度/gelu/单成员，后者残差块/elu/20 成员——差异全部由模型文件声明）。TGLF-NN 对上游 **4.9e-14**；EPED-NN 的统一路径对编译进 wasm 的老路 **逐位相同**（18 个数 0.0）。★该 TGLF-NN 模型（DIII-D 训练）在 ITER 的 ρ≥0.75 两面**出训练域**，逐项报值与界 |
| 混合 Bohm/gyro-Bohm 热输运闭包（`bgb.rs`，ABI v116） | Erba et al. PPCF 39 (1997) 261 / NF 38 (1998) 1013 的公式清净室（JETTO 源一行未读） | ★2026-08-28：Bohm 系数**量出而非引用**——对 JINTRAC 101612 记录的 XE2/XI2 列冻结（其 ρ 标签下逐面 ±0.5 % 平、三片一致、XI2/XE2 恒 2.0000、边缘因子定义被 Δ=2.2 vs 1.04 两片钉死）；gyro-Bohm 道无 oracle（该运行 XE3≡0），带文献值入库并标明（`tests/test_bgb.py`） |
| 准线性回旋流体输运：FLR 与捕获闭包拟合表、色散矩阵装配、非厄米复特征解、粒子 / 电子能 / 离子能通量 | TGLF 的白盒移植 | 对上游 TGLF 记录答案；电磁分支在多个 `β_e` 上逐例核过 |
| 稠密复线性代数（LU 与广义非厄米特征解） | 顶掉外部 LAPACK 依赖 | 对 LAPACK 记录答案 |

**环向 / 平行应力**（动量通道的驱动）：2026-08-22 移植，见 §1.6 与 §3.2 的残差声明。

### 1.6 芯部输运（1.5-D）

规定几何下 `ρ ∈ [0,1]` 的守恒形有限体积输运，**离散化在本项目中只有一份**：

- **通道**：电子 / 离子温度（含电子—离子交换）、密度（逐离子种，电子由准中性导出）、
  磁通与电流扩散、环向动量。★★**动量道 2026-08-29 打通**：环向应力与上游自己写出的
  `out.tglf.gbflux` 在七面里的**五面逐位吻合**（另两面差在 2–3 个近临界 ky 的取根，
  不是应力，见 §3.2），开旋转剪切后**吻合到 2.3e-5**；`closure.momentum_chi_phi`
  不再以「权重没移植」为由拒绝，给定带剪切的旋转剖面就返回有效 `chi_phi`。
  ★**平剖面上它仍拒绝，那是物理不是缺口**：零剪切下的应力是**残余应力**，是力矩源，
  除以零梯度会造出一个求解器照样会 march 的无界扩散率——该项喂 `solve_momentum`
  的 `torque=`。调用方自带 `chi_phi` 仍是受支持的路。
  ★★**闭包已自洽接线（同日）**：`kernel_coefficients(omega=…)` 多出**第三个钩子**
  `momentum(state) -> chi_phi`，与 χ、D 共享同一次湍流运行；ω 经
  `surface_states(omega=)` 落成 `w0`/`w0p`，由内核那段 `tgyro_tglf_map.f90` 映射写出
  `VPAR_SHEAR`/`VPAR`/`VEXB_SHEAR`。**不给 ω 就没有 `"momentum"` 键**——不是零、
  不是 NaN，是没有这个通道。
- **格式**：后向欧拉与稳态解、内层 Picard 与松弛、Pereverzev–Corrigan 数值扩散项、
  Dirichlet 边界。
- **动网格**：带 `dV′/dt` 与标签漂移 `−(ρ/2)(Ḃ₀/B₀)`；不给动网格量时与静网格路径**逐位相同**。
- **通量匹配**：稳态通量匹配与回退、解释性反演（由通量反推输运系数）、回旋玻姆归一化。
  ★★**2026-08-25 起浏览器有调用者了**（此前「内核有、宿主够不着」，见下）：建模页含时
  演化栏的闭包多了一档 **通量匹配（稳态）**——未知量是逐匹配半径的 `a/L_Te` 与 `a/L_Ti`
  （**两条通道**，通道最快排布），模型通量 `gm3·n·χ·z·T`（χ = 新经典下限 + TGLF，与湍流
  档**同一条闭包**），目标是面内源项的体积分除以 `V′`，**电子—离子交换以相反的号进两条
  目标**（`q_e − s_x` / `q_i + s_x`）。牛顿机、雅可比装配、夹持与逐点回退**全在内核**；
  宿主提供的只有通量模型（回调过不了 ABI）。★**匹配区不含近轴**（内边界是控件，缺省
  `ρ̂ = 0.20`）且**燃烧走皮卡分裂**（α 冻在每轮起点，代价在文件里报出）——两条都是实测
  改的设计，理由与数在 `changelog.md`。★**不收敛时按失败报出并指名哪条通道哪个半径，
  绝不退回规定 χ**。★Python 宿主仍只有绑定、没有调用者。
- **★自洽稳态外环**（T-C14，2026-08-25）：通量匹配那一档上多了一层**轮流**——
  源 → 台基 → 输运 → **电流 → 锯齿 → 平衡**，判据是上游那一条的原样（压强与电流
  剖面的轮间相对变化，缺省 5 %）。**缺省 1 轮 = 只匹配不交替**，既有算例一字未变。
  ★**电流那一步解的是稳态而不是「稳定态」**：`dt = ∞` 会把欧姆项 σE = −∂ψ/∂t 整个
  去掉、只剩非感应电流（实测：I_p 对 j_ni 严格线性，对 σ、对边界磁通、对边界速率
  都差 < 1e-12），而平顶上的托卡马克是 ∂ψ/∂t = V_loop 均匀的稳态。所以这一步用
  **长而有限的步长**并**解出**平顶要的那个环电压（I_p 对速率仿射，割线两三步落进
  1 %），解出的电压与它有没有被量程夹住一起报出来。
  ★**外环带欠松弛**（`fmorlx`，缺省 0.5），作用在 q / ψ 与自由边界跑的那三个数上
  ——**混输入不混梯子**，且阻尼不移动不动点。★不收敛按失败报出，裁决先说**外环**
  收没收敛、再说里层那次匹配，绝不拿最后一轮充当答案。
  ★解析 Miller 上多轮**拒绝**（说不出 ⟨\|∇ρ\|²/R²⟩）；g-file 上跳过平衡那一步并
  **写明理由**。
- **锯齿**：`q(0) < q_crit` 触发 + 内容守恒混合与 `q → 1` 重建。**明确不是 Kadomtsev、
  不是 Porcelli**，混合半径没有缺省值。
- **★台基顶 —— 梯子的 Dirichlet 边界不再是一个滑杆上的数**（ABI v112 起）：
  **EPED1-NN**，峰剥—气球与 KBM 两约束的已发表网络代理（Snyder, *Phys. Plasmas* **16**
  056118 的两约束，经 Meneghini, *Nucl. Fusion* **57** 086034 的网络代理；权重取自开源
  EPEDNN.jl，Apache-2.0，sha256 钉在 `pedestal_tables.rs`）。给出台基顶压强 `p_ped`、
  宽度 `Δψ_N` 与 `T_ped ≡ p_ped/(2 n_e,ped k)`（EPED 自己的 `T_e = T_i` 约定），
  **并报告外推距离**——落在训练域之外时说出来，而不是外推一个数。
  ★台基**内部结构**与 SOL 仍不建模（§3.4）：解出的是那一个点，不是那一段剖面。
- **验证依据**：输运本身对 TGYRO 自身的演化表 1e-6。**台基那一路另有四条**：
  ①移植对独立参考评估**逐路 1e-10**；②★**对论文印出来的数逐例对账**（2026-08-25，
  一手全文已取）——两炮 DIII-D 的完整输入集出自 Snyder, *Nucl. Fusion* **49** 085035
  第 6 页，预测点读自同页图 5：**132003** 5.694 kPa / 0.0340（发表 6.7 / 0.036，比值
  0.850 / 0.945）、**132017** 11.103 kPa / 0.0646（发表 12.2 / 0.067，比值 0.910 /
  0.965）；**ITER 基线** 79.026 kPa / 0.0330 / β_N,ped 0.500（发表 92 kPa / 0.04 /
  0.6，出自 *Nucl. Fusion* **51** 103016 第 7 页，比值 0.859 / 0.826 / 0.833）——
  **判据 ±20 %，取的是论文自报的精度**；③论文自己的那句反差（高三角形与更大的
  B_T/I_p 让 132017 显著更高更宽，而 I_p·B_T 只差 5 %）本仓复现为 ×1.95 / ×1.90
  （发表 1.82 / 1.86）；④KBM 恒等式 `Δ = 0.076 √β_p,ped` 全机型 ≤ 5 %
  （`validate-pedestal.mjs` **26 条** + Rust **9 条**）。
  ★**八个比值全部偏低（0.83–0.97），是系统性的**——三个候选来源与不主张哪一个的理由，
  连同全部出处页码，在 `docs/note/pedestal-literature.md`。

### 1.7 加热与电流驱动

| 系统 | 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- | --- |
| **中性束** | 阻止截面、慢化、电子屏蔽、束驱动电流、逐射线沉积、快离子压强、首轨损失 | RABBIT 档快模型（Weiland 2018 一类）；截面与慢化转写自 METIS 与其引用的公开文献 | 与上游公式逐条对照；屏蔽取 Lin-Liu & Hinton 1997 |
| **低杂波** | 可及性、朗道共振、沉积形状与效率 | METIS 一档的解析处理 | 同上 |
| **欧姆** | Spitzer 平行电阻率与欧姆功率 | 含捕获粒子修正 | — |
| **辐射** | 轫致、同步、ADAS 线辐射 | ADAS 冷却率的 12 项拟合（Puetterich 2019），温度域 0.05–50 keV，域外取端值 | 与上游辐射模块一致 |
| **离子回旋（少数离子加热）** | 共振层与谐波自动选、少数离子密度、Stix 分布、电子 / 离子分配、快离子储能、**沉积剖面**（共振层上的高斯，宽度 = 受热体积份额） | METIS 一档的解析链（`PION_fit-Stix`）：共振层取真场，尾巴取 Stix 1975 的解析分布，电子那份是 `2 W_fast/τ_s`（Eriksson） | **对 METIS 认证套件逐例核过**：共振层 ≤ 1.2 %、谐波逐行相等、`P_el` 0.955–1.079、`W_fast` 0.948–1.015（16 条稳态行）；剖面峰位与宽度对上游**逐行拟合**核过（40 行，宽度比 1.000）。★只答稳态；纹波机器**拒绝**（§3.1）；★e/i 分配在径向**均匀**（上游简化，翼部给离子偏多）；★上游把 `pel/2` 分给一个本项目**不建模**的快波电子阻尼通道 |
| **快波驱动电流（FWCD）** | 效率与驱动电流（相位由调用方给，对称谱驱动零电流） | 中心电子温度的线性拟合（上游 `zicd0.m`，回归自 JFT-2M / DIII-D / Tore-Supra 实测）× `6/(5+Z_eff)` | ★**判据不是认证存档**（那里 `fwcd` 全为 0，无非零答案），**是数据**：对 20 个实测点比值 0.77–1.24（中位 0.98）；ITER 外推 0.181×10²⁰ 落在ITER 物理基础所述 0.15–0.25 内；对 `T_e0` 线性为精确性质 |
| **电子回旋（ECRH / ECCD）** | 冷共振层与谐波、束×层求交（ITER 角度约定）、平行折射率 `N_∥`、共振层厚度（多普勒 + 相对论）、沉积映射、**单程光深与冷截止**、吸收份额 `1−e^{−τ}`、电流驱动效率（模型选择器） | 共振层与射线是闭式；宽度是 `sqrt((N_∥ v_th/c)² + (v_th²/2c²)²)`；光深取 Bornatici 1983 表 12 的 O(n≥1) 与 X(n≥2) 两行；效率取 Giruzzi 1987 + Lin-Liu 的 Z_eff 依赖 + 陷俘修正 | 几何与宽度：**闭式恒等式八条**。光深：次序与量级对源图（2 keV 上 τ_X2 ≈ 17、τ_X3 ≈ 0.5，与图一致；τ_O1 低约一倍，源图是径向剖面不是单点）、**温度指数逐个精确**（O1/X2 ∝ T_e，X3 ∝ T_e²）、两处截止。效率：对 METIS 的驱动电流 **0.944 – 1.192** |

### 1.8 聚变与快离子

| 能力 | 模型 / 出处 | 验证依据 |
| --- | --- | --- |
| D-T 反应率 | Bosch–Hale 1992 Table VII；域外（<0.2 或 >100 keV）**返回零而不外推** | 公开表值四点，2 % 以内 |
| α 加热功率密度与电子 / 离子分配 | Bosch–Hale 反应率 + Stix 慢化的 Wesson 份额（中性束用的同一支） | ASTRA ITER 15 MA 燃烧算例：功率密度**逐点 3 %**、体积分 **0.3 %**；分配比该算例的 Post 1984 解析积分**系统性低 11–14 %**（按带宽判，不按等式） |
| 稳态快 α：密度、压强与储能、慢化与驻留时标 | 出生即就地慢化；驻留时标取出生到静止 `(τ_s/3)ln(1+(E_α/E_c)^{3/2})`，慢化时标带 α 的 `1/Z_b²` | 同一算例：快氦密度 **2 %**、快 α β **2 %**、快 α 储能 24.0 MJ（该算例 23.9 MJ） |
| 氦灰**粒子源** | 稳态下即 α 出生率 | 与 α 加热同一反应率，恒等式钉住 |

★氦灰**剖面**、以及快 α 的径向输运不在其中，见 §3.4。

### 1.9 装置数据接入

只读的 mdsip 客户端：取一炮数据不再需要站点安装的 MDSplus。**这不是物理层**——它搬字节，
不算数。

★**一个宿主，一组端点**（2026-09-01）：单文件查看器自己带的 Rust 请求面
（`fylite-app --mdsip HOST:PORT`），它让 `pages/data.html` 与反演页的「直接从 MDSplus 读」
**在可执行体里就能用**——那两页本来就编在里面，缺的只是这半边。
★从前还有一台 Node 网关（`app/server/`），判据是两边**逐字段对拍**；那个判据有一半是
循环的（两边指向同一台**按其中一方的读法写**的假服务器，表达式一起拼错也全绿）。
现在网关退役，判据换成**真机录下来的帧**（`tools/mds-record.mjs` 录）：宿主拼错一个字
就命不中任何一条记录。★★那份录音是 EAST 内网的实验数据，**不在本仓**——2026-09-02 迁到
私有的 `fydata`（`corpus/experiment/east/mdsip-137985.json`）；`FYLITE_MDS_FIXTURE` 指过去
（或拷回 `app/tests/fixtures/mdsip-east.json`）门就照跑，两条都落空时门**自报跳过**。独立实现的对照移到
`validate-jmds.mjs`（纯 Java 客户端，逐位比）。
★★**这一测把内核客户端三处解码缺陷抓了出来**（它此前只有单元测试，从未连过真服务器）：
空载荷不解码（登录应答就是它，握手完不成）· 缺 dtype 5 u64（`TIME_INSERTED` 于是每个
节点都报「从未写入」——**看起来像个答案**）· 定宽文本在解码处被 trim（**填充是分隔符**）。
实测经隧道读 EAST #137985，动理学反演跑出 q₀ 0.320 / q95 3.051 / I_p 403.1 kA。

〔EAST 卷宗带什么（2026-08-23 口径）〕`machine_desc/east/` 的卷宗不再只有一套交付通道值：

| 内容 | 数量 | 出处 |
| --- | --- | --- |
| 极向磁通环 | 35 道，**两套值** | 交付重构自己的通道值（a-file `csilop`，线圈份额已扣）与**原始** est2 读数（**总**磁通，线圈份额未扣） |
| 磁探针 | **79 道原始读数** | est2 归约，全场（含线圈份额）；★这一炮**未标定**，与解相差 RMS 0.11 T ≈ 峰值场 56 %，交付重构本身就是 `probes 0/79 weighted` |
| POINT 弦 | **11 条** | est2 归约的 `n_e` 线积分与 `∫n_e B dl`，条纹门已施 |
| 时片 | **九个真实时片，1–7 s** | `tools/mds-shot-to-fyo.py` 自 EAST 的 MDSplus 服务器拉回，与既有 HDF5 dump 逐通道**差零**；每片自带线圈电流、Rogowski I<sub>p</sub> 与自己的逐道权重 |
| 等离子体电流 | 两个数 | 交付 393.46 kA（a-file）与原始 Rogowski `pcrl01` 400.94 kA，**平顶差 1.90 %**——`pcrl01` 不是等离子体电流 |

〔为什么剖面拟合栏还没有实测点〕**汤姆孙散射这一炮不可用，不是接线没做。** 已从 `ts_east`
拉回并按仓内自带的门（`mds.pressure_from_thomson`：T<sub>e</sub> ∈ [50, 8000] eV、
n<sub>e</sub> ∈ [1e18, 2e21] m⁻³）装配：**门后峰值仍约 3e5 Pa**，而同一时刻交付重构的峰值是
**8.9e3 Pa**——差 **34 倍**；存活点里出现 n<sub>e</sub> ≈ 2e20 m⁻³ 与 T<sub>e</sub> ≈ 7 keV
同时成立。数据已如实写入 `fydata/corpus/east/137985/thomson_*.fyo.jsonld` 并标注，**未接入
任何拟合**。接进来的判据是：换一炮（或换一套 TS 归约）后门后剖面峰值与交付 `pres` 同量级
（差 < 2 倍）。

〔逐时片重构在原始通道下还不可用〕九片里四片不收敛，根因两条，**都在内核不在接线**：
①求解器把线圈电流当**精确已知**，而原始 Rogowski 逐道差 2–9 %（EFIT 自己是把线圈电流一起
拟合的）；②`pcrl01` 不是等离子体电流。页面按失败报出、给出线圈电流来源开关，并把上表写在
开关旁边——详见 `docs/reference/fidelity.md` §逐时片重构。

---

## 2. 适用范围（模型自己的假设）

这些不是缺口，是**用这些功能时必须知道的前提**：

★★**贯穿全篇的一条前提，此前一个字都没写：本项目全栈只做轴对称托卡马克。**
`§1` 的每一条——GS 正解与重建、磁面几何、1.5-D 输运、局域几何与新经典、湍流、
电磁与控制——都建立在**环向对称**之上；Grad–Shafranov 方程本身只对轴对称成立。
因此**仿星器 · 场反位形 · 磁镜 · 任何三维平衡不在范围内**，且它们**不是「还没做」而是
「另一套方程」**：那属于 §3.3 的结构性缺口，不是 §4 的待办。★这条前提**不带编号**，
是为了不改动下面六条已被别处引用的编号（例如 §3.3 引 §2.1）——它管着的是全部六条。
（2026-08-25 补：逐格照一张外部产品能力图谱时才发现表上没有这句话，TODO T-C19。）

1. **芯部输运跑在规定几何上**：`V′`、`g₁`、`g₃` 由调用方给。平衡—输运的自洽是外层轮次
   装配，动网格项（`dV′/dt`、`b0_dot` 漂移）由做时间推进的调用方在轮次接缝上提供。
   ★**内核这一侧已不再只有稳态解**：`core_march` 把热道（含交换）、粒子道与电流道放在同一个
   dt、同一个旧态上一起推进，并带自适应步长控制器；轮内取稳态解是**调用方**的选择，不是这
   一层的限制（`app/` 的含时演化栏就是逐步推进的那种调用方）。
2. **边界是一个 Dirichlet 点**：它的**取值**可由台基模型解出（EPED1-NN，§1.6），
   但台基**内部结构**与 SOL 仍不建模——梯子在台基顶停下。零维那族规定剖面同样
   pedestal-free。
3. **辐射拟合有温度域**：台基脚以下（<50 eV）取端值而非外推——冷边缘看着偏辐射时，
   先想到这一条。
4. **快离子就地慢化**：中性束与 α 都假设「在哪里出生就在哪里慢化」，没有轨道宽度、没有
   径向输运。这一假设在燃烧稳态芯部经外部算例核过（§1.8），在斜坡段与瞬变过程没有。
5. **杂质：方程有，电离态没有。** ★★2026-08-25 改判——**此前这一条写的是「没有杂质
   输运方程」，不成立**（TODO T-C20）：密度道是**逐离子种**的，`z` / 扩散 / 箍缩 / 源 /
   边界逐种一份，`n_ion` 是运行期量而非编译期上限，`tests/test_assembly.py` 里已经在跑
   一条 `Z = 6` 的碳道；辐射（逐种 ADAS 编号）与湍流通量（逐物种）两条闭包也都是逐种的。
   **真正的前提是三条**：①**电离态不演化**——ADAS 取的是**日冕平衡有效冷却率**，不是逐
   电荷态的输运，非日冕效应与电离态分布不在其中；②**杂质源永远是调用方的**（溅射 · 注入 ·
   壁再循环都没有模型，与加料同一条规矩）。★**第三条（「浏览器宿主够不着」）已于
   2026-08-25 落地并从这张表上撤下**：建模页现在能让第二种离子**自己是一条密度道**
   ——它有自己的 `D_z`/`χ_e`、箍缩与（规定的）加料速率，而粒子道与杂质稀释**不再互斥**。
   ★随之而来的一句要写清楚：成分在演化时 **Z_eff 是结果**（逐面的，进会话文件），
   Z_eff 滑杆只定**起始**成分。
6. **只有 n = 0 的 MHD**：垂直位移是刚性模型；锯齿是触发 + 混合，不是稳定性理论。

---

## 3. 缺口

### 3.1 拒绝式缺口 —— 调用会拿到错误，不会拿到数

| 缺口 | 表达 | 为什么是拒绝而不是零 |
| --- | --- | --- |

| **反应堆场景的 α 通道必须由调用方给** | 不给 α 通道时，功率记账拒绝作答 | α 通道现在**有了**（§1.8），但拒绝**留着**：一份静默缺了主导项的反应堆总量，仍然比没有答案糟 |
| **湍流的电磁开关组合与上游行为不一致时** | 拒绝，并说清上游会做什么 | 实测上游会照跑并返回**静电**答案。复现它等于**静默丢掉调用方要的物理** |
| **纹波机器上的离子回旋** | 声明机器有环向场纹波时，少数离子加热拒绝作答 | 上游减的纹波损失用的是**上一步**的热功率——一个本函数没有的反馈量。实测：唯一带纹波的算例（10 MW）上不算它，电子道**系统性高 33 %**，而所有不带纹波的算例都在 8 % 以内。一个静默的 33 % 正是拒绝的用处 |
| **电子回旋：波在截止之外** | 折射率与光深各自拒绝作答 | O 模在 `ω_pe = ω` 截止（70 GHz → 6.08×10¹⁹ m⁻³）。答「一个很小的吸收」等于让一束根本没进等离子体的波去加热它。★同一频率下 X 模能穿过 O 模的截止——机器长大后从 O1 换到 X2 的理由 |
| **电子回旋：束打不到层 / 层不在机器里** | 求层与求交各自拒绝作答 | ★实测的现实场景：同一台 140 GHz 回旋管，机器在 2.2 T 时层在 1.63 m（机内），在 1.8 T 时在 1.33 m（**壁外**）。磁场扫描必然走到这一步，答「边缘」就是让扫描长出一个机器从来没有过的沉积 |
| **共振层不在这台机器里** | 求层拒绝作答 | 上游取最近点的写法会静默返回弦的端点，再被归一半径的截断打扮成边缘沉积。「波不在这个等离子体里」与「波加热边缘」不是同一句话 |
| 场景请求本身不合法 | 未知入口 / 维数不符 / 数据块不符 / 该入口拒绝作答 | 每一条都是关于**请求**的陈述，不是关于物理的 |

### 3.2 声明式缺口 —— 上游有、此处写明「未移植」

- 湍流局域几何中**与波数相关的那半 x 网格函数**未移植；分阶段迁移期间由调用方供给。
- **`UNITS` 不能由调用方指定**。饱和强度上的几何权重按上游**预设**取（饱和规则 2/3 用
  CGYRO、规则 0 用 GYRO、规则 1 留 GYRO），因为这个字段在内核的标量块里**没有位置可搭**。
  与预设不符的牌（例如规则 1 却要 CGYRO）**被拒绝**，不改用另一套单位作答——两者相差
  一个 `SAT_geo0` 因子（锚定面上 1.115），正是那种"看着差不多"而其实不是的量级。
- **模数由调用方给，缺省 1**（上游缺省 2）。多模已移植：通量是**各模之和**，次主模按
  `(γ_i/γ_1)^expsub` 乘在主模的混合率上，饱和规则 2 的归一化常数随模数取值（`3.74` ↔
  `3.55`）。缺省留在 1 是因为既有锚定判据都录在单模上；要复现一次库的运行就得说 `2`。
  **判据实测：说 `2` 之后，一条此前够不着的录制答案被逐点复现到 1e-5。**
- **饱和规则 3 的单位权重只做了一半**：上游只对它的电子尺度分支（`ky > kT`）加几何权重，
  本项目那条分支是另一条锚定过的路径，未动。规则 1、2 的权重已按上游施加。
  规则 3 **拒绝多模请求**——上游按每模的 `QLA` 因子加权，那组因子未移植。
- **形变几何的静电通量还差 2–11 %**：三条录制答案（此前差 1.5–1.9 倍）现在落到这个带宽内，
  残差尚未归因；上游二进制在本机跑不动那几张牌（同一批键崩溃），所以还没法逐 ky 对下去。
- ★★★**动量线收官（2026-08-29）——四轮归因全部证伪，真因是我自己跨饱和规则比了。**
  拿来当参照的 `tests/data/tglf_momentum_jintrac.json` 录在 deck 自身缺省下
  = **SAT_RULE=0 / GYRO**（它自己的 `eflux` 就写着 34.393，规则 2 是 63.7；标准可执行文件
  在缺省下**逐位复现整条记录**），而所有拿它做的测量跑在 **SAT_RULE=2 / CGYRO**。
  ★这个坑同日**已在能流上踩过一次并写进记录**，然后在动量上又踩一遍。
  ★★曾有一轮结论说上游 `mflux` 从不被赋值——那是 **grep 假象**：`--include=*.f90`
  匹配不到 `tglf_run.**F**90`，其 211/237 行注释写着 `Pi_i/Pi_GB` 正是在赋它。
  ★另一处读错：`out.tglf.gbflux` 列序是 **pflux | eflux | mflux | expwd**，
  第四列是**交换功率**不是平行应力（电子与离子等大反号是真的守恒律，守的是能量交换）。
  ★★**设置对齐后本仓是对的**：对 `out.tglf.gbflux` 七面里**五面逐位吻合**；对上游自带的
  旋转回归算例 **tglf06**（GA 标准算例＋`VPAR_SHEAR=1`，公开命名算例）**规则 1/2、
  粒子/能量/应力、两个种全部 1.0000**。另两面（0.354/0.455）是**近临界取根**不是应力——
  只有 2–3 个 ky 不同且 γ≈0.01–0.02，而 x=0.455 上这两个 ky **扛着 101 % 的总应力**
  （谱近抵消 1.49×）。
  ★★**这条线真找到的缺陷（第五处）**：`solve_ky_modes` 算了 `rotation_scaled`
  却把**原始的** `inp.species` 交给 `write_all_rows`——旋转驱动丢了 `sign_It` 与
  `ave_c_tor_par(1,1)/Rmaj`。所有已录 deck 的 `VPAR_SHEAR` 都是 0 故不可见，
  **靠打开旋转问出来的**；判别信号很干净：上游的剪切应力**不随 `SIGN_IT` 翻号**
  （该因子它带两次），只带一次的移植会翻。修后开剪切**吻合 2.3e-5**。
- ★★★**对拍 TGYRO 的映射层（V-06，2026-08-29）：三个没人在看的开关。**
  既有的映射测试比的是**手挑的键表**，于是「没人想到要列的键」永远不会红。把整份
  `out.tglf.localdump` 全键对照，立刻掉出 `USE_BPER`（TGYRO **T** / 本仓缺省 F——
  **生产路径跑静电**）、`USE_MHD_RULE`（TGYRO **F** / 本仓 T——**把漂移里的压强项清零**）、
  `USE_AVE_ION_GRID`（TGYRO **T** / 本仓 F）。
  ★根因不是漏三个键，是**整张 `TGYRO_TGLF_REVISION` 预设表没移植**——它被手写在
  `test_flux_chain.py` 的 `SAT2` 字典里，**测试知道正确答案而生产不知道**。已按
  `tgyro_tglf_map.f90` 的 `select case` 原样落成 `mapping.TGYRO_TGLF_REVISION`
  （五个 revision 全表，各带论文出处）+ `tglf_inputs(revision=)`。
  加上 revision 3 与 `mxh=False` 后：四半径 **78 个共享键、零分歧**。
  ★★**代价已量**（电子道，rev3 比 plain）：r/a=0.20 粒子 **5.45×** / 能流 **3.92×**，
  到 0.65 收敛到 1.19× / 1.03×——**核心处四五倍且修正在半径间变号**。
  ★★**已采用（T-C34，2026-08-29）**：`kernel_coefficients`/`kernel_chi` 的
  `tgyro_revision` 缺省 `"auto"`，按 TGYRO 自己的配对取。`NMODES` 随预设进来
  （8 模比 1 模只贵 2%）。★**有意不照抄 `TGYRO_TGLF_MXH_FLAG=0`**：那是几何保真度
  的取舍不是模型设置，本闭包保留扩展谐波，deck 在 `SHAPE_*` 上不与 TGYRO 逐位相同。
  ★新增的第二道闸子才是能推广的那道：**一个不发出的键不是中立的，它是一句
  「本仓缺省等于上游缺省」的无声断言**——门为每个这样的开关检查那句断言。
- ★★★**GACODE 自带回归套件收录（V-04，2026-08-29）挖出四处移植缺陷（**全部已修**），全部躲过了既有的
  每一道门**——既有的门用一次放电的七个面，而**一次放电只问它碰巧要问的问题**：
  - **交换道少一个 `i`（第八处，已修）**：上游 `freq_QL` = `tglf_LS.f90:444` 的本征值
    `xi*(rr+xi*ri)` = **freq + i·gamma**；本仓传 `gamma − i·freq` 后又乘 `xi`，两次旋转相消。
    **修前 7.4–34 %，修后 3.5e-5**，能流没动。★藏住是因为**全仓从无判据比过这一道**，
    且它在两种实现里都种间等大反号（电荷中性，缺旋转照样成立）——假证据。
  - **`USE_AVE_ION_GRID`（第七处，已修，ABI v118）**：上游缺省把 ky 网格建在**第一个离子**上，
    本仓恒取电荷加权平均。tglf07 上 `rho_ion` 0.6449 对 1.0、整张网格错 1.55 倍、能流差 79 %；
    **修后 ky 网格逐位相同、规则 1 通量 2.1e-5**。★`rho_ion` 同时喂饱和层。
    ★`tests/data` 里每份三种 deck 的杂质都在 10 % 电荷门下，两支恰好重合，故此前不可见。
    ★顺带把零总电荷从静默回落 1.0 改成拒绝（上游那里是 `tglf_error`）。
  - **Waltz E×B 淬灭规则 `get_gamma_net`（第九处，**已修**）**：修前 tglf04 增长率
    **恒高 0.030000000，21 个 ky 同一个数**；修后 **1.4e-13**、规则 2 通量 1.4e-5。
    ★**施加在线性解内部**（对 `gamma_out`），故模宽搜索也搜被淬灭的谱；
    `quench_growth_rates` 是另一件事（SAT1 带状流混合）。
    ★而 `ALPHA_QUENCH` 此前只被处理了**两个角色中的一个**（只用来关谱移模型）——
    **比未知键更坏：deck 看起来是被支持的**，而且有一条测试把这个半处理**断言成了
    正确行为**（已改写）。
  - **`B_per`（`j=2`）的粒子/能量/交换 QL 权重（第十处，**已修**）**：修前增长率精确
    8.7e-14、逐 ky field-1 权重精确 3.8e-11、积分后短 9.9–17.5 %（**没有哪里算错，
    是有东西不在**）；修后 tglf09 规则 1 三道全部 **1.00000**。
    ★`j=3`（`B_par`）同批补上但**未验证**（上游无 USE_BPAR 回归 deck），已在注释点名。
  - ★★**第十一处移植缺陷：谱移的增长率阻尼（2026-08-30，已修）**。第二趟不把原始增长率
    交给饱和层，而先逐 ky 阻尼 `gamma_net = gamma/(1+|ax·kx|^exp_ax)`
    （`tglf_multiscale_spectrum.f90:245-273`；`ax`/`exp_ax` = 规则 1 的 (1.15,4)、
    规则 2/3 的 (1.21,2)），**被阻尼的那个才是带状流混合/淬灭/强度共同读的量**；
    随后 `vzf` 按峰值阻尼缩放（规则 1 则在阻尼谱上重跑 zonal mixing）。
    ★★`ax`/`exp_ax` 此前在 `SaturationConstants` 里、**有一段准确描述这件事的文档、
    却从未被任何代码读取**——**一个被定义、被记录、却没人用的常数是最安静的一种缺项：
    它读起来像已经移植了**。tglf05 规则 1 **8.0e-2→2.1e-5**、规则 2 **6.7e-2→1.4e-5**。
  - ★**线索剩一半（T-C35）**：规则 2 上仍差的只剩 tglf07（三种粒子）5.3e-2 与
    tglf09（有限 β）1.4e-1；两者**只在规则 2 差、规则 1 精确**，而 `kymax`/`vzf` 对上游
    **逐位相同**（上游把这些写在 `out.tglf.scalar_saturation_parameters` 里，可直接对）
    ——所以余量在 zonal mixing 的**下游**。★三个扰动里已有一个（旋转）查出了自己的成因，
    这是继续单独看另外两个的理由。
  ★另记一条**不是缺陷而是静默改写**：`GEOMETRY_FLAG` 全仓只在 `mapping.py:175` 出现一次
  且写死为 1，从不读 deck——`fluxes_rust` 会拒绝扛不住的 `UNITS` 与未移植的饱和规则，
  **唯独几何放行**（s-α 的 deck 被当 Miller 跑）。已单独留门。
- ★★★**第六处移植缺陷：E×B 谱移没有回灌进线性解（T-C33，**已修 2026-08-29**）。**
  由文献对标挖出（Waltz *PoP* **14** 122507 (2007) Table I 的四个工作点）。对上游、
  饱和规则对齐实测：`VEXB_SHEAR=0` 时环向应力 **1.0000**（两规则，精确）；
  `VEXB_SHEAR=0.2` 时 **12.4×**（规则 1）/ 6.0×（规则 2）；再加 `VPAR=0.4` 时
  **0.081×** / 0.109×。**同一批运行里能流只差 ~6 %。** 根因：上游的谱移 E×B 模型是
  **双趟**（`tglf_TM.f90:65-76`），本征模自己带 kx0 位移；本仓 `spectral_shift()`
  在线性解之后才算、只喂饱和强度，kx0 从不回到 `solve_ky_modes`。能流由强度主导故几乎
  无感，而 **E×B 残余应力就是模结构里的那个对称破缺**。
  ★**已修**：双趟落地后 tglf05（`VPAR_SHEAR=0`，全部应力都是 E×B 破缺）从 −2.9e-08 到
  **−7.2375（1.038）/ −4.2162（1.061）**；Waltz 带剪切两行从 12.4×/0.081× 到
  −3.12×/1.19×。★两个各值一个因子的细节：`kx0_e→kx0` 的除数**按 UNITS 分支不按规则**，
  且 GYRO 支的 `wE` 带几何因子 `1+0.40(|grad_r0²/B_geo0|−1)²`。
  ★逐 ky `kx0_e` 对上游 `out.tglf.spectral_shift_spectrum` **逐位相同**；
  剩下 ~6% 是带剪切的**饱和强度**余量，修改前就有。
- ★**跨码验证（本通道唯一一条对独立代码的判据）**：Waltz 2007 Table I 是 GYRO
  **非线性**回旋动理学在 GA 标准算例上的有效动量/热扩散率，其算例参数与 tglf06 的 deck
  逐项相同。有效普朗特数（γ_E=0）：γ_P=1.0 时 GYRO 0.86 对本仓 0.637（规则 1）/
  0.718（规则 2）；γ_P=0.2 时 GYRO 0.74 对 0.682 / 0.759。判据带 **0.5–1.1**，
  宽是有意的——准线性对非线性，吻合到几个百分点是运气；它排除的是因子二、符号错、
  丢一个几何因子。
  ★早先「平行差 7.5 %、随模宽变」那组数在静电运行点上量得，量的是 `j=1` 内部的残差
  ——★**那正是第五处缺陷的指纹**（`ave_c_tor_par(1,1)` 由随模宽变的 x 网格算出）。

### 3.3 结构性缺口 —— 不由计算核承担

1. **计算核不读文件、不解析文档。** 「读入语义文档 → 跑场景 → 写出语义文档」这条链，
   计算核只出中间一段：它**声明**字段表，不**解析**文档。理由是必须能编到 WebAssembly
   且零依赖；代价是文档读写在两个宿主各有一份实现，两份都走同一张生成出来的表，并有跨
   宿主逐位判据钉着。★这是一次**有意的不做**，不是遗漏。
2. **场景装配大多不在计算核。** 输入是装置牌、求解器牌或实验数据归约的那些场景，装配由
   宿主拥有——计算核问不出来。
3. **几何自洽由调用方组织**（见 §2.1）。
4. **非轴对称位形不由本项目承担**（2026-08-25 补声明，TODO T-C19）。仿星器 · 场反位形 ·
   磁镜 · 三维平衡：**不是遗漏，是 §2 那条贯穿前提的直接后果**——环向对称是全栈的
   出发点，换掉它是换一套方程而不是加一个模块。★写在这里而不是 §4，是因为**它没有
   路线图，也不打算有**；把它挂在待办上，等于对读者承诺一件本项目不会做的事。

### 3.4 物理上没有的东西

| 缺口 | 现状 | 说明 |
| --- | --- | --- |
| **加料模型** | 无弹丸、无气吹、无束粒子源 | 粒子源**永远是调用方的**。写在这里的加料模型没有外部答案可核 |
| **芯部中性与电荷交换** | **`neutrals.rs`（1-D 柱 Monte-Carlo，ABI v116）** | ★2026-08-28 落地（原「无」，TODO T-C24）：Woodcock 追踪 + Voronov 电离 + Freeman-Jones CX（发表拟合，清净室——EIRENE/FRANTIC 源一行未读），产出 n0 / S_ion / **离子道 CX 汇** / 电子电离代价，逐炮确定（seed）。对 EIRENE 的通量面平均列（101612）按带验收：**穿透两指标被冷/暖两个可命名发射能括住；离子道 CX 功率在任何发射能下超 1.8–3×——1-D 几何代价，量过入门**（`tests/test_neutrals_oracle.py`）。★仍缺的是**边界源谱**（冷主体/暖尾的混合比）——那要 T-C25 的 SOL/再循环半边才能给出；发射能与 `e_loss_ev`（电离的电子代价，EIRENE 有效值 ≈20 eV）是声明的输入 |
| **主离子再循环与粒子排出闭合** | 无 | ★2026-08-28 补声明（TODO T-C25）（此前这张表**漏了**，与 JINTRAC 的 ASIPP 部署算例矩阵逐条比时才发现）：全仓检索 `recycl` **零命中**。**这一行不能算进上面两条里**：「加料模型」说的是**调用方给的粒子源**，「杂质：电离态与杂质源」里的「壁再循环」说的是**杂质**的——而这里缺的是**主离子**的等离子体—壁粒子回路（电离 / 电荷交换 / 复合 / 再循环），也就是把粒子排出**闭合**的那一环。上游那一侧是 EIRENE（蒙特卡洛中性 + 原子分子过程），JINTRAC 的 5 MA L-mode 算例把它写成自己的判据原话：「closure of particle and energy exhaust」。★与「芯部中性与电荷交换」的区别：那一行是**芯部**的中性与 CX 汇（上游 FRANTIC 一类），这一行是**边界**的回路。缺的是模型；判据有候选（集成码的芯—边算例，`docs/cases/evolve-jintrac-iter-5ma-lmode` 已把它的靶板与功率通道表值收下来了） |
| **5D 动理学（回旋动理学求解）** | 无 | ★2026-08-25 补声明（TODO T-C19）：湍流那一层是**准线性回旋流体**（TGLF 白盒移植，§1.5），**不是 5D 回旋动理学**——PIC 与连续型求解全仓零命中，逐 `ky` 的 `γ`/`ω` 出自回旋流体色散矩阵而非动理学本征值。★**与 §3.3 第 4 条的区别要分清**：非轴对称是**不打算做**，这一条是**没做到**（上游有公开算例，是代价问题）。缺的是模型 |
| **台基内部剖面 / SOL** | 无 | 台基**顶**是有的（EPED1-NN，§1.6），缺的是台基**内部**那一段剖面与整个 SOL——梯子在台基顶停下。★2026-08-28 补 TODO 号（**T-C26**）：这一行声明了很久，**待办册里一直没有对应行**——声明过、却从来没被排过期 |
| **到壁与偏滤器的热流** | 无 | ★2026-08-24 补声明（此前这张表**漏了**，与 FUSE 逐条比时才发现）：**位形判据有**——打击点、最小壁间隙、X 点集（§1.2）——**热流一个都没有**，而打击点正是热流要落的地方。上游那一侧是 `ActorDivertors` / `ActorCoreRadHeatFlux` / `ActorParticleHeatFlux` 三件。**缺的是判据**：没有外部算例的热流模型，本项目的规矩是不写（TODO T-C15） |
| **I_p 反馈控制** | 无 | ★2026-08-24 补声明。**垂直**控制回路有（§1.3），I_p 回路没有——上游那一侧是 `ActorControllerIp`（PID 调 V_loop 追 I_p 时序）。缺的是模型加一层接线，不缺判据（TODO T-C16） |
| **规定道（把一道钉在给定时序上）** | 无 | ★2026-08-28 补声明（TODO T-C27）（此前这张表**漏了**，复现 JINTRAC 作业 102530 平顶段时才发现）：本仓的每一道要么**解**、要么**保持不动**，没有第三态——没有「这一道照给定的时序喂，其余照解」。**这不是 `useref`**：那一条只决定**初值**从哪来，喂的是一帧，不是一段。上游那一侧是 JETTO 的 `IEX*`（`EquationsPanel.<eqn>.select : false` 配 ex-file）。★这一行照出的后果是实的：作业 102530 只解电流与电子温度，T_i / n_e 是喂进去的，所以本仓对它的 T_i 比较是**拿预测比人家的输入**，读不出物理——对照只剩 T_e 一道 （`docs/note/jintrac-flattop-reproduction.md`）。缺的是**机制**，判据现成 |
| **锯齿的周期** | 有（内核；页面控件缺） | ★2026-08-28 补声明（TODO T-C28）后**同日内核侧关**：`saw_period`（0 = 崩即触发的旧行为）+ 跨块相位携带（`saw_elapsed_in/out`），两个宿主接线，门在 hollow 夹具（计量 + 跨块相位）。★同批修好**触发判据**（从前看不见轴外 q = 1 面——缺陷非缺口）。★★归因更正：此行原引 JINTRAC 参考「约 163 次崩塌」——查它自己的配置，**锯齿模型关着**（`SawteethPanel.select : false` / `ISAW = 0`），对标里锯齿应当关着跑（`docs/note/jintrac-flattop-reproduction.md` §8d）。剩页面控件 `sawperiod` 未加，worker 行已备好 |
| **杂质：电离态与杂质源** | 部分 | ★★2026-08-25 改判（TODO T-C20）：**输运方程本来就在**——密度道逐离子种（`z`/扩散/箍缩/源/边界逐种一份，`n_ion` 无编译期上限，测试里已在跑 `Z = 6` 碳道），辐射与湍流通量两条闭包也逐种。**缺的是三件**：①**电离态演化**（ADAS 是日冕平衡有效冷却率，不是逐电荷态输运——上游那一侧是 SANCO 一类的多电荷态求解）②**杂质源**（溅射/注入/壁再循环，与加料同一条规矩：没有外部答案的源模型不写）~~③**浏览器接线**~~（★2026-08-25 落地：第二种离子自己是一条密度道，有自己的 D/箍缩/源；Z_eff 随之成为**结果**）。缺的是模型，**判据有候选**：外部集成码的多组分算例带逐核素输运系数与源 |
| **n > 0 MHD 稳定性** | 无 | 只有 n = 0 刚性垂直位移；锯齿是触发 + 内容守恒混合，明确不是 Kadomtsev、不是 Porcelli |
| **电子回旋的 n=1 X 模光深** | 拒绝 | 源表只给了它的**斜射**式（结构不同、还要一个本函数不收的角），所以拒绝而不是拿 n≥2 那行外推。另：LH/EC 协同、热等离子体折射率（源表用冷折射率）也未做 |
| **离子回旋：快波电子阻尼与 FWCD 剖面** | 电流有、剖面与阻尼没有 | **驱动电流有了**（§1.7 新行）；没有的是快波**电子阻尼**（上游把一半电子功率给它，自己的宽剖面）与 FWCD 的**驱动电流剖面**（上游那条是启发式，且**没有任何算例跑过它**——认证套件里 `fwcd` 全为 0）。★上游在 `fwcd ≠ 0` 时会把**整份**热 ICRH 功率改道给快波；本项目两种改道都不建模，功率怎么分是调用方的事 |
| **通量匹配的 Python 宿主接线** | 浏览器有了，Python 仍够不着 | ★2026-08-24 补声明，★★2026-08-25 改：**浏览器那一半已接上**（§1.6 的「通量匹配（稳态）」档）。剩下的是 **Python 宿主**——`python/fylite/` 下除 `kernel.flux_match` 绑定外仍零调用，装配层没有对应的稳态入口。★★**2026-08-25 再改：把源项 · 台基 · 输运 · 电流 · 锯齿 · 平衡迭代到自洽的那一层已经合拢**（§1.6 的「自洽稳态外环」，T-C14），装配层也有了稳态入口（`scenario/model/stationary.py`，与浏览器逐位对拍）。**剩下的仍是通量匹配本身的 Python 调用者**——`kernel.flux_match` 之外仍零调用 |
| **离子回旋的宿主接线** | 内核有，宿主够不着 | 模型已落地并核过（§1.7），缺的是 **C 导出与装配层**——它要动 ABI 并重建三份 wasm，故意排在并行的浏览器批之后 |
| **α 的快离子输运方程** | 只有稳态闭合 | 就地出生、就地慢化，没有径向输运、没有轨道宽度、没有损失道，所以斜坡段、锯齿崩塌与 ELM 都在它之外。热氦灰**剖面**同理不预言——本项目给的是灰的**粒子源**，剖面要密度通道配一个氦扩散率才有。★稳态那一档是**核过**的（§1.8） |

---

## 4. 待办

排序依据是**判据强弱**，不是难度，也不是时间表：外部答案齐备的先做，判据不齐的先补判据。

| # | 事项 | 现状 | 判据来源 | 建议优先级 |
| --- | --- | --- | --- | --- |
| 1 | **回旋加热的宿主接线**（C 导出 + 装配层 + wasm，离子与电子两条同批） | 两条内核**整条已落地并核过**（§1.7）；接线未做 | 已有：内核那十余条判据，加跨宿主逐位对照 | **高**（物理已在，差一层管子）——★要动 ABI 并重建三份 wasm，须与浏览器批错开 |
| 2 | **氦灰剖面** | 只有灰的粒子源 | 已有：同一 ASTRA 算例的**热氦柱**；还需要密度通道配一个氦扩散率 | 中 |
| 3 | **快 α 的径向输运** | 只有稳态就地闭合 | **判据不足**：现有算例只覆盖燃烧稳态芯部，斜坡段 / 崩塌 / 边缘都没有对应的公开算例 | 中（先找判据） |
| 4 | **湍流的环向应力驱动**（动量通道） | ★★★**整条已落地（2026-08-29）**：环向应力对上游自己写出的 `out.tglf.gbflux` 七面里五面逐位吻合、开剪切吻合 2.3e-5；`closure.momentum_chi_phi` 已开；**闭包第三个钩子 `momentum` 已接**（`kernel_coefficients(omega=)`，与 χ/D 共享一次运行）。余下的是**判据**：本仓没有带旋转的对拍算例，χ_φ 这条链只验证到「与上游同一个应力」为止 | 强且现成（应力侧）：`tests/data/tglf_gbflux_jintrac.json`（★旧 `tglf_momentum_jintrac.json` 的 `mflux` 列**是上游从不赋值的输出**，勿再用）；★**弱（剖面侧）**：需要一份带 ω 剖面的外码算例才能判 χ_φ 本身 | **低**（缺的是判据不是模型） |
| 5 | **杂质的电离态演化与杂质源** | ★改判（2026-08-25）：**输运方程本来就在**（§2 第 5 条、§3.4）；★同日**浏览器接线已落**（第二种离子自己是一条密度道，Z_eff 成为结果），剩下电离态演化与真的杂质源模型 | 判据有候选：外部集成码的多组分算例带逐核素输运系数与源 | 中 |
| 6 | **n > 0 MHD**（撕裂 / NTM、理想 n = 1） | 无 | 需要外部稳定性码的算例 | 未定 |
| 7 | **加料模型**（弹丸 / 气吹 / 束粒子源） | 无 | **判据缺**，这正是它至今不写的原因 | 未定 |

★**每一项落地都要同时交付：模型、外部判据、以及本文件与保真度文档的定档改写。**
只有模型、没有判据的一项，按本项目的规矩不算完成。
★**这张表只列没做的**：落地的一项**从这里删掉、写进 §1**，不留一行划掉的字——
2026-08-24 这样处理了「自举—欧姆—拟合电流的自洽闭环」（→ §1.4）与「台基模型」
（→ §1.6），此后的号是连的。历次落地的逐条实测数在 `changelog.md`。

〔第 4 项的前置件与两处未结的差异（2026-08-22）〕**模宽搜索已移植并落地**：上游按增长率
最大搜模宽，本项目此前只接受调用方给的模宽，于是每一条「没写模宽」的参照答案都够不着——
而带动量通量的参照恰好都在那一批里。移植后对上游**同一算例的逐 ky 增长率谱：21 个 ky 里
20 个五位数字全同**。未结的两处：**①** 有一个 ky 上游判为稳定（γ=0）而本项目在扫描基下判为
不稳定；**②** 同一算例的通量比上游低 5–9 %（圆截面），形变几何上差到 1.5–1.9 倍——增长率
既然逐点相同，差异只能在饱和/强度那一段。**这两处结清之前，应力权重即使写出来也无从判。**

---

## 5. 看着像缺口、其实不是

- **上游算了却从不读的中间量未移植**：移植它们等于移植零。★读到赋值就假定有用，是这
  类工作里最自然的一个错误。
- **若干上游变量恒为零**：上游自己把它们硬写成零、真实赋值被注释掉。这里的零**是上游的
  行为**，不是缺口。
- **多线程开关不改结果**：每个元素独立、算术相同，开关两侧**逐位相同**。

---

## 6. 验证依据

★**逐条的账在 `docs/note/benchmark/`**（登记册 + 报告；机器读的是 `benchmark/registry.jsonld`）。
那里记着对了哪些码、判据从哪来、量到多少、哪道门在守。本节只说**分层**——
哪一层的证据回答哪一种问题，以及一条功能今天靠的是第几层。

★★**三类不可混**（`docs/note/benchmark/spec.md` §1）：**验证**问「移植有没有变形」（容差取
机器精度）、**对拍**问「两套模型差多少」（实测后定带）、**确认**问「对不对得上权威参考」
（取参考自报精度）。把三类的数放一列比大小是范畴错误。

功能的可信度分四层，越靠上越强：

1. **对上游实现的记录答案**——新经典（解析族逐位、漂移动理 1e-8）、湍流、局域几何与映射、
   稠密线性代数。这一层回答的是「移植忠实吗」。
2. **外部独立代码的算例**——第三方运行结果，人可读、逐字保留：ASTRA 的 ITER 15 MA
   燃烧算例与 METIS 认证套件的 17 例（冻结在 `tests/data/reference/`）、FUSE 0.7.0 的
   ITER 一次运行（`tests/data/fuse/`）、JINTRAC 的两份运行（第三方，`$FYDOC_DIR`）。
   ★2026-08-29 起 **GACODE 与 FUSE 在本机可跑**，所以这一层对它们不再只有冻结记录，
   还能随时取新参照。★2026-08-30 加入 **TORAX `b4d40633`**（V-08）——它与本仓有
   **同源**模块（QLKNN-10D、Bosch–Hale、Redl-2021），所以那三条问的是「同一个函数
   算出同一个数没有」（2.4e-15 / 1.0e-14），**属第 1 层的强度而不是第 2 层**。
   ★★对同源实现比对**能问出单靠记录答案问不出的东西**：它掉出了 Redl `L34` 那处
   缺陷（本仓算的不是论文的 `L34`，最劣差 89 %，而论文自称两者「almost identical」
   最多 6 %），而该缺陷在**无碰撞轴上恰好消失**——已发表的极限检查全在那条轴上，
   所以第 3、4 层永远看不见它。
   ★ASTRA 那一份**2026-08-24 从「α 加热与快 α」扩到整条燃烧放电**：密度与边界的口径、
   由对拍定出的 χ₀、归一化剖面形状差、以及台基顶（`app/tests/validate-iter-benchmark.mjs`
   28 项，逐条实测数在 `docs/note/iter-15ma-benchmark.md`）。
   这一层回答的是「算得对吗」。★与之一致**不等于上游对**：它们是众多模型中的一种。
3. **公开文献的表格值与解析极限**——如 D-T 反应率的公开四点、各解析闭包的教科书极限。
4. **内部一致性**——跨层恒等式、跨宿主逐位相等、守恒律。这一层只回答「有没有各改各的」，
   **不回答对错**。★**今天只靠这一层的功能，在 §1 里逐条标了出来**（零维放电时序、
   n = 0 垂直稳定性、电磁回路）——它们不是没被测，是**没有外部答案可比**，把这件事
   写明比给它们一个更好听的措辞重要。

★**符号与坐标约定：2026-08-26 之前这里一条判据都没有，现在有了**（旧文留在
`git log -p -- FEATURE.md`）。当时全仓的 COCOS 只出现**一次**——`equilibrium.rs` 文件头
的一行注释——而 `tests/` 与 `app/tests/` 检索 COCOS 零命中，g-file 那一路照读、不做符号
归一、不记录来源约定。**与任何外部答案对拍，第一步就是符号对齐**，所以这一条按本文件
自己的规矩（**没有判据的实现与一个猜出来的数在使用上没有区别**）是欠着的。

2026-08-26 裁定**以 IMAS v4 / COCOS 17 为标准尺**，并分三段补上（判据与实测数在
`TODO` T-C22 / `changelog`）：

| 段 | 落地的能力 | 凭什么信 |
|:---|:---|:---|
| 〔一〕 | 符号与增减向成了**常驻断言**，不是注释 | 内核自己写下的 GS 方程在本仓自解平衡上 **1.0e-11**、随仓 g-file 上 **8.0e-5**；反证「差 2π」「翻号」各偏 >0.5；方向断言在**两个来源**（自解件 + EAST 重构件）逐条落地；**朝向陷阱入闸**（漏一次 `.T`，同一恒等式由 8e-5 变 0.44 而全程无报错） |
| 〔二〕 | **约定靠量不靠信**：`measure_cocos` 把四种物理上有意义的组合代进方程，谁闭合谁就是写出端用的那个 | 夹具上胜者 `dpsi, per radian` **8.0e-5**、次名 **0.756**（差约 10⁴ 倍）；闸里最关键一条是「把同一平衡改成另一种规范，量测必须跟着改口」 |
| 〔三〕 | **对外交换的显式变换层** `to_convention`：源头靠量、逐字段声明、变换完自量自证 | 四种约定全部可达且 GS 残差**四个都不动**（8.039e-05）；往返回到原件 1e-12；把 `pprime` 故意误分类，方程当场拒收 |

★**那时欠着的一半已补上**（T-C22 全关，2026-08-26）：`require_convention` 现在**接在
活的读入路径**上——`fyo.equilibrium(path)` 量不出约定即**按名拒绝**，量得到的则把结论
盖进文档。此前这里写着「没有接进活读入路径、等一份真实 g-file 定容差」，那句话已经
过时，留一行记去处而不是留在正文里误导。

★**同层的另一件已补上**：**确定性**（A-3）。运行清单现在记下「会改一个数」的环境变量
（按声明取，不按手写名单）与线程设定，而「线程数不改结果」这句话由**实测**支撑——
`breakdown-iter`（30 标量 / 5 数组）与 `discharge-iter`（138 标量 + 整张 ψ 图）在
1 与 4 线程下交付数**逐位相同**。这仍属第 4 层（内部一致性）：它回答的是「同一份代码
在不同机器设置下会不会各算各的」，不回答对错。

关于任何一项功能「能不能定量使用」，以 `docs/reference/fidelity.md` 的逐条实测为准；
本文件只说**有没有、凭什么信**。
