# Changelog

All notable changes to the published fylite distribution (the Python package,
the `fy` executable, the examples and the user documentation).  Versions follow
the `VERSION` file of the release; `-alpha` releases may change entry points and
result formats without a migration path.

## 0.0.1-alpha — unreleased

First release from the release-only repository.  The development history of this
project is not published here; each release is one snapshot.
