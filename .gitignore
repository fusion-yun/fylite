#: 构建产物：本仓不跟踪（见上）。★不要加 `!` 例外把某一份放回来。
*.so
*.wasm

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
build/
dist/
sdist/
wheels/
pip-wheel-metadata/

# 测试与静态检查的缓存
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pytype/
.hypothesis/
.tox/
.nox/

# 覆盖率
.coverage
.coverage.*
coverage.xml
htmlcov/
nosetests.xml

# 环境
.venv/
venv/
env/
.python-version
.env
.env.*

# Notebook
.ipynb_checkpoints/

 
# ── JavaScript / Node ─────────────────────────────────────────────────────
# ★写 `node_modules/` 而不是 `*/node_modules/`：后者只匹配一层
# （`tools/node_modules` 中，`app/x/node_modules` 不中）。JS 面在 `app/tests/`
# 的 50 个 `.mjs` 与页面脚本里，依赖是 `npx` 按需拉的，从来不该入库。
node_modules/
jspm_packages/
.npm/
.yarn/
.pnp.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache
.stylelintcache
*.tsbuildinfo
.parcel-cache/
.cache/

# ── 文档构建 ──────────────────────────────────────────────────────────────
# MyST（`docs/*/myst.yml`）
_build/
.myst/
 
 
# ── Agent 提示面 ──────────────────────────────────────────────────────────
# 由 sp 平台在本地渲染（sp `scripts/render.py`）；fylite 是独立协议成员，
# 提示文件留在本地，从不提交（见 `.context/PROJECT.md` §6）。
# ★`.claude/skills/` 是随发行物走的交付件，**保持跟踪**——所以下面那条否定不能删：
# 只写 `.claude/*` 会把它一起忽略掉，而上面这句话还留在这里，规则与注释就打架了。
.context/
CLAUDE.md
AGENT.md
AGENTS.md
.claude/*


# ── 第三方构建产物 ────────────────────────────────────────────────────────
# tools/jmds — 由 `sh tools/jmds/build.sh` 构建；jar 与拉下来的编译器都是产物，
# 而 `mdsplus-api.jar` 是别人的东西，不由本仓分发。
tools/jmds/.build/
tools/jmds/jmds.jar

# ── 操作系统与编辑器 ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
*~
*.swp
*.swo

# ── Rust（数据层，2026-09-02 起本仓有 Rust 源码）───────────────────────────
#: ★★本仓从此接受 Rust：`rust/fylite_runtime/` 是**中间层**（2026-09-04 自 `fylite_data`
#: 改名，FYL-DESIGN-16 N-1；mdsip 编解码，后续收编
#: g-file/est2），源码公开——它是协议与格式，不是物理 IP。物理内核仍在私有仓。
#: 构建目录不跟踪；制品与其它制品同规矩（见本文件抬头：`*.so` `*.wasm` 不跟踪）。
target/
Cargo.lock

 

#: ★★2026-09-04：`_bin/` 从未真的被忽略——`*.so` / `*.wasm` 两条管不到一个没有扩展名的
#: ELF。FYL-DESIGN-15 R-5 写的是「`_bin/*` 不进 git，打包时装入」，而这条规矩此前只靠
#: 没人在本机跑过 `rust/build.sh --exe` 才成立。改名那次第一次装了它，`git status` 当场
#: 把它列成未跟踪——所以规矩今天才落到文件里。
python/fylite/_bin/

# ── 参考数据 facts/（拖回的输入，2026-09-04 用户裁定）─────────────────────
#: ★★`facts/` 装的是**关于具名个体的断言**——A-Box 的字面含义，用一个不需要术语表
#: 就读得懂的词说出来。按域分轴：`facts/device/<id>` 今天有内容，
#: `facts/amns/<provider>` 与 `facts/experiment/<machine>/<shot>` 是同一个轴上的下一格。
#: 与 `cases/` 成对：`cases/` 是**要跑什么**，`facts/` 是**跑在什么之上**。
#: ★`models/` 不在这里：神经网络权重不是关于世界的断言，是制品。
#:
#: 按需从 A-Box 拖回（`tools/abox-to-facts.py`），一个个体一目录：卡片 + 许可账
#: `rights.json`。**不随 fylite 源码发布**——所以在这里；但**可以进构建出来的制品**，
#: 由许可账逐个判：内部版带全部，公开版不含 EAST（真实放电数据）与上游禁止再分发的 IDS。
#: 页面那一侧经 `app/facts`（指向仓根 `facts/` 的符号链接）取同一份字节——**一个名字
#: 贯穿仓内、页面与发布路径**。问答面 `tools/facts-publish.py --flavour public|internal --list`，
#: 闸子 `python/tests/test_facts_corpus.py`。
/facts/
