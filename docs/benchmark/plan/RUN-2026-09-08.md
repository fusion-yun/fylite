---
title: 验证定序册 · 执行记录 2026-09-08
---

# 执行记录 2026-09-08（第三轮 · 内核 + 数据层 + 两本册子重出）

本轮把前两轮各做一半的事合在一次里做完：**内核的门**（第二轮做过）与**数据层的册子**
（第一轮之后再没跑过）。这是接缝收敛（`kernel-public-seam.md`，2026-09-07 收官）之后
第一次整批复测——第二轮留下的 51 条「跨仓漂移」在这里得到了实测答案。

- 执行环境：本机三仓同址（`fylite` · `fylite_kernel` · `fydoc`），两仓开跑前均干净、
  均在 `develop`
- ★★**本轮与另一处改动同日发生且互不相同**：`fusion_gain` 的 NaN 之争在本轮进行中被
  裁定并落地（内核 `83ea3b1` · 公开仓 `2c31316`：内核改写 DD 空值 −9e40，判据册把空值
  记成**缺席**），S7 那条唯一的 `fail` 因此关闭。本页的物理校验册数字取自那次落地之后的
  册子；本页其余各节（登记册复测、内核的门、九处发现）是本轮自己的。同日另落地的还有
  **`V-15`（S0 的 g-file 往返）**，本册子的「尚未立」因此由 4 减为 3——也不在本轮账上
- 内核：`cargo test --release -p fylite` 现跑；装配层门用装着的 `.so`（与内核检出同日构建）
- 参考侧：算例书与装置牌都在内核检出里（2026-09-05 收录），故本轮**没有一条**因
  「语料不在场」而不可评

## 一、这一轮的净结果

| | 2026-09-05（第二轮） | 本轮开跑时 | 本轮收尾时 |
| :--- | ---: | ---: | ---: |
| `cargo test --release -p fylite` | 439 / 0 | 459 / 0 | **459 / 0** |
| 内核仓 pytest 通过 | 814 | 1190 | **1241**〔含并行会话当日落地的 B-07 三条〕|
| 未通过 | 14 | 8 | **0** |
| 出错（收集期） | 43 | 5 | **0** |
| 跳过 | 33 | 47 | 11 |
| xfail | 〔未记〕 | 〔未记〕 | 9 |
| 公开仓自己的套件 | 〔未跑〕 | — | **2107 通过 / 0 未通过**（283 跳）|
| 公开 V&V 登记册复测 | 〔未跑〕 | — | **成立 25 · 未评估 0**（09-02 是成立 23 · 不成立 1 · 未评估 1）|
| 物理校验册 | 〔未跑〕 | — | 88 条评了 24、**未通过 0**（09-04 是未通过 1）|

★收尾的 0 未通过是**三次实测之后**的：修完开跑那 8 条 → `fusion_gain` 裁定落地带出
F-25（1 红）→ 修 F-25 与退役 F-19 之后，接缝闸子抓出**它自己那一条**（基线还给
`fylite.run` 留着一格额度，而退役把最后一处 import 带走了）。★★那一红是这道门第一次
真的抓到「基线成了愿望清单」——它的注释写着「一本只增不减的账会慢慢变成一张愿望
清单」，今天它证明自己不是装饰。账已按实测降一格。

★收尾这一列是 **17:21 那一跑的实测**（1241 passed / 11 skipped / 9 xfailed，606 s），
不是从上一跑推出来的。上一跑是 1237 + 1 红（接缝那一条），修完本该是 1238——**实测多
出三条**，因为并行会话当日把 B-07 的 TGYRO 门落进了同一棵树（`f64d7ad`）。★这行小账
写下来是因为它正是本册子反复在说的那件事：**一个推出来的数和一个量出来的数，看起来
一样，来源不一样**。

★★**第二轮的 F-14（51 条跨仓漂移）到今天全部结清**：那一批点名的五处里，四处
（`fylite.io.imas_h5` · `fylite.io.jetto_bin` · `run.forward_equilibrium` 与两处符号/路径）
由接缝收敛消掉，今天都在通过的那一侧；**第五处**——公开包不存在的那五个模块——
经用户裁定**退役**（F-19），逐个交代去向的表在内核仓 `tests/PHYSICS-MIGRATION.md`。

★**开跑时的 8 条未通过，逐条查下来没有一条是数值不符**：4 条是环境变量没设
（`$FYLITE_NN_DIR`，权重就在公开检出的 `models/` 里），4 条是路径没跟上重组
（F-17 · F-18 · F-20）。**这一轮从头到尾，没有出现过一条「量到了而没过」。**

## 二、本轮改了什么

| # | 改在哪 | 这一刀 |
| :--- | :--- | :--- |
| 1 | 内核仓 `tests/test_circuits.py` | 缺 `rv6565.ddd` 时**跳过**，不再判负（第二轮 §6 第 3 条点名要的） |
| 2 | 内核仓 `tests/data/tglf_fluxmatch_jintrac.json` | 自 `681d016^` 取回（分仓时丢的），两道门复活 |
| 3 | 内核仓 `tests/test_jintrac_case04_replay.py` | 路径拼了两遍，四条门整模块空跳（F-17） |
| 4 | 内核仓 `tests/test_fuse_benchmark.py` | 公开仓算例书搬家后旧位置没跟（F-18） |
| 5 | 内核仓 `docs/cases/registry.jsonld` | V-02 / V-03 的四条 `nn_tables/` 指针改指 `$FYLITE_PUBLIC/models/`（F-20） |
| 6 | 内核仓 `tools/benchmark-publish.py` | ①渲染件写到**册子实际在的地方**（F-21）②认得公开检出里的制品③把「跳过」读成未评估而不是通过（F-22） |
| 7 | 公开仓 `app/tests/validate-iter-benchmark.mjs` | 参考表认三处落点（F-22 的另一半）；**这一档改从算例文档来**，不再从已撤的菜单来（F-23） |
| 8 | 内核仓 `tests/test_kernel.py` | 钉的是新约定（DD 空值）而不是退役的 NaN（F-25）|
| 9 | 内核仓 五个模块退役 + `tests/test_tglfnn_ukaea_stays_out.py`（新）| F-19：捞回两条许可判据，删掉其余；逐个交代谁接手，落 `tests/PHYSICS-MIGRATION.md` |
| 10 | 内核仓 `tests/test_circuits.py` + `tests/data/vessel_grid_response_east.json`（新）| **不再需要 `rv6565.ddd`**：两道不要外部件的门接住那次验收（F-26）|

## 三、本轮的发现

| # | 发现 | 状态 |
| :--- | :--- | :--- |
| **F-17** | **`test_jintrac_case04_replay` 把 store 路径拼了两遍**：`_STORE` 自 2026-09-05 收录起已深到 `…/FYDOC-CASE-09-jintrac/corpus`，而 `_RUNS` 的每一条也带着同样两级，`_STORE / rel.split("data/")[-1]` 于是指向 `…/corpus/FYDOC-CASE-09-jintrac/corpus/runJINTRAC_101612`。**件就在盘上**，B-03 的四条门却整模块跳过，理由写着「不在挂载的 store 里」 | **已修**。四条门复活并**全部通过** |
| **F-18** | **`test_fuse_benchmark` 找 `<公开检出>/cases/evolve-fuse-iter.jsonld`**，而公开仓的算例书 2026-09-04 搬到了 `docs/examples/<栏>/`。B-01 因此少跑一条，跳过理由写着「设 `$FYLITE_PUBLIC`」——**而它已经设了** | **已修**。B-01 由 8 条变 9 条，新那条通过 |
| **F-19** | **五个测试模块的被测物已经不在公开包里**（`scenario.model.sources` · `scenario.model.tglfnn_ukaea` · `scenario.analysis.selfcal` · `scenario.analysis.tomography` · `io.kfile`）——第二轮 F-14 的五分之一，接缝收敛**没有覆盖到它们**。收敛把物理沉进了内核的门（`code/selfcal` 等已在），但这五个模块仍按旧形 `import` 那些 Python 名字，于是收集期就 ImportError | **已退役**（用户裁定 2026-09-08）。逐个交代去向的表在内核仓 `tests/PHYSICS-MIGRATION.md`。★★净账写在那里而不是这里，因为它不好看：57 条判据分四堆——**捞回 2 · 随被测物一同退役 5 · 有真接手 24 · 真丢了 26**（层析 11 · UKAEA 网络 8 · 装配层编排 4 · 自标定方法学 3）。★捞回的两条是**许可判据**（LGPL 权重不得进任何我们分发的树），它们此前不但没跑，glob 的还是一个不存在的目录——**恒真**；现在看三处，一处都看不到时判负 |
| **F-20** | **登记册的神经权重指针指向一个哪个仓都没有的路径**。三份 `.npz` 与它们的 README 2026-09-01 随发布面迁到公开仓 `models/`（原名 `nn_tables/`），登记册没跟；`test_benchmark_registry` 因此判负，而**公开渲染件把它渲成 `$FYLITE_KERNEL/nn_tables/…`**——纳入类别还标成「私仓制品」，而它们其实是公开可复取的（上游 Apache-2.0 / CC-BY-4.0，就躺在公开仓里） | **已修**。四条指针改指 `$FYLITE_PUBLIC/models/`，类别改 `public-derived`，sha256 本轮首次真的算了出来 |
| **F-21** | **公开册的渲染器六天没有渲染到公开册上**。册子 2026-09-02 从公开仓根 `benchmark/` 搬到 `docs/benchmark/`，而 `benchmark-publish.py` 一直写死仓根那一支：照它自己文档字面跑 `--fylite ../fylite`，会在公开仓根**新建**一份、把 25 条记录渲进去，真正的册子一个字节不动，**退出码 0**。六天里那册子是**手改**维护的（四次 `docs: Follow …`），而它抬头写着「不许手改渲染件」 | **已修**：先找册子在哪（`docs/benchmark/` 优先，旧位置仍认），两处都没有就**拒绝**，不新建第二份 |
| **F-22** | **「跳过」被渲成「成立」的一条路**。`C-01` 的浏览器闸子缺 ASTRA 参考表时按设计「响亮地跳过」并 `exit(0)`——而 `benchmark-publish.py` 的 `read_mjs` 把退出码 0 一律读成 `pass`。自 2026-09-04 起公开检出里根本没有那张表（`tests/` 整级已不在），**这条路上的每一次发布都会把「一次也没比」渲成「复测成立」**。本轮实测复现：不带 `$FYLITE_KERNEL` 跑它，退出码就是 0 | **已修**（两头各一处）：闸子认三处落点并把「跳过」写成一个词，发布工具认那个词。★本轮 `C-01` 因此照实记 `unevaluated`，不是 `pass` |
| **F-23** | **`C-01` 的浏览器闸子本身是陈的**（与 F-22 是两件事：那条讲怎么被读，这条讲它跑起来什么样）。参考表接上之后它真的跑了，并在第一段就 `TypeError: Cannot set properties of null` ——它在 `#model-evolve-case` 上套算例，而**案例菜单 2026-09-01 已随各栏的出厂设置一起撤掉**（`_browser.mjs` 的注释记着这次撤除）。闸子还在按菜单那一版的页面写 | **已修**（用户裁定 2026-09-08）。★不是「重新映一套旋钮」——那正是本册子 §5 禁的顶替。**这一档改从算例文档来**：菜单当年套的就是 `docs/examples/evolve/evolve-iter-15ma.jsonld`，现在直接读它的 **114 条 `parameters[]`**（实测：114 个控件页面上一个不缺）落到控件上，判据一个字没动。缺任何一个控件即判负并把缺的列全——那意味着这一档在页面上已经表达不出来了。★现驱动：**26 条断言全过，页面零报错**，`C-01` 由「未评估」转「成立」 |
| **F-25** | **`fusion_gain` 的裁定落地时，内核仓里钉着旧约定的那道门没跟着改**。`83ea3b1` 把「无注入功率处的 `Q`」由 `NaN` 改写 DD 空值（−9e40），而 `tests/test_kernel.py::test_zerod_q_is_nan_without_injected_power` 仍断言 `np.all(np.isnan(rk["q"]))`——实测拿到的是一整支 `-9e+40`。★**这是一道正在起作用的门**：它钉的就是那条约定，约定改了它就该改，红得完全正确 | **已修**（用户裁定 2026-09-08）。门的形状没变、钉的东西也没变：那一段**不许是 0**（0 会被读成「没有增益」），也不许是一个看着正常的数——变的只是「不适用」用哪个记号说。另加两条：不许有 NaN（数组要能求和、能画），不许有 0 |
| **F-26** | **本仓其实不需要 `rv6565.ddd`，而代码读起来像是需要**（追 F-16 时查出来的）。整个交付面**没有一个调用点**读它——`fylite.device.vessel_response_tables` 唯一的调用者就是那道两算路验收；反演路早已由装置文档的被动几何现算（T-C37），`conductor_geometry` 连 deck 都不再解析。而 `test_circuits.py` 的抬头写着「两条路都在仓里，所以这是一次真正的离线回归」——**只有一条在**。另查出：`conductor_geometry` 自述「矩形由那份 deck 导出、逐位相同」，**没有任何东西在看着这句话** | **已修**（用户裁定 2026-09-08「修 fylite 不需要 rv6565.ddd」）。那道验收是一次**验证**，参考侧是 EFIT 自己的表，三仓与 fydoc 皆无，故它只能跳过——不可伪造。改为由两道**不要任何外部件**的门接住它保下来的东西：①deck 与 fyo 文档描述同一台机器（14 线圈 + 40 段内壳矩形**逐位相同**，实测 0 处不符）②自算的内壳→网格 ψ 响应对着仓内冻结件逐位（实测：正确重算偏差 0.0，改积分阶 nu 6→4 偏 1.2e-4，一段挪 1 mm 偏 5.3e-5，判据 1e-12）。★★第二道明写是**回归钉不是验证**：参考侧是本仓自己的几何，它证明不了「算得对」，只证明「没变」 |
| **F-27** | **退役五个模块之后，接缝账上多出一格额度**。`test_seam_is_fyo_only.py` 的 `BASELINE` 给 `fylite.run` 留着 1 个 import 位，而那一处 import 在 `test_selfcal.py` 里——随 F-19 退役被带走了。★这道门自己的注释写着「一本只增不减的账会慢慢变成一张愿望清单：某一面早就清干净了，而基线还给它留着」，**今天它第一次真的抓到这件事**，抓的是本轮自己 | **已修**：按实测把那一行删掉。★这是本轮唯一一条「门抓住了改动的人」的记录，值得单列：接缝确实又窄了一格，而窄了之后**账不跟上，下一次它就会被当成可以再宽回去的余量** |
| **F-24** | **一段手写的归因住在生成件里**。`docs/benchmark/physics/zerod-iter-15ma.md` 的〈那一条未通过〉是 2026-09-08 手写并提交的（`122847e`），而该文件由 `tools/benchmark-run.py --write` 整体重写——本轮的重出**把它删掉了**（当场逐字恢复）。这一次它自己解决了：几十分钟后 `finite` 被裁定关闭（`2c31316`），那段文字连同判负一起正当退役。**但机制仍在**：生成件里的手写段活不过下一次 `--write`，而不会有任何东西报错 | **本例已消解**，**机制待定**：手写段该住哪（判据册算例声明的 `caveat` · 本册子的 RUN 页 · 渲染器认一个锚点），定下来再往生成件里写第二段 |

★**S7 那条 `fail` 本日关闭**（不在本轮的功劳簿上，见抬头第三条）：`Q` 在 `P_aux = 0`
处此前写 NaN，与「NaN 不是物理态」相撞；裁定不是选边，而是一次测量——那一段
`p_fus = 55 MW` 而 `p_aux = 0`，故它不是「无定义」而是「不适用」，改写 DD 空值
（−9e40），判据册把空值记成**缺席**（既不算违反，也不算有限性的证据）。物理校验册
因此 **未通过 0**。

## 四、两个算例仍然没有产出可判——但拒绝的**理由变了**

这是本轮物理校验册里唯一的实质变化，值得单列：

| 算例 | 2026-09-04 的拒绝 | 本轮的拒绝 | 读法 |
| :--- | :--- | :--- | :--- |
| `discharge-iter` | `[-30] no code code/discharge`：**内核没有这个 code** | `[-33] code/discharge takes the whole device document and is reached through the tree door only` | **能力已经有了**（接缝收敛的第三刀沉的就是它），挡在这里的是**通路**：物理校验册现跑那条路走 `fylite_runtime_case_json` → 内核的**扁平门**，而扁平门只递 f64 槽，递不了文档 |
| `evolve-east-hmode` | 需要 `device` 档的平衡 | 同，且拒绝语句多写了一条出路（`inputs/equilibrium`） | 仍是**缺输入**：这一档要一份平衡文档，算例里没有绑 |

★`discharge-iter` 这一条的下一步是明确的，且零件都在：`case.rs::run_json` 手上已经有
`resolved`，`plan_tree()` 会把它拼成计划树（`DOCUMENT_PORTS` 里就有 `inputs/device`），
`outcome_from_record()` 会把树门的答案读回扁平那一对。**但它同时需要算例把装置文档绑上**
——`discharge-iter.jsonld` 今天只说 `machine_desc/iter` 是它跑在哪台机器上，没有把那份
文档声明成输入。两件事一起做，这一条的 12 项检查才评得了；只做前一件，得到的是另一句
拒绝。**本轮不做**：它改的是数据层的门，不是账。

## 五、逐阶段判决（相对前两轮的变动）

| 阶段 | 上一次 | 本轮 | 依据 |
| :--- | :--- | :--- | :--- |
| **S0** 数据通路与口径 | pass | **pass** | 不变 |
| **S1** 平衡正问题 | unevaluated | **unevaluated** | 参考侧仍在 `$ITER_SCENARIO_ROOT`，不在本机 |
| **S2** 几何与度规 | pass | **pass** | `V-01` 五门 95 条通过（09-02 那次有 2 条不成立） |
| **S3** 新经典 | pass | **pass** | 不变 |
| **S4** 湍流与代理 | TGLF 支 pass · 代理支 unevaluated | **pass** | ★代理支解锁：权重在公开检出 `models/`，`$FYLITE_NN_DIR` 指过去即可（`V-02` 14 · `V-03` 27 条） |
| **S5** 源 · 汇 · 边缘 | pass | **pass** | 不变 |
| **S6** 定态通量匹配 | 映射层 pass | **映射层 pass** | `V-06` 35 · `V-07` 88 条；定态解本身仍无记录（缺口 G-3 不变） |
| **S7** 零维整合 | fail | **pass** | ★`finite` 本日裁定关闭（抬头第三条）：`zerod-iter-15ma` 5 条通过 0 未通过；`B-04` 12 条通过 |
| **S8** 1.5-D 演化 | 自洽层 pass · 对拍层 unevaluated | **pass** | ★对拍层解锁：`V-14` 2 · `B-05` 6 · `B-02` 18 · `B-03` 4 条全部通过（`B-03` 是 F-17 修好的那四条） |
| **S9** 装置级确认 | α 通道 pass · `C-01` unevaluated | **pass** | `C-02` 2 条 cargo；★`C-01` 的闸子当日修好并现驱动，26 条断言全过、页面零报错（F-23）。上游 S7 本日亦转 pass，故它不再受阻塞规则挡 |
| **R** 平衡反演 | 部分 pass | **pass** | `B-06` 18 条全部通过（`rv6565.ddd` 那条已改为跳过，F-16 结清） |
| **K** 垂直稳定 | unevaluated | **pass** | `C-03` 2 条通过（装置牌在内核检出里，本轮跑得到） |
| **P** 起始与击穿 | blocked | **blocked** | 参考侧不在场，不变 |

## 六、下一轮的前置

〔本轮之内已关的：**F-19 · F-23 · F-25 · F-26**（用户裁定 2026-09-08），
以及不在本轮账上的 S7 `finite` 裁定与 `V-15` 落地。〕

1. **F-19 丢掉的那 26 条**——层析的十一条反演质量判据、UKAEA 的八条网络判据、
   装配层的四条编排判据、自标定的三条（含那条方法学对照），今天没有接手方。要不要
   在门那一侧重写它们，是一次**新的排期**，不是这次退役的余项；不排也行，但别让
   后来的人以为从来没有过。
2. **F-24 的去处**——生成件里的手写段活不过下一次 `--write`，而不会有任何东西报错。
   定下来（判据册算例声明的 `caveat` · 本册子的 RUN 页 · 渲染器认一个锚点）再写第二段。
3. **`discharge-iter` 的门**（第四节）——做了它，物理校验册的可评条数才会动。
   它要两件事一起做：`run_json` 走树门，且算例把装置文档绑成输入。
4. **六条阻塞**——今天它们是**唯一**还不通的东西了，全部卡在参考侧，没有一条卡在实现上：
   五条取不到参考（`teq-iter-reference` · `tosca-iter-equilibria` · `dina-iter-scenarios` ·
   `fuse-iter-time` · `transmak-initiation`），一条（`C-08` GENE）**载荷公开而定态不全**，
   缺的两项由上游论文自己写明未发布，可向作者索取。
   〔★本条原写「三处缺口与五条阻塞（`C-08` `B-07` `C-09`……）」，**同日晚间就陈了**：
   并行会话把 `V-15` · `B-07` · `C-09` 三条立起来并各带一道门，「尚未立」归零，`C-08`
   转阻塞。改这一条时一并记下——**一天之内两处计数打架**，正是本册子要靠一个生成源
   而不是靠两处散文维护判决的理由〕
