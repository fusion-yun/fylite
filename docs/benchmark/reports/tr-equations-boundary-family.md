---
title: "tr-equations-boundary-family"
---

# 边界条件族与电流驱动：**四件齐了**

<!-- ★生成件，勿手改：`python tools/benchmark-book.py`。正本是 `records/tr-equations-boundary-family.jsonld`，本页只是它的可读面。 -->

*输运 (Transport) · [方程组求解与边界条件](../domains/tr/equations.md)　|　记录正本：`records/tr-equations-boundary-family.jsonld`*

## 摘要

- **类**：验证　**判决**：**成立**
- **量的是**：边界条件族与电流驱动：**四件齐了**
- **参考**：抄录的判据本身
- **验的需求**：`FR-TR-002`
- **跑在内核**：`sha256:18d901c1abeb76aa…`（新鲜度 **current**）
- **记录版本**：3.5　**评审**：草稿　**日期**：2026-09-17

## 问的是什么

**被量的**：边界怎么表达、电流怎么驱动、通道怎么加（四道通道现在共用一个描述子层）

**参考**：抄录的判据本身

> ★本条没有外部参照：判据点名了**四件具体的事**，逐件去内核里查有没有即可。★★2026-09-17 之前**两件有两件没有**，本条判不成立；同日内核先补齐五型边界、再把四道手写通道求解器改道到一个描述子层——**四件现在齐了**，判决随之翻成成立。★判据一个字没放松，是内核追上了判据。

**口径与适用域**：

> 本次检出的 `rust/fylite/src/transport.rs` 与 `case.rs`。★第一格 2026-09-17 之前是**源审视**（搜 `channel_descriptor` 零命中），现在有门了——新通道实测 + 逐位 parity；第二格是源审视加逐型实测；后两格是实测。★**不判各通道的物理对不对**——那是别的记录的事；本条只判「判据点名的四件事在不在」。

## 判据与量到多少

:::{figure} ../figures/tr-equations-boundary-family-headroom.svg
:alt: tr-equations-boundary-family 的判据余量图
:width: 100%

每条判据离它的带还有多远（对数轴，1 倍即判据本身）。★**绿而窄（< 2 倍）另着色**：它与余量一千倍的判据在下表里都只是一个「成立」。
:::

| 判据 | 容差 | 取法 | 量到 | 判 |
| :--- | ---: | :--- | :--- | :--- |
| 通道集是否**描述子驱动**（按名加/删通道零求解器改动） | — | reference_self_reported | 四道手写求解器（`solve_density` · `solve_psi` · `solve_momentum` · 双温 march）全部改道到 `solve_channel(x, y_old, &ChannelDescriptor, &Numerics, d)`；**求解器体内 `match` 通道名 0 处**；新造一道内核里不存在的 `tracer` 通道，求解器改动 **0 行**；四道既有通道 **15 个位模式逐位不变**（位模式在改道**之前**采集并写死进测试） | **成立** |
| 边界条件的种类数 | 5 | reference_self_reported | 边界改成 ETS 的三元组 `V dY/drho + U Y = W`，五型齐备：**值**（type 1）· **梯度**（2）· **标长**（3）· **通量**（4）· **通用**（5），另加 type 0「不求解」。★逐型各有一条**只有它成立时才成立**的检查，并验了**五型给出五个不同的边界值**——一个把五型都算成 Dirichlet 的实现会在那一格露馅，而其余每一格它都能过 | **成立** |
| 安培 / 回路电压驱动是否成立 | — | reference_self_reported | **回路电压**：solve_psi: `edge += edge_rate * dt`（transport.rs:705）；**安培（Ip 控制）**：code/evolve 的 `ipctl`，带 PI 增益 ip_kp / ip_ki 与积分态 | **成立** |
| ★B0 压缩项：缺省 0 是否**逐位不变的死路径** | — | machine_precision | `label_drift` 在 `b0_dot = 0` 时返回的每个值**位模式都是 +0.0**；`b0_dot = 0.3` 时最大 \|v\| = 6.0000e-02 —— 关得死、开得动 | **成立** |

**`通道集是否**描述子驱动**（按名加/删通道零求解器改动）`** — ★判据原文：「通道集自动生成**且被求解器消费**」。要验它只需问一句：**加一道通道要不要改求解器？**

**`边界条件的种类数`** — ★判据原文：「**五类型 BC**」。★2026-09-17 内核补齐了——判据没有放松，是内核追上了判据。

**`安培 / 回路电压驱动是否成立`** — ★判据的第三句。

**`★B0 压缩项：缺省 0 是否**逐位不变的死路径**`** — ★★判据原文点名「`b0_dot_over_b0` 缺省 0 **逐位不变**」——**这句话本身就是一条判据**：一个新装的项若在关掉时也改答案，它就悄悄改写了这扇门此前给过的每一个结果。

**★★2026-09-17 补齐：通道集改成描述子驱动**

- ★★**这一格此前记的是「另一种架构，要重写这一层」——那句话没说错，这一层确实被重写了。**重写照抄 ETS 的正则形式，不是发明第二套：六组方程压成同一个两点边值问题，通道之间只差系数。
- ★**判据的括号剩半句没兑现**：「通道集自动**生成**」——一次运行该演化哪些通道，仍由调用方挑具名入口决定。括号把验法定成了三件事（描述子驱动 / 零求解器改动 / 逐位 parity），三件都过，所以这一格判成立；那半句记在 `tr-equations-channel-descriptor` 的 caveat 里，**不抹掉**。

**★★2026-09-17 补齐：ETS 的五种边界类型**

- ★★**照抄 ETS 的设计，而不是发明一个枚举**：五种物理边界都翻译成同一个 `(V, U, W)`，于是**装配那一行的代码只有一份**——「新加一种边界」不需要碰求解器。★这正是本条第一格（通道描述子）要的那种性质，只不过用在了边界上。
- ★★**缺省路径逐位不变**：`EdgeBc::value(y)` 落成 `(0, 1, 0, y)`，实现对 `V = 0` 短路，连 `V/h` 这个除法都不做。★660 项既有内核测试（含硬编码到 1e-13 的参考值）一项没动——**这不是巧合，是选那个离散化的理由之一**。
- ★三处照抄时必须看清的地方：〔一〕梯度型 `W = -g`（规范黑体强调：`g` 取正对应向边缘**下降**的正常剖面，写成 `+g` 会得到一个向外爬升的剖面而它看着像生效了）；〔二〕标长型取 `(V,U) = (L,1)` 而非规范翻译表的 `(1,1/L)`——同一条件，但 `L -> 0` 时仍良态，**规范文本与源码在这一处的差别是条件数不是物理**；〔三〕通量型隐式耦合，边界行**每轮重装**。
- ★★**一处实测逼出来的拒绝**：稳态（`dt = inf`）配纯梯度边界（`U = 0`）是**奇异**的——守恒算子每行和为零，纯梯度边界再来一行和为零，常数向量落进零空间。此前那种请求**返回一个 1e14 的答案**，而 1e14 与 1e2 在下游看来都只是「一个数」。现按名拒绝（`SteadyNeedsAnchoredEdge`）。★梯度型本身没问题——它用在**含时**步进上就是良态的，ETS 的方程本就是含时的；被拒的是「稳态 + 不锚定值」这个组合。

**★电流驱动两条路都有**

- ★这一格是本条里成立的那一半，写清楚免得「判 fail」被读成「什么都没有」。

**★★B0 压缩项：关掉时**逐位**不动**

- ★两头都要验：只验「关掉是零」会放过一个**开了也什么都不做**的实现，那种实现同样「逐位不变」，却等于这一项没装。

## 不可比的部分

- ★★**判决从 fail 翻成 pass**：四件全成立——电流驱动两条路、`b0_dot` 压缩项、五型边界、通道描述子。★两次翻动都发生在 2026-09-17 同一天，且都是内核补齐，**判据与容差一个字未改**。
- ★抄录里 `FR-TR-001`（多通道方程组求解）与本条共用同一行判据。描述子那一格的完整证据（新通道实测、逐位 parity 表、隐式汇死路径）落在 `tr-equations-channel-descriptor`，本条只记结论。

## 追溯

- 首次入册 2026-09-17　末次修订 2026-09-18　版本 3.5　评审 草稿

**变更史**（★改判本身留在册里，不覆盖旧结论）：

| 版本 | 日期 | 谁 | 做了什么 |
| :--- | :--- | :--- | :--- |
| 1.0 | 2026-09-17 | Claude Opus 5 (1M context) | 首次入册：`FR-TR-002` 判**不成立**——判据点名四件事，两件有两件没有。有的：回路电压 + Ip 控制两条驱动路；b0_dot 关掉时**逐位**不动、开时动。没有的：通道描述子（零命中，四个手写求解器）、五类型 BC（只有 Dirichlet 一种）。★判 fail 不判「部分」——**少记一条空缺，少的恰是要重写一层才能补的那条** |
| 2.0 | 2026-09-17 | Claude Opus 5 (1M context) | ★★第二格从不成立转为成立：内核照抄 ETS 补齐了五型边界（值/梯度/标长/通量/通用 + type 0），做成三元组 `V Y' + U Y = W`，**装配那一行只有一份代码**。缺省路径逐位不变（660 项既有测试一项没动）。★另记一处实测逼出来的拒绝：稳态配纯梯度边界是奇异的，此前返回 1e14，现按名拒绝。★**判决仍是 fail**——通道描述子那一格是架构缺口，补一件不改判。 |
| 2.1 | 2026-09-17 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-002/008/010/011` 与 0D 三项入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：fylite 侧 110 道、内核侧 656 项全通过。 |
| 3.0 | 2026-09-17 | Claude Opus 5 (1M context) | ★★**翻判：fail → pass**。最后一格（通道集描述子驱动）成立——四道手写通道求解器改道到同一个 `ChannelDescriptor` + `Numerics` + `solve_channel` 层，求解器体内 `match` 通道名 0 处，新造一道 `tracer` 通道零求解器改动，四道既有通道 15 个位模式逐位不变。★**判据未放松**：位模式在改道之前采集，parity 是测出来的不是声明的。★判据括号外的「通道集自动生成」半句仍未兑现，记在兄弟记录的 caveat 里不抹掉。 |
| 3.1 | 2026-09-17 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-014` 线圈受力入内核，新门 `code/forces`）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **671 项全通过**，公开仓侧 2762 项通过。★这一批内核改动是**纯增量**（新函数、新门），没有改动任何既有路径；接口摘要因加了一行 `CASE_CODES` 而变，修订号不动。 |
| 3.2 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`NR-EQ-001` 的通量规统一入内核，ABI 154 → 155）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **671 项全通过**，公开仓侧 2783 项通过。★这一批改动会移动 `code/discharge` 的 ρ 与无 q 剖面时文档梯子的 q（见 `eq-convention-ladder-flux-gauge`）；本条的数**不在那两条路径上**，故未变。 |
| 3.3 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-013` MXH 拟合 · `NR-EQ-003` 后验协方差 · `FR-EQ-016` 补三处入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **675 项全通过**，公开仓侧 2809 项通过。★这一批内核改动是**纯增量**（新函数、既有门加字段与可选设定），接口摘要与 `CASE_CODES` 均未动。 |
| 3.4 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-017` 理想外扭曲模的 q 极限入内核——内核里第一段理想 MHD 稳定性）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **680 项全通过**，公开仓侧 2821 项通过。★这一批内核改动是**纯增量**（`stability.rs` 新增函数，没开门），接口摘要与 `CASE_CODES` 均未动。 |
| 3.5 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-018` 气球模第一稳定边界入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **685 项全通过**，公开仓侧 2828 项通过。★纯增量（`stability.rs` 新增函数，没开门），接口摘要与 `CASE_CODES` 均未动。 |

## 复算

**这次跑在**：

- 内核 `libfylite` `sha256:18d901c1abeb76aa2e70b07453d3730f1aea74d929c0e0121fd3bf9f0a0420d7`

**输入（每一项都带 sha256，否则指针指不住任何东西）**：

- `docs/benchmark/readings/bc_family_and_coupled_block.json`    `sha256:c11dfeb823da4d43155db0dfc735a52a26a2974341895e20f8d942536691c031`    边界种类、通道求解器清点、电流驱动的两条路、b0_dot 死路径实测
- `docs/benchmark/readings/channel_descriptor.json`    `sha256:72577b4e020499cfcbc4e3c5273ccfdbc40e2dce179f4834c036e8c671c573d7`    描述子那一格的实测

**守它的门**：

- `$FYLITE_KERNEL/rust/fylite/src/transport.rs::tests::the_register_numbers_for_the_b0_dot_dead_path_and_the_exchange_ledger` —— ★第四格的门（新写）。前三格是源审视，没有门
- `$FYLITE_KERNEL/rust/fylite/src/transport.rs::tests::a_new_channel_needs_no_solver_change` —— ★第一格的门（新写）——此前这一格是源审视，没有门
- `$FYLITE_KERNEL/rust/fylite/src/transport.rs::tests::the_four_channels_keep_their_numbers_bit_for_bit` —— 第一格的 parity 半边

```bash
python tools/benchmark-book.py --check   # 本页与记录同源吗
python tools/benchmark-book.py --ci      # 过期了吗、不成立吗
```
