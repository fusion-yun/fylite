---
title: "tr-conservation-time-order"
---

# θ 方法的时间收敛阶：**此前一个数都没有**

<!-- ★生成件，勿手改：`python tools/benchmark-book.py`。正本是 `records/tr-conservation-time-order.jsonld`，本页只是它的可读面。 -->

*输运 (Transport) · [守恒、金标 parity 与口径](../domains/tr/conservation.md)　|　记录正本：`records/tr-conservation-time-order.jsonld`*

## 摘要

- **类**：验证　**判决**：**成立**
- **量的是**：θ 方法的时间收敛阶：**此前一个数都没有**
- **参考**：格式**自称**的阶（θ = 1 一阶 · θ = 0.5 二阶）
- **验的需求**：`NR-TR-002`
- **跑在内核**：`sha256:bdd970709d521f7a…`（新鲜度 **current**）
- **记录版本**：1.8　**评审**：草稿　**日期**：2026-09-17

:::{warning} 这是一条**已裁定保留**的缺口

2026-09-17 ★**空间收敛阶仍未量**：Richardson 隔离掉了空间误差，那是它能干净量时间阶的理由，也是它答不了空间阶的理由。★另：金标 parity（FUSE.jl / TORAX）那一半缺的是外部码在同一算例上的输出，属语料。★门在内核仓，本仓 CI 跑不到。
:::

## 问的是什么

**被量的**：θ = 1（后向欧拉）与 θ = 0.5（Crank–Nicolson）两档

**参考**：格式**自称**的阶（θ = 1 一阶 · θ = 0.5 二阶）

> ★★**参照是它对自己的声明**。一个数值格式最基本的一句话就是「我是几阶的」，而这句话在本仓与本册里**从未被量过**——全仓搜不到制造解，也没有任何 dt 的阶次测试。

**口径与适用域**：

> 构造算例：rho ∈ [0,1] 的 201 点均匀网格，V' = 1，扩散 0.05 常数，源在空间上有结构，边界给定值，从同一初态走到 t = 0.020，步数 [10, 20, 40]。★**判的是时间离散的阶**：网格固定，于是两次解之差里空间误差完全抵消。

## 判据与量到多少

:::{figure} ../figures/tr-conservation-time-order-headroom.svg
:alt: tr-conservation-time-order 的判据余量图
:width: 100%

每条判据离它的带还有多远（对数轴，1 倍即判据本身）。★**绿而窄（< 2 倍）另着色**：它与余量一千倍的判据在下表里都只是一个「成立」。
:::

| 判据 | 容差 | 取法 | 量到 | 判 |
| :--- | ---: | :--- | :--- | :--- |
| θ = 1 量到的时间阶 | 0.15 | reference_self_reported | 相邻两次解之差的比 1.994 → 阶 **0.996**（标称 1） | **成立** |
| θ = 0.5 量到的时间阶 | 0.15 | reference_self_reported | 比 4.019 → 阶 **2.007**（标称 2） | **成立** |
| ★两档 θ 的阶之差（θ 到底有没有被用上） | 0.8 | measured_band | 两档阶差 **1.011**（判据 0.8） | **成立** |
| 含时走到底与直解稳态的相对差 | 1e-09 | measured_band | 行进 4000 步（dt = 2e-3）之后与 `solve_steady` 的解最劣相对差 **1.159e-12** | **成立** |
| FUSE.jl / TORAX 的模块级对拍 | — | reference_self_reported | 仓里没有 FUSE.jl 或 TORAX 的模块级参考件。★TORAX 在本仓只作**数据来源**（Mavrin 表抄进 `edge_tables.rs`）与注释里的公式对照，**不是可对拍的输出** | **未评估** |

**`θ = 1 量到的时间阶`** — ★后向欧拉自称一阶。带 ±0.15：Richardson 在三档步长上的估计本身有抖动。

**`θ = 0.5 量到的时间阶`** — ★Crank–Nicolson 自称二阶。

**`★两档 θ 的阶之差（θ 到底有没有被用上）`** — ★★**防假通过的一格**：一个把 `theta` 收下却不用的实现，两档都给一阶——而它在前两格里能过掉第一格，第二格才红。这一格让「θ 没被用上」**直接显形**。

**`含时走到底与直解稳态的相对差`** — ★把两条路钉在一起。★与 `FR-TR-007` 是**不同的一对**：那条比两个**稳态**求解器，这条比**含时的极限**与稳态解。

**`FUSE.jl / TORAX 的模块级对拍`** — ★抄录的另一半。★**本册量不了**，见 finding。判据照抄立在这里，不删。

**★FUSE.jl / TORAX 对拍：**本册量不了****

- ★★**不拿「引用过 TORAX」冒充「对过 TORAX」。** 抄一张表和跑一遍对拍是两件事。★补它要的是一份外部码在同一算例上的输出，那是语料的事，不是内核的。

## 不可比的部分

- ★★**为什么用 Richardson 而不是制造解**：制造解要先把算子的系数约定读对——读错一处，量出来的就是一个**假的阶**，而它看着一样体面。Richardson 的比**与方程的具体形式无关**：p 阶格式给 `2^p`，与 D、V′、源怎么写都无关。★代价是它不验**空间**阶，那一半仍空着。
- ★★**过程中踩到一个坑，记下来免得下一个人再踩**：第一版扩散取 0.5，问题在窗口末已接近稳态，于是 Richardson 量到的是**暂态的衰减**而不是截断误差——阶「量」成了 0.977 / **2.843**。CN 那个数比标称的 2 还好，**看着像好消息，其实是测量塌了**。把扩散调到 0.05 让暂态在窗口里活着，立刻回到 0.996 / 2.007。★**一个在趋稳问题上做的收敛阶研究，量的是问题不是格式。**
- ★本条只答 `NR-TR-002` 的**收敛阶**那一半；金标 parity 那一半仍空着，见第五格。

## 追溯

- 首次入册 2026-09-17　末次修订 2026-09-18　版本 1.8　评审 草稿

**变更史**（★改判本身留在册里，不覆盖旧结论）：

| 版本 | 日期 | 谁 | 做了什么 |
| :--- | :--- | :--- | :--- |
| 1.0 | 2026-09-17 | Claude Opus 5 (1M context) | 首次入册：**此前一个数都没有**——全仓无制造解、无 dt 阶次测试，「θ 方法是几阶的」从未被量过。Richardson 量到 θ=1 阶 0.996、θ=0.5 阶 2.007，两档阶差 1.011（证明 θ 真被用上）；含时走到底落在稳态解上，相对差 1.2e-12。★记下一个坑：趋稳的问题会让 Richardson 量到暂态衰减而非截断误差 |
| 1.1 | 2026-09-17 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-TR-001` 通道描述子入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **667 项全通过**，fylite 侧 2562 项通过。★fylite 侧另有 37 项失败，**逐项核过与本册无关**：30 项是这台检出没建 `rust/fy` 可执行，4 项是本次一并重生成的生成件，3 项（`psi_points` 无参数面等）在本次改动**之前**就是红的。 |
| 1.2 | 2026-09-17 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-014` 线圈受力入内核，新门 `code/forces`）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **671 项全通过**，公开仓侧 2762 项通过。★这一批内核改动是**纯增量**（新函数、新门），没有改动任何既有路径；接口摘要因加了一行 `CASE_CODES` 而变，修订号不动。 |
| 1.3 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`NR-EQ-001` 的通量规统一入内核，ABI 154 → 155）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **671 项全通过**，公开仓侧 2783 项通过。★这一批改动会移动 `code/discharge` 的 ρ 与无 q 剖面时文档梯子的 q（见 `eq-convention-ladder-flux-gauge`）；本条的数**不在那两条路径上**，故未变。 |
| 1.4 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-013` MXH 拟合 · `NR-EQ-003` 后验协方差 · `FR-EQ-016` 补三处入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **675 项全通过**，公开仓侧 2809 项通过。★这一批内核改动是**纯增量**（新函数、既有门加字段与可选设定），接口摘要与 `CASE_CODES` 均未动。 |
| 1.5 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-017` 理想外扭曲模的 q 极限入内核——内核里第一段理想 MHD 稳定性）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **680 项全通过**，公开仓侧 2821 项通过。★这一批内核改动是**纯增量**（`stability.rs` 新增函数，没开门），接口摘要与 `CASE_CODES` 均未动。 |
| 1.6 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-018` 气球模第一稳定边界入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **685 项全通过**，公开仓侧 2828 项通过。★纯增量（`stability.rs` 新增函数，没开门），接口摘要与 `CASE_CODES` 均未动。 |
| 1.7 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-021` 表面电流模型 β 极限、`FR-EQ-025` 共形映射入内核；并救回五条失声的内核门）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **709 项全通过**（新锚 19 条 + 救回 5 条）。★纯增量（`stability.rs` 新增函数、新模块 `conformal.rs`，没开门），接口摘要与 `CASE_CODES` 均未动。 |
| 1.8 | 2026-09-18 | Claude Opus 5 (1M context) | 内核换代后的全册重验（L2 本体入内核：`variational.rs` · `fluid.rs` · `vacuum.rs` · `highbeta.rs`，`FR-EQ-019/020/022/023/024`）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：内核侧 **753 项全通过**（新锚 44 条）。★纯增量（新模块与 `stability.rs` 新增 `surface_current_wall_factor`，没开门），接口摘要与 `CASE_CODES` 均未动。 |

## 复算

**这次跑在**：

- 内核 `libfylite` `sha256:bdd970709d521f7a0722b9776e188d440a155cf86e567aec5e253f62c1420f45`

**输入（每一项都带 sha256，否则指针指不住任何东西）**：

- `docs/benchmark/readings/time_convergence_order.json`    `sha256:31c07bb993b433975e90c0b88b03dc522d06b2ffbb957478cd4ea0dff55eb301`    两档 θ 的差值比与阶、趋稳陷阱的前后对照、行进到底与稳态解的差

**守它的门**：

- `$FYLITE_KERNEL/rust/fylite/src/transport.rs::tests::the_theta_scheme_converges_at_the_order_it_claims` —— ★前三格的门（新写）
- `$FYLITE_KERNEL/rust/fylite/src/transport.rs::tests::a_long_march_lands_on_the_steady_solve` —— ★第四格的门（新写）

```bash
python tools/benchmark-book.py --check   # 本页与记录同源吗
python tools/benchmark-book.py --ci      # 过期了吗、不成立吗
```
