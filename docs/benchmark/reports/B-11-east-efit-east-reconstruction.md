---
title: B-11 · EAST #137985 @ 4.041 s 的平衡反演（efit_east）：条件化对 EFIT 的交付答案
---

# B-11 · EAST #137985 @ 4.041 s 的平衡反演（efit_east）：条件化对 EFIT 的交付答案

| | |
| :--- | :--- |
| **类** | **B 对拍** |
| **参考** | EFIT (ASIPP EAST, efit_east MDSplus tree) · 树内版本号未读 [TBD]；#137985 `\TIME[28]` = 4.041 s 的交付答案，逐值转录于 FYDOC-CASE-23 平衡件（写入日期未读） · experiment（内网来源，不可自公开渠道重取；只收指针与 sha256） |
| **对象** | fylite: `code/reconstruction` 门（`fy run` 计划文档；内核 GS 反演），装置经运行时按炮号与测量链解析 |
| **算例** | `scenario/east-137985-efit-east`（EAST #137985 @ 4.041 s：efit_east 树的一片，磁测量的平衡反演） |
| **数据** | 见 §5 表（3 项，纳入类别 experiment、private-artefact） |
| **门** | `$FYLITE_KERNEL/tools/benchmark-east-efit-east.py` |
| **登记册结论** | 成立（`assertion_state: accepted`） |
| **复测** | 2026-09-14：未评估——0 passed, 0 failed, 0 error, 0 skipped, 0 stale |

> 本页由 `tools/benchmark-publish.py` 从内核仓登记册渲染；判据与量到的数是登记册的，「复测」一行是发布当日在私仓检出上把门跑一遍的结果，两者分开记。

## 1. 判据

| 量 | 范数 | 容差 | 容差来源 | 备注 |
| :--- | :--- | ---: | :--- | :--- |
| 磁轴位置 (R, Z)，对 EFIT 的 RMAXIS / ZMAXIS 的绝对差（两分量各判） | absolute | 0.00651 | measured_band | 实测 dR +4.53 mm · dZ +0.54 mm；盆地 dR +3.60 … +6.51 · dZ -4.32 … +2.93 mm；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★Z 由条件 3（χ² 定的竖直设定点）决定：dZ 读的是设定点选得准不准，不是反演独立定出的竖直位置 |
| q₀（轴上），对 EFIT 的 Q0 | relative | 0.0779 | measured_band | 实测 1.9378 对 EFIT 1.8055（+7.32 %）；盆地 1.8995 … 1.9462（+5.21 … +7.79 %）；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★基 npp=1 / nff=2 取自 EFIT 自己的 PPRIME（线性）与 FFPRIM（二次），两者边缘为零；芯部 q 在磁测量下约束弱，这条是钉住不是验收 |
| q95，对 EFIT 的 Q95 | relative | 0.0768 | measured_band | 实测 6.5767 对 EFIT 7.0320（-6.47 %）；盆地 6.4922 … 6.6311（-7.68 … -5.70 %）；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band` |
| 边界：EFIT 的 108 个边界点到本仓边界折线的距离，中位数 | absolute | 0.00624 | measured_band | 实测 5.04 mm；盆地 4.99 … 6.23 mm；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★度规写明：点到**折线段**的距离。点到**顶点**（本仓边界 121 点）的读法给 12.5 mm——那是顶点间距，不是边界差；H-37b 诊断时记的 12.4 mm 是后一种 |
| 边界：同上距离的最大值（**钉住**，不是验收线） | absolute | 0.147 | measured_band | 实测 130.9 mm，位于 EFIT 边界点 (R, Z) = (1.5506, +0.7661) m——即 EFIT 自己 ψ 图的**上 X 点**（鞍点距该边界点 0.0003 mm）；盆地 123.3 … 146.6 mm；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★★EFIT 的边界是在上 X 点闭合的偏滤器位形（两个鞍点 (1.558, -0.772) / (1.551, +0.766) m）；本仓在上 X 点处离开 131 mm，而 90 % 的边界点在 39.7 mm 以内。这个最大值随设定点单调变化（−30 mm 时 170 mm，−12 mm 时 96 mm），带只把今天的读数钉住 |
| 探针残差，以每道自己的 σ（交付权重 FWTMP2 = 1/σ）为单位，29 道在用的 rms | rms | 0.361 | measured_band | 实测 0.336 σ；盆地 0.336 … 0.360 σ；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band` |
| 磁通环残差，以每道自己的 σ（交付权重 FWTSI = 1/σ）为单位，34 道在用的 rms；**模型值加线圈份额** | rms | 0.0954 | measured_band | 实测 0.0893 σ；盆地 0.0893 … 0.0953 σ；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★门的 `loop_model` 只含等离子体：线圈份额由 `code/coilshare` 以同一组线圈电流（CCBRSP[0:12]）、同一份几何另算后加上，再与 SILOPT 比 |
| 测量 χ²：Σ(FWTMP2·(模型−EXPMPI))² + Σ(FWTSI·(模型+线圈份额−SILOPT))² | absolute | 4.08 | measured_band | 实测 3.547（探针 3.276 + 环 0.271）；盆地 3.547 … 4.071；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★★度规先自证：同一公式作用在 EFIT 自己的计算通道（CMPR2 / CSILOP）上得 0.8873，树里 EFIT 自报 CHISQ = 0.8879（差 -0.06 %）⇒ 两侧 χ² 可比。本仓是 EFIT 的 4.0 倍；★设定点正是按这个数的极小选的（条件 3），所以本条在设定点上**不是独立判据**：它钉住的是极小值本身 |
| ψ 图（EFIT 129×129 全网格，本仓 65×65 双线性插值到其格点），换算单位并去掉规范常数后的逐点残差 rms，对全场跨度归一 | rms | 0.00461 | measured_band | 实测 0.40 %（最大 0.81 %，全场跨度 2.941 Wb，规范常数 +3.19 mWb）；盆地 0.38 … 0.46 %；带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`；★★口径：EFIT 树的 ψ 是每弧度、轴为极小；本仓写出的是整圈 Wb。因子由数据读出——边界内对树值的自由最小二乘斜率 -6.1681，对 −2π = −6.2832（-1.8 %）——然后施加**固定的** −2π（= −1 × 平衡件 converted 块的整圈形式）；COCOS 17 的符号一步仍 [TBD]（树未声明 σ_RφZ），本条只用到号由数据自证；边界内单列（对 \|ψ_b − ψ_a\| = 0.713 Wb 归一）：rms 1.17 %、最大 2.35 % |

## 2. 口径对齐与不可比的部分

四行表（解了哪几道方程 / 哪些量是喂进去的 / 单位与径向标签 / COCOS）的完整账在该组算例书随件的 README（`$FYLITE_KERNEL/tests/data/FYDOC-CASE-23-east-137985-efit-east/FYDOC-CASE-23-east-137985-efit-east.md`）；下面是登记册随本条记录携带的口径说明，逐条照录：

- ★★★**条件化记录（用户裁定 2026-09-14）**：本条接替撤回的 B-06，但**不是**「本仓独立反演对上 EFIT」。三项设定取自参考自己的配置——① EFIT 拟合出的线圈电流 CCBRSP[0:12]；② EFIT 的剖面基 npp=1 / nff=2；③ 以本仓自己的测量 χ² 极小选定的竖直设定点（代 EFIT 的 `fitdelz`）。条件全文在 `validity_domain`。去掉任一条件，本条的数都不适用
- ★★**带是钉子不是验收线**：每条容差都由本次运行及其设定点盆地定出（盆地内最劣值），所以立条当天「成立」是构造使然；它们的用处是抓漂移。规则见各判据注，规范未给公式，待裁定
- ★★**边界最大值在上 X 点**：131 mm，而中位 5.0 mm、九成点在 40 mm 内——X 点附近的边界由鞍点定位决定，不并进中位那条带
- ★★**χ² 是 EFIT 的约 4 倍**（3.55 对同度规 0.887）：同一份测量、同一组权重、同一份线圈，本仓离数据仍比参考远，主要在探针（3.28 对环 0.27）
- ★★**不给条件的独立变体待内核工作**：实测线圈电流（FCCURT）下磁轴差数厘米、q₀ 差三成（见 findings）。需要装配门支持刚性竖直平移或线圈拟合之一，届时另立判据；本条不判它
- ★**内感不比**：两侧 li 定义不同，未对齐 [TBD]。**压强不比**：门的压强号另有修正在进行，本条不依赖它
- ★**不能读作「本仓的反演对得上真实的 EAST」**：参考是另一个码的一次运行；两码一致只说明在同一份外部约束上同意
- ★算例 FYDOC-CASE-23 的 review 仍是 pending（reviewer [TBD]），发布级别 internal；公开渲染只收比较度规与指针，不收读数
- （判据）实测 dR +4.53 mm · dZ +0.54 mm；盆地 dR +3.60 … +6.51 · dZ -4.32 … +2.93 mm
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★Z 由条件 3（χ² 定的竖直设定点）决定：dZ 读的是设定点选得准不准，不是反演独立定出的竖直位置
- （判据）实测 1.9378 对 EFIT 1.8055（+7.32 %）；盆地 1.8995 … 1.9462（+5.21 … +7.79 %）
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★基 npp=1 / nff=2 取自 EFIT 自己的 PPRIME（线性）与 FFPRIM（二次），两者边缘为零；芯部 q 在磁测量下约束弱，这条是钉住不是验收
- （判据）实测 6.5767 对 EFIT 7.0320（-6.47 %）；盆地 6.4922 … 6.6311（-7.68 … -5.70 %）
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）实测 5.04 mm；盆地 4.99 … 6.23 mm
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★度规写明：点到**折线段**的距离。点到**顶点**（本仓边界 121 点）的读法给 12.5 mm——那是顶点间距，不是边界差；H-37b 诊断时记的 12.4 mm 是后一种
- （判据）实测 130.9 mm，位于 EFIT 边界点 (R, Z) = (1.5506, +0.7661) m——即 EFIT 自己 ψ 图的**上 X 点**（鞍点距该边界点 0.0003 mm）；盆地 123.3 … 146.6 mm
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★★EFIT 的边界是在上 X 点闭合的偏滤器位形（两个鞍点 (1.558, -0.772) / (1.551, +0.766) m）；本仓在上 X 点处离开 131 mm，而 90 % 的边界点在 39.7 mm 以内。这个最大值随设定点单调变化（−30 mm 时 170 mm，−12 mm 时 96 mm），带只把今天的读数钉住
- （判据）实测 0.336 σ；盆地 0.336 … 0.360 σ
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）实测 0.0893 σ；盆地 0.0893 … 0.0953 σ
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★门的 `loop_model` 只含等离子体：线圈份额由 `code/coilshare` 以同一组线圈电流（CCBRSP[0:12]）、同一份几何另算后加上，再与 SILOPT 比
- （判据）实测 3.547（探针 3.276 + 环 0.271）；盆地 3.547 … 4.071
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★★度规先自证：同一公式作用在 EFIT 自己的计算通道（CMPR2 / CSILOP）上得 0.8873，树里 EFIT 自报 CHISQ = 0.8879（差 -0.06 %）⇒ 两侧 χ² 可比。本仓是 EFIT 的 4.0 倍
- （判据）★设定点正是按这个数的极小选的（条件 3），所以本条在设定点上**不是独立判据**：它钉住的是极小值本身
- （判据）实测 0.40 %（最大 0.81 %，全场跨度 2.941 Wb，规范常数 +3.19 mWb）；盆地 0.38 … 0.46 %
- （判据）带 = **盆地内最劣值**，三位有效数字向上取：盆地是 `zc_anchor` 扫描（−30 … −12 mm，步长 2 mm）中 χ² ≤ 4.1 的设定点（-24 … -18 mm，4 个）；量到的数本身在盆地里，故带 = 实测结果 + 盆地的散布。★取法已裁定（用户 2026-09-14）：B 类实测带一律取盆地内最劣值、三位有效数字向上取，见 `docs/note/benchmark/vocabulary.md` 的 `measured_band`
- （判据）★★口径：EFIT 树的 ψ 是每弧度、轴为极小；本仓写出的是整圈 Wb。因子由数据读出——边界内对树值的自由最小二乘斜率 -6.1681，对 −2π = −6.2832（-1.8 %）——然后施加**固定的** −2π（= −1 × 平衡件 converted 块的整圈形式）；COCOS 17 的符号一步仍 [TBD]（树未声明 σ_RφZ），本条只用到号由数据自证
- （判据）边界内单列（对 |ψ_b − ψ_a| = 0.713 Wb 归一）：rms 1.17 %、最大 2.35 %
- （场景）★★**参考侧拟合了线圈，被比的一侧在装配门里拟合不了。** EFIT 在这一片上把线圈电流当自由量拟合（交付的拟合值与测量值不同）；一个把线圈电流当输入的反演，若喂测量值，差的是线圈而不是等离子体。任何在本场景上不给出线圈电流取法的比较都读不了。
- （场景）★★**竖直位置由参考自己的一步定住。** EFIT 的竖直位移拟合（`fitdelz`）没有同名的对应物；被比的一侧须说明它的竖直设定点从哪来，否则磁轴 Z 的差量的是设定点。
- （场景）★**权重即 1/σ**：树里交付的 FWTMP2 / FWTSI 就是拟合用的权重（探针 29 道 > 0、全在前 38 槽；环 34 道 > 0）。两侧用同一组权重时，χ² 可以跨码比较——前提是先用参考自己的计算通道复算出它自报的 CHISQ。
- （场景）★**ψ 的单位与号**：树里是每弧度、轴为极小；整圈形式是乘 2π。比较 ψ 图前须显式换算并写明因子与规范常数；COCOS 17 的符号一步待定（树未声明 φ 的手性）。
- （场景）★**内感不可比**：两侧 li 的定义不同，未对齐之前不作判据。
- （场景）★本场景不载**仪器原始采集**：测量集是参考码的输入通道，其预处理（偏置、标定、选道）已在其中。读数来自内网，公开渠道不可重取，仅以指针与 sha256 收录。

## 3. 结果（登记册所记）

| 项 | 偏差 | 种类 | 判 | 备注 |
| :--- | ---: | :--- | :--- | :--- |
| 条件化反演（设定点 -20 mm）· 磁轴 | dR +4.53 mm · dZ +0.54 mm（带 6.51 mm） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· q₀ | 1.9378 对 1.8055：+7.32 %（带 7.79 %） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· q95 | 6.5767 对 7.0320：-6.47 %（带 7.68 %） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· 边界中位 | 5.04 mm（带 6.24 mm） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· 边界最大（上 X 点） | 130.9 mm，在 EFIT 的上 X 点（带 147 mm） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· 探针 σ | 0.336 σ（带 0.361） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· 环 σ（含线圈份额） | 0.0893 σ（带 0.0954） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· χ² | 3.547，EFIT 同度规 0.8873（4.0×；带 4.08） |  | 成立 |  |
| 条件化反演（设定点 -20 mm）· ψ 图 | rms 0.40 % · 最大 0.81 %（带 0.46 %） |  | 成立 |  |
| 竖直设定点扫描与盆地（条件 3 的出处） | -30 mm χ² 6.94 · -28 mm χ² 5.72 · -26 mm χ² 4.76 · -24 mm χ² 4.07 · -22 mm χ² 3.67 · -20 mm χ² 3.55 · -18 mm χ² 3.71 · -16 mm χ² 4.13 · -14 mm χ² 4.84 · -12 mm χ² 5.80；极小在 -20 mm；盆地（χ² ≤ 4.1）-24 … -18 mm | scan | 成立 | 十个设定点全部收敛（346–357 次迭代，残差 ≤ tol 1e-8，见运行日志）；盆地内 dZ -4.3 … +2.9 mm、dR +3.6 … +6.5 mm、q₀ 1.900 … 1.946、q95 6.492 … 6.631；★★与 H-37b 诊断的一处出入：诊断把盆地记作 −24 … −16 mm，但 −16 mm 的 χ² 是 4.128（诊断自己的扫描也是 4.125）> 4.1——按阈值严格读，盆地是 -24 … -18 mm。若把 −16 mm 算进去，唯一变动的带是 χ²（4.08 → 4.13）；dZ 上界变 +5.27 mm 仍在磁轴带内；★阈值 4.1 是诊断时写下的，本条照用、未调；它不是规范给的数 |
| 度规自证：χ² 公式在 EFIT 自己的计算通道上复现它自报的 CHISQ | 0.8873 对 CHISQ 0.8879（-0.06 %） | metric-check | 成立 | ★这一条使「本仓 χ² 是 EFIT 的约 4 倍」成为同一把尺子上的读数，而不是两套权重之比 |
| 不给条件的变体：实测线圈电流（FCCURT）· 同基 · 同设定点 | dR +26.0 mm · dZ -4.0 mm · q₀ +33.2 % · q95 -8.3 % · 探针 0.528 σ · 环 0.142 σ · χ² 8.78 | variant (not judged; waiting on kernel work) | 未评估 | ★★这是本条**不判**的独立变体：EFIT 在这一片上自由拟合了线圈电流，装配门做不到（无刚性竖直平移、无线圈拟合），喂实测线圈时差的主要是线圈。待内核补上其中一样后另立判据；★H-37b 诊断记的是 R +21 mm、q₀ +39 %（不同设定点）；本条在极小设定点上重跑得上列数，量级一致 |
| 复现：自 CASE-23 文档重跑 H-37b 诊断 | 磁轴 +4.53 / +0.54 mm（诊断 +4.5 / +0.5）· q₀ 1.938（1.937）· q95 6.577（6.577）· 边界最大 131 mm（131）· 探针 / 环 0.34 / 0.09 σ（0.34 / 0.09）· χ² 3.55（3.54） | reproduction | 成立 | 输入自算例文档读：测量集给 SILOPT · FWTSI · EXPMPI · FWTMP2 · PLASMA · FPOL(ψ_N=1)，平衡件 efit_east_tree 块的 computed_signals.CCBRSP 给线圈；诊断用的是 npz 证据副本与一份手工几何变体；★几何：本条走运行时解析 `facts:device/east?shot=137985&measurement_chain=efit_east`（提供者 efit_green2022_pcs）；诊断用的变体卡与之在探针槽 0–37 与 35 环上相差 ≤ 50 µm（取整），槽 38–75 不同但权重为零。剩下的 0.05 mm / 0.03 % 级差别即此；★同一输入重跑两次，结果逐位相同；fy sha256 d55c3fe2485fc58a · 内核库 sha256 9efffc0967b27254 |
| 求解器自己的判定与竖直反馈 | converged，357 次迭代，残差 5.47e-09（tol 1e-8）；竖直反馈幅值 `fb_amp` -20.1 kA · 截断保留 3 | solver | 成立 | ★`fb_amp` 不为零：竖直锚点仍以反馈按住质心（B-06 已记这份力是承重的）；它随设定点变化，不作判据 |

## 4. 复测（2026-09-14）

| 门 | 计数 | 首条信息 |
| :--- | :--- | :--- |
| $FYLITE_KERNEL/tools/benchmark-east-efit-east.py | 未执行 |  |

结论：**未评估**（`re-run: gate not executed`）。

## 5. 数据与怎么重跑

| 存储项 | 校验 | 纳入类别 | 规模 |
| :--- | :--- | :--- | :--- |
| $FYLITE_KERNEL/tests/data/FYDOC-CASE-23-east-137985-efit-east/corpus/measurement_east137985_4041ms.fyo.jsonld | sha256:cb39ff0cde30c9cdd1a993ee0982ec2657084e1f76fc04ec0cdf5fef6648d478 | experiment | 13413 B |
| $FYLITE_KERNEL/tests/data/FYDOC-CASE-23-east-137985-efit-east/corpus/equilibrium_east137985_4041ms.fyo.jsonld | sha256:a24846602043b827407fd1ac71302e5d02171ec9e0f8c903d9c3c340c9c8e54c | experiment | 952329 B |
| $FYLITE_KERNEL/facts/device/east/abox/providers/magnetics/efit_green2022_pcs.jsonld | sha256:fb2744b3eedb939defe1a2c6e1aa7f336ed2961758b74f0ddb84e47db65e5010 | private-artefact | 26738 B |

参考侧：按上表的出处取得同一份（受限类别的项读者须自备）。语料**随内核仓检出**（`$FYLITE_KERNEL/tests/data/`）。
本仓侧：门在 `$FYLITE_KERNEL`（私仓）中运行——

```bash
cd $FYLITE_KERNEL   # 语料已在检出里，无需挂载
PYTHONPATH=$FYLITE_PUBLIC/python FYLITE_KERNEL_LIB=rust/fylite/target/release/libfylite_kernel.so \
  uv run --no-project --with pytest --with numpy --with scipy --with h5py \
  python tools/benchmark-east-efit-east.py --out <scratch dir> --check
```

## 6. 结论

登记册：成立。复测 2026-09-14：未评估。只回答本条自己那一类（B 对拍）的问题，不外推。
