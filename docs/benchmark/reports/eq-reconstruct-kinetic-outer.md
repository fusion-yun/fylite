---
title: "eq-reconstruct-kinetic-outer"
---

# 动理学自洽外环：把**故意给错的映射**交给它，看它拉不拉得回来

<!-- ★生成件，勿手改：`python tools/benchmark-book.py`。正本是 `records/eq-reconstruct-kinetic-outer.jsonld`，本页只是它的可读面。 -->

*平衡 (Equilibrium) · [测量重构与约束阶梯](../domains/eq/reconstruct.md)　|　记录正本：`records/eq-reconstruct-kinetic-outer.jsonld`*

## 摘要

- **类**：验证　**判决**：**成立**
- **量的是**：动理学自洽外环：把**故意给错的映射**交给它，看它拉不拉得回来
- **参考**：同一次孪生的**已知真映射**，以及单遍（kinetic_passes = 1）的结果
- **验的需求**：`FR-EQ-010`
- **跑在内核**：`sha256:eb8c9022c405fc1a…`（新鲜度 **current**）
- **记录版本**：1.1　**评审**：草稿　**日期**：2026-09-17

## 问的是什么

**被量的**：逐遍把动理学测点 (R, Z) 重映到 psi_N，再解一次

**参考**：同一次孪生的**已知真映射**，以及单遍（kinetic_passes = 1）的结果

> ★★**这是本册里少有的一条「真值已知」的重构判据**：动理学测点造在真值 psi 图的中平面上，所以「它们该落在哪个 psi_N」是算出来的、不是猜的。于是可以**故意给错**，再看外环拉不拉得回来。

**口径与适用域**：

> EAST #137985 t = 4.041 s 的孪生。动理学测点取真值磁轴同高度的中平面上 9 个点（R = 1.742 .. 2.142 m），读数按真值压强剖面给，sigma 取峰值的 5 %。★初始 psi_N 标签**故意整体推错 +0.12**。外环上限 6 遍、`kinetic_tol` = 1e-4。★**测点是造出来的，不是测出来的**——本条验的是外环这台机器，不是某台装置的汤姆逊系统。

## 判据与量到多少

:::{figure} ../figures/eq-reconstruct-kinetic-outer-headroom.svg
:alt: eq-reconstruct-kinetic-outer 的判据余量图
:width: 100%

每条判据离它的带还有多远（对数轴，1 倍即判据本身）。★**绿而窄（< 2 倍）另着色**：它与余量一千倍的判据在下表里都只是一个「成立」。
:::

| 判据 | 容差 | 取法 | 量到 | 判 |
| :--- | ---: | :--- | :--- | :--- |
| 外环收官时 q0 相对真值的偏差 | 0.005 | reference_self_reported | 把每个测点的 psi_N 标签**故意推错 +0.12**，单遍给出 q0 偏差 +7.116e-02，六遍外环给出 -7.030e-04（带 0.5 %） | **成立** |
| 收官那一遍的映射移动（psi_N） | 0.005 | reference_self_reported | 逐遍移动 0.2345 → 0.1505 → 0.0390 → 0.0113 → 0.0031 → 0.0008 —— 从 0.2345 单调降到 8.48e-04 | **成立** |
| 外环相对单遍的改善倍数（q0 偏差之比） | 5 | measured_band | q0 偏差改善 101 倍（7.116e-02 → -7.030e-04）；动理学行的 chi2 从 8.3611 到 0.0081007（1032 倍）；整体 chi2/dof 从 0.81034 到 0.0052682（154 倍） | **成立** |
| ★证书是否逐遍出（chi2/dof 与映射移动两条迹） | — | reference_self_reported | 逐遍 chi2/dof 0.81034 → 0.11172 → 0.012872 → 0.0060604 → 0.0052682 → 0.0052892；收官于第 5 遍（共 6 遍） | **成立** |

**`外环收官时 q0 相对真值的偏差`** — ★带取 0.5 %：映射给错 0.12 时单遍的 q0 偏差是 7.1 %，而本域另一条（可观测空间）在映射**没给错**时量到的 q0 偏差是 0.12 %。0.5 % 是「外环该把大部分伤害消掉」的一个有原则的界，不是贴在 0.070 % 上。

**`收官那一遍的映射移动（psi_N）`** — ★★**这一格判的是「自洽」二字本身**：映射还在走，就说明剖面挂的面还不是解出来的那个。带取 0.005，约是初始错量 0.12 的 1/20。

**`外环相对单遍的改善倍数（q0 偏差之比）`** — ★★**下限格，也是本条的要害**。一个总能收敛的循环说明不了什么——要紧的是它**真的修好了东西**。没有这一格，「跑了六遍」只是一句话。

**`★证书是否逐遍出（chi2/dof 与映射移动两条迹）`** — ★抄录原文：「`kinetic_outer` 证书含逐遍 chi2/dof 与映射移动；最优遍收官」。**证书才是交付物，不是循环。**

**映射settle下来了**

- ★★**单遍那一次也报了它自己的映射移动（0.2345）**——也就是说，一个不跑外环的调用方也会被告知「你这个解不自洽到这个程度」。★这是证书最实用的一面：**它不需要你跑外环才肯说话。**

**★★证书逐遍出，而且「取最优遍」当场就用上了**

- ★★**第 6 遍比第 5 遍差（0.00528919 对 0.00526821），于是它收官在第 5 遍。**「取最优遍不取最后一遍」这条规则是照着「交替不保证单调」写的防御，而它**在第一次真实测试里就触发了**——不是纸上的谨慎。门也把这件事写进了 notes。
- ★`kinetic_map_shift` 报的是**收官那一遍**的移动，不是最后一遍的：读者要判的是手上这个解自洽到什么程度；整条迹在 `kinetic_pass_map_shift` 字段里。

## 不可比的部分

- ★★**为什么能这样验**：真值平衡自己带压强剖面，测点造在它的 psi 图上，于是「该落在哪个 psi_N」是**算得出来的**。本册里大多数重构判据没有这个便利——★这也提醒：本条证明的是**外环能纠正映射误差**，不是「重构准」。后者是别的记录的事。
- ★**它不保证单调**，记录里那条 chi2/dof 迹自己就是证据（第 6 遍回升）。所以「最优遍收官」不是可有可无的修饰。
- ★真值与测量同出 fylite 自己的前向解，所以与本域另两条一样，它只能声称「反演与前向互为逆」那一层。

## 追溯

- 首次入册 2026-09-17　末次修订 2026-09-17　版本 1.1　评审 草稿

**变更史**（★改判本身留在册里，不覆盖旧结论）：

| 版本 | 日期 | 谁 | 做了什么 |
| :--- | :--- | :--- | :--- |
| 1.0 | 2026-09-17 | Claude Opus 5 (1M context) | 首次入册：FR-EQ-010 由内核实现（2026-09-17）。错映射 +0.12 下 q0 偏差 7.12e-02 → -7.03e-04（101 倍），映射移动降到 3.1e-03；★「取最优遍收官」在第一次真实测试里就触发（第 6 遍劣于第 5 遍） |
| 1.1 | 2026-09-17 | Claude Opus 5 (1M context) | 内核换代后的全册重验（`FR-EQ-002/008/010/011` 与 0D 三项入内核）。★本条的判据与数值**未改口径**；重验的证据是门禁在新内核上跑过：fylite 侧 110 道、内核侧 656 项全通过。 |

## 复算

**这次跑在**：

- 内核 `libfylite` `sha256:eb8c9022c405fc1a788efc99cd13a74d972d375df2b7858e07e28ccd2ac47a69`

**输入（每一项都带 sha256，否则指针指不住任何东西）**：

- `docs/benchmark/readings/kinetic_outer_twin.json`    `sha256:e989f3fe8032a85e70033bf1d2f20383e2168b481e2deac72328937923b1d16b`    测点、真映射、故意给错的初始标签，以及 1 遍 / 6 遍两次的逐遍证书

**守它的门**：

- `python/tests/test_benchmark_equilibrium.py::test_fr_eq_010_the_kinetic_outer_loop_recovers_a_wrong_mapping` —— ★本条四格的门

```bash
python tools/benchmark-book.py --check   # 本页与记录同源吗
python tools/benchmark-book.py --ci      # 过期了吗、不成立吗
```
