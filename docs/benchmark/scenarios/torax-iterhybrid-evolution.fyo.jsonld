{
  "@context": [
    "../context.jsonld",
    {
      "@base": "../"
    }
  ],
  "id": "scenario/torax-iterhybrid-evolution",
  "type": "fyo:ScenarioSpecification",
  "title": "ITER 混合运行情景，五秒演化（TORAX `test_iterhybrid_predictor_corrector`）",
  "abstract": "★这是本册第一个**时间演化**场景，前面所有条目都是定点或局部：ITER 15 MA 混合情景（R = 6.2 m、a = 2.0 m、B₀ = 5.3 T、I_p = 10.5 MA、氘氚），从 t = 0 演化到 t = 5 s，25 单元径向网格、预估—校正求解器，输运由 QLKNN_7_11 代理逐步给出、剖面随之更新。它不是一次放电，是**TORAX 自带的整码回归算例**（`torax/tests/test_data/test_iterhybrid_predictor_corrector.py`），配置在上游仓里公开可取，物理设置追 van Mulders, Nucl. Fusion 61 (2021) 086019 的 ITER 混合情景。选它是因为它同时给出三样东西：一条真实的演化轨迹（85 次网络调用、29 个输出时刻）、两码**同一份权重**（QLKNN_7_11）因而下游可判到机器精度、以及一个**完整写明的输运装配**——不是裸闭包输出。",
  "caveat": [
    "★★**算例的输运装配是场景的一部分，不是背景**。该配置在 ρ < 0.15 处方 χ = 1.0（inner patch）、QLKNN 只在 0.15 < ρ ≤ 0.95 上跑、outer patch 因台基掩膜先切在 ρ ≥ 0.9 而**从不生效**、逐点截到 [0.05, 100]，最后对整条剖面做宽度 0.1 的高斯卷积（`transport_model.py::_build_smoothing_matrix`）。拿裸闭包输出去比它，量到的 89 % 分歧几乎全部是那次缺席的卷积——比的是两个不同的对象。",
    "★★**台基是内部边界条件**（`rho_norm_ped_top` = 0.9），所以剖面在那里有折点。ρ = 0.88 是台基顶下的最后一个面，本仓的胞→面梯度规则（中心差分后插值）会跨折点抹平，而 TORAX 直接在面上差分。端到端残差**整个落在这一个面上**，不是散布在剖面上——这是场景带来的判据而不是本仓的容差。",
    "★**网络输入里的 `smag` 不是输出文件里的 `magnetic_shear`**：是 `calc_s_rmid`（对中平面半径取的剪切，`psi_calculations.py:218`），并且随后被三项修正改写（沙氏位移 α/2、q < 1 的锯齿代理、强反剪切的下限）。混同两者是那一列上 20–30 % 的静默误差。",
    "★**ETG 修正因子在两码上缺省不同**：TORAX 缺省 1/3（`pydantic_model.py:127`），本仓缺省 1.0（网络自己的答案）。本场景按算例的取值显式传入 1/3；比较任何一侧的缺省跑法都会在电子热道上差三倍，而两边都按设计工作。",
    "★**85 次网络调用与 29 个输出时刻不是同一张表**：预估—校正一步不止调一次网络，求解器还走了从不落盘的步。按序号配对比的是不同时刻，得到的每一条差异都是时间错位假扮成建模差异。录制脚本因此把剖面**捕在同一次调用里**；ρ 上的配对靠状态逐位相等，不靠序号。",
    "★不复现的部分：TORAX 的有效 D/V 分解、输运求解器本身、台基模型、源项组合。本场景比的是闭包与输运装配，不是仿真。",
    "上游可重取（TORAX `b4d40633`，`TORAX_VERSION` 1.4.3，Apache-2.0）；权重 QLKNN_7_11 来自 `fusion_surrogates`（软件 Apache-2.0，**权重 CC-BY-4.0**），两侧读的是同一份档案，其 sha256 记在本仓 `nn_tables/qlknn_7_11.npz` 的 `source_sha256` 里。"
  ]
}