---
title: "解析判据阶梯：外扭曲模 q 极限与气球模第一稳定边界"
---

# 解析判据阶梯：外扭曲模 q 极限与气球模第一稳定边界

*MHD 稳定性 (MHD Stability) · 本域答：教科书有闭式答案的那两级：均匀电流柱的 q 带边、气球模 alpha_c(s) 曲线。★它们是本册的**尺**——尺不准，上面的都不算数。*

这一章是本册的**尺**。外扭曲模在均匀电流柱上的 $q$ 带边、气球模的第一稳定边界
$\alpha_c(s)$——这两级在教科书里有闭式答案，所以它们能被验到机器精度，而不是"对拍到
百分之几"。

★★**尺不准，上面的都不算数。** 后面两章（能量原理内核、全 $\delta W$）没有解析解可依，
它们的可信度是从这一章借来的：同一套变分机器，在有闭式解的极限下复现到 $10^{-10}$，
它在没有闭式解的地方给的数才值得看。所以这一章的记录在本册里的地位与别处不同——
它不是"又一条比较"，是**其余稳定性记录的前提**。

这一章还有一类判据是别处没有的：**拒绝求解**。共振面落在计算域内时，正确的行为不是给
一个数，是拒绝——给数就是在一个方程奇异的地方硬凑。同理，理想不稳时必须 fail-loud 而不是
返回一个看起来正常的增长率。★**"它不肯算"也是一条要被验的判据**，而且是容易被忘掉的那种。

收敛档要记明。上游的判据里写着截断阶 24 与 32 差 $<10^{-3}$，并**记明 16 不够**（偏低 3.6 %）——
把"不够的那一档"写进记录，比只写"够的那一档"有用：后者只说明选对了，前者说明为什么。

★上一册在这一域**没有记录**。L0 / L1 这一层的门在内核侧，公开册从未量过它——这是一处
真空白，不是被遗漏的条目。

<!-- BEGIN GENERATED: tools/benchmark-book.py —— 勿手改 -->

### 判据（抄自 `FYTOK-SRS-03` v0.43）

:::{note} 这一节是**抄录**，不是引用
上游 `FYTOK-SRS-03` 标着 `distribution: internal`——本册的读者打不开它。一条读者打不开的引用没有分量，所以判据原文抄在这里，逐字。

抄录件 `transcript.jsonld` 记着源的版本与 sha256；源一变，`python tools/benchmark-transcribe.py --check` 就红，逼人重抽。
:::

**`FR-EQ-017` · 理想外扭曲模的 q 极限（L0）** — MUST · 验证方法：检查 + 测试

> 均匀电流复现解析带边 $q_a=(m-1+(a/b)^{2m})/n$ 与上边 $m/n$（$10^{-10}$）· 壁越近带越窄 · 剖面越峰化带越窄 · 共振面在域内时**拒绝求解** · marginal 点不随网格漂移（$<10^{-9}$）

**`FR-EQ-018` · 气球模第一稳定边界（L1）** — MUST · 验证方法：检查 + 测试

> $\alpha=0$ 必稳（符号锚）· 边界处打靶量为零且两侧异号 · $\alpha_c(s)$ 单调增且大剪切端 $\alpha_c/s\in(0.5,0.75)$（教科书斜率）· 截断收敛（24 vs 32 差 $<10^{-3}$）且**记明 16 不够**（偏低 3.6%）· 域内无跨越时报错不外推

### 本域的记录

★**本域尚无记录。** 这一行不是排版占位，是缺口本身：上面抄录的判据，本册还没有拿出任何一条对着外部答案量过的记录来回应。

### 缺口

本域 **MUST 级空缺 2 条**——SRS 写的是「必须」，而本册没有任何记录覆盖：

- `FR-EQ-017` 理想外扭曲模的 q 极限（L0）
- `FR-EQ-018` 气球模第一稳定边界（L1）

<!-- END GENERATED -->
