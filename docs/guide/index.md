---
title: fylite 用户指南 (fylite User Guide)
---

# fylite 用户指南

fylite 是一个**自足的托卡马克平衡—输运—湍流内核**：一套 Grad-Shafranov 正逆解、
一步 1.5 维芯部输运、新经典与回旋朗道流体闭包，以及一条平衡重构链，写成**一个
可重入的 Rust 内核**；Python 层做装配与编排，浏览器端跑的是同一个内核编译出的
WebAssembly。

本书讲**怎么用**与**结果怎么读**。三种发布形态互不依赖，装哪一种取决于你手边有什么：

:::{list-table}
:header-rows: 1

* - 形态
  - 要装什么
  - 适合
* - [浏览器站点](browser.md)
  - 什么都不用装
  - 上手、教学、快速试算、把一次运行分享给别人
* - [单一可执行文件](browser.md) `fylite-app`
  - 一个文件，双击即用；不装任何运行时
  - 离线、没有 Python 的机器（尤其 Windows）、要从 MDSplus 取数
* - [Python 包](python.md)
  - `pip install fylite`（alpha 期仅 Linux x86-64）
  - 脚本化、批量、把结果接进自己的分析流程
:::

三者装的是**同一份页面**与**同一版内核制品**，命令行也由同一个定义文件建出——
所以三条路上同名的命令是同一条命令（见[安装与环境](install.md)的分发面一节）。

两条路径调用的是**同一个内核**：同一份 `c_api` 导出面，原生库与 WebAssembly
模块出自同一次编译配置。所以浏览器里得到的数与脚本里得到的数是同一个数——
这一点由自动门在每次改动时校验，不靠承诺。

## 典型算例：一族一章，逐条可跑

仓里 `cases/` 下有 25 条**算例计划**。下面五章各挑一族讲透——怎么跑、算出了什么、
每个数该怎么读、这一族**回答不了**什么。每章的命令与数字都是在本仓实测的。

:::{list-table}
:header-rows: 1

* - 章
  - 算例
  - 问的是
* - [算例语料](cases.md)
  - —
  - 计划 / 记录 / 呈现规格三份文档，四个动词，以及被拒绝的算例为什么被拒
* - [0-D 放电](example-zerod.md)
  - `zerod-iter-15ma`
  - 一发放电的功率平衡与时间轨迹（规定剖面 / 预测两档）
* - [1.5-D 芯部输运](example-transport.md)
  - `transport-iter-15ma`
  - 给定度规与 χ，定态剖面长什么样
* - [含时演化](example-evolve.md)
  - `evolve-iter-15ma`
  - 剖面随时间怎么走，α 加热点不点得起来；产出可交付成 IMAS 数据入口
* - [放电设计](example-design.md)
  - `breakdown-iter` · `discharge-iter`
  - 线圈电流该给多少，才有这个场零区 / 这个平顶位形
* - [诊断分析：平衡反演](example-reconstruction.md)
  - EAST #137985 @ 4 s
  - 给定磁测量，位形是什么
:::

## 这本书之外

- [结果怎么读](reading-results.md)——每个结果带的溯源信息是什么意思，
  以及为什么「不算」比「算一个看起来像答案的数」更重要。
- [能力与边界](limits.md)——现在能算什么、不能算什么、alpha 期的稳定性承诺。
- **参考书**（`docs/reference/`）——`保真度边界` · `内核清单` · `API 速查` ·
  `运行报告模板` · `算例报告`，以及 **物理与数值十五章**：每个物理模块**实际实现**的
  方程、假设、参数域与数值算法，每条公式与算法带一手出处。要问「这个数背后是哪条
  方程、出自哪篇文献、验到什么容差」，去那里。

## 状态

**alpha。** 能力与数值口径仍在移动：入口、结果格式可能变化且不保证迁移路径。
缺口是**列出来**的，不是用返回零的函数填上的；超出模型适用范围的请求返回错误，
而不是一个看起来像答案的数。
