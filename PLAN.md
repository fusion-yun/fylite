# PLAN — 动理学反演的落地计划

登记 2026-09-12。本表登记**动理学反演**（`FYL-DESIGN-21`）从设计走到制品的**次序、判据与前置**。

**与两份既有台账的分工**——`TODO.md` 登记**开着的事实与依据**（不写结论、不排次序），
本表登记**要做的事、按什么次序、由什么判据算做完**；设计裁定的真源是
`docs/design/FYL-DESIGN-21.md`（Q-1..Q-9），本表**不新增裁定**，两处不一致时以该篇为准。
一条做完了就写关闭日期与判据，不删行——编号在闸子与提交信息里被按号引用。

---

## 0. 三期总览

〔设计真源 `FYL-DESIGN-21` §七〕先做**只读的图**，因为它删的手写最多、动的内核最少。

| 期 | 前置 | 做什么 | 闸 |
| :--- | :--- | :--- | :--- |
| **Q0 只读的图** | 无 | 今天四栏的调用关系写成一份计划文档；`flow.js` 从它画图；节点状态从四栏已有的记录读；判据读数从 `summary` 行读。四栏**照旧可用** | 流程闸 · 状态闸 |
| **Q1 控制** | Q0 · `run.js` / `checkpoint.js` 接线（`TODO.md` G-4） | 五个控制动作；过期传播；断点进断点仓；节点卡改档位 = 改计划 | 控制闸 |
| **Q2 外环** | Q1 · 外环从内核仓测试树发行到本分发（`-21` G-7） | s4 成环；轮记录；二维验收散点；`converges_by` 判 | 外环闸 |

★**四栏不撤。** Q0 的图是四栏的**另一种看法**，不是替代；撤栏要另立裁定
（`FYL-DESIGN-12` 的 P-9 / P-16 / P-22 / P-23 / P-29 未动）。

---

## 1. Q0 只读的图

| 编号 | 归属 | 事项 | 判据 | 前置 |
| :--- | :--- | :--- | :--- | :--- |
| **P-1** | fylite | **计划文档落地**：一份 `fyo:ScenarioSpecification`，`has_step[]` 五步（s0 基准平衡 · s1 映射 · s1b 剖面拟合 · s2 约束 · s3 反演），s4 **暂不成环**，端口绑定按 `-21` §四 的骨架 | 页面与 `fy run` 读**同一份字节**（sha256 相同），不各持一份 | **P-2 之前**；落点见下〈P-1 的落点〉 |
| **P-2** | fylite | **`app/assets/flow.js`**：计划 → 图。节点取 `has_step[]`，边由 `has_port_binding` 的 `x+run://<step>/<port>` 推出；只读，不驱动 | 页面 HTML 与 `flow.js` 里**没有手写的节点或边**（Q-1）——闸子 grep | P-1 |
| **P-3** | fylite | **页面挂点**：`app/pages/analysis.html` 的 `data-bar="reconstruction"` 内加一个图面板（`data-part="reconstruction"` 保持不变，沿用 `[data-flow]` 挂点记法）；文案入 `lang-zh.js` / `lang-en.js` 两份，不写死中文 | 四栏的既有闸 `validate-analysis.mjs` 仍全过；`validate-typography.mjs` 不新增红 | P-2 |
| **P-4** | fylite | **状态投影**：节点状态 = 该步最近一份记录的 `run_state`（七值）叠步带四态（`-18` U-8）；**过期**是第八种显示，不是第八个 `run_state` | 状态闸：每个节点的状态字符串 = 对应记录的 `run_state`；页面**没有**私有状态变量（Q-2） | P-2 |
| **P-5** | fylite | **判据读数**：GS 残差 · χ² · Δq₀ / Δq₉₅ 逐节点显示，取自记录的 `summary` 行与 `ComparisonFinding` / `ConvergenceCriterion` | 页面**不做**阈值比较，判定（`verdict`）只从记录来（Q-3）——闸子断言无比较运算 | P-4 |
| **P-6** | fylite | **闸子 `app/tests/validate-flow.mjs`**：流程闸（图上节点集合 = 计划 `has_step` 集合，**双向**）+ 状态闸（P-4）+ 无手写图（P-2）+ 无页面自判（P-5）；登记进 `app/tests/README.md` | 本闸在无内核、纯静态服务器下可跑（`validate-form.mjs` 的先例） | P-2..P-5 |

### P-1 的落点（**待裁定**，本表只列事实与代价）

〔已确立·实测 2026-09-12〕三处候选各有一条硬约束：

- `docs/examples/scenario/` 是**逐 code 的生成物**：每份一个 `prescribes_code`，由
  `tools/make-scenario-templates.py` 生成，由 `python/tests/test_scenario_templates.py`
  与目录 `lines.jsonld` 双向对账。手写一份**多步**计划放进去会同时撞生成器与目录闸。
- `app/cases/` 是**会话文档**（`fylite:AppSession/1`，装输入不装结果，经 `FySession.apply`）。
  计划不是会话文档，混进去会让「一个算例就是一份会话文档」这句话不再成立。
- `python/fylite/_manifest/` 已有 `kinetic_reconstruction.jsonld`，但它是
  **`prov:Plan` + `fylite:Workflow`**（workflow-IR 投影），且标 `fylite:executable: false`；
  它不是 `fyo:ScenarioSpecification`，端口与 DAG 用的是另一套键名。

〔推荐〕新目录 `docs/examples/plan/kinetic.jsonld` 收手写的多步计划，与生成的模板分居；
app 侧在构建期取同一份字节（`rust/build.sh` 拷进 `app/assets/`，并进 `app/sw.js` 预缓存表），
于是 P-1 的判据（同一份字节）可断言。代价：要给新目录写一条自己的对账（模板闸不覆盖它）。
★**未裁定前不要动手写 P-2**：图的读入形状由这一条决定。

---

## 2. Q1 控制

| 编号 | 归属 | 事项 | 判据 | 前置 |
| :--- | :--- | :--- | :--- | :--- |
| **P-7** | fylite | **`run.js` / `checkpoint.js` 接线进分析页**——两者写好且闸过，而今天**没有任何页面引用它们**（`TODO.md` G-4）。接线即关闭 G-4 | 断点闸 `validate-checkpoint.mjs` 的步进器由假件换成真内核；等价判据仍成立 | Q0 |
| **P-8** | fylite | **五个控制动作**（Q-4）：运行到此 · 单步 · 从此重跑 · 断点 · 取消。**没有「跳过」**——可选步的开关是计划里的 `fylite:switches`，关掉即节点不在图上或画成关着的 | 闸：动作集合恰为五个；页面里没有 skip 路径 | P-7 |
| **P-9** | fylite | **过期传播**：某步重跑后下游全部标过期（`engine.versioning.Staleness.invalidate` 的图形） | 控制闸后半：「从此重跑」后下游节点**全部**过期 | P-8 |
| **P-10** | fylite | **节点卡改档位 = 改计划**（H-1）：L2 的模型档位面板写回计划文档，不写页面变量 | 改一个档位后导出的计划与手改该字段的计划逐字节相同 | P-8 |
| **P-11** | fylite | **控制闸**（进 `validate-flow.mjs` 或新 `validate-flow-control.mjs`）：「运行到此」的结果 = 逐步手按的结果，**逐记录相同** | 同左 | P-8..P-10 |

---

## 3. Q2 外环

| 编号 | 归属 | 事项 | 判据 | 前置 |
| :--- | :--- | :--- | :--- | :--- |
| **P-12** | fylite · kernel | **外环可执行**：今天 `oracles/loop.py` 在内核仓测试树，`kinetic_reconstruction.jsonld` 标 `executable: false`，本分发里签名已不合（`-21` G-7）。两条路——把它发行回本分发，或按 s1..s3 由宿主逐步驱动重建（后者不需要发行，且与 Q-9「fylite 是 worker」同向） | 一轮 s1→s3 在本分发里跑得通，记录齐全 | Q1 |
| **P-13** | fylite | **s4 成环**：轮记录（含 `fylite:state`）· 逐轮 q₀ / q₉₅ · 二维验收散点（横轴 GS 残差、纵轴磁 χ²，每轮一点，验收区一块） | 收敛由计划的 `converges_by` 判，读数在轮记录里；页面不判 | P-12 |
| **P-14** | fylite | **外环闸**：N 轮一次跑完 ≡ k 轮 + 从轮记录续 (N−k) 轮，**逐记录相同**（`-18` §十三 断点闸的外环版） | 同左；先例 `FYL-REPORT-07` §9.1 ①（40 步 ≡ 20 + 续 20，逐位） | P-13 |

---

## 4. 卡在别处的前置

★本节**只登记谁卡谁**，事实与依据在 `TODO.md` 与 `-21` §九，不在此重述。

| 卡住的 | 被什么卡 | 出处 |
| :--- | :--- | :--- |
| 多步计划的**执行**（P-8 起） | `fy run` 今天一份计划一个 code（`--code` 选一个），`has_step[]` 无消费者 | `-21` G-1 |
| L2 档位面板的**参数名**（P-10） | code 层的参数面没有任何声明；模板词表是语料用过的名字，内核 `*_PARAMS` 是原始入口的 | `TODO.md` G-2 · `-16` K-2 |
| 「knot」与本仓基函数的**对应**（节点卡文案） | `basis` / `outk` / `kpts` 含义待 code 表自报 | `-21` G-4 |
| 二维验收的**阈值**（P-13） | 本仓反演解自己的 GS 残差**未量**（B-10 量的是 CHEASE 固定边界，B-06 量的是与 EFIT 的差）；DIII-D 的数值不可搬 | `-21` G-6 · G-8 |
| 三个**关着的节点**变成开着的 | ELM 相位条件平均 · Tₑ,sep 分离面对齐 · MSE 的 Er 修正**无 code**；MSE / CER / ECE 不在装置卷宗也无绑定表 | `-21` G-2（fylite）· G-3（fydata · fydoc） |
| `cancelled` 状态**有产者**（P-8 的取消） | `fy run` 一次门调用跑完整个 march，宿主没有介入点 | `TODO.md` G-3 |

---

## 5. 不做什么

〔已确立·`FYL-DESIGN-21` Q-9 · `FYL-REPORT-07` §8〕本计划**禁止 (MUST NOT)** 带来：

- **调度器 · 队列 · 服务端组件**（`NR-ENV-001`）。图的驱动在宿主，逐节点一次门调用；
  多时片仍逐片串行，进度数出来。
- **页面自判阈值**。判定只从记录来；页面画读数，不合成绿灯。
- **「跳过某步」**。一个被绕过去的步在记录里没有痕迹，而「这一轮到底做了什么」正是记录要答的。
- **抄 DIII-D 的阈值**。参照量级单列，本仓阈值只写量过的。
- **把 NUBEAM / ONETWO 编进某一步**。fylite 收它们的**产物**作端口绑定；调外部**程序**
  与 DAG 调度是 FyTok 的活。

---

## 记法

- 条目带**判据**；没有判据的条目，与一句愿望在使用上没有区别。
- 归属为他仓者（`kernel` · `fyo` · `fydata` · `fydoc`），本表**不得**作为在本仓改动的依据。
- 关闭一条时写明**关闭日期与判据**，划掉留行不删号。
- 标〔待裁定〕的条目**不开工**：它决定下游条目的形（如 P-1 决定 P-2 的读入形状）。
