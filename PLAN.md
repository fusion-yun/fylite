# PLAN — 仓级规划与开发状态（长期记忆）

★★**这份文件与 `TODO.md` 的分工是项目设定（2026-09-12 用户裁定）**：

- **`PLAN.md`（本文件）是规划与长期记忆**：登记每一条任务的**由来 · 依据 · 落地读数 · 状态**。
  行**只改状态、不删除** —— 一条关掉的任务仍然留在这里，因为「当时为什么这么判、量到了什么」
  是后来人唯一能核的东西。状态取值：**完成 · 开 · 待裁 · 阻塞**（待裁 = 等用户口径；
  阻塞 = 缺仓外材料或被他仓条目卡住）。
- **`TODO.md` 是当前任务列表、短期记忆**：只列**还开着的**那些，一条一行，说下一步做什么。
  一条做完就**改本文件对应行的状态，并把它从 `TODO.md` 里删掉**。

〔记法承旧〕条目**只登记事实与依据**，不写结论；带〔推测〕的判断保留标记。归属列是硬约束：
标 `fyo` / `fydata` / `fydoc` 的条目须按各该仓的治理落地，登记在此只为让本仓知道自己在等什么。

〔本文件的由来〕2026-09-12 之前这些内容住在 `TODO.md` 里 —— 那一份同时是台账与工作清单，
于是关掉的条目（划掉但留着长篇落地记录）与待做的条目并排躺着，读者分不清哪条要动手。
本次按上面的分工拆开：**内容一字未改，只加了状态标与这段抬头**，工作清单另起。

---

登记 2026-09-06，**E 组 2026-09-08 重写；同日晚二次清理**（收官批：`B-08` · `B-10` · `C-11` · `C-10` 落地，`G-14` 修掉）；
**同日 D 组续做**（EAST 标准算例：E-7 · E-8 立项，E-2 清单短一截，新 `F-14`）；
**2026-09-10 E 组增 `F-15`..`F-23`**（起始设计岭缺省一役量到的九条：两条未决的求解器行为、三处闸子长期红或根本没在跑、两处数据缺口、一处构建不可复现、一处路径残留）。本表收**本仓及其上下游**当前未完成的事项——之所以合在一处，是因为它们
多数是同一条链上的：内核声明面 → fyo 本体 → fydata A-Box → fydoc 事实层 → 本仓消费。
分散在五个仓的开放项各自成立，但**谁被谁卡住**只有并排看才看得见。

**归属列是硬约束**：标 `fyo` / `fydata` / `fydoc` 的条目**须按各该仓的治理落地**，不在本仓
改；登记在此只为让本仓知道自己在等什么。标 `kernel` 的是 `fylite_kernel`，标 `fylite` 的
才是本仓自己的活。

---

## 项目设定：公开仓 `docs/` 只放读者面的文档（用户裁定 2026-09-13）

**公开仓 `fylite` 的 `docs/` 不出现开发文档与内部报告。**「开发文档」= 规格链与设计集（`FYL-CONOPS-00` · `FYL-SRS-01` · `FYL-SDD-01` · `FYL-DESIGN-NN`）；「内部报告」= 评估研究（`FYL-REPORT-NN`）。它们的正本在 **fylite_kernel** `docs/design/` 与 `docs/report/`。

本仓 `docs/` 因此是**四篇**：用户指南 · 典型算例 · 参考 · 校验册——读者面的四个问题（怎么用 / 照抄一条路径 / 这个数能不能用 / 量过什么）。

★**物理与数值**十五章 2026-09-13 同批迁往 **fydoc** `physics/`，理由**不是**「它是开发文档」（它不是），而是它写的方程出处、参数域来历与对齐容差属 fydoc 「真实数据与事实的源头」那一职守。

★**引用规矩**：跨仓一律写成仓限定的行内代码（形如 fylite_kernel `docs/design/INDEX.md`、fydoc `physics/05-transport.md`）；跨仓相对链接两侧都解析不了，**禁止**。按 `document_id` 引用仍然成立——那是指认，不是路径。

★**闸**：`python/tests/test_docs_books.py` 的 `test_no_development_document_is_in_the_public_book`（按标识前缀 + 按目录名两条腿）与 `test_the_book_is_the_four_reader_facing_parts`。

## 0. 合并后任务清单（2026-09-11 归纳）

★★**本节只归纳，不作真源。** 每格末尾指向下面各表里的那一条，两处不一致时**以那一条为准**；
按〈记法〉本表只登记事实与依据，归纳不新增事实。★立此节的由来：2026-09-11
`origin/develop` 并入射频线 `2e8684d`（EC 的 ③ 伴随 ECCD · 锥形射线族 · L3 束追踪 ·
BORAY 本地 oracle · `core_sources` 守恒落梯子）之后，开放条目跨了两条互不相干的线，
并排列表已读不出「谁在等谁」。按用户指示分三组。

### 0.1 射线追踪 L1–L3（EC + LH）

★**分层的用语**：内核 EC 页只命名 **L2**（单射线 / 束中心线的冷等离子体轨迹 + 沿射线吸收 +
伴随法 ECCD）与 **L3**（束宽演化 · 衍射 · 聚焦）。**L1 是本节给「射线追踪之前那一层」起的名**
（闭式光深 + 效率模型 + 规定沉积表），不是内核页里的既有术语 —— 记在这里免得两处对不上。

| 层 | 合并后已实现（判据已过） | 余下 | 条目 |
| :--- | :--- | :--- | :--- |
| **L1** 闭式 / 表格 | `heating::ec_optical_depth`（已按 Bornatici 1983 原文表 IV 更正重印之误）· `eccd_efficiency`（对 METIS 认证存档 30 行：ours/METIS 0.026–0.64，中位 0.35，log 相关 0.976）· `sources = table` 的规定沉积 | 弱相对论极化选项（疑为 X2 对 GENRAY 晚 12 mm 之因）| F-31〈EC 残项〉 |
| **L2** 射线 | EC ①冷射线核 · ①b 托卡马克介质与边缘反射 · ② 相对论吸收与 ψ̄ 壳沉积 · ③ 伴随 ECCD，经 `code/rf_ray` 的 `deposit` / `current_drive` 接出；`core_sources` 按 `code/beam` 守恒算子落到绑定梯子（瓦数对壳 1e-9，可直接喂 `sources = table`）；锥形射线族 `rf_rings` / `divergence` 作 L2 束宽。LH ①①b 已实现（对私有 GENRAY EAST 2.45 GHz 四射线过 B 类）| ★**LH-② 2026-09-11 做完**（`rfray::lh_landau_alpha`，闭式；公式由 Maxwell 纵向介电函数**从头推出**，不抄原文，推导在内核 `docs/note/lh-raytracing.md` §4.3）：四道判据全过 —— 温度依赖逐位等于 ζ³e^{−ζ²}（差 < 1e-12）· ζ 与 `heating::lh_resonant_te` 同义（< 1e-12）· 冷极限指数归零且三种「无可阻尼物」给 0 不给 NaN · **谱隙量出来了**：5 keV 上 N∥ 1.5→4.0 跨 2.1e6 倍，格栅峰值 N∥≈2 处 α 0.138 1/m（7.3 m 才 e 折），故**② 单独给的是吸收下界**。★**LH-③ 同日也做完**（`rfray::lh_fp_solve` + `LhFp`）：定态由总平行流为零给出，是一条累积积分不是矩阵；驱动效率的闭式 η → (u2²−u1²)/(4 ln(u2/u1)) → 窄带 u0²/2 **由同一模型推出**，四个带实测差 < 2 %。★**一次真失败改出来的拒绝**：[6,7] 不随网格收敛（21.79 → 16.5）、[7,9] 给 3.7e5 —— 诊断为驱动电流落到求和舍入之下（e^−49 对 1e−16），于是 `efficiency()` 在 |J| ≤ 1e3 × floor 时**拒绝报数**，余量由实测定（接受侧 ≥ 6.5e4，拒绝侧 7.1 与 0.44）。★一维模型没有 (5+Zeff) / 陷俘 / 径向输运，**比趋势不比数值**。★**②↔③ 当日闭合**（`lh_landau_alpha_on`）：吸收可读在任意平行分布上，推广式代入 Maxwell 即回到 ② 的闭式（该恒等式作判据，4001/8001 点差 4.44e−4 / 1.30e−4，比 0.292 对上 du² 的 0.25，量值与收敛比一起卡）。实测（5 keV，平台 u∈[4,6]）：共振落在平台内 α 降到 Maxwell 的 **6.5e−5**（**平台是透明的**），落在平台上沿外 α 升到 **5.0e8 倍** —— 自洽吸收取决于波此前把功率放在哪里，这是「② 是下界」的兑现。**余**：把回代接进 `trace` 的循环迭代到自洽 · **LH-④ 接门与参照**（设计已定、全文缺三篇）· 离子多于 4 种的归并规则（现按名拒绝）· 公开物理页 `09-heating` 只同步到 ② | **F-30** · F-31 |
| **L3** 束 | 笛卡尔 3×3 复曲率 Riccati 方程加约束（Pereverzev 1998 · Poli 2001 的本仓写法；3×3 自带 2×2 约化漏掉的射线弯曲项）· `trace_beam` · `ec_deposit_beam` · 门 `beam_waist` / `beam_focus` / `beam_nodes`；**七条解析判据**（真空像散束 1.1e-8 · P98 §VI C 的 O/X 6.3e-8 / 2.8e-8 · 抛物通道 · 线性层九元全比 1.3e-8 · 束对邻射线 Jacobi 恒等式 7.9e-6）| B 类实算对标（TORBEAM / GRAY）**无可复算输入** `[TBD]`；无像差阶不含窄束的 N∥ 谱展宽与截面不均吸收 → 要不要做 GRAY 式射线族**待裁定** | **F-28** · F-31 ④ |
| **参照件** | GENRAY EAST O/X（私有 B 类）· BORAY 本机 Octave oracle（BSD-3，钉提交）· METIS 30 行 ECCD 带 · ★★**TORAY 全套 oracle 到手**（`FYDOC-CASE-21`：输入 + 名单 + **TORAY 自己的输出 nc**；冻结件 `cfedr_toray_20ma.txt`，判据 `tests/test_cfedr_toray_oracle.py`；驱动电流两侧差 **9 %**、轨迹差 ≤ 4 cm、吸收 1.0000 对 0.9990）| ★它当场量出的符号缺陷**同日修好**（F-33 关闭）：$\sigma$ 改为从文档推，修后 $N_\parallel$ 差 **1.5e-4**、轨迹差**亚毫米**、驱动电流比 **1.046**。余：EAST/GENRAY 夹具作第二检验 | F-27（i）· ~~F-33~~ 均已解 |
| **待裁定** | ★★**2026-09-11 全部裁定**（用户）：① 提 fyo 治理（提案 v1 已起草 → 本表 **O-4**）· ② LH-② 取 (a) 电子 Landau · ③④⑤⑥ 维持现状（LH 的 L3 不进表 · GRAY 式 N∥ 谱射线族暂不 · ECCD 香蕉区外修正在 L2 之外 · 多于 4 种离子仍按名拒绝）| 余下只有**弱相对论极化选项**：两篇全文不在容器，本会话做不了（不编公式）| **F-31** · O-4 |

### 0.2 复现 CASE-20（`FYDOC-CASE-20`；真源是内核 `docs/note/cfedr-15ma-reproduction.md`）

★★★**2026-09-11 收口陈述：复现做完了，余下每一条都卡在「交付件缺席」上，没有一条卡在模型上。**

| 层 | 复现到什么程度 |
| :--- | :--- |
| **几何（S1）** | 度规六量中位全部 |中位| < 2.5 %，最差随梯子加密减半直至收敛；**余差追到底，底在交付包**（见下 S1 行） |
| **源项（S2）· 闭包（S3）** | 逐项对上；③ 逐位同上游 NEO |
| **通量匹配（S4）** | 匹配区逐点 ±3.5 %，全档首次 `converged 1`（最差残差 8.06e-3）；解在给定起点与预算下唯一 |
| **外环（S5）** | 同域对照 W_th **−1.65 %** · P_fus **−0.11 %**；全域差（W_th −8.6 % · P_fus −18.3 %）**已归因**到匹配区**以内**那段未求解的核心 |
| **敏感性（S6）** | 饱和规则方向 · 台基密度三点，均复现 |
| **EC 对拍** | 驱动电流 **+1.7 %** · 吸收 71.94 MW 单程全吸收 · 峰位 ρ 0.3588 对 0.3450；rms 宽窄已量明是束发散角（≈2.1°） |

★**余下四条 `[TBD]`，全部是缺交付件**：`inone`（ONETWO 不能重跑）· `input.tgyro`（求解半径与选项）·
2.3 % 聚变功率差 · **S1 那两列 q 各自的来历**。**没有一条需要改模型。**

★**S0–S6 的逐步状态在内核那页**，本表只登记**卡住的**与**跨仓的**。合并后：

| 件 | 状态 | 余下 | 条目 |
| :--- | :--- | :--- | :--- |
| S0 参照冻结 · S1 几何 · S2 源项 · S3 闭包 · S5 外环 · S6 敏感性 | 跑完 | — | 内核页 §4.1–§4.2 · §5 |
| S4 通量匹配 | 匹配区逐点 ±3.5 %；★**2026-09-11 首次 `converged 1`**（热支在全档续跑十六轮，最差残差 **8.06e-3**）| ★**S4 对照已跑完**：平坦起给够轮数**收敛回交付支**（Q 24.10 → 12.42，落在交付支 12.20/12.32/12.46 那一族 1.8 % 内）⇒ **就这两个起点与这个预算而言解是唯一的**；精简档一度看到的「两支」判为档位效应（Q_i 偏高 29.9 % × α 正反馈）。**余**：匹配区**以内**那一段仍由内边界给（本趟轴上 T_e 15.54 对另一趟 24.08 keV），与 S2 源项一起承担对参照的 W_th −4.2 % / P_fus −17.6 % | 内核页 §5.1–§5.3.1 |
| **EC 对拍**（本案例最后一条未做的逐点比较） | ★★**2026-09-11 做成**：对交付的 `EC_3`，驱动电流 **+1.240 对 +1.219 MA（+1.7 %）**、峰位 ρ 0.3588 对 0.3450、吸收 71.94 MW 单程全吸收 | ★**rms 宽那条差已量明**：扫锥的发散角，**≈2.1°** 上 rms 对上（2° 0.0379 · 3° 0.0469 · 交付 0.0388），而峰位 · 吸收 · 电流几乎不动（0–3° 电流 1.240 → 1.233）—— 差的形状是束宽，不碰主结论。★负结果：`echin` 第 12 槽（两例都是 1.2）若是以度计的发散角则预言 rms ≈ 0.032，对不上，故**不是**它，仍 `[TBD]` | 内核页 §1.9 · `tests/test_cfedr_ec_against_the_delivery.py` |
| ψ̄ 归一 | 判明：两侧分母差 **1.56 %**（TORAY 侧用 ψ 图真跨度 15.9199，本仓用 statefile 一维网格 15.6752）| EC 逐点对比前必须统一；单独可解释约 0.003 的峰位偏移 | **F-32**（b）|
| ~~S1 的两个待归因项~~ | ★★★**2026-09-11 收口。** 「体积亏 3.57 %」早判非偏差；压缩这个候选实测排除；「中位符号翻了」判为记录抄错（在旧提交上重编重跑，逐位相同）；两个中位先证明**不是离散化**（481/961/1921 层四位不动），再由 dρ_N/dψ_N 高 0.826 % 解释掉 `gcap` 的 82 % | ★**追到底了，底在交付包里**：$\rho_N=\sqrt{\int q\,d\psi/\Phi_{\rm edge}}$ 只依赖 q，两侧都自证用的是这个定义（1.3e-4 / 7e-4），于是 ρ 差**全部**是 q 差；而**梯子的 q 不是本仓算的** —— 收的是 statefile 的 `qpsinpsi`，它与 ONETWO 剖面表的 `q` **差 −1.47 %，两列都是交付的**。★预言落空并被钉住：改喂剖面表的 q，`q` 中位没塌（−0.699 → −0.846 %），`gcap` 减半而 `hcap` 差近一倍 —— 比较在匹配 ρ_N 上做，而 ρ_N 自身差 0.493 %，**比较坐标把 ρ 差带进了每一个量**。**余下的度规差不是本仓的一处缺陷**，库内无任何选择能同时归零；判它需设计方说明那两列的来历，**与 `inone` / `input.tgyro` 同类 —— 缺的是交付件不是模型**（`[TBD]`）。仍开：`hcap` 补偿后残下的 −0.583 %，与该不一致纠缠，不单独可追 | **F-32** |
| 交付包的第二份平衡 | 判明：交付 `gfile_efit` **不是这次运行用的平衡**（ψ 图逐点差到 0.320 Wb/rad ≈ 2 % 跨度、磁轴 7.91 mm、`psiaxis` 1.20 %），**而本仓链条一处都不读它** | 对**交付件使用者**是事实（已写进 fydoc 案卷页）；对本仓不是阻塞 | **F-32**（已降级）|
| 缺席件 | — | `inone`（ONETWO 不能重跑，自举模型开关无从切）· `input.tgyro`（TGYRO 实际求解半径与选项 `[TBD]`）· 2.3 % 聚变功率差 `[TBD]` | 内核页 §1.7 |
| 迭代提速（工具，非结论）| 内核 `1dffef4`：`FY_S4_*` 保真度旋钮（便宜档）· 热启动 `FY_S4_DUMP` / `FY_S4_START_FILE`（每轮落盘）· 半径调度改动态取号 | ★**2026-09-11 标定已跑**（`s4_control_the_cheap_fidelity_against_the_full_deck`）：全档 92.5 s / eval · 便宜档 29.8 s（3.1×，Q_e 偏置 −4.7…−22.9 %）· 精简档 4.5 s（20.5×，+0.5…+10.9 %）；精简档 120 次 eval / 297 s 的落点与全档差 **0.7 % · 1.2 % · 1.1 %**（W_th · P_fus · Q）。**「用精简档找解、用全档定值」已量过**；逐点偏置仍须随数引用 | 内核页 §5.1 · `cfedr_s4.rs` 抬头 |

### 0.3b 杂质与 Z_eff：向导出量收窄（2026-09-12 立，用户选定范围 A）

★**现状（评估 2026-09-12）**：给定 $Z_{eff}$ 剖面的多组分闭合与辐射（日冕 ⟨Z⟩ 覆盖 He/Li/Be/C/N/O/Ne/Ar，ADAS 冷却 22 种，未知种辐射为零不报错）满足复现需求；**预测 $Z_{eff}$** 缺源与输运。
★**范围 A 已落**（内核 `impurity_transport`，`docs/note/cfedr-15ma-reproduction.md` §5.6）：一种注入杂质的定态输运剖面（$D$、$v$、边缘、源为设定），电荷日冕，$Z_{eff}$ 导出；三条判据（极限/守恒 · 夹具回归 · CFEDR 轮：平氩钉在 ONETWO 边缘值给 $Z_{eff}$ 中位 2.85、含量 2.11 倍——ONETWO 的氩是空心的）。**无 oracle**（ONETWO 给定 $Z_{eff}$ 反解氩）。
★**$D_z$ 随 χ 已挂**（`imp_d_over_chi = r` → $D_z = r\,\chi_{turb}$，eval/finish 阶段重解；χ≡1 夹具下与常数 D 逐点差 0.000e0）。
**余**：范围 B（He 灰通道）· W 的电荷矩 · $v_z$ 与新经典 $D_z$ 由闭合给出 · 未知种改按名拒绝 · 台基 $Z_{eff}$ 取台基值（B′ 已做）。

### 0.4 台基：向 EPED 收窄（2026-09-11 立，用户指示 A/B 进计划、C 待评）

★★**2026-09-12 oracle 找到了**：`FYDOC-CASE-20` 交付包的 `EPED/eped_state.nc` 是 EPED 自己的运行（十输入 · 答案 · KB/PB 扫描表），冻结为内核 `testdata/reference/cfedr_eped_15ma.txt`，门 `pedestal::tests::eped1nn_against_epeds_own_run_on_cfedr_15ma`。实测：EPED1-NN p_ped 93.0 kPa 对 EPED 110.7（ptotped）/ 115.8（p_E1）kPa，即 **−16 % / −20 %**；宽度 0.0448 对 0.0231（ptotwid）/ 0.0508（wid_E1）；外推距离 0.313 落在 a = 2.5 m（**CFEDR 在代理训练盒之外**）。计划 A 的逐高度 `gamma_KB` / `istable_PB` 表随之可用（内核 `docs/note/cfedr-15ma-reproduction.md` §5.5）。

★**现状**：本仓有的是 **EPED1-NN 代理**（`rust/fylite/src/pedestal.rs` 511 行 + 生成的权重表，
九组解，九道判据），不是 EPED。EPED 是**两条约束在一族自洽重建的平衡上求交**：
非局域剥离-气球稳定性（ELITE）与 KBM 起始 $\Delta\psi_N = 0.076\sqrt{\beta_{p,\mathrm{ped}}}$。

★**已量的差**（内核 `docs/note/pedestal-literature.md` §4）：压强比 0.850 / 0.910 / 0.859，
宽度比 0.945 / 0.965 / 0.826，$\beta_{N,\mathrm{ped}}$ 0.833 —— **系统性偏低，但落在论文自陈的
±20 % 之内**。所以「现有实现不够用」**不能由这个偏差论证**。

| 档 | 内容 | 状态 | 靶 |
| :--- | :--- | :--- | :--- |
| **A** 〔完成〕 | KBM 宽度约束 + **无限 n 气球**（沿场线一维 ODE 本征问题） | **2026-09-12 已落**：`pedestal::salpha_boundaries`（s–α 边际方程，s=1 → 0.612/2.604）；EPED 自己的 G 由表读出 0.072–0.078（E1 行 0.0744）；E1 台基逐面定位：两肩穿理想带、陡区第二稳定 | **A 量出的边界**：理想判据不定高不定宽，高度须 P-B（C）或交付表 E1 行 |
| **B** 〔阻塞〕 | A + **EPED 外环**：扫（高度, 宽度）族 → 每点重建含 $J_{bs}$ 的 GS 平衡 → 两约束求交 | **计划中** | 端到端对已发表的 8 个数 |
| **C** 〔阻塞〕 | 真 P-B（中 $n$ 全局本征值 + $\omega_*$ 抗磁稳定化，ELITE 做的事） | **评估结论见下：先找参照件，不先写求解器** | ✗ 容器内无中间量可对 |
| **B′** | 把外环里**冻结的三样**放进去（$n_{e,\mathrm{ped}}$ · 形状 $\delta,\kappa,a,R$ · $Z_{\mathrm{eff}}$；现在只有 $\beta_N$ 在迭代，且是显式一步滞后） | **2026-09-12 已接**（opt-in `eped_inputs = state`：态的边缘密度 · 物种 $Z_{eff}$ 边缘值 · 梯子边缘 Miller 形状；$a,R$ 不动；β_N 仍一步滞后）。判据：边缘 $Z_{eff}$ 2→3 使 p_ped ×1.056、宽度 ×1.127 | CFEDR 链上的落点移动待 S6 重跑 |
| — | **T-C8 口径差**：本仓送 $\delta_{95}/\kappa_{95}$ 与体 $Z_{\mathrm{eff}}$，上游不一定同源 | 开（跨仓） | 口径未对齐前，第二层那 9–17 % 的归因不可判 |

★★★**C 的进一步评估（2026-09-11）—— 结论是「先找参照件，不先写求解器」。**

**(1) C 到底要算什么。** 边缘对**中 n**（约 5–30）理想 MHD 剥离-气球模的线性稳定性，
判据是增长率对抗磁频率：$\gamma > \omega_*/2$。ELITE 用 $1/n$ 渐近展开（保到 $O(1/n^2)$）
在场线对齐坐标上把它化成一族按极向谐波耦合的径向常微分方程，解广义本征值问题。
**关键**：EPED 的全部要点正是那条**耦合**边界与「纯气球」或「纯剥离」都不同 ——
所以把 A（无限 n 气球）当成 C 的便宜替身**在物理上不成立**，A 只能作自检不能作替代。

**(2) 缺的不止求解器。** 还要 (a) 台基处**远高于输运梯子**的边缘网格与精确度规
（$|\nabla\psi|$ · 局域剪切 · 曲率）；(b) 台基分辨的边缘自举电流（本仓有 Redl 点模型，
但台基内的精度是另一回事）；(c) $\omega_{*i}$（现成）。

**(3) 真正的瓶颈是判据，不是代码。** 容器内可用的靶只有 **8 个端到端数**
（$p_{\mathrm{ped}}$ · $\Delta$），而且论文自陈 ±20 %。一个 P-B 求解器的内部自由度
（n 谱的取法 · 分辨率 · $\omega_*$ 系数 · 边缘电流的处理）**远多于 8 个带 ±20 % 的数所能约束的**。
业界验它用的是**稳定性图**（$\gamma/\omega_*$ 对 $n$；$j_{\mathrm{ped}}$–$\alpha$ 图），
本仓没有可复算的数字版 —— 这是 `[TBD]`，也是本条的真阻塞。

**(4) 本仓自己的经验说得更直白。** 射频线的 F-33（$\sigma_{B_p}$ 符号缺陷）**是因为有
TORAY oracle 才被当场量出来的**；没有 oracle 之前，同一份代码带着那个缺陷过了所有自洽判据。
一个没有参照件的 P-B 求解器**是不可证伪的**，写出来也不能用来判任何事。

**(5) 因此排序**（我的评估，非裁定）：
1. 先做 **B′ → A → B**（都有靶，且 A 可作 C 的**自检**：若代理给的台基离本仓自算的
   无限 n 气球边界很远，那本身就是一条可检出的不一致）；
2. 与此并行，**调查有没有授权相容、可作 oracle 的公开 P-B 码**（照 BORAY / TORAY 的先例），
   结果记进本行；
3. **只有在 (2) 有结果之后**，C 才谈得上立项，且须先立 ADR。

★**本仓已有而 EPED 需要的**：平衡重建（`equilibrium.rs` 2902 行 + 固定/自由边界 GS 求解器）·
流面几何与度规（`surfaces.rs` 2927 行）· 边缘自举电流（`neoclassical::redl_bootstrap_point`）·
KBM 闭合（一行，且已有判据证明代理满足它）。**缺的只有稳定性求解器**。

### 0.3 其他（按被谁卡住分）

| 组 | 条目 | 一句话 |
| :--- | :--- | :--- |
| 声明面 / 本体治理 | K-1 · K-2 · K-3 · K-4 · O-1 · O-2 · O-3 · E-3 | 裸路径的叶与首段不在 DD、两侧节名与单位写法未约定、四张内核自有表无本体承载、测量不确定度无 spo 承载 —— **都不在本仓改** |
| 需数据访问 | E-2 · E-7 · F-8（可发信）| 本炮加热 / 杂质 / 输运只在 MDSplus；EAST 本炮两个平衡互不相容；`C-08` 的 ν* 与逐半径 Miller 参数要向作者索取 |
| 语料与发布闸 | ~~E-4~~ · E-6 | ~~五面态是计算产物不是测量（`dev:result` 七取值待裁）~~ E-4 2026-09-13 关闭（身份归 fyo 模态，见 D 组）；`check_cases.py` 28 处 / 19 组红，其中 14 组等 reviewer 与联系方式 —— **按本仓规矩红着等人给，不许编** |
| 本仓功能与判据 | F-1 · F-2 · F-9 · F-12 · F-14 · F-16 · ~~F-18~~ · F-20 | `discharge-iter` 的门 · 生成件里手写段活不过下一次 `--write` · `code/breakdown` · `B-01` 是否改在 FUSE 1.1.5 重跑 · POINT 条纹清洗只设下界 · 自由边界不收敛且不随预算单调 · node 五道闸长期红（缺数据 / 夹具过期 / 真错三者未分）· 真机参照只有 EAST 一台 |
| 构建与制品 | F-21 · ~~F-22~~ | F-21 差的只是**另一台机器上那两个制品文件**（判别法与工具链戳已落）；F-22 2026-09-11 关闭 |
| 装置 / 文档 | F-10 · ~~F-13~~ · F-19 | 生成的装置清单指向不存在的路径 · ~~`todelete/` 余下未判~~（F-13 2026-09-13 关闭：`todelete/` 整体移除）· 装置文档不声明线圈工程限值 |

---

## A. 内核声明面（`fylite_kernel`）

证据：`fyo` 仓 `FYO-REPORT-05` v0.5（2026-09-06，对内核检出 `08d0c0b` 实测）。三条都**不会
让任何脚本非零退出**——这正是它们至今还在的原因。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **K-1** 〔完成〕 | kernel | ★★**2026-09-11 关闭。** 六条裸路径的叶在 DD 中不存在，已在内核 `fyo.rs` 逐条加 `fylite:`，接口修订 **1 → 2**（改 path 必须升号）、摘要随之更新，公开仓 `test_fyo_paths_have_a_dd_home.py` 的基线按它自己的规矩删掉这六行。★**顺带推翻了一条记录在案的选择**：真空室那四条此前特意留在基线里，理由是「查的是声明表，这个量在 DD 里确实没有家」—— 那把两件事混了：「没有 DD 归宿」是事实，「所以路径必须带 `fylite:`」是内核自己的规则，两者不冲突而后者才是修法；佐证是 `fylite_runtime` 归一化时**原矩形本来就挂在 `fylite:geometry` 下**，即那个裸 `geometry` 指的路径真文档里一处都没有。★**余一条未答**：`core_transport` 的 `rho` 与 `rho_d` 是不是同一条网格（是就该合并）—— 本轮只修好了名字，这个问题仍开。★另：「`vacuum_toroidal_field/b0` 声明 0d 而 DD 记 1 维」**判为非缺陷**——本表的 rank 指一个时间片内的秩，同形的还有九个槽 | 内核 `fyo.rs` 抬头 @fyo-revision |
| **K-2** 〔第一批已落 · 第二批随 O-5 B 批〕 | kernel · fydoc | ★★★**2026-09-13 用户裁定「进文档」，拆两批。第一批（DD 有家的十条）已落**：线圈电阻率 → 逐线圈 DD `pf_active/coil/resistance`〔Ω，按内核元件公式 R = η·Σ2πrN²/(wh) 由原 0.017 μΩ·m 换算；内核与宿主反推回电阻率〕；IC1/IC2 → `pf_active/coil` 并以 DD `function` = `b_field_fb`（index 2）标出，PF 元件集、通道图、EFIT 展平一律跳过它们，`code/vstab` 取作快控执行器；供电 → DD `pf_active/supply/{voltage_limit_max, current_limit_max}`〔A〕，匝数用既有 `element/turns_with_sign`。卡片 `pf_active_circuits` · `ic_coil` · `power_supply` 三节删去，fydoc EAST `pf_active` 页同步（`circuit_model_resistivity` 撤、`resistance` 进逐线圈）。DEVICE 表 55 → 49 行、AOS 加 `supply` · `function`，接口修订 4 → 5（摘要 `e133b3b8a36c02a6`）。★★**顺带修掉一处潜伏错配**：卡片元件按 deck 行序排，而旧 `element_turns` / `max_voltage_V` 按 PCS PF1..PF14 序列，`code/breakdown` 与 `pulse.channel_limits` 按元件序索引，EAST 上除前三路外都配错了匝数与限压——新 `supply` 按元件中心配对写入，EAST 的通道安匝上限因此**应当**变。★IC 几何按裁定「文档取 fydoc/Guo，基准自带 TokSys」：文档记 Guo（r 2.4 · z ±0.6），γ 在其上 −6.53%（出 ±5%），`test_benchmark_toksys.py` 自带 TokSys `east_obj_2013` 的 IC（r 2.309 · z ±0.7425）与 etaf 0.017，γ −2.97%。★★**Guo IC 上测得的一处物理差异**（合成平衡，2026-09-13）：`test_control` 的执行器滞后判据在 Guo IC 上不成立——0.3/γ 滞后过冲 2.49 ξ₀（TokSys 4.73），激进增益停在 1.0 ξ₀（TokSys 32.5，「调高增益更糟」不现）；该判据当初在 TokSys IC 上测得，故也改为自带 TokSys IC，不调参数掩掉。★★**同日查明原因（线性闭环极点：装置 + 一阶滞后 + PD）**：0.3/γ 滞后、测试原增益下最大增长率 TokSys 温和 +1.94 / 激进 +3.86 s⁻¹，Guo +1.17 / **−0.29（激进组反而稳）**——激进组是**重微分**的（kd/kp 1/60 对 1/1000），相位超前抵得过滞后与否取决于执行器权威，而 Guo 对每伏 ξ 的推动大 4.2 倍（几乎全来自 z 0.6 对 0.7425）；按该比缩小 Guo 增益并不能复原判据，TokSys 的轨迹在 10 倍增益处也重新变稳。**结论：「滞后时调高增益更糟」钉的是这组增益在这对线圈上，不是慢电源的普遍性质**；判据断言未改，限定写进 `test_control.py` 的说明 |**第二批**（解算盒四边与网格数六条 → 计划设置）与 O-5 B 批同做，`test_fyo_paths_have_a_dd_home.py` 基线余这六条。〔以下为 2026-09-11 原文〕★★**2026-09-11 判明「加 `fylite:` 前缀」不是它的修法，故未改，条目仍开。** 首段确实不是 84 个 IDS 中的任何一个，但**真装置文档里这十六条的键一个都不存在**：`machine_desc/east/fylite_device_east.json` 的顶层是 `tf` · `pf_active` · `wall` · `magnetics` · `lh_antennas` 与 `fylite:grid` · `fylite:channel_map` · `fylite:vessel_resistivity_uohm_m` 一类，**早已整体搬到 `fylite:` 之下而本表没跟上**。所以 K-2 不是「裸写了非 DD 名」，是 **DEVICE 段相对真文档过期**：`machine/default_grid/r_min` 真正对应的是 `fylite:grid` 底下的某个键。加前缀只会让它看上去对而仍然指不到任何文档。**真修法 = 逐条对着真装置文档重映射**，已在 `test_fyo_paths_have_a_dd_home.py` 里以带理由的开列表登记，红着等那次重映射 | 本轮实测 |
| **K-3** 〔阻塞〕 | kernel · fydata | 条目不变（`fyo.rs` 的 `transport_inputs` 对 A-Box 的 `transport/fylite:rho` · `transport/fylite:y`）。★**本轮未动**：判定要 A-Box 那一侧的实际文档，而本会话虽已取到 `fydata`，核对与改名是跨仓的一件独立活，不并进本轮 | FYO-REPORT-05 O-9 |
| **K-4** 〔完成 2026-09-13〕 | kernel · fyo | ★★**用户裁定「按照 fyo 为准」**。fyo 的单位串即 IMAS DD `dd_units` 文法（点分因子带号幂 `m^2.s^-1`，符号取 DD 的 `u` · `ohm^-1.m^-1` · `s^-1`）。改前 fyo `gen_kernel_graph --stats` 实测 **14 处**（本行原记 12 处之后又多了 `LH.spectrum{,_2d}` 两行 `1` 对 `W`）。内核 `fyo.rs` 声明面 **70 行**（表与块）只改 units：那 14 行取 fyo 原串，其余按同一文法与 DD 符号改写（不做代数化简）；`case.rs` 86 · `stationary.rs` 27 处记录单位标签同表改写，按单位位置扫残留斜杠为 0。`LH.spectrum → W` 与内核用法相符（谱值乘 n_φ 宽度当每 bin 功率）。接口摘要 → `a45d01dbc21e99e1`、`INTERFACE_REVISION` 3 → 4（判据：改 units 即旧读者会读错）。宿主侧只改声明量的 `Wb/rad` 字面量（`session.js` · `scenario-analysis.js` ×2 · `recon_rs.py` · 内核 `make-east-inputs.py`）。★本行原记的「量表路径起点两种约定只在散文里」（O-5）不属单位书写法，未在本批处理。**原文**：单位书写法两侧未约定：12 处不同（9 处斜杠 vs 点-幂语法，3 处符号/角度约定 `amu`↔`u`、`1`↔`e`、`rad/s`↔`s^-1`），**量纲不符 0 处**。量表路径起点有两种约定（IDS 根 / 数组元素）且只写在散文里，机器可读的表不携带 | FYO-REPORT-05 O-5 · O-7 |

★ K-1 / K-2 / K-4 的"应改成什么"含〔推测〕成分，**不应据报告直接改写**——报告只出证据。

## B. 本体侧（`fyo`）

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **O-1** 〔已裁定 2026-09-12 → 见 O-5〕 | fyo | 四张内核自有表（`discharge` 48 · `transport_inputs` 10 · `uq` 5 · `pulse` 4，共 67 行、全部规矩带 `fylite:` 前缀）与五条**承重**缺名（23 条程序间交接路径中占 5 条，尤以跨四 IDS 复发的 `fylite:psi_norm`）是对 `fyo:` 命名空间的提案，待裁定接纳与否 ★**裁定结论**：入 fyo 治理 —— 但逐条核对之后，67 行里**没有一行**是待铸的本体槽：`discharge` / `transport_inputs` / `uq` / `pulse` 是**一次计算的输入面**（`FYO-ADR-07` 已判形态 = 计划 + 绑定的 Dataset），故按 09-12 台账 O-5 的 D-5(d) 处置（fyo 侧无对应 ADR 条款；原记 `FYO-ADR-11`，fyo 并无此件） 归计划参数；跨四 IDS 复发的 `fylite:psi_norm` 则真的是缺槽，已入 `CoreRadialGrid.psi_norm`。 | FYO-REPORT-05 O-3 · O-4 · 须 `FYO-ADR-*` |
| **O-2** 〔部分裁定 2026-09-12 → 见 O-5；**2026-09-13 挂点已落**（`FYO-ADR-15` v0.2：继承上游 `has_uncertainty`，E-3 关）；逐样本不确定度仍等上游 spo〕 | fyo | 测量不确定度无本体承载：spo 有 `UncertaintyStatement`（`standard_uncertainty` + `uncertainty_origin`，正为此而设），但 **fyo 无任何槽指向它**。直接后果见 D 组 E-3 ★**裁定结论**：`uq` 的 `sample_*` 五条是**采样计划**而不是不确定度承载，按 09-12 台账 O-5 的 D-5(a) 处置（fyo 侧无对应 ADR 条款；原记 `FYO-ADR-11`，fyo 并无此件） 归计划参数、不入 Dataset；spo 的 GUM 子结构仍是上游待裁（`FYO-ADR-00` 台账既有 WAITING 行）。 | fydata G-10 · 须 `FYO-ADR-*` |
| **O-5** 〔开 —— ★★fyo 侧 **2026-09-13 才真正落地**：`FYO-ADR-16`（Proposed，D-1 十槽同批落 schema）；09-12 下文所记「fyo 仓落成 `FYO-ADR-11` + 九个新槽」**不成立**——fyo 的 `FYO-ADR-11` 是物理模型那份，所述槽在任何分支头都不存在（fyo `FYO-REPORT-05` v0.6 O-1 独立测到）。下游**第一批（A+C，19 条）已迁**；**B 表按 ADR-16 D-2 改判六处**（斜角是 EFIT 平行四边形↔DD oblique 的几何换算、不是 deg→rad，EAST 30 个斜元件 · `tf` 名为 `b_field_phi_vacuum_r` · 离子是 `name` 不是 `label` · 限制器是跨文档端口 · LH 最大功率无承载 · `z_magnetic` 与 EC 两角待核），**批准后再迁**；D 类待迁〕 | kernel · fylite · fyo | ★★**用户裁定 2026-09-12**：「**fylite 不作为本体前缀，本体增加，入 fyo**」。★同日在 `fyo` 仓落成 **`FYO-ADR-11`**（Proposed，落地批同提交）并**逐行核对了真 schema**：内核声明面 17 张表里 **124 处**带 `fylite:` 的路径、**110 个不同名**，按承载分是**55 处在 DD 血统的 Dataset 之内**、**69 处在 fyo 根本没有的四个「文档类型」里**（`discharge` 51 · `transport_inputs` 10 · `uq` 5 · `pulse` 4）。**四类处置**：**A 去前缀 8**（fyo 已有同名同路径，如 `profiles_1d/psi_norm` · `b_field_pol_probe/length` · 器壁 `rectangle` 四格 · EC `mode`）· **B 改用既有槽 18**（`r_minor`/`r_major` ← `r_inboard`/`r_outboard`；`z_magnetic` ← `geometric_axis/z`；`dvolume_dpsi_norm` ← `dvolume_dpsi`；限制器 ← `wall` 的 limiter outline；`ion_density`/`impurity_density` ← `ion[]/density` + `label`；线圈与器壁 `a1`/`a2` ← `oblique/alpha`·`beta`（**deg → rad**）；`resistivity_uohm_m` ← `resistivity`（**μΩ·m → Ω·m**，单位后缀入槽名从此禁止）；`tf b0` ← `b_field_tor_vacuum_r ÷ r0`；EC 两角 ← `steering_angle_pol`/`_tor`；LH `max_power` ← 工程限值集）· **C 入 fyo 11 处 / 9 个新槽**（`CoreRadialGrid.psi_norm` 补不对称 + `EquilibriumProfiles1d` 的 `r2_average` · `geometric_axis_shift` · `geometric_axis_z_shift` · `elongation_shear` · `triangularity_shear` · `squareness_mxh` · `squareness_mxh_shear` · `mxh_harmonics`，**均已落进 schema**，manifest 0.9.6→0.9.7，登记为 `FYO-MAP-00` S-9）· **D 退出 Dataset 18 + 69**（每次运行的设定 → 计划参数；**五条 `*_prev` 滞后量 → `Checkpoint`**，即本仓 09-12 上午刚落的那五个声明按裁定要改归断点；分组 / 通道图 / 网格盒 / `ladder_levels` / q 的第二套标签 / `steady_change` → 计划 · 工程限值 · 评定层）。★**本体只长了九个槽**，裁定的效果主要是**去掉一个前缀**而不是扩充本体。★**迁移协议**（ADR D-6）：一次一模块，接口**修订号 2 → 3**（既有槽的 path 变了），**不设读侧别名** —— 依据是实测：公开检出已提交文档里只有 **36 处**出现声明槽名且**全在生成件**，fydoc 算例语料 **0 处**。★裁定同时结清了本表的 **O-1**（四张内核自有表的归属）· **O-2**（不确定度承载：`uq` 的 `sample_*` 属计划参数，GUM 结构仍等上游 spo）与 **H-15 1.2** 的槽治理问法（Dα 端口按同一规矩先入 fyo）。 ★★**第一批 2026-09-13 落地**（A 4 + C 15 = 19 条路径）：`fylite:psi_norm` → `psi_norm`（5 处）· 局部形状八条改用 fyo 铸的名字 · 磁探针 `length` · EC `mode` · 器壁元件矩形四条 `geometry`。**接口修订 2 → 3**（摘要 `862b526a362b2495` → `229c7d3c295fbd3d`），装置牌同批改拼法，内核 `OURS` 表 26 → 16，词表 `_fyo_vocab.json` 105 → 100 个词。★落地时判明一处**分类错**并已订正 ADR：器壁矩形原记 A（以为 fyo 已有 `geometry`），而有 `geometry` 的是线圈元件；`Vessel2dElement.geometry` 因此成为第十个新槽。★两道闸按新前提改判据而不是加豁免：`test_fyo_paths_have_a_dd_home.py` 从「DD 是唯一权威」改成「DD 归宿**或** `FYO_OWNED` 逐条登记（注明铸它的 ADR）」，并加两条自检（两表不重叠 · 只准登记真的裸且无 DD 家的）；`device_to_imas.rs` 与 `test_device_export_integrity.py` 各认领一行「导出 IMAS 时丢」——器壁矩形在 fyo 文档里在，只是 DD 没有它，几何本身由四角 `outline` 照常导出。**余下**：B 类 18 条（改用既有槽，含 deg→rad 与 μΩ·m→Ω·m 两处单位归正）与 D 类 18+69 条（退出 Dataset：设定 → 计划参数、滞后量 → 断点、四个「文档类型」→ 计划）。 | `fyo` `docs/adr/FYO-ADR-16.md` · `docs/adr/FYO-MAP-00.md` S-8（2026-09-13；原记 `FYO-ADR-11.md` · S-9，fyo 并无此件） · `schema/src/{fyo-common,plasma_state/equilibrium}.linkml.yaml`（2026-09-12）|
| **O-3** 〔完成 2026-09-13〕 | fyo | ★★**用户裁定「fyo 改本体」**，重开 2026-08-31 那条裁定。fyo `a178b45`：`Wall` 撤回提升到根上的 `type` / `limiter` / `mobile` / `vessel`，恢复多值 `description_2d`（range `Wall2d`，DD maxOccurs 3）；`Wall2dLimiter` / `Wall2dMobile` 的 `dd_path_also` 撤掉；`check_omega` 的 `UNREACHABLE_BY_RULING` 6 → 0（670/670 嵌套 `dd_path` 走得到自己）；注册表 0.17.0 → 0.18.0；`FYO-REPORT-05` v0.6 加状态注记。fydoc `045f502`：六台仍按旧形挂在页根的 `wall` 页（best · cfedr · cfetr · east · jt60sa · west）改成 `description_2d` 列表形，只动结构不动数（改前逐页核过重序列化逐字节相同），`check_abox_corpus` 七台 PASS；`iter` 本就是列表形。下游：本仓 `tools/abox-to-facts.py::wall()` 两形都收、内核 DEVICE 路径本就是 DD 形、fytok `wall.py` 本就读 `description_2d`，皆不必改。fyo 的 `check_competency` 62/62 · `check_conformance` PASS · `check_dd_text` 0。**原文**：`wall` 单套二维描述（**用户裁定 2026-08-31**，非缺陷）的代价已量化：670 个带嵌套 `dd_path` 的类中恰 6 个不能沿属性树走到自己的路径，全在此一 IDS；`maxOccurs="3"` 压成一套。已由 `check_omega` 规则 (e) 设门（`UNREACHABLE_BY_RULING`，第 7 个出现即失败）。**仅在认为该代价过高时重开裁定** | FYO-REPORT-05 O-6 |
| **O-4** 〔完成〕 | fyo | **射频天线表的声明路径与约定**（2026-09-11 用户裁定 ①「现在就提 fyo 治理」立项；提案 v1 在内核 `docs/note/ec-raytracing.md` §7.1）。**现状**：`EC_LAUNCHERS` 八槽里 `name` · `frequency`（`beam/frequency/data`）· `power_launched` · `r` · `z` 走 DD 原名，**两个指向角与模式走 `fylite:`**。★**不是因为 DD 没有这两个名字** —— DD 确有 `beam/steering_angle_pol` / `steering_angle_tor`（rad，内核 `rust/fylite_runtime/ids/ec_launchers.tsv` 核过）；**没验的是约定**。**提案请 fyo 定三件**：(1) DD 那两个角与本仓的一对是不是同义 —— 是则给换算规则、`fylite:` 槽随即退役，否则**治理一对带约定文字的槽**（关键在约定要写下来，按名字猜约定正是一束波瞄到别处而无一处报错的路子）；(2) 模式要一个**支**：DD 只有 `beam/polarization/o_mode_fraction` ∈ [0,1]，而**份额不是支**（0.5 既不命名 O 也不命名 X，把一个变成另一个是模式转换模型，本门没有），现门只收 ±1 并对其它值按名拒绝；(3) **LH 天线行**的声明路径（发射位置 · N∥ 谱与功率权 · 极向 N · 反射面参数）—— LH 的发射以强加折射率而非两个指向角参数化，现按名拒绝。★**本仓不等**：`fylite:` 槽继续可用，门的行为不因提案改；治理落地后换路径。★**提案能提得出来是今天才成立的**：TORAY 的两个角已由数据推明（`angrid1` 极角 · `angrid2` 方位角，实测复现声明值到 0.004°），与本仓一对的换算是算术（102°/214° ⇒ θ_p 14.386° · θ_t 33.164°）—— 有了可提的约定，治理路径才有东西可治 | 内核 `docs/note/ec-raytracing.md` §7.1 · §4.7 · `rust/fylite/src/fyo.rs` 的 `EC_LAUNCHERS` 表注 ★★★**2026-09-11 结案：`FYO-ADR-10`（Proposed）已在 fyo 落地并推送。**用户裁定「fyo 天线参照 imas dd」，逐条对真 schema 核过之后 **fyo 一个自有天线槽都没有新增**：**D-1** 两个指向角的约定由 fyo **声明**并写进 schema（形式与提案逐字相同，且明写 φ̂ 由机器声明的 COCOS 定），**与 DD 自身约定是否一致仍 `[TBD]`**（`FYO-MAP-00` S-8，待 DD 全文）；**D-2** O/X 支**库里本来就有** `ec_launchers/beam/mode`，不新增槽；**D-3** LH 发射谱**本来就在 DD 血统里**（`antenna/row/n_phi` · `n_pol` · `power_density_spectrum_1d/2d`），并更正提案的提法 —— **N∥ 不是天线的属性**，它依赖平衡；**D-4** 反射面与取哪个根判为**模型设定而非装置属性**，不入 device 层，归属场景层待 CONOPS。★**本仓由此产生两条后续**：(a) `fylite:mode` **应当退役改绑** `beam/mode`；(b) 两个角**暂不改绑 DD 名** —— D-1 的 `[TBD]` 未解之前改绑等于把一个未验的一致性假设焊进链路 | fyo `docs/adr/FYO-ADR-10.md` · 内核 `docs/note/ec-raytracing.md` §7.1 |

## C. A-Box 侧（`fydata`）

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **D-1** 〔完成〕 | fydata | **已关闭 2026-09-08**：`check_abox_shape.py` 新增 **S8** 收 `abox/amns/`（记法同装置树，`_ids` 认根类）。开核当次报 **1 处**——`charge_state` 挂在 `amns_data` 顶层，而 DD 放在 `process[]/charge_state[]` 之下；**未改数**，转为 fydata G-12（重铸属转换器的活） | fydata README G-1 |
| **D-3** 〔完成〕 | fydata | **已关闭 2026-09-08**（`fydata 0e114a1`）：**S10 的 147 处「该是数组的槽写成单个结构」**——`coil.element` 110（七份 pf_active）· `flux_loop.position` 35 · `time_slice.profiles_2d` 1 · `coil.conductor.cross_section` 1。T-Box 记 multivalued、DD 记 `1...N`，而 A-Box 写的是一个结构，于是**十三台装置一台也跑不动 `code/breakdown`**（内核按 DD 读 `pf_active/coil/0/element/0`）。★逐行改不走 load/dump（件是手工维护、头注释里有「盲目重跑会把 deg→rad 打回去」的警告）：每处只改块的第一个子键那一行，其后逐字节不变；判据是改前改后 `yaml.safe_load` **语义相同**。S10 147 → 0 | fydata `check_abox_shape.py` S10 · 本仓 `C-10` |
| **D-2** 〔完成〕 | fydata | **已关闭 2026-09-08**：新增 **S9**。★按字面实现的第一版**是错的**——「值域是类⇒必须写结构」报 173 处，而标量简写是通行记法。改判两条：简写的**类型**要配得上值域类；**同一棵树内**不得两种写法混用（跨树不算）。各抓到一件真事：EAST `ec_launchers` 的 `frequency: 140E9` 被 PyYAML 读成**字符串**（YAML 1.1 浮点正则要小数点+带号指数），四束皆是，已改 `1.4e+11`；`Wall2dLimiter.type` 装置树内混用整数与字符串，未改数（口径裁定，fydata G-13） | fydata G-11 |

## D. EAST 标准算例（`fydoc` `facts/experiment/east/137985/`）

现状：电流平顶已核（2.5 s 起 400.36 kA ± 0.27 %，4000 ms 在其内，E-1 已关闭）；磁测面成立，
**动理学面有了实测的量而仍不可用**。★★2026-09-08 该条目自己往前走了一步：fydoc 仓里那份
「待删」的 mdsip 录音（F-13；2026-09-13 随 `todelete/` 整体移除，字节只在 fydoc 的 git 历史 `2a26380`）**含本炮交付 EFIT 的标量**，据此量出**本炮有两个互不相容的
平衡**——交付 EFIT 的储能 187.1 kJ 对 g-file 转换件的 46.7 kJ，**差 4.00 倍**（详见该条目 §五）。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **E-2** 〔阻塞〕 | 需数据访问 | 加热 / 驱动 / 杂质 / 输运的本炮数据只在 MDSplus（`mds.invalid`，内网地址不入仓）。★**2026-09-08 清单短了一截**：本炮的 `\WMHD` · `\BETAP` · `\LI` · `\PCRL01` · `\VP1` · `\DFSDEV` 与 EFIT `MEASUREMENTS` 已在 fydoc 的录音里（`\PCRL01` / `\VP1` / `\DFSDEV` 只有抽稀后的 253–463 点）。**加热 / 驱动 / 杂质 / 输运一支未录**，故不关闭 | 条目 §五.1 · §七 |
| **E-3** 〔已关 2026-09-13〕 | fydoc · 待 fyo | Thomson 的不确定度只能标 `fylite:` 前缀（60 处）——被 B 组 O-2 卡住。★★**2026-09-13 用户裁定「E3 推进 fyo」后关闭**：`FYO-ADR-15` v0.2 对 `spo` 检出核验——上游已把 `has_uncertainty`（→ `UncertaintyStatement`，标量 `standard_uncertainty` + `uncertainty_origin`）施于 `Signal`，汤姆逊 `SignalFlt1dValidity` 经继承即有挂点，fyo **不铸槽**、只加 `CQ-MEA-5`（两门俱绿 63/63）；fydata 汤姆逊切片 60 个误差叶按值迁为 `has_uncertainty`，写它的内核工具同批改。余：逐样本不确定度（ADR OI-5，上游）· 那批数是否 1σ 是读者读法〔TBD〕 | 条目 §八 |
| **E-7** 〔阻塞〕 | 需数据访问 | **本炮两个平衡互不相容**：交付 EFIT（录音）与 deck 储能差 **4.00 倍**、$\beta_p$ 差 4.45 倍，而 deck 自己 GS 闭合到 0.59 %。挡路的是**那份 g-file 不在任何仓里**（只有 sha256），其 `RUN_TYPE` 与名字里的 `loop` 读不到；`FYDOC-CASE-19` 收的三份 EAST g-file 没有本炮的。★★**对本仓的意义要说准**：受影响的是 **`B-06`**（`plan/R/east-137985`）而**不是** `V-15`（后者是 g-file 读写往返的恒等式，与压强内容无关）。`B-06` 的参照侧是 `oracle_east137985_4000ms`，而该 oracle 自陈其约束是「磁测 + `kprfit=1` 直接压强 65 点」——那 65 点正是 deck 的压强列。**故 `B-06` 作为「本仓能否复现这次重建」仍然成立**（它比的是同一个答案），**不**支持「本仓重建出了这一炮真实的平衡」这句更强的话 | 条目 §五.5 |
| **E-4** 〔完成 2026-09-13〕 | 须语料裁定 | ★★**用户裁定「以 fyo 为准，参考 IMAS DD」**，落在 fydoc `facts/experiment/INDEX.md`〈计算产物的身份〉：「核得怎样」与「由什么过程生成」是**两条正交的轴** —— 前者仍是 `dev:result`（七值**不增**），后者是 fyo 的**模态**（`Dataset` 混入的 `spo:ProvenanceMixin.modality`：`static_device` · `measured` · `reconstructed` · `simulated` · `imputed`）。由本炮实测经物理算子算出、给定输入可复现的状态（含自洽五面态）是 **`reconstructed`**，禁标 `measured`；逐 Dataset 按生成过程判。IMAS DD 4.1.1 实读的对应（`ids_properties/occurrence_type`：`reconstruction` 1 · `prediction_fixed` 2 · `prediction_free` 3 · `mapping` 4；`provenance/node/reference`）只作映射核对，不另立字段。**原文**：自洽五面态是**计算产物**不是测量；`dev:result` 现有七个取值没有一个是为它准备的。**这是本条目自己走不过去的那一步** | 条目 §五 · §八 · fydoc 实验层 INDEX |
| **E-5** 〔完成〕 | fydoc | **已关闭 2026-09-08**（那一步已走）：逐片 12 道 PF 通流实测（fydoc `facts/tools/pf_drift.py`，纯标准库、只读）——最静段 **4.0–5.0 s**（总 3.17 %/s · 方向 1.17 deg/s），**4000 ms 落在其起点**，故重建选片不必改；全平顶方向仅转 7.91°，而逐道看 c1 降 50.9 %——两种读法量的不是同一件事，条目已并记。**「任一片都不是严格稳态」不变** | 条目 §二 |
| **E-6** 〔阻塞〕 | fydoc | `check_cases.py` 报 **28 处 / 19 组**不满足（其中 **14 组** reviewer 联系方式待补）。**既存项**，2026-09-08 再经 `git stash -u` 复核确认改动前完全相同。★当日走过 24/12 → 26/13 → **28/14** 三档：先是 `FYDOC-CASE-04`（FUSE）跑出答案、按该组**自己写的**规矩 `review.status` 由 `not-required` 改判 `pending`；再是 `FYDOC-CASE-19-east-efit` 立组（F-11），其 `reviewer` / `contact` 按同一条规矩记 `[TBD]`。**两次都不是新缺陷，是同一条规矩对新落账的东西生效** | fydoc `tools/check_cases.py` |

## E. 本仓（`fylite`）与内核（`fylite_kernel`）

★2026-09-08 更新。**此前这一节写着「本轮未在本仓留下未完成项」**——那句话在 09-06 成立，
当日的验证定序册第三轮与第三方源码盘点之后不再成立。下表是本仓／内核自己的活，
**不依赖上下游任何一条**。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **F-1** 〔门已通，可评条数 0 → 2；2026-09-13 H-19 已关，余下转可做〕 | fylite | **`discharge-iter` 的门**。★**2026-09-13**：下文 ④ 所说的阻塞已解——规范裁为整圈 Wb（COCOS 17，H-19 行），内核记录的 p′/FF′ 自 ABI 154 起与 ψ 同规范。**下一步**：`code/discharge` 把一维 p′/FF′（及 F、q 所需的 FF′ 积分）按整圈 Wb 写进声明的槽（内核 `case.rs` 那段「不写 `dpressure_dpsi`」的注记已改为指向本条），再重跑两步算例看第二步是否过 `profiles_1d/q`。★★**2026-09-12 做了那两件事，并量了结果**：**①** `case.rs::run_json` 的**一份文档**那条路改走**树门**（有树门时；ABI <126 仍回退扁平门）—— 它从前无条件调扁平门，而扁平门只递 f64 槽，于是要整份文档的 code 在这里按名拒绝（`[-33] … reached through the tree door only`），同一份计划经 `fy run` 却跑得动。**②** `docs/examples/design/discharge-iter.jsonld` 把装置文档**绑成输入**（`inputs/device` → `dist/facts/device/iter.jsonld`）。两件之后该算例**跑通**（`run_state: succeeded`），判决由「拒绝」转**通过**。**③** 顺带修了两个真错：退火历史里「不适用」的格子写 DD 空值 `-9e40` 而不是 `NaN`（`finite` 此前因此红：`1 处非有限：entry/history_alpha (1/9)`；现在是「14 437 个数值全部有限；另有 1 个 DD 空值（不适用，不计入有限性）」），以及 `code/discharge` 现按声明的槽交一份 `equilibrium` 文档（二维 ψ · 两条网格 · 边界外形 · 四个全局量 · 限制器）—— 它一直在解自由边界平衡，只是没在文档里说。`boundary-closed` 由此转**通过**（首末点间距 0.0521 m = 0.643 个采样步，判据 1.5；越出限制器的边界点 0/181，最深 0 m；a = 1.692 m）。**④ 余下 10 条为什么还评不上，逐条说清**：`grad-shafranov` 要一维 p′/FF′，而写它得先裁 **H-19**（记录里二维 ψ 与一维 p′/FF′ 实测差 2π）—— 现在写就是把那个缺陷种进新文档；`psi-endpoints` 要一维 ψ 网格，形状设计没有，凭 ψ̄ 现编会让它**按构造**通过（假数）；`volume-monotone` / `grid-monotone` 要 `LADDER/*`；其余六条要 `CORE_PROFILES` / `SUMMARY`，而这一步是**形状设计**，不产生动理学剖面。★**那条「下一步」当日就试过并量了结果：也卡在 H-19**。两步算例（设计 → `code/ladder`）确实跑得动**第一步**（把装置绑定放进步骤内即可，顶层 `inputs` 不被步骤继承 —— `run_steps` 逐步合成），记录里有 `design/entry` 与 `design/equilibrium` 两份产出；**第二步按名拒绝**：`[-31] the document carries no time_slice/profiles_1d/q (q on the uniform psi_N grid)`。`trace_document_ladder` 要 q，而 q 要 F(ψ)，F 要把 FF′ 积起来 —— 正是规范未裁的那一族。**所以 F-1 余下的部分整体在 H-19 下游**，不是「还没做」，是「做了会种下 2π 那个缺陷」。 | RUN-2026-09-08 §四 · §六.3 · `rust/fylite_runtime/tests/json_door.rs`（2026-09-12 实测） |
| F-1（原问法，已被上一行取代） | fylite | **`discharge-iter` 的门**。物理校验册 88 条现只评了 24，做了它可评条数才会动。要两件事一起做：`run_json` 走树门，**且**算例把装置文档绑成输入 | RUN-2026-09-08 §四 · §六.3 |
| **F-2** 〔待裁〕 | fylite | **生成件里的手写段没有活路**。手写归因活不过下一次 `--write`，**而且不会有任何东西报错**——这是它比「写错了」更坏的地方。三个候选落点（判据册算例声明的 `caveat` · 本册子的 RUN 页 · 渲染器认一个锚点）**要先定**，才好写第二段 | RUN-2026-09-08 F-24 |
| **F-3** 〔完成〕 | kernel | **已关闭 2026-09-08**（用户裁定：**Rust 侧重写**）：26 条里 **11 条已重写**——层析 6 + 自标定方法学 3 落 `diagnostics.rs`，装配层编排 3 落 `transport.rs`；`cargo test --lib` 459 → **470 passed**，并做过变异检验（把基的 `clamp(0.0,1.0)` 改 `0.9`，边界那条当场判负）。★**剩下 15 条不是「没写」而是「本 crate 没有那个主体」**：UKAEA 网络 8（模块不在任何树里，许可那两条早已捞回）· 层析的重建质量 5（要反演求解器，本 crate 只导出基与行）· `source_set` 台账报表 1。逐条落位见 `tests/PHYSICS-MIGRATION.md` 该节 | `tests/PHYSICS-MIGRATION.md` 退役表 |
| **F-4** 〔完成〕 | fylite | **已关闭 2026-09-08**：专用 `Pkg.develop` 环境跑通 `FUSE.test_case(Val(:ITER_time), dd)`（FUSE 1.1.5 / IMAS 7.3.0，437.7 s，**61 个时刻**，`evolve_error: null`），答案收进 fydoc `FYDOC-CASE-04-fuse/corpus/1.1.5/`。`B-08` 随之判 pass。★捕获脚本加 `FUSE_TEST_CASE` 门——与 0.7.0 那份**出自同一个读取器**；★走不通的一条路已记在语料 README：把写盘的 `dd` 读回来归约，`json2imas` 在留空的二维字段上抛 `MethodError` | plan.jsonld `B-08` · 内核 `180dd93` |
| **F-5** 〔完成〕 | fylite | **已关闭 2026-09-08**：CHEASE 本机构建成功并自行重跑 `ntcase=2`（不读冻结产物），`B-10` 判 pass（GS 残差 5.257e-02，带 8e-2）。★该条的主要产出不是那个数，是一个读法——**加密盒子不是收敛检验**（101×65 → 401×257 残差不降反升；改内部网格才降 2.1 倍） | plan.jsonld `B-10` |
| **F-6** 〔完成〕 | fylite | **已关闭 2026-09-08**：映射逐项落实，`C-11` 判 pass——离子热通量系统偏低 **−42…−53 %**、电子热通量最差 **+131 % 恰在阈值**、八点全在训练箱内（门里第一条就判这个）。★留痕的那一项：算例是「红/蓝氢」示踪设置（两支同位素氢 + Be + C）而网络只模一个有效主离子，**Be 那一支在低梯度端比氢两支合计还大**——所以离子那一栏的差里含着**成分折叠**，不全是代理误差 | plan.jsonld `C-11` |
| **F-7** 〔完成〕 | — | ~~`B-09`：要不要自跑 DINA 当参考~~ **作废（登记当日）**：DINA 的参考答案就在 `~/workspace/data/ITER Scenario/`，不需要自跑，那次许可/清净室裁定也就不必做了。★留着这一行是因为它示范了一件事——**这条待办从提出到作废不到一小时，而它提出的依据是台账里一句写着「未取回」的旧备注**。备注记的是写它那天的状态 | SURVEY-2026-09-08 §一 · §四 |
| **F-8** 〔阻塞〕 | 可发信 | **`C-08` 向作者索取 GENE 那批次的 ν* 与逐半径 Miller 参数**（或那次 JINTRAC 模拟的对应量）。这是六条阻塞里**唯一一条只差一封信**的 | plan.jsonld `C-08` |
| **F-9** 〔`code/discharge` 部分已裁 2026-09-14；`code/breakdown` 待裁〕 | fylite · fydoc | ★**2026-09-14 用户裁定（经 I-5）：卡片线圈额定 `pf_active/coil/fylite:i_max_aturn` 由 `code/discharge` 缺省读**——内核 `fold_card_limits` 已落（算例绑定优先 · 与 supply 同在取较紧 · 部分无额定则拒 · `card_limits = 0` 旧读法），CFEDR 卡（abox-to-facts 带 104014 表 1 额定）因此不必再在算例侧绑上限；**`code/breakdown` 未改、仍只折 `pf_active/supply`，ITER 等无 supply 的装置它读不读卡片仍待裁**。**`code/breakdown` 仍跑不出结果，但挡路的换了一件事**。G-14 修掉后 ITER 与 EAST 过了几何这一关，卡在下一句：「the device gives no supply current limit (`power_supply/current_limit_kA`) and the plan binds no `i_max_aturn`」。（K-2 第一批后该拒绝按路径 `pf_active/supply` 报；EAST 卡片已带 DD 供电条目。）两条路：装置描述补上供电限值（是**数据缺口**，ITER 的 A-Box 里没有；EAST 的手工牌里有 `current_limit_kA: 14.5`），或算例侧绑一个 `i_max_aturn`。**先定这一条走哪边**，再谈把 breakdown 接进定序册 | 2026-09-08 实测（`fy run design breakdown --device iter --facts <fydoc>/facts`）|
| **F-10** 〔完成〕 | fydoc · fylite | ★★**2026-09-12 关闭，修在消费方而非生成器**：fydoc `facts/device/<slug>/abox/README.md` 明写清单路径保留 `<tree_root>/<相对路径>.yaml` 写法（它兼作 `dev:provenanceKey`），**消费方去掉 `tree_root`、换 `.jsonld` 后缀**——运行时 `assembly::from_manifest` 此前逐字拼接，从未执行这条规则。现 `book_path` 依次试：按原样 · 去 `tree_root` · 换后缀（两种写法），存在者胜，都不存在按原样交出让下游报名。实测（`fy run design breakdown --device …`，六台）：cfedr 15 · best 19 · cfetr 15 · west 17 · iter 12 · east 16 个线圈进装置文档，「no `pf_active/coil`」不再出现；六台随即都停在下一道门「无供电电流上限」（**F-19**）。判据 `a_book_manifest_resolves_tree_root_and_the_jsonld_suffix`（书的布局缩影 + ITER 手写写法按原样取胜）。**原文**：**生成的装置清单指向不存在的路径**。`abox2jsonld.py` 把每份件搬位（剥 `tree_root`）并改名（`.yaml`→`.jsonld`），却把清单里的 `providers[].path` **原样抄过来**——于是 `best` / `cfetr` / `cfedr` 的清单指着 `fyo/latest/providers/pf_active/base.yaml`，书里没有这个文件，解析不到线圈；`west` 更是**一个 provider 都没声明**。四台因此在 `code/breakdown` 上报「the document carries no `pf_active/coil`」。★ITER 之所以能过，是因为**它那份清单是为书手写的**（`path: providers/pf_active/base.jsonld`）——不是生成器做对了 | 2026-09-08 实测（四台逐台跑）|
| **F-11** 〔完成〕 | fydoc | **已关闭 2026-09-08**（fydoc `9eb7657`）：三份 EAST g-file 收进算例书 `FYDOC-CASE-19-east-efit`（`corpus/` 与 `case.yaml` 同址、`checksums` 逐件在册、`payload: in`），本仓的门与登记册指针随之改指。★★查证时发现比「指针脆」更要紧的一层：2026-09-04 的裁定删 `corpus/experiment/` 时写明这批件**不可重取**、「只剩 git 历史与未跟踪的 `todelete/`」，而同日一次**讲文档规则**的提交把 `todelete/` 顺带跟踪了进来、提交信息一字未提——于是一批不可重取的件被一个名叫「待删」的目录持有着。★迁入**不等于放行**：`release: internal` 不变，review 待具名 | 登记册 `V-15` 的 `has_input` |
| **F-13** 〔完成 2026-09-13〕 | fydoc | ★★**用户裁定「`todelete/` 整体移除」**：fydoc 删去余下四件（`todelete/east/{README.md, mdsip-137985.json}` · `todelete/device/east/{README.md, east_signals.json}`；`device/iter` 与 `facts/` 两支 2026-09-10 已删），字节只在 fydoc 的 git 历史（删除前最后一个含它们的提交 `2a26380`）。连带：`facts/tools/stored_energy.py` 的 `--record` 改为必给（原缺省指向 `todelete/`）；#137985 条目 §五.1 加移除说明、E-8 关闭；本仓浏览器门 `_mdsip-replay.mjs` 与 `validate-east-catalog.mjs` 本就在夹具缺席时**自报跳过**，未改。★§五 · §六 的读数（E-7 的 4.00 倍）是当日实测的记录，照旧成立，但**不可在任何检出里复跑**，除非有人从历史取回录音。**原文**：**`todelete/` 余下的部分未判**：`east/mdsip-137985.json`（一次 mdsip 会话的逐帧录音，fylite 的浏览器门经 `FYLITE_MDS_FIXTURE` 读它）是**测量**不是重建产物，按分工归实验层 / `fydata` 的 A-Box；另有 `todelete/device/{east,iter}` 与 `todelete/facts/`。★F-11 只搬了它该搬的那一份，**没有顺手替其余的决定归属**。★★**2026-09-08 这一条的分量变了**：那份录音不是只给浏览器门当夹具的——它是本炮**交付 EFIT 标量在整个生态里的唯一一份**（E-7 全靠它），且**含五炮**（137984 · 137985 · 137986 · 165704 · 165705）而文件名只写了一炮。一份这样的件住在名叫「待删」的目录里 | 2026-09-08 迁址时并记 · 同日实测 |
| **F-14** 〔阻塞〕 | fylite | ★**2026-09-12 判据形状已改（半关）**：`fylite.io.est2.fringe_gate`（纯函数）把判据改成**对数对称带** `gate·median < |n_e,line| < median/gate`（`gate` 0.15 → 天花板 6.67×），`reduce_est2` 走它；七条钉（塌下去 · 跳上去 166× / 7× · 带的对称性 · 无读数永不 good · 中位只取非零弦 · gate≤0 关闭）。★**没关的部分**：c4 的「3× 以上」六片里落在 3–6.67× 的仍过带——3× 是否即条纹跳**未判**（整数条纹的幅度取决于线密度，不是固定倍数），要用第二口径（弦中位数对 `\DFSDEV` 的比值 45 倍漂移）定夺，那一步需要该炮数据（E-2）。**原文**：**POINT 的条纹清洗判据只设下界**。`python/fylite/io/est2.py` 的 `good = (a_ne is not None) and (abs(a_ne) > floor)`（`floor = gate * median`），注释自陈的理由是「丢了条纹的弦会塌到中位数的一小部分」——**这对塌下去成立，对跳上去不成立**，而条纹跳是整数倍相位跳，两个方向都会发生。EAST #137985 实测：**c4 在 9 片中的 6 片上超同片存活中位数 3 倍以上**（最高 **166 倍**），每一片都带着 `weight_nel = 1.0` **交给重建**；c11 另在 1.0–2.0 s 三片上超 7–20 倍。★★同一支工具的第二条口径：弦中位数与干涉仪自己的线平均道 `\DFSDEV` 的比值在九片上走了 **45 倍**（0.03 → 1.34）——弦长不变，故不是几何；1.0–2.0 s 那三片的 POINT 归约实际在读噪声。★**重建选的 4000 ms 落在比值正常的那一段**（0.87–1.34），故本仓现有结论不受影响——但判据的形状是错的 | fydoc `facts/tools/point_chords.py`（2026-09-08 实测）| ★**状态 2026-09-12 由〔开〕改〔阻塞〕**：判据形状那半已落地；余下「c4 落在 3–6.67× 的六片算不算条纹跳」要该炮的线平均道比值，即 **E-2** 的数据访问，本仓做不动。
| **F-12** 〔待裁〕 | fylite | **`B-01` 的参考侧要不要改在 FUSE 1.1.5 上重跑**（该条现有结论建立在已遗弃的 0.7.0 冻结答案上，其 `status_note` 自陈这一问「仍未裁定」）。★2026-09-08 起**成本变了**：跑 1.1.5 所需的 Julia 环境已经在本机建好（F-4 的副产物），此前挡它的正是这一件 | 登记册 `B-01` status_note · SURVEY-2026-09-08 §一 |

| **F-15** 〔完成〕 | kernel | **起始设计的岭调过 2e-1 之后，退火一趟都不接受**。`code/discharge` `stage=anneal` 的 `pass` 事实报 **0** —— 八趟里没有一趟比没动过的起始设计更好，即退火一步没走；交出来的 κ=1.146（要的 1.389）、磁轴离实测轴 **102 mm**（EAST 小半径 0.44 m）。换 8 / 16 / 24 趟、`anneal_hi` 0.10 / 0.03 / 0.01 六种排程，`shape_error` **逐位相同**（0.14660），故不是排程敏感。λ≤1.5e-1 时正常下降（最优趟 6–8，`shape_error` 0.035–0.053）。★两解未分辨：接受规则只收「比历史最优更好」而步长降不到排程下限以下〔推测〕，或该电流水平附近确无更好的点。~~**这条定着岭缺省的上界**~~★★★**已查清并关闭 2026-09-11 —— 本条的三处判断都不成立，逐条量过（EAST #137985，一次只动一个旋钮）**：① ~~「上界 λ ≤ 1.5e-1」~~ —— **λ = 2e-1 能到 gap 0.01405**（`anneal_hi = 0.01`）；真上界在 **2e-1 与 3e-1 之间**，且它由 `anneal_hi` 决定，**不由 λ 单独决定**。② ~~「六种排程 `shape_error` 逐位相同，故不是排程敏感」~~ —— 那六次**全是 pass 0**，即起始设计，**排程根本碰不到它**；退火一失败，报出来的就是个与排程无关的量，**这个测量在原理上就量不出敏感性**。实测敏感性很大：**0.147 → 0.028**。③ 两个未分辨的解**都不成立**：**根本没有接受闸** —— `chan`/`res`/`sm` 每趟无条件前进，只有 `best` 的**记录**是有条件的，所以 `pass = 0` 从来不是「一步没走」；而更好的点确实存在（**0.0776 → 0.0141，好 5.5 倍**）。★**真实机理是发散**：λ = 2e-1 配缺省 `anneal_hi = 0.10` 时，游走从 gap 0.078 m 走到 **0.208 m**，一种排程下直接走死（`gap_last = nan`）。★**旋钮是 `anneal_hi` 不是 `passes`**：λ = 2e-1 上 `hi = 0.01` 在 8 趟与 24 趟都成，`hi = 0.03` / `0.10` 两种趟数都不成。★★**顺带修掉让它被误读三天的那件事**（内核 `c37ad01`）：**走飞了、走死了、探过但没更好，三种结局此前逐字节相同**——都是 `pass = 0` 加起始设计的数。现在门在 notes 里分开说，并给出**量出来的**补法（调小 `anneal_hi`）。两个方向都验过：发散那一组报出「0.0776 → 0.2079」，成功那一组**不报**——一条两边都报的提示，等于没修 | `fylite_kernel` `tests/test_start_against_the_machine.py`（2026-09-08 实测） |
| **F-16** 〔完成 2026-09-12〕 | fylite | ★★**交付的是后一支：起始电流的条件数已报出来**。内核 `pulse.rs::StartDesign` 新增 `normal_cond`，即闭式解那条正规方程 `AᵀA + λ²I` 的 κ₂（λ 的增广行已在 `aa` 里，故 `AᵀA` 就是该方程的矩阵），经 `case.rs::start_facts` 作事实 `start_cond` 交出（`stage=start` 与整条 `discharge` 两处都报；无设计时为 NaN）。**实测**（EAST 公共卡，24 边界点，无控制行）：λ=1e-3 → 2.5562e8 · 1e-2 → 2.7410e6 · 1e-1（缺省）→ 2.7431e4 · 1.0 → 2.7530e2；`κ·λ²` 三个量级同一个数（255.6…275.3，比值 1.077）—— **推论（已测）**：λ² ≫ σ_min²，小头由**岭**定而非数据定，κ ∝ λ⁻²。三台机器上缺省 λ 的 κ 差不到两倍（ITER 2.4133e4 · EAST 2.7431e4 · best 3.9890e4），而 λ 动一个量级差 100 倍。**界是有效的**：ε=1e-9 实测放大 0.372（λ=1e-1，界 √κ=165.6）· 0.681（λ=1e-2，界 1656），越界即 `normal_cond` 装错。闸：`python/tests/test_start_design_conditioning.py` 三条新增（事实在且在带内 · κ·λ² 恒定 · 实测 ≤ √κ）。★**未解释的仍留着**：`free.residual` 对**设计**预算非单调这件事，κ 只给了「落点可被输入放大多少」，没说「为什么 1200 步的那组起始电流恰好让自由解收敛」——该问题另记 F-34。 | `python/tests/test_start_design_conditioning.py` · `fylite_kernel` `rust/fylite/src/{pulse,case}.rs`（2026-09-12 实测） |
| F-16（原读数，已被上一行取代，留作 2026-09-08…09-12 的记录） | fylite | ★★**2026-09-12 重测并改写了读法**（`validate-worker-interp-device`，EAST，出厂岭）。原记的三个数取自 2026-09-08，其后出厂岭等改动已移动落点；今天逐档实测：**(a) 自由解自己的预算**（`spec.freeMaxIter`）—— 400 → **5.5566e-3**、1200 → **2.2625e-3**、4000 → **3.0633e-3**，三档都 `settled: false`，**仍非单调**（1200 优于 4000），加迭代解决不了。**(b) 真正决定它的是上一级**：只改**设计**那一步的预算（`solve.maxIter`，自由解仍 400 步）—— 400 → 5.5566e-3、**1200 → 1.2778e-9**（复测同值，a_minor 0.3283 m）、4000 → 3.5187e-3。即**换一组起始电流，同一个自由解基本就收敛了**（容差 1e-9，差 1.28 倍），所以这不是「自由解在装置档上不收敛」，是**设计交过来的起始电流决定它收不收敛，且该依赖非单调**。★**下一步该动的是交接而不是解算器**：让设计那一步的落点可复现（或把起始电流的条件数报出来）；调自由解的迭代上限是白费。原记的三个数（5.6e-3 / 6.8e-3 / 6.1e-2，含一处 `settled: true` 在 423 步）留作 09-08 的读数，不再是今天的形。**原文**：**装置档自由边界解不收敛，且不随预算单调**。`validate-worker-interp-device`，EAST，缺省 λ=1e-1：`free.residual` 400 步 5.6e-3、1200 步 6.8e-3（`settled: true`，423 步停）、4000 步 **6.1e-2**。**不随预算单调下降**，故不是步数不够。λ=3e-1 上同一条是 5.8e-2。★最早 λ=1e-3 时的 4.9e-8 **不可比**——那份属于一个塌成 2 cm 的位形（a_minor 0.0195 m 对要的 0.357 m），几乎没有东西要收敛 | `app/tests/validate-worker-interp-device.mjs`（2026-09-08 实测） |
| **F-34** 〔完成 2026-09-12〕 | fylite | ★★**假设被否掉，另一件事被量出来**（EAST 公共卡，目标 a=0.3116 m，`solve.maxIter` 全程 400，只改岭 λ；κ₂ 由 F-16 的 `start_cond` 报出）：λ=1e-3 κ 2.5562e8 · aMinor 0.3051（误差 2.08 %）· 残差 7.201e-3；1e-2 κ 2.7410e6 · **0.0195（93.75 %）· 1.627e-5**；3e-2 κ 3.0476e5 · 0.3091（**0.79 %**）· 3.843e-3；1e-1 κ 2.7431e4 · 0.3279（5.24 %）· 5.557e-3；3e-1 κ 3.0488e3 · 0.3572（14.63 %）· 5.833e-2；1.0 κ 2.7530e2 · 0.4167（33.72 %）· 7.111e-4。**①残差不随 κ₂ 单调** —— κ 扫六个量级，残差在 [1.6e-5, 5.8e-2] 里来回走，无次序：F-16 的 κ₂ 界定「输入扰动把电流推多远」，**不**界定自由解收不收敛，原假设否掉。**②最小的残差属于一个塌掉的位形**（λ=1e-2：aMinor 是目标的 6.3 %，残差全表最小，比位形最准那一档小 236 倍，而位形差 118 倍）—— 即**只读 `free.residual` 的验收判据可被一次塌解满足**，这是可操作的那一条。**③位形误差与 `psiRms` 都随 λ 单调**（0.79 % → 33.72 %；2.23e-3 → 4.73e-2，λ≥3e-2 段），而残差不随任何一个单调。闸：`app/tests/validate-free-residual-vs-shape.mjs`（新，两档 9 s）。★**推测（未测，不作结论）**：所要位形不是所设剖面的平衡，电流越把它按到目标上，不动点迭代越被推离自身解 —— 这能同时解释 ① 与 ②，要测就在固定 λ 上扫 `beta0`/`q95` 看残差是否随「目标与平衡的距离」走。 | `app/tests/validate-free-residual-vs-shape.mjs` · `app/tests/README.md`（2026-09-12 实测） |
| F-34（原问法，已被上一行取代） | fylite | ★自由解的 `free.residual` 为何**对设计那一步的预算非单调**（`solve.maxIter` 400 → 5.5566e-3 · 1200 → 1.2778e-9 · 4000 → 3.5187e-3，自由解自身 400 步不变）。F-16 交出的 κ₂ 只界定「输入扰动被放大多少」，不解释「哪一组起始电流让自由解收敛」。**下一步（可做，不需新数据）**：在同一装置上扫 λ，同时记 `start_cond` 与 `free.residual`，看收敛与否是否随 κ 单调；若不随，则决定它的是位形（分离面位置 / X 点距壁）而不是电流的条件数。 | `app/tests/validate-worker-interp-device.mjs` · `python/tests/test_start_design_conditioning.py` |
| **F-17** 〔完成〕 | kernel | **五个测试模块自 T-4 迁移起就收集不了**，因此一直没在跑：`test_assembly`（取 `fylite.scenario.model.sources`）· `test_nn_tglfnn_ukaea`（`…model.tglfnn_ukaea`）· `test_point`（`fylite.io.kfile`）· `test_selfcal`（`fylite.scenario.analysis.selfcal`）· `test_tomography`（`…analysis.tomography`）——都是那一串刀把公共仓模块收进 `tests/oracles/` 之后没有回指的残留。★**红着的闸子有人看得见，收集不了的没有**：此前每一次「全量通过」的读数都不含这五个模块。★★★**已关闭 2026-09-11（核对内核仓记录，非重做）——本条的建议「只差改 import」是错的**：那五个文件 **2026-09-08 已按用户裁定退役并删除**，逐条的退役账在内核 `tests/PHYSICS-MIGRATION.md`〈退役表〉（57 条判据分四堆，含「丢了什么」一栏）。**没有 import 可改，因为文件不在了。**★退役**不是无损的**，而那张表逐个点了名：`test_point` 干净（判据与被测物同时退役）；`test_assembly` 丢 4 条（装配层编排的 Python 侧收敛判据，门测判出入不判中间态）；`test_selfcal` 丢 3 条（含一条**方法学**判据：前提不成立时该方法会安静地报「无需校正」）；`test_tomography` 丢 11 条（反演质量：基在边界为零 · m=0 纯通量 · 伪 m=1 · 截断 · 死道 · 噪声稳健 · 一相机对两相机的分辨率），Rust 侧只有一条掩膜判据接住；`test_nn_tglfnn_ukaea` 丢 8 条（要的正是那个不在的模块，两条已捞出成 `test_tglfnn_ukaea_stays_out.py`）。★★**顺带一条工作树纪律，已记在那张表里**：删除落在一次讲 TGYRO 的提交（`f64d7ad`）里，提交说明一字未提 —— 共享工作树上 `git rm` 入的是**共用索引**，于是并行会话的提交把它顺手带走了。本仓写过「提交只显式 `add` 自己的文件」，但它防的是收进别人的改动；**这次是自己暂存的删除被别人的提交收走** | 2026-09-08 `--collect-only` 实测 · 2026-09-11 核对退役表 |
| **F-18** 〔完成 2026-09-13〕 | fylite | ★★**用户裁定「F-18 移除」**：③⑤ 两道死闸**退役** —— `app/tests/validate-q.mjs` · `validate-zerod.mjs` 已删（同题在内核仓且绿：`trace_surface` · Bosch–Hale `<σv>`），`app/tests/README.md` 与 `FYL-SDD-06` 的闸表随改。④ 的口径随本条一并移除：**不恢复**页面的「套用算例 / 预设入口」；`validate-flux-match` 本身未动，下文量出的 FUSE·ITER 档不收敛读数（30 轮 22260.58 %）留作记录，不再登记为待裁。**原文**：★★**2026-09-12 五道逐道定性（实跑）**：① `validate-guide` —— **夹具过期**（五页带接口 152 戳，源已 153）→ `tools/build-guide.sh` 重生成，**绿**。② `validate-worker-vertical` —— **真错 + 夹具漂移两层**：真错在内核 `code/vstab`：设计页的合成机器声明 `magnetics/flux_loop: []`，门的守卫只测键在不在，`device_magnetics` 对空表拒「declares no flux loops」，整条判据回 `null`（`verticalOf` 的 `catch` 把原因吞了，用补印原因的 worker 副本量到）——已修（空表 = 无环，答无行；钉 `an_empty_flux_loop_list_is_a_device_without_loops_not_a_refusal`）。修后闸子走到比对，对 09-05 录的 1e-7 夹具不合：γ 58.55 对 −0.033、a 0.855 对 0.572、q95 4.87 对 3.37、细丝 285 对 68，而 I_p 同。★★**2026-09-12 定性并重录（这一格因此绿了）**：**旧夹具录的是退化位形**，两个独立口径同向 —— ① 离所问更远：同一个 0.74 m 目标，旧 a 差 **−22.8 %**、今 a 差 **+15.5 %**；② 判据本身说不通：旧夹具 **k 为负**（−6.6e6，k 是去稳力系数）故 γ 亦为负（「稳」），而 κ = 1.6 的位形在无反馈下竖直稳定是这条判据不该给出的答案，今天 k/k_ideal = 0.347 < 1 · γ = +58.6 1/s 才是阻性不稳定区该有的形。★**两个候选解释都排除了**：`031115a` 只在消息带 `targetCurve` 时改行为，而本闸不传；`09d11a5` 把出厂岭 3e-1 改成 1e-1，而把 3e-1 显式传回去**并不还原夹具**（实测 a 0.8717 · q95 6.904 · γ 64.21）。★**重录之外还钉了两样**：本闸按候选序取第一台读得懂的装置（今天 `best`，a_target 0.74 m），此前装置清单一变就会**静默**换一台机器 —— 现在机器名与目标小半径进了夹具，变了按名报出来。③ `validate-q` · ⑤ `validate-zerod` —— **夹具过期（对 T-4）**：各调 `fylite_rs_trace_surface` / `fylite_rs_dt_reactivity`，两者自 T-4 起 `#[cfg(feature = "oracle")]`，不在发行件的 42 个导出里；这两道属 oracle 构建（内核仓）或须改走树门——**内核仓已各有同题**：`tests/_oracle.py` 的 Bosch–Hale `<σv>`（`test_fuse_benchmark.py` 比通道）与 `trace_surface`（`test_discharge_code.py` · `test_bootstrap_code.py` · `test_coupled_code.py`），2026-09-12 全量跑过均绿，所以公开这两道是**重复且已死**，去留待裁定。④ `validate-flux-match` —— **缺数据 + 夹具过期，2026-09-12 已修夹具、并把真读数量出来**：三处改好了 —— 参照表缺席改为**按名跳过**（此前抛 ENOENT 退 1，读者看到的是失败而事实是没有参照件；表另有两处：内核 `rust/fylite/testdata/reference/`、fydoc `cases/FYDOC-CASE-01-astra/corpus/`）· 撤掉的算例菜单改为**直接读菜单当年读的同一份真源**（`docs/examples/evolve/<档>.jsonld` 的 `parameters`，页面不驱动的名字报出来不算错）· 导出按 `#model-iofmt-evolve-json`（菜单项，`validate-beam.mjs` 一直这么按；此前点的是菜单本身，下载事件永远等不到）。★**接上参照表跑到底的读数**：〔甲〕FUSE·ITER 那一档**不收敛** —— 30 轮后最差相对通量差 **22260.58 %**（判据 2.00 %），最差点电子道 ρ̂ = 0.667，栏报「失败」，故无件可导出。★**两种解释未判**：(a) 算例文档没带够当年那个预设套用器供的东西 —— 页面现在**没有「套用算例」入口**（`FyScenario` 只暴露 part / boot / pages / whenDevices / redraw）；(b) 这一档在页面上真的不收敛。定它需要一条口径裁定（页面要不要重新有算例 / 预设入口）。**原记**：**缺数据 + 夹具过期**：参照表在私有 oracle 树（`tests/data` 软链本检出没有；表本身在内核 `testdata/reference/` 与 `FYDOC-CASE-01` 语料里），把表接上再跑（playwright 在 `/opt/node22/lib/node_modules`，本机 8767 起服务）则死在 `model-evolve-case`——该控件自合并 `2df8370` 起已不在页面上。**余**：③⑤改路或迁到 oracle 构建（内核仓已各有同题且绿，见下）；④ 见该格（不收敛的两种解释待裁）。②**已结**。**原文**：**node 闸子五道长期红**：`validate-flux-match` · `validate-guide` · `validate-q` · `validate-worker-vertical` · `validate-zerod`。逐道未定性（缺数据 / 夹具过期 / 真错，三者未分）。已知一处细节：`validate-worker-vertical` 的失败是 `design` 答案里 `vertical: null`——闸子要一个垂直位移判据，门没给 | 2026-09-08 全量扫描 | ★**状态 2026-09-12 由〔开〕改〔待裁〕**：①②④ 的动手部分当日做完（①绿 · ②定性重录 · ④夹具三处修好并量出真读数）；余下两件都是口径 —— ③⑤ 两道死闸**退役还是改走树门**（内核仓已有同题且绿），④ 不收敛的两种解释要先定「页面要不要重新有算例 / 预设入口」。
| **F-19** 〔阻塞〕 | fydoc | **装置文档不声明线圈工程限值**。七台自带装置的 `pf_active.coil` 无一带电流上限。后果：起始设计的真机闸只能拿 EAST #137985 **那一炮自己用到的**最大通道电流当界——那是**演示过的值**不是铭牌值，机器可能能交更多，也可能那一炮本就贴着限值。★与 **F-9** 是同一条数据缺口的两处露头（F-9 缺的是 `power_supply/current_limit_kA`，本条缺的是逐线圈的匝安上限）。★没有出处应留 `[TBD]`，不填估值 | `fylite_kernel` `tests/test_start_against_the_machine.py` 的判据取法（2026-09-08） |
| **F-20** 〔阻塞〕 | fylite · fydoc | **真机参照只有 EAST 一台**。起始设计岭缺省的物理判据全落在 #137985 一炮上——它是两仓里唯一带实测放电的机器（实测 LCFS · Ip · BRSP · 交付平衡）。其余六台只能拿合成目标（限制器包围盒 ×0.6，κ=1.6）扫，证据强度低一档：同一组 λ 扫下来 **ITER 在 3e-1 上反而最好，WEST 全平，CFETR 每个 λ 都不稳**——即 **F-15 那道坎在别的机器上没出现** | 2026-09-08 逐台实测 |
| **F-21** 〔阻塞〕 | kernel | **wasm 构建跨环境不可复现**。同一份源码，开发容器构建得 core 1 810 792 字节 / `55d8876d…`，另一台得 1 765 754 / `dca90b0c…`；**`kernel_ext` 也变**（615 886 → 615 110）而其源码那一役一字未动——差异因此锁在**构建环境**不在源码。两侧 rustc 均 1.94.1；容器内部可复现（同源多次构建逐位相同）。后果：`docs/note/app-provenance.md` 二进制表在两台机器之间来回改，`test_bundled_artifacts` 谁重建谁红（**2026-09-10 合并 develop 时又冲突一次**）。★★★**2026-09-11 诊断（单机，未关闭 —— 关闭要第二个环境）：一条假设已排除，并得到一个不需要重建、不需要第二台机器在手的判别法。**★**已排除：仓库路径**。本仓自己的源码路径进制品的形式是**相对**的（`strings` 实测：`fylite/src/stability.rs`），所以检出放在哪儿改不了字节 —— `rust/build.sh` 的 `--remap-path-prefix` 只映 `CARGO_HOME` / `RUSTUP_HOME`，而**本仓路径本来就不需要映**。这条假设本来最像，量一次就没了。★**容器内可复现，重新量过**：同一容器里真跑一次 `--wasm-check`（★第一次用了 `--wasm`，那不是有效开关，脚本在动 wasm 之前就退了，所以那次「哈希相同」量的是**没人改过的文件**，不是确定性 —— 记下来因为它差点被当成证据），核心 wasm **逐位相同**（`5bce04d886279f6ac35cb36b`）。★★**判别法：制品自己记着是哪个 rustc 编的**。核心 wasm 里嵌着 rustc 的**提交哈希**（本机 `/rustc/e408947bfd200af42db322daf0fadfe7e26d3bd1`，`rustc -vV` 报 1.94.1 · commit-hash `e408947bf` · **LLVM 21.1.8**）。本条现有证据只写了「两侧 rustc 均 1.94.1」——**那是版本串，立不住「同一个编译器」**：同为 1.94.1 的两份安装可以有不同的提交哈希与不同的 LLVM，而两者都改代码生成。所以拿两份分歧制品各跑一次 `strings -n 8 <wasm> | grep -oE '/rustc/[0-9a-f]+' | sort -u`：**哈希不同 → 本条是工具链差异而非构建系统缺陷，修法是钉住工具链**；**哈希相同 → 编译器出局**，剩下 LLVM / 链接器 / `wasm-opt` 版本 / 依赖锁文件。★**还差的一步就是这一步**，它不需要重建，只需要有人手上同时有那两个文件。★★**2026-09-11：判别法已经落进每一次构建**。`rust/build.sh` 的 `toolchain_json()` 把 rustc 版本串 · **提交哈希** · **LLVM 版本** · 目标三元组四项写进 `kernel-static.json`，于是两份构建记录并排就够判「是不是同一个编译器」，不必再 `strings` 制品。★**`RUSTFLAGS` 记的是摘要不是原文**（`rustflags_sha256`，前 16 位）：原文里带 `--remap-path-prefix=/root/.cargo=...`，而这份 json 被 `rust/fylite_runtime/build.rs` **逐字节嵌进** `libfylite_runtime.so` —— 记原文就等于把路径重映射本要藏起来的那条路径发出去，发布前的硬化闸当场拒了链接。**摘要判同异，够用；原文泄漏，不用。**★所以本条现在缺的**只有对侧那两个文件**，本机这边没有再能做的一步 | 2026-09-08/10 两侧实测 · 2026-09-11 本机诊断与工具链戳（内核 `35e25d1` · 修正 `4b6d6bc`）|
| **F-22** 〔完成〕 | kernel | **EAST 手工卡片仍写着构建机的绝对路径**。`9091776` 把 `/home/salmon/workspace/fydata/abox/experiment/east/137985` 从 `machine_desc/east/east_device.yaml` 改成了 `fydata:abox/…`，**同一字符串仍在 `machine_desc/east/fylite_device_east.json` 里**；而 EAST 的 facts 卡是从后者派生的（`tools/abox-to-facts.py` 自陈 EAST「手工卡片保持原样」），故它照旧出现在 `dist/facts/device/east.jsonld`。★★**2026-09-11 关闭**：路径在**三层**上，逐层改完并各配闸子 —— (1) 内核 `machine_desc/east/fylite_device_east.json` 与 `.yaml` 两份卡改成 `fydata:abox/experiment/east/137985`；(2) `tools/est2-dump-to-device.py` 加 `_logical_corpus()`，provenance 从此写逻辑路径而不是磁盘路径，**下一次重生不会把它写回来**；(3) ★真正的盲区在**公开仓自己的那份**：`fylite/dist/facts/device/east/east_device.yaml` 是手工维护的（`tools/abox-to-facts.py` 的 `HANDWRITTEN = {"east"}` 拒绝覆盖它），而 `east.jsonld` → `facts.rs` → 嵌进 `libfylite_runtime.so` 与 wasm 的是**它**；改完跑 `abox-to-facts.py east` + `facts-publish.py --flavour internal`，实测重建后的运行时里开发机路径为 0。闸子 `tests/test_machine_cards_carry_no_local_path.py` 由 3 条加到 **5 条**：新增两条读 `$FYLITE_PUBLIC/dist/facts/device/east/east_device.yaml`（一条判与内核卡逐字节同、一条扫本地路径前缀），逐条做过变异验证。★**这一条的教训写在这里**：我一度按「卡片已改」关掉它，而派生链的**第三层**还没动 —— 闸子当时只扫内核那一侧，所以答绿 | 2026-09-08 `grep` 实测 · 2026-09-11 三层逐层实测与闸子加宽 |
| **F-23** 〔完成〕 | kernel | **内核仓 pytest 长期红 9 条**（排除 F-17 那五个收集不了的模块）：`test_nn_surrogate` 4 · `test_benchmark_registry` 1 · `test_circuits` 1 · `test_jintrac_flattop` 1 · `test_loop` 1 · `test_tglf_selfconsistent` 1。全量读数 **9 failed / 1199 passed / 46 skipped / 9 xfailed**。★用「撤掉当役改动、重编、重跑」核过**是既有的**。★逐条未定性；〔推测〕`test_nn_surrogate` 那四条与权重不随仓发行是同一条线。★★★**已关闭 2026-09-11：九条红全清，但账要分清 —— 其中五条是「声明式跳过」，不是「修好了」。**实测（六个文件一起跑）：**48 通过 / 10 跳过 / 0 失败**。★**四条通过**：`test_benchmark_registry` · `test_jintrac_flattop` · `test_loop` · `test_tglf_selfconsistent`。★**五条是跳过**，且每条都自报理由与补法：`test_nn_surrogate` 的九个用例要 `sat2_em_d3d_azf-1.npz` 与 `epednn.npz`，本发行不带大模型权重；`test_circuits` 一条要 `rv6565.ddd`，本发行不带 Green 响应表（该文件另有一次提交明写「drop rv6565.ddd」，是有意退役）。★★**那条〔推测〕核实了，并且顺带核了 fydata**：用户告知模型数据在 fydata —— 实测 fydata 只有 `sat1_em_iter.npz` 与 `EPED1NNmodel.json`，**不是这两个名字**；而且 `sat1_em_iter.npz` 是 **fytok 格式**（`nn.load` 报「declares none of n_in, n_hidden, n_blocks, n_out, n_members, activation, xnames, ynames」），所以把 `$FYLITE_NN_DIR` 指过去仍是跳过 —— **按名拒绝，不是崩在 numpy 的 KeyError 上**（本会话加固 `Surrogate._REQUIRED` 的那一条正是为此）。★**结论**：这五条是**环境缺件**而非缺陷，补法写在跳过理由里；把它们读成「全绿」是错的，这一行就是为了不让人那么读 | 2026-09-08 全量扫描 · 同日 stash 对照 · 2026-09-11 逐文件复测并核 fydata | 2026-09-08 全量扫描 · 同日 stash 对照 |
| **F-24** 〔完成〕 | fylite · kernel | **T-C36：`ohm` 是差商，却按行进量比较**——`test_crosshost_replay` 的两条判据 2026-09-10 起红，而**它们自己的报错说错了原因**（写「a disagreement about the step」）。实测：**行进量全在机器精度上**（`te` 6.1e-16 · `ti` 1.2e-16 · `psi` 3.2e-16 · `q` 3.7e-15），只有 `ohm` 6.75e-12 与 `p_ohm` 2.9e-13 差四个量级。`ohm` 不是行进出来的：`scenario.rs` 用 `E_par = ratio*(psi[k]-prev[k])/dt` 现算，而本变体上 |psi| 33.35 对 |psi−prev| 中位 1.45e-4，**相消因子 1.07e5（最差节点 1.51e5）**；3.2e-16 × 1.51e5 = **4.8e-11 上界住了那 6.75e-12**。★所以这是**比较口径的范畴错误**，与 `ENTRY_OUT_KIND` 里 `noise` 那一行点明的是同一类；但 `ohm` **不是** `noise`（它是物理加热密度，「两边都小」不是它的判据），所以缺的是一个**新的 kind**：由状态行**差商**出来的实数行，判据是「不超过该状态行自己的一致度乘以相消因子」。落点在内核 `fyo::ENTRY_OUT_KIND` 与本仓 `engine/crosshost.py::compare`，**不是那个数字**。★★**故意留红**：放宽带宽正是该文件自己的说明禁止的（「a band chosen to swallow 6.2e-13 would agree with any future disagreement up to that size, including a real one」），而「多大的放大是可接受的」是**裁定**不是测量。机制已定，待裁的是那个上限★★★**同日追加的第二件，比第一件更要紧**：这条判据**没有任何东西核对两侧制品是否同源**。`compare()` 的 `environment` 记了 native 侧每个库的 `sha256`，wasm 侧**只记路径**（`{"host": "wasm", "artifact": str(WASM)}`，`crosshost.py:226`）。于是**一份过期的 wasm 读起来与一条真实的物理分歧完全一样**——而这正是一个会自我印证的假阳性：判据红了，报错说两个宿主不一致，而它们确实不一致，只是因为它们是两次不同的构建。★这不是假想：本轮为修自举 NaN 重建内核之后，native `.so`（17:51）与 wasm（14:06）**当场不同源**；上面那份相消诊断之所以站得住，是因为量之前**我手工比过两者的 sha256**并排除了版本偏差——**而判据自己不做这一步**。落点：`compare()` 记 wasm 的 `sha256`，并在两侧 ABI/构建戳不一致时**先按不同源拒绝**，而不是把差异算进 `worst` 交给带宽。★这一条**独立于**第一件，且应当先做：不先钉住同源，那个放大上限无论定成多少都在量一件说不清的事。★★**已做（2026-09-10 同日）**：省源台账补进两份 `.so` 的 sha256（此前只记两份 `.wasm`，于是只重建 `.so` 而不补 `--wasm-check`，台账与盘上 wasm 仍一致、闸子照样绿）；`test_bundled_artifacts` 连 `.so` 一起核（改一字节当场红）；`crosshost.compare()` 的记录补上 wasm 的 `sha256` 与字节数；`test_crosshost_replay` 三条比浮点的判据**在比之前**先对台账核同源，不同源就**跳过并说明**（实测：改 wasm 一字节，八条全部跳过并写明「NOT one build」，而不是报 `ohm` 分歧）。**余下待裁的只剩放大上限这一件。** ★★★**已裁定并关闭（2026-09-10，用户：选项 C）**：上限不是一个选定的带宽，而是**这一次运算自己的误差传播上限** `bound = C · reldiff(src) · max_k |src_k|/|src_k−prev_k|`，`C = 4`（因子取在最坏节点上，不逐节点取；C 明写为 `DIFFERENCED_C`，不藏在容差里）。**放大因子在两个宿主上各算一次，要求相差 ≤ 10 %、取较大者** —— 若两侧对**步长本身**有分歧（那才是真缺陷），因子就会分开，只信一侧的因子恰好会把最该抓的那种情形洗掉。实测：`ohm` 落在上限的 **0.023**，两侧因子相同（2.306e5），判词 `same`；注入检验 **1e-10 通过 / 1e-9 被拒**，即上限在 **~3e-10** 咬合，比它所解释的放大量紧三个数量级。落地：`fyo::ENTRY_OUT_KIND` 增两行 `differenced:psi,psi_prev_out`（14 → 16 行，`INTERFACE_DIGEST` 随之更新而 **`INTERFACE_REVISION` 不动**——只增不改，旧读者落到 `real` 路径即今日行为）；`crosshost.compare()` 认这个 kind 并新增 `differenced` 记录。公开侧 `test_crosshost_replay`：**14 通过 / 11 跳过**，两条有意的红转绿。记录见内核 `docs/note/cfedr-15ma-reproduction.md` §6.2 | 2026-09-10 实测（`crosshost.py:226`；native 17:51 对 wasm 14:06）|

| **F-25** 〔完成〕 | kernel | **取向已能推导，但 `signb`/`signq` 仍是 C ABI 的入参**（`c_api.rs:974` · `5212` · `5246`，缺省 +1）。D5 结案后（内核 `docs/note/cfedr-15ma-reproduction.md` §6.1）取向由 `mapping::orientation` 从带号数据按 COCOS 17 推出，于是这两个入参**同时存在两条真源**：宿主给的字面量，与从平衡推出的值。★现状不红只是因为仓内每个宿主都不再给它们；**契约面上它们仍然可被覆盖，而覆盖一个可推导量没有判据能拦**。待裁的是「还要不要留作入参」——留（则需一条判据核对宿主给的与推出的一致，不一致即拒）或撤（则动 ABI 契约，要走版本）。★★★**已裁定并关闭（2026-09-10，用户：「动 API 彻底解决 COCOS 一致性问题」）—— 撤。ABI 152 → 153。**三条导出去掉 `signb`/`signq` 入参；`surf20` 的 **slot 19（原保留位）改载带号环向通量** [Wb]，slot 6 的 `q` 带号，`surface_from_block` 用 `mapping::orientation` + `to_gacode` 推出取向。**两处拒绝而非缺省**：零通量 `-7`（此处零与「调用方没动那个保留位」分不开，故 `field_sign` 的「零映射到 +1」这条规矩**有意不适用**）· `neo_scal5` 前两位非零 `-8`。★**顺带查清的一件**：`signb`/`signq` **只有 `tglf_local` 与 `neo_inputs` 读**。于是旧的字面量点分成两类 —— **活的**（六处，改为推导；其中两处此前在门口就 `.abs()` 掉 `b0`，这才是下游必须「发明」取向的根因）与**惰的**（六处只为把 te/ne/ions 送进碰撞率的哑面，改用新的 `mapping::Surface::for_rates`，该构造器**根本无法表达取向** —— 要说「速率没有方向」，办法是让调用点无从可说）。★`geometry::Surface::signb` 是**另一个量**（Miller 求解器里 `|B|` 的符号，不是 GACODE 旗标），已加注区分；正向 `B_t` 下两者数值相同，这正是混用不会表现为错数的原因。★新判据册 `tests/test_orientation_has_one_source.py`：`mapping.rs` 之外**不得有取向字面量** · 读者集**钉在那两个函数**（每一条「翻 signb 只动旗标」的论证都是按这个集合量的） · 两处拒绝 · 经**制品块**量到的可分离性。**实测**：`cargo test --release --lib` 232 通过 / 0 失败；取向判据 9 通过；内核 pytest 见下一行 | ★★顺带一层：本炮之所以查得清，是因为上游把符号也交给预设——`profiles_gen` 三处 `abs()` 丢弃原件符号，而承载取向的 `kccw_*` 在 statefile 里缺席（实测计数 0），落到 DIII-D 缺省。**「入参」这个形状本身就是上游的形状**，所以这一裁定不是本仓的小事 | 2026-09-10 D5 结案时并记（§6.1 末） |
| **F-26** 〔完成〕 | kernel | **`test_evolve_fluxmatch_code` 的夹具在问一个没定义的问题**。它把**一次运行的压强**与**另一次运行的通量**配在一起，同时把自举打开；于是判据在核一个不自洽的态。实测：`bootstrap = 0` 时该轮返回 `ip` = **8.0000e5**（即请求值，五位吻合），说明求解路径本身是通的，**红的是夹具的口径不是代码**。待裁的是**这条判据应该断言什么**——（a）自举关、断言 `ip` 回到请求值（现已实测成立，但那就不再检验自举）；（b）两侧取同一次运行，自举开、断言自洽 `ip`；（c）退役该夹具，另立一条。★不擅自选：改判据的断言等于改它保护的东西。★★★**已裁定并关闭 2026-09-11（用户：选 D —— 拆成两条）**。先把机理量清楚：**输入梯子完全正常**（q 由内向外 1.000 → 2.600、ψ 0 → 0.759 Wb、a 0.45 · R₀ 1.85 · B₀ 2.0，教科书剖面）；开自举时该轮答 `ip` = **8.161850e6** 对请求 8e5（**10.20×**），因为**非感应电流自己就围出 8.3915e6 A**（`j_ni` 峰值 **1.03e8 A/m²**，高于合理量级约两个数量级），`v_loop` 被推成**负的**（−0.0677 V，**未钳位**）也抵不掉；输出 q 回来是 23.182 · 46.365 · 7.601 · 3.431 … 0.131 · 0.147 · 0.159 —— 轴上振荡、整体反转。★**病根是配对不是几何**：自举由 `dp/dψ` 驱动，而这份 ψ 从来不属于这份剖面；关掉自举，同一套夹具答 **8.000000e5**，请求值，六位吻合。★**且改不自洽**：匹配那一跑按构造就没有通量，所以不存在「对的 ψ」——探针是来补洞的，补法任意。**这条判据问了一个没有定义的问题。**★**落地（D）**：拆成 `test_the_loop_voltage_secant_lands_on_the_requested_current`（**自举关**，钉住割线命中：实测六位吻合，判据带宽 1e-4，比残差松四个数量级）与 `test_the_steady_current_round_reports_coherently_on_a_paired_state`（**保留配对、不断言 ip**，只判在任意态上都成立的那些：有限性 · 边界通量守恒 · frozen 报告 · σ > 0 · relax 单调 · 锯齿记录）。实测该文件 **6 通过**。★**D 的代价当场登记为 F-29，不由这次改判据吞掉** | 2026-09-10 实测（`bootstrap = 0` 对照）· 2026-09-11 逐项量清并拆判据 |

| **F-27** 〔完成〕 | kernel · fydoc | **EC 已裁定走清净室射线追踪（D1，2026-09-10 用户裁定），实现在另一会话，但本仓这一侧两件都没开始**。★**实测**：`fylite_kernel` 与 `fylite` 的所有远端分支里搜不到射线追踪的文件或提交（逐分支扫 `git ls-tree` 与 `git log --all`，按 `raytr` · `ray_tr` · `eccd` · `gray` · `射线追踪`；唯一命中是 CASE-09 语料里的 `gray.data` 与装置书的一张图），所以它在本会话看不见的地方 —— 另一个仓、或尚未推送。**登记在此是为了不让「已实现」三个字盖住余下的两件**：**（一）接线** —— EC 走哪道 `code/*` 门、参数面、拒绝条件，均未定；**（二）对拍缺参照件而非缺模型** —— EC-vs-TORAY 原先被 D1 阻塞，裁定之后阻塞换成了没有对拍口径：参考侧的 TORAY 本机试跑峰位差 0.023、吸收份额 0.487 未解释 `[TBD]`，要当参照须先恢复该运行的 `toray.in`。★清净室约束不随裁定放松：**不读 GRAY / GENRAY / TORAY 源**是 (b) 的定义的一部分。★**2026-09-11 补记（实现所在会话）**：搜不到是因为实现当时未提交——即内核 `rust/fylite/src/rfray.rs`（EC 与 LH 共用的冷射线核），设计与判据在内核 `docs/note/ec-raytracing.md` · `docs/note/lh-raytracing.md`；本条的（一）（二）两件仍未开始。★★**2026-09-11 晚补记（/goal「完成 L2/3」中途被用户叫停时的状态；内核已推 `2e8684d`）**：**（一）接线已完成**——EC 与 LH 共用 `code/rf_ray`（用户裁定）；`deposit`（ψ̄ 壳守恒沉积，全相对论吸收）· `current_drive`（Lin-Liu 2003 伴随法，香蕉区；Z_eff 无缺省、按名拒绝）· 锥形射线族 `rf_rings` / `divergence`（L2 束宽）· **`core_sources` 按 `code/beam` 的守恒算子落到平衡文档上绑定的梯子**（梯子上瓦数对壳 1e-9，可直接喂 `sources = table`；未绑梯子则带 ψ̄ 网格落在壳上）· L3 的 `beam_waist` / `beam_focus`（见 F-28）。**（二）参照件**：GENRAY EAST O/X（私有 B 类）· **BORAY 在本机 Octave 下跑作运行 oracle**（BSD-3，钉提交；X 半吸收点三码 GENRAY < 本线 < BORAY）· **METIS 认证存档 30 行 ECCD 带**（同点对 `ieccd` 与 `heating::eccd_efficiency`：ours/METIS 0.026–0.64，中位 0.35，log 相关 0.976；METIS 不给波，故二次谐波 X、N∥ 0.3 为明说假设，2 行无 X2 根按名跳过）已接；**TORAY 仍缺该运行的 `toray.in` `[TBD]`**。★顺带更正 `heating::ec_optical_depth`：照重印写的 n≥2 光深深 n 倍，已按 Bornatici 1983 原文表 IV 改。★内核 `caa95a4` 之后（③ 起）的改动已作 `2e8684d` 推到内核 develop；公开物理页 `09-heating` 只同步到 ② 沉积（`0c1b0d5`）。LH 余项转 **F-30**，待裁定项与 EC 残项转 **F-31**。★★**2026-09-11 更正口径（用户指出）**：本条（二）与本会话「复现 CASE-20 还缺什么」的答复把缺口说成「TORAY 输入未交付、要向设计方索取」，**过宽**。交付包里有 `fydoc` `cases/FYDOC-CASE-20-cfedr-hmode-15ma/corpus/H model 15MA 20240522/toray_inputs/` 下的 `echin`（13 241 B）与 `psiin`（328 342 B），两份都登在 `case.yaml` 的 sha256 表里；`echin` 表头带 1.9e11 Hz 与 6.3 T 的高斯数，束与场的物理定义在交付件里。**缺的只有控制名单 `toray.in` 一份**——而它非零关紧：只改其中 `NHARM` 就令驱动电流变 2.4 倍（内核 `docs/note/cfedr-15ma-reproduction.md` §1.6 实测）。所以（二）的结论不变（**缺参照件而非模型**），缺的是哪一件现在说得准。★★**2026-09-11 逐记录数完交付件之后，（二）的阻塞收成两条独立的，都实测**：**(i) 数值控制项缺席**——`toray.in` 确实不在交付目录里（按名与按内容全扫，命中只有 `HCD_profiles_from_statefile.nml` 的 `EC_3_*_toray`，那是输出）；但频率 · 场 · 剖面在 `echin`，功率与两路配置在 `notes.txt`（OMFIT 原始标签 `72EC96t213p 10invEC`）与 HCD nml，**谐波数由机器定死**（190 GHz 基频共振 $R = 7.240$ m 在等离子体内，二次谐波要 $R = 14.48$ m，在真空室外），所以真缺的是射线数 / 步长 / 停止条件与一对注入角的 2° 歧义。**(ii) ψ̄ 归一不同源**——TORAY 侧（`psiin`）用 ψ 图的真跨度 15.9199，本仓用 statefile 一维网格的 15.6752，**分母差 1.56 %**；EC 沉积按 ψ̄ 定位，所以逐点对比前必须统一，单独可解释约 0.003 的峰位偏移。★**本句 2026-09-11 同日改过一次**：初稿写作「平衡不同源 …… S1 是拿 g-file 冻的几何」，**那是错的** —— 查代码后确认本仓 CFEDR 链条一处都不读交付 g-file（S1 的参照表从 statefile 生成），而 `psiin` 的 ψ 图与 statefile 的**逐元素相同**。交付包里确有第二份平衡，那是交付件的事实，不是本条的阻塞；详见 **F-32**。两条都记在内核 `docs/note/cfedr-15ma-reproduction.md` §1.6.1–§1.6.3。★★★**2026-09-11 晚：(i) 解了，而且解法是用户指的路** —— 去 `FYDOC-CASE-21` 的 oracle 里找。那一组带着 `auxFILES/toray.in`（且**自陈是 ONETWO 按缺省机器生成的**，所以 CASE-20 的控制项可按同一组缺省推断）、`toray_inputs/echin` · `psiin`，以及 **TORAY 自己的输出 nc**（30 条射线全轨迹 + 201 格沉积与驱动电流）。于是 **EC 的 B 类参照件不再缺**：冻结 `rust/tools/gen_cfedr_toray_reference.py` → `testdata/reference/cfedr_toray_20ma.txt`，判据 `tests/test_cfedr_toray_oracle.py`（6 过 1 xfail）。实测：驱动电流两侧差 **9 %**（1.7256e-2 对 1.9031e-2 A/W）、轨迹在五个匹配 ψ̄ 面上差 **≤ 4 cm**、单程吸收 1.0000 对 0.9990、沉积峰 ψ̄ 差 0.03；模式由 nc 的 |N| 反推为 **O**（本仓 O 档差 1.4 %、X 档差 22 %）。★同时量出一个符号缺陷 → **F-33**。★`echin` 表头七槽与 TORAY 两个发射角的约定也由此解码，CASE-20 的「2° 角歧义」判定为标签与 `echin` 之差（TORAY 读 `echin`，所以是 98 / 215）。 | 内核 `docs/note/cfedr-15ma-reproduction.md` §6.3 · 2026-09-10 逐分支实测 · 内核 `docs/note/ec-raytracing.md` §4.3–§4.5 |★★★**2026-09-11 关闭：（一）（二）两件都完成了。**（一）接线由并行会话落地（`code/rf_ray` 的 `deposit` / `current_drive` / 锥形射线族 / L3 / 梯子映射）。**（二）对拍不再缺参照件，而且是两份**：① `FYDOC-CASE-21` 的 TORAY oracle（输入 + 名单 + 它自己的输出 nc）——驱动电流比 1.046 · 轨迹亚毫米 · N∥ 差 1.5e-4（修 F-33 之后）；② **CASE-20 自己的交付件**：`HCD_profiles_from_statefile.nml` 里 TORAY 的 `EC_3` 沉积与电流（71.93548524268196 MW · 1.219 MA），冻结件 `cfedr_ec_15ma.txt`、判据 `tests/test_cfedr_ec_against_the_delivery.py`。**实测（一条笔形射线，发射由 `echin` 读出）**：驱动电流 **+1.240 对 +1.219 MA（+1.7 %）**、峰位 ρ 0.3588 对 0.3450、吸收 71.94 MW 单程全吸收；rms 宽窄 27 %，成因是一条射线对 TORAY 的 30 条 `.gauss.` 射线（可用锥扫发散角量，带一个自由参数）。★模式非挑选：`echin` 第 7 槽推断为 O，而 X 支在本平衡上根本不吸收（0 %，停在 ρ 0.93），两条独立证据同向。★比较跑在 ρ 上，使包里两个 ψ̄ 归一之差（1.56 %，F-32）相消 —— 这也是 F-32(b) 的实际落地。详见内核 `docs/note/cfedr-15ma-reproduction.md` §1.9 | 内核 `docs/note/cfedr-15ma-reproduction.md` §1.9 · §6.3 · `docs/note/ec-raytracing.md` §4.7 |
| **F-28** 〔**已关 2026-09-13**（用户裁定「close F-28 on the analytic tests」）：可复算束宽参照由解析判据 L3-1…L3-8 担当——文献里不存在输入公开到能复算的另码实算（15 篇逐篇判定见内核 `docs/note/ec-raytracing.md` §4.6「参照件调研」）；日后若出现带完整输入的对标件，作补充不作前提。原状态：阻塞 —— 缺可复算束宽参照件；Poli 2018 · Prater 2008 期刊版 2026-09-13 入库、接受稿退役。★★**2026-09-13 逐篇查过 11 篇 L3 全文：无一同时公开完整输入（平衡 + 束初值）与定量束宽**——最近者 Kravtsov–Berczynski 2007 §7 线性层斜反射（输入全，但是作者自己的 Riccati、L3-4 已解析覆盖、宽度约定有 √2 歧义）· Farina 2007 GRAY ITER 图 2（束初值全、平衡未公开）· Prater 2008 期刊版明言未收集束宽 · Poli 2018 的 GRAY / WKBeam / 全波对标均无输入。用户裁定「再取几篇全文重找」：Figini 2012 EPJ Web Conf. 32 01011（CC-BY，站点对脚本出验证码）· Weber 2015 EPJ Web Conf. 87 01002（WKBeam，同）· Poli 等 2001 Fus. Eng. Des. 53 9（闭源，MPG.PuRe 拒脚本）——三篇全文**同日由用户入库**并逐篇查过：仍**无一可全复算**。Poli 等 2001 FED 图 2（透镜型介质）可在声明假设下复算、并由已发表曲线定出 W 为场 1/e 半径（匹配宽 9.49λ），但与 L3-3 抛物通道同一判据；图 3（磁化平板、沿传播方向抛物密度）缺 ω_c/ω · λ · W(a) · R(a)，其闭式解只在 **Poli · Pereverzev · Peeters 1999 *Phys. Plasmas* 6 5（10.1063/1.873254）**——**不在现有解析判据之列**；★★**同日全文由用户入库，其 §III 闭式已落为内核判据 L3-8**（`rfray.rs` `across_an_inhomogeneous_magnetised_slab_the_beam_follows_poli_1999_section_3`）：冷等离子体平板、B 沿 z、密度按 P99 图 1 随 x 变，O / X 模、与 B 成 30° 的束椭圆（平前沿与聚焦各一），横向复曲率对闭式 5e-9–9e-8，x 行恰零，对照「冻结的均匀介质速率」差须大于误差千倍（判据不能退化）。★它是**文献里的解析判据**，不是另一个码的实算：F-28 所要的 B 类实算对标仍无可复算输入；Figini 2012 的 ITER Scenario 2 只给标量、剖面与磁面仅有图（ITM CPO 库），未发表束宽，另要相对论吸收；Weber 2015 无任何束输入〕 | kernel | **EC 高斯束 / 量子光束追踪（L3）**。2026-09-11 用户确认内核自写清净室射线追踪，L2 只做冷等离子体哈密顿射线 + 沿射线吸收 + 伴随法 ECCD（单射线 / 束中心线，锥形射线族近似束宽）；**束宽演化、衍射与聚焦（高斯束 / 量子光束追踪）不在 L2 里**，登记于此。与 F-27 同一条线：F-27 管 L2 余下的接线与参照件，本条管 L2 之外。★清净室约束随条目走：**不读 GRAY / GENRAY / TORAY 源**，只按公开文献实现；GRAY 移植的暂停裁定不因本条解冻。★开工前提：L2 过判据（解析极限 · 闭式光深 · 效率），且有一个可复算的束宽参照 `[TBD]`。★★**2026-09-11 已实现（内核 `2e8684d`）**：11 篇方法 / 程序全文入库读完后定走**束追踪**——笛卡尔 3×3 复曲率 Ψ = ∂N/∂x 的矩阵 Riccati 方程加约束 Ψv = dN/ds（Pereverzev 1998 式 97、Poli 2001 式 8–9 的本仓写法；Appleton–Hartree 在真空正则，免 TORBEAM 的真空–等离子体匹配；3×3 形式自带 Balakin 2007 所说 2×2 约化漏掉的射线弯曲项）。`rfray::trace_beam` · `ec_deposit_beam`（中心射线吸收按 Gauss–Hermite 横截面分到 ψ̄，Balakin 2008 式 15）· 停因 `ParaxialLimit`（窄宽跌破一个真空波长）；门 `beam_waist` + `beam_focus`（成对、无缺省）· `beam_nodes` · 字段 `beam_width`，与 `rf_rings` 互斥。**「可复算的束宽参照」改由解析判据担当**：真空像散束 1.1e-8 · Pereverzev 1998 §VI C 均匀磁化垂直传播 O / X 6.3e-8 / 2.8e-8 · 抛物通道匹配束与呼吸 · 线性层九元全比 1.3e-8 · 束对邻射线的 Jacobi 恒等式 7.9e-6（样条介质上两套独立实现）；Prater 2008 未发表平衡与束初值，只作宽度量级。**余**：B 类实算对标（TORBEAM / GRAY）无可复算输入 `[TBD]`；无像差阶不含窄束的 N∥ 谱展宽与截面上不均吸收（Balakin 2008：ITER 上窗口宽度为 TORBEAM 的 1.38–1.71 倍）——GRAY 式带 N∥ 谱的射线族**已裁暂不做（F-31 ④，2026-09-11）** | 内核 `docs/note/ec-raytracing.md` §4.6 · `docs/note/rfray-extracts/l3-*.md` |

| **F-30** 〔可做 —— 2026-09-13 全文已齐：Karney & Fisch 1979 · Fisch 1987 · Ignat 1994 期刊版入库挂 Zotero，PPPL 报告版退役〕 | kernel | **LH 的 L2 余三段：LH-② 线性吸收 · LH-③ 一维准线性 Fokker–Planck 与驱动电流 · LH-④ 接门与参照**。2026-09-11 立（/goal「完成 L2/3」被用户叫停时的剩余）。LH-① 冷射线（Stix 多组分、慢波反向波发射规则）与 ①b 托卡马克介质 / 声明的边缘反射已实现，对 GENRAY EAST 2.45 GHz 四射线过 B 类。**全文**：Bonoli 1984 · Bonoli & Englade 1986 · Brambilla 1976 · Peysson 2012 · Rönnmark 1983 · Xie 2024 · Yu 2026 **在库**；**缺** Karney & Fisch 1979 · Fisch 1987 · Ignat 1994（LSC 源码按裁定不读）。逐页渲染核对过的公式摘录在内核 `docs/note/rfray-extracts/lh-*.md`。**LH-②**（设计已定）：Bonoli 1984 式 (20a)/(21a) 的电子 Landau 阻尼作 P 元反厄米部分，`D_I = Im P · ∂D/∂P` 取在射线所走的同一个 Stix D 上，沿射线功率系数 2α_s、`α_s = (ω/c) D_I sgn(∂D/∂ω|_k) / |∇_N D|`，与 EC 共用 Simpson 沉积步进（EC 读数须逐位不变）。★坑：**不能用垂直 k_I**——LH 群速度几乎沿 B，垂直率约为沿射线率的 30 倍；v_e 取 (T/m)^{1/2}、√2 在 ζ 里（Bonoli (40a)(40b) 的 5.7 / 7.0 恰好复现）。判据：摘录里独立算的局地点表（n_e 1.91e20 m⁻³、9 T、4.6 GHz、D、N∥ 3；T_e 2 / 3 / 5 keV → α_s 1.652917e-2 / 1.020860 / 20.89656 m⁻¹，λ→0）· 均匀板上的精确指数 · 带边缘反射的多程功率守恒 · 私有 GENRAY 四射线的功率列（B 类，只判趋势）；离子 Landau 与碰撞阻尼的系数 `[TBD]`。**LH-③**：Bonoli–Englade 一维 v∥ FP，E∥ = 0 时零通量闭式 `f ∝ exp(−∫w dw/(1 + D w³))`，D_ql 由射线功率按能量守恒定（δ 函数分 bin 有因子 2 之歧），与射线吸收（∂f/∂v∥）迭代到电流不变。★单位坑：该文 v_e = (2T/m)^{1/2}，教科书与 Bonoli 1984 取 (T/m)^{1/2}，ν0 差 2^{3/2}；该文式 (35b) 疑缺 ½。判据：无射频回 Maxwell · 强 D_ql 平台 · Alcator C / PLT 表（η̃ 0.121 / 0.099 可复算，各表 P_in = P_d + P_coll + P_τ 逐行闭合）。无陷俘、无 E∥ 协同、无径向扩散（与原文同）。**LH-④**：`code/rf_ray` 现对只有 `lh_antennas` 的计划**按名拒绝**；要补天线行的发射位置、N∥ 谱与功率权（Bonoli–Englade 的分 bin 法；Brambilla 1976 没给阵列谱的归一）、极向 N 与反射面参数——路径待 F-31 ①。参照：Peysson 2012 的 LH 算例**未给 q 剖面**（摘录由其电流剖面推得 q0 ≈ 0.676），BORAY-3D 的 15° 算例与 Peysson 的任一算例都不对应，只能各自复现。★★**2026-09-12 LH-④ 接门完成**（内核 `case::rf_ray_lh`，接口摘要 f6d06d0d，修订号不动：只加槽）：`code/rf_ray` 对只有 `lh_antennas` 的文档按 DD 的 `antenna/row/n_phi` + `row/power_density_spectrum_1d`（FYO-ADR-10 D-3）每 bin 发一条慢波射线（`row/position` 为发射点），`deposit` 时逐射线走自洽准线性回路，`core_sources` 的 `electrons/energy` 落壳；判据：四 bin 1 MW 发射 = 吸收 + 圈外 + 剩余到 1e-6（吸收 0.904 MW，MW 级饱和如 §4.4 所量）。★同日续：`row/n_pol`（本仓声明的极向切向意义）与每壳驱动电流 $-e n_e v_{te}\int u f\,du$（`j_shell` · `core_sources/j_parallel` · `i_driven`；解析算例 0.083 A/W）也接了。2-D 谱 `power_density_spectrum_2d`（每格一条射线）同日也接。**余**：反射参数（D-4）· 一维模型之外的 (5+Z_eff)/陷俘修正 · 参照 2–5 的对拍（GENRAY EAST 2.45 GHz 四射线 2026-09-12 经门过 B 类：(R,Z) ≤ 1.1e-4 m、N∥ ≤ 1.1e-3） | 内核 `docs/note/lh-raytracing.md` §4.3–§4.5 · `docs/note/rfray-extracts/lh-*.md` |
| **F-31** 〔可做 —— 五项已裁；余弱相对论极化一项，Krivenski–Orefice 1983 · Shkarofsky 1966 期刊版在库（2026-09-13 核）〕 | kernel · 待裁定 | **射频线（EC / LH，L2–L3）的五项待裁定与 EC 残项**。2026-09-11 立。★★★**2026-09-11 用户逐条裁定（建议与裁定一致）**：**① 现在就提 fyo 治理**（提案 v1 已起草：内核 `docs/note/ec-raytracing.md` §7.1 —— 写全本仓约定、给出与 TORAY 两角的算术换算、向 fyo 提三件：DD 的 `steering_angle_pol/tor` 是否同义、模式要一个「支」而不是 `o_mode_fraction` 那样的「份额」、LH 行的声明路径；登记在本表 B 组 **O-4**）· **② 取 (a) 只做电子 Landau**（F-30 的 LH-② 模型因此确定，设计见内核 `docs/note/lh-raytracing.md` §4.3）· **③④⑤⑥ 全部维持现状**（LH 的 L3 不进表 · GRAY 式 N∥ 谱射线族暂不做 · ECCD 香蕉区外修正读作 L2 之外 · 多于 4 种离子仍按名拒绝）。**本条余下只剩弱相对论极化一项**（全文不在容器，做不了）。原五项与各自理由（含裁定列）见内核 EC 页 §7.0。原文如下：①天线表路径——走 fyo 治理，还是先用 `fylite:` 前缀的临时槽（EC 现用 `beam/fylite:angle_pol` · `fylite:angle_tor` · `fylite:mode`，理由在内核 `fyo.rs` 表注）；②LH-② 模型——只做 (a) 电子 Landau（本线读法：LSC / Bonoli–Englade 级的标准做法），还是以 (b) Rönnmark 完整热张量为主、(a) 作极限判据；③LH 的 L3（二维 / 三维反弹平均 FP、全波与模式转换、SOL 耦合、涨落展宽）进不进本表；④要不要在 F-28 之外做 GRAY 式带 N∥ 谱的准光学射线族；⑤ECCD 香蕉区以外的碰撞修正与动量守恒修正——本线读作 L2 之外，请确认或推翻。**EC 残项（不待裁定，可直接做）**：①弱相对论极化选项（Krivenski–Orefice 1983 + Shkarofsky 1966；疑为 X2 对 GENRAY 晚 12 mm 之因）—— ★**本会话做不了**：两篇全文在用户的库里、不在本容器，公式摘录也没有这两篇；无原文即无从实现（不编公式）。②离子多于 4 种的归并规则（现为按名拒绝）—— 归并规则本身是一条口径裁定，见待裁定 ⑥。③~~TORAY 参照缺 `toray.in`~~ —— **2026-09-11 解**：`FYDOC-CASE-21` 带名单与 TORAY 自己的输出，已成 B 类 oracle（见 F-27）。④~~公开物理页 `docs/physics/09-heating.md` 同步~~ —— **2026-09-11 做完**：新增〈伴随法 ECCD〉〈束宽：锥形射线族与束追踪〉〈沉积映到输运梯子〉三节；介质段补 $\sigma_{Bp}$ 由文档推出（含 {eq}`eq-p09-sigmabp` 与那句「符号只影响电流方向」的**更正**）；门表补 `current_drive`/`zeff`/`trap_theta`/`rf_rings`/`rays_per_ring`/`divergence`/`beam_waist`/`beam_focus`/`beam_nodes` 与 `current_shell`/`cone`/`beam_width`/`current_driven`/`current_outside`；拒绝段改写（`current_drive` 不再按名拒绝，改为缺 $Z_{\rm eff}$ 或不与 `deposit` 同开时拒绝、锥与束互斥）；范围句与〈适用域〉第 9 条逐条改对。公开 pytest 2195 passed。★同日的迭代提速（RF 判据墙钟约 60 s → 5.4 s：吸收子本地 Bessel 级数、`lin_liu_f` 合指数、τ > 60 不算电流、独立判据并行）已在内核 changelog | 内核 `docs/note/ec-raytracing.md` §6–§7 · `docs/note/lh-raytracing.md` §7 · 内核 `changelog.md` |
| **F-32** 〔完成〕 | kernel · fydoc | ★**编号说明**：本条 2026-09-11 立时取号 F-30，同日 `origin/develop` 已把 F-30 / F-31 给了射频线（`2e8684d`），故改号 **F-32**；F-30 不复用。 **交付的 `gfile_efit` 不是 CASE-20 那次运行用的平衡 —— S1 的几何冻结口径因此要重判。**★**实测（2026-09-11，逐记录读交付件）**：`toray_inputs/psiin` 的 `rmaxis` · `zmaxis` = 8.27683 · 0.909335 m，与 ONETWO `summary` 的「R at mag. axis 827.7 cm / Z 90.9 cm」**逐位吻合**；交付 `gfile_efit` 给 8.28474 · 0.908654，**不吻合**。`simag` −22.9865 对 −22.7132（深 1.20 %），**ψ 跨度 15.9186 对 15.6478，+1.73 %**；计算盒（`rdim` · `zdim` · `rcentr` · `rleft` = 8.6 · 16.0 · 7.8 · 3.5）与 `bcentr`（6.3 T）两侧相同。`psiin` 的 F(ψ) 边缘 49.14 = 7.8 × 6.3 逐位吻合，q 边缘 11.79 与 `summary` 吻合 —— 即**参考侧自己的平衡在 `psiin` 里**。★**后果**：S1 拿 g-file 冻几何，S5 的逐量差把几何当共同基底；1.73 % 的 ψ 跨度差会改 ρ 映射，所以（a）EC-vs-TORAY 的峰位差 0.023 必须在同一平衡上重算，（b）S1 的「几何一致」结论要说明是对哪一份平衡成立。★★**2026-09-11 同日判定，前提已消**：statefile 站在 `psiin` 一侧，而且是逐元素判的 —— `psiin` 的 129×129 ψ 图与 statefile 的 `psi` **16 641 点逐元素差 ≤ 1.3e-6**（单精度舍入量级）， `psiaxis` · `rma` · `zma` · `btor` 逐位相同。交付 g-file 的 ψ 图与它们**最大差 0.320 Wb/rad** （≈2 % 跨度）且非常数偏移（逐点比值 0.935–1.048）。**所以本条的量不是「跨度差 1.73 %」** （那是表观差，混进了 `psibdry` 取面不同这第二件事：statefile −7.3113 对 `psiin` −7.0679， 而两者 ψ 图相同 —— 标量声明之差，不是场之差），**而是 ψ 图逐点 2 % · 磁轴 7.91 mm · `psiaxis` 1.20 %**。★★**2026-09-11 同日查代码后，(a) 撤销、本条降级**：本仓 CFEDR 链条**不读交付 g-file** —— S1 的参照表由 `rust/tools/gen_cfedr_reference.py` **从 statefile 生成**（`psi_rz` 最小值 −22.984310 即 statefile 图的最小值），`cfedr_gates::s1` 追的是这张图，归一用 statefile 的 `psiaxis` / `psibdry`；全树搜 `gfile` 只命中通用几何档与 `outer.rs` 的合成夹具。 **所以那份 g-file 是交付包里没被用到的第二份平衡**：它是关于交付件的事实（已写进 `fydoc` 案卷页），不是本仓复现链上的阻塞。本条余下**只有 (b)**：EC 逐点对比前统一 ψ̄ 归一 —— TORAY 侧用图的真跨度 15.9199，本仓用 statefile 一维网格的 15.6752，**差 1.56 %**， 按 ρ ~ sqrt(ψ̄) 单独可解释约 0.003 的峰位偏移。 ★★**而查这一条时判明了 S1 的另一件**：S1「体积亏 3.57 %」**不是 V′ 偏低** —— ONETWO 的 `volume` 是交付 LCFS 围线围出的（Pappus 对到 6.7e-7），那条围线在 ψ = −7.0666， 不在 `psibdry` = −7.3113；梯子的 1484.19 对它追的那个面是对的（独立收敛积分 1484.74， 差 0.037 %）。顺带量出 statefile 一维通量网格相对它自己二维图的压缩常数 **1.015610**。 闸子 `cfedr_gates::s1::s1_the_volume_target_is_the_boundary_contour_not_psibdry` 已落。 ★★**2026-09-11 测完：不是那个压缩。** 判据 `s1_metrics_under_the_two_flux_mappings` 两种落位各跑一次 ——把交付 `npsi` 表拉到「真实」psi_N 之后 `hcap` 中位 −1.253 → **−3.267 %**、`gcap` +1.810 → **+5.772 %**、`q` −0.699 → **+4.963 %**（最差 4.43 → 14.84 %），**全部变差**；只有 `r2cap` / `xhm2` 的最差好 2.7 倍。`q` 是定 ρ_tor 的那一列，所以标称落位是对的，度规中位与压缩无关 —— **一个候选被实测排除**，条目本身仍开。★由此交付件那条不一致收紧成：**statefile 的 `F`/`q`/度规与标称 ψ 网格自洽，而 `psivolpnpsi` 只有乘 1.015610 才对得上**，即不一致在体积那一列。★★**那条「不能归因」的变动同日归因完了：动的是记录，不是数。** 现象是 `gcap` 中位今日实测 +1.810 %，而 09-10 记 −1.75 %（同 61 层，符号翻了），`r2cap` / `xhm2` / `q` 三项也整列反号。归因法：把主表判据在 2026-09-10 的提交 `c8be9d6` 上**另开工作树冷编重跑**，梯子 61 / 121 / 241 三档 —— 六个中位与六个最差**与今日逐位相同**，故**没有任何提交动过这些数**；旧记录**只有中位列抄错**（最差列 19.2 / 10.3 / 5.5 · 31.5 / 20.0 / 12.5 · 1.31 · 1.24 · 4.43 · 0.012 逐位对得上）。旁证同向：`surfaces.rs`（`gm2` 的出处）与 `kernels.rs`（那个 `gradient`）自 09-10 起一次提交都没动。★**补的钉子** `s1_the_metric_medians_are_pinned`：把默认 61 层的六个中位逐个钉到 ±0.02 个百分点，并在 `S1_LEVELS` 被设时拒绝运行 —— 原来的阈（|中位| < 3 %）对「六个数一起换符号」是盲的，这正是缺陷能活一天的原因。钉住不等于认定正确：两个中位的来源仍 `[TBD]`。成因 `[TBD]`（工作假设：OMFIT 外环的另一轮迭代；`notes.txt` 的 `trunk_iterations/.../V1_0522` 与之相容，不构成证据） | 内核 `docs/note/cfedr-15ma-reproduction.md` §1.6.1 · §1.6.3 | ★★**2026-09-12 结案**：(b) 那一句写死了 —— S1 的一致性是**对 statefile 那一份平衡**的一致性（参照表由 `gen_cfedr_reference.py` 从 statefile 生成、归一用它自己的 `psiaxis` / `psibdry`），交付的 `gfile_efit` 与它 ψ 图逐点差 2 %、磁轴 7.91 mm、`psiaxis` 1.20 %，而**本仓这条链一处也没读它**。同时写进判据抬头 `s1_metrics_against_onetwo` 与内核页〈结论先写〉，因为此前这句只在调查节里，读结论的人看不到。(a) 早于 2026-09-11 撤销。
| **F-33** 〔完成〕 | kernel | ★★★**射线追踪的极向场符号写死成 +1，而本仓声明 COCOS 17（$\sigma_{Bp} = -1$）—— 实测差 25 % 的 $N_\parallel$。** 2026-09-11 由新到手的 TORAY oracle（`FYDOC-CASE-21`）量出。**证据三条，互相独立**：(1) 拿冻结件的 ψ 图在 **TORAY 自己那条射线**上的六个点重算 $N_\parallel$：$\sigma=+1$ 给 0.479…0.655、$\sigma=-1$ 给 0.659…0.786，TORAY 是 0.674…0.804 —— **落在 $\sigma=-1$ 上**（1–3 %，残差是我那份粗糙的双线性梯度）；(2) COCOS 17 的定义本身就是 $\sigma_{Bp} = -1$（3·4·7·8·13·14·17·18 一组），而 `mapping::COCOS = 17`；(3) 物理：两份 statefile 都 `Ipsign = 1` 且 `tot_cur` 为正，正向环向电流在自己外侧给 $B_Z < 0$，而本数据 ψ 向外增大（`psiaxis` −26.5 < `psibdry` −7.1），于是 $B_Z = \sigma\partial_R\psi/R < 0$ 要求 $\sigma = -1$。★**为何一直看不见**：EC 页原写「符号只影响电流方向」——**那句不准确**，翻的只是极向那一半，$B$ 的方向因此转了 13°，$\cos\theta$ 跟着变；而本算例单程几乎全吸收（1.0000 对 0.9990），沉积位置对 $N_\parallel$ 不敏感，峰位仍在 ψ̄ 0.03 内 —— 掩盖它的是这团等离子体，不是判据。**LH 不会被掩盖**（群速度几乎沿 $B$）。★**修的代价要一并付**：`rfray` 自带的 22 条判据与 L3 的七条解析判据里，凡夹具自己按 $\sigma=+1$ 造 $B$ 的（如 `rfray.rs:2609`）必须同批改，否则只是把不一致从一处搬到另一处；③ 的电流方向判据要重看（本算例两档都给 $N_\parallel>0$，方向侥幸未变）。★判据已钉：`tests/test_cfedr_toray_oracle.py::test_our_parallel_index_matches_torays`，**strict xfail** —— 修好即转绿、strict 标记转红，逼修的人回来撤标记 | 内核 `docs/note/ec-raytracing.md` §4.2 · §4.7 · `rfray.rs:1145` |★★★**2026-09-11 同日关闭，并验到四位。** 修法：$\sigma$ 不再是字面量，而是 `PsiMediumInput::sigma_bp`，由 `code/rf_ray` 的门**从文档推** —— $\sigma = -\mathrm{sign}(I_p)\,\mathrm{sign}(\psi_\mathrm{bnd}-\psi_\mathrm{axis})$（安培定律）；文档不声明 `ip` 时仍取 +1 **但 notes 里写明是假设**，不从没有取向声明的文档里读出取向（D5 的规矩）。★**修后同一 oracle 实测**：$N_\parallel$ 差 **1.5e-4**（修前低 25 %）· $|N|$ 差 0.02 % · 轨迹 R 差 **≤ 0.3 mm**、Z **≤ 0.6 mm**（3.5 m 路径）· 最深 ψ̄ 差 0.07 % · 驱动电流 **1.9910e-2 对 1.9031e-2 A/W，比 1.046**（修前 0.907）。★**判据**：`rfray` 48 条 + 门的 Rust 判据 + python 门 14 条 + oracle 8 条**全绿**；strict xfail 已撤（它转绿了，标记随之撤掉 —— 那条机制按设计生效了一次）。★★★**第二检验同日做完，并揭出一件更该记的**：EAST g-file 夹具此前**把 ψ 图整张取负**（`psi_rz.map(|v| -v)` 连 `simag`/`sibry` 一起）—— 取负不改 ψ̄、只翻 $B_\mathrm{pol}$，**那就是 σ=−1，写成了数据变换而不是约定**；于是该夹具里两个错误互相抵消，它的 $N_\parallel$ 判据一直以 1e-3 通过。现在取负去掉、σ 由该参照自己的 `current` 推出，**48 条判据仍全绿**且更准：介质对 GENRAY 自报的 $B$ 差 **4.05e-6**、ec_o 的 $N_\parallel$ 差 **8.99e-6**、(R,Z) 差 5.2e-5 m。★**再加一条不经介质的直算**：GENRAY 自报的 $(B_R,B_Z)$ 沿射线五点对两支公式，σ=+1 两个分量符号都反、σ=−1 同号且主分量差 0.07 %。**推导由两套独立数据各自证成**（CFEDR/TORAY 与 EAST/GENRAY）。★**这一条的教训**：22 条自带判据全绿而符号一直错，因为夹具不声明 `ip` 且自己按同一符号造 $B$ —— **一条判据能发现的东西以它发现的东西为界**，跨实现的参照件是不可替代的。 | 内核 `docs/note/ec-raytracing.md` §4.2 · §4.7 · `rfray.rs` · `tests/test_cfedr_toray_oracle.py` |

| **F-29** 〔完成〕 | kernel | **`code/steady_current` 的自举路径全仓没有判据在跑**。2026-09-11 关闭 F-26 时按用户裁定（选项 D）把唯一驱动它的那条判据改成 `bootstrap = 0` —— 因为开着自举的那套夹具**问的问题没有定义**（见 F-26）。★**这个洞是本来就有的，不是这次改出来的**：实测全仓只有 `tests/test_evolve_fluxmatch_code.py` 调 `code/steady_current`，而 `stationary.rs` 里只有 1 条 Rust 单测。所以在此之前，自举路径的唯一「覆盖」是一条**注定失败**的判据 —— 那不是覆盖。★要补的是一个**自洽**的带电流态：先匹配、再由它播种一次带电流通道的演化，剖面与 ψ 出自同一跑，然后才谈得上断言 ip。这正是 F-26 里没选的选项 B，单独立条是因为它是**新写一条判据**，不是改一条。★★★**同日关闭（内核 `a418d34`）—— 而且不需要两段式播种**：带电流通道的那一跑**本来就同时带着两半**（`_state` 给剖面、`_lad` 给梯子），**拿它跟自己配**就不用探针、也不用凭空补什么。没有通量的是匹配那一跑，拿它出剖面正是把另一套夹具搞成病态的原因。★实测（自洽态）：**自举开 ip 7.944514e5（0.9931×）· v_loop 0.30372 · |j_ni| 峰值 1.315e5**；自举关 ip 8.000000e5 · v_loop 0.34762 · |j_ni| 0。**这独立印证了 F-26 的判断：求解从来不是问题**——同一条路径在配对态上答 10.2×，在自洽态上落在 0.7% 以内。★★**判据钉的是 `|j_ni|` 的量级，不只是割线落点**：病态配对上它是 **1.03e8 A/m²**（高于任何物理值两个数量级），这里是 1.3e5，**差 800 倍**。因为一个错了几个数量级的自举，仍然可以被一个回路电压抵到正确的总电流上，**那时候只有量级能揭发它**。★**先判空转**：自举关时 `j_ni` 必须**恰为零**、开时必须非零 —— 一条「自举关掉也能过」的自举判据等于没有。变异验过：把判据自己那一跑改成 `bootstrap = 0`，当场红在这一条上，不是红在后面某条。★带宽 2% 对实测 0.69%，理由写在报错里：自举在割线**内部**重算，所以收不到自举关那条判据的六位 | 2026-09-11 关闭 F-26 时并记（全仓 grep 实测）· 同日实现并变异验证 | 2026-09-11 关闭 F-26 时并记（全仓 grep 实测）|

★**本仓能自己做完**的现在是 F-2 / F-12（F-14 · F-16 · F-18 · F-34 已关；**F-1 的门已通，余下在 H-19 下游**）（F-4 · F-5 · F-6 已关）；内核侧只剩 **F-21**（F-15 · F-17 · F-22 · F-23 已关；F-23 的九条里五条是环境缺件的声明式跳过，不是修好；F-21 已排除仓库路径一解，并给出不需第二台机器的判别法）；F-9 要先定走哪边；F-8 要对外联系；F-10 / **F-19** 归 fydoc（F-3 · F-11 · F-13 已关），**F-20** 要第二台机器的实测放电；**F-25 已裁定并做完（ABI 153）· F-26 仍等一次裁定，不等测量；F-27 接线已完成、只余 TORAY 参照件；F-28 已实现、只余 B 类对标；F-30（LH 的 L2 余三段）设计已定待写；F-31 等五项裁定（内核射频线的改动已推 `2e8684d`）**，登记在此只为让本仓知道自己在等什么。

★★那四条 `planned`（`C-06` TEQ · `C-07` TOSCA · `B-09` DINA · `C-10` TRANSMAK）**当日全部执行完毕**，
均判 pass；它们本就不在上表里——**执行是定序册的活，不是本表的活**。本表只收「不做就没人做」的事；
判决与排期的唯一生成源仍是 `docs/benchmark/plan/plan.jsonld`（2026-09-08 晚：**通过 34 · 阻塞 1**，
登记册 35 条 · 17 场景，当日复测 34/34 全过）。

〔仍然成立〕本仓在 A–D 那条链上的位置是**消费方**：E-2 到位则 EAST 标准算例可用于本仓的
对拍与回归；E-4 裁定后计算态才有身份可入库。

★★**2026-09-08 加一句**：E-7 提醒的是**同一个消费方位置上一件更细的事**——本仓从上游取来的
「参照答案」可以既是**忠实转录**又**不是这一炮**。`B-06` 的参照侧就是这样：oracle 忠实地复现了
那份 g-file，而那份 g-file 与本炮交付 EFIT 差 4 倍。**「转录对了」与「转录的是对的东西」是两问**，
本仓的登记册此前只问了前一问。

---

## G. 通用功能评估的落地（`FYL-REPORT-07`，2026-09-12）

`FYL-REPORT-07`（本仓 `docs/report/`）按八项通用功能评估了 fy 体系，出建议 R-1..R-9；
用户「落地 R1～9」同日执行。**落成的不再登记在此**（判据与实测见该报告 §9），本节只留
**仍然开着、且不做就没人做**的那几条。

| 编号 | 归属 | 事项 | 依据 |
| :--- | :--- | :--- | :--- |
| **G-1** 〔完成〕 | kernel · fylite | ★★**2026-09-12 大半落地，且把这条的归因改判了**。**做了什么**：马奇实际滞后**六个**量，此前只有三个有名字、一个都交不过去。现在五条带前缀住在 `core_profiles` 上（`fylite:psi_prev` · `sigma_prev` · `exch_prev` · `dn_prev` · `vn_prev`，第六个 `zeff` 本来就有槽），条目块加三行入口（`zeff_prev` · `dn_prev` · `vn_prev`）与两行出口，门按声明的路径读回并塞进入口块；接口摘要 `d10a8fc02a5e6e5c`（只加槽，修订号不动），五个词记进内核 `OURS`。**证据它真的交接了**：把那三个槽从绑定文档里删掉，续跑这一步的 dt 从 **0.0069174 s 变成 0.0076811 s** —— 读到了、也改了答案。★★**改判一**：报告 §9.1 ① 把 61 % 归因于「滞后量交不过去、只能开 `lag_reset`」。实测**不是**：强制 `lag_reset=1` 与不开，读数**逐位相同**（t_end 0.01075795809849784、dt_next 0.006917393144882313），所以那个旋钮从来没动过这些数。★★**改判二（61 % 的分解）**：两条马奇**停在不同时刻**（40 步 0.41806 s 对 20+20 的 0.40008 s），所以「同一位置的剖面差 61 %」大半是时间错位。把控制器钉住（`dt=0.002 dttarget=1e9`）之后两侧 t_end **逐位相同**，残差是 **Te 9.54e-02 · n_e 4.84e-02**，峰值在**紧邻钉死边缘的那个节点**（31 点里的第 29 点），边缘处恰好 0，轴上 3.8e-02。**余**：那 9.5 % 是**剖面往返本身**的缺口（与滞后量无关，dt 已逐位相同），按半径形状看指向边缘/台基那一段的写回；`fylite:state` 要不要从「平的交接单」改成声明过的子树仍归 `-16` G-8。★顺带量到一条：门读 `lag_reset`，而模板的词表不认它（`fy run` 按名拒绝，只能走 `--plan`）—— 正是 G-2 揭开的那三套命名之别的又一例。**原文**：**`fylite:state` 的形归 `-16` G-8**。本次给了它名字（记录里的交接单）与三个读者，但**形**是一份平的清单，不是一棵声明过的子树。直接后果量到了：`code/evolve` 的三条滞后量交不过去（内核从 `evolve/fylite:*` 读、写进自己的原始条目块，而扁平树只收声明过的表里的槽），常数闭合下逐位相同，新经典闭合下 Te 差 **61 %**——今天靠打开内核自己的 `lag_reset` 如实处置 | `FYL-REPORT-07` §9.1 ① | ★★★**2026-09-12 关闭（同日第三次修订，这次是真因）**：余下那 9.5 % 的缝**不在交接单上** —— `code/evolve` 的新经典闭合读 q，而它**写出的**梯子只有九行、**独缺 q**；续跑绑回那份文档时门因为「梯子上有 rho」改走**绑定梯子**档，`q_prof = get("q").unwrap_or(zeros)` 给出零，闭合把 q 夹到 `max(1e-3)`，chi 塌成 ~0 —— **不报错**，只是几乎无输运地升温。逐步实测：整跑第 2 步 chi_neo **0.19–1.6**，续跑那一步 **2e-6**，其余闭合入参逐位相同；**常数闭合一直是 0**，正是它把范围圈到闭合那一路（定位法：closure 0/2 二分 → 通道二分 → 门侧与条目侧各打一行临时输出，见提交 `79d64b9`）。**两处都补**：写出侧写 q；绑定侧对「closure ≥ 2 而 q 全零」按名拒绝。**读数**：当初的 40 步对 20+续 20 由 Te **6.08e-01** · n_e **6.99e-01** 变成 **0.000e+00 · 0.000e+00**，`t_end` 逐位相同。判据两道（内核 `a_neoclassical_march_writes_the_q_its_closure_reads` · 公开 `test_resuming_is_the_same_march_under_the_neoclassical_closure`）。★`fylite:state` 该不该从「平的交接单」改成声明过的子树仍归 `-16` G-8，那是**形**的问题，不再挂着一个错的读数。
| **G-2** 〔完成〕 | kernel · fylite | ★★**2026-09-12 落地**：`code` 层的参数面现在**有一处声明，且是完整的**。推导 `rust/tools/case_params.py` 走门体**与门可达的每一个局部函数**（拒绝跟进派发器 `run` / `raw_entry`），`rust/build.sh` 把它生成进四个宿主（`fyo-interface.json` · `_fyo_interface.py` 的 `CODE_PARAMS` · `fyo-interface.js` · `fyo_interface.rs` 的 `CODES`），`fy list kernel` 文本按 `键=缺省` / `键!`（必填）逐门印出，`--json` 另带类型与**读它的函数**。实测：**35 扇门、617 条声明、342 个不同的键**；最大的 `code/evolve` 130 条，`code/transport` 29 条。★**完整而非下界，这是要点**：宿主拿不完整的声明当筛子，丢值是静默的（R-1..R-9 落地时量到两次）——所以 build 与 `tests/test_case_params.py` 双向卡：两个源文件里每一处 `s.<verb>("键")` 都必须至少被一个码声明。★**同一份推导也交给了图生成器**，于是 `fylite-kernel.jsonld` 的参数节点从 588 升到 **726**（1849 → 1987 节点），今早记下的「图是下界」当日消掉（`FYL-FYO-01` §4）。★**三套命名的关系量出来了**：模板的 `fylite:vocabulary` 是**页面控件名**，与门收的名字不是一回事（`btol`→`b_tol` · `wflux`→`weight_flux`），故公开侧闸子**不求两者一致**，只钉「交集不许变小」（`breakdown` 2 · `discharge` 10 · `evolve` 37 · `reconstruction` 2 · `transport` 16 · `zerod` 20）并记下 `pfwave` · `profile` · `series` 三个模板**没有对应的门**。**原文**：**code 层的参数面没有任何声明**（`FR-KERNEL-002` 在这一层未兑现）。模板的 `fylite:vocabulary` 是**语料用过的名字**（装配层），内核的 `*_PARAMS` 是**原始入口**的参数：`code/transport` 两者交集为 **0**，六个 code 连 `*_PARAMS` 块都没有。★本次落地被它绊了两次，两次都**不报错**——按模板筛端口，交接文档一份没绑；按模板筛参数，续跑从 t = 0 起而退出 0 | `FYL-REPORT-07` §9.2（C-28）·`-16` K-2 |
| **G-3** 〔待裁〕 | fylite | **`run_state` 的 `cancelled` 没有产者**。枚举已收成一份（七值 + 反解），而 `fy run` 一次门调用跑完整个 march，**宿主没有介入点**；要产它得先把 `-18` U-8 的「一串门调用」搬进 `run.rs` | `FYL-REPORT-07` §9.3（C-5） |
| **G-4** 〔待裁〕 | fylite | ★**2026-09-12 半落，并把挡路处说清了**。**做了**：`page_model.html`（行军那条 bar 所在的页）现在**自带** `run.js` 与 `checkpoint.js`（源 `pages/model.html`，经 `make-page-v2.mjs` 生成）；闸子 `validate-checkpoint.mjs` **不再 `addScriptTag` 注入**它们，改为先断言页面自己带了 —— 此前正是那次注入让「没有任何页面加载它们」躲过了闸（闸过的是产品没发的模块）。实测闸子仍全绿（含「每 N 步落一次断点；仓拒绝时行军照走」saved=2）。★★**真正挡路的不是 `<script>`**（这是本次量到的、比原话更准的一句）：**页面根本不产生 `spo:ComputationRecord`** —— 全仓扫 `app/assets/*.js`，只有 `bundle.js` 会**读**记录、`checkpoint.js` 会**校验**记录，没有一处**造**记录；而「断点就是记录」（U-10，有意不设断点格式），所以仓里没有可存的东西。**余**：在浏览器那侧造出记录（要么把运行时的 `record()` 那一套在页面上镜像一份，要么判定记录只由宿主产、页面只存宿主交来的那一份——这是一条口径裁定），之后断点闸才能换掉确定性假件 | `-18` 分期 U0 · `FYL-REPORT-07` §9.3 | ★**状态 2026-09-12 由〔开〕改〔待裁〕**：页面自带模块那半已落地；余下是**记录由谁产**（页面镜像运行时的 `record()`，还是判定记录只由宿主产、页面只存宿主交来的那一份），与 G-3 是同一条裁定。
| **G-5** 〔已关 2026-09-13：`../spo` 检出在位，`check_conformance` 与 `check_competency --require-spo` 当日据以放行 `FYO-ADR-15` v0.2（CQ-MEA-5）与 `FYO-ADR-16` D-1（十槽）两批 schema〕 | fyo | **schema 改不动，因为 `../spo` 不在**：`PROJECT.md` §5 要 `check_conformance.py` 退 0 才准提交 `schema/`。`FYO-ADR-14` / `-15` 已立（Proposed），铸类待检出到位 | fyo `FYO-ADR-15` OI-1 |
| **G-6** 〔阻塞 —— 2026-09-13 G-5 已解、装置卡片在，余 `covers` 边的人工逐条判断〕 | kernel · fylite | **`-20` 的三张表未生成**。`surface/` 今天算得出，`coverage/` 的 `covers` 边要人逐条判断，`availability/` 的 286 格要装置卡片在场——单出一张会把「一处产、两处渲染」变成「渲染半张」 | `-20` M-1 · G-1 · G-5 |

★**已关的**（同日，各带判据，详见报告 §9.4）：C-1 · C-2 · C-3 · C-4 · C-6 · C-10 · C-11 ·
C-12 · C-15 · C-16 · C-21 · C-23 · C-26 · C-27，共十四条。

---

## H. 动理学反演的落地（`FYL-SDD-07`，2026-09-12）

〔由来〕用户 2026-09-12 给出动理学反演的分层描述（OMFIT 三条路径 · 阶段 0..4 · 四层收敛判据 ·
失效模式 · DIII-D 偏置提示），同日落成设计篇 `FYL-SDD-07`（裁定 Q-1..Q-9），并指示把
落地任务登记进本文件。**设计裁定的真源是那一篇**，本节只排次序、判据与前置，不新增裁定；
两处不一致时以 `FYL-SDD-07` 为准。

★**本节的 `H-` 是本台账的行号**（按节字母取，与 A/B/C/D/E/F/G 同制），**不是** `FYL-SDD-02`
的 H-1..H-8（宿主裁定）——两套各在各自的文件内唯一。

〔三期〕先做**只读的图**，因为它删的手写最多、动的内核最少（`-21` §七）：**Q0 只读的图**
（计划文档 → `flow.js` 画图 → 状态从已有记录读；四栏照旧可用）→ **Q1 控制**（五个动作 ·
过期传播 · 断点 · 改档位即改计划）→ **Q2 外环**（s4 成环 · 轮记录 · 二维验收散点）。
★**四栏不撤**：图是四栏的另一种看法，撤栏要另立裁定（`FYL-SDD-07` 的 P-9 / P-16 / P-22 /
P-23 / P-29 未动）。

| 号 | 归属 | 事项与依据 | 判据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| **H-1** | fylite | **计划文档落地**（Q0）：一份 `fyo:ScenarioSpecification`，`has_step[]` 五步（s0 基准平衡 · s1 映射 · s1b 剖面拟合 · s2 约束 · s3 反演），s4 暂不成环，端口绑定按 `-21` §四 骨架。★**落点待裁**，三处候选各有一条硬约束，实测 2026-09-12：`docs/examples/scenario/` 是**逐 code 的生成物**（一个 `prescribes_code`，由 `tools/make-scenario-templates.py` 生成，由 `test_scenario_templates.py` 与 `lines.jsonld` 双向对账），手写多步计划同时撞生成器与目录闸；`app/cases/` 是**会话文档**（`fylite:AppSession/1`，装输入不装结果，经 `FySession.apply`）；`python/fylite/_manifest/kinetic_reconstruction.jsonld` 是 **`prov:Plan` + workflow-IR 投影**、标 `executable: false`，键名另一套。〔推荐〕新目录 `docs/examples/plan/kinetic.jsonld`，app 侧构建期取同一份字节（进 `app/sw.js` 预缓存），代价是要给新目录写一条自己的对账 | 页面与 `fy run` 读**同一份字节**（sha256 相同） | **落点已裁并落地 2026-09-13，验收未达**（★★用户裁定「H-1 落入 fylite_kernel/docs」：`fylite_kernel/docs/cases/plans/kinetic-reconstruction.fyo.jsonld`，不进 `scenarios/`——那里每份都须被登记册记录点名；闸子 `tests/test_kinetic_plan.py`：每步 code 存在 · 参数是该 code 声明的 · 端口是该门真读的输入键 · `x+run://<步>/<名>` 指向更早的步且是它真写的字段 · 端点文件在，五份坏样本逐一被拒。★**草图错处**：`code/reconstruction#kin` 不存在，动理学模式＝绑 `pressure` / `current_source` 约束；`measurements`/`thomson`/`ladder`/`core_profiles`/`current` 这些端口与 `x+run://s0/equilibrium` 一类交接名都指不到门写的东西，改为逐槽绑记录字段（`curve` · `current_source` …）；`ScenarioLoop` 是过程不能坐 `has_step`，s4 改为无 code 子计划、**不带收敛判据**。★**余下**：①外环判据**已裁 2026-09-13：取设计草图 q₀ 相对变化 `dq0_rel` < 0.01、至多 6 轮**——s4 改为 `fyo:ScenarioSpecification` 带 `iteration_cap` 6，判据本身计划侧无槽、写进 caveat，闸子守两处同数（未取：页面自举份额 <0.01×4 · 旧 loop \|Δq₀\|<0.02×8，页面改读计划时须改从计划取）；②运行时读 `has_occurrent_part`/`fylite:steps` 与 `{id: "<步>/<ids>"}`，不读 `has_step`/`x+run://`，页面也不读计划（H-2），故同一份字节的验收未达；③#137985 无汤姆逊件在内核仓，s1b 暂拟合交付重建自己的压强，只证步能接上；④s1 的输出无人读（无门把诊断 (R,Z) 映到 ψ_N）。⑤设计书 FYL-DESIGN-21 正由并行会话迁 `FYL-SDD-07`，落点注待其落地后补）〔2026-09-13 **H-21 已关**：`fy run` 跑通这份计划，② 的运行时一半已解；余「页面读同一份字节」等 H-2〕 |
| **H-2** | fylite | **`app/assets/flow.js`**（Q0）：计划 → 图，节点取 `has_step[]`，边由 `has_port_binding` 的 `x+run://<step>/<port>` 推出；只读，不驱动（`-21` Q-1） | 页面 HTML 与 `flow.js` 里没有手写的节点或边（闸子 grep） | 开（等 H-1） |
| **H-3** | fylite | **页面挂点**（Q0）：`app/pages/analysis.html` 的 `data-bar="reconstruction"` 内加一个图面板，文案入 `lang-zh.js` / `lang-en.js` 两份 | 既有 `validate-analysis.mjs` · `validate-typography.mjs` 仍全过 | 开（等 H-2） |
| **H-4** | fylite | **状态投影**（Q0，`-21` Q-2）：节点状态 = 该步最近一份记录的 `run_state`（七值）叠步带四态（`-18` U-8）；**过期**是第八种显示，不是第八个 `run_state`。★与〈待裁〉的 **G-3 · G-4** 同一条缝：页面今天**不产生记录** | 状态闸：每个节点的状态字符串 = 对应记录的 `run_state`；页面没有私有状态变量 | 阻塞（**G-3 · G-4** 的记录由谁产未裁） |
| **H-5** | fylite | **判据读数**（Q0，`-21` Q-3）：GS 残差 · χ² · Δq₀ / Δq₉₅ 逐节点显示，取自记录的 `summary` 行与 `ComparisonFinding` / `ConvergenceCriterion` | 页面不做阈值比较，判定只从记录来（闸子断言无比较运算） | 开（等 H-4） |
| **H-6** | fylite | **闸子 `app/tests/validate-flow.mjs`**（Q0）：流程闸（图上节点集合 = 计划 `has_step` 集合，**双向**，`-10` G-14 的教训）+ 状态闸 + 无手写图 + 无页面自判 | 无内核、纯静态服务器下可跑（`validate-form.mjs` 先例） | 开（等 H-2..H-5） |
| **H-7** | fylite | **`run.js` / `checkpoint.js` 在分析页上真用起来**（Q1）：两者已随页面装载（见 **G-4**），而页面不产生记录，断点即记录 ⇒ 无可存之物 | 断点闸 `validate-checkpoint.mjs` 的步进器由确定性假件换成真内核，等价判据仍成立 | 阻塞（**G-4**） |
| **H-8** | fylite | **五个控制动作**（Q1，`-21` Q-4）：运行到此 · 单步 · 从此重跑 · 断点 · 取消。**没有「跳过」**——可选步的开关是计划里的 `fylite:switches`，关掉即节点不在图上或画成关着的 | 动作集合恰为五个；页面里没有 skip 路径 | 开（等 H-7） |
| **H-9** | fylite | **过期传播**（Q1）：某步重跑后下游全部标过期（`engine.versioning.Staleness.invalidate` 的图形） | 「从此重跑」后下游节点**全部**过期 | 开（等 H-8） |
| **H-10** | fylite | **节点卡改档位 = 改计划**（Q1，`-16` H-1）：L2 的模型档位写回计划文档，不写页面变量。★参数名受 **G-2** 制（code 层参数面无声明） | 改一个档位后导出的计划与手改该字段的计划逐字节相同 | **会话文档这一半已完成 2026-09-12**（★★**判据按原样落在会话文档上**：`app/tests/validate-setting-is-the-document.mjs` —— ⓪每个档位自己声明缺省 · ①词表 141 个档位各是文档的一个字段 · ②改一个档位只动那一个字段 · ③与手改该字段**逐字节相同** · ④往返同一份 · ⑤越界的手改被控件的界夹逼 · ⑥整份文档除 `@id`/`fylite:created` 外无不定项，改一个档位后两份文档之差恰为 `fylite:config` 的那一个键。★写闸时撞上一个**真错**并已修：六条 LH 档位此前无缺省声明，`input[type=range]` 缺 value 时按 HTML 规范取中点，跑的是词表说不出的数；已补登为今天在跑的那六个中点，`3 MW` 是否为**想要**的 LH 缺省另需裁定）。**余下这一半等 H-1 裁定**：写回「计划文档」要先有计划文档的落点。★**G-2 已于 2026-09-12 关闭**：`code` 层参数面已声明并生成进四个宿主，`fy list kernel --json` 逐门交出；本行的前置因此解除）|
| **H-11** | fylite | **控制闸**（Q1） | 「运行到此」的结果 = 逐步手按的结果，**逐记录相同** | 开（等 H-8..H-10） |
| **H-12** | fylite · kernel | **外环可执行**（Q2）：今天 `oracles/loop.py` 在内核仓测试树、`kinetic_reconstruction.jsonld` 标 `executable: false`、本分发里签名已不合（`-21` G-7）。两条路——发行回本分发，或按 s1..s3 由宿主逐步驱动重建（后者与 `-21` Q-9「fylite 是 worker」同向，且不需要发行） | 一轮 s1→s3 在本分发里跑得通，记录齐全 | 开（等 Q1）〔2026-09-13 H-20 裁物理先行：命令行一半并入 **H-21**，页面一半仍等 Q1〕 |
| **H-13** | fylite | **s4 成环**（Q2，`-21` Q-5）：轮记录（含 `fylite:state`）· 逐轮 q₀ / q₉₅ · 二维验收散点（横轴 GS 残差、纵轴磁 χ²，每轮一点，验收区一块）。★阈值受 **H-14a** 制 | 收敛由计划的 `converges_by` 判，读数在轮记录里，页面不判 | 开（等 H-12）〔2026-09-13：命令行一半并入 **H-22**，页面一半仍等 Q1〕 |
| **H-14** | fylite | **外环闸**（Q2）：N 轮一次跑完 ≡ k 轮 + 从轮记录续 (N−k) 轮，**逐记录相同**（`-18` §十三 断点闸的外环版）。先例：`FYL-REPORT-07` §9.1 ①（40 步 ≡ 20 + 续 20，逐位，常数闭合） | 同左 | 开（等 H-13）〔2026-09-13：命令行版即 **H-22** 判据 ①〕 |
| **H-14a** | fylite | **本仓反演解自己的 GS 残差未量**（`-21` G-6）：登记册 B-10 量的是 CHEASE 固定边界（5.257e-02，带 8e-2），B-06 量的是与 EFIT 的差（q₀ −58.9 %，且该报告自陈无 ASIPP 动理学 oracle）。二维验收（H-13）在有这个量之前谈不了阈值；DIII-D 的量级**不可搬**（`-21` §八） | 本仓一份动理学解的 GS 残差有实测读数 | **完成 2026-09-12** —— 内核判据 `test_the_kinetic_solution_has_its_own_gs_residual`：收敛的 EAST 动理学拟合（1647 次迭代 · 9 行动理学约束）上，**ψ_N ≤ 0.5 为 8.61e-02、全约束区 1.38e-01**（同一算子与归一，故与 B-10 的 CHEASE g-file 5.257e-02 可比，约 2.6 倍）。★量的过程揪出一条真缺陷 → 新条 **H-19**（记录内部二维 ψ 与一维 p′/FF′ 差 2π） |
| **H-15** | fylite | **三个关着的节点**（`-21` G-2）：ELM 相位条件平均 · Tₑ,sep 分离面对齐（用户描述标为影响最大的一步）· MSE 的 Er 修正 | 各有一个 code 且进得了计划的一步 | **1.4 已完成 2026-09-12**（★`code/separatrix_align`，内核 `CASE_CODES` 35 → 36，摘要 `862b526a362b2495`，修订号不动：剖面挂 ψ_N 进 → 解出使 Tₑ(ψ_N = 1) = Tₑ,sep 的刚性标签平移 → 交出新标签、位移与**分离面梯度的前后值**。合成台基实测：Tₑ,sep = 80 eV ⇒ Δ = −0.003529 ψ_N（原剖面在 ψ_N = 1 上 85.61 eV），梯度 −3540 → −2847 eV/ψ_N（**变 20 %** —— 这就是「影响最大」那句话的量）。**不产生 Tₑ,sep**：双点模型闭式在 `edge::t_e_separatrix` 但只有 C 接口那条路，缺 `te_sep` 按名拒绝且拒绝语说出它该从哪来；档位面由 C-28 的机器自动交出（`CODE_PARAMS['separatrix_align']`：`te_sep` 必需 · `shift_max` 缺省 0.05）。闸 `tests/test_separatrix_align_code.py` 八条。`FYL-SDD-07` 表 1.4 由 ✗ 改 ◐）。**1.2 同日完成（算法这一半）**：`io/est2.py` 新增 `elm_onsets` · `elm_phase` · `elm_phase_mask` 三个纯函数与 `reduce_est2` 上一个**缺省关**的 `elm=` 开关（窗内且带内，空交集按名报错而不退回普通窗口）。实测（合成迹，周期 20 ms · 占空比 2 % · σ=0.02）：十次 ELM 数出 9 个起始（首峰上升沿在记录起点之前），只有噪声的迹数出 0 个；★标度**取峰值不取百分位**是实测改过来的 —— 95 百分位落在静默段里，同一条迹被数成 80 次。晚 inter-ELM 窗（0.6–0.9）与全窗在同一段台基信号上差 0.85 对 0.70。闸 `python/tests/test_elm_phase.py` 十条（前六条纯函数、无条件跑）。**余下的是那个「Dα 输入端口」**：本分发里没有任何 Dα 节点名（EAST 绑定表无 filterscope 通道），所以序列今天是交进来的；要「进得了计划的一步」得先有那个端口（槽治理，O 序）。**2.3 MSE 的 Er 修正** —— **阻塞**：装置卷宗里没有 MSE 几何（`-12` G-4），无从量起 |
| **H-16** | fydata · fydoc | **MSE / CER / ECE 不在装置卷宗**，也无绑定表（`-12` G-4 · `-17` G-8）；随包装置 EAST 的 Thomson 只能 `--input` ⇒ 阶段 2 的电流约束在随包装置上只能是**模型边**（`-21` Q-7 的虚线） | 装置卷宗里有几何且有绑定表 | 阻塞（他仓治理）〔**2026-09-13 实测改写**（用户：诊断几何在 facts 下）：几何**部分在** fydoc `facts/device/east/abox/static/now/` —— interferometer 32 个位置齐 · POINT polarimeter 11 弦与 ding2018 逐位相符（`consistent`）· MSE 升级 Port C 10 道 R 1.80–2.27 m 与摘要端值相符、遗留 D 窗口 8 道 null · ECE 50 道里 16 道有 R（corpus）· Thomson 40 道 R 1.98 m、z 全 null（`unpublished`；活树 `ts_east` 另记 25 点 @ R≈1.9 m）· CXRS 62 道全 null。**绑定表** `bind/mdsplus/` 的诊断只有 interferometer 一族，Thomson / CXRS / MSE / ECE 均无。余下缺口是逐道几何与绑定，归 fydoc〕 |
| **H-17** | fylite · kernel | **「knot」与本仓基函数的对应未核**（`-21` G-4）：用户描述的调参核心是 knot 数与位置，本仓用列均衡 + 截断谱；词表 `basis` / `outk` / `kpts` 的含义待 code 表自报 | 节点卡的文案能按名说出改的是什么 | 阻塞（`-16` K-2；★**G-2 已于 2026-09-12 关闭** —— `basis` / `outk` / `kpts` 现在能在 `fy list kernel --json` 的 `code/reconstruction` 参数面里按名查到，余下只是 K-2 的那条口径）|
| **H-18** | fylite | **两条后验检验无判据**（`-21` G-5）：中子产额无 code 无端口；FF′ / j_∥ 振荡无判据（可作 `ComparisonFinding{norm: total_variation}`〔工作假设〕） | 各有一条判据或明写 `unevaluated` | **完成 2026-09-12** —— 两条都进了检查册（`fylite.engine.physics.CHECKS`，17 条）：`source-oscillation` 是**真判据**（`max(TV/量程 − 1)` over {p′, ff′}，单调剖面恰好 0；实测落点：解析家族 **0.0000** · 反演 `nff=2` **0.0246** · `nff=3` **0.0882**，基一富振荡跟着长），`neutron-yield` 是**明写 `unevaluated`** 并自己点名缺什么（没有 code 产中子率、`SUMMARY` 没有收它的槽）。两条都列进 `docs/reference/benchmark.md`，闸子 `test_posterior_checks.py`（5 条）|

| **H-19** 〔完成 2026-09-13〕 | kernel · fylite | ★★**用户裁定「以 fyo 为准，参考 IMAS DD」，并选「全迁到 COCOS 17」**。IMAS DD 4.1.1 实读：`profiles_2d/psi` · `profiles_1d/psi` 为 `Wb`（整圈），`dpressure_dpsi` 为 `Pa.Wb^-1`、`f_df_dpsi` 为 `T^2.m^2.Wb^-1`；fyo 平衡模块由它提升（`dd_units: Wb`，卡上 `FluxCoordinateSystem.cocos`）。★★**本行原文的前提是反的，先量再改**：拿记录**自己解出的电流**（不经 p′）量 EAST #137985 动理学拟合，‖Δ*ψ‖ / ‖μ₀R j_φ‖ = **6.28302**（2π = 6.28319）——**ψ 图本来就是整圈**；‖R p′ + FF′/(μ₀R)‖ / ‖j_φ‖ = **1.00012**——**每弧度的是那对表**，却挂着 `Pa/Wb` 的标签。而且不是一处笔误：`code/reconstruction` · `forward` · `summary` · `refit` 四个码、页面会话文档、Python 宿主都按「ψ 整圈 · p′ 每弧度」写。**改法**（内核 ABI 153 → 154）：`case.rs::per_full_turn`，四个写出处各 ÷2π 一次；读表的码里只有 `code/bootstrap` 把表当物理值用，读入 ×2π；`forward` 表族 · `discharge` · `summary` 表族 · `vstab` 把表当电流**形状**、按 Ip 归一，公因子消在 `jc` 里，不动；`code/steady_equilibrium` 早已按声明读写。宿主：会话文档加印 `fylite:source_gauge: per_full_turn_Wb`（词表同批入册），`appsession.to_geqdsk` 见印 ×2π、无印（旧文件）照旧；页面 g-file 导出 ×2π；交付件与导入 g-file 的参照曲线画在拟合旁时 ÷2π；`fyo.reconstruction` 文档声明整圈；`_c_grad_shafranov` 按声明带 g²；轴标与图注改 `Pa/Wb`。**判据**：内核 H-14a 改写为 COCOS 17 定律 `Δ*ψ = −(2π)²(μ₀R²p′+FF′)`，残差读数与改前逐位同一个数（ψ_N ≤ 0.5 8.6057e-02 · 全区 1.3786e-01），比值钉在 (2π)²；`test_reconstruction_code` · `test_recon_rows_code` · `test_summary_code` · `test_forward_code` · `test_bootstrap_code` 24 过（后四份同一次除法，逐位）；内核 Rust 602 过；`validate-geqdsk` 往返 0；`test_appsession_documents` 增一条（有印 = 无印 × 2π）。★**顺带量清的两件旧事**：`validate-worker-summary` / `validate-worker-recon` 在**改前的基线**（内核 HEAD + 本仓 HEAD，临时 worktree 重建）上已红——q0 / q95 / q 剖面与 X 点梯度各差末位（rel 1.7e-16 一类），与 H-19 无关；本批 H-19 又使两份夹具的 p′/FF′ 按 2π 移动，故一并重录（两道今绿）。**原文**：★★**同一份记录里，二维 ψ 与一维 p′ / FF′ 差一个 2π 的规范**（2026-09-12 量 H-14a 时发现）。原样代进 GS 定律得 ‖Δ*ψ‖ / ‖RHS‖ = **6.22–6.28**，逐半径都是这个数（ψ_N ≤ 0.3 / 0.5 / 0.7 / 0.8 / 0.9 / 0.95 / 1.0 依次 6.282 · 6.259 · 6.233 · 6.232 · 6.226 · 6.223 · 6.223），即 **2π 到 1 % 以内**；把源乘 2π 后残差由 **8.41e-01** 降到 **1.38e-01**。**二维 ψ 是每弧度的**（`fylite:psi_convention`），所以偏的是一维那对导数。★**后果不是纸上的**：任何消费方把 GS 定律套到反演记录上，读到的是一条**假的**「不满足定律」（公开 `_c_grad_shafranov` 就会这么判），这也正是这个数此前没人量过的原因。★**为什么是待裁而不是直接改**：改一侧就把已发布的 p′ / FF′ 数值挪动 2π，`test_reconstruction_code.py` 里那几条「与旧实现逐位相同」的判据是对着现状钉的（旧实现同样带这个 2π），所以要一起判：**改一维导数的规范**（合本仓约定）· **还是在记录里显式声明这两处各是什么规范**（`fylite:psi_convention` 落到 1-D 那一侧）。当前状态已由内核判据钉住那个 2π，规范一改它就红，红的位置正是要动的地方 | 消费方把 GS 套到反演记录上不再读到假的定律违背，且两处规范各有声明 | **待裁** |

〔本节的负面清单〕`-21` Q-9 · `FYL-REPORT-07` §8：**禁止 (MUST NOT)** 引入调度器 · 队列 ·
服务端组件（`NR-ENV-001`）；**禁止**页面自判阈值（判定只从记录来）；**禁止**「跳过某步」
（被绕过去的步在记录里没有痕迹，而「这一轮做了什么」正是记录要答的）；**禁止**抄 DIII-D
的阈值（参照量级单列，本仓阈值只写量过的）；**禁止**把 NUBEAM / ONETWO 编进某一步
（收它们的**产物**作端口绑定；调外部程序与 DAG 调度是 FyTok 的活）。

### H 续：按场景章〈物理算法流程图〉逐阶段排（2026-09-13 立）

〔由来〕用户 2026-09-13「参照 `FYL-SDD-07` 物理算法流程图（The Algorithm as a Flow Graph），根据当前 fylite / fylite_kernel 状况，规划动理学平衡重构」。上表 H-1..H-19 按**页面**三期排（Q0 图 → Q1 控制 → Q2 外环）；本段按**物理链**排——场景章表 `tbl-x24-stages` 的阶段 0..4 与后验，逐阶段对今天的代码实测（2026-09-13），缺什么立一行。**不新增设计裁定**；要动设计书分期的只列待裁（H-20）。行号接 H-19，与上表同一序列。

〔续裁 2026-09-13〕用户：**物理先行**（H-20 已裁）· KEFIT 两件**可作参照**（H-32 已裁）· **诊断几何在 facts 下**（H-16 状态改写）· **起本地 MDSplus（`~/workspace/data/east_mdsplus`），装置数据随炮号不同**（新立 H-35 · H-36）。

〔实测对照〕设计书状态格 → 今天：

| 阶段 | 流程图 | 2026-09-13 实测 | 行 |
| :--- | :---: | :--- | :--- |
| 0 基准平衡 | ✓ | `code/reconstruction` 在；内核计划的 s0 只过结构闸（`tests/test_kinetic_plan.py`），**没有实跑** | H-21 · H-35 · H-36 |
| 1.1 取数 | ◐ | #137985 唯一的 Thomson 件是 fydoc `facts/experiment/east/137985/thomson_03019ms.fyo.jsonld`：**3.019 s**（计划片 4.0 s），判 `divergent`（门控后相邻点差一个量级，件自述 NOT wired as a kinetic constraint）；4.0 s 的剖面只在 MDSplus（E-2），绑定表无 Thomson 道 | H-24 · H-25 · H-36 |
| 1.2 ELM 相位 | ◐ | 算法在 `io/est2.py`，Dα 端口缺 | H-15（原行） |
| 1.3 映射 + 拟合 | ◐ | `code/profile_fit` 在（收已在 ψ_N 上的 `fit_x`）；**(R, Z) → ψ_N 无门**，s1 `code/ladder` 的产出无下游读者（计划 caveat） | H-23 |
| 1.4 分离面对齐 | ◐ | `code/separatrix_align` 在；双点模型 `edge::t_e_separatrix` 无门，缺 `te_sep` 按名拒 | H-26 |
| 1.5 Z_eff | ◐ | `zeff` 是参数，无 n_C（无 CER） | H-16（原行） |
| 2.1 压强约束 | ◐ | `pressure` 端口收一条总压；p_fast 只有 `pfast` / `pfastpk` 给定形状；`code/beam` 交快离子压强，但没有一步把它拼进去 | H-27 |
| 2.2 电流约束 | ◐ | 进 s3 的只有 `code/bootstrap` 的 `current_source`。该门**已收 nₑ 剖面与快离子压强剖面**，计划 s2 却绑密度面板滑杆缺省（`ne0` 3.5e19 · `peaking` 0.5，门自报 `parametrised`）；`code/rf_ray` 门体已有 `deposit` + `current_drive`，而 `fyo.rs` 的自述仍写「吸收与伴随 ECCD 未实现，请求即拒」 | H-28 |
| 2.3 MSE 的 Er 修正 | ✗ | MSE 几何部分在 fydoc（Port C 10 道有 R，D 窗口 8 道 null），CXRS 几何全 null；本地炮 #70754 / #70745 无 MSE / CXRS 数据 | H-15（2.3）· H-16 |
| 3 动理学反演 | ✓ | GS 残差已量（H-14a：ψ_N ≤ 0.5 为 8.61e-02 · 全约束区 1.38e-01）；判据未作 fyo 实例进记录；T-C43 的截断开关 `FY_CONDIN` 缺省未翻 | H-29 · H-34 |
| 4 外环 | ✗ | `tests/oracles/loop.py` 在内核测试树，`test_loop.py` 打桩跑的是**机理**不是实跑；计划 s4 只声明（`iteration_cap` 6，`dq0_rel` < 0.01 写在 caveat）；公开仓运行时的分步执行 `run_steps` 不认 `has_step` / `x+run://` | H-21 · H-22 |
| 后验 | ◐ | 振荡判据与中子产额的 `unevaluated` 已落（H-18）；s0 ↔ s3 对照边、二维验收、fylite 阈值均无 | H-30 · H-31 · H-32 |
| 命令面 | — | MCP `fylite_run` 仍收 `shot` / `pressure: bool` 旧形（X-KIN-G-1） | H-33 |

〔定序〕**P0 链跑通**（H-21，线性、无回边）→ **P1 补链上的步**（H-23 映射 · H-26 双点 · H-27 压强拼合 · H-28 电流组合 · H-29 判据实例，彼此不等，可并行）→ **P2 外环在命令行成环**（H-22）→ **P3 后验与阈值**（H-30 → H-31）。**实测线** H-36（本地 MDSplus #70754，装置数据经 H-35 按炮号取）自 P0 起并行，取代 EAST #137985（本地无此炮，H-24）。**验证线** H-25（合成孪生）自 P0 起并行：EAST 上没有动理学参照（B-06 比的是 EFIT，#137985 的 Thomson 判 `divergent`），「收敛不蕴含正确」只在已知真值的件上核得了。

★**两条随行纪律**：①一行关闭时，同批改 `FYL-SDD-07` 表 `tbl-x24-stages` 的状态格与 `sc-kinetic-flow.svg` 上关着的节点（设计书在内核仓，另提交）；②EAST 上的读数一律标「连通性」，不作验证——H-1 ③ 的 s1b 拟合的是交付重建的压强，而 fydoc 同条目 §五 量到 4.0 s deck 压强的储能只有交付 EFIT `\WMHD` 的 0.25 倍〔TBD：内核 `tests/data/east/fylite_pressure_east.json` 与该 deck 是否同源未核〕。

| 号 | 归属 | 事项与依据 | 判据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| **H-20** | fylite | **物理链与页面分期谁先**。`FYL-SDD-07` 表 `tbl-q21-phases` 把外环（Q2）的前置写作 Q1（页面控制），上表 H-12 因此「开（等 Q1）」，而 Q1 卡在 G-3 / G-4 未裁（H-4 · H-7 阻塞）；场景章命令面的注却写「回边的每一轮就是一次 `fy run … --resume-from`」，不需要页面。〔推荐〕物理链先行：H-21..H-31 不等 Q1；H-12 由 H-21 吸收（一轮 s1→s3 跑通），H-13 / H-14 的命令行一半由 H-22 吸收，页面一半照旧等 Q1。取推荐则 `FYL-SDD-07` 分期表 Q2 的前置一格要改（设计书另批） | 裁定入本行 | **已裁 2026-09-13：物理先行**（用户）。H-21..H-31 不等 Q1；H-12 并入 H-21，H-13 / H-14 的命令行一半并入 H-22，页面一半仍等 Q1；`FYL-SDD-07` 分期表 Q2 前置一格待改（设计书另批） |
| **H-21** | fylite · kernel | **动理学计划在 `fy run` 上跑通**（P0；Q-G-1 · H-1 ②）。公开仓 `rust/fylite_runtime/src/case.rs` 的 `steps_of` 只认 `has_occurrent_part` / `fylite:steps`，步间交接只认 `bound_to: {id: "<步>/<ids>"}` 与 `fylite:carry_forward`；内核计划写 `has_step` + `x+run://<步>/<记录字段>`，且 `code/reconstruction` · `code/bootstrap` · `code/profile_fit` 交的是记录字段而非 IDS 文档。〔推荐〕运行时加认计划形（`has_step` 作步表，`x+run://<步>/<名>` 按名取该步记录的字段或事实）——计划形是 `FYL-SDD-07` §计划骨架定的，运行时现形是 2026-09-11 复现链的约定；若改走「计划迁就运行时」须另报。s4 不 `prescribes_code`，本行不执行它 | ①`fy run` 跑内核 `docs/cases/plans/kinetic-reconstruction.fyo.jsonld` 退 0，顶层记录 `fylite:steps_run` = 5；②链上 s0 · s3 的记录与同设定同绑定的单步 `fy run` **逐字段相同**（链 ≡ 逐步手按，H-11 的命令行版）；③任一步拒绝则后续不跑、`run_state` 为 `rejected`（`run_steps` 既有规则） | **完成 2026-09-13**（★未提交，共享工作树）—— **公开仓运行时** `rust/fylite_runtime/src/case.rs`：`steps_of` 认 `has_step`，只跑指定了 code 的步，无 code 的声明记进记录 `fylite:steps_not_run` · `compose` 认裸写的 `has_port_binding` / `has_parameter_setting`（★此前按精确键读，这份计划会**静默**丢掉全部设定与绑定）· `x+run://<步>/<名>[#col=<k>]` 解成上一步记录的字段或事实（`resolve_run_handles`，解不出按名报错）并按端口路径放进计划树（`inject_inline_numbers`；★此前 `plan_tree` 只放文档，内联的数到不了树门）· `facts:<域>/<标识>/<文件>` 经 facts 搜索路径解析 · 无平铺槽的文件回退改为按格式读（★YAML 装置卡片此前被当 JSON 拒掉）；单元判据 +2，`cargo test --lib` 139 过。**内核仓**：`tools/project-kinetic-inputs.py` 以公开包 `fyo.as_measurements`（与 `test_reconstruction_code.py` 同一个投影）写出 `tests/data/east/discharge_east137985_4000ms.fyo.jsonld` 与 `profile_points_east137985_4000ms.fyo.jsonld`（`--check` 逐字节）；计划 `device` 改绑 `facts:device/east/east_device.yaml`（★原绑的 `abox/machine.jsonld` 是 A-Box 清单，不是门要的装置卡片）、`boundary` 两列加 `#col`、caveat 随之改写；结构闸 9 → 11 条全过。**读数**：①`fy run` 五步 3.2 s 全 succeeded，`fylite:steps_run` 5，`fylite:steps_not_run` [s4]；②链上 s0 · s3 与各自单跑逐字段相同——39 个输出 · 22 个字段，唯一差别是记录 id。★单跑走的是**一步计划**（同一执行器，链上的数内联）；按模板的单码 `fy run` 路径要 `measurements` 端口、不收平铺槽绑定，那一条未比；③连通性读数（非验证）：s3 带 9 行动理学约束，q₀ 0.832 → 0.557 · q₉₅ 3.92 → 3.25 · lᵢ(3) 2.62 → 3.64。★★**s0 / s3 在计划的 `tol` 1e-9（抄自页面）下都跑满 800 步未收敛**（残差 2.0e-5 / 3.8e-4；与 T-C43 记录「缺省 1e-9 到不了」同一件事），外环判据 dq0_rel 读在两个未收敛解上没有意义——H-22 之前要先定。〔**已裁 2026-09-13：`tol` 1e-8**（用户）。★只改 `tol` 读数逐位不变——卡的是 `max_iter` 800 的步数，故同批把 `max_iter` 取为 4000（内核 H-14a 判据所用的值；上限只影响未收敛的解）。实测 s0 975 步 · s3 1668 步收敛（残差 9.79e-9），五步 6.7 s；连通读数 q₀ 0.832 → 0.559 · q₉₅ 3.92 → 3.26 · lᵢ(3) 2.62 → 3.64。计划 caveat 与 s0 注已记为「不同于页面」〕 |
| **H-22** | kernel · fylite | **外环在命令行成环**（P2，阶段 4）。驱动在宿主（`fy run` 运行时）：按 s4 的 `has_step` 逐轮跑 s1..s3，第 k+1 轮 s1 / s2 绑第 k 轮 s3 的记录（与 `--resume-from` 同一机制）；上限取 s4 `iteration_cap`，判据 `dq0_rel` < 0.01 与计划 caveat 同数（计划侧无判据槽，H-1 ①）；每轮一份轮记录（q₀ · q₉₅ · dq0_rel · `fylite:state`）。★不把 `oracles/loop.py` 发行回本分发，不引入调度器（`NR-ENV-001`）；旧外环的「2 轮收敛 · q₀ +11 % · lᵢ −7 %」出自已退役路径，只作参照、不作期望值 | ①N 轮一次跑完 ≡ k 轮 + 从轮记录续 N−k 轮，**逐记录相同**（H-14 的命令行版）；②在 #137985 上报出收敛轮数与逐轮 dq0_rel（读数，标连通性）；③到上限未收敛时记录写 `converged: false` 并说出末轮 dq0_rel，不报成功 | **② ③ 完成 2026-09-13，① 开**（★未提交）—— **公开仓运行时** `case.rs`：`loop_of` 读 s4 声明（部分 · `iteration_cap` · **回边＝环前与末部分同 code 的步**，即 s0；声明不合按名拒：上限非正整数 · 部分不在跑的步里 · 部分乱序 · 多于一个环）；`run_steps` 改为队列，第 1 轮即线性一遍，此后逐轮排入 s1..s3、并把上一轮 s3 的结果以 s0 之名压进句柄表；每轮读末部分的 `q0` 事实判 `LOOP_DQ0_REL` = 0.01（**计划侧无判据槽**，常量带裁定出处，内核闸 `test_the_loop_criterion_is_the_one_the_runtime_applies` 钉住 caveat 与常量同数）；记录加 `fylite:loop`（部分 · 上限 · 回边 · 判据 · 轮数 · `converged` · 逐轮 q₀ / q₉₅ / dq0_rel）；单元判据 +1，`cargo test --lib` 140 过；内核结构闸 12 过。★★**当场抓出一次假收敛**：句柄表按「第一个同名」取，第 2 轮读的仍是原 s0 与第 1 轮的 s1b / s2，逐位重放第 1 轮，dq0_rel **恰 0** 报了收敛——改为取最新（单元判据加一条「后压入者胜」）。**读数**（#137985 @ 4.0 s，连通性非验证）：② 第 3 轮收敛，q₀ 0.5589 → 0.5651 → 0.5650，q₉₅ 3.2619 → 3.2538 → 3.2535，dq0_rel 0.0110 → 9.8e-5；s2 的自举电流求和 9522 A（在 s0 的平衡上）→ 15363 → 15439 A（在上一轮 s3 的平衡上）；三轮 s3 均收敛（1668 / 1344 / 1343 步），13 步 12.4 s。③ 以 `iteration_cap` 2 的副本实跑：第 2 轮停，`fylite:converged` false、末轮 dq0_rel 0.0110。★但顶层 `run_state` 仍是 `succeeded`（spo `RunState` 七值里没有「未收敛」），读者只看 `run_state` 会读成成功——要不要把未收敛映成别的状态**未定**。**余** ①：N 轮一次跑完 ≡ k 轮 + 从轮记录续跑 N−k 轮——分步计划今天没有 `--resume-from`，要先给分步运行接续跑 |
| **H-23** | kernel | **阶段 1.3 的映射门**（计划 caveat「s1 的产出今天没有下游读者」）：诊断点 (R, Z) → ψ_N 于一份平衡文档，点在分离面外按名标出、不外推；s1b 的 `fit_x` 由它经 `x+run://` 绑定，不再预先给在 ψ_N 上。落点（新门或 `code/ladder` 的一档）实现时定 | ①放在 `code/ladder` 描出的面上的点映回各自的 ψ_N 层，差在描迹容差之内（读数先记）；②计划里 s1 至少有一条下游绑定，结构闸随之加「每步产出有读者」 | **门已落 2026-09-13，② 开**（★未提交）—— 内核 `code/psi_points`（`case.rs::psi_points_case`）：读平衡文档的 ψ 图（与 `code/xpoints` 同一组槽）与新槽 `discharge/fylite:point_r` · `point_z`，用 ψ 图**自己的**双线性采样给 ψ_N；判「在芯部」要 ψ_N ≤ 1 **且**在边界轮廓内（私有磁通区的 ψ_N 也可小于 1），落在轮廓外而 ψ_N ≤ 1 的点计入 `n_private` 并写 note，未绑轮廓时写明「只按 ψ_N 判」；框外的点**按名拒绝**（不出 NaN、不外推）。接口：`CASE_CODES` 36 → 37、`DISCHARGE` 50 → 52，只加不改，修订号 5 不动、摘要 `e133b3b8a36c02a6` → `8293141e58e7aba4`，经 `build.sh` 的接口生成段写出四个宿主件；`docs/reference/fylite-kernel.jsonld` 与公开 `docs/reference/scenario.fyo.jsonld` 同批重生。**判据**（`tests/test_psi_points_code.py` 6 条）：网格节点上的点 ψ_N **逐位**等于节点值 · 磁轴 ψ_N 0.0033 判芯部（EAST #137985 交付平衡）· LCFS 外 0.26 m 的点 ψ_N 1.615 判非芯部 · 缩到磁轴附近的轮廓造出 1 个私有点 · 无轮廓时 note · 三类拒绝。★合成 g-file 的边界轮廓就是框内一圈（R 1.3156–2.2844，ψ_N 到框边仍 < 1），做不了「LCFS 外」这一条，已改用 CASE-22 的交付平衡。内核 `cargo test` 606 过 / 17 ignored；内核 pytest（本门 · 参数面 · 计划闸 · xpoints · ladder）36 过；公开接口类 296 过；运行时 141 过。★★**生成件带进了他会话的在制内核改动**：参数面由今天的 `case.rs` 推导，`discharge_case` 新增的十个参数（`box_max_iter` · `hold_tol` · `p0` …）随之进了公开三件与图，**提交时须与那批一起或挑开**。**② 未做**：#137985 没有任何带 (R, Z) 的剖面诊断，计划 s1 的读者要在有几何的炮上接（H-36） |
| **H-24** | fydoc · 需数据访问 | **阶段 1.1：EAST #137985 在计划片上没有可用剖面**。唯一 Thomson 件在 3.019 s 且判 `divergent`；4.0 s 的 Thomson / POINT / 加热只在 MDSplus（E-2）；绑定表无 Thomson / CER / ECE / MSE（H-16）。在此之前 EAST 上的链只证连通 | 计划片（或改取的片）上有一份非 `divergent` 的 Thomson 文档可绑 | 阻塞（E-2 · H-16）〔2026-09-13：本地 MDSplus 只有 #70754 · #70745 · #38300，**无 #137985**，本行仍阻塞；EAST 实测线转 **H-36**〕 |
| **H-25** | kernel | **合成孪生算例**（验证线）：`code/forward` 在 EAST 卡上由给定 p′ / FF′ 解家族解出自由边界平衡并交出磁通环与探针读数 → 在该平衡上按给定 nₑ / Tₑ（及 `tite` · `zeff`）于合成道位（标「合成」；EAST 道几何不在卷宗）采样出带 σ 的点 → 喂 H-21 的链（H-22 之后加外环）→ 比真值。★「收敛不蕴含正确」唯一核得了的地方；阈值只写量过的，本行先出读数 | ①p(ψ_N) 的 rms · q₀ · lᵢ(3) · β_p 四项，逐项报 s0 与 s3 各离真值多远；②外环各轮离真值多远逐轮报（不要求单调，记下单调与否）；③孪生件可逐字节重生（`tools/make_synthetic_case.py` 先例） | 开（可做） |
| **H-26** | kernel | **阶段 1.4 双点模型的门**：`edge::t_e_separatrix`（`SolParams` / `SolState`）今天只有 C 接口一路，`code/separatrix_align` 缺 `te_sep` 即按名拒。立门交出 Tₑ,sep，使计划里对齐那一步的 `te_sep` 由绑定给出而非手填〔TBD：分析链上 P_sep 与 nₑ,sep 从哪来〕 | ①门的读数与既有 C 接口路径同输入**逐位相同**；②计划加一步 `separatrix_align`（可关走 `fylite:switches`，Q-4 无跳过），结构闸过 | 开〔**2026-09-13 勘定，暂缓**：T_e,sep 不是一个闭式——`edge::t_e_separatrix` 要求解器状态（q∥ · α_t · κ_e · T_e,target），而 q∥ 与 α_t 又依赖 T_e,sep，所以门要包的是整个 `edge::solve_forward`（Lengyel 前向不动点），且这一整条今天**只在 `oracle` 特性下**有 C 入口（`fylite_rs_lengyel_inverse` 以 T_target < 0 选前向），发行内核里没有。输入约 30 个：几何 13（R0 · a · κ95 · δ95 · B0 · Ip · n_e,sep · **P_sep** · A · ⟨Z⟩ · B_pol 比 · 入偏滤器份额 · T_i/T_e）+ SOL 14 + `ne_tau` · 主离子电荷 + 杂质（**按种名**，树门今天只收数，要一个名表约定）；参照＝FYDOC-CASE-16 的 TORAX 十例（`test_lengyel_forward.py` 已钉 1.3e-13）。★分析链上 P_sep 与 n_e,sep **仍无来源**——门立起来也要人手给这两个数，所以先做数据实跑线（H-35 · H-36）〕 |
| **H-27** | kernel | **阶段 2.1 压强拼合**：p = nₑTₑ + Σnᵢ Tᵢ + p_fast。今天 `pressure` 端口收一条总压，Tᵢ 由 `tite`、nᵢ 由 `zeff` 推，p_fast 只有给定形状。立一步把分量拼成 `pressure`，出处逐分量记来源（数据 / 模型——Q-7 两种边线在记录侧的形）；p_fast 可绑 `code/beam` 的快离子压强或取给定形状；`derived-from-reconstruction` 拒收不变 | ①拼合恒等式逐点相对 1e-12；②无快离子且 Tᵢ = Tₑ 时，s3 与今天直接绑单曲线的 s3 **逐位相同**；③分量出处在记录里齐 | 开（可做） |
| **H-28** | kernel · fylite | **阶段 2.2 电流约束组合**：`current_source` 由 j_bs 扩为 j_bs + j_NBI（`code/beam`）+ j_ECCD（`code/rf_ray` 的 `deposit` + `current_drive`），欧姆部分仍作拟合余量（与今天「那份电流当已知、只拟合余量」同义）；s2 的 nₑ / Tₑ / 快离子改绑 s1b 与 H-27 的产物（门已收剖面，今天绑的是滑杆缺省）。★同批改 `fyo.rs` 里 `code/rf_ray` 的自述。〔TBD：`code/bootstrap` 交 `j_bs_sauter1999` · `j_bs_redl2021` 两列、电导按 `sigma_vintage` 选，写进 `current_source` 的是哪一列未核——X-KIN-G-2 的实况待此核〕 | ①束与 ECCD 为零时 s3 与今天逐位相同；②非零时 ∫j dA 等于各分量电流之和（相对 1e-9）；③s2 记录的 `parametrised` 为 0 | 开（可做） |
| **H-29** | kernel · fylite | **阶段 0 / 3 的判据作 fyo 实例进记录**（FR-ANALYSIS-010 提案 · Q-3；H-5 的前置）：内层 `ConvergenceCriterion{metric: psi_residual}`（与 H-14a 同算子同归一）· 反演层逐诊断 `ComparisonFinding`（环残差以 `PSIBIT` 逐道 σ 为单位，T-C43 定的度规）；阈值留空，读数与判定分开 | ①s0 / s3 记录各带这两类实例；②`psi_residual` 读数与 `test_the_kinetic_solution_has_its_own_gs_residual` 同输入**逐位相同**；③宿主与页面不做阈值比较（H-5 闸同题） | 开（可做） |
| **H-30** | fylite · kernel | **后验①：s0 ↔ s3 的对照边**（后验一致性第 4 条，流程图上天然有的那条边）：一份 `ComparisonRecord`，β_N · lᵢ(3) · q₀ · q₉₅ · 磁轴的偏离；另加「q = 1 面 vs 锯齿反转半径」一条，缺反转半径测量端口即 `unevaluated` 并点名缺什么（H-18 同形）。两份单步 `fy run` 记录即可做，不等外环 | ①对照记录可由两份记录重生、逐字节相同；②`unevaluated` 那条的原因串非空 | 开（可做） |
| **H-31** | fylite · kernel | **后验②：二维验收与 fylite 阈值**（GS 残差 × 磁 χ²，每轮一点，`norm_note` 写合成规则「映射轮放宽 GS」）。一个读数定不了阈值（Q-G-8）：要先有 H-25 孪生的分布与多片（逐片重构章）上的分布 | 验收区由量过的分布给出并注明来源；无分布前 `overall_verdict` 为 `unevaluated` | 开（等 H-22 · H-25） |
| **H-32** | fylite · kernel | **EAST 动理学参照件用不用、怎么用**。本机有两份 KEFIT 侧件：fywork `data/kefit_wuxm_94048`（#94048 @ 5760 ms，KEFIT_wuxm 的 a / g 输出与 fyo 算例，FYR-REPORT-08）· `~/workspace/third_party/kefit_reference_bundle`（#93060 样本输出，**无 LICENSE**，内部私有）；二者是否为动理学档（有无压强 / MSE 行）未核〔TBD〕。本仓成例：专有 EFIT 的记录输出不进公开仓，受限件只在本机一次性量（T-C37 的 A/B）。要裁：作内核仓私有 B 类参照（只进 fylite_kernel 测试输入），还是只作本机一次性对照、不入册 | 裁定入本行 | **已裁 2026-09-13：可作参照**（用户）。实读：#93060 的 m-file（`samples/output/m093060.01001`）带测量（`COILS` · `EXPMP2` · `BRSP` · `PLASMA` · `BTOR`）与样条 knot（`PPKNT` · `FFKNT` · `KPPKNT` …），`KZEROJ` = 0、无压强 / MSE 键 ⇒ 是**磁测量 + knot 基的参照**（阶段 0，及 H-17 的 knot 对照），不是动理学参照；#94048 wuxm 的 k-file 只有探针、无磁通环（PROVENANCE：环值只在 MDSplus），压强为动理学表示〔TBD：约束行未读〕。两炮均不在本地 MDSplus。落法沿成例：作内核仓私有参照输入，不进公开仓 |
| **H-33** | fylite | **MCP 收计划**（X-KIN-G-1）：`engine/serve.py` 的 `fylite_run` 仍收 `shot` · `time_s` · `pressure: bool` 旧形，与 CLI `--kinetic` 无可核对应；改收 `plan`（或 `line` + `scenario` + `params` 经同一合成器） | 三面（CLI · Python · MCP）解析出的计划逐字段相同（X-KIN-1） | 开（可做） |
| **H-34** | kernel | **阶段 3 的截断阈翻不翻缺省**（内核 `changelog.md` 2026-08-31 T-C43 节）：`FY_CONDIN` 收紧一个量级使 (3,4) / (4,4) 收敛，当时实测 q₀ 缺口 −47.1 % → +2.6 %，代价 dR −17.3 mm 出 R-1；`inverse.rs` 至今缺省不变，台账未见裁定。计划 s0 / s3 取 npp / nff = 3 / 3，几乎不受影响；外环与孪生（H-22 · H-25）要不要放开高阶基取决于它 | 裁定入本行；若翻，B-06 与 H-14a 的读数随之重录 | **待裁** |
| **H-35** | kernel · fylite · fydoc | **装置数据按炮号解析**（用户 2026-09-13「根据炮号，装置数据有差异」）。EAST 的分期逐 IDS 各自成立（边界 45563 · 65726 · 97034 · 100000 · 135000 互不重合），故 fydoc `device.jsonld` 的 `epochs` 只有一层 `now` [0, null]，分期落在 `providers.*.available.*.valid_shots` 与 `bindings.mdsplus.read_rules` 上：magnetics `base` / `east` [0, 97030] 对 `east_new` [97034, —]，`pcs_n_pre80000` [0, 70754] 对 `pcs_vp_post80000` [80000, —]；`read_rules` 的 `efit-east-cm-to-m` [0, 45562]。实测 2026-09-13：公开仓 `rust/fylite_runtime/src/assembly.rs::from_manifest` 按炮号只选 **epoch**，provider 按名或 `default` 取，**不看 provider 的 `valid_shots`**；`read_rules` Rust 侧未实现（09-04 本地 mdsip 实测同记）；内核计划 s0 / s3 绑一份 `machine.jsonld`，没有炮号分支。〔TBD：Green 表按年份分套（`kefit_reference_bundle` 带 green2012…green2022），本仓现算响应是否也须逐代几何未核〕 | ①provider 缺省按炮号取覆盖该炮的那一个（#70754 → `pcs_n_pre80000`，#137985 → `pcs_vp_post80000`），无覆盖者按名拒绝；②`read_rules` 按 `applies_shots` 生效；③计划的 `about_discharge.pulse_number` 与所解析的 provider 写进记录出处 | **①② 完成 2026-09-13，③ 开**（★未提交）—— 公开运行时 `assembly.rs::from_manifest`：①**分代限定的绑定只点名、不代位**：带 `valid_shots` 的 mdsplus provider 覆盖该炮时写 note「覆盖本炮、未绑定、要这一族用 --provider」，缺省绑定（跨代核心集）照用；调用方点名的 provider 照用、炮号出界写 note；缺省的静态 provider 不覆盖该炮而恰有一个同后端的覆盖时换过去；②`read_rules` 覆盖该炮的**逐条点名「本运行时未施加、值是树里原样」**（四种算子 normalize_length_units · scale · select_node · sign **一个都没实现**，施加是另一件事）。单元判据 1 条，运行时 141 过。★★★**当场抓出并改掉自己的一个错**：初版让覆盖该炮的分代 provider **代替**缺省绑定，#70754 因此**丢了 35 个环与 Ip**，并把 38 道 **N 极**信号按下标铺到 **T 极**槽上——而那份绑定件自己的抬头就写着「不混进缺省路径」。实测（fydoc 真清单，`fy data fetch --dry-run`）：#70754 缺省绑定 + 点名 `pcs_n_pre80000` [0, 70754] · #137985 缺省 + 点名 `pcs_vp_post80000` · #75000 缺省、无点名；三炮均点名 TF 的 `tf-focs-node-era`（declared）未施加。实取 #70754（本地 mdsip，4.0–4.1 s）：38 T 极探针 · 35 环 · Ip ≈ 494 kA · 16 路 PF 电流**全部有数**。★顺带实测到 fydoc 绑定件缺陷：TF 绑定 `tf/time` 在 `east` 树 NNF（09-04 已记 `\TIMEPCSA1` 在 `pcs_east`），仍未修（归 fydoc）。**③ 未做**：记录出处里写炮号与所解析的 provider。〔**选项 2 落地 2026-09-13**（用户裁定「2」：按炮号出卡）——★起因：他会话同日把 fydoc EAST 磁测缺省改为 `east_new`（裁定 R-S2，79 探针 · **75** 环），缺省卡片据此重生后本计划的测量（CASE-22，est2，35 环）对不上：投影拒「flux_loop needs exactly 75 channels」、s0 拒「flux_loop has 35 readings, the device has 75 loops」。★★**只按炮号不够**：`east_new` 的 [97034, —] 恰好覆盖 #137985；fydoc 自己的规矩是「选哪份由测量数据的道序决定」「est2 时显式选用 `efit_w_pf`」，而 provider 上没有机读的道序字段，所以落成「**炮号 + 显式选择**」。①公开 `tools/abox-to-facts.py` 加 `--shot N` / `--select ids=provider`：点名的 provider 不覆盖该炮即拒；缺省的声明了范围而不覆盖时换成唯一覆盖的静态 provider；卡片记 `_selection` · `_shot` · `_valid_shots`（所选 provider **声明过的**范围之交；都没声明时只取 [N, N]，日期锚的 provider 不替所有炮号担保）；带选择而不给 `-o` 按名拒（不许盖掉缺省卡片）。生成：`python3 tools/abox-to-facts.py east --shot 137985 --select magnetics=efit_w_pf -o dist/facts/device/east/variants/efit_w_pf` → 79 探针 · 35 环，与旧发布卡片只差名字（fydoc 裁定 R-S3 撤回卡片名，改 `MP01…` / `FL01…` 索引占位），几何与数值逐位同。`dist/` 在 .gitignore 里，变体卡片是**构建产物、不入库**——跑计划的机器要先按上面那条命令生成。②运行时 `case.rs::facts_endpoint` 认 `facts:…?shot=N`（`card_covers_shot`）：卡片不记范围或不覆盖即按名拒，拒绝语带选择；单元判据 +1，运行时 146 过。③内核计划 `device` 三处改绑 `facts:device/east/variants/efit_w_pf/east/east_device.yaml?shot=137985`，caveat 记由来；结构闸改为去掉查询再判文件、并要求查询里的炮号 = 计划的 `pulse_number`（18 过）。**实测**：变体卡片上投影 `--check` 过；计划外环三轮 q₀ / dq0_rel 与 s0 · s3 全部事实**逐位同**此前；把查询改成 137986 的副本在 s0 被拒（「holds for shots [137985, 137985] … outside it」）。★缺省卡片**未重生**（仍是 16:55 的 est2 版）：按新 A-Box 重生是那一侧的事，本计划已不依赖它。★`abox-to-facts.py` 上另有他会话约 825 行未提交改动，本次改动叠在同一份工作树里〕 |
| **H-36** | kernel · fylite | **EAST 实测线：本地 MDSplus #70754 / #70745**（用户 2026-09-13 指示）。服务器：六树路径 `east_path` · `efit_east_path` · `pcs_east_path` · `t2_path` 指各目录，`t1_path` / `t3_path` 指到 `070/` 子目录；`mdsip -m -p 8000 -h hosts`（hosts 两行 `multi \| SELF`、`* \| SELF`），`setsid nohup` 起。★Python 客户端 `openTree` 在本机报 bytes/str 错，改用 TDI `TreeOpen("<树>", <炮>)`。**实测内容**：#70754 六树齐；#70745 只有 `efit_east` · `pcs_east`（`east` 树 6 个节点）；`efit_east` 两炮均为**纯磁**（`MEASUREMENTS:PRESSR` / `RPRESS` / `SIGPRE` / `TANGAM` / `RRGAM` / `FWTGAM` 全 NODATA），`AEQDSK:WMHD` · `BETAP` 有 106 / 109 片；**无 Thomson / CXRS / MSE 节点**；有 ECE01..16（`t3`，每道 1.7e6 点，原始量〔TBD：到 Tₑ 的定标口径，fydoc 记 ECE 两份 corpusdesc 说明不同〕）与 HCN1..3（`t2`，每道 8.5e5 点）；抗磁环仅 ≤80000 有数。★所以这两炮上阶段 1 只有 **ECE Tₑ + HCN 弦积分密度**，压强约束以模型为主（偏置提示所说的情形），读数标连通性 | ①s0 在 #70754 上逐片对 `efit_east`：磁轴 · q₉₅ · β_p · W 的读数；PCS 核心集 38 探针 + 35 环逐道有数；②s1 由 ECE 16 道 R（Z = 0，经 H-23 映射）+ HCN 3 弦出剖面，s3 跑通；③装置数据经 H-35 取 pre-80000 那一套 | 开〔**2026-09-13 进度**：H-23 的门与 H-35 已落；本地实取 #70754 4.0–4.1 s 核心集逐道有数（见 H-35）。参照（`efit_east` AEQDSK，t = 4.06 s）：W_MHD 84.27 kJ · β_p 0.5526 · lᵢ 0.8235 · q₉₅ 4.567 · 磁轴 (1.8888, 0.0130) m · B_CENTR −1.800 T · ψ 轴 / 界 −0.4518 / −0.3515 Wb/rad；`CPASMA` NODATA。★★**下一道坎是装置卡片的通道基**：生成卡 `east_device.yaml` 取 magnetics provider `efit_w_pf`——**est2 79 探针（HBPH…）· 35 环（FL…B）· 12 BRSP 通道**，而 #70754 取回的是 **PCS 38 T 极探针（PCBPV…T）· 35 环（PCFL…）· 16 路 PF 电流（PF1..PF14 · IC1 · IC2）**；重构门按卡片的探针 / 环几何与通道图读数，两套名字与道数都对不上 ⇒ 要一张以 `pcs` provider 生成的卡片（几何与 38 / 35 同序）与 PF 16 路 → 通道的对应，或宿主侧投影；③「pre-80000 那一套」**改判**：缺省核心集才是要用的，N 极族无几何（fydoc 注）〕 |
| **H-37** | kernel · fylite · fydoc | **磁测量反演对拍 `efit_east`（EAST 活服务器，经 SSH 跳板）**（用户 2026-09-13：「关闭本地 MDSplus；经 SSH 跳板访问 EAST MDSplus；验证探针信号名；磁测量反演结果对拍 efit_east 树下结果」）。本地 mdsip 已关；隧道只在命令行里起，地址不入任何仓。**①探针信号名实查**（5 炮 70754 · 94048 · 120000 · 137985 · 160000，按族 `getnci` 名字 + 数据长度）：PCS 核心集 74 节点（38 T 极探针 · 35 环 · `PCRL01`）**逐炮全在且有数**；`PCBP*N` 仅 70754 有数（≥94048 节点在、无数）；`PCVP*` 70754 不存在、≥94048 有数，但 `PCVP10` 在 137985 / 160000 **无数**；`east_new` 的 79 条探针只有 **74 个不同名字**（`HBPL1T`–`HBPL5T` 各列两次），旧炮无、120000 / 137985 全在、160000 缺 `HBPU9N`；`east_new` 环第 71–75 为 VP1–VP5，东树逐炮有数；旧代 `east` 探针在 70754 / 94048 缺 9 个（HBP4N–HBP8N · HBP27N · HBP28N · HBP30N · HBP31N），≥120000 整体退出。★两个客户端坑：整树 `getnci("***")` 与整个 `PSIRZ` / `QPSI` 一次取回会使回包超限、连接被断（此后每次读都报错，**不是节点问题**），须按族通配、按时间片 `data(\\X)[*,k]` 取、ψ 图分 8 行块取；`PSIRZ` 按 `P.T[ir, iz]` 读（EFIT 自己的边界 ψ_N 0.9999 ± 0.0001）。**②对拍（#137985，efit_east TIME[28] = 4.041 s）**：EFIT 读数 轴 (1.9122, −0.0109) m · q₀ 1.806 · q₉₅ 7.03 · β_p 1.693 · lᵢ 1.186 · W 187.1 kJ · a 0.4445 · κ 1.731 · Ip（`PLASMA`）400.9 kA · `BCENTR` −2.432 T · `FPOL` 边 −4.4986 T·m。★**CASE-22 那组输入与 EFIT 树不是同一组**：CASE-22 给 b_tor 1.8 T（fydoc 判 unverified），EFIT 边界 F 对应 2.43 T @ 1.85 m——所以拿 s0 与 EFIT 比量的是输入差不是算法差，本次改用 **EFIT 自己的测量**：`FCCURT[0:12]`（与 CASE-22 的 12 通道逐道差数 %，`CCBRSP` 是拟合值不取）· `PLASMA` · `SILOPT` / `FWTSI` · `EXPMPI` / `FWTMP2` · |B_T| 按 1.85 m 折到 1.75 m（`RBCENT` / `RZERO` 无数，1.85 由 fylite 边界 F 4.4986 = EFIT `FPOL` 边逐位对上而证实），装置卡片取 `--select magnetics=efit`（76 探针 · 35 环，fydoc 注明与 `EXPMPI` 逐道同序）。**读数**：纯环、EFIT 权重 → 1619 步收敛，轴 (1.9039, −0.0222) m（R 差 8 mm），边界点到 fylite LCFS 中位 13.7 mm / 最大 181 mm，fylite 在 EFIT 边界上的 ψ_N 1.004 ± 0.036，q₀ 1.519 · q(0.95) 5.92，q 比 0.72–0.84；纯环、卡片权重 → 轴 R 1.9122（逐位同 EFIT 到 0.1 mm）但 q 比 0.56–0.79。★q 差对上**极向磁通跨度**：fylite 0.876 Wb / 1.049 Wb 对 EFIT 0.713 Wb（×2π），大 23 % / 47 %——同 Ip、同边界而跨度大 ⇒ 电流更峰化，与 T-C43 在 #137985 上记的形状一致（纯环约束不住电流分布）。★★**加权探针一律丢等离子体**：任何带 `FWTMP2` 的组合（实测 `EXPMPI` 或 EFIT 自己拟合的 `CMPR2`、npp/nff 3/3 或 1/2）内核返 −106100 / −106000 / −105900（掩膜清空）；卡片权重下探针行实为关（与纯环逐位同）。连 EFIT 自洽的 `CMPR2` 都失败 ⇒ 疑 fydoc `efit` provider 的探针几何 / 取向与 `EXPMPI` 约定不合，或符号约定，**未诊断**；EFIT 自己的 `EXPMPI` 38–57 道近零而 `CMPR2` 非零（多半是 EFIT 里零权的死道）。 | ①门槛：有 EFIT 加权探针的一次收敛反演；②在其上逐项读 轴 · 边界 · q(ψ_N) · 跨度 · lᵢ 对 EFIT；③判 B 类记录能否入册（参照是 EFIT 树的纯磁结果） | 开〔探针一行未诊断；中间件在会话 scratchpad：`efit_east_137985_k28.npz` · `efit_inputs_137985_k28.fyo.jsonld` · `rec-var-*`〕 |
| **H-37b** | kernel | **H-37 续：EFIT 对拍的偏差分解与条件化读数**（#137985 · efit_east TIME[28] = 4.041 s）。①**环偏移 −0.308 不是测量问题**：`loop_model` 只计等离子体电流的磁通，线圈份额 0.308 ± 0.102 没算进去。②**磁轴被拉偏有两个来源**：页面预设的 (3,3) 基病态，EFIT 用 (1,2)；无锚的内核把等离子体按**垂直无力**求解，而 EFIT 交付的平衡带 F_z ≈ −7 kN（T-C43 已记）。③**条件化读数**：取 EFIT 自己的线圈电流 `CCBRSP`、基 (1,2)、按 χ² 选的垂直设定点 −20 mm → 轴差 +4.5 / +0.5 mm · q₀ +7.3 % · q₉₅ −6.5 %；换成实测线圈 `FCCURT` → R +21 mm · q₀ +39 %。④**压强符号已落（用户裁定 2026-09-14「四处一起改并验证」）**：动理学行 `inverse.rs::pressure_row` · 门输出 `case.rs::reconstruction_case` · oracle `tests/oracles/recon_rows.py::fit_profiles` 三处同改为 +，`tests/test_recon_profiles.py` 的链式法则改为 dp/dx = −span·p′（x = (ψ − ψ_axis)/(ψ_bnd − ψ_axis)），新门 `tests/test_reconstruction_pressure_sign.py`。口径：求解器规范 Δ*ψ = −2πμ₀R·j、ψ 轴上极大 ⇒ p′ > 0，p = +span·∫ₓ¹p′，span = (ψ_axis − ψ_bnd)/2π > 0。★★旧负号三处互为移植、彼此一致，所以没有门看得见；2026-08-14 的回退（`fylite_port` c030188 翻回 0f8be04）是**跨规范**比 p′（EFIT g-file 轴上 ψ 极小）；B-06「span_pr 号相反」那条更正是用第二个负号抵消错号行，p(0) 为正而 p′ 是反的（B-06 已补更正注）；页面 `worker.js` 的 `PSI_GAUGE = −1` 同样只是抵消错号行，两种规范从来不是不同的。〔原记：交付 p₀ = −49021 Pa、补丁「未落」、注释「勿第三次修」——均已被本条取代。〕★**页面耦合（用户裁定 2026-09-14）**：`worker.js` 已回退，页面保留自洽的旧一对——旧 `app/assets/fylite_rs.wasm`（负号行）+ `worker.js` 的 `PSI_GAUGE = −1`，两个负号相抵，动理学压强结果正确；页面改动随**下一次 wasm 重建**（内核 `rust/build.sh --wasm-check`）一起落：删 `app/assets/worker.js` 的 `var PSI_GAUGE = -1;` 及其注释块，`pmeas` 那行推入不取反的 `pv`。在此之前原生内核（+）与页面（旧 wasm）内部行口径相反、物理结果相同 | ①装配反演门能拟合**刚性垂直平移**（EFIT `fitdelz` 的对应物）或**线圈电流**（今天只在给定行那一层有），用实测线圈独立反演对上 EFIT（轴 · q₀ · q₉₅ 与 ③ 中 `CCBRSP` 那组同量级）；②压强符号：门的 `pres` · `pressure_row` · oracle 三处同一口径，p₀ > 0 ——**✓已达（2026-09-14）**：`test_reconstruction_pressure_sign.py` 对 EFIT 自己的 PRES（FYDOC-CASE-23）——纯磁拟合 p₀ −48 083 → +48 087 Pa 对 EFIT +47 271 Pa、剖面 rms 365.8 Pa（门限 366），ψ · 系数 · q · 边界 · 探针/环模型逐位不变；积分恒等式；以 EFIT PRES 为动理学约束（σ 20 %）+ 号收敛、p′ > 0、rms 293.9 Pa、q₀ 1.8526（EFIT 1.8055，纯磁 1.9172），旧负号行未收敛被拒 | 开（余 ①；② 已达）〔2026-09-14 读数：中间件在会话 scratchpad，未入库；压强补丁验证：临时工作树打补丁后 `cargo test -p fylite --lib` 603 过，但读 `pres` 的内核闸（`test_reconstruction_code.py::_same` 等）因 est2 归档全跳过，**没有一条真正读到它**〕〔2026-09-14 续：补丁已落，新门 `tests/test_reconstruction_pressure_sign.py` 在 CASE-23 efit_east 上读到 `pres`，上句「没有一条读到」已失效〕 |
| **H-38** | kernel · fylite · fydoc | **彻底移除 est2**（用户 2026-09-13 晚裁定，接「facts 是装置配置唯一真源」「`basis=` / `provider=` 不作请求选项」「按测量链建模」）。续裁：范围**连内核冻结件一起**；`efit_w_pf` 连限制器一并移除、wall 缺省回 `base`；磁测留 `base`、删重复件 `east`；动理学计划改用 **efit_east 链**；真源在 fydoc/facts；**删除方式＝移入仓顶 `archive/`（镜像原相对路径）**；**CASE-03 搁置**；读 CASE-22 的 ~30 个内核模块「**先按名跳过，再建 efit_east 算例**」；B-06「**记录保留、标为撤回**」。分工：fydoc provider/链表 · `device_resolve.rs` · 公开 `fylite.io.est2`（W4）· FYL-SDD-01 归并行会话；内核计划 / 闸 / 工具 / 示例 / 登记册归本条 | ①fydoc CASE-22 归档；②内核 est2 专用工具 · 计划 · 示例归档，读者按名跳过；③`tests/data/east` 与 CASE-22 镜像归档（在 W4 公开侧跳过落地之后）；④B-06 撤回；⑤efit_east #137985 算例建成、计划改绑、B-06 后继记录以 `superseded_by` 指回 | 开〔**2026-09-13 进度**（均未提交）：①已由并行会话做（CASE-22 在 fydoc 从未入 git，只能普通 `mv` 到 `archive/cases/`；三处目录还原到 HEAD，137985 实验页指向归档件）。②已做：`git mv` `tools/{est2-dump-to-device,delivered-to-device,make-east-inputs}.py` · `docs/cases/plans/kinetic-reconstruction.fyo.jsonld` · `examples/{east137985-recon-figure,recon-to-transport}/` · `examples/scripts/forward_equilibrium.py` → `archive/…`，未跟踪的 `tools/project-kinetic-inputs.py` 普通 `mv`；**留** `case-to-fyo.py`（通用 g/a-file）· `efund-deck-to-fyo.py`（通用 dprobe 名单读取）；`mds-shot-to-fyo.py` **暂留**，去留随 W4 对 `reduce_est2` 通用 / est2 专用的拆分裁定。`test_kinetic_plan.py` 计划缺席即按名跳过；三闸 14 过 2 跳（跳过理由点名 est2 移除）。④已做：内核登记册 B-06 `assertion_state` accepted → **retired**（spo `AssertionState`：withdrawn or superseded; kept）加撤回 caveat；公开册**未跑** `benchmark-publish.py`（无运行日志时会把 09-08 已发布的复测降为「未评估」、并改写历史出处路径），只手补 B-06 的结论行与撤回 caveat，页上自陈。③待 W4 放行（已备：`tests/oracles/east_case.py` 的 `case_document` / `frozen_card` 与 `test_machine_cards_carry_no_local_path` 只在 `archive/` 下有镜像时按名跳过，单纯缺件仍报错——公开侧 W4 同规则；**演练**：内核副本里把两目录移入 `archive/tests/data/` 跑读者 26 模块，对真树基线**唯一新增失败**是登记册在树制品闸，B-06 三条 `has_input` 改指 `archive/…` 后 12 过；23 条跳过点名 est2 移除；真树基线上 `test_rust_kernels` 本就 12 败 8 错，与本条无关）；届时同一步把 B-06 的 `has_input` / account 路径改指 `archive/tests/data/…`（与并行会话议定：不豁免 retired，在树闸照查撤回记录的证据）。⑤随 H-37〕 |
| **H-39** | kernel · fylite · fydoc | **EAST #137985 在 `efit_east` 测量链上的新算例与迁移后的待裁发现**（2026-09-14）。已落：efit 几何按代拆为 `efit_green2015`〔#70745–#70754〕与 `efit_green2022_pcs`〔#137985〕（green2012 页归档）；`FYDOC-CASE-23-east-137985-efit-east`（2026-09-13 实时读 TIME[28]=4.041 s，EFIT 原值 + 整圈 ψ 换算形）；以 CASE-23 为夹具的内核闸迁移；B-06 由有条件 B 类记录 **B-11** 接替（带＝盆地内最劣值）。待裁：**F1** TokSys 锚点是外部 GA `rzrig` 输出（C-03），CASE-23 上无 TokSys 运行、不能重录；**F3** 退火设计在 CASE-23 上要 1.69× 该炮最大 BRSP（任一 λ 1.27–1.98×，PF3 对反向），闸子的 λ 边界不再起作用——设计问题（是否加线圈电流上限约束）；**F4** 门与老配方退火遍历逐位相同，挑最佳一遍的规则不同（配方：形状误差最小；门：边界间隙最小），est2 上恰好同选；**F5–F8** `test_stability` 四条的物理断言成立，失败的是按 est2 划的数值带（CASE-23 近上下对称、失稳刚度低 22%：裕度 1.196、增长比 5.33×、γ_inner 706、质量演示偏移 0.018）；**F9** efit_east 树 BCENTR −2.43 T、F<0 而 Ip>0，物理符号与 COCOS-17 符号步待定；另 CASE-23 reviewer / contact 待具名；**公开仓 EAST 读数清理**（2026-09-14 用户裁定）：`app/tests/fixtures/worker-recon.json` 的 #137985 实测读数已剥去（闸子录制时自动剥离、公开侧先验「无读数」再按名跳过），拟合派生结果（ψ 图、剖面、环模型值、result.ip）按派生结果保留，页面 Rogowski Ip 标量改相对表述；**git 历史不改写**，旧夹具读数仍在历史里 | ①F1 有 CASE-23 的 TokSys 运行或该闸改判；②F3 退火设计的线圈电流上限裁定并落闸；③F4 选定规则、门与配方一致；④F5–F8 数值带的去留裁定；⑤F9 符号定案；⑥CASE-23 review 具名 | 开〔闸子均按名跳过并写明发现；读数在会话 scratchpad `y2/`，未入库〕 |

〔本段的负面清单〕沿用上段；另：**不**把 `oracles/loop.py` 发行回公开仓（外环由宿主按计划驱动）；**不**用 EAST #137985 的读数立阈值（无动理学参照，读数只证连通）；**不**为凑孪生的好读数调门缺省（孪生量的是链，不是被量的对象）。

---

## I. CFEDR 堆芯模拟系统（PCS 驱动 10 ms 步）的落地（内核 `docs/note/cfedr-core-simulator-pcs.md`，2026-09-13）

〔由来〕会议纪要《CFETR 堆芯物理与包层热工水力集成模拟接口》（9.1 更新）要求堆芯模拟模块：PCS 每 10 ms 送
Ip · LCFS · 装置几何 · 加热指令，堆芯回 P_fusion（→ 包层）与 V_loop · βp · lᵢ · Te · Ti · ne（→ PCS），
单步墙钟 < 500 ms（**2026-09-13 用户裁定放宽**：不作硬门，逐步回报墙钟，新上限 `[TBD]`），覆盖起堆 → 爬升 → 平顶 → 下降，**2026-09-30 联调**。用户同日裁定：目标装置 **CFEDR**
（d2025；纪要原文写 CFETR）· NBI/LHW 保留接口 · PCS 只给 LCFS · PCS 发密度 / 加料 / 功率指令 · 入本台账。
**规划、缺口与评估的真源是 fylite_kernel `docs/note/cfedr-core-simulator-pcs.md`**，本节只排次序、判据与前置，
不新增裁定；两处不一致时以那一页为准。

★**本节的 `I-` 是本台账的行号**（按节字母取，与 G / H 同制）；那一页自有 `PCS-G/W/V/D*`，下表在事项里给对照。

〔定序〕09-14—16 接口表发出 + 原生耗时 → 09-15—20 步进会话 + 出口补量 + CFEDR 配置 → 09-17—23 LCFS 平衡 →
09-20—25 加料档 · 相位 · 会话面 → 09-24—27 端到端 → 09-28—30 联调。★两条关键路径各有退路（接口不变，
出口标志注明降级）：I-6 不及 → 固定 CASE-20 几何、按 Ip 缩放 FF′；I-9 不及 → 密度指令直接施加、加料只记账。

★**规划页初稿的 PCS-G1 不成立**（「续跑丢滞后态、新经典下 Te 差 61 %」）：**G-1 已于 2026-09-12 关闭**，真因是
写出的梯子缺 q，续跑现逐位相同；规划页同日勘误。

★★**改方案（2026-09-13 晚，用户）**：「修改方案，实现磁场模型，以测试完成完整建模，保留对外接口与 PCS 系统的平衡（LCFS）交互」——
堆芯侧补上磁位形被控对象（线圈 · 电路 · 自由边界 · 形状 / 位置 / Ip 参考控制器），对外仍是 LCFS。新行 I-14..I-19；I-6 并入 I-15；
规划页 PCS-D12 由此裁定。设计见规划页 §3.7（复用 `FYL-SDD-09` D-14 两档线圈驱动与 D-15「不新写第二条路径」）。

| 号 | 归属 | 事项与依据 | 判据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| **I-1** | fylite · 会议方 | **接口表 v0.1**（PCS-W0）：每步输入 Ip · LCFS 点列（点数 / 闭合 / 坐标约定，本仓 COCOS 17）· NBI / ECRH / ICRF / LHW 功率 · 密度指令 · 供气速率 · 弹丸（频率 / 粒子数 / 速度）· 可选 PF/CS 线圈电流（只核对不驱动）；输出 P_fus · V_loop · βp · lᵢ(1)/(3) · I_bs · I_CD · 等离子体电阻 · W_th · Te/Ti/ne 剖面与标量 · 维持 LCFS 所需线圈电流 · 状态标志；时间语义（第 k 步覆盖 [t_k, t_k+10 ms)，输入步内保持，输出取步末）· 拒绝语义 · SI 单位。I-11 的七问随表提出 | 表发到 PCS 与包层方；I-11 每一问在表里有对应字段或标 `[TBD]` | **完成**（原：开，关键路径，09-16 前）。★**2026-09-14 发送稿已定**（内核 `docs/note/cfedr-pcs-interface.md` v0.1，据 3837e04 草案改）：按改方案补实现 LCFS（`lcfs_real_*`）· 偏差 · 虚拟位置反馈电流 · CS 磁通余量（电路就绪前空值）；EC 按 REPORT-20 tbl-r20-hcd 改为三组（共向 / 反向 / 爬升段，NTM 用 EC 是否单列 `[TBD]`）；写明 βp · lᵢ(1) · lᵢ(3) · V_loop 的定义（Q-3 对口径用）、起堆段（t < 1 s）LCFS 可空、点序不限；I-11 ①..⑦ 与表内 Q-1..Q-7 一一对应，各有字段或 `[TBD]`，请 09-20 前书面回复。★★**完成 2026-09-14**（用户裁定「参考 IMAS DD 标准和常规通行操作，先自行回答 I-1 的问题，不需要征询意见」⇒ 判据「发到会议方」由自答定稿代替）：接口表升 **v0.2**，七问改为自答（见 I-11 行），输入输出各加「DD 对应」列；★据 DD（本地 `third_party/IMAS/IMAS-Data-Dictionary` 4.1.1 · COCOS 17，表中所引 DD 路径逐条核过）改两处口径——**βp 改为 DD 体积分式 4∫p dV/(R₀μ₀Ip²)、p 取总压**（v0.1 写的是周长式热压，即 I-4 已落的 `βp,th`）· **lᵢ(3) 的 R₀ 改为真空场参考半径**（I-4 已落的取边界几何中心）⇒ 步出口须按 v0.2 改口径（随 I-4 / I-8）；`i_cd` 改由 `i_ni` − `i_bs` 给、`r_plasma` 定为 P_ohm / I_ohm²、`lcfs_pf_mismatch` 阈 0.05 m、剖面 51 点 |
| **I-2** | kernel | **原生耗时实测**（PCS-V1）：本机 `--release` 上一个 10 ms 外步（含内步）· `code/discharge` 在 CFEDR 盒上冷启 8 遍与热启 1–2 遍 · `neutrals_mc` 一次 · 解析沉积重算一次；忽略式 PROFILE 测试，不进门测。已记读数都来自浏览器或别的算例（自由边界 65² ≈ 1.2 s · ITER 15 MA 400 步 0.4–0.6 s） | 读数入规划页；目标外步 ≤ 50 ms、热启平衡 ≤ 400 ms（放宽后为参考值） | **完成 2026-09-13**（`rust/fylite/src/pcs_profile.rs`，全 `#[ignore]`）：10 ms 外步 closure 0 **1.9–3.5 ms** ✓ · **closure 2 1.9–4.7 s ✗**（`neo_chi_i` 热点）· 交换上限钉 dt 0.275 ms＝**37 子步**（原估 7 不成立）· 固定盒热启 4.8–40 ms ✓ · `outer::resolve` 109 / 303 ms · 自由边界冷启 504 / 1322 ms ✗（热启在替身外场上不收敛）· `neutrals_mc` 1e4 史 32 ms · EC / IC 沉积 µs 级。表见规划页 §2.1 |
| **I-3** | kernel | **步进会话门**（PCS-W1，暂名 `code/core_step`）：走 `transport::CoreMarch`，闭包内核侧装配 CASE-20 已落的物理 —— 多离子成分 · Redl 电导 · 表格沉积 × 指令功率 · 锚定给定 χ 与 D/V（χ 形状取 CASE-20 S4 解，幅值按 ITER89-P / IPB98(y,2) 定标，出口标「锚定层，非预测」）· 规定档密度。★今天这些物理在含时推进上被拒（`case.rs:769-776` 只放行 closure 4）。〔推荐〕新门、不解开 `code/evolve` —— 页面路径与浏览器逐位互持，解开会移动已录答案 | 1 000 个 10 ms 单步 ≡ 一次 10 s 连续推进，逐位（PCS-V2）；CASE-20 平顶指令下 P_fus 1.51 GW、W_th 807 MJ 各 ±5 %，V_loop 落 0.02–0.03 V（PCS-V3） | **已裁 2026-09-13：解开 `code/evolve` 的拒绝**（用户；〔推荐〕新门未取）。落法：species / 表格沉积 / Redl 电导作 opt-in 设置、缺省不变，使页面已录答案不动——若判据显示缺省路径逐位移动即停下另报。★**已落（内核未提交）**：`scenario::evolve_heat_with` + `MarchPhysics`（`evolve_heat` 是其 `None` 包装）；门只在 probe · 密度通道 / `quasi` · 表格旁再给 pe / pi / beam / icd 时拒绝；新判据 `the_march_takes_tables_and_resolved_species_opt_in`（表格即高斯时 Te / Ti 逐位同 · species 上 Z_eff 2、账 < 1e-10 · Redl 移动 ψ · 两条拒绝）；旧判据 `species_settings_are_inert_on_the_page_and_refused_where_they_cannot_ride` 按裁定改写；内核 lib 604 过、1 忽略。★声明降级：推进算子离子通道按 n_e 加权。★**V3 实测**（`cfedr_gates::pcs_v3_the_flat_top_holds_on_the_march`；★★勘误：实为 0.082 s 不是 3 s，且 χ 未换算，本段读数与归因作废）：W_th −5.29 %、P_fus −10.77 %、T_i0 −2.3 %、0.6 ms/步；补离子权重只到 −4.86 / −9.43 % ⇒ 降级非主因，α 份额与辐射经冻结 ONETWO 剖面也排除（W_th −4.82 / −4.76 %，T_i0 仍 24.02 keV），余候选 ONETWO χ 口径 · 交换 · 对流与 CX / 电离项〔推测〕。★★**真因已定（同日）**：ONETWO 分析 χ 不带几何因子，推进导热带 gm3，ρ_N 0.95 处多传 2.5–2.7 倍——χ 须除以 gm3（`cfedr_gates::pcs_chi_convention_against_onetwo_power_flows`）；换算后 V3（0.082 s）W_th −0.30 %、P_fus −3.0 %。★★★**芯部链在给定 LCFS 上跑通**（`case.rs::pcs_core_chain_tests::pcs_core_chain_on_the_given_lcfs`，`#[ignore]`）：CASE-20 交付边界 → 追踪梯子 52 面 → `code/evolve`（species · RF 表 · χ/gm3 · 电流通道 Redl + 自举）分块续跑，0.18 s/块、能量账 ~1e-13；8 s 内 W_th 801 → 301 MJ、P_fus 1540 → 144 MW 且加快——〔推测〕固定 χ 的燃烧态热不稳定。★★**χ 定标律缩放已落（2026-09-13，用户「go 1」，内核未提交）**：`code/evolve` 设置 `chi_scaling = ipb98`（opt-in，缺省不变；无给定 χ 按名拒绝）→ `scenario::ChiScale`：每步 χ = χ_给定 × (P_loss/P_ref)^0.69 (n/n_ref)^−0.41 (I_p/I_ref)^−0.93 × f_int，夹 [0.1, 10]，P_loss = P_aux + P_α + P_ohm（辐射不扣）；τ_E 锚 f_int ← f_int (W_th/W_target)^(dt/τ_c)，W_target = W_ref (P/P_ref)^0.31 (n/n_ref)^0.41 (I_p/I_ref)^0.93（参考处 H 固定），`chi_scale_tau` 缺省 1 s、0 为开环；参考量取首步（首步逐位同不缩放），经 facts `chi_scale_{ploss,ne,ip,w}_ref` · `chi_scale_int` 交续跑；出口注记「锚定层，非预测」。判据 `the_given_chi_follows_ipb98_opt_in`（首步逐位 · 半加热降 χ · 锚升 χ · 拒绝）；lib 608 过 24 忽略。**芯部链 16 s 读数**：固定 χ 8 s 内 W_th 801 → 301 MJ 加快；开环缩放收敛但沉底 633 MJ、P_fus 995 MW（因子 0.81，给定形状在本推进上本就失衡）；锚 τ_c 1 s：W_th 731–830 MJ 欠阻尼振荡（周期 ~9 s，第二谷 776 MJ 渐衰），16 s 处 776 MJ · P_fus 1420 MW，块墙钟 ≤ 0.21 s/40 步；τ_c 0.3 s 不衰（766–826 MJ，周期 ~6 s）· τ_c 3 s 谷更深（708 MJ）回升更慢。⇒ 纯积分锚欠阻尼。★★**比例项已加并定为缺省**（`chi_scale_kp`，f ×= (W_th/W_target)^k_p，参考步为 1）：τ_c 1 s 下 k_p 1 过冲到 804 MJ 后回 792；**k_p 3 单调**——首 1.6 s 谷 762 MJ，16 s 处 W_th 793.4 MJ · P_fus 1472.3 MW。★**PCS-V3 在芯部链上定阈并过**（`pcs_core_chain_on_the_given_lcfs` 在缺省缩放、≥ 16 s 时断言 ±5 %）：W_th −1.69 %（对 807 MJ）· P_fus −2.50 %（对 1510 MW）；lib 608 过 24 忽略。★★**βp/lᵢ 进步出口已落（PCS-W2 / I-4，内核未提交）**：`code/evolve` 设置 `globals = 1`（opt-in，缺省记录不变）→ 步末态的 facts：W_th · W_fast · P_fus · Q · P_loss · τ_E · τ98 · H98（含辐射扣除版）· β_N（热/总）· f_GW · 体积 · m_eff 走 `outer::globals`（与通量匹配同一份）；lᵢ(3) = 2∫⟨B_pol²⟩dV/(μ0² I_p² R0)、lᵢ(1) = ⟨B_pol²⟩_V/B_pa²、βp,th = 2μ0⟨p_th⟩_V/B_pa²，⟨B_pol²⟩ = ψ′² gm2/(2π)²（ψ 整圈），R0 取边界几何中心，B_pa = μ0 I_p/L，L 取文档边界（`lcfs_perimeter_document` 1）或 Miller 轮廓（0）；`ip_edge` 为梯子边缘面的安培定律电流；ψ 平（电流通道关）时 lᵢ 不给（NaN + 注记）。判据 `the_step_states_its_globals_opt_in`（缺省无此 facts · βp 对 globals 热压逐位 · P_fus 对末步 α 功率逐位 · 不动推进 · ψ 平不报 lᵢ）。**PCS-V4 读数**（芯部链 CASE-20 给定 LCFS）：βp,th 1.366 = 驱动独立算；lᵢ(1) 0.954（ONETWO ali 0.946）；Q 15.2 · H98 1.05 · β_N 2.44；lᵢ(3) 步出 0.661 **= ψ 图在 LCFS 内且 ψ_N ≤ 0.98 的积分 0.661**（edge 0.995：0.680 / 0.681）⇒ 公式对；差在域截断——LCFS 内全量 0.708（截 0.98 −6.6 %、0.995 −3.8 %；梯子边缘电流 14.00 / 14.89 MA）。★★**`surfaces::li3` 在偏滤器位形上高估**：它取 box 内所有 0 ≤ ψ_N ≤ 1 的格子，X 点下的私有通量区也算进去——CASE-20 图上 0.886 对 LCFS 内 0.708（+25 %）；它还喂重构判据与 V13 打印，**未改**（改会移动已录答案，待裁）。★★**平衡按节拍重解已接进芯部链（测量，驱动 env `PCS_RESOLVE_EVERY`，缺省关）**：每 k 块在推进态上跑 `code/steady_equilibrium`（source = inversion · I_p 持 15 MA · 框边界取当前图），文档保留给定 LCFS 轮廓，交回下一块的是新 ψ 图 + 轴 + 重采到**均匀** ψ_N 表的 q/F + Miller 行 + 按 ρ_norm 重映射的态（`lag_reset`）；参照＝交付图自身的 recovered 零测试（0.45 s，轴 (8.251, 0.959) 对交付 (8.277, 0.909)）。**40 块、每 2 s 一解读数**：墙钟 0.69–0.97 s/次（首次 306 迭代，其后 131–169，全收敛）⇒ **超 500 ms，须后台解、晚一拍换几何（W4 退路）**；**LCFS 未钉住**——99.5 % 面对参照 rms 0.096–0.100 m · max 0.41–0.43 m、轴上移 +0.11 m、κ −0.08…−0.09、R_geo −0.04（有界不增长）；**电流峰化**：q0 1.16 → 0.89 → 0.73 → … → 0.42（16 s 仍降；源给 I_p 13.0 → 14.8 MA，FF′ 常数补足；无锯齿模型）——与 20 MA 链 q0 0.77 同一问题；能量账全程 ~1e-13；16 s 处驱动读 W_th 842 MJ（+4.3 %）· P_fus 1457 MW（−3.5 %），V3 仍过（★驱动 W_th 用探针梯子 V′，重解后度规已变，偏；以步出 globals 为准）。★★**交接缺陷已修**：重映射后的态再绑 `evolve/fylite:vprime_old` 会把度规移动算两次——首块账坏到 0.98、T_e0 27.3 → 30.0 keV；去掉后 8e-14、27.7 keV（`PCS_RESOLVE_VPOLD` 缺省 0）。★坑：march 记录无 psi_N 与 Miller 行，而 probe 拒 species/表格——驱动另跑一次裸 probe 取这些行。⇒ 持 LCFS 需要**贴合给定轮廓的固定边界解**或把 C4 位置控制接到框重解上；另需锯齿/q0 钳位。★★**持 LCFS 试验一：框边界真空场等通量修正（用户「go」，时间要求放宽后，内核未提交）**：`outer::resolve_held` + `outer::outline_gap` + `HoldOptions`（`code/steady_equilibrium` 设置 `hold_boundary = 1` · `hold_passes` · `hold_tol` · `hold_basis` · `hold_relax`，缺省 8 · 0.07 m · 2 · 0.5）——每遍重解后读 ψ 于给定轮廓，最小二乘拟合真空场 1 · x² · y · x²y · 四极 · 六极 · 八极（均满足 Δ*ψ = 0）的前 n 项，半步加到全图（框边界作 Dirichlet、内部作起点），等离子体内部自由（★不走轮廓外钉 Dirichlet：fyeq 实测压 Shafranov 位移 ~5 倍）；距离 = |ψ_b − ψ| / |∇ψ|，|∇ψ| 低于中位 1/4 的点（X 点邻域）不计。**读数**（`outer::tests::the_held_resolve_on_the_delivered_map`，`#[ignore]`）：**交付轮廓在交付图自身的 ψ_b 面上就差 6.0 cm rms（最坏 16.5 cm）＝本文档的底**；零测试重解 15 cm；基 2（偏移 + 垂直场）relax 0.5：8 遍 7.9 cm、12 遍 5.8 cm，不拒；relax 0.75 四遍到 6.2 cm 后第五遍溢出框；基 1 到 9.0 cm 后溢出；**基 3（加 Z）· 基 4（加径向场）· 基 7（加形状项）一到两遍即溢出框**。**芯部链上（行进态、inversion 源）**：基 2 首次 20.0 → 17.0 cm、第二次 17.3 → 17.3 cm **不再下降**，2.3–3.2 s/次；对不持的重解仅小改善——99.5 % 面 rms 9.6 → 8.8 cm、最大 41 → 29 cm、R_geo −4.8 → −2.3 cm、κ −0.08 → −0.06，轴仍上移约 +10 cm。⇒ **框边界真空场持不住偏滤器 LCFS**：能纠正垂直位置与形状的项都使等离子体溢出框（推测经 X 点——框解的拒绝不分四个出口，未确认），稳定的两项纠不了形状。余路：(i) 贴合给定轮廓的固定边界解，须保住 X 点拓扑（如限制在 ψ̄ 0.995 面或内边界正则化）；(ii) 自由边界 + C4 位置控制（路线 A，CFEDR 上已得 κ 1.88–1.92）按节拍热启重解，放宽时间后可同步做——**用户已选 (ii)（2026-09-13）**。落法：复用 V11 门的装配——外场＝交付图减交付等离子体丝流、C4 设定点＝给定 LCFS 的**电流形心**（V11 门实测 LCFS→交付 rms 2.0 cm、κ 1.860；R0 偏内 0.33 m 是 `code/discharge` 退火取几何形心所致，非 C4 本身）；源取推进态 p′/FF′，I_p 持，热启上一解。★★**已接进芯部链（测量，`PCS_RESOLVE_FREE = 1`，内核未提交）**：V11 装配（外场＝交付图 − 5273 丝流，设定点电流形心 (7.988, 0.805)，65×65 装置盒 R [4.71, 10.58] Z [−5.95, 6.20]，kp 2 · ki 0.05 · 电流欠松弛 ½），源取 inversion 门的 `dpressure_dpsi`/`f_df_dpsi`（表只作形状，求解每轮归一到 I_p），态留在其 ψ_N 节点、ψ 取自由图，q/F 取框门代理。**读数**：①表截在梯子边 ψ_N 0.98、其外夹住末值 ⇒ 2 s / 4 s 两次都 **3000 轮不收敛**（残差 5e-2 / 0.39，8.9 s，fb_z 到 −7.1 MA）；②在 ψ_N 1 补零（`PCS_FREE_EDGE0`，缺省 1）⇒ **142 / 152 轮 settle，0.22 s/次**，diverted，轴 (8.291, 0.861) / (8.261, 0.860)，形心在设定点，**LCFS 对给定轮廓 rms 10.2 / 8.9 cm、max 23 cm**，κ 1.83，**fb_r +3.4 MA（~23 % I_p）**——C4 只定形心，行进态（βp、电流峰化与交付不同）在交付外场下要不同形状场。★装置盒上隔离（`discharge_cfedr_tests::route_a_profile_edge_on_the_delivered_rig`，`#[ignore]`）：交付回收源在 ψ_N 1 **不为零**（p′ −2.1e6）；原样 170 轮 settle rms 2.0 cm；截 0.98 夹末值 272 轮 settle 4.9 cm；截后补零 3000 轮不 settle、2.8 cm ⇒ 补零对链有效但不普适（交付源上反而拖慢）。下一步：在自由重解里加**形状环**——两次受控解之间按给定轮廓拟合真空场（x² · y · x²y · 四极 · 六极，不含偏移）半步加到外场并持续（线圈随等离子体走）。★★**形状环第一版失败**（`PCS_FREE_SHAPE_PASSES` 4 · 基 x² · y · x²y · 四极 · 六极 · 半步，`outer::vacuum_basis` / `outer::fit_vacuum` 已抽出、`resolve_held` 同用）：2 s 处首解 11.3 cm settle，改外场后每解 **3000 轮不 settle**、轮廓差 19 → 16 → 21 → 24 cm，fb_r +4.6 MA，墙钟 35 s；4 s 处承接坏外场一直 18–23 cm、42 s；推进本身账 8e-14 不受影响；lib 609 过 27 忽略。〔推测〕拟合里的位置项（x² 垂直场、y）与 C4 抢位置——C4 把电流形心拉回设定点，拟合再推开，互相打架；下一步只拟合形状项（四极 · 六极 · 八极），位置交给 C4。★★**扫描否定该推测**（`PCS_FREE_SHAPE_FIRST` / `_BASIS` / `_RELAX`，每次重解 3 遍）：只拟合四极 + 六极（半步）→ 2 s 处 11.3 → 19.2 → 19.5 → 19.4 cm、4 s 处 15–18 cm，改外场后每解 3000 轮不 settle、27–34 s；加八极（0.3 步）→ 11.3 → 17.5 → 20.8 → 16.6 cm、4 s 处 12–18 cm，同样不 settle；x² + y 小步（0.2）→ 每解 160–240 轮 settle、约 1 s，但轮廓差不降反升（11.3 → 12.3 cm，4 s 处 10.9 → 11.4 cm）。⇒ **把轮廓通量误差直接加进外场的形状环在任何变体下都不收敛到给定轮廓**：它忽略了等离子体对每项修正的响应（电流与浮动的 ψ_b 都跟着变）。形状环缺省关（`PCS_FREE_SHAPE_PASSES` 0）；**C4 单独**是目前最好：LCFS 对给定轮廓 9–10 cm rms、0.22 s settle。下一步候选：**测量响应矩阵**（每项外场修正对轮廓误差的有限差分自由解）再做岭最小二乘——即 `code/discharge` 退火对线圈电流已有的做法（响应求积 `nu`）；或直接用装置卡线圈走 `code/discharge` 退火（需 `CFEDR_DEVICE_JSON`，并修其取几何形心的设定点）——**用户已选前者（2026-09-13「1」）：测响应矩阵 + 岭最小二乘**。★★**第一版读数**（`PCS_FREE_RESP_PASSES` 3 · 项 x² · y · x²y · 四极 · 六极 · 差分步 1 % 通量跨度 · λ² 1e-2×列范数² · 松弛 0.7；55 个固定轮廓点，X 点邻域剔除）：2 s 处 base 11.2 → **第一遍 9.2 cm**（4/5 列 settle）→ 10.6 → 12.9 cm，κ 终 1.745、fb_z −5.0 MA；4 s 处承接变坏的外场 base 12.7 → 12.4（不 settle）→ 10.0 → 11.2 cm，fb_r +7.3 MA；每次重解 21–25 s；推进账 1e-13；lib 609 过。⇒ **响应矩阵第一步确实降差（−2 cm，首个有效的修正）但其后发散**，且坏外场遗传到下一次重解。下一步：阻尼牛顿——只接受降差的步（否则步长减半至多三次，无可接受即停，外场不变坏），再扫 λ / 差分步 / 松弛。★★**已加并扫描**（每次重解 4 遍，11 块链，2 s / 4 s 两次重解；「LCFS→给定轮廓」为线段距离 rms / max）：**C4 单独** 10.2 / 23 cm · 8.9 / 23 cm，0.22 s；λ 1e-2 · 差分 1 % · 松弛 0.7 → 轮廓差 11.2 → 9.2 → 9.1 cm 后无可接受步，LCFS 10.0 / 55 cm · 10.0 / 54 cm，64 s / 19 s；λ 1e-1 · 1 % · 0.5 → 11.2 → 10.8 → 10.5 cm，LCFS **9.1 / 23 cm · 8.5 / 22 cm**，54 s / 2.4 s；λ 1e-1 · 0.5 % · 0.5 → 11.2 → 11.2 cm，LCFS 9.5 / 25 cm · 8.7 / 23 cm，22 s / 2.5 s。推进账全程 ~1e-13。⇒ 单调接受保住了不变坏，但**最好的设置只比 C4 单独好约 1 cm**，而墙钟涨 10–300 倍；大多数试步或抬高差、或 3000 轮不 settle——线性响应在相邻自由解之间几乎不成立〔推测未验：C4 每解重启 · 65×65 装置盒格子 ~9×19 cm · 缺列〕。**响应环缺省关（`PCS_FREE_RESP_PASSES` 0），持 LCFS 仍以 C4 单独为准（9–10 cm，交付轮廓自身对 ψ_b 面的底 ~6 cm）**。★★**加密装置盒不是出路**（`rig_n`，`PCS_RIG_N` / `RIG_N`，缺省 65 不变）：129×129 上 C4（65 上调的增益）**交付源本身都不 settle**——3000 轮、残差 0.34、fb_z −5.8 MA、63 s、LCFS rms 5.6 cm（65 上 170 轮 settle、0.27 s、2.0 cm）；截 0.98 夹末值 9.0 cm、补零 3.0 cm，均不 settle、55–66 s；芯部链 2 s 处 C4 单独 3000 轮不 settle、fb_z −6.9 MA、53 s、11.9 cm（65 上 0.22 s、10.2 cm）；4 s 处未见自由重解输出（输出经过滤，原因未查明）。⇒ 65×65 上的 ~9 cm 不能靠加密网格消掉，129 上须重调 C4 增益；**持 LCFS 线在此收住：C4 单独、65×65、0.22 s、9–10 cm**。★`tools/gen-fyo-kernel-graph.py --check` 在 HEAD 上即 STALE（非本批所致，未重生成）。余：V2 分步等价 · V3 漂移归因与定阈 · 开。★★**V2 分步等价读数（2026-09-14，内核未提交）**：芯部链加末态指纹（`PCS_BLOCKS` × `PCS_STEPS`，te · ti · ne · ψ 位哈希）——**40×1 与 1×40 不逐位相同**。二分：① `code/evolve` 在 Miller 上逐开关（热 · 离子种类 · 表格 · 给定 χ · IPB98 · 电流 · 全开）1×2 ≡ 2×1 **逐位相同**（新判据 `a_resumed_march_is_the_same_march_under_the_pcs_opt_ins`）；② 链上补交 Ip 控制器三量 · `capped_in` · `dt_fraction_in` · `state = 1` 均**不改指纹**；③ **追踪（g-file）档 + `reference` 起步**：只开热通道两步即分（te 内点，ti 同），ONETWO 梯子 bound 档同设置两步**相同**；④ **电流通道**：bound 档两步 te/ti 同、ψ 内点分，五步 te/ti 亦分，候选 `conductivity = redl` · `trapped_fraction = miller` · `vloop 0.02`（Miller 判据当时未开）。门的续跑读状态逐位拷贝（非重采样）。锯齿开关 `PCS_SAWTOOTH` 已加（补 `saw_mix`）待测。★★**量级（同日，完整 te 导出逐点比）**：差只在舍入级且不放大——追踪档只开热通道两步：52 点中 8 点不同（边缘 43–50 号），最大相对差 **2.05e-16**（一个 ulp）；追踪档缺省（电流开）40×1 对 1×40：50/52 点不同，最大 **3.18e-15**、中位 7.0e-16；bound 档电流开 5×1 对 1×5：158/201 点，最大 **2.11e-15**。内层迭代从本步起点 `y_old` 起（`transport.rs` `solve_step`），不带上一步历史 ⇒ **不是容差收敛差，也不是交接漏量**（Miller 上逐开关逐位相同、链上补交各量不改指纹、`vprime_old` 未绑），而是梯子档上「调用第一步」的某个量走了不同算术路径。〔推测，未定位到行〕⇒ **PCS-V2「逐位相同」在梯子档上今天不成立，差 ≤ 3.2e-15 相对**；~~待裁：判据保持逐位还是改为 ≤ 1e-14 相对~~ ★★**已裁 2026-09-14（用户「改阈 ≤ 1e-14 相对」），内核已落**：门 `a_resumed_march_is_the_same_march_under_the_pcs_opt_ins` 由逐位改为逐节点相对差 ≤ `V2_REL_TOL` = 1e-14（逐位相同照报「bits differ in []」，Miller 各开关今天仍逐位同）；芯部链 `pcs_core_chain_on_the_given_lcfs` 加 `PCS_V2_DUMP=<文件>` / `PCS_V2_AGAINST=<文件>` 两次分块对判（te · ti · ne · ψ · t_end 共 209 值，超阈即断言失败）。**判定读数**：追踪档缺省（电流开）40×1 对 1×40——位不同 141/209、**最大相对 3.5e-15 ≤ 1e-14 ⇒ PCS-V2 过**。★★**锯齿钳 q0（同日，读数）**：芯部链 `PCS_SAWTOOTH = 1`（推进自带 Kadomtsev 混合，`saw_mix` 1，`saw_period` 0）+ 每 2 s 按节拍重解：**推进 q0 自第一次重解后恒为 1.000**（2.4 → 8 s；同设置不开锯齿时 16 s 降到 0.42）；4–8 s P_fus 1492–1517 MW · W_th 810–817 MJ（对 V3 的 1510 MW / 807 MJ 在 ±1.3 % 内）；重解本身报 q0 0.886 → 0.816 → 0.809（其源取混合前的推进态，钳位在推进上）、墙钟 1.24–1.67 s/次 ⇒ 锯齿 / q0 一项收住 |
| **I-4** | kernel | **步出口补量**（PCS-W2）：βp · lᵢ(1) · lᵢ(3) · P_fus · Q · W_th · β_N · V_loop · I_bs · I_CD · 等离子体电阻；复用 `outer::globals` 与 `surfaces::li3`，不另写一份。今天 βp 只在 0-D、li3 只从 ψ 图算、`globals` 只挂通量匹配出口 | lᵢ(3) 对同一 ψ 图上的 `surfaces::li3`、βp 对 0-D 同式；阈 `[TBD]`，先读数（PCS-V4） | **完成 2026-09-14**（原：开，等 I-3）：`code/evolve` `globals = 1` 已出 W_th · P_fus · Q · τ_E · H98 · β_N · f_GW · lᵢ(1) · lᵢ(3) · βp,th（2026-09-13，PCS-V4 读数见规划页）；★**同日按接口表 v0.2（IMAS DD 4.1.1）补 DD 口径出口**：`beta_pol` = 4∫p dV/(R₀μ₀Ip²)（总压 = 热 + 快 α）· `li_3` = 2∫B_p²dV/(R₀μ₀²Ip²)，R₀ 取真空场参考半径（g-file rcentr，`dd_r0`）· `i_bs` / `i_cd` 以推进自身面积元 V′/(2πR₀) 积分步末剖面 · `i_ni` = 两者之和 · `i_ohm` = Ip − `i_ni` · `r_plasma` = P_ohm / I_ohm²；缺省记录不变（判据 `the_step_states_its_globals_opt_in` 扩展：`beta_pol` 对总压式逐位 · `i_ni` 逐位为两部之和 · 平 ψ 不报 `li_3`，过）。**CFEDR 芯部链读数**（`pcs_core_chain_on_the_given_lcfs`，首块）：`beta_pol` 1.043（周长式热压 1.366；ONETWO betap 1.434，口径不同）· `li_3` 0.663（R₀ 7.800 m）· I_bs 5.86 MA · I_cd 1.12 MA（设计 EC 净 1.08 MA）· I_ni 6.98 MA · I_ohm 8.02 MA · R_p 2.74e-9 Ω（R_p·I_ohm 0.022 V 对 V_loop 0.020 V 自洽）；V3 仍过。★`surfaces::li3` 私有通量区高估（+25 %）仍待裁，不属本行 |
| **I-5** | fydoc · kernel · fylite | **CFEDR 配置**（PCS-W3）：装置卡由 fydoc 重导（内核 `machine_desc/cfedr` 过期：`wall` · `default_grid` · 磁测均 `fylite:absent`）并补解算盒与限制器；平顶锚点取 CASE-20 态；爬升按 fydoc `FYDOC-REPORT-20` 表 tbl-r20-timeline 的节点落成指令回放文件（爬升段 LCFS 无语料，先用 Miller 参数插值并标「合成」）。★**解算盒的落点受 K-2 第二批制**（「解算盒四边与网格数 → 计划设置」随 O-5 B 批）：O-5 未批前按卡片现形放，批后随 K-2 迁 | `code/discharge` 在 CFEDR 卡上起得了盒；回放文件每个节点追得到 REPORT-20 的行或标「合成」 | 进行（2026-09-13）：回放文件 17 节点落内核 `docs/cases/pcs/cfedr-d2025-replay.json`（逐节点出处，下降段合成）；卡片由公开仓 `tools/abox-to-facts.py cfedr` 自 fydoc 导出（带 20 顶点插图第一壁 + 导出盒 R 4.71–10.58 / Z −5.95–6.20；~~缺逐线圈电流上限与 `solver_dims`~~）；★★**2026-09-14 两项已补**（导出脚本改，`dist/` 为构建产物重生成，核过）：① 逐线圈上限——A-Box `pf_active/base` 的 `current_limit_max`（104014 表 1 每匝 Limit × Turn 的整线圈安匝：CS 44.28 MA · PF1 25.74 MA · PF2/PF3/DC1 13.20 MA，15/15）导为线圈条目的 `fylite:i_max_aturn`；★**不写进 DD `coil/current_limit_max`**：DD 4.1.1 定义为「导体中可承受电流」、以 `b_field_max` 为坐标的二维表，量纲语义不同；卡片上的值由哪个门读（`code/breakdown` 今天只折 `pf_active/supply`，`code/discharge` 收算例侧 `i_max_aturn`）~~仍是 F-9 待裁~~ ★★**F-9 已裁 2026-09-14（用户「discharge 缺省读卡片」），内核已落**：`code/discharge` 在算例不绑 `i_max_aturn` 时读卡片 `pf_active/coil/fylite:i_max_aturn`（`fold_card_limits`：元件上限 = 额定 × 元件匝 / 线圈总匝，通道上限 = 各元件上限 / |权重| 取小；无映射卡片上通道上限即线圈额定），与 `pf_active/supply` 同在时逐通道取较紧者，部分线圈无额定则拒（不留无声的无界通道），`card_limits = 0` 退回旧读法；`code/breakdown` 不变（仍折 supply）；判据 `the_card_coil_ratings_fold_into_channel_limits`；CFEDR V13 的无上限对照组改带 `card_limits = 0` ② `solver_dims` nw 65 · nh 65——程序侧取值（生成器 `PROGRAM_SIDE["cfedr"]`，注明非装置事实、无 libefit 构建），即内核 CFEDR 门实测所用网格（C4 在 65 settle、129 同增益不 settle，见 I-3）；★内核 `machine_desc/cfedr` 已被他会话暂存删除，卡片落点随 facts / O-5 定；`code/discharge` 在 CFEDR 上能跑但**塌陷**（见 I-14）· 开 |
| **I-6** | kernel · fylite | **LCFS 平衡节拍**（PCS-W4）：`code/discharge` 收**点列目标边界**（今天只由 `r0 a kappa delta_*` 生成）· 源取推进当前的 p′/FF′ 与 Ip · 上一节拍线圈电流 + ψ 热启（`warm=1`）· 每 0.5 s 或 LCFS / Ip 变化超阈重解 · 超 400 ms 后台解、晚一拍换 · 不收敛保持上一几何并置标志 · ψ 图 → `outer` 梯子 → **按 ρ_norm 搬态**（CASE-20 外环教训）；PF 电流一致性核对（给了就作外场解一次、报 LCFS 偏差，不回灌）。★不走「轮廓外全域钉 Dirichlet」的定形边界（fyeq 实测 Shafranov 位移小约 5 倍）。★与 **F-1** 同门：F-1 让 `code/discharge` 按整圈 Wb 写出 p′/FF′，本条要它读进推进的表 | CASE-20 `rbbbs/zbbbs` + 本仓取回的源 + 15 MA 复原：读轴位置差 · q 在 ψ̄ 0.1–0.95 的中位差 · **边界点到解出 LCFS 的最大距离**（PCS-V8）。★**不得只判 `free.residual`**：F-34 量到塌掉的位形可有全表最小残差 | **并入 I-15**（改方案 2026-09-13 晚） |
| **I-7** | kernel · fylite | **相位**（PCS-W5）：L-H 由 P_sep > P_LH（Martin08）加滞回触发，台基高度取 CASE-20 EPED 读数；t < 1 s 由 0-D 层给输出、1 s 起切 1.5-D（Ip 0.25 MA · lᵢ(3) 1.3）；13–17 s 位形转换切零点行；下降段按 I-11 的回答，未答前合成（Ip −0.25 MA/s 对称于爬升 · 先撤 IC 后撤 EC · n̄e 随 Ip 按 Greenwald 分数降） | 0 → 150 s 过位形转换、过 L-H、合成下降到低 Ip，无 NaN、无拒绝；判跳变的办法 `[TBD]`（PCS-V5） | 开（原：等 I-3 · I-6）。★★**2026-09-14 L-H 相位已落（内核未提交）**：`code/evolve` opt-in `lh_model = martin08`（`scenario::LhModel` 经 `MarchPhysics` 进推进，**不动 fyo 表、接口摘要不变**）——每步末 P_sep = P_aux + P_α + P_ohm − P_rad 对 `zerod::p_lh_threshold`（n̄_e 取体平均作线平均代理 · B₀ · `surface_elliptical(R₀, a, κ)`）；L → H 于 P_sep > `lh_on_factor`·P_LH（缺省 1.0），H → L 于 P_sep < `lh_off_factor`·P_LH（缺省 0.8）；H 相 EPED1-NN 台基定下一步边界（滞后一步，同固定台基），回 L 时给定边界复位；`lh_phase_in` / `lh_phase_out` 交续跑，逐步迹 `lh_phase` · `p_sep` · `p_lh` 与 `lh_transitions` 进记录；与 `pedestal` 同开按名拒。判据 `the_march_takes_an_lh_phase_opt_in` 过（阈不及时逐位同页面 · 阈极低进 H、台基动边界 · 续跑于 H 保持 · 三条拒绝）；芯部链开关 `PCS_LH`。★**链上读数（同日，CFEDR 平顶，给定 χ + IPB98，4 s）**：首步即 L → H（P_sep 268 MW 对 P_LH 155 MW），其后恒 H、无回切；EPED 台基顶 4.21 → 4.20 keV；P_fus 1505 → 1390 MW（4 s，对 V3 的 1510 MW −7.9 %，**出 ±5 % 带**）· W_th 815 → 784 MJ（−2.9 %）⇒ 相位判对，但 H 相改由 EPED 台基定边界后平顶掉功率〔推测：EPED 顶温与 CASE-20 在 ψ_N 0.98 的给定边界不同，未比〕；★内核全量 615 过。余：台基锚定（EPED 读数对 CASE-20 边界）· 起堆 t < 1 s 零维层（宿主）· 13–17 s 位形转换切零点行（随 I-15）· 合成下降段（随 I-10）|
| **I-8** | fylite | **宿主会话面**（PCS-W6）：`engine/serve.py` 的 JSON-RPC stdio 加 `session.open / step / close`；状态留在会话里；平衡的后台线程在此层；每步记墙钟；关会话落 `manifest.json` 与步耗直方图。★不引入服务端组件（`NR-ENV-001`），TCP / 共享内存适配放集成框架侧。★会话给了 **G-3** 一个宿主介入点（`cancelled` 可由会话产），但不替 G-3 裁 | 会话往返；按名拒绝：LCFS 不闭合或约定不符 · dt ≠ 10 ms · I-12 定下的 NBI/LHW 行为（PCS-V6） | **完成 2026-09-14**（原：开，等 I-3 · I-4）：公开仓 `python/fylite/engine/session.py` + `serve.py` 的 `fylite.session.open / step / close`（JSON-RPC 2.0 stdio，状态留在会话里）。每个 10 ms 步是内核 `code/evolve` 的若干次单步调用：★**实测交换上限把一次调用压到 ~1.5 ms（Miller 缺省）**，故宿主把「到步末还剩的时间」作 `dt` / `dt_start` 交入、按内核交接名续跑直到时钟到步末（`dttarget = 0`，上限 10 000 次，超即按名报错）；指令映射：`ip` → `ip`（kA）· `p_ec[i]` / `p_ic` → 内核新设置 `source_power_<k>`（C20-K4 × 指令功率，内核判据 `the_march_takes_tables_and_resolved_species_opt_in` (1b) 逐位过）；输出按接口表 v0.2 §4 取记录（`globals = 1` 时带 DD 口径量，旧内核缺的键给 `null`）。**判据 PCS-V6 过**（`python/tests/test_session.py` 13 过；★同日加料映射后 14 过：`gas_rate` → 内核 `gas_rate`，本步 `pellet` 事件 → `fuel_rate` = Σ `n_atoms` / 10 ms、高斯中心 / 宽度缺省 0.7 / 0.2，无弹丸之步送 0 不漏到下一步——映射抽为 `Session._commands_to_settings` 并单测，随 I-9；★再加相位后 15 过：`flags.phase` 在 t < 1 s 为 `zerod`，其后取内核 `lh_phase` 迹末值给 `L` / `H`（算例开 `lh_model` 时，随 I-7），无迹为 `null`；`lh_phase_out → lh_phase_in` 进续跑交接表）：会话往返与 JSON-RPC 往返 · 每步 t 前进 10 ms（1e-12）· 两个同指令会话逐位相同 · 按名拒绝九条（k 跳号 · dt ≠ 10 ms · `p_nbi` / `p_lh` 非零〔I-12〕· 负速率 · EC 组数不符 · 无沉积表的非零功率 · LCFS 点数 < 16 · 自交 · 份额越界）且拒绝后会话照走；`test_serve.py` 9 过。★余（不挡判据，转后续行）：关会话不落 `manifest.json` / 步耗直方图文件（`close` 交回 p50 / p99 / 最大）· 几何固定为工况文档自带（`geometry_fixed` 恒真；按节拍平衡与实现 LCFS 属 I-15）· 相位 `phase` 在 t ≥ 1 s 为 `null`（I-7）· 规定档密度（`density_prescribed` 恒真，加料属 I-9）· 公开仓 `_lib` 仍链 09-13 内核（ABI 154），新设置要 `rust/build.sh` 重建后才在 Python 侧生效 |
| **I-9** | kernel | **加料档**（PCS-W10）：推进密度通道接粒子源 —— 供气用 `neutrals::neutrals_mc`（按单位源计数：形状在平衡节拍上重算，每步乘指令速率）· 弹丸用高斯沉积（**无消融模型**，中心 / 宽度参数化，缺省取 CASE-20 TGYRO 侧 `gauss_add:(0.7, 0.2, 5e21)`）· NBI 粒子源接口保留；`density_mode` 取 fuelled / prescribed，同一会话可切。★今天推进无加料模型（`CoreMarch.s_n` 恒由调用方给），通量匹配层的 `fuel_rate` / `particle_source` 不进推进；粒子 D/V 无锚点（交付件无粒子扩散剖面） | 粒子守恒（源 − dN/dt − 边界损失）≤ 1e-6 相对；平顶标定后 n_e 剖面对 CASE-20 中位 ±10 %，`[TBD]` 读数后定（PCS-V9） | **完成 2026-09-14**（原：开，等 I-3；弹丸参数随 I-11 —— I-11 已自答）。★★★**PCS-V9 过（同日，CFEDR 芯部链平顶 8 s）**：标定 **D = 0.1 χ_e · `pinch_shape = linear` 边缘 −0.02 m/s · 弹丸高斯 2.5e21 /s（ρ 0.7 / 宽 0.2）**——n_e / CASE-20 按 ρ **中位 0.993（2 s）→ 1.000 → 1.004 → 1.010（8 s）**、逐点 **最小 0.939 · 最大 1.086（全剖面在 ±10 % 内）**、轴上 0.981 → 0.944、⟨n_e⟩ 9.91e19 → 1.007e20；**粒子账 3.6e-13 … 4.9e-13（阈 1e-6）**。同扫：−0.02 配 3.5e21 中位 1.057、最大 1.146（出带）；−0.01 配 4e21 中位 1.028、轴上 0.911（出带）。★照实记：轴上比仍缓降（8 s 0.944）· P_fus 1368 MW（对 V3 −9.4 %，不在 V9 判据内，与 I-7 台基锚定同列待查）。以下为落地过程：★★**2026-09-14 加料档已落（内核未提交）**：① 解开「离子种类 + 密度通道」的两处拒绝（`case.rs` 门 · `scenario.rs` 推进）——算子在离子种类下本以 n_e 为离子权重，故通道直接推进 n_e、成分每步由之重解；`quasi` / 杂质浓度仍拒 ② 弹丸 = 既有高斯 `fuel_rate`（中心 / 宽度参数化穿透，无消融模型）③ 供气 `gas_rate` [atoms/s]：每次调用在起点态上跑一次 `neutrals::neutrals_mc`（梯子小半径壳，1e4 历史，种子 1），电离形状按梯子体积分归一到「速率 × 电离份额」，经 `MarchPhysics.n_source` 每步加进主离子源；记录 `gas_ionized`；无密度通道给 `gas_rate` 按名拒 ④ 判据 `the_species_march_takes_a_fuelled_density_opt_in` 过（Miller：加料后 n_e 变且总量增 · Z_eff 仍为离子种类的 2 · 供气抬边缘 n_e · 电离份额 ∈ (0, 1] · 两条拒绝）；芯部链开关 `PCS_DENSITY`（缺省 CASE-20 弹丸 0.7 / 0.2 / 5e21）已加。★**CFEDR 芯部链首读**（`PCS_DENSITY = 1`，弹丸高斯 0.7 / 0.2 / 5e21 /s，D = 0.5 χ_e，箍缩 0，给定 χ + IPB98，8 s）：能量账 ~1e-13 全程；**n_e 留不住**——⟨n_e⟩ 9.36e19（0.4 s）→ 7.36e19（4 s）→ 6.98e19（8 s），P_fus 1385 → 668 → 575 MW、W_th 780 → 577 → 547 MJ ⇒ 只扩散无箍缩时 5e21 /s 补不回边界流失，正是 **PCS-G18「粒子输运 D/V 无锚点」**（CASE-20 交付无粒子扩散剖面）；★另两条旧判据随拒绝解开改写（`the_march_takes_tables_and_resolved_species_opt_in` (4b) · `species_settings_are_inert_on_the_page_and_refused_where_they_cannot_ride`：由「被拒」改为「推进且有限」）。★**D/V 扫描（同日，4 s，起点 ⟨n_e⟩ 9.92e19）**：链上的箍缩是全截面常数（非 ∝ r，负为向内）——D/χ 0.5 · 箍缩 −1 m/s：1.1 s 内 n_e 翻到 1.81e20、P_fus 7.7 GW 后第 4 块被拒；−3 m/s 第 0 块即拒；+1 m/s 2.4 s 流失到 7.6e18 后被拒 ⇒ 常数箍缩不可用；**只减扩散**：D/χ 0.2 → ⟨n_e⟩ 9.04e19 · P_fus 1070 MW；**D/χ 0.1 → 9.99e19 · P_fus 1270 MW（体平均守住）**。★**D/χ 0.1 · 8 s**：⟨n_e⟩ 9.90e19（0.4 s）→ 9.96e19（3.2 s）→ 10.12e19（8 s，+2 %，体平均守住）；P_fus 1432 → 1274 → 1286 MW（对 1510 −15 %）· W_th 786 → 748 → 764 MJ；剖面比打印未出（探针梯子取 ψ_N 未对上；记录自带梯子也无 ψ_N 行，改按梯子 ρ [m] 对 ONETWO rho_m）。★**对照（同 D/χ 0.1、不加料）**：⟨n_e⟩ 9.76e19 → 9.00e19（3.2 s）→ 8.13e19（8 s），P_fus 1431 → 1187 → 976 MW ⇒ 弹丸 5e21 /s 确实守住体平均；而加料时 P_fus 仍比不开密度通道低约 15 %（体平均同、形状变）〔推测，待剖面比〕。★★**剖面比（同日，按梯子 ρ 对 ONETWO rho_m）**：n_e / CASE-20 **中位** 0.995（0.4 s）→ 1.010（4.8 s）→ **1.025（8 s，在 PCS-V9 的 ±10 % 内）**；但形状漂——**轴上** 0.981 → 0.912 → 0.875、中部 1.011 → 1.049 → 1.067、最大 1.122（边缘 Dirichlet 恒 1.000）⇒ 弹丸高斯沉积在 ρ 0.7、箍缩为零时芯部被掏空、中部鼓起，**P_fus 低约 15 % 的来由**（芯部密度降）。★★**粒子账与箍缩剖面已落（同日，内核未提交，接口表不动）**：① `particle_books = 1` → 每步主离子粒子账（`transport::heat_balance` 用密度算子自己的权重：容量 V′ · 度规 V′gm3 · 源作变化率，度规移动时带旧容量；重试步与 `d_pc > 0` 不审），取最坏相对残差为事实 `particle_balance_worst`，**与能量账分开**（密度推进已录的 `balance` 不动）② `pinch_shape = linear` → 主离子箍缩 V(ρ) = `pinch`·ρ/ρ_edge（负向内、轴上为零），经 `MarchPhysics.v_n_profile` 取代常数箍缩；无密度通道给形状 / 账按名拒 ③ 判据 `the_density_channel_keeps_its_particle_books_and_takes_a_linear_pinch` 过（加料离子种类推进粒子账 < 1e-10 · 线性向内箍缩使轴 / 边 n_e 比高于零箍缩 · 两条拒绝）；芯部链开关 `PCS_PINCH_SHAPE` · `PCS_BOOKS`（缺省开）。★**链上读数（同日，D/χ 0.1 · 弹丸 5e21 /s）**：**粒子账全程 3e-14 … 7e-13（PCS-V9 守恒一半过，阈 1e-6）**；线性向内箍缩在边缘取 −0.2 m/s：8 s 内 ⟨n_e⟩ 9.9e19 → 2.05e20、剖面中位比 2.26、P_fus 8.4 GW（未拒但失控）；−0.5 m/s：3.2 s 中位 2.74 后第 13 块被拒；−1 m/s：1.6 s 轴上比 18 后第 8 块被拒 ⇒ 过强。量级估计：维持 CASE-20 约 2.6 倍的芯边比，无源平衡 V ≈ D·d ln n/dρ，D ~ 0.1 χ_e 时仅约 −0.04 m/s〔推测，按量级〕，−0.2 已大约 5 倍；内核全量 616 过。★**弱箍缩（同日，D/χ 0.1 · 弹丸 5e21 /s，8 s）**：边缘 −0.02 m/s——轴上比 0.98 → 0.97（**不再掏空**），但中位 1.023（2 s）→ 1.060（4 s）→ 1.096（6 s）→ **1.128（8 s，出 ±10 %）**、⟨n_e⟩ 1.11e20、P_fus 1560 MW；−0.05 m/s——中位 1.07 → 1.29、轴上 1.13、P_fus 2093 MW；粒子账 2e-13 ⇒ 箍缩量级对了（−0.02），**加料 5e21 /s 在向内箍缩下偏多**，粒子持续累积。余：箍缩与加料共调（−0.02 配 2.5e21 / 3.5e21；−0.01 配 4e21）使 8 s 中位 ±10 % 且轴上不掏空。余：**PCS-V9 读数**——剖面比（n_e / CASE-20 逐点中位）· P_fus 偏低归因· 粒子守恒账 ≤ 1e-6 · 平顶 n_e 中位 ±10 %· 公开仓会话面 `gas_rate` / `pellet` 映射（`_lib` 重建后）|
| **I-10** | fylite | **典型工况端到端**（PCS-W7）：0 → 150 s 爬升进燃烧 + 截短平顶（`[TBD]` s）+ 合成下降，指令用 I-5 的回放文件；出 P_fus(t) 交包层方试喂 | 全程逐步墙钟：报 p50 / p99 / 最大（~~< 500 ms~~ 时间要求已放宽，硬上限 `[TBD]`）；目标机器 `[TBD]`（PCS-V7） | 开（等 I-3 … I-9）。★★**2026-09-14 首读（部分，降级照实）**：CFEDR 燃烧平顶按会话节奏逐 10 ms 推进 200 步（芯部链 `PCS_STEPS=1`，每步一次 `code/evolve` 续跑；给定 χ + IPB98 · L-H 相位 · 加料档用 I-9 标定：D 0.1 χ_e · 线性箍缩 −0.02 m/s · 弹丸 2.5e21 /s · 粒子账开）——**每 10 ms 步墙钟 p50 284 ms · p99 288 ms · 最大 345 ms · 均 283 ms**（本机 WSL2 以外的沙箱机，单线程 release）；2 s 末 P_fus 1364 MW · ⟨n_e⟩ 9.81e19 · 恒 H（P_sep 245 MW 对 P_LH 154 MW）。★同设置 40 步一块时约 9 ms/步 ⇒ **每次调用约 275 ms 固定开销**〔推测：追踪档每次调用自 g-file 重追梯子，待 bound 档对照〕。★**bound 档对照（同设置 200 次调用）**：每调用墙钟 p50 157 ms · p99 191 ms · 最大 223 ms ⇒ 重追梯子约占 127 ms，**其余约 157 ms 为每次调用的固定开销**（离子种类装配 · EPED · 打包 / 解包 · 记录，未逐项分）；★该档每调用只走约 0.27 ms（交换上限钉步长，200 次到 t 0.055 s），故只作开销对照、非 10 ms 节奏物理。提速方向：会话保持推进状态（PCS-W1 原设计的常驻 `CoreMarch`）或至少首步追踪后复用梯子。★降级：几何固定为 CASE-20（逐节点 LCFS 待 I-15）· 未走公开仓会话层（CASE-20 工况文件只在内核测试数据）· 未含爬升 0 → 60 s 与合成下降。★★**2026-09-14 走公开仓会话层（进行中）**：① 内核芯部链加 `PCS_PLAN_DUMP=<文件>`——把第一块的 `code/evolve` 工况文档（42 个设置 + equilibrium · core_sources · core_transport · core_profiles，CASE-20 给定 LCFS · 表格 RF · 给定 χ + IPB98 · L-H `martin08`）写成 `fylite.io.fydoc.complete` 收的 JSON，因 CASE-20 输入只在内核 crate 内；② 驱动（试点件，未入仓）按回放文件逐 10 ms 发指令：Ip 节点间线性、RF 各系统（EC 三组 + IC）自节点零阶保持并**合成一张** CASE-20 沉积表按总功率缩放（`source_power_0`；工况只绑一张合计 RF 表，分系统沉积形状因此不分，降级照实）；几何固定 CASE-20 平顶，密度走推进自身通道；③ **会话层续跑交接补漏**（公开仓 `engine/session.py`）：内核续跑读六份滞后量（`core_profiles` 声明槽 `fylite:psi_prev` · `sigma_prev` · `exch_prev` · `dn_prev` · `vn_prev` · `zeff`），会话此前只经旧拼法交前三份，闭包的 Z_eff 与粒子 D / V 滞后量每次调用被置零——已改为记录带哪份按同名槽交回，判据 `test_a_resume_hands_every_lagged_profile_the_record_carries`（会话 16 过）。**修前试点**（各 50 步 = 0.5 s，零拒绝，均自 CASE-20 平顶态起步）：平顶 60 s（15 MA · RF 20 MW）每步 p50 295 ms · p99 / 最大 463 ms（首步）· P_fus 1520 → 1437 MW · W_th 802 → 784 MJ · 恒 H；「爬升」17 s（4.25 MA · RF 15 MW）p50 **600 ms** · 最大 729 ms · P_fus 1520 → 1223 MW——★**这不是爬升**：平顶等离子体被突然给 4.25 MA，内核按交换上限切得更碎故更慢；下降 6170 s（10 MA）p50 295 ms · P_fus 1520 → 1378 MW。★**0 → 60 s 爬升本工况无起步态（CASE-20 只有平顶），全程读数只取平顶 + 燃烧 60 → 150 s 与合成下降 6150 → 6209 s（自平顶态起），爬升段记为缺**。★★**首次全程尝试（同日）塌陷，已诊断**：未带 I-9 密度标定的工况（密度通道关）自 60 s 起按回放推进——60–65 s 节点只令 RF 20 MW（EC 爬升组 10 + IC 10），**CASE-20 燃烧态却要约 100 MW 维持**：W_th 800 → 360 MJ（64 s）、61.9 s H → L、65 s 起虽令 102 MW 已冷到 T_e0 ~5 keV 回不来（P_fus ~13 MW），此后 L-H 模型**逐步颤振**（H 一步即回 L，每 0.1–0.5 s 一次，H 步墙钟 ~600 ms）；读到 76 s 停下（CSV 保留）。**对照**（带 I-9 标定重导工况 `PCS_DENSITY=1 PCS_DCHI=0.1 PCS_PINCH=-0.02 PCS_PINCH_SHAPE=linear PCS_FUEL=2.5e21 PCS_BOOKS=1 PCS_LH=1`）：自 65 s（令 102 MW）起 2 s——**恒 H、P_fus 1520 → 1534 MW、W_th 800 → 843 MJ、零拒绝、p50 295 ms · p99 370 · 最大 465 ms**；自 60 s（令 20 MW）起 1 s——仍降 P_fus 1520 → 1318 MW ⇒ **塌陷主因是回放 60–65 s 的加热指令与 CASE-20 燃烧起步态失配，不是加料**（加料缺只让它更坏）；★回放在该段是「EC 爬升组 + IC 10 MW」，与 150 s 节点的 102 MW 燃烧加热出自不同文献族（回放文件 flags 已记），失配属回放而非模拟。★全程改自 65 s 起（加热与起步态相符）：燃烧 65 → 150 s · 合成下降 6150 → 6209 s，带标定工况，后台进行。★★**第二次全程（65 s 起、带标定、102 MW）仍塌，第二个会话交接漏洞，已修**：恒 102 MW 下 P_fus 1520（65 s）→ 1448（70 s）→ 1157（75 s）→ 822（80 s）→ 348 MW 且 85 s 起 L → 18 MW（90.5 s），零拒绝、墙钟照旧 ~295 ms；而同一工况在内核芯部链上 16 s 守在 PCS-V3 ±5 % 内 ⇒ 差在会话层。★**根因**：会话 `_CARRY` 缺 IPB98 定标的四个参考量与 τ_E 锚积分器（`chi_scale_ploss_ref` · `chi_scale_ne_ref` · `chi_scale_ip_ref` · `chi_scale_w_ref` · `chi_scale_int`）——内核自己的续跑判据交这五个，会话不交，于是**每 10 ms 一次调用都以当下状态重设参考**，锚永远不回拉，燃烧慢慢滑到 L 模（读到 90.5 s 停下，CSV 保留）。**已修**：`_CARRY` 补这五对；判据 `test_the_session_carries_every_scalar_the_kernel_resume_hands_on`（会话交接 ⊇ 内核续跑判据的全部标量对；会话 17 过）。★两处会话漏洞（滞后量 3/6 · 定标状态 0/5）都是「内核续跑声明了、会话没跟」——此前会话判据只查确定性（同指令同位）与 10 ms 时钟，**不查与内核整跑的等价**〔下一步候选：会话 N 步对内核一次 N 步的逐节点对判，同 PCS-V2 口径〕。★**修后 65 → 95 s 试点（同日）**：3000 个 10 ms 步、**零拒绝、恒 H**；P_fus 1520 → 1257 MW（69.5 s）后稳在 **1182 MW**、W_th 748 MJ、T_e0 24.85 keV（CASE-20 为 1510 MW / 807 MJ / 28.7 keV：定态比参考低 22 % / 7 %）；每步墙钟 **p50 295 ms · p99 362 ms · 最大 478 ms**（首步）。⇒ 修交接后会话上的燃烧可持续；定态偏低〔推测：会话每 10 ms 一次调用、宿主按交换上限子步，与内核整跑 40 步一块的子步序列不同；未验证〕。★全程（燃烧 65 → 150 s · 合成下降 6150 → 6209 s）接着跑（后台，带标定工况 + 修后会话）。★★**燃烧全程读数（同日完成，CSV 与汇总保留于工作区 `cfedr-discharge-demo/i10/`）**：65 → 150 s **8 500 个 10 ms 步 · 零拒绝 · 恒 H**；P_fus 1520（65 s）→ 1245（70 s）→ 1184.5（80 s）→ **1182 MW 定态**（100 / 125 / 150 s 1182.1 / 1182.3 / 1182.4 MW）· W_th 748 MJ · T_e0 24.90 keV（对 CASE-20 的 1510 MW / 807 MJ / 28.7 keV 低 22 % / 7 % / 13 %）；**逐步墙钟 p50 294 ms · p99 363 ms · 最大 482 ms（首步）· 合计 42.5 min**（本机单线程）⇒ PCS-V7 读数：燃烧段会话节奏可持续、无拒绝，墙钟由调用开销主导（见 I-20）；该全程定为 I-20 / PCS-V15 的参考基线。★★**合成下降段（6150 → 6209 s）在 6172.99 s 中断，已复现**（同一步、确定性；CSV · 日志 · 汇总保留于同目录）：前 2299 步零会话拒绝；6155 s 节点 RF 102 → 82 MW 后 **6157.6 s H → L**；6170 s 节点 RF 降到 10 MW 起 **L-H 逐步颤振**（H 一步即回 L，6170.3–6172.9 s 间几十次）；末几步墙钟升到 0.6–3 s（宿主子步增多）；第 2300 步**内核拒绝 `-23`**：「entry evolve_heat refused the request on the resolved species / tables — a species state that did not settle, a surface the neoclassical conductivity cannot evaluate」，此刻 Ip 9.26 MA · RF 10 MW · L · n_e0 1.39e20 · T_e0 17.8 keV（下降中）；已记段内 P_fus 1520（6150 s）→ 970（6155 s）→ 626（6160 s）→ 471（6170 s）→ 303 MW（6173 s），W_th 800 → 227 MJ；墙钟 p50 295 · p99 891 · 最大 2961 ms。★**两处待办**：① **会话层缺口**——接口表 §5 约定「拒绝不中断会话」，但会话只拒自己的指令校验，**内核拒绝（`Refused`）照样抛出、中断驱动**；应转成 `rejected = true` 的一步（`reason` 带内核码与原句、输出沿用上一步）——**同日已改**：`Session.step` 接住 `fydoc.Refused`，该步 `rejected = true` · `kernel_code` · `reason` 为内核原句 · 状态不前进，下一步照常可送；判据 `test_a_kernel_refusal_is_a_rejected_step_and_the_session_goes_on`（会话 19 过）；② **物理原因待定**——是种类状态不收敛还是新经典电导在该面上不可评，须在该步的续跑文档上逐项开关定位；**正是 I-21 快照的首个用例**（无快照时复现要从 6150 s 重跑 23 s ≈ 13 min）。★★**同日两项旁证发现（未修，待裁）**：① **公开仓运行时过期**——`python/fylite/_lib/libfylite_runtime.so`（09-14 10:13）内嵌的静态内核早于 F-9（`code/discharge` 缺省读卡片 `fylite:i_max_aturn`）；经 `fylite.io.fydoc.complete` 调门时卡片额定**不被读取**（实测 29 个自由边界解 `coil_limits` 全为 0、平顶成限制器），须在算例侧显式绑 `i_max_aturn`（绑后读数与内核测试 I-15 逐项同：平顶偏滤器 · 距 0.353 m · R0 7.574 · κ 1.842）；`rust/build.sh --wasm-check` 重建了内核 `.so` / `.wasm` 与静态库，**不重建运行时**〔补法：公开仓运行时构建，未做〕。② **`code/zerod` 的 `summary/fusion/power` 行实为 α 功率**——CFEDR 平顶实测 4.05e8 W = `prediction_p_alpha`，而同一记录的 `p_fus` 字段为 2.01e9 W、`neutron_power_total` = 0.7987 × p_fus、Q 19.7 ⇒ DD `summary/fusion/power`（聚变功率）装的是 α 份额（1/4.97），读 DD 行的下游会把聚变功率低估 5 倍〔内核待修〕。★本日放电推演演示件（上升 · 平顶 · 下降：自由边界平衡序列 29 个时刻 + `code/zerod` 0-D 全放电 + 会话窗；动画 / 交互页 / IMAS HDF5 条目）落在工作区 `cfedr-discharge-demo/`，不入仓 |
| **I-11** | 会议方 | **向会议方要的回答**（随 I-1 发出）：① PCS 侧**有无自由边界磁位形模型**（无则 LCFS 接口下形状 / 位置环空转，须当面说明；评估见规划页 §3.6，PCS-D12）· ② 密度指令是参考值还是直接施加，弹丸指令带哪些量（PCS-D10）· ③ lᵢ 与 V_loop 的口径（PCS-D4）· ④ 包层 ±20 % 表只覆盖平顶，表外怎么办（PCS-D5）· ⑤ 传输方式与时钟主从（PCS-D2）· ⑥ 下降段与爬升段的 LCFS 序列（设计方，PCS-D3）· ⑦ 若要 NBI/LHW 真沉积：束能 · 注入几何 · LH 频率与 n∥ | 每问有书面回复或会上记录 | ~~阻塞（仓外回复）~~ ★★**完成 2026-09-14（自答，用户裁定不征询）**，答复落内核 `docs/note/cfedr-pcs-interface.md` v0.2 §6：① 按 PCS 侧无磁位形模型设计，PCS 以 `lcfs_real_*` 为被控量、`pf_current` 只核对 · ② `ne_cmd` 为参考值（DD `pulse_schedule/density_control/n_e_line/reference`），缺省加料档；供气按 DD `gas_injection/valve/electron_rate`、D:T 50:50；弹丸按 DD `pellets`（粒子数 · 初速 · T 份额，按事件） · ③ lᵢ(3) 为主（DD `li_3`）、V_loop 取 LCFS（DD `v_loop`）、βp 按 DD `beta_pol` 体积分式取总压 · ④ 包层取 `p_neutron`（DD `summary/fusion/neutron_power_total`），带中心平顶 P_fus 1.51 GW ±20 %，带外不钳位只置标志 · ⑤ stdio JSON-RPC 2.0、PCS 主时钟锁步、无墙钟上限、COCOS 17 带符号 · ⑥ 爬升 LCFS 按 Miller 参数插值、下降段按 PCS-D3 合成，逐步置 `synthetic` · ⑦ 不做，维持按名拒绝（PCS-D11） |
| **I-12** | fylite · kernel | **CFEDR 下 NBI / LHW 非零指令怎么办**（PCS-D11）：接口已保留（用户裁定），而 CFEDR 设计无这两套硬件（EC 82 MW + ICRH 20 MW）；内核有 `beam_deposit` / `lh_deposit`，但无参数可喂。〔推荐〕无硬件参数时按名拒绝，I-11 ⑦ 给出后改按解析模型沉积；备选：通用缺省硬件并标注「非 CFEDR 设计」 | 裁定后 I-8 的拒绝闸按之写 | **完成 2026-09-14**（已裁 2026-09-13：按名拒绝）：随 I-8 落于 `engine/session.py` 的拒绝表——`p_nbi` 非零说出「缺束能、注入几何（切向半径、高度）、粒子种类」，`p_lh` 非零说出「缺频率、n∥ 谱、天线位置」；拒绝后会话照走（`test_session.py` 两条参数化用例过） |
| **I-13** | 会议方 · fylite | **联调与典型工况测试**（PCS-W8） | 联调记录 | 阻塞（等 I-10；节点 **2026-09-30**） |
| **I-14** | kernel | **磁场模型①：CFEDR 静态复原**（PCS-W11 / V10）：CASE-20 a-file 15 个 `ccbrsp`（单位与顺序对 fydoc `pf_active/base`）+ 取回源 + 15 MA，在 fydoc 导出的 CFEDR 卡上自由边界解，复原交付 LCFS；诊断 I-5 的塌陷（anneal 第 1 遍塌成 R≈10.42 m 的 0.1 m 柱，第 0 遍 2000 迭代残差 0.55） | 实现 LCFS 对交付 `rbbbs/zbbbs` 最大 / rms 距离 · 轴 · X 点 · q95 · li3 对 0.946 · βp 对 1.434；★**读形状不读残差**（F-34：塌解的残差与 gap 都可最小，I-5 实测 gap 也被骗，PCS-V11） | 进行：★**2026-09-13 V10 读数**——① `ccbrsp`（A·匝）**对不上 fydoc 线圈集**：192 种排序最优真空残差 0.78、isoflux 0.40 跨度且均越限；g-file 头为 OMFIT 模板 ⇒ 疑为模板 EFIT 线圈集，需设计方 EFIT 线圈几何（并入 I-19）；② 精确外场（交付图减自场）+ 好种子：LCFS rms 0.10–0.12 m、未收敛（残差 4.7e-3–2e-2）、li3 0.71 对同法交付 0.889（−20 %）、βp 1.42–1.52 对 1.33；③ **塌陷根因＝种子位置**：初始电流盘放在初始通量极大处，R 7.0–7.8 进偏滤器支、R 8.2–8.6 塌成外侧柱，`code/discharge` 第 1 遍种子 (8.38, −0.07) 正落塌陷盆；`fb_gain` 8 限循环，2 或 relax 0.1 可定；④ 余：opt-in 种子规则（缺省逐位不动）· li3 −20 % 归因 · 全收敛 · 读形状判据。件：内核 `case.rs` `discharge_cfedr_tests::v10_*`（全 `#[ignore]`）。★**V11（同日）**：种子规则已落（`equilibrium::solve_free_boundary_seeded`，门 `seed = flux_max | target | centroid`，缺省逐位不动，判据 `a_seed_at_the_warm_extremum_is_the_warm_solve`）+ 选优改为塌陷趟永不胜出（内核 lib 606 过、16 忽略）；但门里第 1 遍在 3 种子 × 3 反馈姿态下全部塌陷——**塌陷来自线圈电流步，不是种子**；返回第 8 遍 a −8…−14 %、κ 1.62–1.91、δ 0.14–0.25（目标 0.57）、分离面距 0.32–0.47 m、无一收敛；试「拒步」则每步皆塌、停在起点 κ 1.05（已撤回）⇒ 缺径向 / 垂直位置稳定化（件 `v11_seed_rule_in_the_door`）。★★★**路线 A 位置控制已落（同日，未提交）**：`equilibrium::PositionControl` + `solve_free_boundary_controlled`（PID 控电流形心、电流欠松弛 ½，称 C4；缺省逐位不动），40 组线圈扰动全留偏滤器支（旧律热启 10/10 塌），门 `v11_position_control_holds_the_delivered_cfedr_equilibrium`；接入 `code/discharge` `position_control = c4`，V13：κ 1.88–1.92（目标 1.90）、δ 0.58–0.75、偏滤器位形、a −6…−8 %、分离面距 0.29–0.30 m、5–6 s；R0 偏内 0.33 m（设定点为几何形心，未计 Shafranov 外移）；内核 lib 607 过、21 忽略。余：设定点随遍修正 · 热启单拍耗时 · 固定下 X 点。★★**设定点随遍修正已落（2026-09-14，内核未提交）**：`code/discharge` 新设置 `pc_track_r0`（增益 ≥ 0，缺省 0 逐位不动）——起步解与每遍接受后按「目标 R0 − 内缩面实现 R0」平移 C4 设定点的 R（Z 不跟，免与下 X 点不对称相争）。**V13 读数**（新导出 CFEDR 卡 + 逐线圈上限，65×65，8 遍）：形心设定点 R0 7.59 · κ 1.83 · δ 0.71/0.64 · 分离面距 0.313 m · 偏滤器；**跟踪增益 0.5：R0 7.73 · κ 1.73 · δ 0.45/0.39 · 距 0.265 m（全场最小，但逐遍跳动）· 限制器**；**增益 1.0：R0 7.871（到位）· κ 1.56 · δ 0.28/0.34 · 距 0.397 m · 限制器**；固定 (7.988, 0) 同向（κ 1.53 · 限制器）⇒ **R0 能拉回，代价是丢 X 点与拉长**；两组跟踪退火后 CS1U/CS4U/CS4L/CS1L 贴 ±44.28 MA、PF1U 25.74、DC1 13.2 MA 均在上限〔推测：R0 · κ · X 点在这组线圈上限下不可兼得，待无上限对照〕。★★**无上限对照（同日）推翻该推测**：去掉线圈上限，跟踪增益 0.5 仍 R0 7.78 · κ 1.65 · δ 0.37/0.37 · 距 0.283 m · 限制器（且 PF1U 36.6 MA、DC1 24.1 MA 越出原上限）；形心设定点无上限 R0 7.55 · **κ 1.898** · δ 0.51/0.59 · a −11 % · 距 0.318 m ⇒ **不是线圈上限所限，是把电流形心外推与退火的形状目标相争**：`pc_track_r0` 拉回 R0 必以 X 点与拉长为代价，**判为不是消 R0 偏内的办法**（保留为 opt-in、缺省关，读数照录）。R0 偏内 0.32 m 仍在，另寻（候选：设定点取「目标几何中心 + 解出的 Shafranov 外移」而非逐遍追 R0 · 退火目标内加 R0 权重）。★**热启单拍（同日，V13 新组）**：从形心设定点退火解的线圈安匝热启（`warm = 1`，仅交接安匝、ψ 不交接，种子 flux_max）——`passes = 0`：**0.613 s** · 176 轮 settle · R0 7.576 · κ 1.855 · δ 0.64/0.74 · 距 0.347 m · 下 X 点偏滤器（形状保住）；`passes = 1`：1.056 s、结果同（最佳仍第 0 遍）⇒ **单拍 0.61 s，未达原 400 ms 目标**（时间要求已放宽，非硬门）；对照：C4 自交付平衡起 142–152 轮 0.22 s，故提速候选是连 ψ 一并热启。★**固定下 X 点（同日，V13 新组，门的零点行 `x_r` / `x_z` / `x_weight`，钉点取形心设计自己的 (5.55, −4.43)，非文献值）**：权重 1 → 下 X 点偏滤器、X 点 (5.46, −4.24) 离钉点 0.21 m · R0 7.56 · κ 1.85 · δ 0.56/0.73 · 距 0.377 m；权重 10 → 仍偏滤器、X 点 (5.69, −4.71) 离 0.31 m · κ 1.99 · δ 0.36/0.65 · z0 −0.195 · 距 0.336 m（5 路贴上限）；权重 10 + 跟踪 0.5 → **限制器**（X 点跑到上方且不在边界）但形状最近目标：R0 7.69 · κ 1.79 · **δ 0.55/0.54** · **距 0.238 m · shape_error 0.053（全场最小）** · 8 路贴上限 ⇒ **零点行是软约束、钉不住 X 点**（权重越大离钉点越远、且压上三角形变），与 R0 跟踪同开仍丢 X 点 ⇒ **偏滤器位形 · 目标 R0 · 目标形状在这组线圈与现退火目标下尚未同时达到**，「固定下 X 点」未成。★**ψ 热启（同日）**：`fyo::DISCHARGE` 加 `psi_warm` 槽（52 → 53，接口摘要 8293141e → **612dd239bf1c0863**，只加槽、修订号 5 不动）；`code/discharge` 在 `warm = 1` 且绑 `psi_warm`（记录的盒内 `psi`）时以之作首解种子。V13：同从退火解热启 0 遍——仅安匝 0.664 s · 176 轮；**安匝 + ψ 0.531 s · 163 轮**，形状同（R0 7.570 · κ 1.850 · 下 X 点偏滤器）⇒ 省约 20 %，**仍未到 400 ms**：轮数主要是 C4 自身重新 settle（反馈幅值 · 积分 · 电流欠松弛未热启），非初值所致；再压须交接控制器状态（又一接口项，暂不做）。内核全量 616 过。★★**硬约束与控制器热启已落（2026-09-14，用户确认「在退火目标里给 R0 和 X 点加硬约束；把 C4 控制器状态也纳入热启交接」，内核未提交）**：① `linalg::ridge_lstsq_eq`（带等式行的岭最小二乘，KKT 直解；有线圈上限时把越界最远的列钉在界上再解，钉住不释放 ⇒ 可行而非总是箱内最优），判据 `the_equality_constrained_ridge_holds_its_rows`；`code/discharge` 新设置 `x_hard = 1`（每个零点的三行 ψ(X)=ψ(P0) · B_r · B_z 由加权改为等式；无零点则拒）· `r0_hard = 1`（目标边界最内 / 最外点对 P0 的等通量行改为等式），缺省关逐位不动，事实 `x_hard` · `r0_hard`。**V13 读数**（钉点 (5.55, −4.43)、x_weight 1、65×65）：x_hard + 线圈上限 → 第 2 遍塌（a 0.70）、第 3 遍「岭修正无解」停，返回第 1 遍**限制器** κ 1.32；x_hard 无上限 → 偏滤器，X 点 (5.39, −4.09) **离钉点 0.37 m**（软行权重 1 为 0.21 m）· κ 1.83 · a −10 % · 距 0.349 m；x_hard 无上限 gamma 1 → X 点 (5.49, −4.34) **离 0.11 m（全场最近）** · κ 1.95 · a −11 % · 距 0.337 m · 10.8 s；x_hard + r0_hard + 上限 → 第 5 遍无解，返回第 3 遍偏滤器 X 点 (5.22, −3.97) 离 0.57 m · R0 7.537 · κ 1.74 · 2 路贴上限；无上限 → R0 **7.544（未动）** · X 点离 0.36 m · κ 1.82 · 距 0.351 m ⇒ **在线性化修正里「硬」不等于结果里「硬」**：修正矩阵只有线圈真空响应，每步后 C4 把等离子体挪位（等离子体响应不在矩阵里），且步长只取 gamma 0.4；gamma 1 把 X 点拉近到 0.11 m 佐证前者之外尚有步长因素；R0 偏内 0.33 m **不因边界等式行而变**（R0 由 C4 形心设定点定，与 `pc_track_r0` 结论一致）；等式 + 线圈箱在后几遍变不可行、终止退火。〔推测：要真正钉住须把约束放进非线性解内部（逐 Picard 轮，同 C4 的位置）或把等离子体响应（全解有限差分）并入修正矩阵，未验证〕。② C4 控制器状态热启：`PositionControl.warm`（竖直 / 径向反馈幅值 + 两路积分）、结果补 `ctl_int`；门设置 `c4_warm = 1` + `c4_fb_z_in` · `c4_fb_r_in` · `c4_int_z_in` · `c4_int_r_in`，事实 `c4_*_out`（设置不是文档槽，接口摘要不动）。V13：安匝 + ψ + 控制器状态 **0.495 s · 149 轮**，对照同次从静止起 0.609 s · 163 轮，形状同（R0 7.565 · κ 1.86 · 下 X 点 (5.57, −4.45) 偏滤器）⇒ 再省约 9 % 轮数，**仍未到 400 ms**（非硬门）。件 `discharge_cfedr_tests::v13_hard_rows_and_the_warm_controller`（`#[ignore]`）。内核全量 617 过、28 忽略。★★**零点入解内（同日，上条候选之一已试，内核未提交）**：`equilibrium::NullControl`——逐 Picard 轮读钉点处本轮总通量的 ∇ψ，以线圈上限加权的最小范数通道步抵消（增益 `x_ctl_gain` 0.3 · 每轮步长 ≤ 上限 1 % · 钳在上限内）；门设置 `x_ctl = 1`（需 `position_control = c4` 与零点，否则拒），解后把加上的通道电流并入设计（`aturns` 即产生 `psi` 的电流），事实 `x_ctl` · `x_ctl_amp_max`；缺省关逐位不动。**接入条件是量出来的**：首版第 0 轮全增益接入 → 起步解全塌（a 0.12–0.15 m）；加残差 < 5e-2 锁存 + 100 轮爬升 + 步长帽 → 仍塌（0.135 m）；再加「本轮自身 X 点界定等离子体且离钉点 < 0.5 m 才接入」（只挪零点、不造零点）→ 不塌。**V13 读数**（钉点 (5.55, −4.43)）：带上限 → X 点 (5.49, −4.32) **离钉点 0.12 m 但成限制器界定** · **R0 7.745（不跟踪时最近目标 7.87）** · a −5.6 % · κ 1.68 · 距 0.284 m · 2 路贴上限；无上限 → **偏滤器 · X 点离 0.06 m** · R0 7.61 · κ 1.87 · 距 0.328 m（CS −69 MA 等越原上限）；+ 跟踪 R0 0.5 → 限制器 · R0 7.62 · κ 1.83 · 距 0.274 m · **shape_error 0.085（全场最小）** ⇒ 解内零点控制把 X 点拉到钉点 0.06–0.12 m（外层等式行 0.11–0.37 m），并把 R0 外推约 0.15 m；但**带线圈上限时 X 点不再界定等离子体**，偏滤器 · R0 · 形状三者仍未在上限内同得；各遍 gap 仍逐遍跳（第 2 遍 a ≈ 0.67 的塌陷各组共有，与 `x_ctl` 无关）。件同 `v13_hard_rows_and_the_warm_controller`；内核全量 617 过、28 忽略（缺省路径不动）。★**第 2 遍塌陷（同日）**：塌陷判据是 a < ¼ 目标 a，而钉 X 点各组第 2 遍落在 a ≈ 0.67 m（目标 2.45 m 的 27 %）刚过线，被当作出发点接受；门新设置 `collapse_frac`（缺省 0.25 逐位不动）。V13 取 0.5：第 2 遍半步重试后 a 2.39、塌陷消失，但**后遍仍跳**（第 3 遍 3 次减半、第 6 遍 a 1.23 m）；结果不比 0.25 好——零点入解内 + 0.5：X 点离钉点 0.64 m · κ 1.60 · 距 0.411 m；软零点行 + 0.5：偏滤器 · X 点 (5.435, −4.526) 离 0.15 m · κ 2.06 · a −12 % · 距 0.323 m · 3 路贴上限；+ R0 跟踪时无一遍低于一半、与 0.25 逐位同 ⇒ **第 2 遍塌陷只是症状：退火无接受门，每遍都走，回退（不塌但变坏）的遍照样成为下一遍的出发点**；`collapse_frac` 留作 opt-in。余：R0 偏内（候选：设定点取目标几何中心 + 解出的 Shafranov 外移）· 带上限时 X 点界定（候选：零点控制加 ψ(X) = ψ_b 行 / 限制器接触点的通量约束）· ~~退火逐遍回退（候选：按 gap 的接受门）~~ ★**接受门已试（同日）**：门新设置 `accept_gate = 1` / `accept_tol`（缺省关逐位不动；gap 不优于出发点的遍按塌陷同法减半重试，三次仍不优则原地不动、遍仍记入历史，事实 `n_rejected`）。V13：软零点行 + 门 → 第 2 遍后**每遍连 1/8 步都不优**，6 遍被拒，停在 gap 0.501 m · 限制器 · κ 1.41；零点入解内 + 门 → **与软行逐位同**（停住的出发点 X 点 (5.17, −3.58) 离钉点 0.85 m，出捕获半径，零点控制始终未接入）；+ R0 跟踪 → 第 1 遍后 7 遍被拒，停在 gap 0.466 m · κ 1.40 ⇒ **门让步子停住、结果比无门更差：外层修正（只含线圈真空响应）在 CFEDR 上连小步长都不是 gap 的下降方向**，逐遍跳动是修正方向本身之病，不是步长或接受规则之病；`accept_gate` 留作 opt-in。余：R0 偏内（候选：设定点取目标几何中心 + 解出的 Shafranov 外移）· 带上限时 X 点界定 · ~~外层修正方向（候选：修正矩阵用全解有限差分的含等离子体响应）~~ ★★**实测响应矩阵已试（同日）**：门新设置 `response = fd` / `response_step`（缺省 `vacuum` 逐位不动）——每遍对每路线圈加 `response_step` × |Ip|（0.01，150 kA）做一次热启全解，矩阵列 = 行残差之差 / 步长，C4 与等离子体响应都在矩阵里；事实 `response_fd` · `n_fd_failed`。V13（软零点行、带上限）：**gap 逐遍单调降 0.707 → 0.507 → 0.472 → 0.459 → 0.410 → 0.381 m、零减半、零塌陷**（真空矩阵同组 0.58 → 1.19 → 0.46 来回跳）⇒ **逐遍跳动的根因确是矩阵缺等离子体响应**；但终态**限制器界定**（X 点丢）· κ 1.59 · a −1.9 % · R0 7.54 · 距 0.381 m；+ 接受门 → 第 6 遍 0.367 m 后 2 遍被拒，κ 1.58 · R0 7.55；81–89 s / 次（15 路 × 8 遍热启解）⇒ 走得稳但**收敛到的是加权行目标的极小，不是偏滤器目标形状**：κ 与 X 点不如 C4 形心基线（κ 1.83 · 距 0.313 m · 偏滤器），R0 偏内未动。〔推测：行目标里零点行权重 1 相对 isoflux 行太轻，或限制器接触后 isoflux 行不再对准分离面；未验证〕。余：R0 偏内（候选：设定点取目标几何中心 + 解出的 Shafranov 外移）· ~~带上限时 X 点界定（候选：`response = fd` 配零点行加权 / `x_hard` 于实测矩阵上）~~ ★**已试（同日，件 `v13_measured_matrix_with_the_null_rows_weighted_up`）**：实测矩阵 + x_weight 10 → 限制器 · κ 1.50 · 距 0.429 m，gap 不再单调（0.70 → 0.44 → 0.88）；实测矩阵 + x_hard → 限制器 · κ 1.54 · 距 0.416 m（0.71 → 0.42 → 1.02 → 0.69）⇒ **零点行加重或硬化在实测矩阵上也保不住偏滤器，且把单调性一并丢了**。★**I-14 这一路的总读数（2026-09-14）**：在 CFEDR d2025 卡与逐线圈上限下，岭最小二乘退火的六种改法（外层等式行 · 解内零点控制 · 塌陷判据 · 接受门 · 实测响应矩阵 · 矩阵上加重 / 硬化零点行）**无一在上限内同时给出偏滤器 · 目标 R0 · 目标形状**；最好的仍是 C4 形心设定点单独（偏滤器 · κ 1.83 · 距 0.313 m · R0 7.59 偏内 0.28 m），其次解内零点控制 + R0 跟踪（shape_error 0.085 最小但限制器）。〔推测：该线圈集在 104014 表 1 上限下能否支撑目标偏滤器形状本身未证——无上限时各法都更近偏滤器；需设计方的参考线圈电流（I-19）对照，未验证〕。★★**已裁 2026-09-14（用户）：I-14 收在 C4 形心单独**——`code/discharge` `position_control = c4`、形心设定点、不开 `pc_track_r0` / `x_hard` / `r0_hard` / `x_ctl` / `collapse_frac` / `accept_gate` / `response = fd`（均留作 opt-in，读数照录）为现行磁位形答案：偏滤器 · κ 1.83 · 分离面距 0.313 m · R0 7.59（偏内 0.28 m，**已知偏差，照报**）；热启单拍用安匝 + `psi_warm` + `c4_warm`（0.495 s）。上限内偏滤器 · R0 · 形状同得的可达性**等 I-19 参考线圈电流**再判；I-15..I-18 以此为前置 | 收（2026-09-14，按 C4 形心单独；可达性随 I-19 重开）|
| **I-15** | kernel · fylite | **磁场模型②：形状反演（准静态）**（PCS-W12，并入 I-6）：PCS LCFS → isoflux 起点 → 以自由边界解为模型的形状反馈 → 线圈电流（带上限），每平衡节拍一次；源取推进 p′/FF′；输出实现 LCFS / 偏差 / 贴限。`FYL-SDD-09` D-14 第一档 | 平顶与爬升 Miller 目标上的实现偏离与贴限，阈 `[TBD]`（PCS-V12） | 进行（2026-09-13 用户裁定 B+A 并行，B＝本行）：`pulse::hold_shape` + `code/discharge` `stage = hold` + `equilibrium::solve_fixed_box_trust` 已落（未提交，缺省路径不动）；V12：信任域 40 格时 12 轮走完但不收敛，线圈电流相对变化 0.05–0.9 来回振，κ 1.18–1.50（目标 1.90）、z0 漂 −0.69…+1.00 m、分离面距 0.44–0.71 m ⇒ 与 A 同样缺位置（垂直）控制环。★★**路线 A 沿回放逐节点（2026-09-14，PCS-V12 读数，内核 `discharge_cfedr_tests::v12_c4_shape_hold_along_the_replay`，未提交）**：I-14 的答案（`code/discharge` + C4 形心 + 卡片 104014 表 1 上限）在回放文件带 LCFS 的 14 个节点上各解一次（Miller 目标与 Ip 取该节点；下降段为回放自己标的合成节点）。**冷解**（每节点自起步设计）：限制器爬升 1 / 5 / 10 s（0.25–2.5 MA）形状贴得好——分离面距 rms 0.044–0.075 m、R0 偏 ≤ 0.07 m、线圈比 ≤ 0.29；**17 s / 30 s 要偏滤器、得限制器**（距 0.044 / 0.125 m，形状近）；50 s（12.5 MA）起得偏滤器但距随 Ip 增大——0.218 m（1 路贴上限）→ 平顶 60 / 150 / 6150 s 0.353 m · R0 7.574（偏内 0.30 m，同 I-14）· κ 1.84 · 3 路贴上限；下降 6155 s 距 0.240 m（R0 7.52 对 7.79，2 路贴上限）· 6170 s（10 MA）偏滤器、距 rms 0.196 max 0.762 m · 6193 s 同 17 s 成限制器 · 6200 / 6209 s 同 10 / 1 s；每解 7–12 s。**热启节拍**（自上一节点的安匝 + ψ 起）：**不比冷解好**——限制器段相近（5 s δ 读成负）、17 s 距 0.181 m 反差、平顶 0.319 m · κ 1.94 · 5 路贴上限、6170 s 未 settle；**Ip 大降的两拍被拒**：6170 → 6193 s（10 → 4.25 MA）与 6200 → 6209 s（2.5 → 0.25 MA）热启解起步即拒（code −6）。⇒ **PCS-V12 读数**：C4 在 ≤ 7.5 MA 的爬升 / 下降上把 Miller 目标持到 0.04–0.13 m 但**爬升 17–30 s 与下降 6193 s 的偏滤器位形得不到**；≥ 12.5 MA 偏滤器可得、距 0.2–0.35 m、线圈贴上限；按节拍热启在大幅 Ip 变化处不可用〔推测：热启 ψ 与新 Ip 的电流盘失配，未验证〕；阈 `[TBD]` 未定故不判 |
| **I-16** | kernel | **磁场模型③：线圈电路与磁通**（PCS-W13）：`electromagnetics::channel_matrices`（超导线圈 R≈0；被动结构缺，声明无）· 电源电压上限 `[TBD]` · CS/PF 磁通消耗对接推进边界环电压（关 `FYL-SDD-09` D-G-16） | 爬升磁通消耗对 REPORT-20 ψ(SOB) 252 Wb 与击穿 12.5 Wb（PCS-V13，读数先行） | 开（等 I-15；9/30 后） |
| **I-17** | kernel · fylite | **磁场模型④：测试用参考控制器**（PCS-W14）：形状（控制点 isoflux 误差）· Ip（OH 电压 PI）· 垂直（虚拟反馈，报反馈电流）；D-14 两档驱动可选、档位进记录；只作测试替身，对外接口不变 | 档位可见 · 反馈电流有界 | 开（等 I-16；9/30 后） |
| **I-18** | fylite | **磁场模型⑤：完整建模测试**（PCS-W15）：回放文件驱动推进 ⇄ 电路 ⇄ 平衡全程 0 → 150 s + 合成下降 | 无 NaN · 无塌陷 · 实现 LCFS 偏差有界；逐步墙钟只报读数（PCS-V14） | 开（等 I-14..I-17 · I-10） |
| **I-19** | 设计方 | **磁场模型要的材料**（PCS-D13）：PF/CS 电源电压上限与回路参数 · 真空室 / 被动结构几何与电阻（fydoc G-7）· 磁测位置 · 平衡篇复核时刻（1.6 / 17 / 40 / 60 / 200 / 400 / 780 s）的线圈电流 · **CASE-20 a-file 的 EFIT 线圈几何与 `ccbrsp` 通道序**（I-14 实测对不上 fydoc 线圈集） | 书面交付 | 阻塞（仓外） |
| **I-20** | fylite · kernel | **步长与调用预算**（PCS-W16，内核 `docs/note/cfedr-core-simulator-pcs.md` §3.8）：A 宿主分窗批量（离线回放按指令分段常值切窗、每窗一次 `code/evolve`、窗内 10 ms 输出取逐步迹、剖面窗端插值并标 `interpolated`）· B 自适应物理步长（`dttarget` 放开、交换上限定步、L-H / 锯齿 / 弹丸 / 指令跳变强制小步、步长减半对照）· C 在线闭环砍单次调用开销（缓存追踪梯子 · 复用种类装配与 EPED · 常驻 `CoreMarch`） | **PCS-V15**：A+B 对 I-10 的 10 ms 定步长全程，P_fus / W_th / T_e0 逐 10 ms 相对差 ≤ 1 %、L-H 翻转时刻差 ≤ 10 ms、零拒绝；燃烧 65 → 150 s 墙钟 ≤ 60 s（现 42 min）；C：单次调用开销 ≤ 50 ms（现 295 ms） | 开（2026-09-14 立，用户「规划自适应步长，减少总步数，缩短计算时间」）。★**立项实测**（CFEDR 平顶带标定工况，经公开仓 door，64 核单线程）：M1 会话每 10 ms 步 **1 次调用**、p50 295 ms、`dt_capped` 0；M2 **一次调用走 10 个 10 ms 步 297 ms**（与一步同）⇒ 耗时几乎全是每调用固定开销；M3 放开 `dttarget`（0.05 / 0.2 s 同）步长被交换上限钉在 **~52 ms**，**200 步推进 10.27 s 用 362 ms**，末态对 10 ms 参考 W_th 747.0 / 747.1 MJ · T_e0 24.80 / 24.80 keV · P_fus 1201 / 1194 MW（+0.6 %）；M4 续跑绑定梯子被拒（记录无 ψ 图网格）⇒ 砍调用次数是主收益，梯子复用须走内核缓存。件：公开仓 `engine/session.py` 每步输出 `calls`、`close()` 报 `kernel_calls`（本次加）；探针 `cfedr-discharge-demo/i10/step_probe.py`。★**分项定序（a 基线与比对件 → b 分窗批量 → c 自适应物理步长；d 内核逐步迹补量与 c 并行；e 砍单次调用开销；f 推测执行可选）、风险与负面清单见下节「I-20 细化：自适应时间步」**；参考基线已定：I-10 燃烧 65 → 150 s 10 ms 全程（8 500 步 · 零拒绝 · P_fus 1182 MW · 42.5 min） |
| **I-21** | fylite · kernel | **多步计算快照 · 暂停 · 恢复（先以时间演化为例）**（PCS-W17）：计算按**时间片**推进，片界留快照、可在片界暂停与恢复；**重要节点（状态改变）前保留快照**——可预知的节点（回放节点 · 指令跳变 · 位形切换 · 平衡节拍 · 弹丸）在节点前一步留，不可预知的（L-H 翻转 · 锯齿崩塌 · 塌陷 · 内核拒绝 · 线圈贴限）以滚动快照回溯留存；快照 = 自足的 `code/evolve` 续跑文档 + 驱动游标 + 出处；续跑集合由内核声明（取代宿主手写的交接表） | **PCS-V16**：自任一保留快照恢复的运行与不中断运行**逐位相同**（同一步序列；异步序列时 ≤ 1e-14 相对，PCS-V2 口径）；片界暂停 → 恢复同上；快照往返（写 → 读 → 续跑）逐位；崩溃后自最近的事件前快照重走可达；保留策略下 I-10 燃烧 85 s 快照总量 `[TBD]` MB（按内容指纹去重的不变输入只存一份） | 开（2026-09-14 立，用户「补充 plan 多步计算保留快照，计算流程可暂停，可恢复，先以时间演化为例，按时间片分，重要节点（状态改变）前保留快照」）。★**立项依据（同日实测）**：① I-10 合成下降段在 6172.99 s 崩溃（2299 步、L-H 在 6170 s 起逐步颤振、末步墙钟 ~3 s），**无快照只能自 6150 s 重跑 23 s（~13 min）去复现**；② 会话续跑两次漏交状态（滞后量 3/6 · 定标状态 0/5），根源是宿主手写 `_CARRY` 与内核续跑集合脱节；③ I-20 的分窗重走与推测执行本身需要窗起点快照。细化见下节「I-21 细化」。★**I-21b 首版已落（同日，公开仓未提交）**：`Session.snapshot()` / `Session.from_snapshot()` / `snapshot_session` / `restore_session`（格式 `fylite.session.snapshot/1`：工况文档整份 + 指令映射 + 步号 + 最后一次接受的内核记录 + 计数；**尚未**按指纹去重、**尚未**有内核声明的续跑块）；判据 `test_a_session_restored_from_its_snapshot_marches_bit_for_bit_like_the_uninterrupted_one`（快照 → JSON → 恢复 → 下一步与不中断逐位同，会话 19 过）；驱动（试点件）每 100 步快照入环形缓冲（5 份）、首次内核拒绝时写「拒绝前快照」、连续 20 步被拒即停，可带快照路径续跑 |

### I-20 细化：自适应时间步（2026-09-14，用户「规划自适应步长，减少总步数，缩短计算时间」「自适应时间步写入 plan」）

**问题的实测形状**（I-20 立项读数；本机 64 核、单线程；CFEDR 平顶带 I-9 标定工况；经公开仓 door 调 `code/evolve`）：会话每 10 ms 一步 = **1 次内核调用 ≈ 295 ms**；一次调用走 10 个 10 ms 步 **297 ms** ⇒ 耗时几乎全是**调用固定开销**（I-10 首读：重追梯子 ≈ 127 ms + 种类装配 · EPED · 打包 / 解包 ≈ 157 ms），物理步 < 1 ms；放开步长后内核控制器被**交换上限**（¼ 最快电子–离子交换时间，`scenario.rs` `cap_now = 0.25 / nu_max`）钉在平顶 **≈ 52 ms**，200 步推进 10.27 s 用 **362 ms**，末态对 10 ms 定步长差 ≤ 0.6 %。**参考基线**：I-10 燃烧 65 → 150 s 的 10 ms 全程（8 500 步、零拒绝、恒 H、P_fus 1182 MW、墙钟 p50 294 · p99 363 · 最大 482 ms，合计 ≈ 42 min；CSV 保留于工作区 `cfedr-discharge-demo/i10/`）。

**目标**：总调用数与总墙钟降一到两个量级，**对外仍按 10 ms 出数**，物理答案对基线在判据内。

**定序与分项**（每项独立可验、缺省路径逐位不动；A → B → C，D 与 B 并行，E 可选）：

| 分项 | 做什么 | 依赖 | 判据 / 预估 |
| :--- | :--- | :--- | :--- |
| **I-20a 基线与比对件** | ① I-10 10 ms 全程（燃烧 + 合成下降）CSV 定为参考；② 比对脚本：任一候选运行按 10 ms 时标插值对齐参考，逐量报最大 / rms 相对差与 L-H 翻转时刻差；③ 会话 `calls` / `kernel_calls` 计数（已落） | I-10 全程 | 参考与比对件入 `tests/`（物理数值档，读工作区 CSV 或冻结一段进 oracle） |
| **I-20b A · 分窗批量（离线回放，内核不改）** | 宿主按回放把时间轴切成**指令分段常值窗**：切点 = 回放节点 · 功率阶跃 · 弹丸事件 · 相位切换 · 输出必需时刻；Ip 线性段按 ≤ 1 s 子窗逼近；**每窗一次调用**、窗内 `dt = 10 ms`、`nsteps = 窗长 / 10 ms`、`dttarget = 0`（先只砍调用、不动物理步长）；窗内逐 10 ms 输出取逐步迹，剖面取窗端 | a | **应逐位同基线**（同一物理步序列，只换调用切分——PCS-V2 口径 ≤ 1e-14 相对）；燃烧 85 s 窗长 1 s ⇒ 85 次调用 ≈ **26 s**（基线 42 min） |
| **I-20c B · 自适应物理步长** | 在 b 的窗上放开 `dttarget` 到窗长，由内核控制器 + 交换上限定步（平顶 ≈ 52 ms；冷密爬升可低到 ≈ 0.3 ms）；**事件强制小步**：窗内逐步迹里 `lh_phase` 变号 / 锯齿 `saw_mixed` / 弹丸步 ⇒ 该窗以 10 ms 定步重走（或切在事件时刻两侧）；**误差控制**：每类窗（爬升 · 平顶 · 下降 · 事件邻域）定型时做一次步长减半对照，P_fus · W_th · T_e0 · n_e0 相对差 ≤ 1e-3 才允许放大窗长 / 步长；10 ms 输出由逐步迹线性插值、剖面窗端插值，`flags.interpolated = true` | b · d | **PCS-V15**：对 a 的参考，P_fus / W_th / T_e0 逐 10 ms 相对差 ≤ 1 %（稳态段）、L-H 翻转时刻差 ≤ 10 ms、零拒绝；燃烧 65 → 150 s 墙钟 **≤ 60 s**、合成下降 ≤ 60 s；预估燃烧 85 s ≈ 1 650 物理步、窗长 5 s ≈ 17 次调用 ≈ 6 s |
| **I-20d 内核逐步迹补量** | 10 ms 输出所需而今天**只在调用末有**的量补成逐步迹：W_th · ⟨n_e⟩ / n_e0 · β_p · l_i(3) · I_bs · I_CD · P_fus（今天逐步迹有 t · T_e0 · T_i0 · dt · P_rad · P_line · P_α · β_N · T_ped · 能量账 · P_ohm · P_aux · ω_axis · V_loop · Ip 控制器三量 · 锯齿三量 · L-H 相位 / P_sep / P_LH）；opt-in，缺省记录逐位不动 | — | 新迹在 b 的窗内逐 10 ms 与 10 ms 定步长逐调用记录一致（≤ 1e-12 相对，同一步序列） |
| **I-20e C · 在线闭环砍单次调用开销** | PCS 每 10 ms 送新指令、不能跨步预知 ⇒ 只能压单次调用：① 追踪梯子按平衡输入指纹缓存（省 ≈ 127 ms）；② 种类装配 · EPED · 打包 / 解包在输入不变时复用；③ 终态：常驻 `CoreMarch`（会话内保持推进对象，调用只交指令增量，PCS-W1 原设计） | a | 单次调用开销 **≤ 50 ms**（现 295 ms）；答案逐位同未缓存路径 |
| **I-20f 推测执行（可选）** | 在线模式按当前指令先走 K 步（B 的自适应步长）、快照状态；下一拍指令与假设差在阈内则沿用，否则回滚快照重走 | c · e | 同 PCS-V15 精度；最坏情况不慢于 e |

**风险与须先量的**〔推测，均待对应分项实测〕：① L-H 滞回与台基开合对步长敏感（52 ms 步可能错过或推迟翻转）——c 的事件重走即为此设；② Ip 控制器积分项与 V_loop 在大步长下的稳定性（I-3 的 k_p / τ_c 在 10 ms 步上定的参数）；③ 加料弹丸是离散事件、密度账在大步长下的守恒（I-9 粒子账须仍 ≤ 1e-12）；④ 爬升段交换上限低到 ≈ 0.3 ms 时 B 的收益主要来自少调用而非大步长；⑤ IPB98 定标锚（τ_c 1 s）的积分在窗边界的交接（会话已补交接，窗内由内核自己带）。

**负面清单**：不改 PCS 对外 10 ms 接口与字段；不在窗内偷偷改指令（窗切点必须覆盖所有指令变化）；B 的插值输出必须标 `interpolated`，不冒充逐步解。

### I-21 细化：多步计算的快照 · 暂停 · 恢复（2026-09-14，先以时间演化为例）

**范围**：先做时间演化（`code/evolve` 经会话 / 回放驱动的多步推进）；同一套机制随后推广到其余多步计算（`code/discharge` 退火逐遍、平衡节拍序列、I-15 沿回放逐节点、0-D 多遍）——推广不在本条的判据内。

**1 快照是什么**（自足、可审计、可直接续跑）：

| 部分 | 内容 | 说明 |
| :--- | :--- | :--- |
| 续跑文档 | 一份**可直接交给 `code/evolve` 的续跑工况**：`settings`（工况设置 + 该时刻的指令映射 + 内核声明的全部续跑标量：`t_start` · `dt_start` · 边界 T · 锯齿计时 · Ip 控制器三量 · `capped_in` · `dt_fraction_in` · IPB98 定标四参考与积分器 · L-H 相位 …）+ `inputs`（`core_profiles` 状态 te · ti · ne · 离子密度 · ψ 与全部滞后量 `fylite:psi_prev` · `sigma_prev` · `exch_prev` · `dn_prev` · `vn_prev` · `zeff`） | 不变的大块输入（平衡文档 · 沉积表 · 给定 χ · 装置卡）**按内容指纹引用**、只存一份；快照本体只含随时间变的部分 |
| 驱动游标 | 时间片号 · 步号 `k` · 模拟时间 · 回放指令游标（下一节点序号）· 待发事件表（弹丸 · 节拍）· 会话计数（步数 · 拒绝数 · 调用数 · 墙钟分布） | 恢复后驱动从这里接着发指令，不重发、不漏发 |
| 出处 | 快照号 · 父快照号 · 触发原因（周期 / 节点前 / 事件回溯 / 暂停 / 崩溃前）· 工况文档指纹 · 内核 ABI 与接口摘要 · 运行时版本 · 生成时刻 | 恢复前核对：内核 / 接口摘要不符即拒绝恢复并按名说出 |

格式：JSON（与 `fylite.io.fydoc.complete` 收的工况同构）+ 旁挂清单（sha256）；可选同时导出 IMAS `core_profiles` 时间片供外部读，但**续跑只认 fylite 续跑文档**（`fylite:*` 滞后量不在 DD 里）。

**2 时间片与快照点**：
- 时间片 = 驱动的推进单位（缺省 1 s 模拟时间；与 I-20 的窗对齐——**片界 = 窗界 = 快照点 = 暂停点**）。
- **周期快照**：每个片界一份，入滚动环形缓冲（缺省保留最近 K = 5 份），过期即删。
- **节点前快照（可预知的状态改变）**：驱动事先从回放与事件表知道的节点——回放节点（Ip 斜坡起止 · 功率阶跃 · 位形切换 limiter → diverted）、平衡节拍更新、弹丸注入、会话指令跳变——**在节点所在步之前**强制切片并留快照，**永久保留**（标 `before:<节点名>`）。
- **事件回溯快照（不可预知的状态改变）**：L-H / H-L 翻转（逐步迹 `lh_phase` 变号）、锯齿崩塌（`saw_mixed`）、塌陷判据（W_th 或 T_e0 单片相对降幅超阈）、内核拒绝、线圈贴限（节拍上）——片内检出后，把**该片起点的周期快照**（即事件前最近的一份）从环形缓冲**提升为永久保留**（标 `before:<事件名>@t`）；可选「接近判据」预留：P_sep / P_LH 进入 [0.9, 1.1] 带时加密片长，使事件前快照离事件更近。
- **崩溃前快照**：驱动捕获异常时先把当前片起点快照标 `before:crash` 并写出异常全文，再退出。

**3 暂停 · 恢复 · 崩溃恢复**：
- **暂停**：会话 / 驱动收暂停请求（JSON-RPC `session.pause`、或运行目录下的暂停标志文件），**在下一个片界**写快照、置状态 `paused` 后返回；不在片中途打断（片内是一次内核调用，原子）。
- **恢复**：`session.resume(snapshot)`——读快照、核对出处（接口摘要 · 内核 ABI）、重建会话（续跑文档 + 驱动游标 + 计数），从游标处继续；可在另一台机器 / 另一进程恢复。
- **分叉**：自任一保留快照以改动后的设置恢复（例：事件前快照 + 更小片长 / 另一 L-H 滞回系数），新运行记父快照号，形成可追溯的分支树。
- **崩溃恢复**：自 `before:crash` 或最近的事件前快照重走，而不是从头；今天的 I-10 下降段崩溃即是首个用例（6172.99 s）。

**4 定序与分项**：

| 分项 | 做什么 | 依赖 | 判据 |
| :--- | :--- | :--- | :--- |
| **I-21a 续跑集合由内核声明** | 内核在 `code/evolve` 记录里交出「续跑块」：全部续跑标量对（出名 → 入名）与全部滞后量槽名，**由内核生成**；宿主的 `_CARRY` / `_PROFILE_LAGS` 改为读它 | — | 判据：记录的续跑块 ⊇ 内核续跑判据 `a_resumed_march_is_the_same_march_under_the_pcs_opt_ins` 用到的全部量；宿主不再手写交接表 |
| **I-21b 快照格式与往返** | 快照 = 续跑文档 + 游标 + 出处；不变输入按指纹引用；写 / 读 / 核对；公开仓 `engine/checkpoint.py` | a | 往返逐位：快照写 → 读 → 续跑一步，与不中断的下一步逐位相同 |
| **I-21c 时间片驱动** | 回放 / 会话驱动按片推进；周期快照 + 环形缓冲；节点前强制切片；事件回溯提升；崩溃前快照 | b | PCS-V16 的恢复等价；I-10 回放上节点前快照齐全（每个回放节点一份） |
| **I-21d 暂停 / 恢复 / 分叉接口** | `session.pause` / `resume` / `fork`（JSON-RPC 与 Python）；暂停标志文件；恢复时出处核对 | c | 片界暂停 → 恢复与不中断逐位同；接口摘要不符时按名拒绝 |
| **I-21e 与 I-20 合流** | 片 = I-20 的窗；I-20c 的事件重走与 I-20f 的推测执行直接用片起点快照回滚 | c · I-20b | I-20 的 PCS-V15 在快照机制下仍成立 |
| **I-21f 推广到其余多步计算**（后续，不在本条判据内） | `code/discharge` 退火每遍快照（通道安匝 · ψ · C4 状态 `c4_*_out`）· 平衡节拍序列 · 0-D 多遍 | d | 各自的「恢复 ≡ 不中断」 |

**5 风险与须先量的**〔推测，待分项实测〕：① 快照体积——状态本身每份约数十 KB，但工况文档含平衡与表格 ~330 KB，必须按指纹去重；② 片界选择影响 I-20 的自适应步长序列——片界处步长被截断会改变后续步长，**「恢复 ≡ 不中断」的逐位判据只对同一片切分成立**，跨切分比较退为 ≤ 1e-14 相对；③ 内核内部的随机数（`neutrals_mc` 等）若进推进，种子与状态须入快照；④ 暂停请求的响应延迟 = 一个片长（缺省 1 s 模拟 ≈ 平顶一次调用 ~0.3 s 墙钟），在线 PCS 模式片长须 = 10 ms。

**负面清单**：不在内核调用中途暂停；不以 IMAS 文件代替续跑文档恢复（缺 `fylite:*` 滞后量）；恢复不静默跨内核版本（出处不符即拒）；快照不替代台账——保留快照的触发原因与时刻写进运行汇总。

〔9/30 之后，不立号〕QLKNN_7_11 闭包进推进 · EPED1-NN 台基随态更新 · 弹丸消融模型 · 击穿 / 烧穿 · CFEDR d2026（20 MA）·
（原「若 I-11 ① 答『无』则堆芯模拟承担磁位形模型」一项已由改方案取代，见 I-14..I-19）。

〔本节的负面清单〕规划页 §8：**不在步内**跑 TGLF 与射线追踪，平衡不每步解；**不写**轮廓外钉 Dirichlet 的定形边界；
**不移植** EPED1（协议禁止）；**不借** CFETR 或 d2026 补 d2025 的值；**不建模**击穿雪崩与烧穿。

---

## 记法

- 条目**只登记事实与依据**，不写结论；带〔推测〕的判断保留标记。
- 归属为他仓者，本表**不得**作为在本仓改动的依据。
- 关闭一条时写明**关闭日期与依据**（如 E-1：2026-09-06，逐片 `magnetics/ip` 实测）。
